name: CI

# Red de seguridad de valle-design (modelada sobre el ci.yml del monorepo
# origen, recortada a lo que EXISTE en este repo):
#   quality-gates → contrato + licencias + build turbo + typecheck + unit +
#                   Postgres real + lint + specs web + smoke de bootstrap
#   e2e           → full-stack real: next build (prod) + API NestJS real +
#                   next start + Playwright (goldens moqueados en la frontera
#                   de red — así operan en el origen — y e2e/real SIN mocks
#                   contra la API v1 real, incl. hidratación >1MB)
#   gitleaks      → escaneo de secretos del historial completo (.gitleaks.toml)
#   sbom          → CycloneDX + gate de licencias (evidencia de cadena de
#                   suministro; el gate de licencias también corre en
#                   quality-gates porque ahí bloquea el merge)

on:
  pull_request:
    branches: [main]
    # Un cambio que sólo toca prosa no puede romper un gate: no hay contrato,
    # ni tipo, ni spec, ni byte de SDK que dependa de un `.md`. Correr el
    # pipeline entero sobre documentación costó ~150 minutos de runner el 10 de
    # agosto (PR #63, un único archivo Markdown, dos corridas completas) y fue
    # parte de por qué la cuota mensual se agotó a mitad de ciclo.
    #
    # `paths-ignore` NO se aplica al push sobre `main`: ahí sí queremos
    # veredicto de cada commit, cueste lo que cueste, porque es la rama de la
    # que se despliega.
    paths-ignore:
      - "**/*.md"
      - "docs/**"
      - "LICENSE"
      - "NOTICE"
  push:
    branches: [main]
  # Alarma sobre `main` aunque nadie empuje: un fallo intermitente que sólo
  # aparece 1 de cada N corridas no se ve en una foto, se ve en la serie.
  #
  # 2026-08-12: cron SUSPENDIDO — cada corrida diaria costaba 60-90 min de
  # runner y la cuota mensual está a punto de agotarse. La serie se puede
  # producir bajo demanda con workflow_dispatch. Restaurar como semanal
  # (`17 5 * * 1`) cuando la cuota renueve.
  # schedule:
  #   - cron: "17 5 * * *"
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  # SÓLO se cancelan las corridas de PR, donde el push siguiente sustituye al
  # anterior sobre el MISMO código propuesto.
  #
  # En `main` cancelar era activamente dañino y es la razón de que este repo
  # se quedara rojo sin que nadie lo viera: todos los merges comparten el ref
  # `refs/heads/main`, así que cada fusión mataba la corrida de la fusión
  # anterior. En la serie del 9 de agosto, CINCO commits consecutivos de `main`
  # (792c0603, 99e867d3, 9abe5287, ccffc17c, bfbd26b9) terminaron en
  # «cancelled»: nunca hubo veredicto sobre ellos. Cada commit de `main`
  # merece el suyo, aunque llegue tarde.
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  quality-gates:
    name: Contrato · Build · Test · Lint · Smoke
    runs-on: ubuntu-latest
    # Sin techo, un cuelgue hereda el default de GitHub (360 min) y quema seis
    # horas de cuota sin producir veredicto. El techo es holgura sobre lo
    # medido (~35 min en main), no una aspiración.
    timeout-minutes: ${{ github.event_name == 'pull_request' && 45 || 75 }}

    # Postgres efímero: las suites *.pg.spec.ts y el smoke de bootstrap deben
    # correr contra Postgres real (tipos jsonb/enum), no SQLite.
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: valle_design_ci
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      # La versión de Node sale de .nvmrc (única fuente de verdad).
      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version-file: .nvmrc
          cache: npm

      # `cache: npm` de arriba sólo guarda el TARBALL descargado: `npm ci`
      # sigue borrando y reconstruyendo `node_modules` entero, unos dos minutos
      # en cada job y en cada corrida. Cachear el árbol ya instalado, con la
      # clave atada al hash del lockfile, lo deja en segundos.
      #
      # La reproducibilidad no se pierde: si el lockfile cambia un solo byte la
      # clave cambia y se reinstala desde cero con `npm ci`. Un acierto de
      # caché significa, por construcción, el mismo lockfile.
      - name: Restaurar node_modules
        id: deps
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: |
            node_modules
            apps/*/node_modules
            packages/*/node_modules
          key: deps-${{ runner.os }}-${{ hashFiles('package-lock.json') }}

      - name: Install dependencies (npm ci reproducible)
        if: steps.deps.outputs.cache-hit != 'true'
        run: npm ci

      # Vulnerabilidades conocidas en dependencias de PRODUCCIÓN. Sólo bloquea
      # HIGH/CRITICAL: una MODERATE avisa en el log pero no tumba el merge —
      # el umbral es deliberado para que el gate no se vuelva ruido que alguien
      # termine ignorando. Trabaja sobre el lockfile + registro, así que corre
      # igual con caché de node_modules que sin ella.
      - name: Production dependency audit (high+)
        run: npm audit --omit=dev --audit-level=high

      # Puerta de CONTRATO: los YAML de packages/contracts/specs validan
      # (el SDK se genera de ahí; un spec roto es un SDK roto). La CLI viene
      # del lockfile (devDependencies raíz): un gate bloqueante no puede
      # depender de que npm sirva una descarga suelta en cada corrida.
      - name: Contract lint (Redocly)
        run: npx redocly lint packages/contracts/specs/design-api.v1.yaml

      # check:cad (vía check:dwg-evidence) y check:dwg comparan contra
      # docs/cad/evidence/dwg-*.json, que se generó en local CON el corpus
      # hermano montado. Sin este paso, CI recalcula "cero bundles" en frío
      # y el deepStrictEqual contra la evidencia comiteada revienta — no es
      # una regresión de código, es que CI nunca vio el corpus. El repo
      # hermano es público hoy, así que el checkout no necesita credencial;
      # se fija al commit exacto del pin para no depender de su HEAD.
      - name: Leer el pin del corpus DWG
        id: dwg-corpus-pin
        run: echo "commit=$(node -p "require('./scripts/dwg/corpus-pin.json').commit")" >> "$GITHUB_OUTPUT"

      - name: Checkout del espejo del corpus DWG (repo hermano, sólo lectura)
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          repository: Sergiovalle3121/valle-design-dwg-conformance
          ref: ${{ steps.dwg-corpus-pin.outputs.commit }}
          path: .dwg-corpus-mirror

      - name: Exportar VALLE_DWG_CORPUS_MIRROR
        run: echo "VALLE_DWG_CORPUS_MIRROR=$GITHUB_WORKSPACE/.dwg-corpus-mirror" >> "$GITHUB_ENV"

      - name: CAD contract and legacy-route gates
        run: npm run check:cad

      - name: DWG experimental laboratory gates
        run: npm run check:dwg

      # Puerta de LICENCIAS (misma política del origen): el producto es
      # UNLICENSED; toda dependencia de producción debe ser permisiva.
      - name: Proprietary governance gate
        run: npm run check:governance

      - name: Dependency licenses gate
        run: npm run sbom && npm run check:licenses && npm run sbom:full

      # El SBOM ya está generado por el paso de arriba, que es el que BLOQUEA.
      # Antes existía un job `sbom` aparte que repetía checkout + setup + `npm
      # ci` + `npm run sbom` sólo para subir este mismo archivo: ~2,5 minutos de
      # runner por corrida produciendo un artefacto idéntico al que aquí ya
      # está en disco. La evidencia de cadena de suministro no cambia; lo que
      # desaparece es pagarla dos veces.
      - name: Upload SBOM artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: valle-design-sbom-${{ github.sha }}
          path: |
            sbom.cdx.json
            sbom.full.cdx.json
          if-no-files-found: error
          retention-days: 90

      # Build turbo del monorepo: contracts → design-sdk → api → web.
      - name: Build (turbo)
        run: npx turbo run build

      # Presupuesto de bundle por ruta. Va AQUÍ y no en el job de E2E porque
      # necesita exactamente lo que este job acaba de producir —el `.next` de
      # producción— y porque un exceso de peso debe verse en minutos, no al
      # final de los ~40 que tarda la suite de navegador. El script levanta su
      # propio `next start` en un puerto suyo y lo mata al terminar.
      - name: Presupuesto de bundle por ruta (bloqueante)
        run: node scripts/perf/bundle-budget.mjs

      # Lighthouse sobre el MISMO build: no mide bytes sino tiempo hasta que la
      # página es útil, que es lo que el peso no ve — una fuente que bloquea el
      # pintado o un script que ocupa el hilo principal salen bien en KB y mal
      # en pantalla. Dos pasadas, escritorio y móvil, tres corridas por ruta.
      # El umbral de RENDIMIENTO va en AVISO en las dos pasadas hasta medirlo
      # en este runner: en el contenedor de desarrollo da 93-95, pero ese número
      # no se traslada a una máquina más pequeña y compartida, y un gate que
      # falla por la carga del runner enseña a ignorar los rojos. La primera
      # corrida aquí produce esa medida (P1-FE1). La ACCESIBILIDAD sí bloquea:
      # no depende de lo rápida que sea la máquina.
      - name: Lighthouse (accesibilidad bloqueante; rendimiento en aviso hasta calibrar)
        run: npm run check:lighthouse

      # Los informes, SIEMPRE, pase o falle el paso de arriba. Sin esto las
      # puntuaciones del runner sólo existen dentro del log del job, que se
      # trunca antes de llegar a ellas: el gate corre y nadie puede leer lo que
      # midió. Es exactamente el dato que le falta a P1-FE1 para calibrar el
      # umbral de rendimiento, que hoy está en aviso por no tenerlo.
      #
      # `if-no-files-found: error`, y no `warn`, porque la primera versión de
      # este paso subió CERO ficheros y terminó en verde en un segundo: el gate
      # medía y nadie podía leerlo, que es el fallo exacto que este paso existe
      # para evitar. Un aviso en un log truncado no es una señal.
      - name: Subir informes de Lighthouse
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: lighthouse-${{ github.sha }}
          path: |
            .lighthouseci-escritorio/
            .lighthouseci-movil/
            .lighthouseci-resumen.json
          if-no-files-found: error
          retention-days: 14

      - name: Typecheck (API)
        working-directory: apps/api
        run: npm run typecheck

      - name: Typecheck (web, incluye specs y e2e)
        working-directory: apps/web
        run: npm run typecheck

      - name: Unit tests (API)
        working-directory: apps/api
        run: npm test

      # Suites *.pg.spec.ts contra PostgreSQL REAL (REQUIRE_POSTGRES_TESTS:
      # sin base es FALLO, no salto silencioso).
      - name: Postgres tests (API)
        working-directory: apps/api
        env:
          TEST_DATABASE_URL: postgres://postgres:postgres@localhost:5432/valle_design_ci
        run: npm run test:pg

      - name: Lint API (bloqueante, sin --fix)
        working-directory: apps/api
        run: npm run lint:check

      # ESLint necesita montón EXPLÍCITO, y no es un capricho: sobre `main`
      # (e41f12d) este paso murió con `FATAL ERROR: Ineffective mark-compacts
      # near heap limit — JavaScript heap out of memory`, exit 134, tras 91 s
      # de GC dando vueltas. No falló ninguna regla: el linter se cayó.
      #
      # El límite por defecto de Node en el runner son ~2 GB, y medido sobre
      # este árbol eslint necesita entre 1,5 y 2: revienta con 1024, 1280 y
      # 1536 MB, y pasa con 2048. Es decir, el gate venía corriendo con margen
      # CERO, y por eso el veredicto dependía de la suerte — #69 pasó su lint,
      # este PR pasó el suyo, y la suma se cayó. Un linter que se muere no
      # dice «hay un error»: deja de decir nada, que es peor.
      #
      # 4 GB da el doble de lo que hace falta en un runner de 16. Esto es un
      # tope, no una cura: el coste viene del linteo con tipos sobre un
      # monolito de 23k líneas (`Layout3DEditor.tsx`), y quien reduzca ese
      # monolito recuperará también este margen.
      - name: Lint web
        working-directory: apps/web
        env:
          NODE_OPTIONS: --max-old-space-size=4096
        run: npm run lint

      # Suites script-style del kernel CAD y la plataforma web (tsx).
      - name: Web specs (tsx)
        working-directory: apps/web
        run: npm run test:specs

      - name: CAD deterministic corpus benchmark (10k + 100k, blocking)
        working-directory: apps/web
        run: npm run benchmark:cad:smoke -- --output ../../cad-benchmark-smoke.json

      # Presupuesto de TIEMPO del pipeline de render, contra la línea base
      # versionada de `render-baseline.json`.
      #
      # Vive AQUÍ y no en «Web specs (tsx)» por lo que demostró #65: el ruido de
      # planificación es absoluto y el coste de este pipeline es pequeño, así
      # que un hipo de 5 ms DUPLICA un número de 6 ms. Dentro del runner de
      # specs —250 specs seguidos, máquina nunca tranquila— eso es un
      # intermitente en la puerta de todo el mundo. Como paso propio, en serie y
      # en su propio proceso, las condiciones son las mismas que las del
      # benchmark de corpus de arriba, que lleva bloqueando desde siempre sin
      # parpadear.
      #
      # El spec `render-gate.spec.ts` sigue en «Web specs», pero sólo con las
      # INVARIANTES —recuentos de entidades, no milisegundos—, que es lo que
      # caza el regreso del muestreo y no puede parpadear.
      # Mismo criterio que la suite de rendimiento del E2E: esto MIDE, no
      # comprueba corrección, y `--repeat 3` son tres pasadas de 100k. Su sitio
      # es `main`, el cron y el disparo manual, que es donde el número tiene
      # significado; en una rama sólo encarece cada push.
      - name: CAD render pipeline budget (100k, blocking)
        if: github.event_name != 'pull_request'
        working-directory: apps/web
        run: npm run benchmark:cad:render -- --repeat 3 --output ../../cad-benchmark-render.json

      - name: DWG deterministic smoke benchmark
        run: npm run benchmark:smoke --workspace=@valle-design/dwg-codec -- --output "$RUNNER_TEMP/dwg-benchmark-smoke.json"

      - name: Upload DWG benchmark evidence
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: dwg-benchmark-${{ github.sha }}
          path: ${{ runner.temp }}/dwg-benchmark-smoke.json
          if-no-files-found: error
          retention-days: 90

      # Un INFORME de capacidad a 500k y 1M de entidades. No bloquea nada y no
      # cambia entre un push de rama y el siguiente: es un retrato del motor,
      # no una prueba del cambio propuesto.
      - name: CAD scale capability report (500k + 1M, hardware guarded)
        if: github.event_name != 'pull_request'
        working-directory: apps/web
        run: npm run benchmark:cad:scale -- --output ../../cad-benchmark-scale.json

      - name: Upload CAD benchmark evidence
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: cad-benchmark-${{ github.sha }}
          path: |
            cad-benchmark-smoke.json
            cad-benchmark-scale.json
            cad-benchmark-render.json
          retention-days: 90

      # Test de compatibilidad de contrato del SDK generado vs @valle-design/contracts.
      - name: SDK contract-compat test
        working-directory: packages/design-sdk
        run: npm test

      # El mapa de evidencia del viaje de aceptación CAD (50 pasos) debe
      # apuntar a specs que EXISTEN (mismo checker del origen).
      - name: CAD acceptance journey check
        working-directory: apps/web
        run: npx tsx e2e/golden/cad-acceptance-journey.check.ts

      # Puerta crítica: arranca el grafo DI completo del dist compilado contra
      # Postgres (migraciones + esquema materializado).
      - name: Bootstrap smoke (compilado vs Postgres)
        working-directory: apps/api
        env:
          NODE_ENV: test
          DATABASE_URL: postgres://postgres:postgres@localhost:5432/valle_design_ci
          SYNCHRONIZE: "false"
          MIGRATIONS_RUN: "true"
          IDENTITY_RATE_LIMIT_KEY_SECRET: valle-design-ci-rate-limit-key-32chars
        run: npm run smoke:bootstrap

      # El `dist` compilado viaja al job `deploy-readiness` en vez de
      # reconstruirse allí. Reconstruirlo costaría otro `npm ci` y otro build
      # completos (~4 min de runner por corrida) para producir bytes idénticos
      # a los que aquí ya están en disco, y la cuota de Actions de este repo
      # está agotándose. Retención de un día: es un insumo entre jobs de la
      # MISMA corrida, no evidencia que haya que conservar.
      - name: Upload API dist (insumo de deploy-readiness)
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: api-dist-${{ github.sha }}
          path: |
            apps/api/dist
            packages/contracts/dist
            packages/design-sdk/dist
          if-no-files-found: error
          retention-days: 1

  # ───────────────────────────────────────────────────────────────────────────
  # ¿Se puede DESPLEGAR esto?
  #
  # Los gates de arriba responden «¿el código es correcto?». Este responde otra
  # pregunta, y hasta ahora nadie la hacía: «¿la imagen que se va a construir
  # es reproducible, y el binario se comporta como un servicio operable?».
  #
  # Un repo verde que no se sabe desplegar, vigilar ni apagar no es un producto
  # vendible, y los dos fallos que este job caza no los ve ninguna spec:
  # una imagen que corre como root o sin healthcheck, y un proceso que ante
  # SIGTERM corta las conexiones en vuelo en lugar de drenarlas.
  #
  # Coste: sin `npm ci` (usa la caché del lockfile) y sin build (descarga el
  # `dist` del job anterior). Es del orden de un minuto de runner.
  # ───────────────────────────────────────────────────────────────────────────
  deploy-readiness:
    name: Despliegue · Imagen reproducible + arranque productivo
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: quality-gates

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: valle_design_deploy
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version-file: .nvmrc
          cache: npm

      # Invariantes del Dockerfile: no necesita dependencias ni base de datos,
      # así que corre ANTES que nada y falla en segundos si algo se rompió.
      - name: Gate de imagen reproducible
        run: node scripts/deploy/validate-dockerfiles.mjs

      - name: Restaurar node_modules
        id: deps
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: |
            node_modules
            apps/*/node_modules
            packages/*/node_modules
          key: deps-${{ runner.os }}-${{ hashFiles('package-lock.json') }}

      - name: Install dependencies
        if: steps.deps.outputs.cache-hit != 'true'
        run: npm ci

      - name: Descargar el dist compilado por quality-gates
        uses: actions/download-artifact@v4
        with:
          name: api-dist-${{ github.sha }}
          path: .

      # Arranca el proceso real: falla cerrada ante configuración productiva
      # incompleta, probes, /metrics protegido y drenaje con SIGTERM.
      - name: Smoke de arranque productivo
        env:
          DATABASE_URL: postgres://postgres:postgres@localhost:5432/valle_design_deploy
        run: node scripts/deploy/production-startup-smoke.mjs

  e2e:
    name: E2E Playwright (PostgreSQL · Chromium + Firefox)
    runs-on: ubuntu-latest
    # Techo sobre lo medido (~48 min en main con Firefox+perf); ver nota en
    # quality-gates. Y no arrancar 30+ min de Playwright si un gate barato ya
    # falló: needs no añade minutos en verde y los ahorra todos en rojo.
    timeout-minutes: ${{ github.event_name == 'pull_request' && 50 || 100 }}
    needs: quality-gates

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: valle_design_e2e
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Restaurar node_modules
        id: deps
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: |
            node_modules
            apps/*/node_modules
            packages/*/node_modules
          key: deps-${{ runner.os }}-${{ hashFiles('package-lock.json') }}

      - name: Install dependencies
        if: steps.deps.outputs.cache-hit != 'true'
        run: npm ci

      # `dwg-import-real.spec.ts` (e2e/real) exige VALLE_DWG_CORPUS_MIRROR
      # apuntando a un clon local del repo hermano — sin esto se auto-salta
      # con su propio test.skip, silenciosamente, en todo run. Mismo patrón
      # (pin exacto, checkout de sólo lectura) que `quality-gates` ya usa
      # para `check:dwg-evidence`; este job es OTRO runner y no hereda su
      # checkout ni su VALLE_DWG_CORPUS_MIRROR.
      - name: Leer el pin del corpus DWG
        id: dwg-corpus-pin
        run: echo "commit=$(node -p "require('./scripts/dwg/corpus-pin.json').commit")" >> "$GITHUB_OUTPUT"

      - name: Checkout del espejo del corpus DWG (repo hermano, sólo lectura)
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          repository: Sergiovalle3121/valle-design-dwg-conformance
          ref: ${{ steps.dwg-corpus-pin.outputs.commit }}
          path: .dwg-corpus-mirror

      - name: Exportar VALLE_DWG_CORPUS_MIRROR
        run: echo "VALLE_DWG_CORPUS_MIRROR=$GITHUB_WORKSPACE/.dwg-corpus-mirror" >> "$GITHUB_ENV"

      # Los navegadores pesan cientos de megas y no cambian entre corridas: la
      # clave los ata a la versión de Playwright del lockfile.
      - name: Restaurar navegadores de Playwright
        id: browsercache
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: ~/.cache/ms-playwright
          key: pw-${{ runner.os }}-${{ hashFiles('package-lock.json') }}-${{ github.event_name == 'pull_request' && 'chromium' || 'both' }}

      # Cobertura ESCALONADA por navegador, y la escala está elegida a partir
      # de los fallos reales de este repo, no por ahorrar sin más:
      #
      #   PR      → Chromium. Los tres intermitentes que costaron un día entero
      #             (goldens 18, 33 y 20) se reprodujeron en Chromium; el
      #             navegador no era la variable.
      #   main    → Chromium + Firefox. Aquí sí importa: Firefox necesita
      #             llvmpipe por X11 y es donde aparecieron dos de los tres
      #             rojos del 9 de agosto. Un commit de `main` sin veredicto de
      #             Firefox no está verificado.
      #
      # Firefox DUPLICA el job (~30 min de runner por corrida). Correrlo en
      # cada push de rama es pagar dos veces por la señal que ya da el otro.
      - name: Seleccionar navegadores
        id: browsers
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "list=chromium" >> "$GITHUB_OUTPUT"
            echo "projects=--project=chromium" >> "$GITHUB_OUTPUT"
          else
            echo "list=chromium firefox" >> "$GITHUB_OUTPUT"
            echo "projects=" >> "$GITHUB_OUTPUT"
          fi

      - name: Install Playwright browsers
        working-directory: apps/web
        run: npx playwright install --with-deps ${{ steps.browsers.outputs.list }}

      # WebGL por software para Firefox. El runner no tiene GPU: Chromium cae
      # solo en SwiftShader, pero Firefox agota los drivers
      # (FEATURE_FAILURE_WEBGL_EXHAUSTED_DRIVERS) y se queda SIN viewport, así
      # que las pruebas que dependen de la escena real no medían el producto.
      # La alternativa —excluir Firefox o saltarse esos specs— escondería el
      # problema; darle a Mesa su driver software lo resuelve de verdad.
      # Se instala SIEMPRE, también en los PR que sólo corren Chromium. Hacerlo
      # condicional ahorraba ~15 s y a cambio cambiaba el ENTORNO DE EJECUCIÓN
      # de las pruebas: sin el paquete no hay `xvfb-run`, así que Chromium
      # pasaba a correr sin display donde en `main` corre con uno. Quince
      # segundos no valen introducir una variable en el único gate que mide el
      # producto de punta a punta.
      - name: Install Mesa software OpenGL (Firefox WebGL)
        run: |
          sudo apt-get update
          # `eglinfo` en este runner demuestra que llvmpipe está disponible
          # (OpenGL 4.5, Mesa) pero SÓLO en la plataforma surfaceless: GBM,
          # Wayland y X11 fallan `eglInitialize` por no haber servidor gráfico.
          # De ahí Xvfb: con un display real Firefox usa llvmpipe por X11.
          sudo apt-get install -y libgl1-mesa-dri libglx-mesa0 libegl1 libegl-mesa0 libgbm1 mesa-utils xvfb
          echo "Renderer disponible:"; (glxinfo -B 2>/dev/null || echo 'glxinfo no disponible')
          (eglinfo -B 2>/dev/null | head -24 || echo 'eglinfo no disponible')

      # El build de producción INLINEA NEXT_PUBLIC_API_URL: debe apuntar al
      # origen de la API real que este job arranca (localhost:4000). Los
      # goldens interceptan ese origen en la frontera de red (herméticos);
      # e2e/real NO intercepta nada.
      #
      # NEXT_PUBLIC_DWG_NATIVE_IMPORT_BETA también se INLINEA aquí, no en el
      # paso de Playwright: sin ella, `dwg-import-real.spec.ts` se auto-salta
      # con su propio test.skip pase lo que pase después — faltaba en este
      # job y la beta de import DWG nunca se había ejercitado de verdad en
      # CI, en ningún navegador (hallazgo verificado corriendo el spec real
      # contra este mismo stack fuera de CI, no asumido).
      - name: Build (turbo, web en modo E2E real)
        env:
          NEXT_PUBLIC_API_URL: http://localhost:4000
          NEXT_PUBLIC_DWG_NATIVE_IMPORT_BETA: "true"
          # Se incrusta al compilar y viaja en cada reporte de «algo salió mal».
          # En CI vale el SHA: si un reporte llegara desde una ejecución, diría
          # exactamente contra qué código se produjo.
          NEXT_PUBLIC_APP_VERSION: ci-${{ github.sha }}
        run: npx turbo run build

      # API real (R1) contra PostgreSQL 16: mismas migraciones y rutas
      # v1, mismo dominio CAD + blob store + hidratación que producción.
      - name: Start API (NestJS real, PostgreSQL 16 + migrations)
        working-directory: apps/api
        run: |
          nohup node dist/main.js > /tmp/design-api.log 2>&1 &
          for i in $(seq 1 60); do
            if curl -sf http://localhost:4000/health > /dev/null; then
              echo "API arriba tras ${i}s"; exit 0
            fi
            sleep 1
          done
          echo "La API no arrancó; log:"; cat /tmp/design-api.log; exit 1
        env:
          PORT: "4000"
          NODE_ENV: test
          DATABASE_URL: postgres://postgres:postgres@localhost:5432/valle_design_e2e
          SYNCHRONIZE: "false"
          MIGRATIONS_RUN: "true"
          ALLOWED_ORIGIN: http://localhost:3000
          IDENTITY_TEST_HARNESS: "true"
          IDENTITY_TEST_HARNESS_KEY: valle-design-e2e-harness-key-32-characters-minimum
          IDENTITY_RATE_LIMIT_KEY_SECRET: valle-design-e2e-rate-limit-key-32chars
          # El modo de lanzamiento, tal y como va a salir a producción: 90 días
          # de prueba. La Jornada Real y el embudo leen la duración del catálogo
          # público, así que el CI ejercita la oferta que se anuncia, no otra.
          TRIAL_DAYS: "90"
          # El buzón del botón «algo salió mal». Sin él la API responde 503 —a
          # propósito, para no tragarse reportes— y la prueba de la primera hora
          # lo cazaría, que es exactamente lo que debe pasar si producción se
          # despliega sin configurarlo.
          SUPPORT_EMAIL: soporte@valledesign.test
          AI_MOCK: "1"

      # ── LA JORNADA REAL, PRIMERO Y SOLA ──────────────────────────────────
      #
      # Corre antes que el resto y falla rápido a propósito. Es LA prueba de
      # que el producto está vivo: registro, dibujo, guardado, re-login con los
      # mismos números, DXF verificado por contenido numérico, review link en un
      # segundo navegador y la regla de oro del día 91.
      #
      # Ponerla aquí y no confiar en que la barra la recoja más tarde es una
      # decisión de SEÑAL: la suite completa tarda ~40 minutos, y enterarse a
      # los 40 de que el registro está roto cuesta una corrida entera. Aquí el
      # veredicto llega en menos de un minuto.
      #
      # Sí, se ejecuta dos veces (el barrido de abajo la vuelve a incluir).
      # Cuesta segundos y compra el fallo temprano; el día que eso moleste, se
      # excluye del barrido, no de aquí.
      - name: La Jornada Real (E2E full-stack, sin mocks) — falla rápido
        working-directory: apps/web
        env:
          E2E_PROD: "1"
          E2E_API_ORIGIN: http://localhost:4000
          E2E_REAL_API: "1"
          E2E_IDENTITY_HARNESS_KEY: valle-design-e2e-harness-key-32-characters-minimum
          CAD_PERF_E2E: "0"
        run: npx playwright test e2e/real/jornada-real.spec.ts e2e/real/free-launch-funnel.spec.ts --project=chromium --retries=0

      - name: Playwright E2E (goldens + performance + full-stack real)
        working-directory: apps/web
        env:
          E2E_PROD: "1"
          E2E_API_ORIGIN: http://localhost:4000
          E2E_REAL_API: "1"
          # `dwg-import-real.spec.ts` comprueba esta bandera DIRECTAMENTE en su
          # propio test.skip (proceso de Node de Playwright, no sólo el bundle
          # del navegador) — verificado corriendo el spec real: sin ella aquí
          # TAMBIÉN, además de en el paso de build, los 9 tests se saltan en
          # silencio pase lo que pase arriba.
          NEXT_PUBLIC_DWG_NATIVE_IMPORT_BETA: "true"
          E2E_IDENTITY_HARNESS_KEY: valle-design-e2e-harness-key-32-characters-minimum
          # La suite de rendimiento vive en su PROPIO job (`e2e-perf`, abajo):
          # aquí SIEMPRE apagada. La historia que obligó a separarla:
          # `cad-dense-editing-100k` consumía su techo de una hora, Playwright
          # lo reintentaba (retries=1 en CI) y el job entero moría cancelado a
          # los 100 minutos — TODO lo posterior al test ~99 (incluido el
          # proyecto Firefox completo) llevó del 21 al 26 de agosto sin
          # ejecutarse ni una vez, y `main` sin veredicto. Un job de medida no
          # puede costarle el veredicto al job de corrección.
          CAD_PERF_E2E: "0"
          # Fuerza el rasterizador software de Mesa (llvmpipe) para que Firefox
          # obtenga contexto WebGL sin GPU. Chromium no lo necesita y no le
          # afecta: ya usa su propio SwiftShader.
          LIBGL_ALWAYS_SOFTWARE: "1"
          GALLIUM_DRIVER: llvmpipe
          # Firefox headless obtiene su contexto por EGL sin superficie.
          MOZ_ENABLE_WAYLAND: "0"
        # Bajo Xvfb. `eglinfo` en el runner demuestra que llvmpipe SÍ está
        # disponible (OpenGL 4.5, Mesa) pero SÓLO en la plataforma surfaceless:
        # GBM, Wayland y X11 fallan `eglInitialize` porque no hay servidor
        # gráfico. Firefox no toma esa ruta surfaceless y por eso agota los
        # drivers. Darle un display real es la forma canónica de que use
        # llvmpipe por GLX/EGL-X11. Chromium no se ve afectado.
        # Xvfb sólo cuando hay Firefox; con Chromium solo es un envoltorio
        # inútil y un servidor X de más en el runner.
        # Bajo Xvfb SIEMPRE, con o sin Firefox: lo único que cambia entre un PR
        # y `main` debe ser QUÉ navegadores corren, nunca CÓMO se ejecutan.
        run: xvfb-run -a --server-args="-screen 0 1920x1080x24" npx playwright test ${{ steps.browsers.outputs.projects }}

      # `include-hidden-files` NO es cosmético: sin él este paso nunca subió
      # nada. `.report` y `.test-results` empiezan por punto, y
      # upload-artifact@v4 excluye los ocultos por defecto, así que las tres
      # corridas rojas de `main` del 9 de agosto avisaron
      # «No files were found with the provided path: apps/web/e2e/.report».
      # El informe y las trazas —lo único que explica un fallo intermitente—
      # se descartaban justo cuando hacían falta.
      - name: Upload Playwright report
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: playwright-report-${{ github.sha }}
          path: |
            apps/web/e2e/.report
            apps/web/e2e/.test-results
          include-hidden-files: true
          retention-days: 14

      - name: API log (diagnóstico)
        if: failure()
        run: cat /tmp/design-api.log || true

  # ───────────────────────────────────────────────────────────────────────────
  # La suite de RENDIMIENTO, en su propio carril acotado.
  #
  # Medida ≠ corrección: estos specs (e2e/performance) publican números y
  # invariantes a 100k, y su coste es de decenas de minutos. Dentro del job
  # `e2e` convertían cualquier regresión de rendimiento en un job cancelado a
  # los 100 minutos SIN veredicto para el resto de la suite (así estuvo `main`
  # del 21 al 26 de agosto). Aquí: sólo Chromium (Firefox no añade señal a una
  # medida SwiftShader), sin retries (reintentar una medida de 20 minutos es
  # pagar dos veces por el mismo número) y con techo propio. Sigue BLOQUEANDO
  # el veredicto de `main`: un fallo aquí pone el run en rojo, sólo que ya no
  # arrastra al resto de la suite ni la deja sin ejecutar.
  # ───────────────────────────────────────────────────────────────────────────
  e2e-perf:
    name: E2E rendimiento (100k · Chromium · SwiftShader)
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 75
    needs: quality-gates

    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Restaurar node_modules
        id: deps
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: |
            node_modules
            apps/*/node_modules
            packages/*/node_modules
          key: deps-${{ runner.os }}-${{ hashFiles('package-lock.json') }}

      - name: Install dependencies
        if: steps.deps.outputs.cache-hit != 'true'
        run: npm ci

      - name: Restaurar navegadores de Playwright
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: ~/.cache/ms-playwright
          key: pw-${{ runner.os }}-${{ hashFiles('package-lock.json') }}-perf-chromium

      - name: Install Playwright browsers
        working-directory: apps/web
        run: npx playwright install --with-deps chromium

      # Mismo build E2E que el job `e2e`: los perf specs interceptan la red en
      # la frontera (no necesitan la API real), pero el bundle debe apuntar al
      # mismo origen para que la intercepción case.
      - name: Build (turbo, web en modo E2E real)
        env:
          NEXT_PUBLIC_API_URL: http://localhost:4000
        run: npx turbo run build

      - name: Playwright performance suite (Chromium, sin retries)
        working-directory: apps/web
        env:
          E2E_PROD: "1"
          E2E_API_ORIGIN: http://localhost:4000
          CAD_PERF_E2E: "1"
          # UNA repetición por gesto en CI: con el valor por defecto (3, para
          # medianas) la sola fase geométrica del estrés denso excede su techo
          # de 35 min en un runner sin GPU — medido en local con REPEATS=1
          # rozando el techo. La evidencia con medianas de 3 repeticiones es
          # la corrida manual documentada (scripts/cad/dense-editing-evidence);
          # el papel de este job es el VEREDICTO funcional acotado, y el
          # artefacto publica repeats=1 en vez de disimularlo.
          CAD_DENSE_REPEATS: "1"
        # SÓLO el estrés denso — el veredicto funcional cuya ausencia dejó a
        # main sin ejecutar ~200 tests del 12 al 26 de agosto. Los otros tres
        # specs de e2e/performance (viewport-100k, render-browser,
        # import-fuzzing) suman techos por encima del techo del job y siguen
        # siendo corridas MANUALES documentadas (CAD_PERF_E2E=1 en local);
        # dejarlos fuera se declara aquí en vez de descubrirse en un timeout.
        run: npx playwright test e2e/performance/cad-dense-editing-100k --project=chromium --retries=0

      - name: Upload performance artifacts
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: e2e-perf-${{ github.sha }}
          path: |
            apps/web/e2e/.artifacts
            apps/web/e2e/.report
            apps/web/e2e/.test-results
          include-hidden-files: true
          retention-days: 30

  gitleaks:
    name: Gitleaks (historial completo)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout (historial completo)
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0

      # Binario pineado (sin GITLEAKS_LICENSE): escanea TODO el historial con
      # la config del repo (.gitleaks.toml — allowlist EXACTA del fixture
      # documentado; cualquier hallazgo nuevo rompe el gate).
      - name: Run gitleaks
        run: |
          GITLEAKS_VERSION=8.24.3
          curl -sSfL "https://github.com/gitleaks/gitleaks/releases/download/v${GITLEAKS_VERSION}/gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz" -o /tmp/gitleaks.tgz
          echo "9991e0b2903da4c8f6122b5c3186448b927a5da4deef1fe45271c3793f4ee29c  /tmp/gitleaks.tgz" | sha256sum -c -
          tar -xzf /tmp/gitleaks.tgz -C /tmp gitleaks
          /tmp/gitleaks version
          /tmp/gitleaks detect --source . --config .gitleaks.toml --redact --no-banner

