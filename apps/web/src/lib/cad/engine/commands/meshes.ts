import type { CadAnyCommandDescriptor } from "../command-types";

export const CAD_MESH_COMMANDS: readonly CadAnyCommandDescriptor[] = [
  // MESH y todos los comandos de malla retirados (T18): pendientes de kernel de mallas.
];