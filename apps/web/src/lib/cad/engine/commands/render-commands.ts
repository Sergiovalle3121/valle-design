/**
 * Familia de comandos de RENDER (13 comandos).
 *
 * Stubs retirados en T18B: RENDER, RENDERCROP, RENDERWIN, RENDERPRESETS,
 * RENDEREXPOSURE, RENDERENVIRONMENT, MATERIALS, MATERIALMAP, MATERIALATTACH,
 * POINTLIGHT, SPOTLIGHT, DISTANTLIGHT, SUNPROPERTIES, GEOGRAPHICLOCATION.
 * Volverán cuando el pipeline WebGL esté operativo.
 */
import type { CadAnyCommandDescriptor } from "../command-types";

export const CAD_RENDER_COMMANDS: readonly CadAnyCommandDescriptor[] = [];