/** Catálogo de mensajes en inglés (idioma por defecto). */
import appUpdate from "./en/appUpdate.json";
import language from "./en/language.json";
import offline from "./en/offline.json";

const en = {
  appUpdate,
  language,
  offline,
};

export default en;
