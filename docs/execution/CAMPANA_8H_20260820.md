# CAMPAÑA AUTÓNOMA DE 8 HORAS — 2026-08-20

> **Este archivo es la memoria de la campaña.** Si el contexto se compacta o la sesión
> se reinicia, la primera acción es releer este archivo y continuar donde va la cola.
> Después de CADA ítem: actualizar estado (✅ hecho / 🔴 rojo / ⏸ pendiente) + hora.

**Misión:** dejar el producto LISTO PARA COBRAR Y OPERAR — seguridad de producción,
cobro completo con factura, paridad profesional (cotas y estilos), rendimiento medido
en GPU real y limpieza del legado. Al terminar: main verde, empujado a origin, informe
honesto en `docs/execution/INFORME_CAMPANA_8H_20260820.md`.

**Reglas activas:** no detenerse a preguntar (decisión conservadora + bitácora);
ítem bloqueado >20 min → PENDIENTES y seguir; gates locales completos antes de cada
push (`check:cad && check:dwg && typecheck && test && lint && build`); push a main al
cerrar cada ola; main rojo = prioridad 1; prohibido aflojar umbrales/goldens/tests;
DWG lab fuera del producto (ADR-0007); es-MX en UI; variables nuevas a `.env.example`
+ `docs/guides/environment-variables.md` + `DEPLOYMENT.md`; migraciones con spec en
migration-chain y reversibles documentadas.

**Entorno:** shell = Git Bash (PowerShell roto: error 800705af). Playwright necesita
`PLAYWRIGHT_BROWSERS_PATH=D:\dev\.cache\ms-playwright`. ODA File Converter y gitleaks
en `D:\dev\tools`. Base: commit `abfd8f3`.

---

## COLA DE LA CAMPAÑA

### OLA 0 — Estado y vulnerabilidades — ✅ CERRADA (commit 405ace8, push a origin)
- [x] 0.1 Suite completa verde en local (exit 0; API 639 tests, web specs, lint, build).
- [x] 0.2 nanoid 3.3.18 + dompurify 3.4.14; `npm audit --omit=dev`: 0 vulnerabilidades.
- [x] 0.3 Paso bloqueante de audit en quality-gates + dependabot semanal agrupado.
- [x] 0.4 `check-json-duplicate-keys` (spec 50 checks) encadenado PRIMERO en check:cad.

### OLA 1 — Seguridad de producción — código ✅, pendiente suite completa + push
- [x] 1.1 `TenantIntegrityRls`: NOT NULL en 7 tablas (sf_cad_blocks queda nullable: carril
      de sistema de la biblioteca sembrada); RLS en las 8 con política
      `current_setting('app.tenant_id', true)` (+ lectura de sistema en sf_cad_blocks);
      pre-check que ABORTA sin mutar ante filas NULL; sin FORCE (dueño = app de hoy no
      sujeta; rol runtime no-dueño documentado en DEPLOYMENT.md §3.2). Spec .pg con
      SET ROLE a rol no dueño: sin scope → 0 filas; scope A no ve B; INSERT ajeno 42501;
      carril sistema legible e inviolable; down reversible. 29→30 suites .pg verdes.
- [x] 1.2 /health/metrics/commercial con el bearer de METRICS_TOKEN (404/401 como
      /metrics); RUNBOOK y outbox-replay actualizados; spec de wiring con gemelos.
- [x] 1.3 SSL estricto default en producción (escape DB_SSL_STRICT=false explícito);
      DB_POOL_SIZE=20, DB_STATEMENT_TIMEOUT_MS=30000, DB_IDLE_IN_TRANSACTION_TIMEOUT_MS=30000,
      DB_LOCK_TIMEOUT_MS=10000; valor ilegible ABORTA el arranque. 8 tests.
- [x] 1.4 `ApiRateLimitService` (identity global; claves HMAC opacas + store PG
      compartido): content 120/min/doc, archive 30/min/doc, vision 10/min/cuenta,
      checkout 10/min/org, review-comments 30/min/sesión. Spec unidad + .pg
      (concurrencia: 10 de 30 pasan; tabla sin identificadores en claro).
- [x] 1.5 32 MiB (evidencia document-limits: 100k entidades ≈ 24.7 MB → el techo
      efectivo sigue siendo entidades, con 26% de holgura).
- [x] 1.6 Proyección explícita en listados (sin cadDocument/dxfData/legacyMetadata) y
      búsqueda `q` EN SQL (LOWER LIKE + escape, portable PG/SQLite, dentro del
      TenantScopedRepository): total real, sin tope de 1000, docs viejos alcanzables.
- [x] 1.7 Gate de contrato ampliado a auth/organizations/commercial: 77/77 operaciones
      en biyección (familias fuera del alcance ahora son ERROR); `_development/` excluido
      documentado. migration-chain: candado que deriva la lista del DIRECTORIO (corre
      también sin PG).

### OLA 2 — Cobro completo con factura — código ✅, pendiente suite completa + push
- [x] 2.1 `FacturamaCfdiProvider` tras el puerto (cliente HTTP inyectable, Basic auth,
      POST /2/cfdis, descarga /cfdi/{formato}/issued/{id}, IVA 16% desglosado exacto en
      centavos, PUE, forma 31 default). `CFDI_PAC_NAME=facturama` ya ARRANCA (PAC
      desconocido sigue reventando); exige `CFDI_ISSUER_POSTAL_CODE`. 15 tests de
      contrato con dobles. Verificación sandbox real: PENDIENTE de credenciales del
      dueño (declarado en DEPLOYMENT.md y environment-variables.md — parcial declarado
      parcial).
- [x] 2.2 Tabla `cfdi_receipts` (migración con únicos parciales por invoice y por
      período global; estados CERRADOS por CHECK) + `CfdiIssuanceService` en el tick
      del worker (compuerta horaria, idempotencia POR ESQUEMA, reintentos con techo 5,
      lote 25): pago→nominativo con RFC, pago→pool sin datos, pool→factura global
      mensual XAXX010101000 (día 1, mes anterior, sólo MXN, una por período). 6 tests
      .pg del ciclo completo (152/152 pg verdes).
- [x] 2.3 GET /v1/commercial/cfdi + GET /v1/commercial/cfdi/{id}/files/{format} (el
      producto descarga autenticado del PAC y sirve el adjunto; owner/admin), en
      contrato (79 operaciones en biyección), SDK (`cfdiReceipts`, `cfdiFileUrl`) y
      sección CFDI en /cuenta/facturacion (es-MX, estados honestos).
- [x] 2.4 VERIFICADO ya cubierto: `commercial-refunds-disputes.pg.spec.ts` (8 tests:
      refund NO revoca acceso —esa es la política—, dispute→suspended sin borrar
      datos, idempotencia, correlación por cargo expandido). El entitlement con
      suscripción suspended se apaga POR CONSTRUCCIÓN (WHERE status='active').
      Además: una factura reembolsada antes de timbrar jamás genera CFDI (spec).

### OLA 3 — Paridad profesional: cotas y estilos — 3.1 ✅ (427a121) · 3.3 ✅ · 3.2 parcial · 3.4 → PENDIENTES
- [ ] 3.1 DIMSTYLE de verdad: ~30 DIMVARs núcleo (DIMTXT, DIMASZ, DIMEXE, DIMEXO, DIMGAP, DIMDLI, DIMTAD, DIMJUST, DIMTIH, DIMTOH, DIMTOFL, DIMTIX, DIMSCALE, DIMLFAC, DIMDEC, DIMZIN, DIMRND, DIMPOST, DIMSAH/DIMBLK/DIMBLK1/DIMBLK2 con flechas estándar, DIMCLRD/DIMCLRE/DIMCLRT, DIMLWD/DIMLWE); comando DIMSTYLE completo (crear/modificar/aplicar/comparar); aplicación retroactiva por estilo; round-trip DXF (tabla DIMSTYLE + overrides XDATA); golden de navegador; fila en rubric.json con evidencia.
- [ ] 3.2 MLEADERSTYLE y TABLESTYLE aplicables (estilos controlan flecha/landing/texto y bordes/filas, viajan por DXF, golden).
- [ ] 3.3 Escalas anotativas v1: texto/cotas/hatch anotativos con tamaño de papel constante entre escalas de viewport; round-trip DXF del flag; golden con dos viewports a escalas distintas.
- [ ] 3.4 (solo si 3.1–3.3 verdes y sobra tiempo) Bloques dinámicos v1: visibilidad + estiramiento con grips, guardado canónico, sin round-trip DXF de dinámica (pérdida declarada). Si no alcanza → PENDIENTES con diseño esbozado.

### OLA 4 — Rendimiento en GPU real (~1 h)
- [ ] 4.1 Suite SLO de navegador (browser-slo / cad-viewport-100k) en Chromium SIN SwiftShader (GPU real), corpus architecture/cartography/text-hostile 10k y 100k; evidencia con máquina declarada (CPU, GPU, RAM, SO).
- [ ] 4.2 Presupuestos de PRODUCTO (report-only primero): apertura 10k < 5 s, zoom asentado < 1 s, pan ≥ 30 fps, 100k detalle completo < 15 s en GPU real. Documentar hotspots + terminar instrumentación por etapas del pipeline (WIP P1 ola 2) con spec de que apagada no cuesta.
- [ ] 4.3 Arreglo de mayor palanca para los que no cumplan (candidatos: subida de geometría por lotes, atlas de texto en detalle, culling de hatch). Máximo el tiempo restante de la ola; resto a PENDIENTES con perfil adjunto.

### OLA 5 — Limpieza del legado y pulido (~1 h)
- [ ] 5.1 Layout3DEditor.tsx: −400 líneas netas hoy (extraer bloques NO-CAD: mapa de calor MES, paseo en primera persona, balanceo de línea, CSV de estaciones → módulos tras flag "modo planta" apagado); sacar las 2 importaciones de jspdf del monolito unificando PDF en lib/cad/plot/plot-pdf.
- [ ] 5.2 Retirar de la UI el copiloto legado de fábrica (parser NL de 47 comandos) o esconderlo tras flag; línea de comandos CAD única entrada visible. Motor NL→CAD del puerto CIDE no se toca.
- [ ] 5.3 Barrido de fugas de inglés en el estudio (Start, Monochrome, Scope, Layers…) → es-MX. Pasada mínima de accesibilidad en línea de comandos, paletas, barra de estado (roles/aria-label, foco visible, Tab/Escape).
- [ ] 5.4 STEP: soporte CYLINDRICAL_SURFACE con spec, o declarar límite exacto en PRODUCT.md y UI ("importa STEP de caras planas").

### OLA FINAL — Cierre y verdad (~30 min, OBLIGATORIA)
- [ ] F.1 Suite completa de gates + push final; revisar CI de los pushes; main rojo se arregla antes de cerrar.
- [ ] F.2 `node scripts/cad/rubric.mjs --history` (corte del día) + reconciliar CAMPANA_10_DE_10.md y gap-matrix.
- [ ] F.3 DEPLOYMENT.md + .env.example + docs/guides/environment-variables.md con TODA variable nueva.
- [ ] F.4 `docs/execution/INFORME_CAMPANA_8H_20260820.md`: hecho (con commits), rojo, PENDIENTES priorizados con estimación, 10 siguientes ítems.

### COLA DE RESERVA (en este orden)
- [ ] R.1 DWG lab: round-trip AC1015 verificado por LECTOR EXTERNO (ODA File Converter local como oráculo; origen tool-converted-original admitido). Meta: roundTripsVerificadosPorLectorExterno ≥ 1.
- [ ] R.2 Corpus DWG del harness: +10 dibujos variados (cotas con estilos, hatch con islas, bloques anidados, xrefs) por la misma vía + bundles del repo de conformance.
- [ ] R.3 Corpus DXF de TERCEROS con licencia clara (QCAD/LibreCAD, ejemplos gubernamentales) + procedencia + matriz de fidelidad.
- [ ] R.4 Evidencia review-concurrency (carga concurrente de review links + merge con todos los roles).
- [ ] R.5 Export DXF multi-versión (R12 y 2018 además de 2000) con matriz de pérdidas por versión.

---

## PENDIENTES

- **[3.1-resto] Esquema de entidad para el núcleo DIMVAR completo** (~2-3 días): los
  campos sólo-de-tabla (DIMTXT/alturas, DIMCLR*/colores, DIMLW*/grosores, DIMTAD/
  DIMJUST/DIMTIH/DIMTOH/DIMTOFL/DIMTIX, DIMZIN/DIMRND/DIMLFAC) hoy se definen,
  persisten, editan y viajan por DXF, pero NO gobiernan el render por cota porque la
  entidad no los expresa. Camino: esquema 10 del documento con esos opcionales +
  buildCadDimensionGeometry consumiéndolos + golden. El gap de la rúbrica lo declara
  con fecha.
- **[3.1-golden] Golden de navegador del ciclo DIMSTYLE** (~0.5 día): crear estilo por
  comando → cota nueva lo hereda → Aplicar re-hornea → export DXF → reimport conserva
  tabla. El ciclo está cubierto por specs de unidad/integración (27+41 checks); falta
  el golden Playwright (patrón del 16/51).
- **[3.2-resto] MLEADERSTYLE/TABLESTYLE por DXF + tabla que gobierna bordes/filas**
  (~1-2 días): mleader style ya controla flecha/landing/texto AL CREAR; el estilo de
  tabla no gobierna nada al render. Diseño: replicar el patrón DIMSTYLE (tabla propia
  en el DXF vía XDATA en la entrada + códigos estándar donde existan; para TABLESTYLE
  el objeto DXF vive en OBJECTS, no en TABLES — declarar pérdida si se pospone).
- **[3.4] Bloques dinámicos v1** (~3-4 días, diseño esbozado): parámetros de
  visibilidad (lista de estados por definición de bloque, grip de ciclo) y
  estiramiento (parámetro + acción con caja de influencia, grip lineal); guardado
  canónico en `CadBlockDefinition.dynamic` (nueva sección opcional); SIN round-trip
  DXF de la dinámica (AutoCAD la guarda en ACAD_EVALUATION_GRAPH binario) — pérdida
  declarada en el manifiesto; goldens de grips. `dynamic-blocks.ts` ya consume
  `annotativeHeightMm`, buen punto de partida.
- **[4-instrumentación] Encender el perfil por etapas EN EL NAVEGADOR** (~1 día): hoy
  sólo corre por el arnés Node (`profileCadRenderStages`); falta el hook en el spec
  Playwright/editor (la mina del puntero de módulo está documentada en
  render-profile.ts) y las etapas de GPU/composición declaradas sin medir.

---

## BITÁCORA

- **[inicio]** Sesión arranca. PowerShell roto en esta máquina (error 800705af al cargar);
  toda la campaña corre en Git Bash. Repo limpio y al día en `abfd8f3`. Archivo de
  campaña creado. Arranca OLA 0.
- **[OLA 0]** 0.1: suite completa lanzada en fondo (API: 639 tests verdes hasta ahora,
  27 suites .pg saltadas en local como está diseñado — `npm test` salta sin PG,
  `test:pg` exige). PG16 portable YA corría (PID en `D:\dev\pgdata-valle`, PostgreSQL
  16.9) con `valle_design_test` disponible: los specs .pg de la campaña correrán con
  `TEST_DATABASE_URL` local.
- **[OLA 0]** 0.2: `npm audit --omit=dev` confirma exactamente las 2 esperadas:
  nanoid <3.3.18 ALTA (vía next→postcss, hoy 3.3.16) y dompurify ≤3.4.12 MODERADA
  (vía jspdf 4.2.1). Fix semver disponible para ambas; se aplica cuando la suite
  termine para no contaminar la línea base.
- **[OLA 0]** 0.3 ✅: paso bloqueante `npm audit --omit=dev --audit-level=high` añadido
  a quality-gates (tras la instalación; umbral high deliberado para que moderate no
  vuelva ruido el gate) y `.github/dependabot.yml` creado (npm semanal lunes, agrupado
  en un PR de versiones + grupo de seguridad, límite 5 PRs por la cuota de Actions).
- **[recon]** Workflow de 4 agentes mapeó OLAS 1-2. Decisiones de diseño derivadas:
  - **1.1**: las tablas CAD reales son 8, no 6: se suman `cad_sheet_sets` y
    `sf_cad_blocks`. PERO `sf_cad_blocks` usa `tenant_id IS NULL` como carril de
    SISTEMA (biblioteca arquitectónica sembrada por la migración
    `ArchitecturalBlockLibrarySeed`): NO puede ser NOT NULL. Decisión: NOT NULL en
    las 7 restantes con pre-check que ABORTA si hay filas NULL (fail-closed, sin
    mutar datos); RLS en las 8 (política por `current_setting('app.tenant_id', true)`;
    en `sf_cad_blocks` política extra de lectura del carril sistema). Sin FORCE:
    el dueño de las tablas (la app) no queda sujeto — la protección aplica a todo
    rol no-dueño, y el camino a rol runtime no-dueño queda documentado en
    DEPLOYMENT.md. TenantContext NO usa hoy variable de sesión PG (aislamiento 100%
    aplicación): el spec .pg prueba con `SET ROLE` a rol sin privilegios.
    Consecuencia documentada: importaciones enterprise por el carril NULL de
    cad_projects/cad_documents exigirán asignar tenant al importar.
  - **1.4**: el store de rate limit (`IDENTITY_RATE_LIMIT_STORE`) es global y
    exportado; se crea `ApiRateLimitService` compartido en identity y se aplica
    imperativo (patrón existente) en los 5 endpoints.
  - **1.6**: `documentSummary`/`projectResource` sólo usan columnas ligeras →
    proyección `select` explícita; la rama `q` pasa a SQL portable
    (`LOWER LIKE` + escape) manteniendo el scoping de tenant (operador dentro de
    `find`, nunca QueryBuilder sin scope).
  - **1.7**: cobertura real del contrato hoy: 43 de 77 operaciones (43/73 quedó
    viejo). Fuera: auth (12), organizations (5), commercial (15) + webhook Stripe y
    `_development/email-outbox` que necesitarán allowlist documentada.
  - **2.x**: no existe mecanismo de fixtures HTTP grabados; el patrón de la casa es
    cliente HTTP inyectable + dobles en spec (`httpDouble` de Stripe). El CFDI
    seguirá ese patrón. Emisión CFDI: tabla `cfdi_receipts` + servicio en el tick
    del worker del outbox (patrón `RenewalReminderService`), nunca HTTP dentro de
    la transacción del webhook. Config PAC existente: 4 variables `CFDI_PAC_*`/
    `CFDI_ISSUER_*` (no hay env `CFDI_PROVIDER`; la selección será por
    `CFDI_PAC_NAME=facturama`).
- **[OLA 4 prep]** Máquina declarada: AMD Ryzen 5 5500U (12 hilos), 8 GB RAM,
  AMD Radeon(TM) Graphics integrada (Vega 7), Windows 11 Home. "GPU real" aquí es
  la iGPU — la evidencia lo declarará explícitamente.
- **[OLA 0/1]** LECCIÓN: `npm audit fix --omit=dev` PODA las devDependencies de
  node_modules (jest, openapi-typescript desaparecieron y dos gates murieron con
  MODULE_NOT_FOUND). El `--omit` pertenece a `npm audit`, no al install del fix.
  Se restauró con `npm install` (lockfile intacto). Si un gate muere con
  MODULE_NOT_FOUND tras un audit fix, el primer sospechoso es esto.
- **[OLA 1]** Spec RLS: primera corrida falló porque la fila huérfana ADOPTADA en el
  test del pre-check quedaba viva en el test del probe (2 filas donde esperaba 1) y el
  fallo dejó la conexión del pool con SET ROLE pegado (cascada "must be owner").
  Arreglo: estado limpio por test + RESET ROLE/ALL en finally. Lección: en specs .pg
  con SET ROLE, el RESET va en finally SIEMPRE — un rol filtrado convierte los tests
  siguientes en falsos rojos de permisos.
- **[OLA 1]** El gate de contrato ampliado pasó a la PRIMERA con 77/77: las cuatro
  familias ya estaban en biyección. La deuda "43 de 73" era del gate, no del router.
- **[PUSH OLAS 1+2]** `2613613` en origin main (pull --rebase --autostash integró 4
  commits de la campaña DWG paralela). Verificación pre-push: check:cad completo verde;
  check:dwg/typecheck de dwg-codec ROJOS por el WIP no committeado de la campaña
  paralela (su territorio; mis commits no lo tocan; CI valida el estado committeado —
  y su push posterior lo arregló). Fases restantes corridas con turbo filtrado a mis
  4 workspaces: 2 timeouts de contención en tests de blob (5s/30s) que pasaron en
  reintento solo — la máquina corría 12 tareas + la otra sesión. El spec de la consola
  API cazó de verdad: 77→79 operaciones exigió regenerar operations.generated.json.
- **[incidente]** `git add -A` arrastró archivos EN CURSO de la campaña DWG paralela
  al commit de OLA 2 → reset --soft y recommit con staging explícito. Regla nueva en
  memoria persistente: en este repo, staging SIEMPRE explícito por ruta.
- **[OLA 0]** 0.4 ✅: no existía gate de claves JSON duplicadas. Creado
  `scripts/cad/check-json-duplicate-keys.mjs` (escáner del TEXTO con parser propio —
  JSON.parse esconde el duplicado; claves comparadas tras resolver escapes; falla
  cerrado ante manifiesto ausente/roto; workspaces derivados de los globs, no lista a
  mano) + spec con 50 comprobaciones incluido el escenario real del merge que mató
  3 gates. Encadenado PRIMERO en `check:cad` vía `npm run check:json-keys`. Verde en
  local: 9 manifiestos limpios.
