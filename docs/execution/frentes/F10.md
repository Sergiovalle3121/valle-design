# F10 · Evidencia independiente — bitácora

Territorio: `docs/cad/evidence/`, `docs/cad/corpus/oraculos/`,
`apps/web/src/lib/cad/verification/`, `scripts/cad/` (ficheros nuevos). No toca
producto. Arranca el primer día de la campaña «El lunes de un arquitecto»
(T-03 de la Ola 0) y no para hasta agotar la lista de
`docs/cad/evidence/independencia-por-fila.json`.

Verificado antes de empezar: `curl -sS "$HTTPS_PROXY/__agentproxy/status"`
confirma PyPI, npm y crates.io en `noProxy`. `pip3 install --user` funciona
sin red de sistema (apt no alcanza, PyPI sí). `raw.githubusercontent.com`
también responde directo (usado para el corpus GIS de Natural Earth);
`api.github.com` no está en el alcance de esta sesión.

---

## Oráculo D · `pyproj` (envuelve PROJ) → filas `geo` y `toolset-map3d`

**Terminada, escrita antes de empezar:** `crs-pyproj.spec.ts` y
`shapefile-pyproj.spec.ts` en verde, artefacto
`docs/cad/corpus/oraculos/pyproj-3.7.2.json` congelado, herramienta registrada
en `HERRAMIENTAS.md` y en `oraculos-externos-registro.ts`.

**Estado final: HECHA.**

- Un solo trabajo, dos filas, tal como preveía el censo.
- `docs/cad/corpus/oraculos/censo-pyproj.py`: reproduce con PROJ (a) la MISMA
  malla de control de México que `crs.spec.ts` genera con su propia fórmula
  (660 puntos, lat 14°-33°, lon -118°--86°, zonas 11N-16N) y (b) los puntos de
  un shapefile PÚBLICO de terceros (Natural Earth, dominio público, 13 de 243
  lugares poblados caen en el dominio de México).
- `crs-pyproj.spec.ts` (en `apps/web/src/lib/geo/`, junto a `crs.spec.ts`):
  regenera la malla con la MISMA fórmula, compara `geodeticToUtm()` contra
  PROJ con tolerancia 1 mm (peor error medido: 2,5 nanómetros en la malla,
  0,06 mm en los puntos de Natural Earth — la tolerancia declarada es
  deliberadamente holgada frente a lo medido). 687 comprobaciones.
- `shapefile-pyproj.spec.ts`: usa el lector de PRODUCCIÓN
  (`readShapefile()` de `apps/web/src/lib/geo/shapefile.ts`, el mismo que usa
  `MAPIMPORT`) para abrir el `.shp`/`.shx`/`.prj` REAL de Natural Earth,
  reproyecta con `reprojectGeoPoint()` (también de producción) y compara
  contra PROJ. 80 comprobaciones, incluida la puerta de derechos
  (sha256 de los 5 archivos y de la licencia contra
  `docs/cad/corpus/terceros-gis-manifest.json`).
- Material de terceros: `ne_110m_populated_places_simple.{shp,shx,dbf,prj,cpg}`,
  Natural Earth, dominio público (declarado por el propio proyecto en su
  `LICENSE.md`, sin condición de atribución), descargado desde
  `raw.githubusercontent.com/nvkelso/natural-earth-vector` anclado al commit
  `ca96624a56bd078437bca8184e78163e5039ad19`. Licencia descargada, hasheada y
  archivada en `docs/cad/corpus/licencias/natural-earth-vector-PUBLIC-DOMAIN.md`.
- Límite declarado dos veces (en `HERRAMIENTAS.md` y en ambos specs): PROJ
  resuelve la transversa de Mercator con el mismo linaje matemático que Karney
  (2011), igual que `crs.ts`. Acredita OTRA IMPLEMENTACIÓN, no OTRO MÉTODO —
  eso ya lo dan Snyder y la cuadratura dentro de `crs.spec.ts`.
- Petición escrita en `F10-peticiones.md`: sube `geo.crs` y
  `toolset-map3d.georreferencia`, +2 pt.

## Oráculo E · `openapi-spec-validator` → fila `api-sdk`

**Terminada, escrita antes de empezar:** `api-sdk-openapi.spec.ts` en verde,
artefacto `openapi-spec-validator-0.9.0.json` congelado anclado al sha256 de
`design-api.v1.yaml`, herramienta registrada.

**Estado final: HECHA.**

- `censo-openapi.py`: `OpenAPIV31SpecValidator` (PyPI, Apache-2.0) dictamina
  `packages/contracts/specs/design-api.v1.yaml` — VÁLIDO, 0 errores, contra el
  esquema OFICIAL de OpenAPI 3.1, no contra `check-design-contract.mjs` (que es
  nuestro).
- `api-sdk-openapi.spec.ts`: ancla el sha256 del contrato (217 420 bytes) al
  dictamen, y si la herramienta está presente la vuelve a correr y exige el
  mismo veredicto. 13 comprobaciones.
- Petición: sube `api-sdk.contract`, +1 pt.

## Oráculo F · Python `hmac`/`hashlib` (biblioteca estándar) → fila `events`

**Terminada, escrita antes de empezar:** `events-hmac.spec.ts` en verde,
artefacto `hmac-stdlib.json` congelado, herramienta registrada (sin rueda que
hashear: es la biblioteca estándar, no un paquete de PyPI).

**Estado final: HECHA.**

- Fixture DETERMINISTA (secreto y timestamp fijos, nunca aleatorios) con la
  MISMA forma de cuerpo que `webhook-outbox.transport.ts` produce para un
  evento de dominio, firmada `HMAC-SHA256(secreto, timestamp + "." + body)`.
- El verificador REAL de producción (`verifyOutboxSignature`, importado sin
  modificarlo vía `import()` dinámico + `pathToFileURL`, el mismo patrón que
  `independencia-rubrica.spec.ts` usa para `scripts/cad/rubric.mjs`) confirma
  que la fixture es fiel al contrato: acepta la firma genuina, rechaza la
  firma sobre un cuerpo alterado en un byte.
- `censo-hmac-stdlib.py`: con `hmac.compare_digest` y `hashlib.sha256` de la
  biblioteca ESTÁNDAR de Python (cero paquetes de PyPI), recalcula la firma
  desde cero sobre la fixture y llega al mismo dictamen: coincide con la
  genuina, no coincide con la alterada.
- 12 comprobaciones en `events-hmac.spec.ts`.
- Petición: sube `events.operational`, +1 pt.

## Oráculo G · `mpmath` → fila `wasm`

**Terminada, escrita antes de empezar:** `curve-kernel-mpmath.spec.ts` en
verde, artefacto `mpmath-1.4.1.json` congelado, herramienta registrada.

**Estado final: HECHA.**

- `censo-mpmath.py`: copia LITERAL de `ARC_EDGE_CASES`/`ELLIPSE_EDGE_CASES` de
  `curve-kernel-corpus.ts` (10 arcos + 7 elipses, las esquinas escritas a mano
  que el generador aleatorio no alcanza), teselados con `mpmath` a 50 dígitos
  de precisión para `steps` 24 y 96, redondeados al `f64` más cercano.
- `curve-kernel-mpmath.spec.ts` (junto a `curve-kernel-parity.spec.ts`):
  ejercita el teselador de referencia (`tessellateArc`/`tessellateEllipse` de
  producción) **y** el binario WASM del árbol (`createCadCurveKernel`) contra
  la referencia de mpmath, usando la MISMA magnitud «desviación relativa a la
  escala de la curva» que `curve-kernel-corpus.ts` ya publica (no ULP crudo,
  que dispara cerca de un cero matemático por cancelación). 110
  comprobaciones, 5 252 magnitudes, peor desviación medida 2,22×10⁻¹⁵
  (idéntica en JS y WASM) contra una tolerancia declarada de 3,55×10⁻¹⁵ (16
  épsilon de máquina, el doble del peor caso con margen).
- Esto es justo lo que `curve-kernel-parity.spec.ts` declaraba que le faltaba
  en su propia cabecera: «comparar los motores entre sí dice si se parecen,
  nunca cuál tiene razón […] falta que el que calcule sea otro» — ahora hay
  un tercero, y dice que los dos tienen razón.
- Petición: sube `wasm.toolchain`, +1 pt.

## Oráculo H · `atheris` → fila `json-import`

**Terminada, escrita antes de empezar:** `json-import-atheris.spec.ts` en
verde, artefacto `atheris-3.0.0.json` congelado (500 documentos), herramienta
registrada.

**Estado final: HECHA.**

- **Hallazgo previo:** `hypothesis` (la opción que nombraba
  `PROMPT_MAESTRO_FABLE.md`) es **MPL-2.0**
  (`https://pypi.org/pypi/hypothesis/6.167.1/json` →
  `license_expression: "MPL-2.0"`), y `CORPUS_POLICY.md` la prohíbe «sin
  excepción y sin discusión». Se desinstaló de la máquina sin que llegara a
  ningún artefacto comprometido. Sustituto: `atheris` 3.0.0 (Google,
  Apache-2.0; la 3.1.0 no publica rueda `cp311`).
- `atheris-fuzz-worker.py`: el proceso de fuzzing guiado por cobertura
  (subproceso aparte, porque `atheris.Fuzz()` termina el intérprete al agotar
  `-runs=N`). Usa un CEBO de cobertura (`assert_safe_json_mimic`, fiel a
  `assertSafeJson` de `document-import.ts` pero escrito por este frente, NUNCA
  el validador real) para que las mutaciones de libFuzzer tengan señal.
  Cobertura medida: `cov: 7, ft: 7` en 20 000 ejecuciones — un techo pequeño,
  declarado tal cual, no inflado.
- `censo-atheris.py`: orquesta la corrida (semilla `1`, determinista —
  comprobado corriendo dos veces y comparando byte a byte), registra los 500
  textos DISTINTOS que el fuzzer probó (no sólo el corpus final tras la
  minimización de libFuzzer, que reduce agresivamente).
- `json-import-atheris.spec.ts`: alimenta los 500 textos al importador REAL
  (`validateImportFile` + `importDocumentText`), clasificando con el MISMO
  `classifyCadImportError`/`CAD_IMPORT_OUTCOMES` que usa el fuzzer interno
  `document-import-fuzz.ts` (importados, no reimplementados). Resultado: 512
  comprobaciones, histograma `{tamano-invalido:1, json-no-analizable:473,
  no-canonico:26}`, **cero** «desconocido», **cero** lanzamientos no
  tipados. El producto sobrevive limpio a 500 documentos que ningún humano
  de este proyecto decidió escribir.
- Petición: sube `json-import.fuzzing`, +1 pt.

## Resumen de la sesión

**Seis oráculos cableados: D (pyproj), E (openapi-spec-validator), F (HMAC
stdlib), G (mpmath), H (atheris) — cinco filas de la rúbrica suben +1 pt cada
una si el coordinador aplica las peticiones (`geo.crs`,
`toolset-map3d.georreferencia`, `api-sdk.contract`, `events.operational`,
`wasm.toolchain`, `json-import.fuzzing`): +6 pt, de 244 a 250/271.** Ninguna
línea de producto tocada. Un hallazgo de licencia (`hypothesis` MPL-2.0)
corregido antes de que entrara a ningún artefacto.

## Lo que sigue (no entró en esta sesión)

- **`toolset-raster`: BLOQUEADO, medido, no sólo intentado.**
  `apps/web/src/lib/cad/raster-decode.ts` sólo lee PNG y BMP (JPEG, TIFF, GIF
  y WebP se rechazan por nombre con su motivo: «el motor no lleva
  descodificador»). Los escaneos HABS/HAER de la Library of Congress se
  sirven en TIFF/JPEG, nunca en PNG ni BMP. Y `loc.gov`/`tile.loc.gov` **no
  son alcanzables desde esta sesión**:
  `curl -sS https://www.loc.gov` → `curl: (56) CONNECT tunnel failed,
  response 403` (igual con `tile.loc.gov`), confirmado el 2026-09-06. No hay
  `ImageMagick` (`command -v convert magick` en blanco) ni `Pillow`
  (`python3 -c "import PIL"` → `ModuleNotFoundError`) instalados para
  convertir un TIFF/JPEG a PNG aunque el archivo llegara. **Qué lo
  desbloquearía:** o bien `loc.gov` entra al `noProxy` de una sesión futura,
  o bien aparece un plano de dominio público servido YA en PNG/BMP (no se
  encontró ninguno reachable en el tiempo de esta sesión), o bien se instala
  Pillow (PyPI, HPND — licencia permisiva, admisible) para convertir un
  TIFF/JPEG alcanzable por otra vía a PNG antes de usarlo. No se intentó
  fabricar un PNG sintético con geometría inventada: eso sería exactamente
  la trampa que este frente ya se comió una vez (evidencia fabricada por
  casa disfrazada de material ajeno).
- **`toolset-electrical`, `toolset-mechanical`, línea de comandos**: no
  alcanzados por tiempo. Aviso de licencia para quien los retome:
  - `toolset-mechanical` (banco de tornillería FreeCAD, que
    `independencia-por-fila.json` sugiere): FreeCAD el PROGRAMA es
    LGPL-2.1-or-later (ya registrado como inadmisible en `HERRAMIENTAS.md`
    por el frente DWG). El banco de datos de tornillería concreto podría
    tener una licencia distinta a la del programa que lo aloja, pero
    **hay que comprobarlo archivo por archivo antes de usarlo**, no darlo
    por bueno porque el prompt lo sugiere: si el banco hereda LGPL,
    `CORPUS_POLICY.md` lo excluye igual que excluyó `pythonocc-core`.
  - Línea de comandos (tabla de alias de LibreCAD/BricsCAD, que
    `PROMPT_MAESTRO_FABLE.md` sugiere): LibreCAD es GPL-2.0 (ya registrado
    como inadmisible en `HERRAMIENTAS.md`). Una tabla de alias que viviera
    dentro de ese repositorio hereda su licencia salvo que el archivo
    concreto declare otra cosa por separado — **comprobar antes de usar**.
    BricsCAD es software propietario: su tabla de alias, si se pudiera
    obtener, no sería material redistribuible sin permiso explícito.
  - `toolset-electrical`: **también BLOQUEADO, medido.**
    `apps/web/src/lib/cad/electrical/nom-conductors.ts` ya cita artículos de
    la NOM-001-SEDE (Tabla 310-15(b)(16), Cap. 9 Tabla 8, Art. 240-4(D),
    Art. 240-6(A), Tabla 250-122, Art. 210-19(A)) y **ya declara por
    escrito** que esos números «se transcribieron sin acceso al texto
    oficial en línea», pidiendo el cotejo que falta. Se intentó ese cotejo
    en esta sesión: `www.dof.gob.mx` y `sidof.segob.gob.mx` **no son
    alcanzables**: `curl -sS https://www.dof.gob.mx` →
    `curl: (56) CONNECT tunnel failed, response 403` (igual con
    `sidof.segob.gob.mx`), confirmado el 2026-09-06. Sin acceso a la
    publicación oficial, anclar «cita y fecha» sería escribir una fecha que
    nadie de esta sesión comprobó — exactamente lo que la regla 1 de la casa
    prohíbe («ningún claim sin evidencia»). **Qué lo desbloquearía:**
    `dof.gob.mx` o `sidof.segob.gob.mx` en el `noProxy` de una sesión futura,
    o que el titular aporte el PDF oficial ya descargado (con su fecha de
    publicación) para anclarlo por sha256 igual que las licencias de este
    frente.
- **Xrefs** y **MLEADER y tablas**: los dos candidatos más caros de la lista
  original (`independencia-por-fila.json` los marca "no lo sirve material
  ajeno" — piden usuario real, no alcanzables por este frente).
- **`layouts.fidelity`**: explícitamente NO es de este frente (lo hace F4
  dentro de T-31).
