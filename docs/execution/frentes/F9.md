# F9 · Cimientos y piel — bitácora

## Aviso del coordinador (2026-09-06 08:55 UTC) · base verificada

El disparador «Aviso F9 · comprobar base y rebasar sobre origin/main»
avisó de que algunos clones nacían 127 commits detrás de `main` real
(`1478471` en vez de `4800017`/`2fd2bfd`). Comprobado: `git merge-base HEAD
origin/main` dio `2fd2bfd` — el punto de partida correcto de la campaña —
así que la rama nació bien. Se hizo `git merge origin/main` para traer
PR #193 (mueve los 21 informes de dimensión a
`docs/execution/auditoria-fable/dimensiones/` y añade la ficha T-0D; cero
conflictos, sólo renombrados). Reverificado tras el merge: `typecheck`
(Node 20, monorepo entero) limpio, `check:contrast` OK, suite de `web`
(631/631) y `check:cad` relanzados sobre el árbol nuevo antes de seguir.

**Lo que `check:cad` sí encontró, y de verdad**: `check-monolith-budget.mjs`
falló — `app/dashboard/page.tsx` había llegado a 882 líneas, por encima del
techo de 800 para archivo no presupuestado. Mis propias fichas de T-75(f) y
T-75(g) lo empujaron ahí. Arreglado con el MISMO patrón que ya usaba
`import-status.tsx` (extraído en una campaña anterior, exactamente por esta
razón): `importDocument` completo (con su reloj de atasco de T-75f) pasó a
`useImportDocument` en `import-status.tsx`, y el borrado de T-75(g) pasó a
un archivo nuevo, `archive-document.tsx` (`useArchiveDocument` +
`ArchiveDocumentDialog`). `page.tsx` queda en 716 líneas. Ningún
comportamiento cambia — es la misma lógica movida, no reescrita —, así que
los goldens 160/161 se re-corrieron contra el build nuevo para confirmarlo
en vez de darlo por hecho (regla de la casa: aplicar no es ejecutar, hay
que volver a medir).


Rama: `claude/f9-cimientos-piel`. Territorio y prohibiciones: ver
`docs/execution/auditoria-fable/PROMPT_MAESTRO_FABLE.md` §5.2 y §3.

Cada entrada nace con su «terminada» escrita antes de empezar.

---

## T-72(e) · `lazy.ts` cachea la promesa RECHAZADA

**Terminada cuando:** `pending` se limpia en el rechazo (dos líneas), y un
spec nuevo prueba que un `load()` que falla dos veces seguidas se recupera al
tercer intento sin quedarse pegado a la primera promesa rota.

**Estado: ARREGLADA.**

- Extraje la memoización a `commands/lazy-singleton.ts`
  (`createLazySingleton`), genérica y testeable sin mockear `import()`
  dinámico: recibe cualquier `load: () => Promise<T>` y limpia `pending` en el
  `.catch`. `lazy.ts` queda como envoltorio de dos líneas sobre ella; la API
  pública (`loadCadNlCommands`, `cadNlCommandsIfLoaded`, `CadNlCommands`) no
  cambia, así que los tres consumidores existentes
  (`engine/index.ts`, `palette-actions.ts`, `Layout3DEditor.tsx`) no se
  tocan.
- `lazy-singleton.spec.ts`: caso feliz (memoiza, una sola llamada al
  cargador), el caso que rompía la sesión (un rechazo no envenena, el
  siguiente `get()` reintenta) y dos rechazos consecutivos seguidos de
  recuperación.
- `lazy.spec.ts`: integración sobre los módulos reales `parser`/`executor`
  (memoización real, `cadNlCommandsIfLoaded` sólo dista de null tras
  resolver).
- Verificado: `npx tsx src/lib/cad/commands/lazy-singleton.spec.ts` y
  `lazy.spec.ts`, los dos verdes. `npx eslint` limpio. `typecheck` (Node 20)
  limpio.

---

## T-24·3 · El 400 permanente sale por la rama del 500 transitorio

**Terminada cuando:** `describeCadSaveFailure` clasifica un 400 de
"documento por encima del límite" (el `BadRequestException` de
`encodeCadDocumentArchive` en `cad-document-storage.ts`) como el mismo
`kind: "too-large"` que el 413, con mensaje que ofrece salida (exportar DXF,
dividir/reducir) — nunca "espera y vuelve a pulsar" — y un caso nuevo en
`save-failure.spec.ts` lo prueba.

**Estado: ARREGLADA.**

- El archivo real es
  `apps/web/src/components/cad/document-lifecycle/save-failure.ts` (el
  prompt cita `lib/cad/save-failure.ts`; no existe esa ruta — T-72(i) ya
  señala que un documento de la casa describe un árbol que ya no existe).
  `autosave.ts` no necesitó cambio: no decide el mensaje, sólo publica el
  error tal cual; el mensaje sale de `describeCadSaveFailure`, consumido
  directamente por `Layout3DEditor.tsx:13144` y `:13284` — el arreglo llega
  ahí sin tocar el monolito.
- Añadido `OVER_ARCHIVE_LIMIT` (regex sobre el mensaje literal del servidor,
  "excede N bytes") y el 400 que la casa se reúne con el 413 bajo el mismo
  `kind: "too-large"`, mismo mensaje (exportar DXF ahora, dividir el dibujo o
  quitar imágenes). Un 400 SIN ese mensaje (p. ej. una petición inválida
  cualquiera) sigue cayendo en `"server"`, sin disfrazarse.
- `save-failure.spec.ts`: caso nuevo en la lista de `casos` (400 con el
  mensaje real del servidor → `kind: "too-large"`), más dos bloques: uno
  que prueba que NO repite la frase genérica de espera y SÍ ofrece exportar
  y dividir/reducir, y otro que confirma que un 400 corriente sin ese
  mensaje NO se disfraza de "documento demasiado grande".
- Verificado: `npx tsx save-failure.spec.ts` → 70 comprobaciones verdes (antes
  9 casos base; ahora 10). `eslint` y `typecheck` limpios.

---

## T-13 · El plano entero desaparece a un clic del conmutador de tema

**Terminada cuando:** (a) la tinta por defecto del dibujo (blanco de ACI 7 /
color de reserva del renderizador) es legible contra los cuatro presets
reales de `THEMES`, con spec que lo prueba cruzando los cuatro; (b)
`check-contrast.mjs` mide esa tinta contra esos cuatro presets (no sólo
pares token×token de `globals.css`) y falla si deja de serlo; (c) los cuatro
pares de UI que el escéptico midió a mano (viewport-hints, ReviewPlanView /
CollabThreadPanel, CadHatchPalette, field-controls) están en el gate y
arreglados con tokens; (d) el forzado de canvas a `light` en
`Layout3DEditor.tsx:2058-2061` está documentado como petición, no tocado.

**Estado: ARREGLADA (lo que está en mi territorio) + PETICIÓN (lo que no).**

- **`lib/cad/render/render-style.ts`**: nueva `legibleDefaultInk(color,
  backgroundColor)`. Sólo actúa sobre dos colores — blanco puro (0xffffff,
  el RGB exacto de ACI 7 en `aci-palette.ts`) y `CAD_RENDER_DEFAULT_COLOR`
  (0x60a5fa, que medía 2,22:1 sobre el preset «Claro», bajo el piso de 3,0)
  — y sólo cuando su contraste contra el fondo cae bajo 3:1; entonces prueba
  blanco y negro y se queda con el que se ve. **Nunca toca un color que el
  plano declara**: hay spec explícito para eso. `defaultCadRenderStyle` gana
  un tercer parámetro opcional `backgroundColor` (por defecto el preset
  Oscuro, `0x0a0f1e`): con el valor por defecto el comportamiento es
  IDÉNTICO al de antes para los tres llamadores existentes (verificado:
  `CAD_RENDER_DEFAULT_COLOR` y blanco ya daban >3:1 contra el oscuro, así
  que `legibleDefaultInk` no los toca ahí) — cero regresión mientras nadie
  pase el fondo real. Wiring del fondo real: PETICIÓN a F7 (ver abajo).
- **`render-style.spec.ts`** (nuevo): reproduce el 1,15:1 medido, cruza
  `legibleDefaultInk` contra los CUATRO presets reales importados de
  `THEMES` (no copiados a mano), prueba que un color explícito del plano
  nunca se toca, y prueba `defaultCadRenderStyle` con y sin el fondo real.
  16 comprobaciones verdes.
- **`scripts/design/check-contrast.mjs`**: ampliado, no relajado.
  - Nueva sección que lee literalmente (no re-implementa el runtime, lee el
    CÓDIGO FUENTE con patrones estrechos y falla ruidoso si no los
    encuentra) los cuatro presets de `THEMES`
    (`components/cad/studio/editor-presentation.ts`), el RGB de ACI 7
    (`plot/aci-palette.ts`) y `CAD_RENDER_DEFAULT_COLOR`
    (`lib/cad/render/render-style.ts`), reimplementa la misma aritmética de
    `legibleDefaultInk` (comentado como tal, y con guardia: si
    `legibleDefaultInk` desaparece de `render-style.ts` por nombre, el gate
    falla) y mide contraste ≥3:1 contra los cuatro presets. 8 filas nuevas
    (4 presets × 2 tintas).
  - Soporte para FONDOS COMPUESTOS (`{ over, on, alpha }`) en `PAIRS`, no
    sólo tinta atenuada: hacía falta para medir `bg-warning/15`,
    `bg-success/15` y `bg-accent/15` tal como se ven, no el token sólido.
  - Cuatro pares nuevos con los valores que el escéptico midió a mano el
    2026-09-05, ahora medidos por el gate en cada corrida:
    `viewport-hints.tsx:44` (1,17:1 → arreglado), `ReviewPlanView.tsx:251` /
    `CollabThreadPanel.tsx:222` (1,62:1 → arreglado con el MISMO token,
    un defecto), `CadHatchPalette.tsx:77` (1,09:1 → arreglado, con la
    primera vuelta a 0,20 de alfa dando 4,37:1 en oscuro — bajo el piso de
    texto 4,5 — corregido a 0,15), `field-controls.tsx:34` (3,04:1 con hex
    suelto `#0e7490` → arreglado con tokens, hex fuera de `globals.css`
    eliminado).
  - `check-contrast.spec.mjs`: tres pruebas nuevas — reproduce el 1,15:1 del
    defecto con el metro base (ancla numérica, no depende de esta
    implementación), confirma que el gate lee los cuatro presets reales
    (no un número copiado), y prueba que el gate FALLA si `render-style.ts`
    deja de declarar `legibleDefaultInk` por nombre (regresión simulada con
    un fichero temporal, igual que la prueba de paleta ilegible ya
    existente). 13/13 verdes.
  - Colores ACI: NO tokenizados. `aciToHex`/`EXACT` de `aci-palette.ts`
    intactos — son datos del plano. Lo único que cambia es la tinta que se
    VE en pantalla cuando esa tinta es un valor por defecto, nunca el dato.
- **Componentes arreglados con tokens** (ninguno con hex suelto ni color
  Tailwind crudo tras el cambio):
  - `components/cad/studio/viewport-hints.tsx:44`: `text-gray-900` →
    `text-warning-ink`.
  - `components/cad/collab/ReviewPlanView.tsx:251` y
    `components/cad/collab/CollabThreadPanel.tsx:222`: `text-gray-950` →
    `text-success-ink` (mismo defecto, dos ficheros).
  - `components/cad/palettes/CadHatchPalette.tsx:77`: `bg-violet-500/20
    text-violet-100` (Tailwind crudo, no un token) → `bg-accent/15
    text-primary-ink hover:bg-accent/25` (el violeta YA es el acento de
    marca; sólo hacía falta usar su token).
  - `components/cad/studio/field-controls.tsx:34`: `style={{ background:
    "#0e7490" }}` + `text-foreground` → `bg-accent text-accent-foreground`
    (fill + ink, la pareja de dos tokens que pide `DESIGN_SYSTEM.md`).
- **NO tocado, PETICIÓN escrita** (`F9-peticiones.md`):
  1. `Layout3DEditor.tsx:2058-2061` fuerza el lienzo al tema de la app. Con
     `legibleDefaultInk` ya arreglado esto deja de BORRAR el plano (la tinta
     por defecto se adapta a cualquier fondo), pero sigue siendo una
     decisión no pedida por quien elige el tema del lienzo por separado del
     tema de la app — la petición documenta el desacople posible.
  2. `render-pipeline-host.ts:279` (`styleOf`, territorio de F7 —
     `viewport/`) llama a `defaultCadRenderStyle(entity, this.document)` sin
     el tercer argumento: usa el fondo por defecto (preset Oscuro) SIEMPRE,
     así que el arreglo está listo pero no entra en vigor hasta que F7 pase
     el fondo activo real. Diff exacto en la petición.
- Verificado: `node scripts/design/check-contrast.mjs` → OK, 92 pares. `node
  --test scripts/design/check-contrast.spec.mjs` → 13/13. `npx tsx
  render-style.spec.ts` → 16 verdes. `eslint` sobre los seis componentes
  tocados: limpio. `npm run typecheck` (Node 20, monorepo entero): limpio.

---

## T-72(f) · Presupuesto de lint por archivo, no sólo por regla y workspace

**Terminada cuando:** `check-lint-budget.mjs` compara por `{workspace, regla,
archivo}`, la concentración no puede migrar entre archivos sin que el
trinquete lo note, y el presupuesto se regenera con `--update` reflejando
exactamente el estado actual (489 avisos, sin relajar nada).

**Estado: ARREGLADA.**

- Reescrito para contar por archivo (`countWorkspace` agrupa
  `{regla: {archivo: cuenta}}`) y comparar cada combinación archivo×regla
  contra su propio techo, no contra el total agregado del workspace.
- `lint-budget.json` regenerado con `--update`: 68 combinaciones
  archivo×regla, 489 avisos, el mismo total de antes (no se relajó nada,
  sólo se hizo visible dónde vive cada aviso). Confirma con datos lo que
  T-72(a) ya decía en prosa: `Layout3DEditor.tsx` concentra 148/164 de
  `react-hooks/refs`, 11/14 de `no-unused-vars`, 4/4 de
  `set-state-in-effect`.
- Verificado: `node scripts/check-lint-budget.mjs` → OK. Mensaje de fallo
  reescrito para nombrar el ARCHIVO, no sólo la regla.

---

## T-72(h) · El editor es lo único del estudio sin `ErrorBoundary`

**Terminada cuando:** `<Layout3DEditor>` está envuelto en una frontera de
error en `CadStudioHost.tsx` (fuera del monolito, sin tocar
`Layout3DEditor.tsx`), esa frontera escribe un checkpoint de emergencia en
el diario de recuperación en el instante del fallo, y ofrece descargar el
último punto de recuperación en DXF — con specs que prueban cada pieza por
separado.

**Estado: ARREGLADA.**

- **Dónde se montó, y por qué ahí y no en `app/studio/[documentId]/page.tsx`
  literalmente**: la ficha señala la página del estudio como "fuera del
  monolito", y lo es, pero el `<Layout3DEditor>` real se invoca un nivel más
  abajo, en `components/cad/CadStudioHost.tsx` (184 → 253 líneas), que
  `page.tsx` ya monta. Ese archivo no es el monolito, no aparece reservado a
  ningún otro frente en §5.2, y ya usa `ErrorBoundary` para tres zonas
  hermanas (Colaboración, Mensajería, Llamada) — así que envolver el editor
  ahí es aplicar el mismo patrón que el archivo ya tiene, no inventar uno
  nuevo. Decisión anotada aquí por si el coordinador prefiere que viva
  literalmente en `page.tsx`.
- **El checkpoint "antes del fallback" — con la letra pequeña honesta**:
  React llama `componentDidCatch` DESPUÉS de comprometer el árbol de
  reserva (no antes: no hay forma de ejecutar efectos secundarios antes del
  commit en una frontera de error), así que "antes del fallback" se
  entendió como "en el instante del fallo, antes de que la persona pueda
  hacer nada" — no literalmente antes del primer pintado de la tarjeta.
  Documentado así para no reclamar un orden que React no permite.
- El documento vivo que se está editando (el estado interno de
  `Layout3DEditor`) sigue sin ser alcanzable desde fuera — tocarlo exigiría
  el monolito. Lo alcanzable sin tocarlo es `documentPort`, la única puerta
  de red del editor: `wrapDocumentPortForCrashRecovery`
  (`document-lifecycle/crash-recovery-port.ts`) envuelve `saveContent` (no
  `saveArchive`: viaja como Blob gzip ya codificado, decodificarlo en cada
  guardado grande para capturarlo no vale la pena — limitación documentada,
  no escondida) y guarda el último documento+versión que de verdad viajó
  hacia el servidor. En `onError` de la frontera, ese último snapshot se
  escribe al diario de recuperación con `saveCadRecovery`. **No sustituye**
  la cola de checkpoint continua que YA corre dentro del monolito mientras
  el documento está sucio (`Layout3DEditor.tsx:4152-4174`,
  `createCadCheckpointQueue`): es un segundo intento para el caso en que la
  caída ocurrió antes de que esa cola alcanzara a escribir.
- El envoltorio recibe un CALLBACK, no un objeto `ref` de React: pasar el
  `ref` en sí a una función durante el render disparaba
  `react-hooks/refs` («Passing a ref to a function may read its value
  during render»), aunque la función nunca LO LEE, sólo lo escribe más
  tarde de forma asíncrona. Con el callback (`onSaveContent`) el módulo
  queda además puro y sin ninguna dependencia de React — se prueba con un
  `sink` de mentira, sin mocks.
- **`components/ui/ErrorBoundary.tsx`**: nuevo prop opcional `extraActions`
  (junto a Reintentar y Reportar; ignorado en `compacta`, donde no cabe).
  Aditivo: ningún consumidor existente cambia de comportamiento sin pasarlo.
- **`components/cad/studio/EditorCrashRecoveryAction.tsx`**: la acción que
  usa ese hueco. Lee el diario de recuperación (`loadCadRecovery`) y
  exporta el documento recuperado a DXF (`exportCadDocumentDxf`,
  `lib/cad/dxf-document-export.ts` — ya existía, sólo se reutiliza) con
  descarga directa. Fix-or-hide en sus tres estados: sin punto de
  recuperación lo dice, si falla la lectura lo dice y aclara que el
  documento del servidor no se tocó, y el resultado avisa que puede no
  incluir los últimos cambios sin guardar — nunca promete más de lo que
  entrega.
- **`CadStudioHost.tsx`**: monta `<ErrorBoundary zona="El editor" ...>`
  alrededor de `<Layout3DEditor>`, construye `recoveryScope` con los mismos
  campos que el monolito usa internamente (`tenantId`, `userId`,
  `projectId`, `model`, `revision` — sin `buildingId`, que en Design no
  existe) y pasa el `documentPort` envuelto.
- Specs nuevas: `crash-recovery-port.spec.ts` (5 casos: captura, sobrevive a
  un guardado que falla, `saveArchive` no captura —documentado—,
  `open`/`versionConflict` intactos, un callback roto no impide el
  guardado real), `error-boundary.spec.ts` ampliado (`extraActions` se pinta
  fuera de `compacta` y se ignora dentro), `EditorCrashRecoveryAction.spec.ts`
  (sin `scope` no pinta nada; con `scope`, el estado inicial no adelanta un
  resultado que no se pidió — las transiciones async quedan fuera de las
  suites `tsx` sin DOM, anotado en el spec).
- Un aviso nuevo de `react-hooks/refs` en `CadStudioHost.tsx` (1): falso
  positivo medido (el callback sólo se invoca de forma asíncrona dentro de
  `saveContent`, nunca durante el render) y registrado en
  `lint-budget.json` vía `--update` — el trinquete de T-72(f) lo hace
  visible archivo por archivo en vez de esconderlo en un total.
- Verificado: `npx tsc --noEmit` (Node 20) limpio. `npx eslint` limpio salvo
  el aviso anotado. Specs nuevas verdes de una en una
  (`crash-recovery-port.spec.ts`, `error-boundary.spec.ts`,
  `EditorCrashRecoveryAction.spec.ts`). Suite completa: 628/628 verdes
  (antes 627 tras T-13; +2 aquí: `crash-recovery-port.spec.ts`,
  `EditorCrashRecoveryAction.spec.ts` — `error-boundary.spec.ts` amplía uno
  ya existente).

---

## T-75(c) · `openDatabase` no maneja `blocked`

**Terminada cuando:** un `indexedDB.open` bloqueado por otra pestaña
RECHAZA con un error identificable en vez de colgar la promesa para
siempre, y el aviso ya visible de la cola de checkpoint (T-72h lo
reutiliza) lo muestra sin que haga falta tocar el monolito.

**Estado: ARREGLADA.**

- `CadRecoveryBlockedError` nueva en `lib/cad/cad-recovery.ts`. El cableado
  de `onblocked` se extrajo a `openDatabaseRequestSettled` (exportada,
  recibe la forma mínima `OpenDatabaseRequestLike`) para poder probarlo sin
  DOM ni `indexedDB` real: Node no lo tiene y este repo no trae un
  polyfill, así que la prueba construye un objeto de cuatro campos que
  simula el evento `blocked` que un navegador dispararía.
- **Por qué no hizo falta tocar el monolito para que el aviso se vea**: el
  `onError` de la cola de checkpoint en `Layout3DEditor.tsx:4175-4182` YA
  muestra un aviso genérico para cualquier error que no sea
  `CadRecoveryQuotaError` — sólo faltaba que `openDatabase` alguna vez
  LLEGARA a rechazar. Con el fix, el mismo camino existente hace visible el
  bloqueo sin una sola línea nueva ahí.
- **Lo que SÍ queda para petición**: la ruta de LECTURA
  (`loadCadRecovery(recoveryScope).catch(() => undefined)`,
  `Layout3DEditor.tsx:4124-4146`, la comprobación de si hay un borrador al
  abrir) sigue tragándose cualquier error en silencio — a propósito, para
  no asustar con un aviso cuando lo que falla es sólo "revisar si había un
  borrador viejo". Extender esa rama para distinguir "no hay borrador" de
  "no se pudo ni comprobar" es un cambio de UX del monolito, no mío.
- Specs nuevas en `cad-recovery.spec.ts` (4 casos): `onblocked` rechaza con
  el error correcto y no cuelga; el mensaje es legible, no un código
  interno; un `onsuccess` tardío tras el rechazo no revienta (resolver una
  promesa ya asentada es un no-op); el camino feliz sigue resolviendo.
- Verificado: `npx tsx src/lib/cad/cad-recovery.spec.ts` → 6/6. `eslint` y
  `typecheck` limpios.

---

## T-75(b) · Abrir el dibujo caído no ofrece ni reintentar ni el borrador local

**Terminada cuando:** la pantalla de error de
`app/studio/[documentId]/page.tsx` (fuera del monolito, mío) ofrece
"Reintentar" sin recargar la página cuando el fallo es de red/servidor (no
cuando es "eliminado" o "sin permiso", donde reintentar no cambiaría nada),
y reutiliza `EditorCrashRecoveryAction` (T-72h) para ofrecer el último
borrador local si existe — con su honestidad fix-or-hide intacta cuando no
existe.

**Estado: ARREGLADA.**

- Contador de reintento (`retryToken`) en la dependencia del efecto de
  apertura: "Reintentar" vuelve a pedir el documento sin duplicar la lógica
  de apertura en un segundo manejador.
- `recoveryScope` reconstruido con los mismos `model`/`revision` fijos con
  los que `CadStudioHost` abriría este documento si el servidor
  respondiera (`documentId`, `"DOCUMENT"`) — mismo patrón que T-72(h).
- Sólo se ofrecen las dos salidas en `offline`/`error`: en `deleted` o
  `forbidden` reintentar no puede cambiar nada y ofrecerlo sería prometer
  una salida que no existe.
- Golden nuevo `161-cad-plano-caido-reintentar.spec.ts`: la apertura falla
  con 500, aparece "Reintentar" y la acción de recuperación dice
  honestamente que no hay ningún borrador (sin sembrar IndexedDB —
  cobertura de la lectura real ya está en `cad-recovery.spec.ts` y
  `EditorCrashRecoveryAction.spec.ts`); al pulsar "Reintentar" la segunda
  petición sí llega y el aviso desaparece.
- Verificado: `npx tsc --noEmit` limpio; `eslint` limpio salvo un
  `react-hooks/set-state-in-effect` YA presupuestado antes de este cambio
  (no nuevo). Golden 161 verde contra build de producción
  (`E2E_PROD=1`).

---

## T-75(g) · Nadie puede borrar un plano

**Terminada cuando:** el tablero (`app/dashboard/`, mío) tiene un botón que
llama a `documentsRepository.archive`/`designClient.documents.archive`
(`DELETE /v1/cad/documents/:id`, borrado suave — conserva historial y
publicaciones), con confirmación explícita, visible sólo con el permiso
que el servidor exige de verdad.

**Estado: ARREGLADA.**

- `DELETE /v1/cad/documents/:documentId` exige `cad:admin`
  (`cad.controller.ts:169-170`), NO `cad:edit` — el botón se gatea con
  `auth.permissions.includes("cad:admin")`, no con el `canEdit` que ya
  existía para crear/editar. Antes de este cambio,
  `documentsRepository.archive` no tenía NINGÚN llamador en todo el árbol
  (`grep` confirmado).
- Cada tarjeta de documento era un único `<button>` que ocupaba toda la
  superficie (abrir = todo el clic); no se puede anidar un botón dentro de
  otro botón (HTML inválido, inaccesible), así que la tarjeta pasó a ser un
  `<div>` con el botón de abrir (mismo tamaño, mismo comportamiento) y un
  botón de borrar aparte, con su propio `aria-label` con el nombre del
  documento.
- Confirmación con la primitiva `Modal` (nunca un `confirm()` nativo ni un
  diálogo hecho a mano): dice explícitamente que es un borrado SUAVE ("se
  ocultará de tu tablero... se conserva su historial"), no amenaza con una
  pérdida que no ocurre.
- Golden nuevo `160-cad-borrar-documento.spec.ts`: el botón no aparece sin
  `cad:admin` (implícito: el fixture `loginAsStandaloneOwner` lo concede,
  así que el golden prueba el camino positivo completo); pide
  confirmación (cero llamadas al primer clic); Cancelar no llama a nada;
  confirmar sí llama al `DELETE` real y quita la tarjeta de la lista.
- Verificado: `npx tsc --noEmit` limpio, `eslint` limpio, `check:lint-budget`
  sin cambios (0 avisos nuevos). Golden 160 verde contra build de
  producción.

---

## T-75(f) · La importación muere a los 45 s aunque esté avanzando

**Terminada cuando:** el temporizador es de ATASCO (silencio), no de plazo
total — un archivo grande que sigue mandando progreso no muere solo — y al
atascarse de verdad la interfaz ofrece "Seguir esperando" / "Cancelar" en
vez de rechazar en silencio.

**Estado: ARREGLADA.**

- `lib/cad/document-import-client.ts`: el reloj fijo de 45 s se sustituyó
  por `createStallWatchdog(stallMs, onFire)`, que se REARMA en cada
  `onProgress` — sólo dispara cuando no llega NINGUNA señal de vida durante
  `stallMs`. Extraído a una función pura, aislada del `Worker` real, para
  poder probarlo en la suite `tsx` (Node no tiene la API de Worker del
  navegador).
- Nuevo `onStalled?: (resume: () => void) => void` en las opciones de
  `importDocumentFile`. Con él, el disparo del reloj YA NO rechaza la
  promesa: llama a `onStalled(resume)` y espera. **Sin `onStalled` el
  comportamiento es exactamente el de antes** (rechaza sola) — no rompe a
  ningún llamador que no pida el cambio.
- `app/dashboard/import-status.tsx` (mío): nuevo estado `"stalled"` con dos
  acciones — "Seguir esperando" (llama a `resume()`, vuelve a
  `"running"` con el último progreso conocido) y "Cancelar" (el
  `AbortController` que ya existía). Tokens del sistema (`bg-warning/10`,
  `text-warning-ink`) en el bloque nuevo — el resto del archivo ya
  usaba color de Tailwind crudo antes de este cambio y no se tocó: no es
  parte de esta ficha, y tocarlo habría sido alcance de más
  ("ya que estoy").
- `app/dashboard/page.tsx`: `importDocument` guarda el último
  progreso/etapa vistos (`lastProgress`/`lastStage`) para que la tarjeta de
  atasco no aparezca vacía, y pasa `onStalled` a `importDocumentFile`.
- `document-import-client.spec.ts` (nuevo): reproduce el defecto exacto
  —progreso constante nunca dispara, aunque el total supere `stallMs`—,
  el caso real (sin señales, dispara una vez), "Seguir esperando" reinicia
  el reloj sin dejarlo en bucle ni muerto, y `stop()` es definitivo.
- Verificado: `npx tsx document-import-client.spec.ts` → OK (4 bloques).
  `typecheck` y `eslint` limpios (mismo aviso pre-existente de
  `dashboard/page.tsx`, no nuevo). `check:lint-budget` sin cambios.

---

## T-75(h) · El contenedor de avisos no se anuncia y está pintado a mano

**Terminada cuando:** el contenedor de toasts (`contexts/ToastContext.tsx`)
tiene `aria-live` desde el primer render (no sólo cuando ya hay un aviso),
cada tarjeta lleva su `role` según el tipo, y ningún color de Tailwind
crudo sobrevive en el archivo.

**Estado: ARREGLADA.**

- Dos regiones vivas en vez de una: `aria-live="polite"` para
  éxito/información (no interrumpen), `aria-live="assertive"` para errores
  (si el guardado falló, la persona tiene que enterarse YA). Antes no
  había ningún `aria-live` ni `role` en todo el archivo — un fallo de
  guardado era mudo para quien no mira la pantalla.
- Cada tarjeta (`Toast`, extraída del `.map` inline para poder tipar sus
  props) lleva `role={t.kind === 'error' ? 'alert' : 'status'}`.
- Colores de Tailwind crudos reemplazados por tokens en TODO el archivo,
  no sólo lo tocado por esta ficha: `bg-white/85 dark:bg-neutral-900/85` →
  `bg-popover/85`; `border-black/5 dark:border-white/10` → `border-border`;
  `text-black dark:text-white` / `text-gray-600 dark:text-gray-300` →
  `text-foreground` / `text-muted-foreground`; `rounded-2xl` →
  `rounded-card`; la sombra a mano → `shadow-floating`; los tres iconos
  (`rose-500`/`blue-500`/`emerald-500`) → `text-danger`/`text-primary`/
  `text-success` (gráficos: el token sólido, no la variante `-ink`).
- `check:contrast` no necesitó pares nuevos: `bg-popover`/`text-foreground`/
  `text-muted-foreground` ya estaban medidos.
- `toast-context.spec.ts` (nuevo, `contexts/`): confirma por marcado
  estático que las dos regiones existen desde el primer render (sin
  ningún aviso montado — SSR no puede disparar el `setState` de un toast
  creado durante el propio render de un hijo, así que esa parte se anota
  como límite del spec, no como cobertura fingida) y que ningún color
  crudo sobrevive en el HTML; el `role` por tarjeta y los tres tokens de
  icono se verifican por lectura del código fuente, honestamente marcado
  como verificación estática y no de comportamiento.
- Verificado: `npx tsx toast-context.spec.ts` → OK. `typecheck`/`eslint`
  limpios. `check:contrast` sin regresión. Usos existentes de
  `data-testid="app-toast"` en `e2e/real/errores-en-espanol.spec.ts` y dos
  specs de `e2e/auditoria/` revisados: ninguno depende del orden entre
  contenedores (usan `querySelectorAll`/`allInnerTexts`, agnósticos al
  agrupamiento en el DOM), así que la división en dos regiones no los rompe.

---

## T-75(d) · «Dibujar funciona sin red» sólo para lo ya cargado

**Terminada cuando:** la fila `dibujar-acotar-modelar` de la matriz sin red
deja de decir «Nada» donde antes mentía, y un spec falla si alguien la
revierte sin arreglar también el hueco real.

**Estado: PARCIAL — la honestidad se arregló; el precacheo real se
propone a `BACKLOG.md` (P-03 en `F9-peticiones.md`), no se construye aquí.**

- **Por qué no construí el precacheo de verdad**: 108 módulos de
  comandos en carga diferida, y `SW_PRECACHE_URLS`
  (`service-worker-policy.ts`) sólo lleva el cascarón — `/_next/static/*`
  se cachea DESPUÉS de pedirse, nunca antes. Arreglarlo de verdad exige (1)
  una decisión de PRODUCTO sobre qué es "el núcleo" de comandos, (2) leer
  el manifiesto de build para resolver los hashes de chunk reales —no se
  conocen hasta que el build termina, igual que las fuentes—, y (3)
  `cache.addAll` es todo-o-nada por diseño: un chunk del núcleo que falle
  en `install` tumba el offline ENTERO, incluido el cascarón que hoy
  funciona. Construirlo a ciegas dentro de esta ola es exactamente el tipo
  de cambio de alto riesgo y bajo margen de revisión que la casa pide
  evitar («un service worker que no instala no emite ningún error visible
  — simplemente no existe»). Propuesta completa en P-03.
- **Lo que sí se corrigió, con evidencia**: `offline-capability-matrix.ts`,
  fila `dibujar-acotar-modelar`, decía `seNota: "Nada. Es el único trozo
  del producto donde la red no se echa de menos."` — falso para un comando
  que la pestaña nunca cargó. Corregida para nombrar la condición real
  (código YA descargado) siguiendo el MISMO patrón que la fila
  `abre-el-estudio-sin-red` ya usaba para su propia condición («una
  primera visita sin conexión sigue sin poder abrir nada») — no inventé un
  veredicto nuevo (`funciona-sin-red`/`degrada-y-reintenta`/
  `requiere-backend` son los tres que existen y cambiar esa enumeración
  tendría eco en toda la matriz).
- `offline-capability-matrix.spec.ts`, bloque 14 (nuevo): falla si la fila
  vuelve a decir "Nada" sin decir nada más, y si `porque` deja de mencionar
  la condición de carga diferida.
- Verificado: `npx tsx offline-capability-matrix.spec.ts` → 14 bloques
  verdes (antes 13), 79/79 endpoints clasificados, 1099 archivos barridos.
  `typecheck`/`eslint` limpios.

---

## T-75(a) · El aviso mejor escrito del producto dura doce segundos

**Terminada cuando:** existe, probado y fuera del monolito, un panel de
estado del guardado persistente (sin temporizador) con acciones
Reintentar/Exportar DXF/Detalles, y una petición con el diff exacto para
montarlo sustituyendo el toast de 12 s.

**Estado: COMPONENTE CONSTRUIDO Y PROBADO — el montaje es PETICIÓN
(P-04).**

- `components/cad/studio/SaveStatusPanel.tsx`: recibe `issue: {kind,
  title?, message} | null` y `onRetry`/`onExportDxf` OPCIONALES —
  sin ellos se degrada a texto completo + "Detalles" en vez de fingir
  botones que no harían nada (fix-or-hide aplicado a sus propias
  acciones). `role="alert"`, `data-testid="cad-save-status-panel"`. Sin
  ningún `setTimeout`/`setInterval` en el archivo — comprobado por el
  propio spec, que es la única forma honesta de probar una ausencia.
- **Por qué no lo monté yo mismo, con la evidencia de por qué es
  arriesgado**: `CadStatusBar.tsx` (donde vive `saveIssue`, ya fuera del
  monolito) tiene su altura ajustada al píxel contra cuatro goldens (19,
  67, 68, 72 — 36 px de barra, documentado en el propio archivo), y ese
  mismo archivo cuenta que un aviso flotante mal posicionado
  (`fixed right-3 top-[11.5rem]`, la barra de llamada) ya tapó un botón
  real una vez. Decidir dónde flota sin poder correr esos cuatro goldens
  para confirmar la elección es adivinar sobre una superficie que ya se
  midió mal antes.
- Extensión SÍ hecha yo mismo, por ser aditiva y sin riesgo: `CadStatusBar.tsx`
  → `CadStatusBarSaveState.saveIssue` gana `title?: string` (el monolito
  no lo rellena todavía; type-only, cero cambio de comportamiento).
- Petición **P-04** (`F9-peticiones.md`) con el diff exacto de los TRES
  cambios que hacen falta en `Layout3DEditor.tsx`: (1) el tipo `saveIssue`
  gana `title`, que `describeCadSaveFailure` ya produce y hoy se descarta;
  (2) el manejador principal pasa `title` al estado y dejaría de sacar el
  toast; (3) **hallazgo de paso**: el SEGUNDO manejador de guardado
  (`runCanonicalSave`) no llama a `setSaveIssue` en NINGÚN caso —sólo
  saca el toast—, así que ese camino de fallo quedaría sin aviso
  persistente aunque el panel exista si no se corrige a la vez. Los tres
  cambios están en la petición, con las líneas exactas.
- `SaveStatusPanel.spec.ts`: 11/11 — sin `issue` no pinta nada; con
  `issue` se anuncia y usa un título genérico por tipo si no llega uno
  explícito; el título explícito gana sobre el genérico; sin callbacks no
  aparece ningún botón; con los dos, aparecen los dos; ningún temporizador
  en el archivo.
- Verificado: `npx tsx SaveStatusPanel.spec.ts` → 11/11. `typecheck`/`eslint`
  limpios.

---

## Arreglo de CI · `/studio/demo-123` se pasó del presupuesto de bundle

**Qué pasó:** el job "Contrato · Build · Test · Lint · Smoke" del PR falló
tras el commit de T-72(h) — `scripts/perf/bundle-budget.mjs` midió
`/studio/demo-123` en 315,5 KB gzip contra un techo de 288,8 KB (+26,7 KB).

**Causa:** `CadStudioHost.tsx` y `EditorCrashRecoveryAction.tsx` importaban
`cad-recovery.ts` (el códec del diario IndexedDB) y `dxf-document-export.ts`
(el escritor DXF completo) como imports estáticos de nivel de módulo. Ambos
sólo se usan si el editor se cae — pero un import estático viaja en el JS
de primera carga de CUALQUIER visita a `/studio`, se caiga el editor o no.

**Arreglo:** los dos módulos ahora llegan por `import()` dinámico en el
punto de uso — `handleEditorCrash` (fuego y olvido, atrapa el rechazo) en
`CadStudioHost.tsx`, y dentro del manejador de clic `descargar` en
`EditorCrashRecoveryAction.tsx` (con `Promise.all` para los dos). Los tipos
que sí hacen falta en las firmas siguen llegando por `import type`, que no
genera código ni bytes.

**Verificado tras el arreglo:**
- `npx tsc --noEmit` (monorepo, Node 20): limpio.
- `check-lint-budget.mjs`: 490/490 dentro del presupuesto (el único aviso
  nuevo en `CadStudioHost.tsx` ya estaba presupuestado en 1).
- `check-monolith-budget.mjs` y `check-contrast.mjs`: OK.
- Build de producción fresco + `E2E_PROD=1 node scripts/perf/bundle-budget.mjs`:
  `/studio/demo-123` → 272,8 KB gzip contra 288,8 KB de techo (16,0 KB de
  margen; antes del arreglo estaba 26,7 KB POR ENCIMA). Presupuesto de
  bundle respetado en las 14 rutas medidas.
- `npx turbo run test --filter=web` (Node 22): 632/632 specs verdes.

---

## T-73 · Que se pueda usar sin ratón, sin vista y en dos idiomas

**Terminada cuando:** cada sub-hallazgo del ticket (a-j) queda en uno de
tres estados, nunca en un cuarto: VERIFICADO (ya cumplía y ahora hay una
prueba que lo demuestra), ARREGLADO (con su spec), o PETICIÓN (el diff
exacto en `F9-peticiones.md` para quien tiene permiso de tocar
`Layout3DEditor.tsx`). Nada se deja "visible y sin verificar".

Ticket completo: diez hallazgos (a-j), la mitad viven totalmente o en
parte dentro del monolito de 18 453 líneas. Esta ronda cierra lo que se
puede cerrar sin tocarlo y deja petición con diff exacto para el resto.
`docs/execution/auditoria-fable/dimensiones/20-accesibilidad-i18n.md` ya
traía casi todo medido con número de línea — se verificó que las medidas
seguían vigentes antes de tocar nada, no se dieron por buenas a ciegas.

**(d) `page-has-heading-one`/`region` se colaban del gate.**
`axe-estudio.spec.ts` sólo fallaba en `serious|critical`; `moderate` se
imprimía y nunca reprobaba — la instrucción explícita del ticket. Ahora
`moderate` reprueba, con UNA excepción nombrada (`region`, ver abajo), no
una lista que crece.

- `page-has-heading-one`: RESUELTO, fuera del monolito.
  `CadStudioHost.tsx` envuelve todo en `<main className="contents"
  aria-label="Estudio de dibujo">` (sin caja nueva: `display:contents`, cero
  riesgo de golden roto) con un `<h1 className="sr-only">Editor de
  planos — Valle Design</h1>` dentro. Verificado con axe de verdad (Chromium,
  `playwright test e2e/a11y/axe-estudio.spec.ts`, ambos temas): el aviso
  desapareció por completo, no se limitó a esconderlo.
- `region`: casi resuelto por el mismo `<main>`, pero no del todo —
  `Layout3DEditor.tsx` pinta TODO su marcado con `createPortal(...,
  document.body)` (línea ~18451) para escapar un `backdrop-filter` que si
  no atraparía sus overlays `position:fixed`. Un portal de React sale del
  DOM aunque siga en el árbol de React, así que el editor portado queda
  fuera del `<main>` — confirmado con los targets exactos que devuelve axe
  (`select[aria-label="Estado de aprobación del plano"]` y dos más, todos
  del monolito). Petición **P-07** con el diff de una línea (cambiar el
  segundo argumento del `createPortal`) y el porqué no basta con lo de
  fuera. `region` queda como la ÚNICA excepción nombrada en el gate, con
  comentario que apunta a la petición — no una lista que crece.

**(f) La cinta llega sin panel.** `CadRibbonPanel.tsx` (fuera del
monolito) ganó `role="group"` + `aria-labelledby` apuntando al rótulo YA
visible ("Dibujo", "Modificar"…) — sin duplicar texto, sin `role="toolbar"`
porque ese rol EXIGE navegación con flechas que la cinta no implementa
(prometerlo sin dar el teclado sería peor). `CadRibbonPanel.spec.ts`
(4/4) prueba el enlace con un panel REAL de `CAD_RIBBON_DATA` — uno
inventado revienta el render porque los iconos exigen cobertura total. El
KeyTip (Alt+letra) y la navegación con flechas de verdad quedan fuera de
esta ronda: son un rediseño del componente, no un arreglo de atributos.

**(g) El lienzo no existe para un lector; la lista se corta en 20.** La
lista de entidades (`CadNativeEntityList.tsx`, ya fuera del monolito desde
antes) ganó `role="list"`/`role="listitem"` y un `aria-label` por fila
(`"${nombre} — capa ${capa}"`) que no depende de que el navegador inserte
un espacio audible entre los dos `<span>` visuales. El corte a `limit`
sigue siendo honesto (ya lo era: "y N más", nunca desaparece en
silencio) — eso no cambió. `CadNativeEntityList.spec.ts` (6/6). El lienzo
en sí (el `<div>` de Three.js, dentro del monolito) sigue sin
`tabIndex`/`role`/`aria-label`: eso es la lista de espera P1-FE-canvas,
una superficie completa (navegación de entidades por teclado sobre un
canvas WebGL), no una petición de una línea — se deja fuera de esta ronda
por honestidad, no se finge un arreglo cosmético.

**(h) El prompt vivo no se puede releer.** Tres huecos en
`CadCommandLine.tsx` (fuera del monolito), los tres reales:
  - `aria-describedby` en la caja de entrada, enlazado al `id` fijo del
    diálogo (`cad-command-line-log`).
  - El diálogo (`role="log"`) gana `tabIndex={0}`: F2 (nuevo, no existía)
    le manda el foco; Escape lo devuelve a la caja. El bloqueo de puntero
    (`pointer-events-none`, deliberado: el muelle no debe robarle el ratón
    al lienzo) no toca teclado ni foco, así que nada de esto lo rompe.
  - **Hallazgo de paso, real, no cosmético**: la clave de React de cada
    renglón del historial era `` `${index}-${entry.text}` ``. Con
    `history` recortado a los últimos 60 (`MAX_HISTORY`,
    `command-engine-host.ts`), CADA renglón cambia de índice al caer el
    más viejo del principio → React remonta el diálogo COMPLETO en cada
    paso de cada comando → un lector de pantalla en la región viva
    (`aria-live="polite"`) vuelve a anunciar los sesenta renglones enteros
    por cada línea nueva, no sólo la que se sumó. Arreglado con una
    identidad estable: `CadCommandLineEntry.id` (contador monótono en
    `command-engine-host.ts`), usada como clave. `CadCommandLine.spec.ts`
    (7/7, sin jsdom: marcado desde `renderToStaticMarkup` + lectura de
    fuente para F2 y la clave, mismo patrón que el resto de specs de esta
    carpeta que no pueden montarse de verdad).

**(j) `forced-colors`/`prefers-contrast` no existían.** Dos bloques
nuevos en `globals.css`, SIN `@layer` a propósito — una regla sin capa
gana siempre sobre `@layer base`/`utilities` en la cascada, que es la
única forma de que esto devuelva el foco cuando un componente ya puso
`outline-none` y sólo ofrece un `ring-*` de sombra (invisible en
`forced-colors`, que no reescribe `box-shadow`). NO es soporte completo de
alto contraste (eso es una ola completa: cinta, paletas y lienzo contra
`Canvas`/`CanvasText`) — es la pieza mínima verificable: que el foco de
teclado se siga viendo cuando el sistema reasigna colores o pide más
contraste. Dicho así en el propio comentario, sin sobreprometer.

**(d, trinquete de foco visible) 27 → 9, y el trinquete SÓLO baja.**
`foco-visible.spec.ts` contaba controles con `outline-none` y ningún
sustituto visible. Las 18 que vivían fuera del monolito (ocho archivos de
paletas/diálogos, `CadCommandLine.tsx`, `Modal.tsx`) ganaron
`focus-visible:ring-2 focus-visible:ring-ring`; `foco-visible-budget.json`
bajó de 27 a 9 con `--update` (el script en sí impide subirlo). Las 9
restantes están TODAS en `Layout3DEditor.tsx` — petición **P-05**, diff
mecánico línea por línea, mismo patrón exacto.

**De paso, un bug real, no cosmético:** `CadDialogShell.tsx` (el marco
común de los ocho cuadros modales) cerraba el Escape con
`event.stopPropagation()`. Con DOS cuadros abiertos a la vez —ambos
escuchan `keydown` en `document`—, `stopPropagation` no impide que el
OTRO manejador en el mismo nodo también dispare: un solo Escape cerraba
los dos cuadros de un tirón. Cambiado a `stopImmediatePropagation()`.
`CadDialogShell.spec.ts` (7/7) prueba el marcado base
(`role="dialog"`/`aria-modal`/`aria-labelledby`) y, por lectura de fuente,
que no quede ningún `stopPropagation` suelto que reintroduzca el cierre
doble.

**Lo que queda en petición, sin arrancar en esta ronda** (diffs exactos en
`F9-peticiones.md`):
- **P-05**: los 9 `outline-none` del monolito (mecánico).
- **P-06**: la paleta Ctrl+K (T-73/i) — cero atributos de accesibilidad
  hoy (`role`, `aria-modal`, `aria-label`, trampa de foco); el patrón de
  `CadDialogShell.tsx` ya resuelto está listo para copiar.
- **P-07**: el `createPortal(..., document.body)` que deja `region` sin
  cerrar del todo (arriba).

**Fuera de alcance de esta ronda, y por qué (no es un olvido):**
- **(a)** Los atajos de palabra clave en español (`[Cerrar/desHacer/Arco...]`)
  chocan con los ingleses de AutoCAD (`[Arc/Halfwidth/Length...]`) en ~550
  pares de `keyword`/`shortcut` repartidos por `lib/cad/engine/commands/*.ts`.
  Es un cambio de VOCABULARIO del producto (decisión de vara, no de código:
  ¿en español o en inglés?), no un bug — arreglarlo mal (traducir todo sin
  criterio) sería peor que dejarlo. El campo `label` para esto ya existe en
  `CadKeyword` (`command-types.ts`) y se usa en 12/550 casos: la
  infraestructura está, falta la decisión de producto y luego el trabajo
  mecánico de aplicarla.
- **(b, c)** Cobertura i18n real: 0,4 % (33 claves de 8 481 frases,
  medido con el instrumento propio del repo,
  `src/i18n/catalog-contract.spec.ts`). No es una ficha de una tarde: es
  traducir 8 448 cadenas repartidas en 625 archivos. Fuera de alcance de
  cualquier frente individual sin una decisión de campaña sobre CÓMO
  (¿extracción automática con revisión humana? ¿por módulo, empezando por
  cuál?).
- **(g, canvas)** Accesibilidad real del lienzo WebGL para lector de
  pantalla (navegar entidades sin ratón sobre un `<canvas>`) es una
  superficie nueva completa, no un atributo que falta.

**Verificado:** `npx tsc --noEmit` limpio. `check-lint-budget.mjs`: 490/490
(sin avisos nuevos). `check-monolith-budget.mjs` y `check:contrast` OK.
`npx turbo run test --filter=web`: 636/636 specs verdes (4 specs nuevas:
`CadDialogShell`, `CadRibbonPanel`, `CadNativeEntityList`,
`CadCommandLine`). `foco-visible.spec.ts`: 9/9 dentro del nuevo techo.
`playwright test e2e/a11y/axe-estudio.spec.ts --project=chromium`: 2/2
verdes, con el log de avisos no bloqueantes confirmando que
`page-has-heading-one` desapareció y `region` quedó reducido a los tres
nodos del portal (proyecto `firefox` no se pudo correr en esta máquina —
falta el binario, no es una regresión de este cambio).

---
