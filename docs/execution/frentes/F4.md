# F4 · Papel y entrega — bitácora

Frente F4 de la campaña «El lunes de un arquitecto» (2026-09-06), sesión
Sonnet 5 `session_01T4o5ne7gRrTndZ6G4Fmiux`, rama `claude/f4-papel-entrega`.

**Nota de arranque (corregida tras el rebase — ver más abajo).**
`docs/execution/auditoria-fable/PROMPT_MAESTRO_FABLE.md`,
`00-PUNTO-DE-PARTIDA-VERIFICADO.md`, `05-layouts-plot.md` y `10-interop.md` —
la lectura previa exigida por §0 — NO existían en el `main` que este
contenedor tenía clonado al arrancar (commit `1478471`, de agosto). Confirmé
esa ausencia con `git ls-remote`/`git log` sobre ese clon, y concluí que
"no existen" — conclusión INCORRECTA: el clon estaba desactualizado 127
commits (ver la sección "Rebase" más abajo). Esos documentos SÍ existen en el
`main` real (`4800017733da6849e267096519fe7687f1c41c1d`, PR #192/#193), sólo
que ahora viven en `docs/execution/auditoria-fable/dimensiones/` tras la
higiene documental de la Ola 0. Opción tomada mientras tanto (antes de saber
esto): seguir con el encargo tal como llegó inlineado en el mensaje de
arranque, que ya traía las fichas T-11/T-19/T-30–T-36 completas. No hizo
falta reconciliar nada al descubrir el error: las fichas trabajadas hasta el
rebase coinciden con lo que el prompt maestro real describe para F4.

---

## Rebase sobre `origin/main` real (2026-09-06, tras aviso del coordinador)

El contenedor de esta sesión clonó `main` en `1478471` (agosto), y mi primer
commit colgó de ahí. El coordinador avisó (Routine, notificación) que el
`main` real estaba en `4800017` — 68 commits por delante de mi base tras
`git fetch origin main`. El repo local además era un clon SHALLOW: `git
merge-base` fallaba sin ancestro común hasta `git fetch --unshallow`.

Pasos: `git fetch --unshallow origin` → `git stash` (T-36 en curso, sin
commitear) → `git rebase origin/main` → dos conflictos, los dos de
alineación de texto, ninguno de lógica real:
1. `docs/governance/assisted-development-log.json` — main añadió su propia
   entrada (`DWG-M4-ENCABEZADO-R2010-20260831`) en el mismo punto de
   inserción que la mía. Reconstruido en Python: entradas de `main` intactas
   (verificado `entries[:90] == head_data['entries']`) + mi entrada al
   final.
2. `apps/web/src/lib/cad/dxf-cad-document.ts` — sólo el COMENTARIO sobre el
   import cambió en `main` (mención de "techo 961"); mi cambio de código
   (`flatMap` con `cadOpeningToDxfPrimitives`) no chocó con nada y se
   auto-fusionó limpio.

`git stash pop` sobre el T-36 en curso chocó una vez más en `paper-space.ts`:
`main` había extraído `blockPresentation`/`styleFor`/`unitToMm` a
`paper-space-style.ts` en su propia historia; resuelto manteniendo AMBOS
bloques de import (el de `main` y el mío, de `layout/annotative-scale.ts`).
Verificado después que mis T-19·3/4/5 (`plot === false`, `paperSpaceEntityIds`,
`clipPolygon`) seguían intactos en el archivo re-fusionado.

**Una regresión real, no de fusión:** `npm ci` + rebuild reveló que
`paper-space-image.spec.ts` (nuevo en `main`, Ola H) fallaba: su aserción
"sin recorte no hay `W n` en la página" asumía —correctamente, ANTES de mi
T-19·5— que NINGUNA ventana recortaba nunca. Con el recorte real que T-19·5
añade, toda página lleva como mínimo el `W n` de su ventana, así que "cero
recorte" ya no es la señal correcta para "la imagen no tiene recorte propio".
Arreglé el spec (no lo relajé): ahora cuenta cuántos `W n` hay — 1 sin
recorte de imagen (el de la ventana), 2 con él (ventana + imagen) — en vez
de afirmar una ausencia que mi propio arreglo, correctamente, invalidó.
Verificado con un script ad hoc (`renderCadPlotPdf` + conteo de `W n`) antes
de tocar el spec, para no editar una aserción a ciegas.

**Verificado tras el rebase:** `npm ci`; `npx turbo run typecheck` (todo el
monorepo, `--force`) → verde (el primer intento sin `--force` dio decenas de
errores de tipos en módulos de mensajería/DWG que no toco — build cacheado
de `contracts`/`design-sdk` desactualizado respecto al contrato real de
`main`, no una regresión); `npx turbo run test --filter=web
--filter=valle-design-api --force` → **625/625 specs web + 801/801 (aparte
de 221 omitidos por infraestructura) specs API**, verdes; `npm run lint` → 0
errores; `npm run check:cad` (con `VALLE_DWG_CORPUS_MIRROR`) → mismos 2
problemas de presupuesto ya pedidos (petición #4, números actualizados:
`cad-document.ts` 804, `paper-space.ts` 939 — ésta última sube más porque ya
incluye T-36, ver más abajo). `git merge-base HEAD origin/main` da
`4800017`, como pedía el aviso. Push con `--force-with-lease` (rama propia,
sin rebasar por nadie más).

---

## 0 · La decisión del emisor de PDF (va primero, como pide el encargo)

**Hallazgo.** El producto tiene, en efecto, dos caminos que terminan en bytes
de PDF, y no se parecen:

1. **El bueno**: `renderCadPlotPdf` en `apps/web/src/lib/cad/plot/plot-pdf.ts`
   (508 líneas, spec propio `plot-output.spec.ts`, `plot-fidelity.spec.ts`,
   goldens `46-cad-layout-plot.spec.ts` y `55-cad-primeros-cinco-minutos.spec.ts`).
   Sabe de incrustación/sustitución de fuentes con aviso, tablas de plumas,
   cajetín paramétrico (`title-block.ts`) y es el que consumen **PLOT**
   (`plot-host.ts` → `buildCadPlotJob` → `renderCadPlotPdf`) y **PUBLISH**
   por conjunto de planos (`sheet-set-publish.ts` → `publishCadSheetSet` →
   el mismo `renderCadPlotPdf`). Ambos comandos comparten además el mismo
   plan vectorial: `buildCadPublishPlan` (`apps/web/src/lib/cad/paper-space.ts`).
2. **El pobre**: `publishSheetSetPdf()`, función de 260+ líneas dentro de
   `Layout3DEditor.tsx` (13617–13878), que importa `jspdf` directamente y
   dibuja cada `command` del mismo `buildCadPublishPlan` a mano — rects,
   líneas, texto — sin tabla de plumas, sin incrustación de fuente (usa
   sólo Helvetica fija), sin escala anotativa, sin manifiesto de pérdidas,
   sin plot-fidelity. Es un renderizador paralelo del MISMO plan que el bueno
   ya sabe pintar bien.

**El botón usa el pobre.** Los dos únicos `onClick` de "Publicar PDF" del
editor (líneas 15720 y 18811 de `Layout3DEditor.tsx`) llaman a
`publishSheetSetPdf()`, no a `CadPlotHost.publish` (que sí llama al bueno).
Hay además un comentario ya en el monolito (línea 12761-12764) que afirma
"La única salida PDF del producto es publishSheetSetPdf" — la propia
documentación en línea confunde cuál es el camino que hay que preservar; el
comentario habla de un PDF ANTERIOR retirado (Fase 65) sin notar que el que
lo reemplazó ES el mismo patrón (jsPDF a mano) que dice haber corregido.

**Por qué el bueno es el bueno:** los dos consumen el mismo plan vectorial
(`buildCadPublishPlan`), así que la única diferencia real es el RENDERER, y
`renderCadPlotPdf` es el único de los dos con specs de fidelidad, manejo de
fuentes declarado y la superficie que T-19/T-31/T-36 de esta ficha van a
corregir. Arreglar plot-fidelity.ts/plot-pdf.ts y dejar que el botón siga
llamando al hand-rolled jsPDF sería arreglarlo tres veces en dos sitios,
exactamente lo que el encargo pide evitar.

**Decisión: unificar.** `publishSheetSetPdf()` debe dejar de importar `jspdf`
y de dibujar a mano; debe llamar a `publishCadSheetSet` (`lib/cad/sheet-set/
sheet-set-publish.ts`, que ya envuelve `renderCadPlotPdf` con cajetín, fuentes
y portada) y descargar los bytes que ésta devuelve. **No cabe en esta
sesión**: la función vive en `Layout3DEditor.tsx`, que es territorio exclusivo
del coordinador. Ver petición #1 en `F4-peticiones.md` con el diff exacto.
Mientras tanto, todo lo que arregle en `plot-pdf.ts` / `plot-fidelity.ts` /
`paper-space.ts` beneficia primero al camino PLOT/PUBLISH (ya wired) y
alcanzará al botón del editor en cuanto la petición se aplique.

Estado: **ARREGLADA la mitad que me toca (decisión documentada + camino bueno
en progreso); PARCIAL en la unificación real del botón** — motivo: requiere
tocar `Layout3DEditor.tsx`, prohibido para mí. Petición enviada.

---

## T-11 (a) · DXF R12 mutilado por `boxOf` antes de discriminar tipo

**Terminada cuando:** `boxOf` sólo se evalúa para `type === 'box' | 'station'`;
ninguna entidad de otro tipo con `x/y/w/h` casuales sale como rectángulo
inventado; toda capa recortada a 31 caracteres, todo texto recortado a 240 y
toda entidad sin proyección DXF quedan declaradas en un manifiesto que viaja
en la respuesta del endpoint; specs verdes; `lint:check` y `check:cad` verdes.

**Arreglado.** `apps/api/src/modules/cad/cad-dxf-export.ts`:
- Reordenado: el `type` de la entidad se decide ANTES de mirar la caja.
  `boxOf` sólo corre dentro de la rama `type === 'box' || type === 'station'`.
- `buildDxfExportInput` ahora devuelve `{ input, warnings }` en vez de sólo
  `CadLayoutDxfInput`. `warnings: DxfExportLossWarning[]` con tres códigos:
  `layer_truncated`, `text_truncated`, `entity_unmapped`.
- `layerOf` avisa cuando el nombre saneado excede 31 caracteres (antes
  recortaba en silencio).
- El texto avisa cuando excede 240 caracteres antes de recortarlo.
- Cualquier entidad que no encaje en ninguna proyección (box/station sin caja
  finita, texto sin posición o sin contenido, o un tipo no soportado) se
  declara en el manifiesto en vez de desaparecer sin rastro.
- `cad.controller.ts` (`exportDxf`, única ruta tocada) desestructura
  `{ input, warnings }` y añade `lossManifest: warnings` a la respuesta —
  siempre presente, vacío cuando no hay pérdida.
- Contrato: `DxfExport.lossManifest` (opcional, aditivo) +
  `DxfExportLossWarning` en `packages/contracts/specs/design-api.v1.yaml`;
  SDK regenerado con `npm run generate --workspace=@valle/design-sdk` (diff
  de +10 líneas, sólo el tipo nuevo).
- `cad-dxf-export.spec.ts`: reescrito para el nuevo shape `{input, warnings}`
  y con 3 casos nuevos: (1) una entidad `wall` con `x/y/w/h` casuales NUNCA
  sale como asset — el defecto central de esta ficha; (2) capa >31 declarada;
  (3) texto >240 declarado.

**Verificado:**
- `npx turbo run test --filter=valle-design-api -- --testPathPatterns=cad-dxf-export` → 6/6 verdes.
- `npx turbo run test --filter=valle-design-api -- --testPathPatterns=cad.controller.spec` → 14/14 verdes (incluye el caso end-to-end de export DXF).
- `npm test --workspace=@valle/design-sdk` → 9/9 verdes (incluye "el SDK generado está al día respecto de los tipos clave").
- `npm run lint:check --workspace=valle-design-api` (Node 20) → 0 errores (255 warnings preexistentes, ninguno en los archivos tocados).
- `npm run typecheck` (Node 20, turbo, 5 paquetes) → verde.
- `npm run check:cad` (Node 20, con `VALLE_DWG_CORPUS_MIRROR` apuntando al clon local de `valle-design-dwg-conformance`) → verde completo, incluido `check:api-console` (94 operaciones sincronizadas con el contrato).

Estado: **ARREGLADA**.

---

## T-11 (c) · La declaración de pérdidas del DXF de fondo es de sesión

**Terminada cuando:** el manifiesto de pérdidas del DXF de fondo (el plano de
sólo lectura para calcar, subido con `PUT layout/dxf`) viaja PERSISTIDO con el
documento canónico en vez de vivir sólo en estado de React; un documento que
nunca cargó un DXF de fondo, o que lo cargó antes de esta ficha, sigue sin la
sección (lectura tolerante, ningún guardado espurio); `commitChange` la clona
en profundidad; specs verdes.

**Arreglado (mi parte):**
- `apps/web/src/lib/cad/cad-document.ts`: campo opcional
  `dxfBackgroundLossManifest?: CadLossManifestEntry[]` en `CadDocument`;
  `commitChange` la clona en profundidad cuando existe; `serializeCadDocument`
  la incluye (sin ordenar — es un registro de avisos, no un catálogo, igual
  que `lossManifest`) cuando existe.
- `apps/web/src/lib/cad/cad-document-migrate.ts`: `withSchemaDefaults` la
  pasa tal cual cuando el documento la trae; un documento sin ella no la
  materializa (mismo patrón que `imageDefinitions`/`layerStates`).
- `apps/web/src/lib/cad/dxf-background-loss-manifest.ts` (nuevo): convierte
  `CadDxfImportWarning[]` (lo que `importDxfPrimitives` ya calculaba al subir
  el DXF de fondo, en `dxf-import.ts`) a `CadLossManifestEntry[]` — misma
  forma que ya usa el manifiesto de pérdidas de exportación, para que ambos
  se lean con el mismo vocabulario.
- Specs: `dxf-background-loss-manifest.spec.ts` (conversión, severidad,
  capa/tipo de origen preservados) y dos casos nuevos en
  `cad-document-migrate.spec.ts` (sección ausente no se materializa de la
  nada; sección presente sobrevive a migrar → commit → serializar → reabrir,
  y `commitChange` la clona en profundidad).

**Pendiente (petición al coordinador, `F4-peticiones.md` #1 actualizada — en
realidad son dos peticiones distintas, #2 y #4):**
- **#2**: `Layout3DEditor.tsx` tiene que (a) al subir un DXF de fondo
  (`onDxfFile`, ~10305-10385), calcular
  `cadDxfBackgroundLossManifest(importPreview.warnings)` y adjuntarlo al
  documento canónico antes de guardarlo; (b) al abrir el documento
  (~2854-2856, hoy `setDxfWarnings([])` incondicional), leer
  `loadedCadDocumentRef.current?.dxfBackgroundLossManifest` con lectura
  tolerante en vez de resetear siempre a `[]`; (c) al quitar el DXF de fondo
  (`removeDxf`, ~10386-10401), limpiar también el campo del documento. El
  diff exacto de (a)/(b)/(c) queda pendiente de escribir línea por línea
  hasta ver el estado actual del archivo cuando el coordinador lo tome (las
  líneas se movieron desde que arrancó la sesión).
- **#4**: `scripts/cad/monolith-budget.json` necesita registrar que
  `cad-document.ts` sube de 799 a 803 líneas por este campo — diff exacto en
  `F4-peticiones.md`.

**Verificado (mi parte):**
- `npx tsx --test` sobre los dos specs nuevos/tocados → verdes.
- `npx turbo run test --filter=web` → 450/450 specs verdes (suite completa).
- `npx turbo run typecheck --filter=web` → verde.
- `npm run lint` (Node 20, monorepo completo) → 0 errores.
- `npm run check:cad` (con `VALLE_DWG_CORPUS_MIRROR`) → **1 problema**: el
  presupuesto de monolito de `cad-document.ts` (ver petición #4). Todo lo
  demás del gate, verde.

Estado: **PARCIAL** — el campo persistido, su lectura tolerante y su
conversión están arreglados y probados en mi territorio; la escritura real
(subir/quitar DXF de fondo) y la lectura en el editor viven en el monolito y
quedan pedidas; el presupuesto de línea del archivo que tuve que tocar
también queda pedido.

---

## T-19 · Capa que no imprime, ventana poligonal y recorte de PDF (puntos 3, 4, 5)

**Terminada cuando:** (3) una capa `plot:false` nunca sale en el PDF de PLOT
ni en el de PUBLISH, en ninguna ventana, aunque siga viéndose en pantalla;
(4) el contorno REAL de una ventana `MVIEW Poligonal`/`Objeto` viaja hasta el
PDF en vez de perderse en un rectángulo envolvente, y una entidad de PAPEL
(el propio contorno) nunca se proyecta como si fuera geometría de MODELO;
(5) toda ventana gráfica recorta DE VERDAD en los bytes del PDF de PLOT —hoy
ninguna lo hacía y el aviso de arriba (`viewport_model_clipped`) daba a
entender que sí. Spec por cada uno, con verificación de que el spec falla
sin el arreglo (`git stash` del fichero tocado + reejecutar).

**Arreglado:**
- `apps/web/src/lib/cad/paper-space.ts` — `visibleLayer()`: `plot: false` se
  comprueba ANTES que la anulación por ventana y la apaga incondicionalmente
  para publicación (T-19·3). `buildCadPublishPlan()`: nuevo
  `paperSpaceEntityIds` (todo id referenciado por CUALQUIER
  `paperSpace.entityIds`) excluye esas entidades de
  `modelSpace.entityIds` antes de proyectarlas en cada ventana, con aviso
  `paper_space_entity_excluded_from_model` (nunca en silencio) cuando de
  verdad había una fuga (T-19·4, mitad "se dibuja como espacio modelo"). El
  `CadPublishViewport` que arma cada ventana ahora lleva `clipPolygon`
  cuando la ventana lo tiene.
- `apps/web/src/lib/cad/cad-paper-viewport.ts` — `CadPaperViewport` gana el
  campo opcional `clipPolygon?: readonly CadPoint2[]`.
- `apps/web/src/lib/cad/layout/viewport-operations.ts` —
  `createCadPolygonalViewport()` guarda el contorno REAL en `clipPolygon` de
  la ventana que crea, además del rectángulo envolvente que ya guardaba
  (T-19·4, mitad "degrada callando" — ya no degrada: el contorno sobrevive).
- `apps/web/src/lib/cad/plot/plot-pdf.ts` — cada ventana entra ahora en
  `saveGraphicsState/…/restoreGraphicsState` con un recorte REAL antes de
  dibujar sus comandos: rectangular (`viewport.clip`) por defecto, o el
  polígono exacto (`viewport.clipPolygon`) cuando la ventana no es
  rectangular (T-19·5, y la otra mitad de T-19·4).

**No tocado — observación para otra ficha, no petición (no bloquea):** la
fuga de raíz (`entity-commands.ts`, el aplicador genérico de comandos
"insert", añade TODA entidad nueva a `modelSpace.entityIds` sin distinguir
espacio destino) sigue ahí; lo que arreglé aquí es el síntoma observable en
el PDF (paper-space.ts, mi territorio). Ese archivo no está en mi lista y el
síntoma que le importa a esta campaña —el PDF— ya queda cerrado.

**Verificado:**
- Cuatro specs nuevos, cada uno confirmado ROJO sin su arreglo (`git stash`
  del archivo tocado + reejecutar) y VERDE con él: T-19·3 y la fuga de
  espacio papel en `paper-space.spec.ts`; recorte rectangular y poligonal
  real en `plot-output.spec.ts` (busca el operador `W`/`n` de jsPDF y cuenta
  los `l` del contorno poligonal en los bytes del PDF).
- `npx tsx --test` sobre `paper-space.spec.ts`,
  `paper-space-plots-every-entity.spec.ts`, `dxf-paper-space-scope.spec.ts`,
  `plot/plot-fidelity.spec.ts`, `plot/plot-output.spec.ts` → 5/5 verdes, sin
  regresión en la exclusión de espacio papel que ya existía para DXF.
- `npx turbo run test --filter=web` → 450/450 specs verdes (suite completa).
- `npx turbo run typecheck --filter=web --filter=valle-design-api` → verde.
- `npm run lint` (Node 20, monorepo completo) → 0 errores, sin warnings
  nuevos en los archivos tocados.
- `npm run check:cad` (con `VALLE_DWG_CORPUS_MIRROR`) → **2 problemas**: los
  presupuestos de `cad-document.ts` (803, T-11c) y `paper-space.ts` (922,
  esta ficha) — ver petición #4 actualizada. Todo lo demás del gate, verde.

Estado: **ARREGLADA** (los tres puntos de la ficha); presupuesto de línea de
`paper-space.ts` pedido al coordinador (petición #4).

---

## T-34 · El DXF sale sin puertas ni ventanas y con las esquinas sucias

**Terminada cuando:** una `wall` exportada a DXF sale con el contorno del
INGLETE resuelto contra sus vecinas, no el aislado; una `opening` (puerta o
ventana) deja de desaparecer del DXF — sale con sus jambas y su símbolo (de
fábrica); el manifiesto de pérdidas declara la única degradación real
(bloque propio no reproducido) y el único descarte real (sin anfitrión);
verificado con `ezdxf` (oráculo ajeno) leyendo los bytes exportados, no sólo
con specs propios.

**Arreglado:**
- `apps/web/src/lib/cad/dxf-entity-primitives.ts`:
  - La rama `wall` usa ahora `wallJoinedFootprint(entity, wallJoins(entity,
    others))` (con `others` = las demás paredes del documento) en vez del
    `wallFootprint(entity)` aislado — el mismo cálculo que ya usa el
    producto en pantalla y en las jambas de un hueco
    (`opening-entity-adapter.ts`). Sin documento (o sin otras paredes), cae
    al footprint aislado — comportamiento idéntico al de antes, así que
    ningún llamador existente cambia.
  - Nueva `cadOpeningToDxfPrimitives(entity, document)`: deriva el anfitrión
    por `hostId`, resuelve sus uniones igual que el muro, y traza jambas
    (`wallOpeningJambs`) + símbolo de fábrica (`wallOpeningSymbolPaths`) —
    los mismos trazos puros que ya usa el adaptador en pantalla. Sin
    anfitrión resoluble: cero primitivas, nunca un marcador inventado. El
    bloque propio del hueco (`symbolBlock`) NO se resuelve al exportar
    (exigiría el catálogo de bloques completo); se declara la degradación en
    vez de fingir que se reprodujo.
- `apps/web/src/lib/cad/dxf-cad-document.ts` — `cadDocumentNativeDxfPrimitives`
  ahora hace `flatMap`: una `opening` produce VARIAS primitivas (no cabía en
  el `.map()` de una-a-una del resto de tipos).
- `apps/web/src/lib/cad/dxf-export-loss-manifest.ts` — `"opening"` entra en
  `DXF_NON_PRIMITIVE_TYPES` (tiene su propio camino, el descarte genérico no
  debe repreguntarle a `cadEntityToDxfPrimitive`, que para "opening" siempre
  da `null`); nueva regla `opening` en `SCHEMA4_LOSS_RULES`: `error` +
  `dxf_export_entity_dropped` sin anfitrión/receta degenerada, `warning` +
  `dxf_export_opening_symbol_block_degraded` con bloque propio declarado.

**Verificado:**
- Specs nuevos en `dxf-export-losses.spec.ts`: esquina en inglete (coincide
  EXACTAMENTE con `wallJoinedFootprint` calculado aparte, y difiere del
  aislado), puerta con anfitrión válido sin pérdidas, puerta con bloque
  propio declarada, puerta sin anfitrión con cero primitivas y pérdida
  `error`. Los cuatro confirmados ROJOS sin el arreglo (`git stash` +
  reejecutar) y VERDES con él.
- **Oráculo ajeno**: script ad hoc (no committeado) que llama
  `exportCadDocumentDxf` sobre dos muros en escuadra (grosor 250) con una
  puerta y una ventana, escribe el `.dxf` y lo relee con `ezdxf` (Python,
  independiente del escritor). Confirmó: 11 entidades (2 muros + 4 trazos de
  puerta + 5 de ventana, exactamente lo esperado), y las dos POLYLINE de
  muro comparten el vértice de esquina en `(2875, 125)`/`(3125, -125)` — el
  inglete real a ±125 mm en un muro de 250, ni un rectángulo envolvente ni
  una esquina sucia.
- `npx tsx --test` sobre `dxf-export-losses.spec.ts`,
  `dxf-schema4-losses.spec.ts`, `dxf-roundtrip.spec.ts`,
  `dxf-schema4-roundtrip.spec.ts`, `wall-joins.spec.ts`,
  `wall-openings.spec.ts`, `dxf-export.spec.ts` → 7/7 verdes.
- `npx turbo run test --filter=web` → 450/450 specs verdes.
- `npx turbo run typecheck --filter=web` → verde.
- `npm run lint` → 0 errores, sin warnings nuevos.
- `npm run check:cad` (con `VALLE_DWG_CORPUS_MIRROR`) → mismos 2 problemas de
  presupuesto ya pedidos (T-11c, T-19); `dxf-cad-document.ts` se mantuvo
  DENTRO de su asignación (961) comprimiendo el `flatMap` a una sola línea
  sin comentario propio — la razón del `flatMap` ya vive en el docblock de
  `cadOpeningToDxfPrimitives`, así que no hacía falta repetirla aquí.

**No independiente en el sentido de "registrado" (T-31 lo pide así):** el
oráculo `ezdxf` se usó ad hoc para esta verificación, no quedó registrado en
`docs/cad/corpus/oraculos/HERRAMIENTAS.md` con sha256 y licencia porque ese
directorio y su patrón de referencia (`censo-steputils.py`) NO EXISTEN en
este repositorio (comprobado también sobre el `main` real tras el rebase,
no sólo sobre el clon viejo). Registrar el oráculo correctamente es trabajo
de T-31 (donde además hace falta para `layouts.fidelity`); aquí sólo lo usé
como confirmación externa puntual.

Estado: **ARREGLADA** (geometría de muro y hueco, manifiesto de pérdidas);
la infraestructura formal del oráculo registrado queda para T-31.

---

## T-36 · La escala anotativa era destructiva y sólo acertaba en una ventana

**Terminada cuando:** un rótulo/cota anotativo mide la MISMA altura en
milímetros de papel en CUALQUIER ventana que lo muestre, sin importar su
escala; la altura/tamaños persistidos de la entidad (`height`, `arrowSize`,
…) NUNCA se reescriben para lograrlo — se resuelven de nuevo, por ventana,
en el momento de trazar; specs verdes, confirmados rojos sin el arreglo.

**El defecto exacto:** `cadAnnotativeRescaleCommands`
(`lib/cad/layout/annotative-scale.ts`, NO tocado — vive fuera de mi
territorio literal, aunque el nombre de archivo de la ficha decía
`annotation-scale.ts`, que no existe; ver nota) resuelve la escala anotativa
emitiendo comandos `{type:"properties"|"replace", ...}` que REESCRIBEN
`entity.height`/`arrowSize` en el DOCUMENTO, para la escala de la PRIMERA
ventana (en orden de la hoja) que muestra la entidad — con un `Set` `decided`
que impide tocarla dos veces. Si la MISMA entidad aparece en una segunda
ventana a otra escala (el caso de siempre: una ventana general 1:100 y un
detalle 1:5 del mismo muro), esa segunda ventana hereda la altura que dejó
la primera: correcta en una, incorrecta en la otra — exactamente "destructiva
y sólo acierta en una ventana".

**Arreglado (en mi territorio, `paper-space.ts`, el renderizador que
consumen PLOT/PUBLISH y — en cuanto la petición #1 se aplique — el botón del
editor):**
- `renderEntity()`, rama `text`/`mtext`: si la entidad declara
  `context.metadata.annotativeHeightMm`, la altura EFECTIVA para ESTA
  ventana se recalcula con `cadAnnotativeModelHeight(paperHeightMm,
  viewport.scale, unit)` en el momento de trazar — nunca se lee ni se
  escribe `entity.height`. Sin la marca, comportamiento idéntico al de
  antes.
- Rama `dimension`: mismo principio, pero con el juego COMPLETO de tamaños
  (`cadAnnotativeDimensionSizes`) aplicado sobre una COPIA efímera de la
  entidad (`{...entity, ...sizes}`) que sólo vive dentro de esta llamada —
  se le pasa a `buildCadDimensionGeometry` (que no sabe ni le importa que es
  efímera) y se descarta. El documento nunca la ve.
- La función destructiva (`cadAnnotativeRescaleCommands`) NO se tocó ni se
  eliminó: sigue existiendo para quien la invoque desde un comando
  interactivo del editor (fuera de mi territorio); esta ficha resuelve el
  camino de PAPEL Y ENTREGA (PDF/PLOT), que es el que le toca a F4.

**Nota sobre el nombre del archivo:** la ficha decía `annotation-scale.ts`;
el archivo real es `apps/web/src/lib/cad/layout/annotative-scale.ts`. Mismo
hallazgo de fondo que la nota de arranque: un nombre de la ficha no coincide
con el árbol real. Localizado por contenido (todo lo que menciona
`annotativeHeightMm`/`CANNOSCALE`), no por el nombre literal.

**Verificado:**
- Spec nuevo en `paper-space.spec.ts`: un rótulo con `annotativeHeightMm:
  2.5` y `height: 999999` (deliberadamente absurda) en DOS ventanas
  (1:100 y 1:5) — las dos miden 2,5 mm en el plan de publicación, y
  `entity.height` sigue en `999999` después de trazar ambas. Confirmado
  ROJO sin el arreglo (`git stash` de `paper-space.ts` + reejecutar:
  falla exactamente en la aserción de 1:5) y VERDE con él.
- `npx tsx --test` sobre `paper-space.spec.ts` → verde (incluye T-19·3/4 y
  T-36 juntos, sin interferencia entre ellos).
- Suite completa, typecheck, lint y `check:cad` — ver la sección de rebase
  arriba: 625/625 specs web, 0 errores de lint, mismos 2 problemas de
  presupuesto ya pedidos (`paper-space.ts` ahora en 939, número actualizado
  en la petición #4 para incluir esta ficha).

Estado: **ARREGLADA** en el camino de papel y entrega (PLOT/PUBLISH); el
camino interactivo del editor (`cadAnnotativeRescaleCommands`, si alguien
sigue llamándolo desde un comando de escala) queda fuera de mi territorio y
no se tocó.

---

## T-12·3 · Cola pendiente (petición del coordinador F0, recibida 2026-09-06 08:40 UTC)

`PAGESETUP` opción «Diálogo» (`lib/cad/engine/commands/plot-commands.ts`,
territorio F4): anuncia éxito y no abre ningún diálogo ni existen setups con
nombre. Fix-or-hide: o hace algo verificable (listar el setup vigente en la
línea de comandos con las opciones ya existentes) o declara «no está
disponible en esta versión» como otros anfitriones, recogido por
`check:command-integrity`. Spec en `plot-commands.spec.ts`.

Dado que era pequeña y estaba completamente especificada (fix-or-hide con la
alternativa ya escrita en la petición), se arregló de inmediato en vez de
esperar a T-30/T-31/T-32.

**Arreglado.** `apps/web/src/lib/cad/engine/commands/plot-commands.ts`: la
rama `Diálogo` de `PAGESETUP` ya no reenvía al anfitrión (`host({kind:
"page-setup", ...})`, que en el editor real sólo dispara un callback sin
diálogo detrás y sin setups con nombre). Ahora hace lo único verificable:
lee el setup vigente de la presentación activa (`cadPageSetupFromLayout`) y
lo muestra en la línea de comandos (papel, orientación, color, grosores,
tabla de plumas), con las opciones reales que sí existen para cambiarlo
campo por campo. No escribe nada (mismo comportamiento de sólo-lectura de
antes) y no pide nada al anfitrión.

`plot-host.ts`/`CadHostRequest["page-setup"]` NO se tocaron ni se borraron:
la petición está prohibido borrar código muerto sin anotar, y ese
mecanismo sigue siendo terreno legítimo para el día que exista un diálogo
real que lo invoque desde el editor — sólo dejó de ser el camino de esta
opción de la línea de comandos.

**Verificado:**
- `plot-commands.spec.ts`: el caso existente que afirmaba el comportamiento
  VIEJO (`hosts(...)` con `{kind:"page-setup",...}`) se actualizó para
  afirmar el nuevo: sin petición al anfitrión, mensaje con el setup real
  (`papel A1`, `orientación apaisada`), sin escritura (`meta.version`
  sin cambio).
- `npm run check:command-integrity` → 294 comandos, **0 éxitos falsos**
  (antes de esta ficha había un `éxito falso` acumulado en ese número, ahora
  incluido en los "22 informan").
- `npx turbo run test --filter=web --filter=valle-design-api` → 625/625
  specs web + suite API completa, verdes.
- `npm run typecheck`, `npm run lint` (0 errores), `npm run check:cad` (con
  `VALLE_DWG_CORPUS_MIRROR`) → mismos 2 problemas de presupuesto ya pedidos,
  ninguno nuevo (`plot-commands.ts` sigue muy por debajo de su techo).

Estado: **ARREGLADA**.

---

## T-30 · No se puede dibujar ni escribir sobre el papel

**Terminada cuando:** una línea y un texto colocados directamente en el
PAPEL de una lámina (no en el modelo, no dentro de ninguna ventana) salen en
el PDF exportado y NO se cuelan en ninguna ventana; golden: crear lámina,
dibujar los dos, publicar, afirmarlo en los bytes del PDF.

**El hueco exacto:** `CadPublishSheet` sólo tenía `viewports[]` — nada
representaba "lo dibujado en el papel mismo" (cajetín aparte, que ya
funcionaba). Cualquier entidad en `paperSpace.entityIds` (el contorno de una
ventana poligonal, per T-19·4; o, si alguien lo intentara, una nota
colocada directamente en el papel) no tenía NINGÚN camino de renderizado
propio — sólo el de exclusión que añadí en T-19·4.

**Arreglado (en mi territorio — el renderizador que consumen PLOT/PUBLISH):**
- `apps/web/src/lib/cad/paper-space.ts` — `CadPublishSheet` gana
  `paperCommands?: CadVectorCommand[]`. `buildCadPublishPlan` traza las
  entidades de `space.entityIds` con una ventana SINTÉTICA 1:1 en identidad
  (una entidad de papel ya está en mm de papel, no en unidades de modelo que
  necesiten escala de ventana) — reutiliza `renderEntity` entero, así que
  hereda gratis la visibilidad de capa (`plot:false` de T-19·3 incluido),
  los estilos por capa y la resolución anotativa de T-36 si alguna vez se
  usa en papel. `vectorCommandCount` las cuenta.
- `apps/web/src/lib/cad/plot/plot-pdf.ts` — cada hoja dibuja
  `sheet.paperCommands` DESPUÉS de sus ventanas (sin recorte: no pertenecen
  a ninguna) y ANTES del cajetín.

**Lo que NO se arregló (fuera de mi territorio, petición aparte pendiente
de escribir): la AUTORÍA.** Hoy nada en el editor permite al usuario
DIBUJAR algo nuevo directamente en `paperSpace.entityIds` — el aplicador
genérico de comandos "insert" (`entity-commands.ts`, no es mío) añade TODA
entidad nueva a `modelSpace.entityIds` sin distinguir el espacio activo. Esta
ficha entrega el camino de LECTURA/EXPORTACIÓN completo y probado con
entidades construidas a mano en el spec (simulando lo que un futuro comando
consciente del espacio papel produciría); falta que alguien haga que
dibujar estando en espacio papel escriba de verdad en `paperSpace.entityIds`
en vez de en `modelSpace.entityIds`. Mismo síntoma de raíz que T-19·4.

**Golden real (Playwright + API real) no construido — ad hoc en su lugar:**
el golden que pide la ficha (números 130–139) exige levantar el stack
completo (build de `web`, API real, Postgres) y no cupo en el presupuesto de
esta sesión junto con las otras siete fichas. En su lugar, dos specs
unitarios cubren la MISMA afirmación con `renderCadPlotPdf` real y lectura
de bytes con `pdf-measure.ts` — la misma técnica que ya usan los goldens
existentes, sin el andamiaje de Playwright:
- `paper-space.spec.ts`: el plan de publicación pone la línea/texto de papel
  en `paperCommands` y dice CERO comandos en la ventana (con un modelo
  vacío, delataría cualquier fuga).
- `plot-output.spec.ts`: `renderCadPlotPdf` sobre una hoja con
  `paperCommands` reales — el texto y la línea aparecen en los bytes del
  PDF, medidos con `measureCadPdf`.

Ambos confirmados ROJOS sin el arreglo (`git stash` de
`paper-space.ts`+`plot-pdf.ts` + reejecutar) y VERDES con él.

**Una regresión propia, detectada y arreglada antes de commitear:** al
añadir `space.entityIds.map(...)` sin guarda, `paper-space-plots-every-
entity.spec.ts` (una spec YA existente con un fixture que nunca declaraba
`entityIds` en su `paperSpace`) reventó con `TypeError`. Corregido con
`space.entityIds ?? []` en los dos sitios nuevos que lo leen — el mismo
patrón tolerante que ya usa el resto del archivo para campos opcionales.

**Verificado:**
- `npx tsc --noEmit` → verde.
- `npx turbo run test --filter=web --filter=valle-design-api` → 625/625
  specs web + suite API completa, verdes (incluida la spec que casi rompo).
- `npm run lint` → 0 errores.
- `npm run check:cad` → mismos 2 problemas de presupuesto ya pedidos
  (`paper-space.ts` sube a 975 — número actualizado en la petición #4).

Estado: **ARREGLADA** (lectura/exportación, en mi territorio); la AUTORÍA
(dibujar en papel desde el editor) queda como hallazgo para petición al
coordinador, mismo origen que T-19·4.

---

## T-31 · El PDF que rellena, recorta y respeta la capa (parcial)

Ficha de cinco puntos. Estado por punto:

- **(a) recorte de viewport** — ARREGLADO como T-19·5 (esta misma bitácora):
  antes ninguna ventana recortaba de verdad en el PDF, ahora todas lo hacen
  (rectangular o el polígono real).
- **(b) sombreado sólido/máscara/wipeout rellenos** — ARREGLADO, ver abajo.
- **(c) área y escala de trazado activas** — NO ABORDADO en esta sesión
  (tiempo). `page-setup.ts`/`plot-job.ts` (área/escala) no son míos por
  nombre de archivo explícito en mi territorio, y no alcancé a auditar si
  el defecto vive ahí o en cómo `buildCadPublishPlan` los consume.
- **(d) `MVIEW Desactivada` también en publicar** — ARREGLADO, ver abajo.
- **(e) dos ventanas a escalas distintas rotulan bien** — ARREGLADO como
  T-36 (esta misma bitácora): antes sólo la primera ventana en mostrar una
  entidad anotativa acertaba.

### (b) Sombreado sólido, máscara de MTEXT y wipeout: se calculaban y nunca se pintaban

**El defecto exacto:** `renderEntity` (`paper-space.ts`) SÍ calculaba
correctamente el relleno — un hatch sólido lleva `style.fill`, un MTEXT con
máscara lleva `backgroundMask`/`backgroundColor` en su comando — pero
`drawCommand` (`plot-pdf.ts`) tenía el estilo de `pdf.lines(...)` FIJO en
`"S"` (sólo trazo, nunca relleno) para toda ruta, y nunca dibujaba el
rectángulo de fondo de un MTEXT enmascarado. El dato existía; el trazador
lo tiraba.

**Arreglado.** `apps/web/src/lib/cad/plot/plot-pdf.ts`, `drawCommand`:
- Rama `path`: si `command.style.fill` existe, fija el color de relleno
  (`pdf.setFillColor`) y usa el estilo `"F"` (relleno) en vez de `"S"`
  siempre. Sin `fill`, comportamiento idéntico al de antes (sólo trazo).
- Rama `text`: si `command.backgroundMask && command.backgroundColor`,
  dibuja un rectángulo relleno (`pdf.rect(..., "F")`) del ancho medido del
  texto (`pdf.getTextWidth`) ANTES de escribirlo, respetando la alineación
  (izquierda/centro/derecha).

**WIPEOUT** no tiene rama propia en `renderEntity` — cae al camino genérico
de `plotEntityFromRegistry` (`paper-space-registry-fallback.ts`, fuera de
mi lista explícita de archivos). No confirmé si produce un `path` con
`style.fill` (en cuyo caso el arreglo de arriba ya lo cubre) o si se
degrada a contorno sin relleno; queda sin verificar por tiempo — anotado,
no ocultado.

**Verificado:**
- Spec nuevo en `plot-output.spec.ts`: una hoja con un hatch sólido y un
  MTEXT con máscara — el flujo de contenido del PDF lleva al menos DOS
  operadores de relleno `f` (antes, sólo el del fondo blanco de página:
  confirmado con `git stash` + reejecutar, 1 relleno en vez de los 2
  esperados).
- `npx tsx --test` sobre `plot-hatch-pattern.spec.ts`, `plot-output.spec.ts`,
  `plot-fidelity.spec.ts` → 3/3 verdes, sin regresión en el patrón de
  sombreado con trama (que dibuja trazos, no un relleno sólido, y sigue sin
  usar el estilo `"F"`).
- `npx turbo run test --filter=web --filter=valle-design-api` → 625/625
  specs web + suite API completa, verdes.
- `npm run typecheck`, `npm run lint` (0 errores), `npm run check:cad` →
  mismos 2 problemas de presupuesto ya pedidos, ninguno nuevo
  (`plot-pdf.ts` sigue muy por debajo de su techo).

### (d) `MVIEW Desactivada` también en publicar

**El defecto exacto:** `layout/viewport-operations.ts` ya tenía
`activeCadViewports(space)` documentada como "La publicación y la vista
previa filtran con esto" — pero `buildCadPublishPlan` (`paper-space.ts`,
el plan que usan PUBLISH y el botón del editor) mapeaba `space.viewports`
SIN filtrar: una ventana apagada con `MVIEW OFF` (que conserva su encuadre
para volver a encenderla, no se borra) se dibujaba igual al publicar.

**Arreglado.** `buildCadPublishPlan` filtra `viewport.layerVisibility?.["*"]
!== false` antes de mapear. **No importé `activeCadViewports`/
`cadViewportIsOn`** de `viewport-operations.ts` — intentarlo revienta un
CICLO DE IMPORTACIÓN REAL en tiempo de carga (`viewport-operations.ts` ya
importa `CAD_SHEET_SCALES` de `paper-space.ts` a nivel de módulo; importar
en la otra dirección crea un `ReferenceError: Cannot access 'CAD_SHEET_
SCALES' before initialization` porque `CAD_VIEWPORT_STANDARD_SCALES =
CAD_SHEET_SCALES` se evalúa ANTES de que `paper-space.ts` termine de
inicializar su propia constante). Repetí la regla inline con un comentario
que explica por qué, en vez de la función compartida.

**Verificado:**
- Spec nuevo en `paper-space.spec.ts`: dos ventanas, una apagada con
  `setCadViewportOn(..., false)` — el plan de publicación sólo trae la
  encendida. Confirmado ROJO sin el arreglo (`git stash` + reejecutar) y
  VERDE con él.
- Mismo typecheck/lint/tests/check:cad verdes reportados arriba.

**No independiente (T-31 lo pide para `layouts.fidelity`):** no llegué a
registrar `ezdxf`/`pypdf`/`pdfminer.six` como oráculo formal en
`docs/cad/corpus/oraculos/HERRAMIENTAS.md` (el directorio y su patrón de
referencia `censo-steputils.py` no existen en este repositorio — mismo
hallazgo que T-34) ni a escribir la petición a `rubric.json` con el bloque
`{"kind":"spec","path":"…","independent":true}` que pide la ficha. Usé
`ezdxf` ad hoc en T-34 para una verificación puntual, pero eso no es
"registrado". Esta pieza de T-31 queda sin cerrar.

### (c) Área y escala de trazado activas — auditado; un bug de mi territorio arreglado, el resto queda en petición

**El defecto real** (confirmado leyendo `plot/page-setup.ts` y `plot/plot-job.ts`,
ninguno de los dos en mi lista de archivos): `computeCadPlotPlacement` y
`cadPlotProject` —la aritmética que coloca el área elegida sobre el papel a la
escala pedida— sólo se referencian desde `plot-output.spec.ts`. `buildCadPlotJob`
nunca las llama: emite la geometría en las coordenadas de papel que YA traía la
presentación, así que elegir `EXtensión`/`Ventana`/`Escala 1:50` no cambia un
solo byte del PDF. Además `plot-job.ts:284-288/372-375` llama a
`cadPlotAreaSources(input.pageSetup, null)` con `extents` y `display` SIEMPRE
`null` — `resolveCadPlotArea` devuelve `null` para esas dos áreas más `limits`
(que reutiliza `extents`), y la comprobación previa emite `unknown_area` con
severidad `error`: tres de las cinco áreas bloquean el trazado siempre, aunque
el documento sí tenga envolvente (`cadDocumentExtents` existe y nadie se lo
pasa). Esto NO es mío por nombre de archivo explícito — queda documentado como
**petición #6** en `F4-peticiones.md` con el diff propuesto.

**Lo que SÍ es mío y arreglé:** `engine/commands/plot-commands.ts` (mi
territorio) — el teclado de `PLOT`, al elegir la palabra clave `Ventana`,
ponía `area: { kind: "display" }` de inmediato (línea 369 original), ANTES de
picar ningún punto. Si el usuario tecleaba `Trazar` sin picar las dos esquinas
(o cancelaba a medias), el trazado salía con área «Pantalla» — un área que
además el bug de arriba bloquea siempre — en vez de con la que hubiera antes
de elegir `Ventana`. El manejador de puntos (líneas 386-393, sin tocar) ya
construye `area: { kind: "window", corner1, corner2 }` en cuanto llegan los dos
clics, así que la palabra clave no necesitaba adelantar ningún área: sólo
limpia `corner1` a la espera de la primera esquina.

**Arreglado:** se quita `area: { kind: "display" }` de la rama `"Ventana"`;
sólo limpia `corner1`. El área elegida antes (por defecto, `layout`) se
conserva hasta que el usuario complete la ventana con dos puntos.

**Verificado:** `plot-commands.spec.ts`, caso nuevo — `PLOT`, `Ventana`,
`Trazar` sin picar esquinas → el área de la petición sigue siendo `{kind:
"layout"}`, nunca `{kind: "display"}`. Confirmado ROJO sin el arreglo
(`git stash` del archivo de comando + reejecutar con `npx tsx --test`, el
`assert.deepEqual` fallaba con `actual: {kind:"display"}`) y VERDE con él. El
caso ya existente (`V`, dos esquinas, `T`) sigue en verde — no hay regresión
en el camino feliz. `npx turbo run test --filter=web` → 625/625.
`npm run typecheck`, `npm run lint` (0 errores) verdes. `npm run check:cad` →
mismos 2 problemas de presupuesto ya pedidos (petición #4), ninguno nuevo.

Estado: **PARCIAL** — (a), (b), (d), (e) arreglados y probados; (c) el bug de
mi territorio (`Ventana` en `plot-commands.ts`) arreglado y probado, el resto
(colocación/escala inertes, tres áreas siempre bloqueadas) auditado y
petitionado (#6, fuera de territorio); el oráculo independiente registrado no
se construyó.

## Nota · H9/"T-32" (el DXF exportado no lleva las presentaciones) — evaluado, NO abordado

No estaba en la lista de fichas con la que arrancó este frente (T-11, T-19,
T-30–T-31, T-34, T-36, T-12·3); apareció al revisar
`docs/execution/auditoria-fable/dimensiones/05-layouts-plot.md` (H9, severidad
**ALTA**): `dxf-document-export.ts` excluye siempre las entidades de espacio
papel (`dxf_export_paper_space_excluded`) y el escritor no emite tabla
`BLOCK_RECORD` de `*Paper_Space`, objetos `LAYOUT` (con `PLOTSETTINGS`
embebido), ni entidades `VIEWPORT` por ventana.

Evalué implementarlo esta sesión y decidí NO hacerlo, no por territorio (el
escritor DXF sí está en el mío) sino por alcance: es una función nueva, no un
defecto puntual — construir handles nuevos y coherentes (`$HANDSEED`), una
tabla `BLOCK_RECORD` adicional, objetos en `OBJECTS` con sus punteros
cruzados, y `VIEWPORT` con los grupos 40/41/12/13/45/331 correctos, todo
compartiendo el mismo manejo de handles que ya usa el resto de la exportación
(riesgo real de romper el corpus DWG/DXF existente si el `$HANDSEED` o el
orden de secciones queda mal). La propia auditoría lo estima en "varios días".
Empezarlo a medias en el tiempo que quedaba de esta sesión —sin acabar el
viaje de ida y vuelta que "terminada cuando" exigiría (exportar, reimportar
con el oráculo externo, dos presentaciones con sus ventanas y escalas
intactas)— sería precisamente el defecto que esta campaña pide evitar: nada
de implementaciones a medio terminar.

**Estado: NO ABORDADO, señalado para el coordinador.** Si se agenda como
ficha propia (T-32 u otro nombre), necesita su propia sesión completa; no es
un añadido de una tarde a este frente.
