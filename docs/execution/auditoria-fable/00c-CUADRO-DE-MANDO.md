# Cuadro de mando de la auditoría: las veinte dimensiones, ya verificadas

> **Generado del diario del flujo, no escrito a mano.** Cada nota es la del
> **escéptico** que revisó el informe de esa dimensión contra el árbol real, no la que
> se puso el auditor: la del auditor era sistemáticamente más alta. Regenerar este
> documento es volver a leer `journal.jsonl` del flujo; no se edita a mano.

**Media corregida: 5.15/10** sobre 20 dimensiones.

De **295** huecos auditados, **285 se confirmaron reales** y 10 se refutaron
—ya estaban construidos—. De los reales, **19 bloquean el trabajo** y 118 son de severidad alta.
Los escépticos añadieron además **106 huecos que el auditor no había visto**.

Que sólo 10 de 295 se cayeran no dice que la auditoría fuera blanda: dice que el
material que revisaba era real. Los diez refutados son la parte más útil del ejercicio,
porque son cosas que ya están hechas y que una sesión sin este documento habría
vuelto a construir.

## Las notas, de peor a mejor

| Nota | Dimensión | Reales/auditados | Bloqueantes |
|---:|---|---:|---:|
| **2** | Toolsets Mechanical y Electrical | 14/14 | 2 |
| **3** | Visualización 3D: estilos visuales, render, materiales, luces, navegación | 16/16 | 1 |
| **3** | Toolset Architecture | 15/15 | 2 |
| **3.5** | Toolsets MEP y Plant 3D | 15/15 | 0 |
| **4** | Modelado 3D: sólidos, superficies, mallas, SCU | 14/15 | 2 |
| **4** | Interoperabilidad: DWG, PDF, IFC, nubes de puntos, Revit | 15/15 | 3 |
| **4** | Trabajar con otros: xrefs, contenido, revisión, versiones | 14/14 | 1 |
| **4.5** | Espacio papel, láminas y publicación | 15/15 | 1 |
| **5** | Toolsets Map 3D y Raster Design | 12/14 | 0 |
| **5** | Automatización y productividad del despacho (auditoría 12) | 13/15 | 2 |
| **5.5** | La cinta y cómo se usa de verdad | 14/14 | 0 |
| **6** | Seguridad: autenticación, secretos, multiusuario, OWASP | 15/15 | 1 |
| **6** | Frontend: arquitectura, estado, monolitos (apps/web) | 13/14 | 1 |
| **6** | Landing, alta de cuenta y primeros cinco minutos | 15/15 | 1 |
| **6** | Accesibilidad, teclado e idiomas | 15/15 | 0 |
| **6.5** | Backend: API, datos, escalabilidad | 14/15 | 0 |
| **7** | El flujo diario de dibujo 2D | 15/15 | 1 |
| **7** | Rendimiento: bundle, render, 100k entidades, memoria | 13/15 | 0 |
| **7.5** | Diseño visual: sistema, tipografía, color, densidad, movimiento | 14/14 | 1 |
| **7.5** | Calidad del código: deuda, duplicación, tipos, pruebas débiles | 14/15 | 0 |

## Los bloqueantes confirmados, en orden de nota

Ése es el orden de la cola: lo que peor está y además bloquea. Cada uno trae la prueba
que dio el escéptico, con sus rutas y sus `grep`, para no volver a buscarla.

### Toolsets Mechanical y Electrical — 2/10

**1. No existe el esquema eléctrico: no hay un solo símbolo de control**

apps/web/src/lib/cad/mep-symbols.ts — ocho símbolos totales (ids en las líneas 45, 60, 74, 86, 99, 114, 123, 136), cuatro eléctricos y los cuatro de planta. Grep repo-completo (apps/web/src, apps/api/src, packages/, apps/web/e2e) de 'bobina|contactor|relevador|guardamotor|seccionador|60617|schematic': CERO aciertos en código de producto. familyForBlock (engine/commands/electrical-tag.ts:76-84) reconoce sólo LUMINARIA/CONTACTO/APAGADOR/TABLERO.

*Matiz del escéptico:* Dos matices al enunciado, no al hueco. (1) M, PB, TR y SN NO son «inalcanzables»: FAMILY_OPTIONS (electrical-tag.ts) ofrece las ocho familias en la ruta de uno en uno, y cadIsElectricalInsert (electrical/device-tags.ts:168-172) admite CUALQUIER inserción cuya capa empiece por 'IE-', así que un bloque ajeno importado por DXF sí se puede etiquetar -M1. Lo inalcanzable es sólo la deducción automática de AETAG Todos, y el comando lo DECLARA («N sin familia reconocible, sin tocar»). (2) Lo que no existe es el SÍMBOLO, no la etiqueta. El hueco sigue siendo bloqueante por eso mismo: sin bobina ni contacto no hay esquema de mando que dibujar.

**2. El número de conductor no se dibuja: el plano impreso no lo lleva**

wireCommands (engine/commands/electrical-wire.ts:130-172) emite exactamente dos comandos: el upsert de la capa IE-CIR y el insert de la polilínea con cadWireMetadata. Grep de 'mtext|"text"' en ese fichero: sólo dos aciertos, y son input.kind === "text" del parser de entrada (líneas 227 y 233). CAD_IE_NUMBER sólo se lee en electrical/wire-numbering.ts y en specs. El cuadro de cargas (data-extraction/circuit-schedule-table.ts:70-101) imprime por CIRCUITO, no por conductor, así que el número tampoco llega al papel por ahí.

*Matiz del escéptico:* Añado la mitad que falta y que agrava: ESCALERA.md declara esta fila en peldaño 5 («Conductor con circuito, número y calibre») y su columna «qué falta» dice sólo «la marca vive en context.metadata». No dice, con todas las letras, que el número NO SE ROTULA en el plano. Para la regla de la casa —lo parcial se declara— esa fila está incompleta, y es lo que hace que el hueco se lea como sorpresa.

### Visualización 3D: estilos visuales, render, materiales, luces, navegación — 3/10

**3. H-1 · El estilo visual sólo alcanza a solid3d**

Confirmado tres veces. (a) apps/web/src/components/cad/viewport/solid-shade-host.ts:321-324 filtra literalmente `entity.type === "solid3d"`. (b) `grep -n 'style\|Style' components/cad/viewport/wall-solid-host.ts components/cad/viewport/room-solid-host.ts components/cad/viewport/native-mass-hosts.ts` → CERO líneas; los tres anfitriones de masa nativa no tienen parámetro de estilo. (c) `grep -rn 'applyVisualStyle' apps/web/src` devuelve exactamente 4 hits y el único emisor es studio-engine-bridges.ts:144 → solidShadeHost. (d) lib/cad/wall-solid-three.ts sólo lee `cadWallMaterialStyle(wall.material).color` (:213, :248) y clava `side: THREE.FrontSide` (:214) sin mirar ningún estilo. El golden 47 §3b (e2e/golden/47-cad-solids.spec.ts:225-241) afirma ÚNICAMENTE el texto «Estilo visual: Alámbrico.», nunca la escena: el éxito falso está protegido por la única prueba que existe.

*Matiz del escéptico:* Ninguna: es el hueco mejor probado del informe y el único que merece «bloqueante» de verdad, porque es un ÉXITO FALSO (la línea de comandos confirma un cambio que no ocurrió sobre muros, losas, cielorrasos y cubiertas) y la casa cobra 5 puntos en `integrity.commands` por no tenerlos. Añadiría a su lista de ficheros que el estilo NO se persiste ni se puede consultar: es un campo privado `private style` en solid-shade-host.ts:121, se pierde en cada recarga, y `VSCURRENT`+Intro devuelve `result: { kind: "none" }` con mensaje VACÍO (view-visual.ts:66-71) en vez del valor vigente entre ángulos, como hace AutoCAD.

### Toolset Architecture — 3/10

**4. No puedo sacar un corte ni un alzado: FLATSHOT y SECTION no ven la entidad `wall`**

apps/web/src/lib/cad/flatshot-solids.ts:181-192 — `if (entity.type === "solid3d") …; if (entity.type !== "box" && entity.type !== "station") { skipped.push(… `${entity.type.toUpperCase()} no tiene volumen: no proyecta ocultas.`) }`. `wall` cae por ahí ANTES de llegar a `volumeFor`, así que ningún catálogo de alturas lo salva. SECTION y SLICE (command-manifest.ts:302-303 → commands/solids-modify) usan `selectedSolids` (engine/commands/solids-support.ts:44-51), que filtra `entity.type === "solid3d"`. Verificado que WALL emite `type: "wall"` (engine/commands/draw-wall.ts:74-79) y que el golden 92 monta `{type:'box', kind:'wall'}` con `schema: 3` (e2e/golden/92-cad-alzado-del-arquitecto.spec.ts:32-34): son dos objetos distintos. El cuerpo B-rep del muro existe y sólo lo consumen wall-solid-three.ts:105 y validation-report.ts (`grep -rn wallSolidBodyLocalWithDiagnostics`).

*Matiz del escéptico:* Hay que corregir DÓNDE se cobra el hueco. El auditor dice «el golden 92 respalda la fila»: FALSO. `grep -n '92-cad-alzado' docs/competitive/rubric.json` no devuelve nada — el golden 92 no es evidencia de ninguna fila de la rúbrica, y toolset-architecture se sostiene sobre los goldens 53, 77, 78, 79. Quien SÍ sobre-promete es docs/parity/ESCALERA.md:366-367, que da peldaño 5 a «FLATSHOT/SOLPROF sobre el modelo del ARQUITECTO (muros…)» citando el golden 92. Ese peldaño 5 está cobrado sobre el muro HEREDADO y no sobre la entidad que emite WALL; la columna «todavía no» de esa misma fila lo insinúa («la altura sale del catálogo de arquetipos del visor») pero no lo dice. El arreglo documental es de minutos y va antes que el de código.

**5. El DXF sale sin puertas ni ventanas y con las esquinas de los muros sucias**

(1) `grep -n opening dxf-entity-primitives.ts` → 0 coincidencias; `grep -rn opening dxf-export-loss-manifest.ts` → 0; `opening` no está en DXF_NON_PRIMITIVE_TYPES (líneas 50-64) y `cadEntityToSchema4Primitive` sólo trata point/xline/ray/solid/wipeout/image/attdef/table (dxf-schema4-primitives.ts, líneas 46-125), así que el fallback devuelve null y cae en `dxf_export_entity_dropped` severidad `error` (dxf-export-loss-manifest.ts:296-308). (2) dxf-entity-primitives.ts:111-124 usa `wallFootprint(entity)` y su comentario afirma que coincide con el dibujo; wall-entity-adapter.ts:164 dibuja `wallJoinedFootprint(entity, joins)` y wall-joins.spec.ts:67-69 mide `rightExtension = +125`, `leftExtension = -125` y `cap:false` en una esquina de muros de 250. La regla `wall` del manifiesto (dxf-export-loss-manifest.ts:179-201) declara la pérdida de eje/grosor/altura pero NO la divergencia del contorno.

*Matiz del escéptico:* Ninguna en el fondo; y refuerzo su punto (2) con una prueba que el auditor no usó: el trazado a papel y la proyección del enlace de revisión SÍ pasan el documento (paper-space-registry-fallback.ts:74-78 y collab/plan-projection.ts:96-100 llaman `renderer.paths(entity, PLOT_SEGMENTS, document)`), o sea que el PDF y el visor dibujan el contorno UNIDO y sólo el DXF dibuja el contorno base. El exportador es el único disidente del repositorio, lo cual convierte D-04 de «inconsistencia» en «regresión localizada de un solo fichero».

### Modelado 3D: sólidos, superficies, mallas, SCU — 4/10

**6. 1 · No existe ninguna transformación 3D: un sólido no se puede inclinar**

Confirmado en tres sitios independientes. (a) `grep -nE '3DMOVE|3DROTATE|3DSCALE|3DALIGN|MIRROR3D|3DARRAY' apps/web/src/lib/cad/engine/command-manifest.ts` → cero; los únicos vecinos son ALIGN (:171) y MIRROR (:191), los 2D. (b) `CadSolidPlacement` (apps/web/src/lib/cad/cad-entities-v5.ts:270-281) es literalmente {a,b,c,d,e,f,dz?}: afín 2×3 más un desplazamiento en Z, y su propio comentario lo dice («la misma afín 2×3 del resto del documento»). (c) `placeBody` (apps/web/src/lib/cad/solid3d-build.ts:546-573) aplica x' = a·x + c·y + e, y' = b·x + d·y + f, z' = z + dz — la z no se mezcla nunca con x ni con y, así que NO hay ninguna colocación expresable que incline un cuerpo. (d) `commands.transform` del adaptador (solid3d-adapter.ts:306-317) compone la afín 2D y devuelve `{...composed, dz: current.dz}`: dz sobrevive intacto, luego MOVE no sube nada. (e) `CadEntityTransform` (entity-runtime.ts:184-195) sólo tiene translation/rotationDeg/scale/scaleXY/mirror/affine, todo CadPoint2. El transporte entero es 2D. ESCALERA.md:376 lo declara en cero y no miente.

*Matiz del escéptico:* Ninguna. Es el hueco mejor establecido de la lista y la severidad está bien puesta: no es «falta un comando», es que el esquema persistido no tiene dónde escribir el giro. Añado un detalle que refuerza el diagnóstico y que el auditor no usó: `placeBody` YA invierte las caras cuando el determinante es negativo, así que la mitad difícil de MIRROR3D (la lateralidad) está resuelta; lo que falta son las tres filas de la matriz.

**7. 4 · EXTRUDE siempre en +Z del mundo, y aplana el perfil inclinado EN SILENCIO**

Confirmado línea a línea. `planeFrameAt` (solid3d-profiles.ts:139-146) devuelve siempre `zAxis: {x:0,y:0,z:1}` con origen {0,0,elevation}. `solids-create.ts:138` lo usa tal cual en el nodo extrude. `profileFromEntity` (solid3d-profiles.ts:84-128) toma la elevación de UN vértice en los cinco casos (region :88 `outer[0].z`, circle :100 `center.z`, polyline :108 `vertices[0]?.z ?? 0`, ellipse :117, spline :125) y la geometría de `pathPoints`, que devuelve `paths[0].points` del renderizador — y `CadRenderPath.points` es `CadPoint2[]` (entity-runtime.ts:62-65). No hay ni una comprobación de planaridad ni un mensaje: se aplana y se sigue. Y el contraejemplo está escrito y verde: draw-spatial.spec.ts:147-152 comprueba que RECTANG sobre el SCU `faldon` a 30° da cuatro vértices SOBRE el faldón, «y el rectángulo tiene cota: no es su sombra». O sea que el dibujo entrega la cota correcta y EXTRUDE la tira.

*Matiz del escéptico:* Ninguna en el fondo. La severidad está BIEN puesta y quiero subrayar por qué, porque no es por la funcionalidad ausente (extruir por la normal del perfil, que sería alta) sino por el SILENCIO: es un resultado geométricamente falso —más pequeño por el coseno y a la cota de una esquina— presentado como correcto, y la doctrina de la casa («una ausencia silenciosa es peor que una limitación declarada») lo convierte en el peor de los quince. El arreglo barato que propone —medir la desviación de planaridad y devolver el motivo en vez del perfil aplanado— es correcto y debería ir solo, hoy, antes que el arreglo bueno.

### Interoperabilidad: DWG, PDF, IFC, nubes de puntos, Revit — 4/10

**8. 1. No puedo abrir el .dwg que me mandan. Ninguno**

REAL en el resultado, GRAVEMENTE MAL DIAGNOSTICADO en la causa. El auditor sólo miró la puerta de PROMOCIÓN GENERAL y se dejó una segunda puerta entera, FIRMADA y CABLEADA de extremo a extremo. apps/web/src/lib/cad/dwg-interop-flag.ts:190 `dwgBetaImportIsEnabled()`, con DWG_BETA_AUTHORIZATION (línea 173): ownerSigned:true, adrRef 0009 §6-bis/§6-ter/§6-quater, signedDate 2026-08-24, profile AC1015_MODELSPACE_2D_V3 (LINE/POINT/CIRCLE/ARC/LWPOLYLINE/TEXT/INSERT + ELLIPSE/SPLINE + MTEXT/DIMENSION/HATCH). Y una TERCERA: DWG_AC1018_BETA_AUTHORIZATION (línea 240), ownerSigned:true, misma fecha, ADR-0009 §7. El cableado NO es teórico: document-import-validation.ts:124 admite .dwg por `dwgBetaImportIsEnabled(dwgBetaEnabled)`; document-import-client.ts:106 lo resuelve desde NEXT_PUBLIC_DWG_NATIVE_IMPORT_BETA; document-import.worker.ts:78-101 importa dinámicamente `readDwgNeutralDatabase`; document-import.ts:111-115 y :144 llaman a `dwgNeutralDatabaseToCadDocument` (dwg-document-bridge.ts, 712 líneas). El selector del TABLERO ya ofrece .dwg cuando la variable está encendida: dashboard/page.tsx:692. Además el auditor no vio que la ESCRITURA también está construida: dwg-export-flag.ts:44 declara DWG_EXPORT_GATES.publicWriterExists:true con `writeCanonicalDwg` fusionado (dwg-native-writer.ts, 469 líneas) y ADR-0009 §8 firmada 2026-08-25; lo único abierto es `externalOracleVerified:false`, una corrida de ODA File Converter que sólo puede hacer el titular. Por tanto la frase «faltan tres firmas» es falsa: dos de las cinco autorizaciones YA están firmadas. Lo que de verdad falta es (a) la firma de la FAMILIA MODERNA —DWG_MODERN_BETA_AUTHORIZATION, línea 336, ownerSigned:false— que es justo la que importa, porque AC1032 es lo que guarda AutoCAD 2018-2026 por defecto y es lo que manda el estructurista; (b) la revisión jurídica, hoy `pending_parallel`; (c) encender la variable en un despliegue. Y la nota «DWG lectura/escritura 0/10» no es defendible tal como está escrita.

*Matiz del escéptico:* El resultado para un usuario de un despliegue público sigue siendo cero, así que la severidad se mantiene. Pero hay que reescribir el hallazgo entero: no es «la bandera está apagada y faltan siete gates», es «hay una beta AC1015+AC1018 FIRMADA y CABLEADA que un despliegue puede encender hoy, y lo que falta es la firma de la familia moderna AC1024/AC1027/AC1032 —la única que cubre lo que AutoCAD guarda por defecto— más el dictamen jurídico». Eso cambia la apuesta: no es «construir el acuse de recibo porque no se puede leer», es «pedir UNA firma más y encender una variable». El acuse de recibo sigue siendo buena idea, pero deja de ser el único camino.

**9. 2. El espacio papel no entra ni sale**

REAL, verificado en los dos sentidos. ENTRADA: dxf-model-space-scope.ts:47-58 `modelSpaceOnly()` descarta toda entidad con `paperSpace` y sólo la CUENTA en `excludedCount`. SALIDA: dxf-document-export.ts:132-149, `paperSpaceEntityIds` filtra y emite `dxf_export_paper_space_excluded`. `grep -n "LAYOUT|Paper_Space|VIEWPORT|BLOCK_RECORD|ACAD_LAYOUT" dxf-export.ts dxf-write-tables.ts` devuelve CERO líneas. `grep -rn "410" dxf-import.ts dxf-model-space-scope.ts` devuelve CERO: ni siquiera se lee el código que agruparía por presentación. Y la propia rúbrica lo confirma en el gap de la categoría `layouts`: «el DXF exportado no lleva presentaciones —dxf-export.ts no tiene noción de espacio papel: ni *Paper_Space, ni objetos LAYOUT, ni entidades VIEWPORT— así que un despacho que recibe el DXF recibe el modelo sin sus láminas».

*Matiz del escéptico:* Ninguna. Es el hallazgo mejor construido del informe y la severidad es correcta. La ruta de arreglo que propone (BLOCK_RECORD + ACAD_LAYOUT + código 410 + reutilizar `document.paperSpaces` que ya existe) es la correcta.

**10. 4. El API público entrega un DXF R12 mutilado y SIN manifiesto**

REAL, verificado línea por línea, y es el hallazgo más serio del informe. `grep -cin "loss|manifest|warning|advert" apps/api/src/modules/cad/cad-dxf-export.ts` = 0 en 291 líneas. cad-dxf-export.ts:58-77: `const box = boxOf(entity); if (box) { assets.push(...); continue; }` va ANTES de toda discriminación por `type` — confirmado el D-3: cualquier entidad con x/y/w/h finitos sale como rectángulo etiquetado, o sea geometría INVENTADA. layerOf (líneas 210-215): `raw.replace(/[^A-Za-z0-9_$-]+/g,'_').slice(0,31)` sin aviso. Texto: `.slice(0,240)` sin aviso. El escritor de destino es AC1009 R12: apps/api/src/modules/cad-documents/line-dxf.ts:199-200 `g(9,'$ACADVER'); g(1,'AC1009')`. El endpoint es cad.controller.ts:331-352, `@RequirePermissions('cad:view')`. Y el agravante comercial también es exacto: packages/design-sdk/src/generated/design-api.ts:2506 dice literalmente «LA REGLA DE ORO — el entitlement EXISTIÓ y venció… GET .../export/dxf sigue respondiendo 200… Los datos del usuario nunca quedan rehenes de un cobro».

*Matiz del escéptico:* SUBIR de alta a bloqueante. El auditor la infravaloró. Razón: es el único punto del árbol donde el producto CONTRADICE ACTIVAMENTE dos garantías escritas (CONTRATO-INTEROP §1 «pérdida declarada o pérdida prohibida» y §2 «el original es sagrado») y además el criterio `integrity.no-silent-loss` (4 puntos de la rúbrica), cuyo gap afirma «CERO pérdidas silenciosas» midiendo únicamente la ruta del navegador. No es una carencia como las otras catorce: es una afirmación falsa cobrada en la rúbrica, servida por la puerta de salida de datos del cliente que se va. Por la regla de la casa (fix-or-hide), o el endpoint emite manifiesto o deja de ser público.

### Trabajar con otros: xrefs, contenido, revisión, versiones — 4/10

**11. H1 · No hay CATÁLOGO del inquilino: para adjuntar hay que teclear el UUID**

`grep -rn xrefCatalog apps packages scripts` (fuera de node_modules) devuelve EXACTAMENTE 13 líneas y ninguna es un proveedor: command-types.ts:259 (la declaración), compare-drawings.spec.ts:109 y xrefs.spec.ts:102 (specs), xrefs.ts:21/136/200/354/369/372 y compare-drawings.ts:20/232/235 (los consumidores), design-center.ts:22 (la nota). Cero en Layout3DEditor.tsx y en cualquier .tsx. La vía gráfica: CadXrefPalette.tsx:93-104 es un `<input>` libre con placeholder «PLANT-ARCH», y fetchCadXrefSnapshot (Layout3DEditor.tsx:5406) resuelve por `layout?model=<assetId>&revision=<rev>`, que para un documento nuevo es el UUID (layout-http-adapter.ts:317-333, rama de UUID). compare-drawings.ts:139-141 declara NO_CATALOG y se para. design-center.ts:12-24 sólo ofrece `document.externalReferences`.

*Matiz del escéptico:* Dos precisiones. (1) La frase de titular —«no puedo navegar la biblioteca de dibujos de mi propia oficina»— es MÁS AMPLIA de lo que el árbol sostiene: el tablero SÍ la navega (apps/web/src/app/dashboard/page.tsx:141-142 lista `designClient.projects.list` y `designClient.documents.list`). El hueco exacto es que el catálogo no entra al CAD: ni al motor, ni a la paleta, ni a ADCENTER, ni a COMPARE. (2) El hueco NO es una mentira oculta: está declarado por escrito en tres sitios de la casa —rubric.json categoría `xrefs` («el motor no tiene CATÁLOGO del inquilino, así que «?» no puede listar»), ESCALERA.md:339 («**Todavía no**») y BACKLOG.md:114-122—. Es un «todavía no» honesto, no una fila cobrada sin evidencia. La severidad se sostiene igual: sin él el bucle diario no arranca.

### Espacio papel, láminas y publicación — 4.5/10

**12. 1 · No se puede dibujar ni escribir sobre el papel**

apps/web/src/lib/cad/paper-space.ts:823 — `const commands = document.modelSpace.entityIds.map(...)`, dentro del `.map` de viewports; `CadPublishSheet` (paper-space.ts:806-863) no tiene ningún campo `paperCommands`. `grep -rn "space.entityIds" apps/web/src` da tres hits productivos: viewport-operations.ts:308 (escritor), Layout3DEditor.tsx:2781 (copia defensiva al recargar) y cad-document-merge.ts:582 (fusión). Ningún lector de dibujo. BACKLOG P2-14 líneas 711-718 lo declara.

*Matiz del escéptico:* El hueco es real y bloqueante, pero la frase de apoyo «ese contorno tampoco se dibuja» es FALSA, y la verdad es peor. `createCadPolygonalViewport` (viewport-operations.ts:305-317) emite `{type:"insert", entity: clip}`, y `entity-commands.ts:359-372` mete SIEMPRE todo insert en `createdFrontIds`, que en la línea 547 se concatena a `document.modelSpace.entityIds`. Así que el contorno SÍ se dibuja: como polilínea de espacio modelo, con coordenadas de PAPEL, proyectada por `viewportTransform` en TODAS las ventanas de TODAS las hojas del PDF. Eso es exactamente lo que dice P2-14 («sin también sumarla a modelSpace.entityIds») y el auditor lo leyó al revés.

### Automatización y productividad del despacho (auditoría 12) — 5/10

**13. 1. Nada de lo que configura el despacho se comparte: rutinas, paletas, plantillas y atajos viven en localStorage**

grep -n '^  /v1/' packages/contracts/specs/design-api.v1.yaml: no existe ninguna ruta de rutinas, plantillas, normas, paletas, macros ni alias. apps/web/src/components/cad/lisp/library-storage.ts:15-40 lo declara en su propio encabezado ('es persistencia LOCAL'). apps/web/src/lib/cad/tool-palettes.ts:58-63 (clave valle_cad_toolpalettes:<tenant>:<user>). apps/api/src/modules/ no tiene módulo de configuración de despacho.

*Matiz del escéptico:* Dos correcciones, una a favor y otra en contra. EN CONTRA DEL AUDITOR: '/v1/cad/blocks es el ÚNICO almacén compartido por organización' es falso — /v1/cad/sheet-sets, /v1/cad/documents y /v1/cad/projects también son almacenes de servidor por organización (el propio informe lo reconoce en §1.7 al decir que el conjunto de planos se guarda en el servidor). La frase correcta es: el único almacén compartido de CONFIGURACIÓN de despacho es /v1/cad/blocks. A FAVOR DEL AUDITOR, y esto lo agrava: la tabla de plumas .ctb y el archivo .lin del despacho ni siquiera llegan a localStorage — viven en la SESIÓN y se pierden al recargar (docs/parity/ESCALERA.md:359 'Las tablas viven en la SESIÓN y se pierden al recargar'; lib/cad/engine/commands/plot-commands.ts:470 y settings-palettes.ts:374). El auditor puso el .ctb en la columna de AutoCAD y nunca midió el lado de Valle.

**14. 2. AutoLISP no ve los atributos de un bloque**

apps/web/src/lib/lisp/dxf/from-entity.ts, case "insert": emite exactamente subclass(AcDbBlockReference), 2, 10, 41, 42, 43, 50. Ni 66 ni ATTRIB. `grep -rni 'attrib' apps/web/src/lib/lisp/dxf/` devuelve CERO. En cambio lib/cad/block-text-adapters.ts:465 sí publica attribute:<TAG> y :474 sabe escribirlo.

*Matiz del escéptico:* Confirmado tal cual, pero el auditor se quedó corto en la consecuencia. Aunque se arregle esto MAÑANA, su escenario titular —'exportar la carpintería a Excel'— sigue sin funcionar: lib/lisp/builtins/unavailable.ts:138-143 declara no disponibles `getfiled`, `open`, `close`, `read-line`, `write-line`, `read-char` y `write-char`. AutoLISP aquí no tiene NINGUNA salida a archivo. El hueco 2 y ese otro son un solo bloqueo, y sólo nombró la mitad.

### Seguridad: autenticación, secretos, multiusuario, OWASP — 6/10

**15. 1 · No se puede echar a nadie de la organización**

Confirmado y ampliado. `grep -n "@Get(\|@Post(\|@Delete(\|@Patch(\|@Put(" apps/api/src/modules/organizations/organizations.controller.ts` → seis rutas exactas (121, 151, 264, 284, 326, 390), ninguna mutante sobre una membresía. `grep -rn "@Delete|@Patch|@Put" --include=*.ts apps/api/src` sobre TODO el API devuelve 17 rutas y ninguna es de organizations. Y comprobé las dos palancas de escape que el auditor no miró, para no darle la razón gratis: (a) `DELETE /v1/auth/sessions/:sessionId` NO sirve — `identity.service.ts revoke(id, userId)` filtra por `userId` de la sesión que llama, así que sólo te revocas a ti mismo; (b) los asientos tampoco — `seat-entitlement.service.ts` sólo CUENTA membresías (`occupied`), no expone ninguna ruta para liberar una. `grep -ni "expuls|quitar.*miembro|membership" docs/execution/BACKLOG.md` → nada: no está ni en cola. El hueco es real, completo y no está planificado.

*Matiz del escéptico:* Una sub-afirmación hay que desinflarla: «no se puede revocar una invitación pendiente» es cierta como ruta, pero el radio de daño está acotado por construcción — la invitación caduca a los 7 días (`organizations.controller.ts:363`, `expiresAt: new Date(Date.now() + 7 * 86_400_000)`) y es de un solo uso (`consumedAt` en la entidad, comprobado en la aceptación, línea 401). Esa mitad es baja, no bloqueante. La otra mitad —quitar un miembro y degradar un rol— es exactamente tan grave como dice.

### Frontend: arquitectura, estado, monolitos (apps/web) — 6/10

**16. 2. Cerrar o retirar el copiloto de frases: 40 entradas de Ctrl+K prometen un panel que no existe**

REAL y PEOR de lo que dice, pero con la causa mal identificada. Verificado el circuito entero: palette-actions.ts:124 lanza toastSuccess('Preview listo en el Copiloto CAD.'); las 40 entradas son CAD_COMMAND_REGISTRY (grep -c '^    id: "' lib/cad/commands/registry.ts = 40); commandPreview (:1523) no tiene UN uso en el JSX (sus únicos lectores son applyCommand :11677-11849 y el flag :13687); applyCommand, interpretCommand, navigateCommandLineHistory, undoLastCommand y redoLastCommand salen los cinco como no-unused-vars al correr `npx eslint src/components/cad/editor/Layout3DEditor.tsx`; commandText (:1522) sólo se lee en interpretCommand:11390-11391, que también está muerto; commandLog sólo se serializa a localStorage (:1793). Confirmado: elegir una entrada «Frase» produce un toast y nada más. DOS CORRECCIONES. (a) «Copiloto aparece UNA sola vez» es FALSO: hay TRES cadenas que el usuario ve — palette-actions.ts:124, lib/cad/commands/registry.ts:361 `title: "Comandos del copiloto CAD"` (el título de un informe que el usuario LEE) y registry.ts:1165 «Lista todos los comandos del copiloto…», que command-palette.ts:68 pinta DENTRO de la paleta como «Frase · …». (b) El panel no es que falte: se BORRÓ a propósito. lib/cad/no-ai-boundary.spec.ts:11 dice que se retiraron «rutas /v1/cad/intent y /v1/cad/vision, panel «Copiloto CAD», el bloque del monolito y la fila de rúbrica», y :95 lista components/cad/palettes/CadCommandDock.tsx entre los ficheros que el guardián impide que vuelvan.

*Matiz del escéptico:* La severidad se mantiene, pero la opción A del auditor —«montar el panel de preview» llamado Copiloto CAD— ESTÁ PROHIBIDA por IDENTITY.md y por no-ai-boundary.spec.ts, que retiró ese panel por nombre. Sólo quedan dos salidas honestas: retirar las 40 entradas «Frase» y las tres cadenas, o cerrar el circuito bajo un nombre que no mienta (el propio guardián dice por qué la paleta las etiqueta «Frase» y no «Copiloto»: el parser es determinista, no hay modelo). Y la causa raíz no la vio: ver hueco añadido A.

### Landing, alta de cuenta y primeros cinco minutos — 6/10

**17. El correo de fin de prueba se encola, no se puede renderizar, y se descarta en silencio**

grep -n "case '" apps/api/src/modules/outbox-receiver/email-templates.ts → sólo 4 casos (líneas 46, 57, 68, 70): identity.verify-email, identity.reset-password, organization.invitation, commercial.renewal-reminder. No hay 'commercial.trial-expiry'. El productor está en apps/api/src/modules/commercial/trial-expiry-reminder.service.ts:60 (TRIAL_EXPIRY_REMINDER_TEMPLATE) y se usa en el enqueue de la línea ~139. El receptor: apps/api/src/modules/outbox-receiver/outbox-receiver.service.ts:74-96 captura EmailTemplateError, pone outcome=error.code, inserta el WebhookReceipt y devuelve {status:'ignored'} — 200, sin reintento ni alarma. Cableado confirmado: outbox-worker.service.ts:105 llama a trialReminders.maybeRun().

*Matiz del escéptico:* El auditor se quedó CORTO, no largo. El agujero no es de una plantilla, es de CUATRO. Además de commercial.trial-expiry están huérfanas: identity.new-sign-in (identity.service.ts:543 — el aviso de inicio de sesión desde un dispositivo nuevo, que es un control de SEGURIDAD), product.feedback (feedback.service.ts:119, FEEDBACK_TEMPLATE) y support.incident (support.service.ts:69, SUPPORT_INCIDENT_TEMPLATE). Ninguna de las cuatro tiene case. Y el detalle que lo explica: las dos specs que cubren el camino del descarte (outbox-receiver.pg.spec.ts:136,144 y email-templates.spec.ts:118) usan una plantilla INVENTADA, 'marketing.navidad', nunca un nombre real del árbol — por eso el gate está verde sobre cuatro correos muertos. La spec de cobertura que el auditor propone es la correcta, pero hay que dimensionar el arreglo a cuatro renderizadores, no a uno.

### El flujo diario de dibujo 2D — 7/10

**18. Cuatro modos de captura que el cuadro promete y que ningún adaptador sirve**

CONFIRMADO línea por línea. `CadSnapKind` son cinco valores (apps/web/src/lib/cad/entity-runtime.ts:81-86). El puente `snap-scene.ts:191-207` reparte: center→centers, control→nodes, quadrant→quadrants, tangent→tangents y TODO LO DEMÁS→endpoints; no hay rama para midpoint, node, insertion ni geometric-center porque el tipo no los tiene. Los seis emisores que cita están donde dice: basic-native-adapters.ts:52-56 (medio de línea → 'center'), :89-92 (círculo: centro y cuatro cuadrantes, sin tangente), polyline-entity-adapter.ts:314-330 ('control' para el medio de cada tramo, recto o de arco), point-line-adapters.ts:165 (POINT → 'endpoint' con etiqueta "Nodo"), block-text-adapters.ts:459 (inserción de INSERT → 'endpoint'), wall-entity-adapter.ts:288 (medio del eje → 'center'). Y snap-scene.ts:178-190 restringe midpoints y perpendicularSegments a `entity.type === "line" && path.points.length === 2`. Comprobado además que la promesa es completa: CadDraftSettingsDialog.tsx:23-38 pinta las catorce etiquetas y draft-settings-host.ts:71-75 las enciende todas menos `grid`. Y ESCALERA.md no declara este hueco en ninguna parte (grep de "catorce", "modos de", "OSMODE": nada), así que no está cobijado por «todavía no».

*Matiz del escéptico:* Dos matices que el auditor no midió y que cambian el enunciado. (1) NO es cierto que «nadie sirve» esos cubos: `cadSnapSceneFromBoxes` (snap-scene.ts:97-101) llena midpoints, geometricCenters e insertions desde las cajas del editor, y `pdf/pdf-snap-geometry.ts:632-653` llena insertions desde el sustrato de PDF. Lo que está muerto es sobre GEOMETRÍA CANÓNICA — que es lo que importa, pero el enunciado correcto es «los adaptadores del documento no alimentan cuatro de los catorce cubos», no «los cubos no existen». (2) La prueba más dura la tiene delante y no la usó: professional-snapping.spec.ts:22-42 afirma que el motor resuelve insertion, geometric-center, tangent y quadrant sobre una escena escrita a mano — un spec VERDE sobre una función MUERTA. Eso, en esta casa, vale más que cualquier argumento del lunes.

### Diseño visual: sistema, tipografía, color, densidad, movimiento — 7.5/10

**19. 1 · Nadie mide el contraste de lo que se pinta, sólo el de la paleta**

Corrí `node scripts/design/check-contrast.mjs` → «Gate de contraste OK: 76 pares medidos en 2 temas (38 por tema)», exit 0, y son todos token×token SÓLIDOS. Reproduje los cuatro cocientes del auditor con el metro de la casa (scripts/design/contrast.mjs, composite+contrastRatio) y salen EXACTOS: viewport-hints.tsx:44 (`bg-warning/15 text-gray-900`) = 1,17:1 sobre --background y 1,22:1 sobre el lienzo, en oscuro, que es el tema por defecto (ThemeContext.tsx:76 `DEFAULT_SCHEME = "dark"`); ReviewPlanView.tsx:251 y CollabThreadPanel.tsx:222 (`bg-success/15 text-gray-950`) = 1,62:1 sobre --card; CadHatchPalette.tsx:77 (`bg-violet-500/20 text-violet-100`) = 1,09:1 en claro; field-controls.tsx:34 (`background: "#0e7490"` + text-foreground) = 3,04:1 en claro. Los cuatro ficheros y líneas están donde dice. El elemento se monta de verdad: Layout3DEditor.tsx:16116 pinta <CadViewportPrompt>.

*Matiz del escéptico:* Ninguna en el fondo. Sólo esto: el auditor dice que el gate «mide la paleta y no la pantalla», y su propia cabecera (check-contrast.mjs, líneas 18-24) declara lo contrario por escrito — «No se mide la paleta: se miden las COMBINACIONES QUE LA APP PINTA». O sea que la intención estaba escrita y la implementación no la alcanza. Eso hace el hueco MÁS grave, no menos: no es un gate que nunca se propuso el trabajo, es uno que declara haberlo hecho.


**Total: 19 bloqueantes confirmados.**

## Lo que el auditor NO vio, y el escéptico sí

106 huecos más, encontrados al revisar. Aquí sólo los de severidad alta o bloqueante;
el resto vive en el informe de cada dimensión.

**Toolsets Mechanical y Electrical**

- *bloqueante* · **La etiqueta de componente (-LT1) TAMPOCO se dibuja ni sale como ATTRIB: es el mismo defecto del número de hilo, en el sitio donde el repositorio jura lo contrario** — El auditor elogió AETAG por escribir en los ATRIBUTOS «con el argumento correcto: un atributo se dibuja y viaja al DXF como ATTRIB», y ESCALERA.md lo declara en peldaño 5 con esa misma frase. Lo comprobé y no ocurre. cadMepBlockDefinition (mep-symbols.ts:158-160) devuelve `{id, name, basePoint, entities}` — SIN campo `attributes`, o sea sin ninguna ATTDEF. Y las dos rutas de salida exigen esa declaración: el trazador dibuja los atributos recorriendo `Object.entries(block.attributes ?? {})` (paper-space.ts:714) y el exportador escribe ATTRIB sólo tras `.filter(([tag]) => !!definition?.attributes?.[tag])` (dxf-export.ts:791-794). Sin ATTDEF: cero texto en la lámina, cero ATTRIB en el DXF; el -LT1 sobrevive únicamente en la XDATA propia de Valle, que sólo Valle lee. La prueba de que es un olvido y no una decisión está a un fichero de distancia: el módulo hermano de Plant SÍ lo declara — cadPidBlockDefinition (plant/pid-symbols.ts:192) lleva `attributes: { TAG: { prompt: "Etiqueta del equipo", default: "" } }`, y por eso la etiqueta de equipo sí se traza. Consecuencia: de las dos únicas cosas que el toolset Electrical pone sobre el papel —el número del hilo y la etiqueta del componente— NINGUNA se imprime. El plano eléctrico que sale de Valle hoy es un montón de rayas amarillas y símbolos mudos. Esto hunde el 1,5/10 de Electrical más de lo que el auditor supo, y a la vez es de los arreglos más baratos del informe: una línea en mep-symbols.ts copiada de pid-symbols.ts.
- *alta* · **Los informes eléctricos no son entregables: AEWIRELIST y AETAGLIST son un renglón truncado, sin tabla y sin CSV** — La rúbrica cobra 2 puntos por «Informes: listado de conductores, cuadro de cargas en el dibujo y revisión contra la NOM» (toolset-electrical.informes). El cuadro de cargas sí es una TABLE del dibujo; el LISTADO DE CONDUCTORES no existe como entregable. AEWIRELIST está declarado `kind: "inquiry", mutates: false` (command-manifest.ts:128) y su salida es un renglón de la línea de comandos que además se TRUNCA a propósito: DE_A_VISIBLES = 6 y SUELTOS_VISIBLES = 3 (engine/commands/electrical-wire.ts:247-250). En un tablero de 60 conductores, el electricista ve seis. Y no hay salida a fichero: DATAEXTRACTION → CSV (engine/commands/data-extraction-commands.ts:101-116) exporta buildCadBimSchedule —el cuadro de cantidades de obra— y no los circuitos ni la lista de conductores ni la lista de materiales mecánica. En AutoCAD Electrical los informes son el producto que se le manda al tablerista, en Excel o como tabla en la lámina. Aquí el mejor trabajo del módulo —el DE/A con su criterio declarado, que es de verdad excelente— muere en la barra de estado cuando se teclea el siguiente comando.
- *alta* · **El criterio de la rúbrica se llama «Esquemas eléctricos: símbolos normalizados» y su evidencia es un fichero de símbolos de PLANTA — y ni la rúbrica ni ESCALERA declaran que no hay símbolos de esquema** — El auditor renunció a esto por educación («No discuto la rúbrica: sus criterios declarados verifican de verdad») y ahí dejó pasar la única falla de honestidad de toda la dimensión, que es la que a esta casa más le importa. El criterio toolset-electrical.esquemas de docs/competitive/rubric.json cobra 2 puntos por un texto que empieza con la palabra «Esquemas» y aporta como evidencia `apps/web/src/lib/cad/mep-symbols.ts`, que son cuatro símbolos de instalación en planta. El campo `gap` de esa misma fila enumera con toda honradez cinco ausencias —referencias cruzadas, escalerilla, E/S de PLC, plano de gabinete, catálogo de fabricante— y NO menciona la mayor: que no existe un solo símbolo de esquema de control. Igual la sección de ESCALERA.md, que se titula honestamente «La instalación eléctrica (Ola 5)» y declara cuatro peldaños 0 sin declarar éste. Regla de la casa: lo parcial se declara «todavía no». Aquí lo parcial se declaró en cuatro sitios y se saltó el quinto, que es justo el que decide si un despacho puede cambiarse el lunes. La fila no debería seguir cobrando 2/2 en `esquemas` con esa evidencia, o el criterio debería renombrarse a lo que verifica: símbolos de instalación.

**Visualización 3D: estilos visuales, render, materiales, luces, navegación**

- *alta* · **El GLB —la vía de escape que él aplaude— exporta el modelo con las caras del revés** — En §2.7 celebra `exportGltf` como la salida a Blender, y en §4.1 demuestra que los tres constructores nativos invierten el giro. No juntó las dos cosas. lib/cad/glb-export.ts recoge por su papel los MISMOS objetos de escena (`architecture: CadNativeMassHosts.group + CadSolidShadeHost.group`, cabecera del módulo y `CadGlbExportGroups`), así que el `.glb` sale con los índices invertidos horneados en la geometría. El cliente que abre ese archivo en Blender, en un visor web o en otro CAD lo ve con el backface culling al revés y las normales contra la cámara. Es decir: el único camino que hoy permite «materializar» el modelo fuera del producto también está roto, y arreglar §4.1 lo arregla gratis. Severidad: alta.
- *alta* · **«Exportar imagen (PNG)» captura el plano con la cámara en PERSPECTIVA** — El bucle de cuadros dibuja con `activeCamera()` = `viewController.camera`, que en modo 2D es la ORTOGRÁFICA (Layout3DEditor.tsx:7710 + view-controller.ts:142-144). Pero `exportPng` (Layout3DEditor.tsx:12495-12505) fuerza `r.render(sc, cam)` con `cameraRef.current`, que es la `PerspectiveCamera` cruda de :5985. El botón está siempre en la barra (:15301), no gateado a 3D. Resultado: en planta, el PNG que se le manda al cliente NO es lo que hay en pantalla, y lleva exactamente la deformación que lib/cad/view/perspective-distortion.spec.ts:24-31 declara inaceptable («una entidad elevada se dibuja MÁS GRANDE que otra idéntica a cota cero»). Es un fix-or-hide incumplido en la única salida de imagen del producto, y el auditor lo dio por bueno. Severidad: alta.

**Toolset Architecture**

- *alta* · **BLOQUE sobre un muro lo hace INVISIBLE, borra el original y se lleva la puerta por delante — en silencio** — `cadDefineBlockCommands` (blocks/block-workflow.ts:194-235) no filtra tipos: acepta cualquier entidad designada, y con la disposición por defecto (`insert`) BORRA los originales (línea 229). Pero el teselador de hijos de bloque sólo conoce nueve tipos —line, circle, arc, ellipse, spline, polyline, hatch, mtext, dimension, mleader (block-text-adapters.ts:271-284)— y para todo lo demás `return []`. Un muro dentro de un bloque no dibuja nada. Y como el muro original se borra, el GC transaccional retira además sus huecos. Secuencia real: designo tres muros con sus puertas, tecleo BLOCK, y el trozo de planta desaparece del dibujo sin una advertencia, sin quedar en el manifiesto de pérdidas y sin que REVISA lo vea. Es exactamente la clase de fallo que este repositorio persigue —geometría que se va callada— y no está declarado en ningún sitio: `grep -rn blockChildPaths` no encuentra ninguna spec que cubra wall ni opening, y ESCALERA.md no lo menciona. El arreglo barato y fiel a la casa es fix-or-hide: que BLOCK se niegue nombrando el muro, como OFFSET ya hace.
- *alta* · **COPIAR un muro con su puerta deja la puerta en el muro ORIGINAL** — `hostId` sólo se escribe al colocar el hueco (engine/commands/draw-opening.ts:332) y NADIE lo reasigna después: `grep -rn hostId --include=*.ts` fuera de specs devuelve seis sitios, todos de lectura (bim-schedule.ts:213-216, wall-entity-adapter.ts:141,220, plant/clash.ts:447). La rama `copy` de entity-commands.ts:401-410 clona la entidad con id nuevo y le aplica la traslación, y para un `opening` `transform` deja `position` intacta a propósito (opening-entity-adapter.ts:298-316). Resultado: designo un muro y su ventana, hago COPY, y la ventana copiada se aloja OTRA VEZ en el muro de origen —dos ventanas superpuestas en la fachada vieja— mientras el muro nuevo sale ciego. El cuadro de carpintería cuenta 2, que es el total correcto, así que la tabla no delata el error: hay que verlo en el plano. Ninguna spec lo cubre (`grep -rln hostId --include=*.spec.ts` no incluye entity-commands.spec.ts ni modify-basics.spec.ts). Lo mismo aplica a MIRROR y ARRAY, que pasan por la misma orden `copy`. Copiar una crujía es lo primero que hace cualquiera que dibuja un edificio.

**Toolsets MEP y Plant 3D**

- *alta* · **Ni la lista de líneas ni el metrado de tubería pueden salir a la lámina como TABLE: la planta es la única de las tres disciplinas sin cuadro en el plano** — DATAEXTRACTION ofrece cuatro cuadros —Superficies, Instalaciones, Circuitos y Carpintería (engine/commands/data-extraction-commands.ts:44-49, :134-154)— y ninguno es de planta. PIDLIST y PIDMTO son `kind: inquiry` (command-manifest.ts:235, :237): escriben un renglón en la línea de órdenes y se lo lleva el viento. El único cuadro de materiales que llega al papel es el que PIDISO dibuja dentro del isométrico (plant-iso.ts:122). O sea: la lista de líneas y la requisición del proyecto no existen como entrega, mientras el lado MEP y el eléctrico sí tienen su TABLE en la lámina y la rúbrica los cobra por eso (toolset-mep.tablas, toolset-electrical.informes). Es el mismo defecto de H1 —el dato existe y el papel no lo ve—, en la mitad que el auditor no miró, y es la palanca más barata del frente: dos funciones al lado de mep-schedule-table.ts.
- *alta* · **El contorno a doble línea del ducto y de la charola no tiene huella ni aviso de «quedó viejo»** — mep-tracing.ts:251-259 persiste DOS entidades independientes: el anillo (metadata.outline) y el eje (metadata.axis). mep-runs.ts:105 salta el contorno, así que el cuadro y el análisis de choques leen el EJE. No existe nada equivalente a la huella `pl:huella` y a cadPipeSolidsStale que el sólido de tubería sí tiene (pipe-solid.ts:96, :361, avisado en PIDMTO desde plant-route.ts:470). Consecuencia: mueva un vértice del eje con MOVE o STRETCH y el ducto dibujado se queda donde estaba, en silencio; el plano y la medición dicen cosas distintas y nadie avisa. La casa ya resolvió exactamente este problema para el sólido y razonó por escrito por qué («un sólido que miente en el visor…», pipe-solid.ts:65-70); el ducto se quedó con la misma deuda y sin el aviso.
- *alta* · **device-tags.ts afirma en su cabecera —y su spec la repite— que la etiqueta «se DIBUJA» y «viaja al DXF como ATTRIB», y ninguna spec lo comprueba mientras el código lo desmiente** — Es la clase de claim que esta casa existe para cerrar. device-tags.ts:16-24 y device-tags.spec.ts:11 lo declaran como razón de diseño (por eso la etiqueta va en atributos y no en metadatos), pero para los bloques MEP —que son los que AETAG etiqueta, device-tags.ts:172— la definición no declara ningún atributo (mep-symbols.ts:157-159), así que dxf-export.ts:794 la filtra y el ATTRIB nunca se escribe. `grep -rn 'ATTRIB' electrical/*.spec.ts` devuelve sólo esa frase de prosa: cero afirmaciones. La fila toolset-electrical.esquemas está cobrada con `device-tags.spec.ts` y `mep-symbols.ts` como evidencia, y la mitad del argumento de esa evidencia no se sostiene.

**Modelado 3D: sólidos, superficies, mallas, SCU**

- *alta* · **Dentro de un comando, designar un sólido en 3D va por su SOMBRA en el plano de trabajo — el repositorio ya diagnosticó este defecto y lo arregló sólo para el enganche** — Hay DOS semánticas de designación en el mismo visor y sólo una es correcta. La selección libre (fuera de comando) sí usa rayo 3D de verdad: Layout3DEditor.tsx:6885-6893 hace `raycaster.intersectObjects(nativeGroup.children, true)` y sólo cae al plano si no hay impacto (:6905-6918). Pero el puente del motor de comandos (:6691-6717) define `hitEntity: (point) => hitCanonical(point,1)[0]?.id ?? null` alimentado por `worldPoint: cadDrawingPointOrNull(floorWorld(event))`, y `floorWorld` en 3D (:6447-6452) devuelve la intersección del rayo con el PLANO DE TRABAJO; `hitCanonical` (:6465-6471) consulta el índice 2D, cuyo hit-tester para solid3d es `solidPaths` — la proyección en planta (solid3d-adapter.ts:97-117). Consecuencia: UNION, SUBTRACT, INTERSECT, SLICE, SECTION, FILLETEDGE, CHAMFEREDGE, INTERFERE y SOLIDEDIT·Cuerpo, que todos piden CAD_ACCEPT_ENTITY_PICK, designan la pieza que está bajo la sombra del cursor en el suelo, no la que se ve. En una caja de 3 m de alto vista a 50° eso son metros de error, y en un sólido elevado no se designa nunca. Lo demoledor es que el repositorio YA ESCRIBIÓ este diagnóstico, palabra por palabra, en lib/cad/view/solid-snap.ts:12-18 — «para engancharse a la esquina superior de una caja, hay que colocar el cursor sobre la SOMBRA de esa esquina en el suelo, no sobre la esquina que se ve dibujada. Es un enganche que existe pero que nadie puede usar» — y lo resolvió para OSNAP construyendo un índice de pantalla, sin llevar el mismo arreglo a `hitEntity`. El atajo existe (designar antes de teclear la orden: casi todas son selection:'optional'), pero es justo el gesto que un usuario de AutoCAD no hace en 3D. El auditor vio el subobjeto (hueco 9) y no vio que falla la designación de la ENTIDAD.
- *alta* · **El modelo 3D no sale del navegador por NINGUNA vía: ni STEP/IGES descargable, ni DXF, ni una sola malla — y eso a pesar de que entran cuatro** — El auditor separó el hueco 5 (DXF) del 8 (descarga de STEP/IGES) y no cerró el círculo ni miró la tercera puerta. EXPORT ofrece exactamente dos formatos (solids-interop.ts:35-36, :112-113: FORMAT_STEP y FORMAT_IGES) y ninguno se descarga. No existe escritor de malla alguno: `grep -rn 'writeStl|exportStl|exportObj|writeObj|toStl' apps/web/src/lib` → cero, mientras lib/cad/interop/ tiene cinco LECTORES (obj-mesh-reader.ts, stl-mesh-reader.ts, gltf-mesh-reader.ts, collada-mesh-reader.ts y el detector de formato). Es una asimetría total: entran OBJ, STL, glTF y COLLADA y no sale ninguno. Para un despacho eso cierra tres flujos completos de golpe: impresión 3D (STLOUT/3DPRINT en AutoCAD), render en un programa externo, y mandarle la pieza a alguien que no tenga cuenta. Y `tessellateBody` ya produce posiciones e índices, así que un escritor STL ASCII son cuarenta líneas — el mismo tamaño que el arreglo del hueco 8. Juntos convierten «no sale» en «sale por tres puertas» en un día.

**Interoperabilidad: DWG, PDF, IFC, nubes de puntos, Revit**

- *alta* · **El DXF se rechaza de plano por encima de 12 MB, y un plano ejecutivo real pesa más** — document-import-validation.ts:27 `export const MAX_DXF_IMPORT_BYTES = 12_000_000`, aplicado en importLimitForFileName (línea 60) y lanzado como error duro en validateImportFile (líneas 133-137). No hay ruta alternativa: `grep -rn "dxf" apps/api/src/modules/cad/*.ts | grep -i import` no devuelve ningún importador de servidor, así que no existe una puerta que trague más. La aritmética asusta con las propias medidas del proyecto: floorplan.dxf son 1,1 MB para 961 entidades, o sea ~1,1 KB por entidad; 12 MB son unas 10 000 entidades. Un plano arquitectónico ejecutivo completo con sus capas, sus sombreados y sus cotas pasa de 50 000 entidades sin esfuerzo y su DXF de texto ronda los 60-200 MB. AutoCAD no tiene ningún tope de este tipo. Esto significa que el flujo que el auditor pone como escenario central —«el estructurista me manda el plano y me pide que se lo devuelva»— falla POR TAMAÑO incluso cuando el remitente hace el favor de exportar a DXF, que es justo la salida que el producto le recomienda al usuario en DWG_IMPORT_DISABLED_REASON. Es un hueco de la misma familia que el DWG y de gravedad comparable, y no aparece en ninguna de las quince fichas.
- *alta* · **El estudio tiene su PROPIA puerta de importación que se salta el validador y la beta DWG entera** — Hay dos puertas y dan respuestas distintas al mismo fichero. La del tablero (dashboard/page.tsx → document-import-client.ts:106 → worker) resuelve las cuatro banderas de beta y, con NEXT_PUBLIC_DWG_NATIVE_IMPORT_BETA=true, ADMITE un .dwg AC1015/AC1018. La del estudio (Layout3DEditor.tsx:10404-10419, `onDxfFile`, con `accept=".dxf,.dwg"` en la línea 15314) no llama a validateImportFile, no conoce ninguna beta, tiene su propio tope de 12 000 000 escrito a mano —y en unidades UTF-16, no bytes— comprobado DESPUÉS de materializar el fichero entero como cadena, y responde SIEMPRE `toast.error(DWG_UNAVAILABLE_REASON)` a cualquier DWG. Es decir: en un despliegue con la beta firmada encendida, el mismo .dwg entra por el tablero y el estudio le dice al usuario que el editor no lee DWG. El auditor sí encontró los tres defectos técnicos de esa función (su D-4) pero no vio lo que significan: que la superficie más visible del producto miente sobre una capacidad que el propio producto ya tiene encendida, que es el fallo de veracidad más caro del catálogo de esta casa.

**Trabajar con otros: xrefs, contenido, revisión, versiones**

- *bloqueante* · **El plano del estructurista llega APLASTADO a UNA sola capa gris: no puedo apagarle nada** — Es lo PRIMERO que hace un arquitecto con una xref y aquí no se puede hacer. xref-projection.ts:100-105 clona cada entidad con `detachedEntity(entity, id, cadXrefLayerId(xrefId), …)`, y esa función (:62-79) devuelve `{...structuredClone(entity), id, layer}` — sobrescribe la capa de CADA entidad con una sola, `xref:<id>:layer`. Esa capa se crea en :145-158 con nombre `XREF|<nombre>`, color fijo `#64748b` y grosor fijo 0,18. Traducción: las cuarenta capas del estructurista se convierten en una capa gris. No puedo congelar sus textos, ni apagar su retícula, ni bajarle el grosor, ni atenuarlo: no hay VISRETAIN ni XDWGFADECTL (grep sobre apps/web/src: 0; `fade` sólo existe para imágenes ráster, cad-entities-v4.ts:169). El comando XREF ofrece Descargar, Recargar, Desligar, Enlazar y Ruta (xrefs.ts:115-120) — ninguna opción de capas. Todo lo que puedo hacer es apagar la xref ENTERA. Y toca la evidencia: la fila `xrefs.resolution` de rubric.json cobra 2 puntos por «Resolución de recursos, CAPAS DE XREF y bind con round-trip» con una sola spec de bind; docs/competitive/distancia-autocad-completo-20260901.md ya lo tenía medido y escrito («las capas se aplastan a una (medido)») y la fila sigue cobrando. Esto duele más que el catálogo: con el catálogo arreglado, lo que se adjunta sigue sin poder gestionarse.
- *bloqueante* · **El botón «Versiones» de la barra abre un diálogo cuyo servidor devuelve 404 SIEMPRE** — Fix-or-hide, en la superficie, sobre un control visible. Layout3DEditor.tsx:15361-15367 pinta un botón con title «Versiones / escenarios — guardar, restaurar» que llama a `openVersions` (:10522) → `loadVersions` (:10513) → `legacyCadFetch("layout/snapshots?…")`. Esa ruta NO EXISTE: layout-http-adapter.ts:46-49 la declara por escrito entre los endpoints «SIN EQUIVALENTE EN /v1/cad → 404 limpio», y al no casar ningún `if` cae en `legacyNotFound(route)` (:175, :218-227). Consecuencias exactas: la lista de «versiones del servidor» del diálogo está SIEMPRE vacía, y pulsar «Guardar versión» (saveVersion, :10575-10598) siempre entra por `if (!r.ok)` y saca `toast.error("No se pudo guardar la versión.", "Versiones")`. Lo mismo `restoreVersion` (:10601) y `deleteVersion` (:10622). Es decir: el estudio tiene una puerta al historial, es la única que el usuario encuentra, y no abre. Esto corrige y agrava H3 a la vez —el auditor dijo «la interfaz no tiene ninguna puerta» y la tiene, rota— y es exactamente lo que la regla 3 de cimientos prohíbe.
- *alta* · **La biblioteca del despacho se corta a 200 documentos sin decirlo, y no sólo en la resolución de xrefs** — El auditor cazó el tope de 200 en `resolveDocumentId` (su D3, layout-http-adapter.ts:302-313) pero se dejó que el MISMO tope está en la única pantalla que lista los dibujos de la oficina: apps/web/src/app/dashboard/page.tsx:141-142 pide `projects.list({limit:200})` y `documents.list({limit:200})` y ahí acaba — no hay `offset`, no hay cursor, no hay bucle, no hay buscador contra el servidor (grep de offset/cursor sobre el fichero: nada). 200 es el tope duro del contrato (@Max(200), cad.dto.ts:44). En un despacho con 201 dibujos, el 201 no existe para el producto: ni sale en el tablero, ni se puede abrir desde él, ni se puede resolver como xref. Y no hay ningún aviso de truncamiento: la lista simplemente termina. Es el fallo que aparece cuando el cliente ya pagó, y aparece en dos sitios, no en uno.

**Espacio papel, láminas y publicación**

- *bloqueante* · **A · El producto tiene DOS emisores de PDF que no se parecen, y el botón usa el pobre** — El auditor vio la divergencia del recorte y la contó al revés. La verdad estructural: el botón de publicar —`publishSheetSetPdf`, Layout3DEditor.tsx:13366-13391, el mismo flujo que calcula el SHA-256 y registra la publicación auditada que el informe elogia en su §1.7 y sobre el que apuesta en su §4— llama a `buildCadPublishPlan` y de ahí DIRECTO a `renderCadSheetSetPdf` (components/cad/editor/sheet-set-pdf.ts, 233 líneas). Nunca pasa por `buildCadPlotJob`. Consecuencias medidas: (1) el cajetín paramétrico ISO 7200 / mexicano de plot/title-block.ts —«la mejor pieza de esta dimensión» según el propio auditor— NO se usa: `grep -rn layoutCadTitleBlock` da plot-job.ts:240, plot-pdf.ts:326 y starter-templates.ts, ninguno en ese camino; sheet-set-pdf.ts:192-216 dibuja un cajetín de ocho celdas cableado a mano. La responsiva del DRO no sale por el botón. (2) La tabla de plumas no se aplica: `styleCommand` vive en plot-job.ts y ese camino no lo llama. Cargar la CTB del despacho y pulsar publicar no cambia un byte. (3) No hay comprobación previa: `preflightCadPageSetup` sólo corre dentro de `buildCadPlotJob`. (4) Ninguna `CadPageSetup` se consulta: papel, márgenes y escala de grosores del PAGESETUP no llegan. Lo que el auditor llamó «regresión del camino nuevo» es en realidad un producto con dos verdades y la que entrega es la que menos sabe.
- *alta* · **B · Una capa marcada «no se imprime» se imprime, en los dos PDF, y la interfaz dice lo contrario** — `CadLayerDef.plot?: boolean` existe (cad-document.ts:406). La paleta de capas tiene el interruptor con su `data-testid="cad-layer-plot-${row.id}"` y su título «No se imprime» (CadLayerManagerPalette.tsx:233-241); `use-layer-actions.ts:94-99` lo persiste con `updateCadDocumentLayer(document, id, { plot })` y le contesta al usuario «Capa X no se imprime.»; y la norma mexicana hace nacer la capa AUXILIAR con `plot: false` y la razón escrita —«Construcción auxiliar: se dibuja para apoyarse, no se imprime»— en standards/mexican-layers.ts:143-150, con su regla citada en mexican-drafting-sources.ts:479. Pero la publicación NUNCA consulta ese campo: el único filtro es `visibleLayer` (paper-space.ts:396-405), que delega en `cadLayerShown` = `layer.visible && layer.frozen !== true` (cad-layer-visibility.ts:29). `grep -rn "layer.plot\|plot === false\|plot !== false" apps/web/src` no devuelve ni un consumidor en el camino de trazado. Resultado: los ejes de construcción, los DEFPOINTS y la capa AUXILIAR de la norma del propio producto salen impresos en el plano que entra a la alcaldía. Es exactamente el claim sin evidencia que esta casa prohíbe —la interfaz afirma un comportamiento que los bytes no cumplen— y NO está declarado: cero menciones en rubric.json, ESCALERA.md ni BACKLOG.md. Coste: una línea en `visibleLayer` más su spec.
- *alta* · **C · MVIEW Desactivada apaga la ventana en PLOT y no en el botón de publicar** — `cadViewportIsOn` (viewport-operations.ts:56-58) lee la clave reservada `"*"` de `layerVisibility`, y `buildCadPlotJob` la respeta: plot-job.ts:208 filtra las apagadas a `skippedViewports`. Pero `buildCadPublishPlan` no la conoce —paper-space.ts:808 hace `(space.viewports ?? []).map(...)` sin filtro alguno, y `grep -rn cadViewportIsOn apps/web/src` no da ningún hit en paper-space.ts—. Como el botón de publicar consume el plan crudo (hueco A), una ventana que el usuario apagó con MVIEW Desactivada vuelve a aparecer en el PDF que entrega. Es el mismo defecto de fondo que el recorte, en otro campo, y ninguno de los dos lo vio el auditor.

**Toolsets Map 3D y Raster Design**

- *alta* · **El escaneo adjunto es una puerta de un solo sentido: no hay IMAGEDETACH, PURGE no conoce las imágenes y ninguna paleta las lista** — Es el hueco más grave de Raster que no está en su lista, y es de los que rompen el documento en vez de limitarlo. `entity-commands.ts:376-383` sólo sabe DAR DE ALTA una definición de imagen (`type:"image-definition"` hace upsert); no existe ninguna orden de baja — grep -w IMAGEDETACH, IMAGEUNLOAD e IMAGERELOAD sobre apps/web/src = 0, mientras el sustrato PDF sí tiene PDFDETACH, PDFUNLOAD y PDFRELOAD (ribbon.ts:100,171). PURGE no las alcanza: sus candidatos son `kind: "block"|"layer"|"style"` (lib/cad/blocks/cad-purge.ts:58) y AUDIT tampoco, porque sus huérfanos son `orphan-layer|unused-block|unused-style` (lib/cad/audit/orphans.ts:16). Y no se ven en ningún sitio: `CadXrefPalette.tsx` —la paleta de referencias externas, que es donde AutoCAD guarda imágenes y xrefs juntas— sólo recibe `CadExternalReference[]`; grep de `image` sobre components/cad/palettes/ sólo devuelve un `data:image/svg+xml` de miniatura de bloque. Consecuencia concreta: adjunto el escaneo equivocado de 8 MB, borro la entidad IMAGE, y los 10,7 MB de base64 se quedan en el documento para siempre — no hay orden que los quite, no hay pantalla que los enseñe y no hay auditoría que los nombre. El tope de 8 MB es POR ARCHIVO (image-attach-payload.ts:28), así que se apilan. En el flujo que la Ola H vende —probar tres escaneos hasta dar con el bueno— el documento sale con los tres dentro y el usuario no puede saberlo.
- *alta* · **Mover el marcador de georreferencia falsea el dibujo en silencio; borrarlo, al menos, se nota** — El auditor cargó contra el borrado del marcador (su H-4) y se dejó el modo de fallo peor, que es el que de verdad es silencioso. El marcador ES el ancla: `cadGeoreferenceOf` (georeference.ts:64) devuelve `anchor: {x: entity.position.x, y: entity.position.y}`, y el POINT es una nativa como cualquier otra, así que MOVE, STRETCH con ventana de cruce, un tirón de pinzamiento o un MIRROR sobre la capa GEO —creada `locked:false` en las dos rutas, geo-location.ts:107 y geo-import-plan.ts:130— la desplazan. Borrarla degrada a ruidoso: ID deja de informar coordenadas y MAPIMPORT cambia de modo y lo dice en su plan. Moverla no degrada a nada: ID sigue contestando un Este/Norte con dos decimales, MAPIMPORT sigue diciendo «colocadas con la georreferencia del dibujo», y todo el predio está desplazado exactamente lo que se movió el punto. Un CAD que da precisión que no tiene es lo que la casa persigue en el rechazo del NAD27; aquí ocurre por un arrastre de ratón.

**Automatización y productividad del despacho (auditoría 12)**

- *alta* · **`PAUSE` no existe: `(command "LINE" p1 PAUSE "")` —el idioma con el que está escrita media biblioteca de despacho— muere** — `grep -rn PAUSE apps/web/src/lib/lisp/` devuelve CERO. Y lib/lisp/builtins/interaction.ts:571-577 lo declara con todas las letras al quedarse sin argumentos: «A diferencia de AutoCAD, aquí el comando no queda activo esperando al usuario: se cancela para no dejar el dibujo a medias». Una rutina portada que pide al usuario un punto a mitad de un `(command …)` —que es el patrón de CUALQUIER rutina de colocación: inserta el bloque donde yo señale— no arranca. El auditor puso «Sin… PAUSE» dentro de una celda de su tabla de sub-notas y NUNCA lo convirtió en hueco numerado, ni le dio fichero, ni línea, ni escenario de fallo. Para portar una biblioteca esto pesa tanto como el hueco 2 de los atributos, y a diferencia de aquél no aparece en ninguna de las quince entradas.
- *alta* · **AutoLISP no tiene NINGUNA salida a archivo: `open`, `write-line` y `getfiled` están declarados no disponibles** — lib/lisp/builtins/unavailable.ts:138-143 declara `getfiled`, `open`, `close`, `read-line`, `write-line`, `read-char` y `write-char`. Importa porque es la otra mitad del escenario titular del propio auditor: en su §3.2 dice que sus rutinas sirven para «leer el cajetín, sacar el cuadro de luminarias, renumerar equipos, EXPORTAR LA CARPINTERÍA A EXCEL». Arreglar los atributos (hueco 2) no desbloquea la cuarta: la rutina podría leerlos y no tendría dónde escribirlos —ni archivo, ni TABLE por entmake (hueco 13)—. El informe presenta el hueco 2 como si al cerrarlo la biblioteca arrancara; no arranca.
- *alta* · **El `.ctb` y el `.lin` del despacho ni siquiera llegan a localStorage: viven en la sesión y se pierden al recargar** — docs/parity/ESCALERA.md:359 lo declara: «Las tablas viven en la SESIÓN y se pierden al recargar»; lib/cad/engine/commands/plot-commands.ts:470 y settings-palettes.ts:374 confirman que el único camino es cargarlas de un archivo cada vez. El auditor listó el `.ctb` en la columna de lo que hace AutoCAD (§3.1, «una carpeta de red reparte… *.ctb…») y nunca midió el lado de Valle, así que su §3.1 los da por implícitamente equiparados al resto. No lo están: son PEORES que la biblioteca .lsp que sí documentó. Un despacho que traza con su tabla de plumas la vuelve a cargar en cada pestaña, todos los días.

**La cinta y cómo se usa de verdad**

- *alta* · **El catálogo de paletas de herramientas no tiene NINGUNA escritura: save() y remove() no los llama nadie en producción** — Lo apunto aparte porque cambia el plan del hueco 5, no sólo su descripción. `grep -rn "toolPalettes" apps/web/src` (excluyendo specs) devuelve cinco sitios: la construcción en session-catalogs.ts:100-105, la declaración del tipo en command-types.ts:381, y dos lecturas en settings-palettes.ts:295 y :308, ambas dentro de `-TOOLPALETTES`, que es `mutates: false`. Los métodos `save` (tool-palettes.ts:185) y `remove` (:193) sólo se ejercitan en session-catalogs.spec.ts. Es decir: el usuario no puede crear, editar ni borrar una paleta por ningún camino existente — ni por interfaz, ni por la línea de comandos. Lo único que hay son las tres paletas de fábrica (Dibujo, Modificación, Consulta; tool-palettes.ts:71-100), todas de comandos y ninguna de bloques. Consecuencias: (a) el informe dice «el modelo de datos ya está construido y no tiene interfaz» cuando lo correcto es «está construido, no tiene interfaz Y no tiene camino de escritura»; (b) el golden que propone —«crear una paleta con -TOOLPALETTES, pulsar Ctrl+3…»— no es ejecutable hoy; (c) el argumento de venta que dibuja («los 40 bloques del despacho, con su escala») está a dos capacidades, no a una. La regla 1 de la campaña de cimientos muerde más fuerte de lo que él escribió.

**Seguridad: autenticación, secretos, multiusuario, OWASP**

- *alta* · **`/v1/messaging` —el chat del equipo, donde se habla de los clientes— es la mayor superficie de inquilino sin una sola prueba de aislamiento contra PostgreSQL real** — `ls apps/api/src/modules/messaging/*.spec.ts` devuelve UN fichero, `messaging.controller.spec.ts`, un spec de unidad. No hay `.pg.spec.ts`. Compárese con el resto del árbol: `cad`, `cad-presence`, `commercial` (siete), `identity` (cuatro), `organizations`, `feedback`, `support`, `legal` y `audit-log` TODOS tienen el suyo — `ls apps/api/src/modules/*/*.pg.spec.ts` da 27 ficheros y ninguno es de messaging. Y messaging queda además fuera del barrido programático, que filtra `/v1/cad` (hueco 12). Su aislamiento descansa por entero en que `messaging.service.ts` use bien `TenantScopedRepository` y `tenantCtx.requireTenantId()` (líneas 235-242, 325, 357-359) — y la cabecera de `tenant-scoped.repository.ts:22-33` declara ella misma que catorce métodos delegan SIN filtro de tenant. Es exactamente el tipo de superficie donde una fuga entre despachos sería catastrófica y silenciosa: mensajes sobre proyectos de clientes. El auditor celebró el barrido de `/v1/cad` como «la mejor pieza de seguridad del repositorio» y no preguntó cuál era la pieza más valiosa que se queda fuera de él.

**Frontend: arquitectura, estado, monolitos (apps/web)**

- *alta* · **El guardián sin-IA declara un detector de prosa y NUNCA lo pasa por el árbol: tres cadenas de «copiloto» sobreviven a la vista del usuario** — Es la CAUSA RAÍZ del hueco 2, y el auditor encontró el síntoma sin ella. lib/cad/no-ai-boundary.spec.ts existe porque IDENTITY.md dice que Valle Design no tiene IA, y su propia cabecera fija la frontera «en lo que el usuario VE o TECLEA». Define para eso RECLAMA_EN_PROSA en :57 — y `grep -n RECLAMA_EN_PROSA` sobre el fichero devuelve exactamente tres líneas: la declaración (:57) y dos assert.ok de autocontrol contra cadenas literales (:80-81). NUNCA lee un fichero. Sus tres bloques reales sólo miran nombres y alias de comandos, la lista de módulos retirados vía `git ls-files`, y el YAML del contrato. Consecuencia medida con `grep -rn 'copiloto|Copiloto' --include=*.ts --include=*.tsx src`: sobreviven TRES cadenas que el usuario lee — palette-actions.ts:124 («Preview listo en el Copiloto CAD.»), lib/cad/commands/registry.ts:361 (`title: "Comandos del copiloto CAD"`, el título de un informe que se pinta) y registry.ts:1165 («Lista todos los comandos del copiloto…»), que command-palette.ts:68 renderiza dentro de la propia paleta Ctrl+K como «Frase · Lista todos los comandos del copiloto…». El arreglo es una regla más en el guardián que ya existe: pasar RECLAMA_EN_PROSA por los .tsx/.ts de src con una lista de exenciones para los comentarios históricos que el propio guardián dice que sí puede haber. Sin eso, borrar el toast del hueco 2 deja las otras dos vivas y el mismo defecto vuelve.
- *alta* · **La paleta Ctrl+K —la superficie insignia— no tiene ni una sola marca de accesibilidad ni una sola prueba de extremo a extremo** — Layout3DEditor.tsx:16251-16252 es `{showPalette && (<div className="absolute top-3 right-3 …">`: un div pelado. Barrí las noventa líneas del bloque buscando `role=`, `aria-` y `data-testid` y no hay NINGUNO. No usa CadDialogShell ni Modal, así que no hereda nada de la trampa de foco que el hueco 10 daba por ausente y que sí existe para los ocho cuadros. `grep -rn 'role="listbox"|role="option"|aria-activedescendant' --include=*.tsx src` devuelve CERO en todo el árbol, así que la lista de resultados tampoco tiene semántica de lista. Y no hay cobertura: `grep -rn 'showPalette|Ctrl K|Cmd-K' e2e/` sólo encuentra un comentario en e2e/auditoria/refutacion-palabra-imprimir.spec.ts:13; ningún golden la abre. El auditor sí notó que palette-actions.ts no tiene specs, pero se le pasó que la paleta ENTERA —el gesto que un usuario de AutoCAD prueba en el primer minuto, con sus 291 comandos del motor más las 40 frases más los símbolos— no está gateada ni por axe ni por teclado ni por golden. Para un lector de pantalla, abrir Ctrl+K no anuncia nada; para quien navega con teclado, Tab sale de la paleta al lienzo de detrás. Es exactamente donde el hueco 2 vive, y explica por qué nadie lo cazó.

**Landing, alta de cuenta y primeros cinco minutos**

- *bloqueante* · **El agujero de plantillas huérfanas es de CUATRO, no de una — y una de ellas es un aviso de seguridad** — El auditor encontró commercial.trial-expiry y paró. El mismo switch de email-templates.ts (4 casos, líneas 46/57/68/70) deja fuera otras tres plantillas que el árbol ENCOLA hoy: identity.new-sign-in (identity.service.ts:543 — el aviso «alguien entró en tu cuenta desde un dispositivo nuevo», con su idempotencia por sesión y su spec en identity-mfa.pg.spec.ts:383), product.feedback (feedback.service.ts:119) y support.incident (support.service.ts:69). Las tres siguen el mismo camino: EmailTemplateError('unknown_template') → outbox-receiver.service.ts:76 → 200 con status 'ignored' → entrega marcada como hecha. Consecuencias que el informe no cuenta: el usuario nunca recibe el aviso de acceso desde un dispositivo nuevo (un control de seguridad muerto, peor que un recordatorio comercial); el operador nunca recibe los comentarios que el producto le pide al usuario desde /comentarios; y nunca recibe el correo de incidente de soporte. Y el motivo por el que CI está verde sobre las cuatro está a la vista: las dos specs que ejercitan el descarte (outbox-receiver.pg.spec.ts:136,144 y email-templates.spec.ts:118) usan una plantilla INVENTADA, 'marketing.navidad', nunca un nombre real del árbol. El gate de cobertura que el auditor propone es el correcto, pero el arreglo son cuatro renderizadores, no uno, y la prioridad la manda identity.new-sign-in, no el recordatorio de prueba.
- *alta* · **No existe arrastrar-y-soltar un DXF en ninguna parte del producto — ni en el estado vacío, ni en el tablero, ni en el lienzo** — El auditor escribió como línea base de AutoCAD «arrastrar un DWG/DXF al lienzo lo abre, siempre» y luego sólo auditó el botón. El árbol: `grep -rniE "onDrop=|ondragover|DataTransfer" apps/web/src` devuelve exactamente cuatro líneas, todas en components/cad/editor/Layout3DEditor.tsx:17822-17828, y son el reordenado interno de viewports de una lámina — no reciben ficheros. No hay un solo manejador de soltar-fichero en el tablero, en FirstMinute ni en el estudio. Es decir: el gesto canónico con el que un dibujante de AutoCAD abre un plano desde hace veinte años no existe en Valle Design, y el único camino es el `<input type=file>`. Duele exactamente donde el informe dice que duele (el profesional que ya tiene trabajo hecho y está evaluando en veinte minutos), y es más barato que cualquiera de los arreglos del §4.2, porque `importDocumentFile` y `splitDocumentSelection` ya aceptan un array de File: falta el onDrop.
- *alta* · **/demo YA abre el DXF del propio visitante, sin cuenta — y nada en el sitio lo dice** — El auditor trató /demo como «el editor real con un plano precargado» y construyó toda su §5 («la apuesta ganadora») sobre adoptar ESE plano. Se le escapó el activo mayor: DXFIN está en el registro de comandos (lib/cad/engine/command-manifest.ts:150, módulo commands/interop-dxf) y su implementación no toca la red — `grep -n "fetch|designClient|/v1/" lib/cad/engine/commands/interop-dxf.ts` devuelve CERO. El fichero entra por el canal de interfaz (command-types.ts:444, `engine.feedFile` en use-command-engine.ts:518), no por HTTP. Como /demo monta el mismo CadStudioHost con el puerto sin red (DemoStudio.tsx:54-65), un visitante anónimo puede teclear DXFIN y ver SU PROPIO plano en el editor en diez segundos, sin cuenta, sin instalar y sin subir nada a ningún servidor. Eso es literalmente la pregunta que decide una evaluación de CAD («¿abre bien MI archivo?») y la respuesta más difícil de dar que tiene este mercado. Y no lo dice nadie: ni el titular del héroe (page.tsx:382-387 dice sólo «Probar sin cuenta»), ni el metadata de /demo (demo/page.tsx:19-23 habla de «dibuja sobre un plano de casa habitación»), ni el banner permanente (DemoStudio.tsx:72-73). Ninguna prueba cubre DXFIN dentro de /demo. Es un hueco de PROMESA, no de capacidad, y por eso el auditor —que buscaba lo que falta— no lo vio: aquí sobra producto y falta el renglón que lo anuncia.
- *alta* · **El arreglo que el informe propone para el returnTo (§3.4) no entregaría el dibujo: choca con el botón apagado del §3.3** — El auditor trata §3.3 (crear en blanco) y §3.4 (returnTo) como dos huecos independientes con dos arreglos distintos, y estima §3.4 en «un día» de sessionStorage más un enlace. Pero el consumidor de la adopción es startDocumentContent(demoAdoption, galleryStart), invocado en un único sitio: dashboard/page.tsx:264, dentro de createDocument — y createDocument empieza en la línea 251 con `if (!canEdit || !name.trim() || !selectedProject || busy) return;`. Una cuenta recién verificada tiene cero proyectos, luego selectedProject === "", luego el botón que dispara createDocument está deshabilitado (page.tsx:657). Aunque el returnTo sobreviva a la verificación y el tablero reciba ?demo=1, el visitante llega a una pantalla que le enseña el aviso «tu documento nacerá del dibujo de la demostración» (DemoAdoptionNote) encima de un botón apagado. Los §3.3, §3.4 y §4.2 son el mismo defecto —ninguna pantalla del primer minuto sabe crear el proyecto implícito— y hay que arreglarlos juntos o no se arregla ninguno. Importa para la planificación: la «apuesta ganadora» del §5 no es un día de trabajo, y si se hace sólo la mitad que el informe describe, el CI se queda verde y la promesa del banner sigue incumplida.

**Accesibilidad, teclado e idiomas**

- *alta* · **El estudio no tiene ni un solo encabezado: cero <h1>…<h3> en 18.453 líneas** — `grep -c '<h1\|<h2\|<h3' apps/web/src/components/cad/editor/Layout3DEditor.tsx` → 0, y `grep -n '<h1' CadStudioHost.tsx` → sin salida. La navegación por encabezados es el PRIMER modo de recorrido de un lector de pantalla, antes que los landmarks: NVDA y JAWS enseñan a pulsar H antes que D. El auditor filó los landmarks y el skip-link y se dejó esto, que es el mismo agujero y se cierra con el mismo commit. Y se le escapa por exactamente el mecanismo que él mismo describió para la regla `region`: `page-has-heading-one` es de impacto **moderate**, y axe-estudio.spec.ts:27-29 filtra a serious|critical y las moderadas sólo las imprime por consola. El gate está verde y el estudio no tiene ni un título.
- *alta* · **El prompt vivo no se puede volver a leer: la caja de comandos no tiene aria-describedby** — CadCommandLine.tsx:213-221 da a la caja `aria-label="Línea de comandos CAD"` y nada más. El prompt («Precise el siguiente punto o [Cerrar/desHacer]») sólo existe como anuncio efímero de la región viva de :155-161. Quien no lo oyó —porque estaba en otro control, porque el lector estaba hablando, o porque los 60 nodos se remontaron (hueco 12)— no tiene NINGUNA vía de recuperación: el log no recibe foco (hueco 10), F2 no existe (hueco 10), y el nombre accesible del control no dice qué se está pidiendo. Es la diferencia entre una línea de comandos anunciada y una línea de comandos usable, y convierte tres huecos «media» sueltos en un callejón sin salida que ninguno de los tres describe por separado.

**Backend: API, datos, escalabilidad**

- *alta* · **El auditor auditó el endpoint equivocado: para todo plano de más de 1 MB el navegador manda PUT /archive, no PUT /content** — Todo su hueco 1 describe PUT /v1/cad/documents/:id/content. Pero apps/web/src/components/cad/document-lifecycle/controller.ts:155-165 elige saveArchive en cuanto el documento serializado supera CAD_DOCUMENT_ARCHIVE_THRESHOLD_BYTES = 1_000_000 (:14) — es decir, exactamente en los planos donde el coste importa. Ese camino es MÁS caro, no menos: cad.controller.ts:208-244 recibe hasta 20 MiB en memoria (FileInterceptor con memoryStorage), descomprime y valida con decodeCadDocumentUpload (que a su vez hace su propio stringify + parse en cad-document-validation.ts:725-734), y luego entra a saveContent con forceArchive:true, que vuelve a serializar y a comprimir. Y es el único camino que la evidencia de carga declara NO MEDIDO: api-load-tests.json → notMeasured, «Subida multipart de archivo (PUT /v1/cad/documents/:id/archive) y el camino gzip de 128 MiB». El camino caliente real del CAD es el que no tiene número publicado.
- *alta* · **La cadencia del autoguardado y el techo del limitador chocan exactamente: 30 contra 30** — El autoguardado es un debounce de 2 000 ms (Layout3DEditor.tsx:1338) — hasta 30 guardados por minuto. El techo de PUT /archive es cadArchiveWritePerDocument = 30 por minuto (api-rate-limit.service.ts:63) en ventana FIJA de 60 s (:41, store.consume(key, max, 60_000)). Un arquitecto que dibuja sin parar sobre un plano de más de 1 MB agota su ventana en los primeros ~60 s y ve «Demasiados guardados seguidos» (save-failure.ts:159-166), sin margen alguno para pulsar Guardar a mano. El mensaje es honesto y los cambios no se pierden, pero nada en el cliente lee retryAfterSeconds para reprogramar el autoguardado: el scheduler simplemente publica estado «error» (document-lifecycle/autosave.ts:52-54). Es el defecto que un usuario de AutoCAD siente en el primer minuto: «mi plano no se está guardando».
- *alta* · **No existe guardado incremental: el cliente reenvía el documento ENTERO cada dos segundos** — controller.ts:145-165 serializa el CadDocument completo en cada guardado; no hay delta, ni parche, ni lista de entidades sucias, en ninguno de los dos endpoints. AutoCAD hace guardado incremental desde R13 (ISAVEPERCENT) precisamente para que salvar un plano de 200 MB escriba sólo lo que cambió. Ésta es la decisión de diseño única que está debajo de los huecos 1, 3 y 5 a la vez: es la razón por la que el guardado cuesta CPU proporcional al plano entero, la razón por la que cada autoguardado engendra un blob nuevo completo, y la razón por la que el crecimiento por dibujante es de megabytes por minuto. Mientras no se declare como «todavía no» con su condición de reapertura, cada uno de esos tres huecos se va a tratar por separado y ninguno se va a cerrar.

**El flujo diario de dibujo 2D**

- *alta* · **Ningún prompt de «Designe objetos» acepta palabras clave: no hay Todo, Previo, Último, Ventana, Captura, Valla, Borrar ni Añadir** — En AutoCAD, TODA petición de designación acepta ese vocabulario, y son la mitad de las designaciones del día: `P` para volver al conjunto anterior sin rehacerlo, `Borrar`/`Añadir` para arreglar una designación mal hecha sin empezar de cero, `T` para tomarlo todo, `V` para la valla. En Valle el prompt compartido es `const SELECT_PROMPT = { message: "Designe objetos", options: [] }` (apps/web/src/lib/cad/engine/commands/modify-basics.ts:31) y `pendingSelection` (:33) acepta `CAD_ACCEPT_SELECTION | CAD_ACCEPT_ENTITY_PICK` — sin CAD_ACCEPT_KEYWORD. El grep de Previo/Último/Ventana/Captura/Cpolígono/Vpolígono sobre engine/ y selection/ sólo devuelve el `Todo` de ZOOM (view-navigation.ts:54,60), el de PURGE (groups.ts:216), el de modify-foreign.ts:131 y el `Todos` de bordes de TRIM. Duele el doble porque el motor de selección profesional está COMPLETO por debajo —ventana, cruce, polígono, valla y lazo en native-selection-index, más QSELECT y FILTER— y no hay forma de llamarlo por teclado desde dentro de un comando. Es, además, la puerta por la que entraría gratis la valla de TRIM que el auditor pide en su H-07.
- *alta* · **El punto medio mal enrutado no es sólo una ausencia: es un imán FALSO que devuelve un punto correcto con el nombre equivocado — y apagar el modo no salva** — El auditor cuenta H-01 como «MED no imanta». La otra mitad es peor y no la cuenta: con CEN encendido, el cursor se pega al PUNTO MEDIO de toda línea y de todo eje de muro y el HUD anuncia «centro» (basic-native-adapters.ts:54 y wall-entity-adapter.ts:290 → snap-scene.ts:192 → snap-engine.ts:192). Con NOD encendido, se pega al punto medio de cada tramo de polilínea y anuncia «nodo» (polyline-entity-adapter.ts:325-329 → snap-scene.ts:193-205). Su propio caso 2.3 —«enciendo sólo NOD para no coger la línea»— falla al revés de como él lo cuenta: NOD no coge el POINT y SÍ coge los medios de las polilíneas. Apagar el modo que estorba no ayuda porque el candidato está en el cubo equivocado, y el rótulo del HUD miente sobre qué se capturó. Un imán que da un punto con nombre falso es un fallo de integridad, no de comodidad, y no está declarado en ESCALERA.md.

**Rendimiento: bundle, render, 100k entidades, memoria**

- *alta* · **H-A · El artefacto del SLO mide un pipeline que el editor no ejecuta: los 25,3 s de architecture@100k no son el número del producto** — Es el hueco que el auditor tenía delante y leyó al revés. El arnés (apps/web/src/lib/cad/benchmark/browser-harness.ts:331-335) mete las 100.000 entidades —los 34.000 INSERT incluidos— en `CadRenderScene.replace()`; el editor excluye los INSERT y los sólidos sombreados (Layout3DEditor.tsx:3262-3263) y los dibuja instanciados. La propia evidencia lo declara: browser-slo-100k.json → `scope.notMeasured[0]` dice «el editor: el pipeline nuevo todavía no está enchufado a Layout3DEditor.tsx, así que esto mide los módulos, no el recorrido de interfaz». El texto está caduco en su motivo (el pipeline SÍ está enchufado) y sigue siendo CIERTO en su conclusión (esto no mide el recorrido de interfaz). Consecuencia práctica: el criterio `performance.architecture-100k` de la rúbrica —que resta un punto— y el `performance.browser-slo` —que cobra dos— se juzgan sobre un montaje que ni el camino de hoy ni el de ayer ejecutan. Nadie sabe hoy cuánto tarda el editor en materializar architecture@100k. El arreglo no es optimizar: es que el arnés monte la escena por donde pasa el usuario, o que declare la diferencia entidad por entidad.
- *alta* · **H-B · Cada edición reconstruye ENTERO el lote instanciado de los 34.000 INSERT** — Layout3DEditor.tsx:3177-3183, dentro de `syncNativeScene` y SIN ninguna guarda de `documentChanged` ni de `patch`: se quita el grupo anterior, se hace `disposeCadNativeObject` y se llama `buildCadInsertBatchObject(document, viewport)` de cero. Y `syncNativeScene` es lo que el motor de comandos invoca en cada lote aplicado (`syncScene: (document, patch) => syncNativeScene(document, patch)`, :6743). Cada reconstrucción recorre `document.entities` completo (professional-blocks.ts:537), y ADEMÁS por cada cubo hace `document.blocks.find` y `document.entities.find` (entity-three.ts:326-330): con ocho cubos sobre 100.000 entidades son ~800.000 recorridos, más la resolución de cada definición de bloque, más 34.000×6 flotantes nuevos subidos a la GPU. Todo en el hilo principal, después de mover una sola línea. Es exactamente la clase de regen que el auditor acusó al producto de hacer y que el producto no hace al ABRIR — pero sí al EDITAR. El caso que duele es el que la casa dice que le importa: «designar todo y cambiar de capa» sobre un plano ajeno con carpintería repetida.
- *alta* · **H-C · El cuerpo de render de Layout3DEditor construye dos estructuras de 100.000 entradas en CADA render de React** — Layout3DEditor.tsx:13737-13741, después del `if (!open ...) return null` y por tanto en el camino de render sin memoizar: `const nativeById = new Map(allNativeEntities.map((entity) => [entity.id, entity]))` y `const nativeEntityLabels = cadEntityLabels(allNativeEntities)`. Son dos Map de 100.000 entradas por render de un componente con 60 `useState`. Y el React Compiler —que las habría memoizado— está APAGADO por defecto: next.config.ts:102 `reactCompiler: process.env.VALLE_REACT_COMPILER === "1"`. Esto es estrictamente peor que el H10 que el auditor sí cobró (aquel al menos sólo corre cuando la selección cambia) y está a tres líneas de un `useMemo`. El gate de interacción de 320 ms no lo caza porque su documento tiene 400 líneas, no 100.000: es justo el hueco que deja el gate que sí existe.
- *alta* · **H-E · Nadie vuelve a MEDIR en CI: los gates de tiempo a 100k juzgan un fichero, no una corrida** — Es la forma correcta —y más incómoda— del H3 del auditor. `check:etapas-100k` sí corre en CI (por `npm run check:cad`, ci.yml:238), pero scripts/check-etapas-100k.mjs lee docs/cad/evidence/render-stage-architecture-100k.json y lo compara contra scripts/perf/etapas-100k-budget.json: juzga el ARTEFACTO PUBLICADO contra el techo publicado. Los dos ficheros están en el árbol y ninguno cambia cuando alguien empeora el teselado. O sea: un PR puede triplicar `tessellate` y `check:etapas-100k` seguirá verde, porque nada vuelve a medir — el trinquete sólo se activa el día que alguien decide republicar la evidencia. Lo mismo vale para cad-render-benchmark-100k.json, el único `enforcement: blocking`. El gate está bien diseñado y protege de la deshonestidad (republicar peor sin explicarlo); no protege de la regresión silenciosa, que es lo que el titular del H3 quería decir y dijo mal.

**Diseño visual: sistema, tipografía, color, densidad, movimiento**

- *bloqueante* · **A · La tinta por defecto del dibujo es BLANCA y el preset «Claro» del lienzo es casi blanco: 1,15:1 — el plano entero desaparece a un clic del conmutador de tema** — Éste es el defecto que el auditor buscaba y no encontró: no es un rótulo flotante, es el DIBUJO. La cadena está entera en el árbol. (1) La capa 0 —la capa por defecto de todo plano importado— se crea con color ACI 7: `dwg-document-bridge-layers.ts:47` → `{ id: "0", name: "0", color: aciToHex(7), … }`, y `aci-palette.ts:36` dice `7: [255, 255, 255]`. Los mismos blancos por defecto están en `audit/recover.ts:117` y `flatshot.ts:156`. (2) `layer.ts:65` resuelve PorCapa a `layer.color`. (3) El preset claro del lienzo es `0xeaf0f8` (`editor-presentation.ts:33`). Medido con el metro de la casa: **#ffffff sobre #eaf0f8 = 1,15:1**, peor que el 1,17:1 que el auditor llama bloqueante. Y no hace falta un plano importado: el color por defecto del renderizador, `CAD_RENDER_DEFAULT_COLOR = 0x60a5fa` (`render/render-style.ts:30`), da **2,22:1** sobre ese mismo lienzo — por debajo del piso de 3,0 que el propio gate de la casa exige a todo lo que comunica sin llevar letra. (4) No hay compensación: `grep -rn "luminan|invert|contrast" src/lib/cad/render/` no devuelve nada que invierta el color según el fondo, que es exactamente lo que AutoCAD hace desde siempre con el índice 7 (blanco sobre fondo oscuro, negro sobre fondo claro) y lo que hacen Rhino y Fusion. (5) Y no es un rincón: `Layout3DEditor.tsx:2058-2061` fuerza el lienzo a `light` en cuanto el usuario elige el tema claro de la aplicación, así que está a UN clic de cualquiera que prefiera trabajar en claro. El auditor pasó por `editor-presentation.ts` y por los cuatro presets, y midió el color del FONDO contra la marca; no midió el fondo contra la TINTA. Severidad: bloqueante.
- *alta* · **B · `forced-colors` y `prefers-contrast` no existen en ninguna parte del árbol** — `grep -rn "forced-colors|prefers-contrast|prefers-reduced-transparency" apps/web/src apps/web/e2e` devuelve CERO. Y no es que la casa ignore las media queries de preferencia: `prefers-reduced-motion` aparece 10 veces en globals.css y su excepción está razonada mejor que en ningún repo que haya leído (el bloque 618-632 que cancela el aplastado a 0,001 ms para no dejar el plano borrado). O sea que el criterio existe y esta rama concreta falta. Contra AutoCAD esto es una REGRESIÓN, no un empate: AutoCAD es una aplicación nativa de Windows y el Modo de alto contraste del sistema le reasigna los colores gratis, sin que Autodesk escriba una línea. Una aplicación en el navegador tiene que optar dentro, y una que no lo hace, con el 90% de su pantalla en un <canvas> que forced-colors no toca, le entrega al usuario de baja visión un marco de interfaz repintado por el sistema alrededor de un lienzo que sigue igual: lo peor de los dos mundos. El auditor auditó la accesibilidad del color (contraste) y la del anuncio (ARIA) y no auditó la de la PREFERENCIA del sistema operativo.

**Calidad del código: deuda, duplicación, tipos, pruebas débiles**

- *alta* · **La declaración de pérdidas del DXF de fondo es de sesión: al reabrir el documento vuelve a cero** — Layout3DEditor.tsx:2858 hace setDxfWarnings([]) y setDxfImportPreview(null) al cargar el documento, y :2867 reconstruye el fondo con `parseDxf(raw.data)` A SECAS — sin pasar por importDxfPrimitives, que es quien cuenta las entidades no soportadas y el recorte. Resultado: la primera sesión avisa «DXF cargado con 37 advertencia(s)», y a partir de la segunda el mismo plano, con las mismas pérdidas, se presenta con el panel DXF limpio y el contador en cero. Quien recibe el archivo de un compañero nunca ve el aviso. Un delineante de AutoCAD que usa un plano de cliente como referencia decide sobre lo que ve; si lo que ve está recortado y nada lo dice a partir del segundo día, mide sobre un plano incompleto. El manifiesto de pérdidas ya existe (buildDxfConversionLossManifest) — lo que falta es persistirlo con el DXF, no calcularlo.
- *alta* · **lib/cad/commands/lazy.ts cachea la promesa RECHAZADA: un chunk caído mata la línea de comandos para toda la sesión** — `pending ??= Promise.all([import("./parser"), import("./executor")]).then(...)` (lazy.ts:21-27) no lleva catch. Si el import dinámico falla una vez —despliegue nuevo mientras el estudio está abierto, chunk 404, red intermitente— la promesa rechazada queda cacheada y TODA llamada posterior a loadCadNlCommands devuelve el mismo rechazo. previewCommandText hace `await loadCadNlCommands()` sin try/catch y sus llamantes (Layout3DEditor.tsx:11390, :11391, :13908, :15888) no la esperan ni la capturan. Traducido: la línea de comandos y el clic derecho dejan de responder, sin mensaje, y sólo recargar la página lo arregla. Es el caso concreto que justifica el hueco 3, y es además una violación de la regla que engine/index.ts:27-32 declara para sí mismo («falla en voz alta: un descriptor cuya implementación no está cargada NO finge»). El arreglo es de dos líneas: capturar y limpiar `pending` en el rechazo.
- *alta* · **modeling3d.z-roundtrip cobra 2 puntos con evidencia sólo de specs unitarios que un spec de navegador committeado contradice** — El criterio afirma «PLINE y RECTANG dibujan en el plano del SCU inclinado» y se defiende con tres specs de Node (draw-spatial, z-frontiers, dxf-import-cota) — ningún golden, ningún command, ningún imported. Enfrente, apps/web/e2e/auditoria/tresd.spec.ts, declarado en manifiesto.json como bloquea_el_trabajo, comprueba en el navegador que sobre la fachada «no se puede dibujar un rectángulo: sólo LINE se declara espacial» y que con el ratón el trazo se aplana al suelo sin avisar. docs/parity/ESCALERA.md:172 es honesto y coloca la capacidad en peldaño 3 diciendo que falta «un golden de navegador pinchando sobre una cara», pero la rúbrica cobra los 2 puntos igual. Es la intersección exacta de los huecos 12 y 13, y es la que un comprador notaría el primer día que intente levantar un alzado: el motor sabe hacerlo si le tecleas coordenadas, el estudio no lo deja hacer con el ratón, y la puntuación no distingue las dos cosas.


**49 de los 106 son de severidad alta o bloqueante.**