# Revisión de Claude sobre tus commits de la noche (15-sep, 01:30)

Siete revisores leyeron tus commits en `445352d8` y un verificador escéptico por tarea intentó refutar cada hallazgo. **Sólo aparecen aquí los que sobrevivieron a la verificación.**

Cómo trabajarlos:
- Van antes que cualquier tarea nueva, primero los de severidad alta.
- Un commit por hallazgo con el prefijo `Revisión:` y el id del hallazgo en el mensaje.
- Cada arreglo lleva un spec que habría fallado con el defecto: que ejecute el comando o la función real, no una copia hecha a mano.
- Anota en la bitácora cada hallazgo cerrado, con su id.
- Si un hallazgo te parece falso después de leer el código, escribe la razón en `docs/execution/PREGUNTAS.md` y sigue con el siguiente.

## T1 · 3DMOVE y 3DROTATE (colocación afín 3×4) — commits 2f222b8f y 0e684821, revisados en 445352d8

**Resumen del revisor:** No está listo para main. Lo que funciona: 3DMOVE y 3DROTATE están bien registrados (command-manifest pasa a 296 comandos, all-commands a 110 módulos, lazy-commands tiene ambos módulos y los alias 3M/3R no chocan con nada). Además, la rama 3D de placeBody arma la matriz 3×3 con a/b/c/d y calcula bien el determinante, la rotación de Rodrigues es correcta y compose3dPlacements multiplica en el orden correcto (overlay × base). Las colocaciones guardadas antes (sólo 2D) siguen yendo por el camino de siempre, y la ida y vuelta del documento conserva los campos nuevos, porque parseCadDocument no reconstruye `placement`. Lo que falla es el modelo de traslación. La rama 3D ignora e/f/dz y ninguno de los dos comandos los incorpora, así que cualquier sólido que ya se había colocado con MOVE, con el grip o con el panel de propiedades salta de sitio con 3DMOVE o 3DROTATE. Y al revés: el adaptador 2D que ya existía (MOVE/ROTATE/SCALE/MIRROR/COPY y los grips de esquina) reescribe la colocación sólo con a-f y dz, y borra el giro 3D de lo que se guarda. Otros fallos: el eje por dos puntos ignora dónde está el eje; una selección con algo que no sea sólido aborta el lote con un mensaje que nombra MIRROR3D y 3DALIGN, que no existen; el servidor valida sólo el menor 2D. Las 20 aserciones existen y el runner las recoge (glob src/**/*.spec.ts), pero ninguna ejecuta los comandos, ni rotationMatrix3x4, ni compose3dPlacements, ni tz. El caso «3DROTATE» pasaría igual con la rotación rota.

**Afirmaciones de tus commits que no se cumplen:**

- 3DMOVE tecleable con punto base y destino en 3D
  - Evidencia: El comando existe y está registrado (command-manifest.ts, all-commands.ts m101, lazy-commands.ts; alias 3M), pero transform-3d.ts:95-103 escribe tx=(tx??0)+dx dejando e/f/dz fuera, y placeBody los ignora en la rama 3D. Un sólido con e=5000 (movido antes con MOVE) vuelve a X=0 al subirlo 100 en Z.
- 3DROTATE con eje por dos puntos o X/Y/Z y fórmula de Rodrigues
  - Evidencia: La matriz de Rodrigues (transform-3d-rotate.ts:112-151) es correcta y los ejes X/Y/Z funcionan. Pero con eje por dos puntos el giro se hace alrededor de una recta que pasa por el punto base, no por p1-p2 (línea 216), y la composición ignora e/f/dz (líneas 179 y 189-191), así que un sólido colocado antes con MOVE sale en otro sitio.
- Composición de colocaciones (compose3dPlacements) en el orden correcto
  - Evidencia: El producto overlay×base de la parte 3×3 y de tx/ty/tz es correcto (transform-3d-rotate.ts:180-191), pero copia e/f/dz sin rotarlos (línea 179). Y la composición 2D que ya existía (solid3d-adapter.ts:307-317) borra m02…tz, así que componer 3D y 2D en cualquier orden pierde datos.
- El spec final tiene 20 aserciones, incluido «giro Z 90° por centro» para 3DROTATE
  - Evidencia: Las 20 aserciones existen (se añaden 4 en transform-3d.spec.ts:237-242), pero el caso 6 no llama a 3DROTATE ni a rotationMatrix3x4: construye la matriz a mano y gira alrededor del propio centroide, que no se mueve ni con rotación ni sin ella. El caso 1, «traslación Z», usa dz (el camino antiguo), no tz ni 3DMOVE.

### t1-transformaciones-3d-1 · [alta] 3DMOVE y 3DROTATE descartan e/f/dz: el sólido salta de sitio

- **Fichero:** `apps/web/src/lib/cad/solid3d-build.ts:597`
- **Problema:** En cuanto hay un campo 3D, placeBody usa como traslación sólo tx/ty/tz e ignora e, f y dz. 3DMOVE (transform-3d.ts:95-103) suma el desplazamiento a tx/ty/tz partiendo de 0 y deja e/f/dz como estaban, y compose3dPlacements (transform-3d-rotate.ts:179, 189-191) compone sólo tx/ty/tz y copia e/f/dz sin rotarlos. Cualquier sólido colocado antes con MOVE, con el grip base o con el panel (que escriben e/f/dz) pierde esa traslación. Además, un 3DROTATE sobre Z con base en el origen deja has3D falso, y la rama 2D aplica el giro a a-d pero no a e/f, así que el sólido gira alrededor de su propia esquina y no del punto base.
- **Evidencia:** Caja 1000×600×500. MOVE 2D (5000,0) → placement {a:1,b:0,c:0,d:1,e:5000,f:0,dz:0} (solid3d-adapter.ts:317). 3DMOVE de (0,0,0) a (0,0,100) → transform3d {e:5000, tx:0, ty:0, tz:100}. has3D es verdadero por tz, y la línea 597 calcula x' = a·x + c·y + m02·z + tx, sin e: la caja vuelve a X 0..1000 en vez de 5000..6000. Con dz=300 puesto desde el panel, 3DMOVE también pierde esos 300 en Z. El comentario de cad-entities-v5.ts:310-315 («gana sobre e») describe algo que ningún comando cumple.
- **Verificación independiente:** Lo confirmo en el código. En solid3d-build.ts:575-578, has3D se activa con cualquier tx/ty/tz o m* distinto de su valor neutro. Entonces las líneas 595-599 calculan x' = a·x + c·y + m02·z + tx, sin m.e, m.f ni m.dz. transform-3d.ts:95-103 copia e/f/dz tal cual y suma dx/dy/dz sólo a tx/ty/tz, que parten de 0. El ejecutor (entity-commands.ts:509) mezcla {...existing, ...transform3d}. Resultado: un sólido con e=5000 (MOVE 2D, adaptador :317) o con dz del panel pierde esa traslación tras 3DMOVE. En compose3dPlacements (transform-3d-rotate.ts:179), e/f/dz se copian sin rotar y las líneas 189-191 componen sólo tx/ty/tz. Con EjeZ y base en el origen, rotationMatrix3x4 da m02=m12=m20=m21=0, tx=ty=tz=0 y m22=cos+(1−cos), que redondea a 1. has3D queda falso y la rama 2D (solid3d-build.ts:622-626) calcula (−y+5000, x) cuando lo correcto es R·(p+e) = (−y, x+5000). El comentario de cad-entities-v5.ts:310-315 («gana sobre e») describe precisamente la pérdida, y :275 nombra m10/m11, campos que no existen en la interfaz.
- **Arreglo:** Define una única traslación efectiva (e+tx, f+ty, dz+tz). En placeBody (solid3d-build.ts:597-599) suma m.e, m.f y m.dz a tx, ty y tz. En compose3dPlacements toma como traslación del base (e+tx, f+ty, dz+tz), aplícale la rotación y devuelve el resultado en tx/ty/tz con e=f=dz=0. Usa siempre la rama 3D cuando la rotación no sea la identidad, para que la rama 2D nunca deje e/f sin girar. En move3dCommands suma dx/dy/dz sobre esa misma traslación efectiva. Corrige los comentarios de cad-entities-v5.ts:275 (quitar m10/m11) y 310-315. Añade al spec dos casos que ejecuten los descriptores reales con begin/step y context.entity: (a) sólido con e=5000 y dz=300; 3DMOVE de (0,0,0) a (0,0,100) debe dejar el centroide en X=5500 y Z=300+250+100; (b) caja w×d con e=5000; 3DROTATE EjeZ 90° con base (0,0,0) debe dejar el centroide XY en (-d/2, 5000+w/2).

### t1-transformaciones-3d-2 · [alta] MOVE/ROTATE/SCALE/MIRROR/COPY 2D y los grips borran el giro 3D guardado

- **Fichero:** `apps/web/src/lib/cad/solid3d-adapter.ts:317`
- **Problema:** El adaptador del sólido compone la transformada 2D sólo con a-f y reescribe la colocación como {...composed, dz}, así que desaparecen m02, m12, m20, m21, m22, tx, ty y tz. Pasa en MOVE, ROTATE, SCALE, MIRROR, en los grips de esquina (que llaman a transform) y en la rama 'copy' de executeCadEntityCommandBatch (entity-commands.ts, que llama a adapter.commands.transform con traslación 0). Por otro lado, el grip base (línea 198), el panel de propiedades (líneas 283-288) y solidBasePoint (164-167) leen y escriben e/f/dz, que la rama 3D ignora: editar no tiene efecto y el punto base se dibuja donde no está el sólido.
- **Evidencia:** 3DROTATE EjeX 90° sobre una caja → placement con m12≈-1, m21≈1, m22≈0 y tx/ty/tz del centro. Después, MOVE (100,0) → withPlacement(entity, {a,b,c,d,e,f, dz}): se pierden m12/m21/m22/tx/ty/tz, la caja vuelve a estar de pie en su posición original y así se guarda en el documento. Con COPY la copia sale sin girar. Con el grip base sobre un sólido que ya tiene tx≠0, e/f cambian pero placeBody no los usa: el sólido no se mueve.
- **Verificación independiente:** Lo confirmo. solid3d-adapter.ts:307-317 compone cadAffineCompose sólo con a-f y guarda withPlacement(entity, {...composed, dz}). Como withPlacement (:169-170) sustituye la colocación entera, se pierden m02, m12, m20, m21, m22, tx, ty y tz. MOVE/ROTATE/SCALE/MIRROR llegan por command.type==='transform' → adapter.commands.transform (entity-commands.ts:515-516). La rama 'copy' (entity-commands.ts:418-420) llama al mismo transform con traslación 0, así que la copia sale sin giro 3D. Los grips de esquina también pasan por transform (:213). El grip base (:198) y properties.write (:283-288) escriben e/f/dz conservando tx/ty/tz, pero la rama 3D de placeBody no usa e/f/dz y el sólido no se mueve. solidBasePoint (:164-167) y properties.read (:245-247) muestran e/f/dz, que no es la posición real.
- **Arreglo:** En solid3d-adapter.ts:307-317 compón la transformada 2D (parte lineal L, traslación t) con la afín 3×4 completa: [a c; b d] ← L·[a c; b d]; (m02, m12) ← L·(m02, m12); traslación XY efectiva ← L·(e+tx, f+ty) + t, guardada con el mismo criterio que el hallazgo 1; conserva m20, m21, m22, tz y dz. Haz que solidBasePoint, el grip 'base' y properties.read/write usen la traslación efectiva (e+tx, f+ty, dz+tz): al escribir, ajusta e/f/dz de modo que la suma sea el valor pedido. Añade un spec: 3DROTATE EjeX 90°, luego MOVE (100,0) y luego COPY; los dos sólidos deben conservar m12/m21 y tener el centroide girado y desplazado 100 en X.

### t1-transformaciones-3d-3 · [media] Los specs de 3D no ejecutan 3DMOVE, 3DROTATE, Rodrigues ni la composición

- **Fichero:** `apps/web/src/lib/cad/transform-3d.spec.ts:227`
- **Problema:** Ninguno de los 20 checks importa los comandos, rotationMatrix3x4 ni compose3dPlacements (no se exportan). El caso 6, «3DROTATE Z 90°», construye la matriz a mano y gira alrededor del propio centroide (100,100,100): el centroide queda igual con la rotación bien o mal hecha, así que sólo comprueba el volumen. El caso 1, «traslación Z», usa dz (el camino antiguo), no tz ni 3DMOVE. Con estos specs en verde, los hallazgos 1 y 2 pasan desapercibidos.
- **Evidencia:** transform-3d.spec.ts:227-234 fija a=0,b=1,c=-1,d=0 y tx=200 a mano; las líneas 240-242 esperan el centroide en (100,100,100), lo mismo que daría la identidad. Las líneas 74-77 usan {dz:500} sin ningún campo 3D, así que has3D es falso. Los imports (líneas 20-29) sólo traen solid3d-build y cad-document.
- **Verificación independiente:** Lo confirmo. Los imports de transform-3d.spec.ts:20-29 sólo traen spec-support, un tipo, solid3d-build y cad-document. rotationMatrix3x4 y compose3dPlacements no se exportan: son funciones sin export en transform-3d-rotate.ts:112 y :158. Busqué 3DMOVE/3DROTATE/CAD_3D*_COMMANDS en todos los *.spec.ts de 445352d8 y sólo aparecen los títulos del caso 6. Ese caso (:226-242) arma la matriz a mano y gira alrededor de (100,100,100), el propio centroide, así que espera (100,100,100), lo mismo que daría la identidad. El caso 1 (:74-77) usa sólo dz, con has3D falso, y no prueba tz ni 3DMOVE. Los hallazgos 1 y 2 pasan con este spec en verde.
- **Arreglo:** Exporta rotationMatrix3x4 y compose3dPlacements, o prueba los descriptores con begin/step y un context con entity(). Reescribe el caso 6 para girar la caja 0..200 alrededor de un punto que no sea su centro (base (0,0,0), EjeZ 90°) y espera el centroide en (-100,100,100). Añade la composición de dos giros (EjeX 90° y luego EjeZ 90°) comparada con la matriz esperada, para fijar el orden de composición. Añade un caso de 3DMOVE que pase por el comando y verifique tz y el centroide sobre un sólido con colocación previa. Cambia el caso 1 para que use tz o renómbralo a «dz legado».

### t1-transformaciones-3d-4 · [media] Con eje por dos puntos, 3DROTATE gira alrededor del punto base y no del eje

- **Fichero:** `apps/web/src/lib/cad/engine/commands/transform-3d-rotate.ts:216`
- **Problema:** Cuando el eje se designa con dos puntos, rotationMatrix3x4 recibe como centro state.base y sólo toma de p1-p2 la dirección: el giro es alrededor de una paralela que pasa por el punto base, no de la recta que el usuario designó («Precise el primer/segundo punto del eje de giro»). Además, si el ángulo se designa con un punto (líneas 284 y 306), se mide con atan2 en el plano XY desde la base, aunque el eje sea X, Y o una recta cualquiera, así que el ángulo no corresponde al plano de giro.
- **Evidencia:** Caja 0..1000 × 0..600 × 0..500 con base en (0,0,0) y eje designado de (1000,0,0) a (1000,600,0), su arista derecha, con 90°. Se gira alrededor de la recta x=0, z=0 y la caja acaba bajo el plano XY junto al origen, en vez de abatirse sobre la arista x=1000. Con EjeX, pinchar el punto base+(0,10) da 90° sin relación con el plano YZ en que se gira.
- **Verificación independiente:** Lo confirmo, con un matiz. transform-3d-rotate.ts:211-216 toma de p1→p2 sólo la dirección y llama a rotationMatrix3x4(base, axis, …). El giro es alrededor de la paralela que pasa por el punto base, aunque los prompts (:67 y :81) piden «primer/segundo punto del eje de giro». La cabecera (:8) dice «El punto base es el centro de giro», así que se hizo a propósito, pero no concuerda con pedir dos puntos del eje: p1 y p2 no quedan sobre el eje en general. La parte del ángulo por punto es un error sin matices: :284 y :306 usan atan2 en XY desde la base sea cual sea el eje (X, Y o una recta cualquiera), y ese ángulo no se mide en el plano de giro.
- **Arreglo:** En rotate3dCommands, cuando no hay worldAxis, llama a rotationMatrix3x4(state.axisPoint1, p2 - p1, rad); el punto base sólo sirve de centro con los ejes X/Y/Z. Para el ángulo por punto, proyecta el vector base→punto sobre el plano perpendicular al eje y mide el ángulo ahí contra una referencia fija de ese plano; si no, con ejes distintos de Z acepta sólo CAD_ACCEPT_ANGLE. Añade el caso de la arista al spec.

### t1-transformaciones-3d-5 · [media] Una selección con algo que no es sólido aborta 3DMOVE/3DROTATE y nombra comandos inexistentes

- **Fichero:** `apps/web/src/lib/cad/entity-commands.ts:505`
- **Problema:** move3dCommands (transform-3d.ts:84) y rotate3dCommands (transform-3d-rotate.ts:219) emiten transform3d para todos los ids seleccionados sin filtrar por tipo, y el ejecutor lanza un error si la entidad no es un solid3d. Como el lote es transaccional, basta una línea en la ventana de selección para que tampoco se muevan los sólidos. El editor muestra el mensaje en un toast (Layout3DEditor.tsx, catch con toast.error) y ese mensaje anuncia 3DALIGN y MIRROR3D, que no existen en command-manifest.
- **Evidencia:** Ventana que captura un SOLID3D y una LINE → 3DMOVE → el lote lanza «3DMOVE/3DROTATE/3DALIGN/MIRROR3D sólo aplica a SOLID3D.» y no se aplica nada. En 445352d8, git grep de MIRROR3D/3DALIGN sólo encuentra ese mensaje y un comentario del spec.
- **Verificación independiente:** Lo confirmo. move3dCommands (transform-3d.ts:84-105) y rotate3dCommands (transform-3d-rotate.ts:219-232) emiten transform3d para todos los ids de state.selection sin filtrar por tipo. El ejecutor lanza un error si source.type !== 'solid3d' (entity-commands.ts:504-505). El editor llama a executeCadEntityCommand/executeCadEntityCommandBatch sobre el checkpoint dentro de un try (Layout3DEditor.tsx:4673-4703). En el catch (:4735-4741) muestra toast.error(cause.message) y no hace commit: no se aplica nada del lote. El mensaje nombra 3DALIGN y MIRROR3D. En 445352d8, esos nombres sólo aparecen en ese error y en un comentario del spec (:118). command-manifest sólo registra 3DMOVE y 3DROTATE (:315-316). selectedSolids existe y ya filtra (solids-support.ts:46-53).
- **Arreglo:** En move3dCommands y rotate3dCommands filtra con selectedSolids(context, state.selection) (solids-support.ts:46). Si no queda ningún sólido, devuelve result {kind:"message", text:"3DMOVE sólo transforma sólidos 3D."} (o el equivalente de 3DROTATE). Cambia el texto de entity-commands.ts:505 a «La transformación 3D sólo aplica a SOLID3D.» sin nombrar 3DALIGN ni MIRROR3D.

### t1-transformaciones-3d-6 · [media] El API valida la colocación sólo en 2D y no valida los campos 3D

- **Fichero:** `apps/api/src/modules/cad-documents/cad-solid-invariants.ts:433`
- **Problema:** assertPlacement exige que a·d−b·c sea distinto de 0 y no mira m02, m12, m20, m21, m22, tx, ty ni tz. Rechaza al guardar giros 3D válidos cuyo menor 2D sea exactamente 0 y acepta colocaciones 3D singulares o con valores no numéricos, que después hacen fallar placeBody en el cliente.
- **Evidencia:** La colocación del propio spec (transform-3d.spec.ts, casos 2 y 5: a=1,b=0,c=0,d=0,m12=-1,m21=1,m22=0; det3=1) da determinante 2D 0 y el servidor la rechaza con «colocación singular». En cambio {a:1,b:0,c:0,d:1,e:0,f:0,m22:0} tiene det3=0, el API la acepta y placeBody lanza «La colocación 3D de un SOLID3D es singular»; {…, tx:"abc"} también pasa.
- **Verificación independiente:** Lo confirmo, con un matiz. assertPlacement (cad-solid-invariants.ts:420-443) valida sólo a-f y dz y el determinante 2D a·d−b·c. En apps/api/src no hay ninguna referencia a m02/m12/m20/m21/m22/tx/ty/tz, así que valores no numéricos o una 3×3 singular (p. ej. m22:0 con a=d=1) pasan. Después placeBody lanza su error (solid3d-build.ts:586-593). El matiz está en el rechazo de giros válidos: un 3DROTATE hecho con Math.cos deja d≈6e-17, no 0 exacto, y pasa la comprobación. Sí se rechazan colocaciones con ceros exactos, como las del propio spec (casos 2 y 5, d=0 con m12=-1 y m21=1) o documentos tecleados o importados. La validación del API no está al día con la afín 3×4.
- **Arreglo:** En assertPlacement exige que m02, m12, m20, m21, m22, tx, ty y tz sean finitos cuando estén presentes. Si hay alguno de m02/m12/m20/m21/m22, calcula el determinante 3×3 con la misma fórmula que solid3d-build.ts:586-589 y rechaza sólo si vale 0; si no hay ninguno, deja la comprobación 2D actual. Añade al spec de invariantes del API la colocación del giro X 90° como aceptada y {a:1,d:1,m22:0} como rechazada.

### t1-transformaciones-3d-7 · [baja] Los avisos de eje degenerado y selección vacía nunca se muestran

- **Fichero:** `apps/web/src/lib/cad/engine/commands/transform-3d-rotate.ts:300`
- **Problema:** done() devuelve {kind:"none"} si no hay comandos y descarta la etiqueta. Por eso «Los dos puntos del eje son el mismo: no definen un eje de giro.» (línea 300) y «3DROTATE: necesita al menos un objeto designado.» (línea 258) no llegan al usuario: el comando termina sin decir nada. Lo mismo pasa con el aviso de selección vacía en transform-3d.ts.
- **Evidencia:** En done() (transform-3d-rotate.ts:91-104) el resultado es commands.length > 0 ? {kind:"document", commands, label} : {kind:"none"}. command-types.ts:560 tiene la variante {kind:"message"; text}, que solids-support.ts:186 sí usa.
- **Verificación independiente:** Lo confirmo. done() (transform-3d-rotate.ts:91-104 y transform-3d.ts:57-70) devuelve {kind:'none'} cuando no hay comandos, y la etiqueta se pierde porque la variante 'none' no lleva texto (command-types.ts:582). Por eso «3DROTATE: necesita al menos un objeto designado.» (:258), «Los dos puntos del eje son el mismo…» (:300) y «3DMOVE: necesita al menos un objeto designado.» (transform-3d.ts:132) nunca llegan al usuario. El motor sí publica {kind:'message'} (command-engine.ts:256), y solidMessage (solids-support.ts:186-188) ya lo usa.
- **Arreglo:** Añade en ambos ficheros una función message(text) que devuelva result {kind:"message", text}, igual que solidMessage, y úsala en transform-3d-rotate.ts:258 y :300 y en el aviso de selección vacía de transform-3d.ts. Deja done() sólo para el resultado del documento o para cancelar.

### t1-transformaciones-3d-8 · [baja] Rodrigues, valores por defecto y flujo duplicados

- **Fichero:** `apps/web/src/lib/cad/engine/commands/transform-3d-rotate.ts:112`
- **Problema:** rotationMatrix3x4 vuelve a implementar la rotación de Rodrigues, que en 0a5f5600 ya existía y tenía spec. El bloque de valores por defecto de la colocación está copiado en los dos comandos y repite resolveSolidPlacement. step/done/selección están duplicados entre transform-3d.ts y transform-3d-rotate.ts, y 3DMOVE compone la traslación por su cuenta en vez de usar compose3dPlacements.
- **Evidencia:** En 0a5f5600 ya estaban v3RotateAroundAxis en apps/web/src/lib/brep/vec3.ts:168 (con casos de Rodrigues en vec3.spec.ts:83-108) y rotateAround en lib/cad/view/view-3d.ts:100. transform-3d.ts:95-103 y transform-3d-rotate.ts:222-229 son el mismo bloque de «?? 1 / ?? 0», que ya hace resolveSolidPlacement (solid3d-build.ts:113).
- **Verificación independiente:** Lo confirmo. Es duplicación, no un fallo funcional. En 0a5f5600 ya estaban v3RotateAroundAxis (brep/vec3.ts:167, «Rotación de Rodrigues…», con casos en vec3.spec.ts:83-108) y la rotación de view-3d.ts:99. rotationMatrix3x4 (transform-3d-rotate.ts:112-151) la vuelve a escribir. El bloque de «?? 1 / ?? 0» se repite en transform-3d.ts:95-103 y transform-3d-rotate.ts:222-229, y hace lo mismo que resolveSolidPlacement, exportada en solid3d-build.ts:~113-133. step/done/selección están casi copiados entre los dos ficheros, y 3DMOVE suma la traslación por su cuenta en vez de usar compose3dPlacements.
- **Arreglo:** Mueve compose3dPlacements y un constructor de matriz de rotación a solid3d-build.ts, junto a resolveSolidPlacement. Construye las columnas de la rotación con v3RotateAroundAxis aplicado a (1,0,0), (0,1,0) y (0,0,1). Sustituye los bloques de valores por defecto por resolveSolidPlacement(entity.placement). Haz que 3DMOVE componga una traslación pura con ese mismo helper.

## T2 · SLICE y SECTION con planos XY/YZ/ZX (c43f0871, revisado en 445352d8)

**Resumen del revisor:** coordinatePlane() está bien escrita: normales +Z, +X y +Y, origen a la cota y cota en unidades de dibujo. Las palabras clave XY/YZ/ZX sí se ofrecen en el prompt del primer punto. Pero el flujo del comando está roto: al fijar la cota, planeStep no se salta la rama `!state.second` y pide «Precise el segundo punto del plano de corte» con una preview `[null, null]`. CadEnginePreview.draw lee `from.x` de esos nulos y lanza TypeError, tanto al teclear la cota como en cada movimiento del puntero (refreshPreview). En SLICE nunca aparece «¿Qué lado se conserva?»: Intro corta sin preguntar y conserva el lado positivo, y Derecha o Ambos sólo se alcanzan tras dos clics inútiles. Hay más problemas: 1) Un clic, una coordenada o Intro en el prompt de la cota dan cota 0 en silencio. 2) En YZ, «Izquierda» conserva la parte derecha en planta. 3) SLICE escribe el plano en coordenadas del mundo, pero el nodo slice se evalúa antes de placement; con un sólido subido por 3DMOVE (tz) o con dz, SLICE XY corta a otra altura que SECTION XY. 4) El spec tiene sus 6 aserciones y el runner lo recoge, pero no llama ni a coordinatePlane ni a los comandos: escribe los planos a mano sobre el nodo slice, que ya existía. Pasaría en verde con el flujo roto tal como está. No bloquea por esquema ni por reglas de UI: el esquema no cambia, no hay hex ni data-testid tocados, y «AutoCAD» sólo aparece en un comentario de código. No debe llegar a main así.

**Afirmaciones de tus commits que no se cumplen:**

- SLICE y SECTION funcionan de extremo a extremo con planos XY/YZ/ZX (elegir plano → cota → lado o región)
  - Evidencia: Tras la cota, planeStep cae en `if (!state.second)` (301-307): pide el segundo punto con preview [state.first!, state.first!], que vale [null, null]. CadEnginePreview.draw (engine-preview.ts:101) hace `from.x` sobre null y lanza. El prompt de lado de SLICE (308-311) sólo se alcanza con dos clics que no se usan para nada.
- El spec valida el corte por XY/YZ/ZX a la mitad de una caja con volumen y centroide correctos, y verifica coordinatePlane
  - Evidencia: La aritmética es correcta (por ejemplo, centroide z = (200+400)/2 = 300), pero los planos están escritos a mano como literales (spec líneas 54, 80 y 106) sobre el nodo slice, que ya existía. coordinatePlane no se exporta ni se importa, y ni sliceCommand ni sectionCommand se ejecutan. La cabecera (línea 4) dice «Verifica que la función `coordinatePlane` produce los planos correctos», y eso es falso. No hay caso de SECTION.

### t2-slice-section-1 · [alta] Tras la cota, el comando pide un «segundo punto» con preview nula y revienta el render

- **Fichero:** `apps/web/src/lib/cad/engine/commands/solids-modify.ts:301`
- **Problema:** planeStep no contempla el modo coordenado después de la cota. Con planeMode y planeElevation fijados, pero first y second a null, la rama `if (!state.second)` devuelve «Precise el segundo punto del plano de corte» con preview [{points:[state.first!, state.first!]}], es decir [null, null]. El prompt de lado de SLICE («¿Qué lado se conserva?») y el de SECTION («Pulse Intro para crear la región de sección») nunca se muestran tras la cota. Además, CadEnginePreview.draw lanza TypeError.
- **Evidencia:** planeStep, líneas 289-307: la condición de la línea 295 es `!state.first && !state.planeMode`, pero la 301 es sólo `!state.second`. La preview de la línea 306 lleva `state.first!`, que vale null. El motor emite esa preview (command-engine.ts:146), el host la aplica (command-engine-host.ts: case "preview" → bridge.preview → Layout3DEditor drawPreview → enginePreview.draw) y engine-preview.ts:101 ejecuta `(from.x - width / 2)` con from = null. Pasa también al mover el ratón en el prompt de la cota: pointer-router.ts:306 llama refreshPreview, que invoca step con {kind:"text"}; el bloque de cota (líneas 344-348) lo toma como cota 0 y devuelve el paso con la preview nula. En SLICE, Intro en ese prompt llega al step (accept() despacha enter sin mirar accepts), se salta la guarda de la línea 359 porque hay planeMode y corta con KEEP_LEFT sin preguntar. Derecha y Ambos sólo se alcanzan tras dos clics distintos que luego se ignoran (líneas 350-352).
- **Verificación independiente:** Confirmado en 445352d8 (el fichero no cambió después de c43f0871). Si se fijan planeMode y planeElevation con first y second a null, planeStep (solids-modify.ts:287-313) no entra en la línea 289 (la cota ya existe) ni en la 295, porque `!state.first && !state.planeMode` da false, y cae en la 301 (`if (!state.second)`). Esa rama devuelve «Precise el segundo punto del plano de corte» con accepts CAD_ACCEPT_POINT y preview `[{ points: [state.first!, state.first!] }]` (línea 306), que en ejecución es [null, null]. El motor emite esa preview sin filtrarla (command-engine.ts:146) y el host la aplica sin try/catch: dispatch en command-engine-host.ts:680, applyEffect case "preview" en la 773, bridge.preview → drawPreview → CadEnginePreview.draw (Layout3DEditor.tsx:4794 y 6562; studio-engine-bridges.ts:134). En draw, un path con 2 puntos pasa la guarda de longitud y engine-preview.ts:101 lee `from.x` con from = null, lo que da TypeError. También salta al mover el ratón en el prompt de la cota: pointer-router.ts:306 llama a refreshPreview, que pasa {kind:"text",value:""} (command-engine-host.ts:472); las líneas 344-347 lo toman como cota 0 y devuelven ese mismo paso con la preview nula. Tras la cota, el prompt no tiene opciones ni CAD_ACCEPT_KEYWORD, así que teclear «Derecha» termina en «Entrada no válida» (input-pipeline.ts:176 y 283). En cambio, Intro pasa directo al step (accept() despacha enter en command-engine-host.ts:394 y el motor no mira accepts, command-engine.ts:460); la guarda de la línea 359 no se aplica porque hay planeMode, la 363 acepta el enter y la 365 corta con KEEP_LEFT. Derecha o Ambos sólo se alcanzan tras dos clics (líneas 351 y 355), cuyos puntos se ignoran en la 368-370. El matiz «revienta el render» es impreciso: lo que lanza es el manejador de entrada o de movimiento, no React. El defecto es real.
- **Arreglo:** En planeStep, cambia la condición de la línea 301 a `if (!state.planeMode && !state.second)` para que, con planeMode y planeElevation fijados, se pase directo al prompt final (líneas 308-311). Quita las aserciones no nulas `state.first!` de la preview y deja la rama del segundo punto sólo para el modo de dos puntos. En step de SLICE (líneas 350-358) y de SECTION (436-444), ignora las entradas `point` cuando state.planeMode no es null. Añade al spec un caso que ejecute sliceCommand.begin/step con keyword XY, distance 200 y keyword Derecha, y compruebe que el paso tras la cota tiene message «¿Qué lado se conserva?», que preview es undefined y que el resultado es un documento.

### t2-slice-section-2 · [media] Un clic, una coordenada o Intro en el prompt de la cota fijan la cota 0 en silencio

- **Fichero:** `apps/web/src/lib/cad/engine/commands/solids-modify.ts:345`
- **Problema:** El prompt de la cota declara CAD_ACCEPT_POINT, pero cualquier entrada que no sea `distance` (un punto designado, una coordenada tecleada «3000,500», Intro, o el text vacío de refreshPreview) se convierte en cota 0 sin avisar. Para YZ y ZX, designar un punto en planta es la forma natural de dar la cota (su x o su y), y el plano acaba en x=0 o y=0. La guarda `if (elevation === null)` es código muerto: la expresión nunca da null.
- **Evidencia:** Línea 345 (SLICE) y 431 (SECTION): `const elevation = input.kind === "distance" ? input.value : 0;`. Las líneas 346 y 432 comprueban `elevation === null`, que es imposible. El prompt de las líneas 289-294 anuncia `accepts: CAD_ACCEPT_DISTANCE | CAD_ACCEPT_POINT`.
- **Verificación independiente:** Confirmado. solids-modify.ts:345 (SLICE) y 431 (SECTION): `const elevation = input.kind === "distance" ? input.value : 0;`. Cualquier entrada que no sea distance y llegue ahí fija la cota 0: un clic (pickPoint → point), Intro (accept → enter), el text vacío de refreshPreview o una keyword. El prompt declara CAD_ACCEPT_DISTANCE | CAD_ACCEPT_POINT (línea 293), y en input-pipeline.ts un número suelto se lee como distance (223-224), pero «3000,500» no se puede leer como longitud y se resuelve como point tecleado (247-263), que también acaba en cota 0 sin aviso. Las guardas `if (elevation === null)` de las líneas 346 y 432 no pueden cumplirse nunca, porque la expresión siempre es un number: son código muerto. Que un punto deba dar su x o su y según el plano es una decisión de diseño, pero la cota 0 silenciosa es un defecto verificable.
- **Arreglo:** En el bloque de cota de SLICE (344-348) y de SECTION (430-434), acepta sólo `distance`, que fija la cota, y `point`, que fija la cota como point.x para YZ, point.y para ZX y, para XY, (point as {z?:number}).z ?? 0 o se rechaza con mensaje. Con enter, text o cualquier otra entrada, devuelve planeStep(state, ...) sin cambiar el estado. Borra la guarda muerta `elevation === null`. Añade specs: point {x:150,y:0} con YZ da un nodo con plane.origin.x === 150, y enter en el prompt de la cota no avanza.

### t2-slice-section-3 · [media] SLICE en planos coordenados corta a la altura equivocada si el sólido tiene placement (tz o dz)

- **Fichero:** `apps/web/src/lib/cad/engine/commands/solids-modify.ts:368`
- **Problema:** SLICE escribe en el nodo slice el plano en coordenadas del MUNDO, pero el árbol se evalúa en coordenadas LOCALES y la colocación se aplica al final. En un sólido subido con 3DMOVE (tz) o con dz, `SLICE XY 3500` corta a 3500 + tz: puede no atravesar el sólido o partirlo por otro sitio. SECTION usa el cuerpo ya colocado, así que `SECTION XY 3500` sobre el mismo sólido corta a la cota correcta y los dos comandos no coinciden. Con verticalPlane la Z nunca influía (normal con z=0); el modo XY es el primero que depende de tz y dz.
- **Evidencia:** Línea 369: `coordinatePlane(state.planeMode, state.planeElevation ?? 0)` se escribe tal cual en `nodes.push({ id: rootId, op: "slice", operand: source.root, plane, keep })`, pocas líneas más abajo. solid3d-build.ts:334: `return placeBody(evaluate(entity.root), entity.placement);`, es decir, el slice se evalúa antes de colocar. transform-3d.ts:103 escribe `tz: (current.tz ?? 0) + dz` en placement. SECTION usa selectedFlattenableBodies → solid3dBody(entity), que ya aplica placeBody (solids-support.ts:87).
- **Verificación independiente:** Confirmado. SLICE escribe el plano de coordinatePlane tal cual en el nodo (solids-modify.ts:368-369 y 381) y copia la colocación a cada mitad (línea 384). evaluateSolidTree evalúa el árbol en local y coloca al final: `return placeBody(evaluate(entity.root), entity.placement);` (solid3d-build.ts:334); sliceBody corta en ese espacio local (420-421). placeBody suma tz en la rama 3D (línea 599) y dz en la 2D (línea 631). 3DMOVE compone `tz: (current.tz ?? 0) + dz` (transform-3d.ts:103), y entity-commands.ts:509 lo fusiona en placement. Con tz = 1000, `SLICE XY 1200` corta en z local 1200, que en el mundo es 2200. SECTION, en cambio, usa selectedFlattenableBodies → solid3dBody (solids-support.ts:87) → entryFor → evaluateSolidTree (solid3d-build.ts:688), que es el cuerpo ya colocado, así que corta a la cota mundial correcta y los dos comandos no coinciden. Matiz: verticalPlane ya ignoraba tx/ty/e/f antes de T2 (c43f0871^:solids-modify.ts:329), así que la discrepancia en planta es anterior; lo nuevo de T2 es la dependencia de tz/dz en XY, y la de tx/ty en YZ y ZX.
- **Arreglo:** Antes de escribir el nodo slice, lleva el plano a coordenadas locales del sólido con la inversa de resolveSolidPlacement(source.placement): el origen por la afín inversa (incluye tx, ty, tz y dz) y la normal por la traspuesta de la inversa, normalizada. Exporta de solid3d-build.ts un helper `cadSolidWorldPlaneToLocal(plane, placement)` y úsalo tanto con coordinatePlane como con verticalPlane. Añade un spec: una caja 200×300×400 con placement {tz: 1000}, sobre la que SLICE XY 1200 da volumen = mitad y centroide z mundial ≈ 1300.

### t2-slice-section-4 · [media] Con planos coordenados, «Izquierda» y «Derecha» no corresponden a lo que se ve (en YZ quedan invertidas)

- **Fichero:** `apps/web/src/lib/cad/engine/commands/solids-modify.ts:374`
- **Problema:** El prompt final ofrece Izquierda, Derecha y Ambos, con Izquierda por defecto, y la asignación Izquierda → positive sólo tiene sentido para verticalPlane, cuya normal se construye apuntando a la izquierda del recorrido. En YZ la normal es +X, así que «Izquierda» (y también Intro) conserva x > cota: la parte DERECHA en planta. En XY conserva la de arriba y en ZX la de +Y, ninguna de las cuales es «izquierda».
- **Evidencia:** Línea 374: `: [keyword === KEEP_RIGHT.keyword ? "negative" : "positive"]`. Líneas 308-311: options [KEEP_LEFT, KEEP_RIGHT, KEEP_BOTH], defaultOption KEEP_LEFT. Líneas 281-282: YZ → normal (1,0,0). El comentario de verticalPlane explica que la normal apunta a la IZQUIERDA para que las palabras signifiquen lo que se ve.
- **Verificación independiente:** Confirmado en lo esencial. El prompt final ofrece siempre [KEEP_LEFT, KEEP_RIGHT, KEEP_BOTH] con Izquierda por defecto (solids-modify.ts:310), y la línea 374 traduce Derecha → negative y el resto → positive. En sliceBody (solid3d-build.ts:535-536) la caja se extruye hacia el lado descartado con height = -size cuando keep es positive, así que positive conserva el lado al que apunta la normal (el propio spec lo asume: YZ positive → centroide x ≈ 150). coordinatePlane da YZ → normal (1,0,0) (línea 282), de modo que «Izquierda», y también Intro, conserva x > cota, que en planta es la DERECHA. Eso contradice el comentario de verticalPlane (líneas 258-260), que orienta la normal para que Izquierda y Derecha signifiquen lo que se ve. En XY, izquierda y derecha no significan nada (conserva la parte de arriba). Matiz: en ZX, +Y coincide con la izquierda de un recorrido hacia +X según la convención de verticalPlane, así que esa parte del hallazgo es discutible; la inversión en YZ y el sinsentido en XY sí son reales.
- **Arreglo:** En planeStep, cuando state.planeMode no sea null, ofrece palabras clave propias del lado: para XY «Arriba» (atajo AR, positive) y «Abajo» (AB, negative); para YZ «Positivo X» y «Negativo X»; para ZX «Positivo Y» y «Negativo Y»; en todos los casos «Ambos». Traduce esas palabras a positive o negative en step de SLICE, en lugar de reutilizar KEEP_LEFT y KEEP_RIGHT. Añade al spec un caso YZ en el que la opción de +X conserve centroide x ≈ 150 y la de −X centroide x ≈ 50.

### t2-slice-section-5 · [media] El spec no ejercita coordinatePlane ni los comandos: pasa con el flujo roto

- **Fichero:** `apps/web/src/lib/cad/slice-coordinate-planes.spec.ts:4`
- **Problema:** La cabecera dice que el spec verifica coordinatePlane, pero la función no se exporta ni se importa. Las 6 aserciones construyen a mano los planos {origin, normal} y evalúan el nodo slice del B-rep, que ya existía antes de T2. Ni sliceCommand ni sectionCommand se ejecutan: no hay palabra clave, cota, lado ni SECTION. El spec seguiría en verde con normales cambiadas en coordinatePlane, con la cota ignorada o con el flujo que hoy lanza en la preview (hallazgo t2-slice-section-1).
- **Evidencia:** Líneas 54, 80 y 106: `plane: { origin: { x: 0, y: 0, z: cutHeight }, normal: { x: 0, y: 0, z: 1 } }` y equivalentes, escritos como literales. Los únicos imports son spec-support, cad-entities-v5 y solid3d-build. No aparece «SECTION» en ninguna aserción.
- **Verificación independiente:** Confirmado. La cabecera de slice-coordinate-planes.spec.ts:4 dice que verifica `coordinatePlane`, pero la función no se exporta: git grep en 445352d8 sólo la encuentra definida en solids-modify.ts:278, usada en las líneas 369 y 458, y citada en el comentario del spec. Los únicos imports del spec son spec-support, cad-entities-v5 y solid3d-build (líneas 7-9). Los tres casos escriben el plano como literal (línea 54: `plane: { origin: { x: 0, y: 0, z: cutHeight }, normal: { x: 0, y: 0, z: 1 } }`, y equivalentes en los otros dos casos) y evalúan con solid3dMassProperties un nodo slice que ya existía antes de T2 (c43f0871^: cad-entities-v5.ts:216, solid3d-build.ts:419, solids-modify.ts:329). No se ejecutan sliceCommand ni sectionCommand, ni hay keyword, cota, lado ni SECTION, así que el spec seguiría en verde con el flujo que hoy lanza en la preview (hallazgo 1), con la cota 0 silenciosa (hallazgo 2) o con normales cambiadas dentro de coordinatePlane.
- **Arreglo:** Reescribe el spec para que recorra los descriptores reales: busca SLICE y SECTION en CAD_SOLID_MODIFY_COMMANDS y construye un CadCommandContext mínimo con la caja seleccionada. Encadena begin → keyword XY → distance 200 → keyword del lado, y comprueba: el message de cada paso, que no haya preview con puntos null, el nodo slice escrito (plane.origin.z === 200, normal (0,0,1)) y solid3dMassProperties del sólido resultante (volumen y centroide). Haz lo mismo con YZ y ZX. Para SECTION XY 200, comprueba que se inserta una region cuyo contorno tiene todos los z === 200 y área 200×300. Si prefieres un test unitario, exporta coordinatePlane y afírmala directamente. Corrige la cabecera para que describa lo que se prueba de verdad.

## T14 · Cuadros de arquitectura con la unidad del documento (c212e3f4, 1ea4e60f; revisado en 445352d8)

**Resumen del revisor:** La conversión de las tablas insertadas es real y correcta. Las longitudes usan el factor mm/unidad, las áreas el factor al cuadrado y los volúmenes al cubo (data-extraction.ts:41-70). Pulgadas (25,4) y pies (304,8) están en CAD_MM_PER_UNIT, y una unidad desconocida o ausente cuenta como mm, así que los documentos viejos y el catálogo de huecos (que llama a openingMark con widthMm y sin unidad) no cambian. openingMark pasa a mm antes de redondear a cm, y context.unit sí llega desde el anfitrión real (studio-context.ts:129). Pero hay cuatro problemas. (1) El CSV de DATAEXTRACTION, el único fichero que entrega el comando, sigue sin unidad: el commit añade `unit` a buildCadDataExtractionCsv, pero la única llamada (data-extraction-commands.ts:140) no lo pasa. En un documento en metros el CSV sale con la marca bien (P-090x210) y con Ancho (mm) = 1 y un área de 12 m² como 0.00. (2) En la punta 445352d8, command-integrity.json dice total 296 y el registro tiene 297 (T9 añadió PERSPECTIVE sin regenerar la evidencia), así que check:command-integrity, dentro de check:cad, falla en CI. El conteo 296 / informa +2 era coherente en 1ea4e60f. (3) Ningún spec prueba una unidad distinta de mm. (4) Quedan comentarios que ahora son falsos (P-000x000) y la función LISP vd-carpinteria da una marca distinta de la del cuadro. No se ejecutó nada: todo sale de leer el código.

**Afirmaciones de tus commits que no se cumplen:**

- Los cuadros de muros, huecos y locales salen con la unidad correcta en todas las salidas de DATAEXTRACTION (el commit también añade unit a buildCadDataExtractionCsv)
  - Evidencia: data-extraction.ts:117 añade `unit?: string` a buildCadDataExtractionCsv, pero la única llamada de producción, data-extraction-commands.ts:140-143, sigue siendo buildCadDataExtractionCsv(schedule, { wires, tags }) sin tercer argumento. wallRowValues, openingRowValues y roomRowValues reciben unit=undefined, que cuenta como mm. El parámetro existe pero no está conectado.
- command-integrity.json actualizado: 296 comandos
  - Evidencia: En 1ea4e60f era cierto: 0e684821 dejó el manifiesto en 296. En la punta revisada 445352d8 ya no: el commit T9 subió command-manifest.ts a 297 (añade PERSPECTIVE) y no tocó docs/cad/evidence/command-integrity.json, que sigue con "total": 296 en la línea 3. check-command-integrity.mjs recalcula total = CAD_COMMAND_REGISTRY_V2.names().length y compara con el fichero.

### t14-cuadros-unidades-1 · [alta] El CSV de DATAEXTRACTION sigue sin unidad: el parámetro unit añadido no está conectado

- **Fichero:** `apps/web/src/lib/cad/engine/commands/data-extraction-commands.ts:140`
- **Problema:** El commit añade `unit` a buildCadDataExtractionCsv (data-extraction.ts:117), pero el comando llama buildCadDataExtractionCsv(schedule, { wires, tags }) sin él. Como el schedule sí se construye con context.unit (línea 135), las marcas del CSV salen en mm y todas las demás columnas (espesor, longitud, áreas, volumen, ancho, alto, antepecho, perímetro) siguen tratando los números del documento como mm. Resultado: un CSV incoherente en cualquier documento en cm, m, in o ft.
- **Evidencia:** Documento en m con el cuarto de 4×3 del spec a escala (muros de 0,2 × 2,6) y una puerta de 0,9×2,1. En el CSV la marca sale P-090x210 (openingMark con unit), pero Ancho (mm) = fmt(0.9,0) = "1", Alto (mm) = "2", Espesor (mm) = "0", Longitud (m) = 14/1000 = "0.014" y Área a ejes (m²) = 12/1e6 = "0.00". El comentario de la línea 136 dice que el CSV «es el único fichero que DATAEXTRACTION entrega».
- **Verificación independiente:** Confirmado en 445352d8. data-extraction-commands.ts:135 construye el schedule con context.unit, pero en :140-143 llama buildCadDataExtractionCsv(schedule, { wires, tags }) sin tercer argumento, aunque data-extraction.ts:117 ya lo acepta. No hay más llamadores en apps/ (git grep: sólo el comando y el spec). Las filas del schedule guardan las medidas crudas del documento (bim-schedule.ts:242-244, width/height/sill = opening.*; :299 thickness). Dentro del CSV, wallRowValues, openingRowValues y roomRowValues reciben unit=undefined, y cadMillimetresPerUnit(undefined) vale 1 (architecture-support.ts:27-28). fmt es toFixed (data-extraction.ts:33-35). Por eso, en un documento en m (m=1000, architecture-support.ts:24) la marca sale bien, P-090x210 (openingMark ya recibió la unidad), pero Ancho = (0.9).toFixed(0) = "1", Espesor = "0", Longitud = 0.014 y Área = 0.00. El CSV mezcla columnas convertidas y sin convertir, justo en la única salida de fichero del comando.
- **Arreglo:** En apps/web/src/lib/cad/engine/commands/data-extraction-commands.ts:140, pasa la unidad como tercer argumento: buildCadDataExtractionCsv(schedule, { wires: …, tags: … }, context.unit). Añade en data-extraction.spec.ts un caso con el cuarto construido en metros (coordenadas /1000, thickness 0.2, height 2.6, puerta 0.9×2.1). Debe afirmar que el CSV contiene "14.000", "12.00", "P-090x210" y ",900,2100," (o las celdas equivalentes) al llamar buildCadDataExtractionCsv(schedule, undefined, "m"), y que sin unit NO las contiene.

### t14-cuadros-unidades-2 · [media] command-integrity.json (296) no coincide con el registro de la punta (297): check:cad falla

- **Fichero:** `docs/cad/evidence/command-integrity.json:3`
- **Problema:** 1ea4e60f dejó la evidencia en 296 comandos. Luego 445352d8 (T9) añadió PERSPECTIVE al registro y al manifiesto (297) sin regenerar la evidencia. check-command-integrity.mjs compara siempre payload.total con el fichero y sale con código 1 si difieren. El gate está en check:cad, que CI corre (ci.yml:238), así que la rama no pasa CI o la evidencia describe un registro que ya no existe.
- **Evidencia:** command-manifest.ts en 445352d8: «METADATOS de los 297 comandos», con la entrada { name: "PERSPECTIVE", … module: "commands/view-visual" }. command-integrity.json:3 dice "total": 296 y el diff de 445352d8 no toca docs/cad/evidence/. check-command-integrity.mjs:111-113 hace `if (onDisk.total !== payload.total) fields.push(...)` y luego process.exit(1).
- **Verificación independiente:** Confirmado, con un matiz: 1ea4e60f era coherente cuando se hizo (el manifiesto en 1ea4e60f dice «296 comandos»). El desfase lo mete 445352d8. Ese commit añade PERSPECTIVE a CAD_VIEW_VISUAL_COMMANDS (view-visual.ts:170-172) y al manifiesto («297 comandos»), y no toca docs/cad/evidence/. Tampoco lo hacen los commits posteriores de la rama (b26a3fa5, ef43bb6e). La sonda recorre CAD_COMMAND_REGISTRY_V2.names() (command-integrity-probe.mts:369-373) con total = outcomes.length (:390). check-command-integrity.mjs:107-130 compara siempre con el JSON en disco ("total": 296) y hace process.exit(1) si difiere. Ese script entra en check:cad (package.json:60 y :98), que CI corre en ci.yml:238. En la punta revisada el gate falla.
- **Arreglo:** Tras el último commit que añada comandos, regenera la evidencia con `node scripts/cad/check-command-integrity.mjs --write` (no a mano) y commitea docs/cad/evidence/command-integrity.json con total 297 y los veredictos que dé la sonda (PERSPECTIVE, mutates:false, debería sumar a delegado o informa). Añade a la regla de trabajo que cada commit que cambie command-manifest.ts regenere también esta evidencia en el mismo commit.

### t14-cuadros-unidades-3 · [baja] Ningún spec prueba los cuadros en una unidad distinta de mm

- **Fichero:** `apps/web/src/lib/cad/data-extraction/data-extraction.spec.ts:36`
- **Problema:** T14 no añade ni cambia specs. data-extraction.spec.ts construye todo en mm y llama a los cuatro constructores sin unit. architecture-openings-catalog.spec.ts sólo prueba openingMark en mm, y bim-schedule.spec.ts nunca pasa unit. Si se quitara la conversión, o si el área usara mm en vez de mm², todos los specs que recoge run-specs.mjs (src/**/*.spec.ts) seguirían en verde. Por eso no se detectó el CSV sin unidad.
- **Evidencia:** git grep de 'unit' en data-extraction.spec.ts y bim-schedule.spec.ts en 445352d8: ninguna llamada con segundo argumento (buildCadBimSchedule({ entities: room() }) en :36, buildCadDataExtractionCsv(named) en :113). En architecture-openings-catalog.spec.ts, openingMark sólo se llama con entidades en mm (:198, :260, :322).
- **Verificación independiente:** Confirmado. c212e3f4 sólo toca bim-schedule.ts, data-extraction.ts y data-extraction-commands.ts; ningún spec. data-extraction.spec.ts:36, 45, 62, 67, 96, 107 y 113 llama a los constructores sin unit, con geometría en mm. bim-schedule.spec.ts sólo declara unit "mm" (:126) y nunca pasa segundo argumento. data-extraction-commands.spec.ts:330-331 reconoce que su contexto de prueba no declara unit. En architecture-openings-catalog.spec.ts, openingMark se llama sin unit (:198, :260, :322); los bucles UNITS (:209, :242, :266) prueban la colocación y el ida y vuelta de medidas, no la marca ni las tablas. Con mm el factor vale 1, así que quitar la conversión, o usar mm en lugar de mm², no rompería ningún spec. Por eso nadie vio el fallo del hallazgo 1.
- **Arreglo:** En data-extraction.spec.ts, añade un bloque que construya el mismo cuarto y huecos en "m" y en "in" (coordenadas y medidas convertidas con cadFromMillimetres). Para la tabla de muros, la de superficies, la de carpintería y el CSV, afirma que las celdas coinciden carácter a carácter con las del documento en mm ("14.000", "12.00", "10.64", "13.44", "P-090x210", "900"). En architecture-openings-catalog.spec.ts, dentro del bucle UNITS, añade eq(openingMark(placeType(type, unit), unit), cadOpeningTypeMark(type)).

### t14-cuadros-unidades-4 · [baja] Comentarios que ahora afirman algo falso: «en metros o pies el cuadro imprime P-000x000»

- **Fichero:** `apps/web/src/lib/cad/architecture-openings-catalog.ts:48`
- **Problema:** Tras T14, openingMark acepta la unidad y el cuadro de carpintería de DATAEXTRACTION ya imprime la marca correcta en m o ft. La documentación del catálogo y de su spec sigue declarando el defecto como vigente («sigue en pie»), lo que confunde a quien mantenga el módulo.
- **Evidencia:** architecture-openings-catalog.ts:48-54: «La marca (`openingMark`) lee sus números como milímetros, así que el cuadro de carpintería de un documento en metros o en pies imprime hoy P-000x000… y sigue en pie». architecture-openings-catalog.spec.ts:26-31: «En metros o en pies la marca del cuadro es P-000x000 para todo».
- **Verificación independiente:** Confirmado. architecture-openings-catalog.ts:48-54 aún dice que openingMark lee milímetros, que el cuadro de carpintería de un documento en metros o pies imprime P-000x000 y que eso «sigue en pie». architecture-openings-catalog.spec.ts:26-31 repite que en metros o pies la marca del cuadro es P-000x000. Tras c212e3f4 ya no es así: openingMark(opening, unit) convierte con cadToMillimetres (bim-schedule.ts, diff del commit), y DATAEXTRACTION pasa context.unit a buildCadBimSchedule y a buildCadOpeningScheduleTable (data-extraction-commands.ts:168 y :192). Ese context.unit lo rellena studio-context.ts:129 desde document.meta.unit, así que el cuadro insertado imprime P-090x210 en m. El comentario describe como vigente un defecto que el commit corrigió (sólo sigue en el LISP, hallazgo 5). Severidad baja, pero real.
- **Arreglo:** Reescribe architecture-openings-catalog.ts:48-54 y architecture-openings-catalog.spec.ts:26-31. Deben decir que openingMark(opening, unit) convierte a mm con cadToMillimetres, que el cuadro de DATAEXTRACTION pasa context.unit y que sin unit se asume mm. Borra la afirmación de P-000x000 como límite vigente.

### t14-cuadros-unidades-5 · [baja] vd-carpinteria (LISP) no pasa la unidad y ya no coincide con el cuadro del producto

- **Fichero:** `apps/web/src/lib/lisp/builtins/bim.ts:37`
- **Problema:** El módulo LISP promete devolver «los mismos números que enseña el producto», porque salen de bim-schedule.ts. Tras T14, DATAEXTRACTION calcula las marcas con la unidad del documento, pero schedule() en bim.ts sigue llamando buildCadBimSchedule sin unit. En un documento en metros, el cuadro insertado dice P-090x210 y (vd-carpinteria) devuelve P-000x000 para la misma puerta. Antes de T14 las dos salidas coincidían, aunque ambas mal.
- **Evidencia:** bim.ts:36-38: `return buildCadBimSchedule(requireHost(ctx, name).document());`. host.ts:38 declara document(): CadDocument, así que meta.unit está disponible. bim.ts:14-18: «devuelven los mismos números que enseña el producto —salen de `bim-schedule.ts`, no de un cálculo paralelo—».
- **Verificación independiente:** Confirmado. bim.ts:36-38 hace buildCadBimSchedule(requireHost(ctx, name).document()) sin unit. host.ts:38 declara document(): CadDocument y cad-document.ts:70 define meta.unit como string, así que la unidad está al alcance. bim.ts:14-18 y sandbox-surface.spec.ts:110-111 prometen «los MISMOS números que enseña el producto», calculados en bim-schedule.ts. Tras T14, en un documento en m, el cuadro de DATAEXTRACTION usa context.unit y sale P-090x210. vd-carpinteria entrega row.mark calculado con unit=undefined: Math.round(0.9/10) = 0, o sea P-000x000. Antes de T14 las dos salidas decían P-000x000; ahora no coinciden. Que ancho y alto sigan en unidades del dibujo es una convención defendible, pero la marca es la misma clave y hoy difiere.
- **Arreglo:** En apps/web/src/lib/lisp/builtins/bim.ts:36-38, cambia schedule() por `const document = requireHost(ctx, name).document(); return buildCadBimSchedule(document, document.meta?.unit);`. Deja ancho, alto y demás números de vd-* en unidades del dibujo (es lo que espera una rutina) y documenta en el comentario de vd-carpinteria que la marca va en cm reales. Añade una aserción en el spec de builtins BIM con un documento en "m" que espere la marca P-090x210.

## T7 alzados verdaderos · T9 proyección paralela · T8 estilos visuales (9d4f8de5, c3027d83, 445352d8)

**Resumen del revisor:** Ninguna de las tres tareas cumple lo que anuncia, y la rama no pasaría el CI.

T7: el desbloqueo del tope polar no sirve. applyStandardView lo desbloquea y lo vuelve a poner en el mismo momento, antes de que corra el controls.update() del bucle de render. En el siguiente cuadro OrbitControls recorta φ a 87,8° y las vistas Frontal, Posterior, Izquierda, Derecha e Inferior se deshacen igual que antes. La órbita del usuario no se queda desbloqueada, pero sólo porque el desbloqueo nunca llega a actuar. VPOINT Rotar (setOrbit) ni siquiera pasa por el desbloqueo. No hay spec del cambio: las cifras de aserciones del mensaje son de specs que no se tocaron y que no usan la API nueva.

T9: el comando PERSPECTIVE existe y escribe la variable, pero nadie la lee. setProjection no tiene llamadores y sólo copia un campo: `get camera()` sigue dando la PerspectiveCamera en 3D. No hay cámara ortográfica, ni encuadre al cambiar, ni UI. Además PERSPECTIVE no tiene icono ni resumen (fallan command-icons.spec y command-summaries.spec) y la evidencia command-integrity.json sigue en 296 (falla check:cad).

T8: las tres filas están en la tabla y los atajos N, R y G no chocan con nada, pero Conceptual y Tonos de gris son copias exactas de Sombreado con aristas. VSCURRENT confirma el cambio y el visor pinta lo mismo. Rayos X sí se ve distinto: los constructores leen opacity 0.35 y las caras salen translúcidas. Subir la tabla a 7 estilos rompe dos specs que exigen exactamente 4.

Reglas del repo: no se añaden colores hex, no se renombra ningún data-testid ni identificador persistido, y los textos visibles están en español sin marcas prohibidas.

**Afirmaciones de tus commits que no se cumplen:**

- T7: VPOINT y VIEW dejan la cámara en un alzado verdadero (φ = 90°) o en la vista inferior
  - Evidencia: view-controller.ts:399 llama lock?.() en el mismo tick, antes de cualquier controls.update(). Ni emit() ni navigation-host.publish() llaman a controls.update(), y el bucle de render sí lo hace en cada cuadro (Layout3DEditor.tsx:7576), ya con maxPolarAngle otra vez en π/2,05. La vista Frontal (offset y=0, view-3d.ts:198) se recorta a 87,8°. VPOINT Rotar pasa por setOrbit (view-controller.ts:338), que ni siquiera llama al callback.
- T7: camera-policy.spec (23 aserciones) y view-3d.spec (131 aserciones) respaldan el cambio
  - Evidencia: `git show --stat 9d4f8de5` no toca ningún spec. `git grep unlockPolarAngleForCommand|onBeforeCommandedView 445352d8 -- '*.spec.ts'` no da resultados. La sección 7 de view-3d.spec.ts (línea 236) usa un CadViewController sin OrbitControls, así que pasa aunque el desbloqueo no funcione.
- T8: al aplicar Conceptual o Tonos de gris el visor pinta algo distinto
  - Evidencia: Ambas filas son idénticas a shaded-edges (faces, edges, occludes y removesHiddenEdges a true; opacity 1). solid3d-three.ts:219-262 y visual-style-mesh.ts:58-87 sólo leen esos campos, y solid-shade-host.ts:301 sólo trata aparte a «hidden». El resultado es la misma malla y el mismo material.
- T8: el render de Rayos X queda pendiente, porque nadie consume opacity 0.35
  - Evidencia: opacity sí se consume: solid3d-three.ts:222-223 y visual-style-mesh.ts:61-62 crean un MeshLambertMaterial con transparent = true y opacity 0.35, así que las caras salen translúcidas con todas las aristas. Lo que sigue pendiente es el sombreado Gooch y la desaturación.
- T9: setProjection alterna entre PerspectiveCamera y OrthographicCamera sin salto de encuadre
  - Evidencia: view-controller.ts:202-206 sólo hace this.current = {...this.current, projection} y emite. `get camera()` (líneas 153-155) devuelve la perspectiva siempre que mode sea '3d', y setProjection no tiene ningún llamador en el repositorio.
- T9: campo projection en CadView, con persistencia
  - Evidencia: El campo existe (cad-view.ts:46), pero sólo lo escribiría setProjection, que nadie llama. Ningún lector lo usa y no se guarda en lastCamRef ni en vistas guardadas.
- T9: la UI expone el cambio de proyección
  - Evidencia: No hay cambios de UI. PERSPECTIVE no tiene entrada en command-icons.ts ni en command-summaries.ts, y la regex de Vistas 3D de ribbon.ts:178 no lo incluye.

### vista-alzados-proyeccion-estilos-1 · [alta] El desbloqueo polar se restaura antes del update() de OrbitControls: los alzados se siguen recortando a 87,8°

- **Fichero:** `apps/web/src/lib/cad/view/view-controller.ts:399`
- **Problema:** applyStandardView desbloquea maxPolarAngle (línea 388) y lo restaura con lock?.() en el mismo tick (línea 399). Nadie llama a controls.update() entre medias. La primera llamada es la del bucle de render en el siguiente cuadro, y para entonces el tope ya es π/2,05, así que φ = 90° se recorta a 87,8°. VIEW Frontal, VPOINT Frontal, Posterior, Izquierda, Derecha e Inferior siguen sin dar alzados exactos: la T7 no arregla nada.
- **Evidencia:** view-controller.ts:388 `const lock = this.onBeforeCommandedView?.();` y :399 `lock?.();`. Los listeners de emit() en Layout3DEditor.tsx:6082 sólo marcan batchedViewDirty, y navigation-host.ts:122 publish() no toca los controles. Layout3DEditor.tsx:7576 llama `controls.update();` en cada cuadro. applyCadCameraPolicy fija `controls.maxPolarAngle = plan ? 0.05 : Math.PI / 2.05` (camera-policy.ts). La vista Frontal tiene offset {x:0, y:0, z:-1} (view-3d.ts:198), es decir φ = 90°.
- **Verificación independiente:** Confirmado leyendo 445352d8. En view-controller.ts:387 se hace `const lock = this.onBeforeCommandedView?.()` y en :398 `lock?.()`, dentro del mismo método síncrono, después de emit() (:397). Las líneas reales son 387 y 398, no 388 y 399. Entre las dos llamadas nadie llama a controls.update(). syncFromPerspective (:461-475) sólo recalcula `current`. Los oyentes de onChange tampoco lo hacen: Layout3DEditor.tsx:6082 sólo pone batchedViewDirty, solid-shade-host.ts:172 sólo llama a refreshHiddenLines, y collab-overlay sólo programa trabajo. plan-wheel-anchor.ts:146 llama a update() dentro de panDrawing, no en onViewChange. navigation-host.ts:122-130 (publish) no toca los controles, y use-command-engine.ts:237 pasa el controlador sin envolverlo. El primer update() llega en el bucle de render (Layout3DEditor.tsx:7576), y para entonces maxPolarAngle ya vuelve a valer π/2,05 (camera-policy.ts:151). OrbitControls de three ^0.185.1 recorta phi a [minPolarAngle, maxPolarAngle] en cada update(). La vista Frontal usa offset (0,0,-1) con up (0,1,0) (view-3d.ts:198-199), o sea φ = 90°, que acaba en 87,8°. Hay además un segundo restaurador: Layout3DEditor.tsx:8336-8347 vuelve a aplicar applyCadCameraPolicy en cada cambio del motor de comandos. El desbloqueo no tiene efecto.
- **Arreglo:** No restaures el tope en applyStandardView: quita `lock?.()` de view-controller.ts:399. Cambia unlockPolarAngleForCommand (camera-policy.ts:257) para que deje maxPolarAngle = Math.PI - 1e-4 y registre UNA sola vez un `controls.addEventListener('start', restore)` que devuelva el tope de arrastre de la política (Math.PI/2.05 en 3D) cuando el usuario empiece a orbitar, y que después se dé de baja. Si llega un segundo comando con un restaurador pendiente, reutilízalo: no captures el valor desbloqueado como dragLimit. Ten en cuenta que applyCadCameraPolicy (Layout3DEditor.tsx:8336 y :8347) reescribe maxPolarAngle al abrir un comando con designación: haz que respete el desbloqueo pendiente. Añade a camera-policy.spec.ts un caso con OrbitControls real (o con su aritmética de clamp): cámara colocada en φ = 90°, llamada a controls.update(), comprobar que φ sigue en 90° ± 1e-6; después disparar 'start' y comprobar que maxPolarAngle vuelve a π/2,05.

### vista-alzados-proyeccion-estilos-2 · [alta] PERSPECTIVE no cambia la cámara: variable sin lectores, setProjection sin llamadores ni cámara ortográfica

- **Fichero:** `apps/web/src/lib/cad/view/view-controller.ts:202`
- **Problema:** El comando escribe PERSPECTIVE = 0/1 y contesta «Proyección: Paralela.», pero nada reacciona. Nadie lee la variable, nadie llama a setProjection, y setProjection sólo copia el campo projection sin tocar cámaras, al contrario de lo que dice su JSDoc. `get camera()` sigue devolviendo la PerspectiveCamera en 3D. No hay encuadre coherente al conmutar, no hay zoom en paralela y no hay control en la UI. Es un éxito falso de la línea de comandos.
- **Evidencia:** `git grep -n -E "\bPERSPECTIVE\b" 445352d8 -- apps packages` sólo encuentra command-manifest.ts:332, view-visual.ts:135/163 y system-variables.ts:176. `git grep setProjection 445352d8` sólo encuentra la definición. view-controller.ts:153-155: `return this.current.mode === "2d" ? this.orthographic : this.perspective;`. view-controller.ts:202-206 sólo hace `this.current = { ...this.current, projection }; this.emit();`.
- **Verificación independiente:** Confirmado. setProjection (view-controller.ts:201-205) sólo hace `this.current = { ...this.current, projection }; this.emit();`. Su JSDoc (:193-200) promete alternar a una OrthographicCamera, pero el código no lo hace. `get camera()` (:152-154) devuelve la perspectiva en 3D, y la única ortográfica (:135-136) es la del 2D. `git grep setProjection 445352d8` sólo encuentra la definición. `git grep PERSPECTIVE` en código sólo encuentra command-manifest.ts:332, view-visual.ts:128/135/163 y system-variables.ts:176: nadie lee la variable. `grep .projection` en lib/cad/view y components/cad no encuentra lectores de CadView.projection; los usos de Layout3DEditor.tsx:9913+ son de otro tipo. El comando devuelve `kind: "variables"` con el texto «Proyección: Paralela.» (view-visual.ts:160-165) sin ningún efecto visible. ribbon.ts:178 no incluye PERSPECTIVE. Es un éxito falso.
- **Arreglo:** 1) En Layout3DEditor suscríbete al almacén de variables de sistema y, cuando cambie PERSPECTIVE, llama `viewController.setProjection(value === 0 ? 'parallel' : 'perspective')`; aplica también el valor inicial al montar. 2) En CadViewController haz que `get camera()` devuelva una OrthographicCamera 3D cuando `mode === '3d' && projection === 'parallel'`. Esa cámara debe copiar position, quaternion y up de la perspectiva, con frustum de alto 2·distancia·tan(fov/2) y ancho alto·aspect, y resincronizarse en orbitPerspective, orbitFreePerspective, setOrbit, applyStandardView, panPerspective, zoomPerspective y en el 'change' de OrbitControls. En paralela, el zoom tiene que cambiar la altura del frustum y no sólo la distancia. 3) Expón el conmutador en la pestaña Vistas 3D (añade PERSPECTIVE a la regex de ribbon.ts:178). 4) Añade un spec que ejecute `PERSPECTIVE P` y compruebe que controller.camera es OrthographicCamera, que la altura visible coincide con la de la perspectiva previa (sin salto) y que `PERSPECTIVE PE` vuelve a la perspectiva.

### vista-alzados-proyeccion-estilos-3 · [media] VPOINT Rotar (setOrbit) no pasa por onBeforeCommandedView: elevación 0 o negativa recortada

- **Fichero:** `apps/web/src/lib/cad/view/view-controller.ts:338`
- **Problema:** El mensaje de T7 dice que VPOINT coloca alzados, pero sólo la rama de palabra clave (standard-view) pasa por el callback. `VPOINT R 270 0` va por orbit-to y setOrbit, que no desbloquea: la elevación 0° acaba en unos 2,2° en el siguiente update(), y cualquier elevación negativa (vista desde abajo) sube por encima del horizonte.
- **Evidencia:** view-navigation-3d.ts:345-354 devuelve `{ kind: "orbit-to", azimuthDeg: 90 - …, elevationDeg: value }`. navigation-host.ts muestra `case "orbit-to": controller.setOrbit(...)`. view-controller.ts:338-353 coloca la cámara sin llamar a onBeforeCommandedView, y controls.maxPolarAngle vale π/2,05.
- **Verificación independiente:** Confirmado. En VPOINT, la rama Rotar (view-navigation-3d.ts:343-356) devuelve `kind: "orbit-to"` con elevationDeg igual al valor tecleado. navigation-host.ts:234-237 lo manda a controller.setOrbit. setOrbit (view-controller.ts:337-352) coloca la cámara sin invocar onBeforeCommandedView: sólo applyStandardView (:387) lo invoca. orbitStep y orbitCameraPosition (visual-styles.ts:181-227) sólo acotan a ±89,9°. Así, una elevación de 0° deja φ = 90° y una de −30° deja φ = 120°. El update() del bucle (Layout3DEditor.tsx:7576), con maxPolarAngle = π/2,05, recorta ambos a 87,8°, es decir, a unos 2,2° de elevación. El fallo ya existía antes de T7, pero T7 anuncia que arregla VPOINT y deja esta rama sin cubrir.
- **Arreglo:** En setOrbit (view-controller.ts:338) invoca `this.onBeforeCommandedView?.()` antes de colocar la cámara, con el contrato corregido del hallazgo 1 (restaurar en 'start', no en síncrono). Añade un spec que ejecute VPOINT Rotar 270, 0 y luego −30, y compruebe tras controls.update() que la elevación resultante es 0° y −30°.

### vista-alzados-proyeccion-estilos-4 · [media] T7 sin spec: las cifras de aserciones citadas no prueban el desbloqueo

- **Fichero:** `apps/web/src/components/cad/viewport/camera-policy.ts:257`
- **Problema:** El commit presenta «camera-policy.spec 23 aserciones, view-3d.spec 131 aserciones» como respaldo, pero no modifica ningún spec y ninguno referencia unlockPolarAngleForCommand ni onBeforeCommandedView. La sección 7 de view-3d.spec prueba applyStandardView sobre un controlador sin OrbitControls, así que pasaba antes del cambio y sigue pasando con el desbloqueo roto del hallazgo 1.
- **Evidencia:** `git show --stat 9d4f8de5` sólo lista Layout3DEditor.tsx, camera-policy.ts y view-controller.ts. `git grep -n -E "unlockPolarAngleForCommand|onBeforeCommandedView" 445352d8 -- '*.spec.ts'` no da resultados. view-3d.spec.ts:236 hace `new CadViewController(...)` sin controles.
- **Verificación independiente:** Confirmado. `git show --stat 9d4f8de5` sólo lista Layout3DEditor.tsx, camera-policy.ts y view-controller.ts. `git grep -E "unlockPolar|onBeforeCommanded"` sobre los *.spec.ts de 445352d8 no encuentra nada. camera-policy.spec.ts sólo usa un doble con maxPolarAngle para la política (:36-62). En view-3d.spec.ts, la sección 7 (:236-243) y la 8 (:327) crean `new CadViewController(...)` sin OrbitControls, así que onBeforeCommandedView es undefined y el desbloqueo no se ejecuta. Esos specs pasaban antes del cambio y siguen pasando con el fallo del hallazgo 1. Los recuentos de aserciones del mensaje de commit no respaldan nada.
- **Arreglo:** Añade a camera-policy.spec.ts aserciones que fallen si el desbloqueo no funciona: con OrbitControls (o una réplica exacta de su clamp de phi), aplicar la vista frontal a través de onBeforeCommandedView, llamar update() y exigir φ = π/2; exigir también que antes de 'start' maxPolarAngle siga desbloqueado y que después vuelva a π/2,05. No cites en el mensaje de commit recuentos de specs que no has tocado.

### vista-alzados-proyeccion-estilos-5 · [media] Conceptual y Tonos de gris son copias de Sombreado con aristas: VSCURRENT confirma un cambio que no se ve

- **Fichero:** `apps/web/src/lib/cad/view/visual-styles.ts:107`
- **Problema:** Las filas conceptual (línea 107) y grayscale (línea 125) tienen exactamente los mismos valores que shaded-edges. Los constructores del visor sólo leen esos cinco campos, así que construyen la misma malla y el mismo material. `VSCURRENT N` o `VSCURRENT G` anuncian «VSCURRENT Conceptual» o «Tonos de gris» y el sólido se ve igual: es el mismo éxito falso que T-10a combatía. En main quedarían expuestos al usuario.
- **Evidencia:** visual-styles.ts:107-114 y :125-132 tienen faces, edges, occludes y removesHiddenEdges a true y opacity 1, igual que shaded-edges (:98-105). solid3d-three.ts:219-262 y visual-style-mesh.ts:58-87 sólo ramifican por esos campos, y solid-shade-host.ts:301 sólo distingue «hidden».
- **Verificación independiente:** Confirmado. En visual-styles.ts, conceptual (:106-114) y grayscale (:124-132) tienen faces, edges, occludes y removesHiddenEdges a true y opacity 1, exactamente igual que shaded-edges (:97-105). Los constructores sólo ramifican por esos campos. solid3d-three.ts:219-262 crea un MeshLambertMaterial con SOLID_COLOR, transparent = opacity < 1, y aristas si edges es true. visual-style-mesh.ts:58-87 hace lo mismo. solid-shade-host.ts:301 sólo distingue «hidden». Fuera de specs y de la tabla, nadie compara con "conceptual" ni con "grayscale". VSCURRENT devuelve la etiqueta «VSCURRENT Conceptual» (view-visual.ts:119) y plot-host.ts:176 anuncia «Estilo visual: Conceptual.» con el mismo render. El propio commit admite que el render queda pendiente. Rayos X sí se distingue por opacity 0.35 y occludes false.
- **Arreglo:** Elige una de dos. (a) Retira conceptual y grayscale de CAD_VISUAL_STYLES, de CadVisualStyleId y de STYLE_SHORTCUTS hasta implementar su render, y deja sólo xray, que ya se pinta translúcido. (b) Añade a CadVisualStyle un campo aditivo, por ejemplo `shading: 'lambert' | 'gooch' | 'grayscale'`, que solid3d-three.ts:219 y visual-style-mesh.ts:58 consuman (material Gooch cálido/frío para conceptual, color desaturado para grayscale, sin hex sueltos: usa las constantes existentes), y un spec que construya el grupo con cada estilo y exija que el material de conceptual y grayscale difiera del de shaded-edges.

### vista-alzados-proyeccion-estilos-6 · [media] Pasar la tabla a 7 estilos rompe dos specs del runner de CI que exigen exactamente 4

- **Fichero:** `apps/web/src/lib/cad/solid3d-view.spec.ts:91`
- **Problema:** c3027d83 amplía CAD_VISUAL_STYLES pero no actualiza los specs que fijan el recuento. run-specs.mjs recoge src/**/*.spec.ts y CI ejecuta `npm run test:specs` (ci.yml:366). report() lanza si algún check falló, así que el job queda rojo y bloquea la fusión de toda la rama.
- **Evidencia:** solid3d-view.spec.ts:91 `check("hay exactamente cuatro estilos", CAD_VISUAL_STYLES.length === 4)`. hidden-lines.spec.ts:44 `check("siguen siendo cuatro estilos", CAD_VISUAL_STYLES.length === 4)`. spec-support.ts report() hace `assert.fail(...)` cuando hay fallos.
- **Verificación independiente:** Confirmado en 445352d8. solid3d-view.spec.ts:91 tiene `check("hay exactamente cuatro estilos", CAD_VISUAL_STYLES.length === 4)` y hidden-lines.spec.ts:44 tiene `check("siguen siendo cuatro estilos", CAD_VISUAL_STYLES.length === 4)`, mientras que la tabla ya tiene 7 filas (visual-styles.ts:69-133). Los dos specs importan check y report de spec-support y llaman a report() al final (solid3d-view.spec.ts:201, hidden-lines.spec.ts:253). report() hace `assert.fail` si hay fallos (spec-support.ts:72-75). run-specs.mjs:43 recoge `src/**/*.spec.ts`, apps/web/package.json:13 lo expone como test:specs y ci.yml:366 ejecuta `npm run test:specs`. Ningún commit posterior hasta 445352d8 ajusta esos recuentos.
- **Arreglo:** Actualiza solid3d-view.spec.ts:91 y hidden-lines.spec.ts:44 al número real de estilos que queden tras el hallazgo 5, y cambia el recuento por aserciones de lo que distingue a cada estilo nuevo: Rayos X con material transparent y opacity 0.35 y todas las aristas; Conceptual y Tonos de gris con material distinto de shaded-edges. Ejecuta `npm run test:specs -w apps/web` antes de commitear.

### vista-alzados-proyeccion-estilos-7 · [media] PERSPECTIVE sin icono, sin resumen y con command-integrity.json desfasado: fallan dos specs y check:cad

- **Fichero:** `apps/web/src/lib/cad/engine/commands/view-visual.ts:134`
- **Problema:** El comando nuevo entra en el registro (vía manifiesto) sin sus contratos de catálogo. command-icons.spec y command-summaries.spec exigen icono y resumen para todo comando registrado, y check-command-integrity.mjs compara el total del árbol (297) con la evidencia commiteada (296). Además el manifiesto se editó a mano aunque su cabecera dice «GENERADO — no se edita a mano».
- **Evidencia:** `git grep PERSPECTIVE 445352d8 -- apps/web/src/components/cad/ribbon/command-icons.ts apps/web/src/lib/cad/engine/command-summaries.ts` no da resultados. command-icons.spec.ts:22-23 hace assert.deepEqual(mudos, []). command-summaries.spec.ts:24-31 hace assert.deepEqual(silent, []). docs/cad/evidence/command-integrity.json:3 dice `"total": 296`. check-command-integrity.mjs:108-130 sale con process.exit(1) si hay diferencias, y va dentro de check:cad (ci.yml:238).
- **Verificación independiente:** Confirmado en lo esencial. PERSPECTIVE entra en el registro vía CAD_VIEW_VISUAL_COMMANDS (view-visual.ts:169-172), el mismo módulo de VSCURRENT. command-icons.ts sólo tiene `VSCURRENT: Sun` (:432) y ninguna entrada PERSPECTIVE. command-summaries.ts sólo tiene VSCURRENT (:312), sin resumen ni exclusión para PERSPECTIVE. command-icons.spec.ts:22-23 hace assert.deepEqual(mudos, []) y command-summaries.spec.ts:24-31 hace assert.deepEqual(silent, []) contra CAD_COMMAND_REGISTRY_V2, así que los dos fallan. docs/cad/evidence/command-integrity.json:3 dice `"total": 296`, y check-command-integrity.mjs compara el total y los veredictos, y sale con process.exit(1) si difieren. Ese script corre dentro de check:cad (package.json:60 y :98; ci.yml:238). Que el manifiesto se editara a mano no puedo demostrarlo sin ejecutar el generador, y no afecta a los dos fallos de spec ni al de integridad, que sí están confirmados.
- **Arreglo:** Añade `PERSPECTIVE: <icono lucide existente>` a CAD_COMMAND_ICONS en command-icons.ts y un resumen en español de México de 10 a 110 caracteres a CAD_COMMAND_SUMMARIES (por ejemplo «Conmuta la vista 3D entre proyección paralela y perspectiva.»). Regenera el manifiesto con `node scripts/cad/build-command-manifest.mjs --write` y la evidencia con `node scripts/cad/check-command-integrity.mjs --write`, y commitea los dos ficheros generados.

## T22 aristas cóncavas · T12 orientación de caras (b3f862a6, 1321ade6; punta revisada 445352d8)

**Resumen del revisor:** T12 es matemáticamente correcto y no rompe nada. La permutación (x,y,z)→(x,z,y) tiene determinante −1. El marco del muro es una rotación: n es la perpendicular izquierda (wall-openings.ts:71), así que el total también da −1. El anillo exterior de la losa sale antihorario (bim-schedule.ts:587-589). placeBody invierte el cuerpo cuando la colocación es un espejo, tanto en 2D como en 3D. No hay espejo en la cámara (up=(0,0,−1)) ni escalas negativas en los grupos.
Las normales son explícitas y pasan por la misma matriz, que es ortogonal y simétrica, así que siguen apuntando hacia fuera; con el índice invertido, el producto cruz coincide con ellas. Mejora el sombreado Lambert con FrontSide, el picking (el Raycaster con FrontSide descarta caras traseras) y la exportación GLB, que exporta esos mismos objetos de escena. No afecta a EdgesGeometry, porque invertir todos los triángulos no cambia el producto punto, ni a las líneas ocultas, que trabajan sobre el B-rep en coordenadas de dibujo. Nadie compensaba el giro viejo: no hay consumidores de face.normal y mesh-three-bridge sólo sirve a importadores.
El problema de T12 es el spec. Nunca llama a los tres constructores: reimplementa por su cuenta la permutación y la inversión, así que seguiría verde aunque se quitara el arreglo.
T22 usa bien la convención: diedro interior, convexa < π y cóncava > π, el mismo umbral π+1e-9 que el kernel (fillet.ts:213). No excluye aristas convexas y T12 no toca el B-rep. Pero cierra sólo la mitad de D4: las aristas planas (diedro ≈ π) siguen entrando en el juego preferido y el kernel las rechaza (fillet.ts:103). Toda booleana deja caras en abanicos de triángulos coplanarios, así que FILLETEDGE sigue fallando entero sobre cualquier sólido que haya pasado por UNION o SUBTRACT. Además, T22 no trae ningún spec.
Ninguno de los dos commits toca esquema, textos visibles, colores ni data-testid. El runner (src/**/*.spec.ts, ci.yml:366) recoge el spec nuevo.

**Afirmaciones de tus commits que no se cumplen:**

- T12: el spec con 3 aserciones verifica que la corrección de los constructores restaura la concordancia
  - Evidencia: face-orientation-fix.spec.ts tiene 3 check(), pero sólo importa solid3dMesh/clearSolidCache (líneas 11-13). Las aserciones 2 y 3 permutan e invierten índices con su propio código (líneas 77-100) y nunca llaman a buildCadSolidGeometry, buildCadWallSolidGeometry ni buildCadArchitecturalSlabGeometry. Si se borrara el arreglo de los tres módulos, el spec seguiría verde.

### geometria-aristas-caras-1 · [media] El spec de T12 se prueba a sí mismo y no toca los tres constructores

- **Fichero:** `apps/web/src/lib/cad/face-orientation-fix.spec.ts:92`
- **Problema:** El spec no ejercita el código corregido: permuta posiciones y normales y luego invierte los índices con su propia copia del algoritmo. Si el arreglo desaparece de solid3d-three.ts, wall-solid-three.ts o room-solid-three.ts, el spec sigue verde. Tampoco cubre el muro, cuyo marco en planta tiene rotación, ni la losa. El criterio de terminado de T12 pedía comprobar cross(b−a,c−a)·normal(a) > 0 sobre la malla de ESCENA.
- **Evidencia:** Líneas 11-13: sólo importa solid3dMesh, clearSolidCache y spec-support. Líneas 77-86 hacen la permutación local y líneas 92-97 la inversión local ('const corrected = [...mesh.indices]'). Las aserciones 2 y 3 (88, 99) operan sobre esas copias. No hay ninguna llamada a buildCadSolidGeometry, buildCadWallSolidGeometry ni buildCadArchitecturalSlabGeometry. Los specs existentes wall-solid-three.spec.ts y room-solid-three.spec.ts cuentan impactos de rayo con |det|, así que tampoco detectan el giro.
- **Verificación independiente:** Confirmado. face-orientation-fix.spec.ts:11-13 sólo importa clearSolidCache, solid3dMesh y spec-support. En las líneas 77-86 el spec permuta posiciones y normales por su cuenta y en 92-97 invierte los índices con su propia copia ('const corrected = [...mesh.indices]'). Las aserciones 2 y 3 (88 y 99) operan sobre esas copias, y en el fichero no hay ninguna llamada a buildCadSolidGeometry, buildCadWallSolidGeometry ni buildCadArchitecturalSlabGeometry. La primera aserción mide la malla del B-rep antes de la permutación, que ya era concordante antes del arreglo. Si se borra la inversión de índices en solid3d-three.ts:179-185, wall-solid-three.ts:148-154 o room-solid-three.ts:63-69, este spec sigue verde. Los demás specs tampoco lo detectarían. solid3d-view.spec.ts:69-84 sólo comprueba cajas envolventes. wall-solid-three.spec.ts:44 y room-solid-three.spec.ts:44 descartan con Math.abs(det), así que el rayo es de doble cara y no ve el giro. Ningún otro spec lee getIndex() de estos constructores. El criterio de T12 (NOCHE_MIMO_20260914.md, sección T12) pide cross(b−a,c−a)·normal(a) > 0 'para cada triángulo de la malla de escena', y eso no está demostrado. Matiz: el arreglo en sí es correcto también para el muro. El marco u=(dx,dy)/L, n=(−dy,dx)/L (wall-openings.ts:71-78) es una rotación de determinante +1, y compuesto con la permutación da −1. El defecto está en la cobertura, no en el código.
- **Arreglo:** Reescribe face-orientation-fix.spec.ts para que mida la geometría THREE que devuelven los constructores reales. (a) buildCadSolidGeometry(extrudedBox, viewport). (b) buildCadWallSolidGeometry con un muro en diagonal (de (1000,1000,0) a (4000,5000,0)) y una puerta. (c) buildCadArchitecturalSlabGeometry(anillo antihorario, 0, 300, viewport). (d) buildCadSolidGeometry de un SOLID3D con colocación espejo (a=−1). Para cada uno, lee geometry.getAttribute('position'), getAttribute('normal') y geometry.getIndex() y comprueba con check que hay cero triángulos con cross(b−a,c−a)·normal(a) ≤ 0. Elimina los bloques 2 y 3, que permutan e invierten por su cuenta. Verifica que el spec falla si comentas la inversión de índices en cualquiera de los tres módulos.

### geometria-aristas-caras-2 · [media] preferredFeatureEdges sigue metiendo aristas planas que el kernel rechaza: FILLETEDGE sigue fallando tras cualquier booleana

- **Fichero:** `apps/web/src/lib/cad/solid3d-build.ts:471`
- **Problema:** D4 dice que el juego por defecto puede contener aristas que el kernel garantiza que va a rechazar. El filtro sólo excluye diedro > π+1e-9, pero chamferCrossSection también rechaza las aristas planas (diedro ≥ π−1e-9). Toda booleana deja las caras en abanicos de triángulos coplanarios, sin fusionarlas, así que un SOLID3D que pasó por UNION o SUBTRACT tiene aristas de 180° que siguen siendo candidatas. Caso 1: dos rectángulos extruidos lado a lado (x 0..2 y 2..4) y luego UNION. Las costuras verticales en x=2 son verticales, de vértices disjuntos y diedro π; se eligen primero y FILLETEDGE falla con 'La arista es plana', citando una arista que el usuario no designó. Caso 2: un cajeado ciego con SUBTRACT. Al quitar T22 las paredes cóncavas, quedan libres los vértices del fondo y entra su diagonal plana, así que antes fallaba por la cóncava y ahora falla por la plana. Además, las aristas de borde (dihedral null) se conservan, aunque edgeGeometry las rechaza (fillet.ts:71-73).
- **Evidencia:** solid3d-build.ts:471: 'const concave = dihedral !== null && dihedral > Math.PI + 1e-9;'. fillet.ts:103: 'if (dihedralRad >= Math.PI - 1e-9) throw … La arista es plana'. boolean.ts:33-38: 'las caras del resultado son triángulos … una fusión de caras coplanarias … No está hecha'. solid3d-build.ts:389-397 evalúa union y subtract con booleanUnion/booleanDifference sin fusión. fillet.spec.ts:160-170 demuestra que la unión de dos cajas contiguas deja aristas planas y que achaflanarlas se rechaza.
- **Verificación independiente:** Confirmado. solid3d-build.ts:471 sólo excluye 'dihedral > Math.PI + 1e-9', así que una arista plana (diedro π) sigue siendo candidata. El kernel la rechaza: chamferEdges llama a chamferCutter en su bucle (fillet.ts:188), que llama a chamferCrossSection, y ésta lanza 'La arista es plana' si el diedro es ≥ π−1e-9 (fillet.ts:103-104). Las booleanas del árbol se evalúan con booleanUnion/booleanDifference sin fusionar coplanarias (solid3d-build.ts:389-397, boolean.ts:289-295). mergeCoplanarFaces sólo se llama desde solids-edit-branches.ts:395 (SOLIDEDIT Limpiar), y boolean.ts:33-38 reconoce que la fusión no está hecha. fillet.spec.ts:158-170 demuestra que unir dos cajas contiguas deja aristas de 180° y que achaflanarlas se rechaza. Caso 1: las costuras en x=2 son verticales (|dz|/len = 1 > 0.999) y no comparten vértices, así que se ordenan primero y el voraz las elige. Caso 2: con las paredes y el borde del fondo del cajeado filtrados por cóncavos, a las esquinas del fondo sólo llegan aristas planas (la diagonal del fondo y las de las paredes), así que alguna plana acaba elegida. En los dos casos FILLETEDGE termina con 'no produjo un sólido válido' vía finishedSolid (solids-support.ts:161-167). Además, la propia D4 (01-3d-modelado.md:680) se titula 'puede contener una arista que el kernel garantiza que va a rechazar' y proponía filtrar con '< Math.PI', que también excluye las planas; MiMo aplicó un filtro más estrecho. Matices: es un comportamiento anterior a T22, no una regresión. Y la parte de las aristas de borde (diedro null) es marginal, porque una booleana valida el cierre por defecto y un cuerpo cerrado no tiene aristas de borde.
- **Arreglo:** En preferredFeatureEdges (solid3d-build.ts:470-479) sustituye el criterio por uno que sólo admita las aristas que el kernel acepta: 'const dihedral = edgeDihedralAngle(body, index); const rejected = dihedral === null || dihedral >= Math.PI - 1e-9 || dihedral <= 1e-9;' y filtra con !rejected. Usa los mismos umbrales que fillet.ts:103-106 y :213. Actualiza el comentario: se excluyen cóncavas, planas y de borde. Añade al spec del hallazgo 3 el caso de dos cajas contiguas unidas (nodo union de dos box, x 0..100 y 100..200): ninguna arista elegida tiene |diedro − π| < 1e-6, y FILLETEDGE con radio 5 escribe un nodo fillet y el volumen baja.

### geometria-aristas-caras-3 · [baja] T22 no tiene ningún spec: quitar el filtro deja el CI verde

- **Fichero:** `apps/web/src/lib/cad/solid3d-build.ts:479`
- **Problema:** El commit b3f862a6 sólo toca solid3d-build.ts. Ningún spec llama a preferredFeatureEdges, y el único spec de FILLETEDGE por comando usa un cubo, que no tiene aristas cóncavas. El criterio de terminado de T22 (perfil en L extruido: FILLETEDGE elige las verticales convexas, no la cóncava, y funciona) no está demostrado, y una regresión que quite el .filter no la detecta nadie.
- **Evidencia:** git grep preferredFeatureEdges en 445352d8 sólo aparece en solid3d-build.ts y en un comentario de solids-modify.ts:22. solids.spec.ts:310-331 prueba FILLETEDGE sobre rectangle('base',0,0,100,100) extruido, es decir, un cubo.
- **Verificación independiente:** Confirmado. b3f862a6 sólo modifica solid3d-build.ts. En 445352d8, 'git grep preferredFeatureEdges' sólo sale en solid3d-build.ts:452,459,496 y en el comentario de solids-modify.ts:22, en ningún *.spec.ts. Ningún spec construye un nodo con 'edges: []'. El único spec por comando, solids.spec.ts:309-331, extruye rectangle('base',0,0,100,100) a altura 100, es decir, un cubo sin aristas cóncavas. fillet.spec.ts:132-156 prueba la L sólo a nivel de kernel, con índices explícitos pasados a chamferEdges y filletEdges, sin pasar por preferredFeatureEdges ni por FILLETEDGE. Quitar el .filter de solid3d-build.ts:479 no rompe ningún spec, y el criterio de terminado de T22 (FILLETEDGE sobre una L extruida funciona) no tiene prueba. Nota: el criterio dice '4 verticales convexas', pero la L tiene 5 (fillet.spec.ts:153), así que el hallazgo acierta al pedir 5.
- **Arreglo:** Crea apps/web/src/lib/cad/preferred-feature-edges.spec.ts con spec-support. (1) Extruye una L (0,0)-(60,0)-(60,20)-(20,20)-(20,60)-(0,60), altura 30, con solid3dBody. Comprueba que preferredFeatureEdges devuelve 5 aristas, todas verticales, que ninguna tiene edgeDihedralAngle > π y que no comparten vértice. (2) Llama a apply('FILLETEDGE', [select(id), distance(3)]) como en solids.spec.ts y comprueba que escribe un nodo op 'fillet' y que el volumen baja. (3) Añade el caso de dos cajas unidas del hallazgo 2. Confirma que el spec falla si quitas el .filter de solid3d-build.ts:479.

## T23 · STEP/IGES descargable (4a3e18cf, revisado en 445352d8)

**Resumen del revisor:** La tubería está conectada de verdad. EXPORT emite una petición `download` con nombre (export.stp o export.igs), MIME (application/step o application/iges) y el texto STEP/IGES. CadCommandEngineHost la atiende en su propio `applyEffect`, antes de reenviarla al puente, así que llega igual en todos los montajes. El filtro de plot-host sólo compara el tipo exacto de petición y no rompe ninguna otra. Sin DOM no revienta: el try/catch lo convierte en un mensaje de error.

Hay un problema bloqueante. El commit cambió la salida de EXPORT de mensaje a petición al host, pero no tocó `solids.spec.ts`, que en la línea 460 sigue exigiendo un mensaje. `npm run test:specs` (ci.yml:366) queda en rojo y la rama no se mergea. Además, ningún spec prueba la descarga.

Hay tres fallos reales más:
- El object URL se revoca en el mismo turno. El propio repo ya documenta en `downloadCadFile` que eso cancela la descarga en Firefox, y el commit duplica ese helper en vez de usarlo.
- Con varios sólidos, el archivo junta varios STEP (o IGES) completos uno tras otro. El resultado es inválido, y el propio importador del repo sólo lee el primer sólido.
- El archivo declara siempre milímetros aunque `INSUNITS` indique otra unidad. Esto ya existía antes, pero ahora sale como archivo descargable.

Menores: la rama de plot-host devuelve una cadena vacía, y el comentario de cabecera de solids-interop.ts todavía dice que EXPORT devuelve el archivo como mensaje.

**Afirmaciones de tus commits que no se cumplen:**

- La unidad deja el CI en verde (implícito en el flujo de merge automático)
  - Evidencia: solids.spec.ts:460 sigue haciendo `messageOf(run("EXPORT", ...))`, y `messageOf` (línea 96) exige `result.kind === "message"`. Ahora EXPORT devuelve `kind: "host"`, así que la aserción falla con «debía responder con un mensaje, dio host». run-specs.mjs recoge `src/**/*.spec.ts` y ci.yml:366 corre `npm run test:specs`. 4a3e18cf no toca el spec (git log 0a5f5600..445352d8 sobre solids.spec.ts sale vacío).
- Existe un spec que prueba la descarga STEP/IGES
  - Evidencia: `git grep '"download"' 445352d8 -- apps/web/src` sólo encuentra command-engine-host.ts:745, plot-host.ts:267, solids-interop.ts:163 y host-requests.ts:205. Ningún *.spec.ts comprueba el nombre, el MIME, el contenido ni la rama del anfitrión.

### t23-step-iges-descarga-1 · [alta] solids.spec.ts sigue esperando un mensaje de EXPORT y rompe test:specs; nada prueba la descarga

- **Fichero:** `apps/web/src/lib/cad/engine/commands/solids.spec.ts:460`
- **Problema:** El commit cambió la salida de EXPORT de `kind: "message"` a `kind: "host"` sin actualizar el spec que recorre el comando. La aserción de `messageOf` falla, el gate de specs del CI queda en rojo y el script no mergea la rama. Además, ningún spec comprueba el nuevo contrato: nombre, extensión, MIME, contenido no vacío ni la rama del anfitrión.
- **Evidencia:** solids.spec.ts:95-98 (`assert.ok(result && result.kind === "message", ...)`) y :460-462 (`const exported = messageOf(run("EXPORT", [select(solid.id), keyword("STEP")], ...))`). solids-interop.ts:157-166 ahora devuelve `result: { kind: "host", request: { kind: "download", ... } }`. ci.yml:366 `npm run test:specs` → run-specs.mjs, patrón `src/**/*.spec.ts`. `git grep '"download"'` no encuentra ningún spec.
- **Verificación independiente:** Confirmado. En 445352d8, solids.spec.ts:95-98 `messageOf` exige `result.kind === "message"`. La línea 460 sigue haciendo `messageOf(run("EXPORT", [select(solid.id), keyword("STEP")], ...))`, y el diff de 4a3e18cf no toca el spec (git log de esas rutas sólo muestra 4a3e18cf, que no incluye solids.spec.ts). solids-interop.ts:157-166 devuelve ahora `result: { kind: "host", request: { kind: "download", ... } }`, y el helper `run` (spec:64-77) devuelve `step.result` tal cual, así que la aserción de la línea 96 falla. run-specs.mjs usa el patrón `src/**/*.spec.ts` sin lista de exclusión, y ci.yml:366 ejecuta `npm run test:specs`. `git grep '"download"'` en *.spec.ts no da ningún resultado: la rama nueva no tiene prueba.
- **Arreglo:** En apps/web/src/lib/cad/engine/commands/solids.spec.ts, sustituye `messageOf(run("EXPORT", ...))` de la línea 460 por una aserción de que el resultado es `kind === "host"` con `request.kind === "download"`, `filename === "export.stp"`, `mime === "application/step"` y `content` que contiene `ISO-10303-21`. Usa `request.content` como payload del IMPORT de las líneas 462-463 (sin `slice`). Añade el mismo caso con `keyword("IGES")` esperando `export.igs` y `application/iges`, y que el contenido se reimporte con el mismo volumen. Añade también en command-engine-host.spec.ts un caso que ejecute EXPORT y compruebe la rama `download` con el descargador inyectado como stub (ver t23-step-iges-descarga-2).

### t23-step-iges-descarga-2 · [media] Revoca el object URL en el mismo turno (cancela la descarga en Firefox) y duplica downloadCadFile

- **Fichero:** `apps/web/src/components/cad/command-line/command-engine-host.ts:756`
- **Problema:** `URL.revokeObjectURL(url)` corre en el mismo turno que `a.click()`. El helper que el repo ya tenía para esto (`downloadCadFile`, anterior a MiMo) documenta que así la descarga se cancela en Firefox y difiere la revocación con `setTimeout`. MiMo reimplementó la descarga a mano, sin `rel`, sin ocultar el ancla y con el defecto. Además, si `document.createElement` o `click` lanzan después de `createObjectURL`, el URL nunca se revoca.
- **Evidencia:** command-engine-host.ts:748-756: `const url = URL.createObjectURL(blob); ... a.click(); a.remove(); URL.revokeObjectURL(url);`. plot-host.ts:163-166 (ya en 0a5f5600): «Revocar en el mismo turno cancelaría la descarga en Firefox: se deja al siguiente tick» → `setTimeout(() => URL.revokeObjectURL(url), 0)`. use-command-engine.ts:56 ya importa `downloadCadFile` y lo pasa a los anfitriones de DXF, ETRANSMIT y DATAEXTRACTION (líneas 464-466).
- **Verificación independiente:** Confirmado. command-engine-host.ts:746-757 crea Blob y ancla a mano y llama a `URL.revokeObjectURL(url)` justo después de `a.click()`, en el mismo turno. El helper `downloadCadFile` ya existía en plot-host.ts:146-167. Añade `rel="noopener"` y `display:none`, y difiere la revocación con `setTimeout(() => URL.revokeObjectURL(url), 0)`, con un comentario (plot-host.ts:163-164) que dice que revocar en el mismo turno cancela la descarga en Firefox. use-command-engine.ts:56 lo importa y lo inyecta en DXF, ETRANSMIT y DATAEXTRACTION (:465-467). También es cierto que la rama toca `document` sin inyección, cuando el diseño del repo pide inyectar la descarga para probarla en Node (use-command-engine.ts:259-260). Y como intercepta antes de `bridge.host`, un `host` propio de un guion no puede sustituirla (use-command-engine.ts:452). Si `createElement` o `click` lanzan, el catch no revoca el URL.
- **Arreglo:** Quita la creación manual de Blob/ancla de command-engine-host.ts:746-757 y reutiliza `downloadCadFile(filename, new TextEncoder().encode(content), mime)` de plot-host.ts. Hazlo inyectando un `download` opcional en el puente de CadCommandEngineHost, cableado en use-command-engine.ts con `downloadCadFile`. Si el puente no trae `download` (Node/SSR), registra «EXPORT …: este espacio de trabajo no puede descargar archivos.» en lugar de tocar `document`. Así la revocación diferida se conserva y la rama queda probable con un stub.

### t23-step-iges-descarga-3 · [media] Con varios sólidos, el .stp/.igs concatena varios archivos completos y resulta inválido

- **Fichero:** `apps/web/src/lib/cad/engine/commands/solids-interop.ts:153`
- **Problema:** EXPORT acepta varios sólidos designados («Designe los sólidos que exportar») y llama a `exportSolidEntity` una vez por sólido. Cada llamada produce un archivo Part 21 completo (o un IGES completo con sus secciones S/G/D/P/T). Unirlos con "\n" y descargarlos como un solo `export.stp` genera varias estructuras ISO-10303-21 en un archivo, con los ids `#n` repetidos; en IGES quedan varias secciones T y la numeración repetida. Otros programas lo rechazan, y el importador del propio repo sólo lee la primera sección DATA, así que pierde en silencio todos los sólidos salvo el primero.
- **Evidencia:** solids-interop.ts:141-153: `for (const solid of solids) parts.push(exportSolidEntity(solid, ...))` y luego `const content = parts.join("\n")`. step-export.ts:208-219 envuelve cada cuerpo en `ISO-10303-21;` … `END-ISO-10303-21;`. step-import.ts:65 y :82 hacen `source.indexOf("DATA;")` y `indexOf("ENDSEC;", dataStart)`, es decir, sólo el primer bloque. iges.ts:150 escribe una sección T por archivo.
- **Verificación independiente:** Confirmado. EXPORT (solids-interop.ts:136-153) recorre `selectedSolids`, que devuelve todos los SOLID3D designados (solids-support.ts:46-53), llama a `exportSolidEntity` por cada uno y une los resultados con `parts.join("\n")` en un único `export.stp`/`export.igs`. Cada `exportStep` escribe un archivo completo, de `ISO-10303-21;` a `END-ISO-10303-21;` (step-export.ts:208-219), con ids `#n` que empiezan de nuevo. `parseStepFile` sólo lee desde el primer `DATA;` hasta el primer `ENDSEC;` (step-import.ts:65, 82-83), así que los demás sólidos se pierden sin aviso. En IGES, `build` escribe S/G/D/P/T con la numeración propia de cada archivo (iges.ts:137-160). `parseIges` junta todas las líneas D y P: los punteros P se repiten, los parámetros se concatenan en el mismo puntero y las entradas D del segundo archivo quedan sin parámetros (iges.ts:280-312). El archivo resultante no es válido.
- **Arreglo:** En EXPORT (solids-interop.ts), si `solids.length > 1`, no concatenes archivos. Opción A: combina los cuerpos en un único `exportStep`/`exportIges` (exportStep ya escribe varios MANIFOLD_SOLID_BREP en una representación, step-export.ts:202-205). Opción B: emite una petición `download` por sólido con nombre distinto (`export-<id>.stp`). Mientras no exista ninguna de las dos, responde con `solidMessage(state, "EXPORT: designe un solo sólido; varios sólidos en un archivo aún no se admiten.")`. Añade al spec un caso con dos sólidos que reimporte el archivo y compruebe el volumen total.

### t23-step-iges-descarga-4 · [media] El archivo descargado declara milímetros aunque INSUNITS diga otra unidad

- **Fichero:** `apps/web/src/lib/cad/engine/commands/solids-interop.ts:144`
- **Problema:** EXPORT no lee `INSUNITS` ni escala el cuerpo, y los escritores declaran siempre mm. Un dibujo con INSUNITS=1 (pulgadas) o 6 (metros) se descarga con las coordenadas en unidades de dibujo etiquetadas como mm: la pieza abre 25.4 veces (o 1000 veces) más chica en el otro programa. El defecto es anterior al commit, pero T23 es lo que lo convierte en un archivo entregable.
- **Evidencia:** solids-interop.ts:144-147 llama a `exportSolidEntity(solid, { format, timestamp })` sin unidades. step-export.ts:132 escribe fijo `SI_UNIT(.MILLI.,.METRE.)` e iges.ts:85 escribe fijo `hollerith("MM")`. system-variables.ts:109 define `INSUNITS` (1 pulgadas, 2 pies, 4 mm, 5 cm, 6 m), y command-engine.ts:319 ya lo lee de `context.variables` con `cadDrawingUnitFromInsunits`.
- **Verificación independiente:** Confirmado, aunque el defecto es anterior al commit. `CadSolidExportOptions` (solid3d-interop.ts:50-57) no tiene opción de unidad, y EXPORT llama a `exportSolidEntity(solid, { format, timestamp })` (solids-interop.ts:144-147) sin leer `INSUNITS`. solid3d-build.ts no escala por unidad (grep de unit/scale sin resultados). step-export.ts:132 escribe fijo `SI_UNIT(.MILLI.,.METRE.)`, e iges.ts:84-85 escribe el flag de unidades `2` y `hollerith("MM")`. Las coordenadas se guardan en la unidad del dibujo: system-variables.ts:109 define INSUNITS con 1, 2, 4, 5 y 6; dwg-document-bridge.spec.ts:515 dice que el documento «nace en metros»; command-engine.ts:319 usa `cadDrawingUnitFromInsunits` para interpretar lo tecleado. Un dibujo en pulgadas o metros sale etiquetado como mm. No es una mala lectura ni está resuelto en otro sitio.
- **Arreglo:** En EXPORT lee `context.variables?.get("INSUNITS")`, conviértelo con `cadDrawingUnitFromInsunits` y pásalo a `exportSolidEntity` como opción `unit`. En solid3d-interop.ts escala el cuerpo a mm antes de `exportStep`/`exportIges`, o haz que los escritores de lib/brep acepten la unidad y escriban el `SI_UNIT` o el flag IGES que corresponda. Si INSUNITS=0 (sin unidad), trátalo como mm y dilo en el mensaje. Añade al spec un caso con INSUNITS=1 que compruebe que un cubo de 1 unidad reimportado mide 25.4 mm.

### t23-step-iges-descarga-5 · [baja] La rama `download` de plot-host devuelve una cadena vacía en vez de decir qué falta

- **Fichero:** `apps/web/src/components/cad/command-line/plot-host.ts:268`
- **Problema:** Todas las ramas vecinas que no son de trazado (clipboard, chain-command, dxf-export…) devuelven un renglón que explica el fallo de cableado, justo para que la exhaustividad de la unión avise. La rama nueva devuelve "". Si alguna vez llega aquí, CadCommandEngineHost la registra como `info` vacía, porque `"" ?? …` no cae al aviso: una descarga perdida en silencio con apariencia de éxito.
- **Evidencia:** plot-host.ts:267-268: `if (request.kind === "download") return ""; // Lo atiende command-engine-host directamente.` Compárese con :247-250 (`"El portapapeles de geometría lo atiende el anfitrión del motor, no el de trazado."`). command-engine-host.ts:763-767: `answered ?? ...` con nivel `answered === null ? "error" : "info"`.
- **Verificación independiente:** Confirmado, severidad baja. plot-host.ts:267-268 devuelve `""` para `download`. Las ramas vecinas devuelven un renglón que explica el fallo de cableado, y el comentario de plot-host.ts:244-246 dice que «si llegara aquí sería un fallo de cableado, y se dice». plot-host.ts:249-250 hace lo mismo para `chain-command`, otra petición que también atiende el anfitrión del motor. En la cadena de use-command-engine.ts:457-468, `plot.handle(request)` es el último eslabón. Si devolviera `""`, command-engine-host.ts:763-767 calcularía `answered = ""` (el `??` no salta con cadena vacía) y registraría un `info` vacío. Hoy la rama no se alcanza, porque command-engine-host.ts:745 intercepta antes, pero precisamente por eso incumple la convención de las ramas vecinas.
- **Arreglo:** En plot-host.ts:267-268 devuelve un renglón como las ramas vecinas: `return "La descarga de archivos la atiende el anfitrión del motor, no el de trazado.";`.

### t23-step-iges-descarga-6 · [baja] El comentario de cabecera de solids-interop.ts contradice el nuevo comportamiento

- **Fichero:** `apps/web/src/lib/cad/engine/commands/solids-interop.ts:13`
- **Problema:** La documentación del módulo sigue diciendo que EXPORT devuelve el archivo como mensaje y que el botón de descarga está pendiente en el anfitrión. Desde este commit es falso, y quien lea el módulo buscará el archivo en el mensaje. Además, el commit borró el comentario que justificaba la marca de tiempo fija.
- **Evidencia:** solids-interop.ts:13-16: «EXPORT devuelve el archivo COMO MENSAJE. Es la única salida que el contrato del motor permite hoy […] el botón de descarga es un cambio del anfitrión, no del motor». Mientras tanto, las líneas 157-166 devuelven `result: { kind: "host", request: { kind: "download", ... } }`.
- **Verificación independiente:** Confirmado. En 445352d8, solids-interop.ts:13-16 sigue diciendo que «EXPORT devuelve el archivo COMO MENSAJE» y que el botón de descarga es un cambio pendiente del anfitrión. Las líneas 5-8 dicen que el comando no puede «disparar una descarga». Las líneas 157-166 ya devuelven una petición `host`/`download`. El diff de 4a3e18cf borra el comentario de la marca de tiempo fija (antes de `timestamp`, línea 146). La justificación sigue en la cabecera de solid3d-interop.ts («## Marca de tiempo»), así que esa parte pesa menos. Lo sustancial, la documentación del módulo contradiciendo el comportamiento, es real.
- **Arreglo:** Reescribe solids-interop.ts:13-16 así: «EXPORT entrega el archivo con una petición `download` al anfitrión (host-requests.ts); el motor sólo produce el texto y el anfitrión lo descarga». Restaura encima del `timestamp` de la línea 146 el comentario sobre la marca de tiempo fija que se borró.

## Bitácora, estados de tareas y registro de gobernanza (18241c28, b146bce0, 9cda1244, 4c0ede8d, 65ec7a1e, d31e71e9; verificado contra 445352d8 y 0a5f5600)

**Resumen del revisor:** La bitácora no es fiable y no debería llegar a main tal como está. El fallo de fondo es que la tabla «Estado de las tareas» (docs/execution/NOCHE_MIMO_20260914.md:353-365) no usa los números de la cola. A partir de T5, cada fila lleva el número de una tarea y la nota de otra. Leídas por su número, marca como HECHA cuatro funciones que no existen en 445352d8:
- T5: VSCURRENT sobre muros.
- T6: corte vivo. SECTIONPLANE, LIVESECTION, clippingPlanes y localClippingEnabled no aparecen ni una vez.
- T10: VPORTS en el modelo. No hay setScissorTest; VPORTS sólo sale en ribbon.ts.
- T11: transparencia. No hay campo transparency.

T3 «ya estaba» también es falso: el propio código dice que extruir por la normal está pendiente. Lo que ya existía era el rechazo del perfil inclinado, que es T21, y T21 sigue «en cola».

Revisé también el código de lo que se da por hecho y hay cuatro fallos serios:
- T7, alzados verdaderos: el tope polar se restaura en el mismo instante, antes de que OrbitControls haga su update() en el siguiente cuadro, así que la vista vuelve a 87,8°.
- T9, proyección paralela (445352d8): escribe la variable PERSPECTIVE, pero nadie la lee y setProjection nunca se llama.
- T23, STEP descargable: rompe solids.spec.ts:460 y revoca el enlace de descarga en el mismo turno, lo que cancela la descarga en Firefox.
- T14, cuadros con la unidad correcta: el CSV sigue sin recibir la unidad.

Otros problemas de menor peso:
- El spec de T12 es tautológico: no llama a ninguno de los tres constructores.
- Varias tareas «hechas» no cumplen su propio criterio de terminado: faltan los goldens de 3DROTATE, VPOINT FR y EXPORT STEP, y el spec del perfil en L.
- La razón de «T4 bloqueada» cita como pendiente CAD_ACCEPT_EDGE_PICK, que ya existe (command-types.ts:118); en realidad es sólo esfuerzo.

Registro de gobernanza: la entrada pasa todas las validaciones de check-proprietary-governance.mjs. Tiene id único, fecha válida, assistants y externalSources como arreglos, adopción obligatoria en estado proposed con Sergio como adoptante y evidencia, aiCoAuthorTrailers en false (correcto: no hay trailers) y aiClaimsAuthorship en false. Los commits los firma Sergiovalle3121. Pero la entrada nunca se actualizó: derivedAreas sólo nombra la bitácora y la evidencia dice «sin código de producto tocado», cuando la rama modifica unos 20 ficheros de producto. Eso incumple ASSISTED_DEVELOPMENT.md:17-25.

El mapa de «lo que ya supera» apunta a ficheros y goldens que sí existen: 47, 65, 92 con «1 hueco(s) restado(s)» y 2.200, 22, 87, 89, glb-export.spec con GLTFLoader, ISO 286 y NOM-001. Tiene imprecisiones menores: wall-openings.ts tiene 448 líneas y no 427, y no existe `walk-mode`.

**Afirmaciones de tus commits que no se cumplen:**

- El registro de gobernanza cubre los cambios asistidos de la rama
  - Evidencia: El log sólo lo toca 18241c28 (git log 0a5f5600..445352d8 -- assisted-development-log.json). En 445352d8, derivedAreas (línea 3629) sólo lista docs/execution/NOCHE_MIMO_20260914.md, y la evidencia (línea 3638) dice «sin código de producto tocado». Sin embargo, 2f222b8f, 0e684821, c43f0871, c212e3f4, 9d4f8de5, c3027d83, b3f862a6, 4a3e18cf, 1321ade6 y 445352d8 modifican código de producto.
- T3 YA ESTABA: EXTRUDE en la normal del perfil
  - Evidencia: 0a5f5600:solids-create.ts:28-29: «Extruir por la normal del perfil es el arreglo bueno y está pendiente». El mensaje de rechazo dice «extruirlos por su normal está pendiente». Lo que ya existía es el rechazo del perfil inclinado (horizontalProfileFromEntity, solids-create.ts:150), que corresponde a T21, y la tabla deja T21 «EN COLA».
- T4 BLOQUEADA porque requiere edge-ray.ts, solid-edge-ref.ts y CAD_ACCEPT_EDGE_PICK
  - Evidencia: edge-ray.ts y solid-edge-ref.ts no existen, eso es cierto. Pero CAD_ACCEPT_EDGE_PICK = 256 ya está declarado en command-types.ts:118 desde 0a5f5600. No hay ninguna dependencia externa que bloquee: es trabajo de «varios días», el mismo esfuerzo que la cola ya estimaba (línea 113).
- T5 HECHA (en la cola: STYLE visual/VSCURRENT sobre muros y la entidad wall)
  - Evidencia: La fila 358 dice «Cuadros arquitectura con unidad correcta», que es T14. Ningún commit toca wall-solid-three.ts ni room-solid-three.ts para aplicar el estilo visual; el único cambio en ellos es el giro de índices de 1321ade6.
- T6 HECHA (en la cola: corte vivo SECTIONPLANE + LIVESECTION)
  - Evidencia: git grep en 445352d8 -- apps packages: cero apariciones de SECTIONPLANE, LIVESECTION, clippingPlanes y localClippingEnabled. La fila 359 describe en realidad T7 (alzados).
- Alzados verdaderos hechos (unlockPolarAngleForCommand)
  - Evidencia: La función existe y está conectada (Layout3DEditor.tsx:6079), pero el cierre se ejecuta en view-controller.ts:398, justo después de emit() y en el mismo turno. El único controls.update() del editor está en el bucle de render (Layout3DEditor.tsx:7576), que en el siguiente cuadro recorta φ a π/2,05 (camera-policy.ts:151). El commit anuncia camera-policy.spec con 23 aserciones y view-3d.spec con 131, pero no toca ningún spec.
- T9 YA ESTABA: atributos MEP ya declarados en mep-symbols.ts
  - Evidencia: Lo que dice la nota es cierto: 0a5f5600:mep-symbols.ts:170 declara attributes.TAG, que es T20. Pero T9 en la cola es la proyección paralela, que no existía. 445352d8 añade PERSPECTIVE (view-visual.ts:163) sin ningún consumidor: git grep PERSPECTIVE sólo encuentra el manifest, el comando y system-variables, y setProjection (view-controller.ts:201) nunca se llama.
- T10 HECHA (en la cola: VPORTS en el modelo)
  - Evidencia: No hay setScissorTest ni ningún renderizado multivista; VPORTS sólo aparece en ribbon.ts:105 y :182 (espacio papel). La fila 363 describe en realidad T22.
- T11 HECHA (en la cola: transparencia de capa y objeto)
  - Evidencia: No existe ningún campo transparency en capas ni en entidades; sólo un comentario futuro en layer-manager-model.ts:17. No hay grupo 440 en DXF. La fila 364 describe en realidad T23.
- STEP/IGES descargable (Blob + enlace de descarga)
  - Evidencia: 4a3e18cf cambia el resultado de EXPORT a kind 'host'. Pero solids.spec.ts:460 usa messageOf, que exige kind 'message' (líneas 95-98), así que el spec falla. Además command-engine-host.ts:756 revoca la URL en el mismo turno, y el propio repo documenta en plot-host.ts (downloadCadFile) que eso cancela la descarga en Firefox.
- T5/T14: un documento en metros ya da los cuadros correctos, incluida la marca P-090x210
  - Evidencia: Las tablas y la marca reciben la unidad, pero el CSV de DATAEXTRACTION (data-extraction-commands.ts:140) llama a buildCadDataExtractionCsv(schedule, {...}) sin el tercer argumento unit. Además no hay ningún spec en metros: data-extraction.spec.ts:87-115 sólo usa milímetros.
- T12 hecha: orientación de caras corregida, con spec de 3 aserciones
  - Evidencia: La corrección está en los tres constructores, pero la bitácora deja T12 «EN COLA» (línea 365). El spec face-orientation-fix.spec.ts sólo importa solid3dMesh (línea 11) y simula él mismo la permutación y el cambio de índices (líneas 76-101): no ejecuta ninguno de los tres constructores.
- T1 HECHA / BLOQUE 1 COMPLETADO de Modelado 3D
  - Evidencia: El criterio de T1 pide un golden de navegador que teclee 3DROTATE, y 2f222b8f y 0e684821 no añaden ningún fichero e2e. MIRROR3D y 3DALIGN no existen. En el bloque, T3 sigue pendiente y T4 sin hacer. La frase de la línea 411 «pasa de3,5» está truncada.

### bitacora-estados-1 · [alta] La tabla de estados usa números desfasados y da por hechas funciones que no existen

- **Fichero:** `docs/execution/NOCHE_MIMO_20260914.md:358`
- **Problema:** A partir de T5, cada fila lleva el número de una tarea de la cola y la nota de otra. Leída por su número, que es como la leerá el titular, declara HECHA T5 (VSCURRENT sobre muros), T6 (corte vivo SECTIONPLANE/LIVESECTION), T10 (VPORTS en el modelo) y T11 (transparencia de capa y objeto), y ninguna de las cuatro existe. También dice «YA ESTABA» de T8 (VISUALSTYLES, que es parcial) y de T9 (proyección paralela, que no existía). Y deja «EN COLA» T12, T14, T17, T20, T21, T22 y T23, que están hechas o ya existían.
- **Evidencia:** Filas 358-365: T5→«Cuadros arquitectura» (es T14), T6→«Alzados verdaderos» (es T7), T7→VISUALSTYLES (es T8), T8→cadWireNumberLabel (es T17), T9→atributos MEP (es T20), T10→aristas cóncavas (es T22), T11→STEP/IGES (es T23). git grep en 445352d8 -- apps packages: cero apariciones de SECTIONPLANE, LIVESECTION, clippingPlanes, localClippingEnabled y setScissorTest; VPORTS sólo en ribbon.ts:105,182; transparency sólo en el comentario de layer-manager-model.ts:17.
- **Verificación independiente:** Confirmado en 445352d8:docs/execution/NOCHE_MIMO_20260914.md. Las filas 358-364 van numeradas por orden de ejecución: T5 lleva la nota de T14, T6 la de T7, T7 la de T8, T8 la de T17, T9 la de T20, T10 la de T22 y T11 la de T23. En cambio, la línea 365 («T12–T25 EN COLA») usa los números de la cola; la sección «T5/T14» de la línea 403 delata el cambio de numeración. Leída por su número, la tabla da por HECHAS T6, T10 y T11, que no existen: `git grep` no encuentra SECTIONPLANE, LIVESECTION, clippingPlanes, localClippingEnabled ni setScissorTest; VPORTS sólo aparece en ribbon.ts:105,182, y transparency sólo en el comentario de layer-manager-model.ts:17. T8 es parcial según su propio commit, c3027d83 (7 de 10 estilos). Quedan EN COLA tareas hechas (T12 en 1321ade6, T14 en c212e3f4, T22 en b3f862a6, T23 en 4a3e18cf) y otras que ya existían en 0a5f5600: T17 (electrical-wire.ts:174-177), T20 (mep-symbols.ts:170) y T21 (solids-create.ts:131,142-160). Un matiz corrige al revisor: T5 (VSCURRENT sobre muros) sí existía antes de MiMo. Lo prueban, en 0a5f5600, native-mass-hosts.ts:39-50, wall-solid-host.ts:132 y 250 (pasa style al constructor) y native-mass-hosts.spec.ts:101. Por eso T5 debe ir como YA ESTABA y no EN COLA, como propone su arreglo. En cualquier caso, la fila «T5 HECHA» es falsa.
- **Arreglo:** Rehaz la tabla «Estado de las tareas» usando el número de la cola en cada fila: T5 EN COLA, T6 EN COLA, T7 con el estado real de 9d4f8de5, T8 PARCIAL (c3027d83), T9 con el estado real de 445352d8, T10 EN COLA, T11 EN COLA, T12 con el estado de 1321ade6, T14 HECHA (c212e3f4), T17 YA ESTABA (electrical-wire.ts:176), T20 YA ESTABA (mep-symbols.ts:170), T21 YA ESTABA, T22 y T23 con su SHA. En la bitácora de ejecución añade una sección por tarea con el mismo número de la cola y su commit.

### bitacora-t7-alzados · [alta] Alzados verdaderos marcados HECHA: el tope polar se restaura antes de que OrbitControls actúe

- **Fichero:** `apps/web/src/lib/cad/view/view-controller.ts:398`
- **Problema:** applyStandardView desbloquea maxPolarAngle, coloca la cámara y llama al cierre lock?.() en el mismo turno, justo después de emit(). OrbitControls no se actualiza en ese momento, sino en el bucle de render del cuadro siguiente. Para entonces el tope ya está restaurado en π/2,05 y update() vuelve a recortar φ a 87,8°. VPOINT FR sigue sin dar un alzado verdadero y VPOINT Inferior sigue sin funcionar. Como update() recorta en cada cuadro, restaurar el tope mientras la cámara siga en φ=90° la hará saltar siempre.
- **Evidencia:** view-controller.ts:387 desbloquea y :398 restaura tras emit() (:397). El único oyente del editor (Layout3DEditor.tsx:6083-6085) sólo marca batchedViewDirty. El único controls.update() del editor está en el bucle (Layout3DEditor.tsx:7576), y camera-policy.ts:151 fija maxPolarAngle = Math.PI / 2.05. El JSDoc de unlockPolarAngleForCommand (camera-policy.ts:253-255) exige llamar al cierre «después de que OrbitControls haya hecho su update()». 9d4f8de5 no toca ningún spec, aunque su mensaje cita camera-policy.spec con 23 aserciones y view-3d.spec con 131.
- **Verificación independiente:** Confirmado. En view-controller.ts, applyStandardView desbloquea el tope en la línea 387, emite en la 397 y llama a lock?.() en la 398, que restaura maxPolarAngle en el mismo turno. El camino de VPOINT es navigation-host.ts:241, que llama a applyStandardView y después a publish() (122-130); publish() sólo avisa a sus oyentes y no llama a controls.update(). El único oyente del controlador en el editor (Layout3DEditor.tsx:6082-6084) sólo pone batchedViewDirty. El único controls.update() del editor está en el bucle de render (Layout3DEditor.tsx:7576), así que se ejecuta en el cuadro siguiente, con el tope ya restaurado en π/2,05 (camera-policy.ts:151). OrbitControls.update() recorta φ siempre, luego el alzado vuelve a 87,8° y la vista inferior sigue sin funcionar. El propio JSDoc (camera-policy.ts:253-255) pide restaurar después del update(). Además, 9d4f8de5 sólo toca Layout3DEditor.tsx, camera-policy.ts y view-controller.ts, sin ningún spec, aunque su mensaje cita recuentos de aserciones.
- **Arreglo:** No restaures el tope dentro de applyStandardView. Deja maxPolarAngle desbloqueado mientras dure la vista ordenada y restáuralo sólo cuando el usuario empiece a orbitar: en el evento 'start' de OrbitControls, recolocando antes la cámara dentro del rango. Añade un spec con un OrbitControls simulado que recorte φ en update() y compruebe que, tras applyStandardView('front') y dos update(), |position.y − target.y| < 1e-3. Hasta entonces marca T7 como PARCIAL.

### bitacora-t9-proyeccion · [alta] Proyección paralela: el comando PERSPECTIVE no cambia nada y la bitácora dice «ya estaba»

- **Fichero:** `apps/web/src/lib/cad/engine/commands/view-visual.ts:163`
- **Problema:** En 445352d8 PERSPECTIVE sólo publica la variable del mismo nombre y responde «Proyección: Paralela.». Nadie lee esa variable, y setProjection nunca se llama; además sólo copia un campo, no cambia la cámara activa a ortográfica. Es un éxito falso. La bitácora, por su parte, da T9 como «YA ESTABA».
- **Evidencia:** git grep PERSPECTIVE en 445352d8 -- apps packages sólo encuentra command-manifest.ts:332, view-visual.ts:128/135/163 y system-variables.ts:176. setProjection (view-controller.ts:201-205) hace `this.current = {...this.current, projection}; this.emit()` y no tiene llamadores. El caso 'variables' de command-engine-host.ts:779-790 sólo delega en bridge.variables.
- **Verificación independiente:** Confirmado. En 445352d8, PERSPECTIVE sólo aparece en command-manifest.ts:332, view-visual.ts:128/135/163 y system-variables.ts:176: nadie lee la variable. setProjection (view-controller.ts:201-205) no tiene llamadores y sólo copia el campo projection antes de emitir. El getter camera (view-controller.ts:152-153) devuelve la cámara en perspectiva en 3D sin mirar projection, así que el JSDoc de setProjection (193-200), que promete alternar a la ortográfica, es falso. El resultado 'variables' produce el efecto de variables y además el mensaje «Proyección: Paralela.» (command-engine.ts:257-259), y el host (command-engine-host.ts:779-790) sólo delega en bridge.variables: es un éxito falso. En la bitácora, la fila T9 (línea 362) dice «YA ESTABA» con la nota de los atributos MEP.
- **Arreglo:** Conecta PERSPECTIVE: cuando la variable cambie, que el editor (Layout3DEditor) llame a viewController.setProjection. Que setProjection cambie de verdad la cámara con la que se dibuja y se lanzan los rayos a la ortográfica de view-controller.ts, con la altura derivada del FOV y la distancia. Añade el spec del criterio de T9: dos rectas paralelas a cotas distintas con la misma separación en pantalla en paralela. Corrige la fila T9 de la bitácora.

### bitacora-t23-export · [alta] STEP/IGES descargable marcado HECHA: rompe solids.spec.ts y la descarga se cancela en Firefox

- **Fichero:** `apps/web/src/lib/cad/engine/commands/solids.spec.ts:460`
- **Problema:** 4a3e18cf cambia EXPORT de responder con un mensaje a hacer una petición host de descarga. El spec existente que prueba EXPORT seguido de IMPORT por comando exige un mensaje, así que falla y se pierde la ida y vuelta por comando. Además la nueva descarga revoca la URL del Blob en el mismo turno, lo que el propio repo documenta como cancelación de la descarga en Firefox, y duplica downloadCadFile de plot-host.ts.
- **Evidencia:** solids.spec.ts:460: `const exported = messageOf(run("EXPORT", ...))`. messageOf (líneas 95-98) hace assert.ok(result.kind === "message"). solids-interop.ts ahora devuelve `result: { kind: "host", request: { kind: "download", ... } }`. command-engine-host.ts:756 llama a URL.revokeObjectURL(url) justo después de a.click(), y plot-host.ts (downloadCadFile) advierte: «Revocar en el mismo turno cancelaría la descarga en Firefox».
- **Verificación independiente:** Confirmado. 4a3e18cf cambia EXPORT (solids-interop.ts) para que devuelva `result: { kind: "host", request: { kind: "download", ... } }`. En solids.spec.ts, `run` devuelve step.result sin transformarlo (64-79), y la línea 460 aplica messageOf a EXPORT; messageOf (95-98) exige kind === "message". Esa línea está en un bloque de nivel superior que se ejecuta (434-473), y run-specs.mjs recorre src/**/*.spec.ts, así que el spec falla y se pierde la ida y vuelta EXPORT→IMPORT por comando. El diff añade a command-engine-host.ts `a.click(); a.remove(); URL.revokeObjectURL(url);` en el mismo turno, mientras que downloadCadFile (plot-host.ts:146-167) ya existía, revoca en un setTimeout y advierte en las líneas 164-165 que revocar en el mismo turno cancela la descarga en Firefox. Es código duplicado y peor. Tampoco hay golden de EXPORT STEP en apps/web/e2e.
- **Arreglo:** En command-engine-host.ts atiende la petición 'download' con downloadCadFile de plot-host.ts (codifica el texto con TextEncoder) en vez de duplicar el código, o revoca la URL en un setTimeout. Adapta solids.spec.ts:459-462 para leer result.request.content cuando result.kind === 'host' y mantener la ida y vuelta con IMPORT. Añade el golden del criterio de T23: EXPORT STEP, interceptar la descarga y comprobar que la cabecera es ISO-10303-21.

### bitacora-t14-csv · [media] «Un documento en metros ya da los cuadros correctos»: el CSV sigue en milímetros y no hay spec en metros

- **Fichero:** `apps/web/src/lib/cad/engine/commands/data-extraction-commands.ts:140`
- **Problema:** c212e3f4 añade el parámetro unit a buildCadDataExtractionCsv, pero el comando no se lo pasa. En un documento en metros, el CSV, que según el propio comentario es «el único fichero que DATAEXTRACTION entrega», sigue dando las longitudes, áreas y volúmenes de muros y locales mil veces más pequeños. Ningún spec prueba un documento en unidades distintas de mm, así que la afirmación de la bitácora (línea 409) y el criterio de T14 («4×3 en metros → 12,00 m²») no están demostrados.
- **Evidencia:** data-extraction-commands.ts:140-143: `buildCadDataExtractionCsv(schedule, { wires, tags })`, sin tercer argumento. data-extraction.ts:114-118 declara `unit?: string`, y cadMillimetresPerUnit(undefined) vale 1 (architecture-support.ts:27-28). data-extraction.spec.ts:87-115 y bim-schedule.spec.ts no tienen ningún caso con unit 'm'.
- **Verificación independiente:** Confirmado. En data-extraction-commands.ts:140-143, la rama CSV llama a `buildCadDataExtractionCsv(schedule, { wires, tags })` sin el tercer argumento `unit` que añadió c212e3f4 (data-extraction.ts:114-118). Con unit undefined, cadMillimetresPerUnit devuelve 1 (architecture-support.ts:27-28), así que wallRowValues y roomRowValues dividen entre 1000 y 10^6 cantidades que ya están en metros. Pasar context.unit a buildCadBimSchedule (línea 135) sólo corrige openingMark; las tablas insertadas sí reciben unit (commit, líneas 172/192/210), pero el CSV no. c212e3f4 no toca ningún spec: data-extraction.spec.ts no menciona unit, y bim-schedule.spec.ts sólo tiene `unit: "mm"` en la línea 126. Ni la afirmación de la línea 409 ni el criterio de T14 están demostrados.
- **Arreglo:** Pasa context.unit como tercer argumento a buildCadDataExtractionCsv en data-extraction-commands.ts:140. Añade a data-extraction.spec.ts el mismo local de 4×3 con coordenadas en metros y unit 'm', y comprueba que la tabla y el CSV dan «12.00» y que la puerta de 0,9×2,1 da la marca «P-090x210».

### bitacora-t3-falso · [media] T3 «YA ESTABA» es falso: extruir por la normal del perfil sigue pendiente

- **Fichero:** `docs/execution/NOCHE_MIMO_20260914.md:356`
- **Problema:** La cola define T3 como «EXTRUDE en la normal del perfil», con el criterio de las 8 esquinas a 0 o 100 del faldón. La bitácora la da por existente, renombrándola «EXTRUDE rechaza perfiles inclinados» (línea 393). Ese rechazo es T21, que sigue «EN COLA». La extrusión por la normal no existe, y el código lo dice.
- **Evidencia:** 0a5f5600 y 445352d8: solids-create.ts:28-29, «Extruir por la normal del perfil es el arreglo bueno y está pendiente». El mensaje de rechazo, en el mismo fichero, dice «extruirlos por su normal está pendiente». extrudeResult usa frame: planeFrameAt(extracted.elevation), un plano horizontal.
- **Verificación independiente:** Confirmado. La cola (líneas 91-99) define T3 como extruir por la normal, con el criterio de las 8 esquinas a 0 o 100 del faldón. En 445352d8, e igual en 0a5f5600, solids-create.ts:28-29 dice «Extruir por la normal del perfil es el arreglo bueno y está pendiente», y el mensaje de rechazo de la línea 131 repite que «extruirlos por su normal está pendiente». extrudeResult (197-219) sigue usando `frame: planeFrameAt(extracted.elevation)`, un plano horizontal. Lo que ya existía es el rechazo de los perfiles inclinados (horizontalProfilesOf, 142-160; solid3d-profiles.spec.ts:156), que es T21 (líneas 294-301). La bitácora lo usa para dar T3 por «YA ESTABA» (líneas 356 y 393-396) y deja T21 EN COLA (línea 365).
- **Arreglo:** Marca T3 como EN COLA y T21 como YA ESTABA en la tabla, citando horizontalProfilesOf (solids-create.ts:141-160) y solid3d-profiles.spec.ts. Cambia el título de la sección de la línea 393 a «T21 — EXTRUDE rechaza perfiles inclinados».

### bitacora-t12-spec · [media] El spec de orientación de caras (T12) es tautológico: no ejecuta los constructores que corrige

- **Fichero:** `apps/web/src/lib/cad/face-orientation-fix.spec.ts:91`
- **Problema:** El spec toma la malla B-rep, aplica él mismo la permutación (x,y,z)→(x,z,y) y el intercambio de índices, y comprueba que su propia simulación es concordante. No llama a buildCadSolidGeometry, buildCadWallSolidGeometryWithDiagnostics ni buildCadArchitecturalSlabGeometry. Si se borrara la corrección de los tres ficheros, seguiría en verde. Además la bitácora deja T12 «EN COLA».
- **Evidencia:** Sólo importa solid3dMesh y clearSolidCache (línea 11). Las líneas 76-89 permutan posiciones y normales a mano y las 91-101 invierten los índices a mano. Ninguna importación de solid3d-three, wall-solid-three ni room-solid-three.
- **Verificación independiente:** Confirmado. face-orientation-fix.spec.ts sólo importa clearSolidCache y solid3dMesh de solid3d-build (línea 11), más utilidades de spec. Permuta posiciones y normales a mano (76-89) e intercambia los índices a mano (91-100), así que comprueba su propia simulación y nunca llama a buildCadSolidGeometry, buildCadWallSolidGeometryWithDiagnostics ni buildCadArchitecturalSlabGeometry. Si se borraran los tres bloques que añadió 1321ade6, el spec seguiría en verde. La corrección es coherente con la permutación real de solid3d-three.ts:157-168, pero no está probada contra los constructores. La tabla (línea 365) sigue dejando T12 EN COLA.
- **Arreglo:** Reescribe face-orientation-fix.spec.ts para construir la geometría con buildCadSolidGeometry, buildCadWallSolidGeometryWithDiagnostics y buildCadArchitecturalSlabGeometry. Sobre los atributos position, normal e index del BufferGeometry resultante, exige cross(b−a, c−a)·normal(a) > 0 en todos los triángulos, que es el criterio de T12. Actualiza la fila T12 de la bitácora con 1321ade6.

### bitacora-criterios-1 · [media] Tareas marcadas HECHA sin cumplir su propio criterio de terminado

- **Fichero:** `docs/execution/NOCHE_MIMO_20260914.md:354`
- **Problema:** La cola fija un criterio verificable para cada tarea, y la bitácora marca HECHA sin él:
- T1 pide un golden que teclee 3DROTATE, guarde y recalcule; no hay golden, y MIRROR3D y 3DALIGN, que están en el título, no existen.
- T2 pide la esfera partida por la mitad y la opción 3puntos; el spec usa una caja y 3puntos no existe.
- T22 pide el perfil en L con FILLETEDGE; no hay spec.
- T23 pide un golden de EXPORT STEP; no hay golden.
- Los alzados piden el golden de VPOINT FR; no hay golden.
- El «BLOQUE 1 COMPLETADO» de la línea 411 incluye T3 pendiente y T4 sin hacer.
- **Evidencia:** Estadísticas de los commits: 2f222b8f y 0e684821 no tocan apps/web/e2e. c43f0871 sólo añade slice-coordinate-planes.spec.ts. b3f862a6 sólo toca solid3d-build.ts. 4a3e18cf no añade specs ni goldens. 9d4f8de5 no toca specs. En apps/web/e2e/golden no hay ninguno con 3DROTATE, VPOINT FR ni EXPORT STEP.
- **Verificación independiente:** Confirmado. Ningún commit de la rama toca apps/web/e2e, y `git grep` en apps/web/e2e no encuentra 3DROTATE, VPOINT ni ISO-10303. Tampoco hay golden de T1, T23 ni de los alzados. MIRROR3D y 3DALIGN no están en command-manifest.ts, all-commands.ts ni lazy-commands.ts; la línea 379 reconoce que falta MIRROR3D, pero marca T1 como HECHA igualmente. slice-coordinate-planes.spec.ts sólo usa cajas (líneas 13, 47, 76 y 105), y en solids-modify.ts no existe ninguna opción 3puntos, así que no hay esfera ni plano oblicuo (criterio de T2). b3f862a6 sólo toca solid3d-build.ts y ningún spec llama a preferredFeatureEdges, de modo que no hay perfil en L con FILLETEDGE (T22). La línea 411 declara «BLOQUE 1 COMPLETADO» con T3 pendiente (ver solids-create.ts:28-29) y T4 BLOQUEADA.
- **Arreglo:** Cambia a PARCIAL las filas de T1, T2, T7, T22 y T23 hasta que existan sus pruebas, o añade las pruebas de cada criterio: golden de 3DROTATE con recálculo desde lo persistido, spec de la esfera de radio 100 cortada por XY en z=0, spec del perfil en L con FILLETEDGE sobre las 4 verticales convexas, golden de VPOINT FR y golden de EXPORT STEP. Quita «BLOQUE 1 COMPLETADO» de la línea 411.

### bitacora-gobernanza-1 · [media] La entrada del registro no se actualizó con el código de producto de la rama

- **Fichero:** `docs/governance/assisted-development-log.json:3638`
- **Problema:** ASSISTED_DEVELOPMENT.md:17-25 exige que todo cambio material asistido añada o actualice una entrada con sus áreas derivadas. La entrada de la noche sólo declara la bitácora, y la evidencia de adopción dice «sin código de producto tocado». Pero la rama cambia el esquema persistido (cad-entities-v5.ts), comandos, el visor y la exportación. El merge automático adoptaría un diff que el registro no describe. check:governance no valida derivedAreas, así que el CI no lo detecta.
- **Evidencia:** Línea 3629: derivedAreas = ["docs/execution/NOCHE_MIMO_20260914.md"]. Línea 3638: «documentación y planificación sin código de producto tocado». El log sólo lo toca 18241c28, mientras la rama modifica cad-entities-v5.ts, solid3d-build.ts, command-manifest.ts, all-commands.ts, lazy-commands.ts, Layout3DEditor.tsx, command-engine-host.ts, host-requests.ts, data-extraction.ts, bim-schedule.ts y otros.
- **Verificación independiente:** Confirmado. En 445352d8:docs/governance/assisted-development-log.json, la entrada NOCHE-MIMO-SUPERAR-AUTOCAD-20260914 tiene derivedAreas = ["docs/execution/NOCHE_MIMO_20260914.md"] (3629-3631) y su adoption.evidence dice «documentación y planificación sin código de producto tocado» (3638). `git log 0a5f5600..445352d8` sobre el log muestra que sólo lo tocó 18241c28, mientras la rama modifica cad-entities-v5.ts, solid3d-build.ts, command-engine-host.ts, Layout3DEditor.tsx, data-extraction.ts, bim-schedule.ts y otros. ASSISTED_DEVELOPMENT.md:17-25 exige añadir o actualizar la entrada con sus áreas derivadas. `git grep derivedAreas` fuera de docs no da resultados, así que check-proprietary-governance.mjs no valida ese campo. Matiz: la entrada sigue en proposed y aún se puede corregir antes del merge, pero en la punta revisada describe mal el diff.
- **Arreglo:** Actualiza la entrada NOCHE-MIMO-SUPERAR-AUTOCAD-20260914 sin cambiar id ni estado (sigue proposed): lista en derivedAreas cada fichero de producto y de spec tocado por los commits de la rama, y reescribe adoption.evidence con los SHA y los gates realmente ejecutados por tarea. Mantén aiClaimsAuthorship:false y aiCoAuthorTrailers:false.

### bitacora-t4-razon · [baja] La razón de «T4 bloqueada» cita como pendiente algo que ya existe

- **Fichero:** `docs/execution/NOCHE_MIMO_20260914.md:357`
- **Problema:** La tabla (línea 357) y la sección de la línea 401 dicen que T4 requiere crear CAD_ACCEPT_EDGE_PICK, pero la constante ya está declarada. Además no hay ninguna dependencia que bloquee: la razón es sólo el esfuerzo, que la cola ya estimaba en «varios días». Llamarlo «bloqueada» confunde un aplazamiento con un bloqueo.
- **Evidencia:** 0a5f5600 y 445352d8: command-types.ts:118, `export const CAD_ACCEPT_EDGE_PICK = 256;`. Lo que no existe es quien la atienda: edge-ray.ts y solid-edge-ref.ts no están en el árbol. El comentario de solids-edit-branches.ts:58 que dice que no existe está caducado.
- **Verificación independiente:** Confirmado, con severidad baja. `export const CAD_ACCEPT_EDGE_PICK = 256;` ya está en command-types.ts:118, tanto en 0a5f5600 como en 445352d8. Aun así, la bitácora (línea 401) lo enumera entre lo que hay que crear, y la cola (línea 109) lo trata como algo por modelar. Lo que falta de verdad es quien lo atienda: edge-ray.ts y solid-edge-ref.ts no están en el árbol, y `git grep` sólo encuentra la declaración y el comentario caducado de solids-edit-branches.ts:58, que dice que no existe y probablemente causó la confusión. Llamarlo «bloqueada» en lugar de «aplazada» es una cuestión de terminología, pero el dato erróneo sí es real.
- **Arreglo:** Cambia el estado de T4 a «APLAZADA (esfuerzo de varios días)». En la línea 401, sustituye «CAD_ACCEPT_EDGE_PICK» por «atender CAD_ACCEPT_EDGE_PICK (ya declarado en command-types.ts:118) en el anfitrión de designación».

### bitacora-mapa-1 · [baja] El mapa tiene cifras e identificadores que el código no respalda

- **Fichero:** `docs/execution/NOCHE_MIMO_20260914.md:18`
- **Problema:** El mapa promete «Nada que el código no demuestre», pero:
- Cita wall-openings.ts con 427 líneas.
- Da `walk-mode` como prueba del recorrido a pie (línea 27).
- Describe como huecos T17 (número de conductor no rotulado) y T20 (MEP sin attributes), que ya estaban resueltos en 0a5f5600 y siguen en la cola.
- **Evidencia:** `git show 445352d8:apps/web/src/lib/cad/wall-openings.ts | wc -l` da 448, y ningún commit de MiMo toca ese fichero. `walk-mode` no aparece en el árbol, sólo toggleWalk (Layout3DEditor.tsx:2409) y walkMode (editor-keyboard.spec.ts). 0a5f5600:electrical-wire.ts:173-177 ya inserta el rótulo del conductor y 0a5f5600:mep-symbols.ts:170 ya declara attributes.TAG, al contrario de lo que afirman las líneas 252-253 y 285 de la cola.
- **Verificación independiente:** Confirmado, con severidad baja. wall-openings.ts tiene 448 líneas en 0a5f5600 y en 445352d8, y ningún commit de la rama lo toca; la línea 18 dice 427. `walk-mode` no aparece en el código: sólo en la propia bitácora (línea 27) y como propuesta en auditoria-fable/dimensiones/02-3d-visual.md:651. En 0a5f5600, electrical-wire.ts:171-177 ya inserta el rótulo del conductor con cadWireNumberLabel (182-205, entidad de tipo text), y mep-symbols.ts:170 ya declara `attributes: { TAG: ... }`, lo que contradice los huecos de las líneas 253 y 286. Matiz: el rótulo es TEXT con el formato «circuito-número», no el MTEXT «1»/«2» del criterio del golden 93, pero el hueco tal como está escrito («no se imprime», «no declara attributes») es falso.
- **Arreglo:** En la línea 18 pon «wall-openings.ts (448 líneas)». En la línea 27 sustituye `walk-mode` por `toggleWalk` (Layout3DEditor.tsx). Marca T17 y T20 como «YA ESTABA» con esas líneas como prueba y corrige su descripción de «Hueco» en la cola.
