# Segunda revisión de Claude: tus 38 commits de 01:15 a 05:40 (15-sep)

Varios revisores leyeron tus commits en `52898e03` y un verificador escéptico por bloque intentó refutar cada hallazgo. **Sólo aparecen aquí los que sobrevivieron a la verificación.**

Cómo trabajarlos:
- Van antes que cualquier tarea nueva: primero los que rompen el CI (specs o goldens existentes que fallan), luego los de severidad alta.
- Un commit por hallazgo con el prefijo `Revisión:` y el id del hallazgo en el mensaje.
- Cada arreglo lleva un spec que habría fallado con el defecto: que ejecute el comando o la función real, no una copia hecha a mano.
- Anota en la bitácora cada hallazgo cerrado, con su id.
- Si un hallazgo te parece falso después de leer el código, escribe la razón en `docs/execution/PREGUNTAS.md` y sigue con el siguiente.

## Eléctrico, MEP y Plant

**Resumen del revisor:** Revisé los 5 commits sobre el código en 52898e03. Cuatro problemas son de severidad alta:
(1) D5 pierde choques. El bucle nuevo se salta el par cuando la ruta del filtro tiene un id mayor que la otra. PIDROUTE y el trazado MEP generan ids `cad_<marca de tiempo>`, así que la ruta recién trazada siempre tiene el id mayor y nunca se informa su choque contra rutas ya dibujadas.
(2) T16 rompe command-icons.spec: AESYMBOL entra al registro sin icono.
(3) El aviso de metadatos de D2 cuenta también la procedencia DXF (sourceType/sourceLayer). Cualquier DXF importado cae en el preflight, y el golden 27, que espera la descarga al primer clic, se rompe.
(4) AETAG etiqueta mal los símbolos IEC: los contactos quedan como CT (la familia de la toma de corriente) y el guardamotor como M. Además, K, F, Q, X y H no se pueden elegir a mano.

Hay tres de severidad media:
- Los 12 símbolos no dibujan la geometría IEC 60617 y la mayoría tiene el cuerpo sin unir a los bornes.
- D-2 mide bien los arcos con bulge (signo y más de 180° correctos), pero suma la Z por separado: un tramo inclinado da más de lo que mide y una LINE pierde la Z.
- El manifiesto ignora el filtro de exportación.

Hay dos de severidad baja: AESYMBOL no está en el panel Instalaciones de la cinta, y D-1 no tiene spec. D-1 en sí es coherente con AECIRCUIT y cadNextWireNumber, que ya comparaban en mayúsculas.

Ninguno de los 5 commits añade specs.

**Afirmaciones de tus commits que no se cumplen:**

- D-2: la NOM ahora calcula la caída con la longitud verdadera
  - Evidencia: circuit-check.ts:142-146 suma |dz| aparte de la longitud 2D. Un tramo (0,0,0)→(3000,0,4000) da 7000 en vez de 5000; antes se usaba hypot en 3D. En una LINE, cadEntityCurves devuelve un segmento 2D y la Z sólo se suma a las polilíneas, así que una LINE vertical mide 0. El fallback de las líneas 149-162 ya no se alcanza para LINE.
- D2 MEP: la entrada cuenta sólo los metadatos de dominio (circuito, calibre, número de línea, etiqueta)
  - Evidencia: La línea 403 cuenta cualquier context.metadata no vacío. Las entidades importadas de DXF llevan metadata {sourceType, sourceLayer} (dxf-cad-document.ts:70-73, aplicado a arcos en :131). También cuenta metadatos que el exportador sí usa: annotativeHeightMm (dxf-cad-document.ts:555) y la tolerancia que se escribe como XDATA (dxf-export.ts:131).
- D2 MEP: el informe usa el mismo ámbito que la exportación
  - Evidencia: La línea 402 recorre document.entities en lugar de `scoped` (líneas 309-311). Eso contradice el comentario de la propia función, que exige usar el mismo filtro.
- T16: hay 12 símbolos IEC 60617 de esquema eléctrico
  - Evidencia: schematic-symbols.ts tiene 12 entradas, pero la geometría no corresponde a IEC 60617. El contacto NA es un semicírculo entre dos barras verticales (línea 62) y el relevador térmico es casi igual (arco de radio 25 más una raya). En el fusible, la bobina, el piloto, el motor y el guardamotor, el cuerpo flota entre las barras x=0 y x=100 sin ninguna línea que lo una. En el borne, la barra x=0 no toca el círculo (30..70).
- T16: AETAG reconoce las familias K/F/M/Q/X/H
  - Evidencia: electrical-tag.ts:82 revisa CONTACTO antes que las reglas IEC, así que IEC-CONTACTO-NA, -NC y -NA-T reciben CT. La línea 86 revisa MOTOR antes que GUARDAMOTOR, así que IEC-GUARDAMOTOR recibe M. CAD_IE_FAMILIES (device-tags.ts:48-57) no incluye K, F, Q, X ni H, de modo que no se pueden elegir a mano.
- D5: se siguen detectando choques contra rutas que no están en el filtro, y los duplicados se evitan ordenando por entityId
  - Evidencia: clash.ts:710 salta el par si a.entityId > b.entityId aunque b no esté en el filtro, y ese par nunca vuelve a aparecer como exterior. newId genera `cad_${Date.now().toString(36)}_…` (Layout3DEditor.tsx:978-979), así que la ruta nueva de PIDROUTE (plant-route.ts:221, :297) o del trazado MEP (mep-tracing.ts) tiene siempre el id mayor y se pierden todos sus choques contra rutas anteriores. El caso sin filtro sí queda simétrico y sin duplicados.

### electrico-mep-plant-1 · [alta] Los choques entre la ruta filtrada y una ruta no filtrada con id menor se pierden

- **Fichero:** `apps/web/src/lib/cad/plant/clash.ts:710`
- **Problema:** La deduplicación `if (a.entityId > b.entityId) continue;` se aplica también cuando b no está en `mias`. Como b nunca es bucle exterior, ese par no se evalúa jamás. PIDROUTE y el trazado MEP filtran por la ruta recién creada, cuyo id `cad_<timestamp>` es siempre mayor que el de las rutas previas, así que dejan de informar cualquier choque ruta contra ruta con lo que ya estaba dibujado.
- **Evidencia:** clash.ts:704-710 recorre mias×todas y salta si a>b. plant-route.ts:221 crea la ruta con context.newEntityId() y en :297 llama con routeIds:[nueva.id]. mep-tracing.ts hace lo mismo con routeIds:[nueva.id]. newId en Layout3DEditor.tsx:978-979 = `${p}_${Date.now().toString(36)}_…`. Caso concreto: la ruta existente cad_a (6") y una nueva cad_b (4") que se solapan como en el bloque 7 de clash.spec daban un choque-duro antes de 52898e03 y ahora dan 0. clash.spec.ts no prueba choques entre rutas con filtro: el bloque 11 separa las rutas 6000.
- **Verificación independiente:** Confirmado. En 52898e03, clash.ts:705-711 recorre mias×todas y hace `if (a.entityId > b.entityId) continue;` aunque b no esté en el filtro. Como b nunca es bucle exterior, el par (nueva, vieja con id menor) no se evalúa. Antes el código sólo saltaba si ninguna de las dos estaba en routeIds. Quién llama con filtro: plant-route.ts:299 y mep-tracing.ts:187, con `routeIds:[nueva.id]`. En los dos casos el id sale de context.newEntityId() (plant-route.ts ~221; mep-tracing.ts:221 y :253). En el editor ese generador es newId("cad") (Layout3DEditor.tsx:4775), que da `cad_${Date.now().toString(36)}_…` (:978-979). Así, una ruta nueva queda lexicográficamente por encima de las anteriores y pierde todos sus choques contra ellas. El bloque 11 de clash.spec.ts (385-403) no lo detecta: pone las rutas en x=2000 y x=8000, sin solape.
- **Arreglo:** En clash.ts, construye `const filtro = options.routeIds ? new Set(options.routeIds) : null;` y deduplica sólo cuando las dos rutas están en el ámbito: `if (a.entityId === b.entityId) continue; if ((!filtro || filtro.has(b.entityId)) && a.entityId > b.entityId) continue;`. Añade en clash.spec.ts un bloque con dos rutas solapadas "r1" y "r2": con routeIds:["r2"] debe salir exactamente 1 choque con routeId "r2" y againstId "r1"; con routeIds:["r1"], 1 choque; sin filtro, exactamente 1 renglón.

### electrico-mep-plant-2 · [alta] AESYMBOL sin icono rompe command-icons.spec

- **Fichero:** `apps/web/src/components/cad/ribbon/command-icons.spec.ts:23`
- **Problema:** El spec exige que todo comando registrado tenga icono. T16 registró AESYMBOL en el manifiesto, pero no lo añadió a CAD_COMMAND_ICONS, así que el runner de specs del CI falla con «comandos registrados sin icono: AESYMBOL».
- **Evidencia:** CAD_COMMAND_REGISTRY_V2 se construye de CAD_COMMAND_MANIFEST (engine/index.ts:193-199), que incluye AESYMBOL en command-manifest.ts:127. `git grep AESYMBOL 52898e03 -- apps/web/src/components/cad/ribbon/command-icons.ts` no devuelve nada; sólo existen AEWIRE (:377) y AETAG (:381). run-specs.mjs recoge src/**/*.spec.ts.
- **Verificación independiente:** Confirmado. command-icons.spec.ts:22-23 exige que `mudos` esté vacío, y lo calcula sobre CAD_COMMAND_REGISTRY_V2. Ese registro se construye con CAD_COMMAND_MANIFEST.map(descriptorPerezoso) (engine/index.ts:~190-199), y el manifiesto incluye AESYMBOL en command-manifest.ts:127. CAD_COMMAND_ICONS es un literal (command-icons.ts:238-543) sin AESYMBOL: git grep sólo encuentra AEWIRE (:377) y AETAG (:381). El mapa no tiene spreads ni se genera, así que no hay otra vía de cobertura. run-specs.mjs:43 ejecuta src/**/*.spec.ts, que incluye este spec.
- **Arreglo:** Añade `AESYMBOL: <icono lucide ya importado en command-icons.ts, distinto de MEPSYMBOL>` a CAD_COMMAND_ICONS. Confirma después que command-icons.spec.ts y ribbon-icons.spec.ts no reportan mudos ni huérfanos.

### electrico-mep-plant-3 · [alta] El aviso de metadatos cuenta la procedencia DXF y rompe el golden 27

- **Fichero:** `apps/web/src/lib/cad/dxf-export-loss-manifest.ts:403`
- **Problema:** La condición cuenta cualquier context.metadata no vacío. Las entidades importadas de DXF llevan metadata {sourceType, sourceLayer}, así que todo documento con algo importado recibe el aviso «metadatos de dominio (circuito, calibre…)», que es falso. Como export-host abre el preflight y no descarga cuando hay cualquier pérdida, la primera pulsación de «Descargar DXF» ya no descarga.
- **Evidencia:** dxf-cad-document.ts:60-73 (sourceContext pone metadata.sourceType y sourceLayer) y :131. Layout3DEditor.tsx:10009 convierte el ARC importado con cadDxfCurvesToNativeEntities. export-host.ts:682-700 retorna sin descargar si exportLosses.length > 0. El golden e2e/golden/27-cad-dxf-loss-manifest.spec.ts:133-137 importa ese ARC, pulsa Descargar DXF una vez y espera `waitForEvent('download')`: ahora agota el tiempo. Además, annotativeHeightMm (dxf-cad-document.ts:555) y la tolerancia (dxf-export.ts:131) sí se usan al exportar.
- **Verificación independiente:** Confirmado. dxf-export-loss-manifest.ts:402-404 cuenta cualquier context.metadata no vacío. sourceContext en dxf-cad-document.ts pone siempre metadata {sourceType, sourceLayer} a los arcos, elipses y splines importados. En el golden 27 el ARC entra por cadDxfCurvesToNativeEntities (Layout3DEditor.tsx ~10006-10013). El contexto no se pierde después: replaceEditorProjection lo conserva (cad-document-projection.ts) y la transformación del arco lo clona (curve-entity-adapters.ts:258). Antes de D2 ese documento no tenía pérdidas: en esta vía nadie rellena unsupportedEntities, sólo dwg-document-bridge. Ahora sale 1 warning, y export-host.ts:687-701 muestra el preflight y hace return sin descargar en la primera pulsación. El golden (27-cad-dxf-loss-manifest.spec.ts:133-137) pulsa una sola vez y espera waitForEvent('download'). No lo ejecuté, pero la cadena es coherente. Además, annotativeHeightMm sí se exporta (dxf-cad-document.ts ~555-561), igual que la tolerancia (dxf-export.ts:131).
- **Arreglo:** En dxf-export-loss-manifest.ts, cuenta sólo las claves de dominio que el exportador no escribe: las `ie:*` de electrical y las claves de plant/line-numbers.ts, plant/pipe-route.ts y mep-runs.ts. Excluye explícitamente sourceType, sourceLayer, sourceBlock, annotativeHeightMm y la tolerancia. Añade en dxf-export-losses.spec.ts: una polilínea con cadWireMetadata da 1 entrada dxf_export_metadata_not_transported, y un ARC con metadata {sourceType:"ARC", sourceLayer:"0"} da 0 entradas.

### electrico-mep-plant-4 · [alta] AETAG asigna CT a los contactos IEC y M al guardamotor

- **Fichero:** `apps/web/src/lib/cad/engine/commands/electrical-tag.ts:86`
- **Problema:** familyForBlock revisa CONTACTO (línea 82, la toma de corriente MEP) antes que las reglas IEC, así que IEC-CONTACTO-NA, -NC y -NA-T se etiquetan -CT1… También revisa MOTOR (línea 86) antes que GUARDAMOTOR (línea 88), así que IEC-GUARDAMOTOR sale -M1. Esas etiquetas equivocadas se escriben en attributes.TAG, que se persiste y viaja al DXF. Además, CAD_IE_FAMILIES no incluye K, F, Q, X ni H, así que no se puede corregir eligiendo la familia a mano.
- **Evidencia:** electrical-tag.ts:79-92: "IEC-CONTACTO-NA".includes("CONTACTO") devuelve CT, y "IEC-GUARDAMOTOR".includes("MOTOR") devuelve M. device-tags.ts:48-57 sólo tiene M, PB, LT, CT, SW, TB, TR y SN. cadIsElectricalInsert (device-tags.ts:171) acepta la capa IE-ESQ, así que «AETAG Todos» sí los etiqueta, pero con la familia equivocada. Ningún spec lo cubre.
- **Verificación independiente:** Confirmado. En electrical-tag.ts, familyForBlock evalúa includes("CONTACTO") en la línea 82, antes que las reglas IEC. Por eso IEC-CONTACTO-NA, -NC y -NA-T devuelven "CT". También evalúa includes("MOTOR") en la 86, antes que GUARDAMOTOR en la 88, así que IEC-GUARDAMOTOR devuelve "M". Con una familia no nula, finishOne escribe la etiqueta sin preguntar, y finishAll la aplica en lote; en los dos casos va a attributes.TAG. cadIsElectricalInsert (device-tags.ts, `layer.startsWith("IE-")`) acepta los INSERT de AESYMBOL, que van en la capa IE-ESQ (electrical-schematic-symbol.ts, finish). CAD_IE_FAMILIES (device-tags.ts:48-57) sólo tiene M, PB, LT, CT, SW, TB, TR y SN, así que no se puede elegir K, F, Q, X ni H a mano. No hay spec que lo cubra.
- **Arreglo:** En familyForBlock, antes de las reglas MEP, resuelve los bloques IEC con el catálogo: `const iec = cadSchematicSymbolFor(nombre); if (iec) return iec.family;`. Si prefieres no importar el catálogo, compara ids exactos y evalúa GUARDAMOTOR antes que MOTOR. Añade de forma aditiva a CAD_IE_FAMILIES { prefix: "K", label: "Contactor / relevador" }, { prefix: "F", label: "Protección" }, { prefix: "Q", label: "Seccionador" }, { prefix: "X", label: "Borne" } y { prefix: "H", label: "Piloto" }. Añade un spec que compruebe la familia de los 12 ids IEC y que MEP-CONTACTO siga dando CT.

### electrico-mep-plant-5 · [media] Los símbolos no dibujan IEC 60617 y los cuerpos no están unidos a los bornes

- **Fichero:** `apps/web/src/lib/cad/electrical/schematic-symbols.ts:62`
- **Problema:** Los 12 bloques usan dos barras verticales paralelas (x=0 y x=100, y de -50 a 50) como «terminales» y ponen el cuerpo suelto en medio. El contacto NA es un semicírculo (no tiene cuchilla inclinada) y es casi igual al relevador térmico. En el fusible, la bobina, la bobina temporizada, el guardamotor, el piloto y el motor, ninguna entidad une las barras con el rectángulo o el círculo. En el borne, la barra x=0 no toca el círculo. Un conductor que llega al símbolo no tiene un punto de conexión definido, y el esquema no se lee como IEC. No hay spec de T16.
- **Evidencia:** schematic-symbols.ts:59-62: NA = barras más arc((50,0), r30, -90..90). Relevador térmico: arc r25 más una raya vertical. Fusible: rectángulo 35..65 × -15..15 sin conexión con x=0 ni x=100. Piloto y motor: círculos de 30..70 (o 20..80) sin conexión. Borne: barra x=0 y círculo 30..70. `git grep schematic-symbols 52898e03 -- '*.spec.ts'` no devuelve nada.
- **Verificación independiente:** Confirmado leyendo schematic-symbols.ts en 52898e03. Los 12 símbolos usan dos barras verticales en x=0 y x=100, de y=-50 a 50. El contacto NA es sólo un arco r30 de -90 a 90, sin cuchilla inclinada, casi igual al relevador (arco r25 más una raya vertical). Bobina, bobina temporizada, fusible (35..65), guardamotor, piloto (círculo 30..70) y motor no tienen ninguna entidad que una las barras con el cuerpo. En el borne, t2 sí toca el círculo en (70,0), pero la barra x=0 no. Sólo NC y seccionador llevan la línea 0..100. No existe schematic-symbols.spec.ts, aunque NOCHE_MIMO_20260914.md:243 pedía como criterio de terminado un spec que afirmara la geometría.
- **Arreglo:** Redibuja cada símbolo con los dos bornes sobre el mismo eje: una línea de (0,0) al cuerpo y otra del cuerpo a (100,0). Usa la forma normalizada de cada elemento: contacto NA con cuchilla inclinada y hueco; NC con cuchilla que cruza el tope; bobina como rectángulo; fusible como rectángulo atravesado por el conductor; seccionador con cuchilla y tope perpendicular; relevador térmico con su elemento térmico; piloto como círculo con aspa; motor como círculo con M; borne como círculo pequeño sobre el conductor. Crea schematic-symbols.spec.ts que compruebe: 12 ids únicos, que los extremos (0,0) y (100,0) existen y conectan con el cuerpo, que no hay dos símbolos con geometría idéntica salvo escala, y que AESYMBOL emite block define, layer upsert IE-ESQ e insert.

### electrico-mep-plant-6 · [media] La Z sumada aparte sobremide los tramos inclinados y una LINE pierde la Z

- **Fichero:** `apps/web/src/lib/cad/electrical/circuit-check.ts:145`
- **Problema:** Tras D-2, cada tramo mide su longitud 2D más |dz| (suma tipo Manhattan) en lugar de la longitud 3D. Una polilínea (0,0,0)→(3000,0,4000) mide 7000 en vez de 5000 y la caída de tensión sale inflada. Para type "line", cadEntityCurves devuelve un segmento 2D y la Z sólo se suma a las polilíneas, así que una LINE vertical mide 0; antes medía 2500 con hypot. Los arcos de polilíneas cerradas incluyen el tramo de cierre, pero su dz no se suma. D-2 no añadió spec del bulge.
- **Evidencia:** circuit-check.ts:137-148. curve-model.ts:139-146 (una LINE da un segmento 2D). El fallback de circuit-check.ts:149-162 con hypot 3D ya no se alcanza para LINE ni para polilíneas. circuit-check.spec.ts sólo prueba la L plana (7000) y una subida puramente vertical (2500), y ningún bulge.
- **Verificación independiente:** Confirmado. cadEntityCurves devuelve para type "line" un segmento 2D (curve-model.ts:139-146), y curveLength de un segmento usa distanceBetween en 2D (curve-model.ts:340). En circuit-check.ts:137-148 la Z sólo se suma para polilíneas, y como |dz| aparte: (0,0,0)→(3000,0,4000) da 3000+4000=7000 en lugar de 5000. Una LINE vertical da 0. El fallback con hypot 3D (149-162) ya no se alcanza ni para line ni para polyline, así que es una regresión respecto al código anterior. polylineCurves incluye el tramo de cierre, pero el bucle de dz no lo incluye. El spec sólo prueba la L plana (circuit-check.spec.ts:189) y una polilínea puramente vertical (196-199), sin bulge ni LINE.
- **Arreglo:** En cadEntityRunLength, calcula por curva: `const dz = z[i+1]-z[i]` (con el tramo de cierre si closed), sumando `Math.hypot(curveLength(curve), dz)`. Para type "line", usa hypot 3D de start a end antes de llamar a cadEntityCurves. Borra el comentario del fallback que ya no aplica. Añade a circuit-check.spec.ts: bulge 1 sobre cuerda 2000 da π·1000; bulge -1 da lo mismo; bulge tan(270°/4) sobre cuerda c da (3π/2)·R; el segmento (0,0,0)→(3000,0,4000) da 5000; una LINE de z0 a z2500 da 2500.

### electrico-mep-plant-7 · [media] El aviso de metadatos ignora el filtro de exportación

- **Fichero:** `apps/web/src/lib/cad/dxf-export-loss-manifest.ts:402`
- **Problema:** El recuento recorre document.entities en lugar de `scoped`. Al exportar una selección, sólo el espacio modelo o con capas ocultas excluidas, el aviso cuenta entidades que no van al fichero y abre el preflight igualmente, justo lo que el comentario de la función (líneas 306-311) dice que no debe pasar.
- **Evidencia:** Las líneas 309-311 definen `scoped` con el filtro. La línea 402 usa `for (const entity of document.entities)`. export-host.ts:682-684 pasa dxfExportEntityFilter y dxf-document-export.ts:142 pasa modelSpaceOnly.
- **Verificación independiente:** Confirmado. dxf-export-loss-manifest.ts:309 construye `scoped` con el filtro, y el comentario de 306-308 dice que el informe debe usar el mismo filtro que la exportación. Sin embargo, la sección nueva de la línea 402 recorre `document.entities`. export-host.ts:682-685 pasa dxfExportEntityFilter (selección y capas ocultas), así que las entidades fuera del ámbito cuentan igual y el preflight se abre.
- **Arreglo:** Cambia la línea 402 a `for (const entity of scoped)`. Añade en dxf-export-losses.spec.ts un documento con una entidad con metadatos ie:* y otra sin ellos, llamando con un filtro que excluya la primera: debe dar 0 entradas dxf_export_metadata_not_transported.

### electrico-mep-plant-8 · [baja] AESYMBOL no está en el panel Instalaciones de la cinta

- **Fichero:** `apps/web/src/lib/cad/ribbon.ts:147`
- **Problema:** El patrón del panel Instalaciones agrupa MEPSYMBOL, AEWIRE, AECIRCUIT, AECHECK y AETAG, pero no AESYMBOL. Por su kind "draw", el comando cae en el panel de reposo Inicio › Dibujo, lejos del resto de órdenes eléctricas.
- **Evidencia:** ribbon.ts:147 `/^(PIPE|DUCT|CABLETRAY|MEPSYMBOL|AEWIRE|AEWIRELIST|AECIRCUIT|AECHECK|AETAG|AETAGLIST|PIDLINE|…)$/`. ribbon.ts:75 `draw: "inicio"`. CAD_RIBBON_FALLBACK_PANEL.inicio = "Dibujo".
- **Verificación independiente:** Confirmado, y es más grave de lo que dice el hallazgo. El patrón de ribbon.ts:147 no incluye AESYMBOL, y AESYMBOL tampoco coincide con el patrón de «Dibujo». Por su kind "draw" cae en la pestaña inicio, en el panel de reposo «Dibujo» (ribbon.ts:195). buildRibbonTabs lo añade entonces a RIBBON_PANEL_FALLBACKS (ribbon.ts:260), y ribbon.spec.ts:59 exige `assert.deepEqual(cadRibbonPanelFallbacks(), [])`. Así que no sólo queda mal colocado: también rompe ribbon.spec en CI. La severidad debería ser alta.
- **Arreglo:** Añade AESYMBOL al patrón de la línea 147 junto a MEPSYMBOL y comprueba que ribbon.spec.ts y ribbon-golden.spec.ts siguen verdes.

### electrico-mep-plant-9 · [baja] D-1 sin spec y AEWIRELIST sigue distinguiendo mayúsculas

- **Fichero:** `apps/web/src/lib/cad/electrical/circuit-check.ts:225`
- **Problema:** La fusión de C-1 con c-1 no está probada: ningún caso de circuit-check.spec.ts usa grafías distintas. Además, AEWIRELIST agrupa por wire.circuit sin normalizar, así que muestra c-1 y C-1 como dos circuitos mientras AECHECK los presenta como uno.
- **Evidencia:** circuit-check.spec.ts sólo usa "C-1"…"C-8" en mayúsculas. electrical-wire.ts:375-377 hace `porCircuito.get(wire.circuit)` y `porCircuito.set(wire.circuit, lista)`.
- **Verificación independiente:** Confirmado. circuit-check.spec.ts sólo usa circuitos en mayúsculas (C-1…C-12), así que la fusión de D-1 no está probada. cadWiresOf (wire-numbering.ts:91-98) sólo recorta con readString, sin pasar a mayúsculas. AEWIRELIST agrupa por wire.circuit tal cual (electrical-wire.ts:375-377), mientras que cadCheckCircuits ahora usa trim().toUpperCase(). Por eso AEWIRELIST lista «c-1» y «C-1» por separado y AECHECK los junta. El propio módulo ya compara sin mayúsculas en el siguiente número (wire-numbering.ts:118-121) y en los repetidos (:138), así que AEWIRELIST es el que queda incoherente. Severidad baja.
- **Arreglo:** Añade a circuit-check.spec.ts dos tramos de 10 m con circuit "C-1" y "c-1": debe salir 1 fila con circuit "C-1", wires 2 y lengthM 20. Añade también que "C-1" y "C-10" dan 2 filas. En electrical-wire.ts:375-377, agrupa con `wire.circuit.trim().toUpperCase()`.

## Mecánico y estructura metálica (91a673d5, b6909fbf, 976615e9, df7b62e0, 4bd7f9dd, 69704b9d; punta revisada 52898e03)

**Resumen del revisor:** No debe llegar a main así. En T18, CENTERMARK dibuja la cruz, pero ninguno de los dos comandos está completo:
- **Registro:** CENTERMARK y CENTERLINE no tienen resumen en command-summaries.ts. `cadCommandSummary` lanza un error y ribbon.ts la llama para cada comando al cargarse, así que la cinta revienta. Tampoco tienen patrón de panel en la cinta y sus alias no están en alias-table, que es lo que consulta la línea de comandos.
- **Capa y tipo de línea:** el tipo de línea CENTER nunca se aplica (la constante está sin usar). La capa CENTER no se da de alta ni cae a la capa activa como dice la cabecera.
- **CENTERLINE:** entre dos rectas paralelas une sus puntos medios y traza una perpendicular, no el eje.
- **Arreglo para el gate (b6909fbf):** además de devolver el mensaje, que era lo único que la sonda necesitaba, eliminó el sobresaliente configurable. Quedó fijo en 3 unidades de dibujo y hay código muerto.
- **Asociatividad:** no existe; la marca en metadata no la usa nadie, y CENTERLINE sólo guarda el primer objeto.
- No hay spec ni golden, y aun así la bitácora dice HECHA.

En T19 hay tres problemas:
- **Peso mal etiquetado:** la tabla imprime «Peso unit. (kg)», pero el valor es kg/m, y «Peso total» multiplica ese valor por el número de inserciones. La longitud que ahora pide STEELSHAPE sólo aparece en el mensaje; no se guarda.
- **Unidades de la longitud:** el aviso de STEELSHAPE toma el número como mm aunque la línea de comandos lo entrega en unidades de dibujo.
- **Specs y golden rotos:** pasar de 5 a 7 columnas rompe engine/commands/mechanical.spec.ts y el golden 84. El paso nuevo de longitud rompe los recorridos de STEELSHAPE de ese mismo spec.
- **Spec de lib:** mechanical.spec.ts sólo ajusta aserciones existentes, y hasta quitó la comprobación de description. Ninguna aserción prueba un área o un peso reales.

Lo que sí es correcto:
- El área se guarda como tercer campo de description y un bloque viejo de dos campos se sigue leyendo sin error.
- La evidencia de command-integrity cuadra: 299 y 300 comandos coinciden con el manifiesto, y los veredictos suman 299 y 300.

**Afirmaciones de tus commits que no se cumplen:**

- 91a673d5: CENTERLINE traza el eje entre dos objetos (según la cabecera, entre dos rectas paralelas o dos círculos)
  - Evidencia: center-marks.ts:215-223 toma el punto medio de cada línea y 233-245 los une. Con dos rectas paralelas horizontales y=0 e y=10 sale una vertical de (50,-3) a (50,13), perpendicular a ellas, no el eje y=5. Entre dos círculos sí une los centros.
- 91a673d5: las líneas van en la capa CENTER (o en la activa si CENTER no existe)
  - Evidencia: center-marks.ts:29, 153 y 268 fijan layer "CENTER" siempre. No hay comando {type:"layer",op:"upsert"} como en electrical-schematic-symbol.ts:53-56 ni recurso a context.activeLayer. Si el dibujo no tiene la capa CENTER, las entidades apuntan a una capa inexistente.
- 91a673d5: las líneas usan el tipo de línea CENTER
  - Evidencia: CENTER_LINESTYLE (center-marks.ts:30) no se usa en ningún sitio. Las entidades de 103-125 y 234-244 sólo llevan context.metadata, sin context.presentation.linetype.
- 976615e9: T18 queda HECHA en la bitácora
  - Evidencia: El criterio de NOCHE_MIMO_20260914.md:265 pide las dos líneas en CENTER y el sobresaliente medido, y la auditoría pide center-marks.spec.ts y un golden. No hay spec ni golden, no hay tipo de línea CENTER y el sobresaliente es fijo.
- 4bd7f9dd: BOM_HEADERS gana «Peso unit. (kg)» y «Peso total (kg)» calculados con cadSteelKgPerMetre
  - Evidencia: Las columnas existen (mechanical-bom.ts:86), pero mechanical-bom.ts:125 escribe cadSteelKgPerMetre(area), que son kg/m, bajo «Peso unit. (kg)», y 126 multiplica kg/m por el número de inserciones. No hay longitud en ningún sitio: el criterio «14 m de PTR → 63,0 kg» es imposible.
- 4bd7f9dd: mechanical.spec con 133 aserciones respalda el BOM con peso
  - Evidencia: El diff sólo modifica aserciones existentes (no añade ningún eq/ok) y quita block.description de la línea 194. Todas las filas probadas salen con «—» (tornillo y tuerca sin área; la tabla de la línea 336 se construye sin blocks). Además deja rotos engine/commands/mechanical.spec.ts y el golden 84.
- bb387103/8a388571: T19 queda HECHA («STEELSHAPE longitud + BOM con peso»)
  - Evidencia: NOCHE_MIMO_20260914.md:378. El criterio (NOCHE_MIMO:276) pide 63,0 kg para 14 m de PTR y que el golden 84 afirme la columna de peso. Ninguna de las dos cosas existe.

### mecanico-estructura-1 · [alta] CENTERMARK y CENTERLINE sin resumen: la cinta lanza al cargarse y command-summaries.spec falla

- **Fichero:** `apps/web/src/lib/cad/engine/command-summaries.ts:342`
- **Problema:** Los dos comandos nuevos no tienen entrada en CAD_COMMAND_SUMMARIES y la lista de exclusiones está vacía (línea 332). cadCommandSummary lanza un error para cualquier comando sin resumen. ribbon.ts calcula CAD_RIBBON_DATA al cargarse y llama a cadCommandSummary para cada descriptor de CAD_COMMAND_DESCRIPTORS, que se genera a partir del manifiesto.
- **Evidencia:** command-summaries.ts:339-347 lanza «El comando … no tiene resumen». ribbon.ts:266 hace summary: cadCommandSummary(descriptor.name) dentro de buildRibbonTabs, y ribbon.ts:292 ejecuta buildRibbonTabs() al importar. engine/index.ts:189-190 genera CAD_COMMAND_DESCRIPTORS con CAD_COMMAND_MANIFEST.map, que incluye CENTERMARK y CENTERLINE (command-manifest.ts:84-85). CadRibbon.tsx:6 importa CAD_RIBBON_DATA. command-summaries.spec.ts:24-31 exige que todo comando tenga resumen. git grep -w CENTERMARK en command-summaries.ts no da resultados. (PERSPECTIVE y AESYMBOL, fuera de este bloque, tienen el mismo hueco.)
- **Verificación independiente:** Confirmado en 52898e03. command-summaries.ts:24-325 no tiene entrada para CENTERMARK ni para CENTERLINE: `git grep -w` sólo los encuentra en command-manifest.ts:84-85 y en center-marks.ts. La lista de exclusiones está vacía (línea 332) y cadCommandSummary lanza un error (líneas 339-347). engine/index.ts:189-190 genera CAD_COMMAND_DESCRIPTORS desde el manifiesto. ribbon.ts:266 llama a cadCommandSummary(descriptor.name) dentro de buildRibbonTabs, y ribbon.ts:307 ejecuta `CAD_RIBBON_DATA = buildRibbonTabs()` al cargar el módulo, sin try/catch. CadRibbon.tsx:6 importa CAD_RIBBON_DATA, así que importar la cinta lanza. command-summaries.spec.ts:24-31 recorre CAD_COMMAND_REGISTRY_V2.all() y exige resumen para todos. También confirmo que AESYMBOL no tiene resumen.
- **Arreglo:** Añade a CAD_COMMAND_SUMMARIES en apps/web/src/lib/cad/engine/command-summaries.ts, junto a BALLOON (línea 292), una línea en español de entre 10 y 110 caracteres para CENTERMARK y otra para CENTERLINE. Comprueba que command-summaries.spec.ts y ribbon.spec.ts pasan.

### mecanico-estructura-2 · [alta] BOM de 7 columnas rompe engine/commands/mechanical.spec.ts y el golden 84

- **Fichero:** `apps/web/src/lib/cad/engine/commands/mechanical.spec.ts:230`
- **Problema:** 4bd7f9dd cambia BOM_HEADERS de 5 a 7 columnas y sólo actualiza src/lib/cad/mechanical.spec.ts. El spec del motor, que recoge run-specs.mjs (patrón src/**/*.spec.ts), y el golden e2e 84 siguen esperando filas de 5 celdas.
- **Evidencia:** engine/commands/mechanical.spec.ts:230-231 esperan filas de 5 celdas de la tabla BOM. La línea 287 espera [3, 5, 5000] (5 columnas), la 288 una fila de 5 y las 316-317 celdas de 5. e2e/golden/84-cad-plano-de-fabricacion.spec.ts:180 espera ["Pos.","Cant.","Denominación","Norma","Bloque"] y la 181 una fila de 5. Hoy mechanical-bom.ts:131-139 produce 7 celdas por fila. git log de esos dos ficheros en 91a673d5^..52898e03 no muestra ningún commit.
- **Verificación independiente:** Confirmado. En 4bd7f9dd, mechanical-bom.ts:86 pasa a 7 cabeceras y la tabla genera 7 celdas por fila (líneas 127-135). engine/commands/mechanical.spec.ts sigue esperando 5 celdas: fila(2) y fila(3) en 230-231, `[3, 5, 5000]` en 287, la fila de 288 y las celdas de 316-317. También falla la 324, que exige conservar columnWidths de 5 elementos. Esas widths se descartan porque mechanical-annotate.ts:207 compara 5 con 7. e2e/golden/84-cad-plano-de-fabricacion.spec.ts:180-181 sigue con 5 columnas. run-specs.mjs:43 recoge src/**/*.spec.ts. `git log 91a673d5^..52898e03` sobre esos dos ficheros no da ningún commit. Sólo se tocó src/lib/cad/mechanical.spec.ts.
- **Arreglo:** Actualiza engine/commands/mechanical.spec.ts (líneas 230-231, 287-288, 316-317) y e2e/golden/84-cad-plano-de-fabricacion.spec.ts:180-181 a las 7 columnas. Añade en el golden 84 la aserción de la columna de peso que exige el criterio de T19.

### mecanico-estructura-3 · [alta] El paso nuevo de longitud de STEELSHAPE rompe los recorridos del spec del motor

- **Fichero:** `apps/web/src/lib/cad/engine/commands/mechanical.spec.ts:239`
- **Problema:** df7b62e0 añade el prompt «Longitud del tramo» entre el punto y el ángulo. Los recorridos existentes del spec terminan con [point, enter]: el Intro se consume como longitud 0 y el comando se queda esperando el ángulo sin producir resultado.
- **Evidencia:** engine/commands/mechanical-parts.ts:303-304 y 338-345: con state.length === null, Intro fija length 0 y vuelve a preguntar. En el spec, la línea 237 recorre [enter,enter,enter,enter,point(0,0),enter] y la 239 written() exige result.kind === "document". La 242 (IPR, …point(10,10),enter) y la 245 (messageOf con «no deja hueco») tampoco llegan a rematar. drive() (spec:101-110) sólo avanza con las entradas que recibe.
- **Verificación independiente:** Confirmado. PTR tiene 3 parámetros e IPR tiene 4 (lib mechanical-parts.ts:331-335 y 374-379). En engine mechanical-parts.ts:303-304 y 338-345, con length === null un Intro fija length 0 y vuelve a askShape, que ahora pregunta el ángulo sin dar resultado. Recorridos del spec: la línea 237 es shape, b, h, t, punto e Intro, y el Intro se consume como longitud. La 242 es Ipr, d, bf, tw, tf, punto e Intro, igual. La 245 da t=30, punto e Intro, pero cadMechanicalSteelShape, que produce «no deja hueco», sólo se llama tras el ángulo (línea 348). drive() (spec:100-110) termina sin result, así que written() en la 239/242 y messageOf() en la 245 fallan.
- **Arreglo:** Añade un Intro más (o distance(n) para probar la longitud) en los recorridos de STEELSHAPE de engine/commands/mechanical.spec.ts:237, 242 y 245. Añade un caso que afirme el aviso con longitud: PTR 50,8×50,8×3 con 14000 mm → «63.04 kg».

### mecanico-estructura-4 · [alta] «Peso unit. (kg)» muestra kg/m y «Peso total (kg)» multiplica kg/m por inserciones

- **Fichero:** `apps/web/src/lib/cad/mechanical-bom.ts:125`
- **Problema:** La tabla que se imprime en el plano rotula en kg un valor en kg/m. La longitud del tramo que pide STEELSHAPE no se guarda en ningún sitio (ni context.metadata del INSERT ni el bloque), así que el BOM no puede calcular kilos. «Peso total» = kg/m × número de inserciones no tiene unidad física. La bitácora marca T19 como HECHA.
- **Evidencia:** mechanical-bom.ts:125 hace weightPerUnit = cadSteelKgPerMetre(areaMm2), y cadSteelKgPerMetre devuelve kg/m (mechanical-parts.ts:392-395). La línea 126 multiplica por row.count, y la 86 rotula «Peso unit. (kg)» y «Peso total (kg)». engine/commands/mechanical-parts.ts:351-354 sólo usa state.length para el texto del aviso, y cadMechanicalInsertCommands (líneas 71-89) no recibe la longitud. Con 14 m de PTR 50,8×50,8×3 la tabla dice 4.50 / 4.50 en lugar de 63,0 kg. NOCHE_MIMO_20260914.md:378 marca T19 HECHA.
- **Verificación independiente:** Confirmado. mechanical-bom.ts:125 hace weightPerUnit = cadSteelKgPerMetre(areaMm2), que devuelve kg/m (lib mechanical-parts.ts:392-395). La línea 126 lo multiplica por row.count y la 86 rotula «Peso unit. (kg)» y «Peso total (kg)». La longitud sólo se usa en el texto del aviso (engine mechanical-parts.ts:351-354). cadMechanicalInsertCommands (líneas 71-88) no la recibe ni la guarda en el INSERT ni en el bloque, así que el BOM no puede dar kilos. El criterio de T19 en NOCHE_MIMO:276 (14 m de PTR → 63,0 kg; golden 84 con columna de peso) no se cumple, y aun así la bitácora lo marca HECHA en la 378.
- **Arreglo:** Persiste la longitud del tramo en context.metadata.mechLength (mm) del INSERT que crea STEELSHAPE. En buildCadMechanicalBom suma por bloque la longitud total de sus INSERT. En buildCadMechanicalBomTable pon una columna «Peso lineal (kg/m)» con cadSteelKgPerMetre(area), otra «Longitud (m)» y «Peso total (kg)» = kg/m × metros sumados, con «—» si no hay área o longitud. Añade en mechanical.spec.ts el caso de 14 m de PTR → 63,0 kg y un bloque viejo sin área → «—». Deja T19 como PARCIAL en la bitácora hasta entonces.

### mecanico-estructura-5 · [alta] CENTERLINE entre dos rectas paralelas traza una perpendicular, no el eje

- **Fichero:** `apps/web/src/lib/cad/engine/commands/center-marks.ts:216`
- **Problema:** Para líneas, el comando toma el punto medio de cada una y los une. Con dos rectas paralelas, que es el caso que anuncia la cabecera, el resultado es un segmento perpendicular a ellas entre sus puntos medios, en lugar del eje equidistante y paralelo a ambas.
- **Evidencia:** center-marks.ts:215-216 y 222-223 calculan ax/ay y bx/by como puntos medios, y 233-245 trazan la línea de A a B prolongada 3. Con línea 1 de (0,0) a (100,0) y línea 2 de (0,10) a (100,10) sale (50,-3)→(50,13) en lugar de (−3,5)→(103,5). La cabecera (líneas 6-7) promete «el eje entre dos rectas paralelas o entre dos círculos».
- **Verificación independiente:** Confirmado. center-marks.ts:215-216 y 222-223 toman el punto medio de cada línea, y 227-240 trazan de A a B prolongando el sobresaliente. Con (0,0)-(100,0) y (0,10)-(100,10) los puntos medios son (50,0) y (50,10): sale (50,-3)→(50,13), perpendicular a ambas. La cabecera (líneas 6-7) y la auditoría 08:298-299 prometen «el eje entre dos rectas paralelas». No se comprueba el paralelismo.
- **Arreglo:** En cadCenterlineEntities, cuando las dos entidades sean líneas, comprueba que sean paralelas (producto vectorial de las direcciones ≈ 0) o recházalo con un mensaje. Proyecta los cuatro extremos sobre la dirección común, toma la recta equidistante (media de las dos rectas) y trázala desde la proyección mínima hasta la máxima, más el sobresaliente. Deja la unión de centros sólo para círculos y arcos. Añade center-marks.spec.ts con el caso de las dos paralelas y el de dos círculos.

### mecanico-estructura-6 · [alta] Sin tipo de línea CENTER y con referencia a una capa inexistente

- **Fichero:** `apps/web/src/lib/cad/engine/commands/center-marks.ts:30`
- **Problema:** El commit dice «en capa CENTER con tipo de línea CENTER». CENTER_LINESTYLE no se usa y las líneas salen con el tipo de línea de su capa. Además la capa CENTER nunca se da de alta, y tampoco se usa la capa activa como dice la cabecera. Las entidades quedan apuntando a una capa que el documento no tiene, contra la garantía de la casa.
- **Evidencia:** center-marks.ts:30 declara CENTER_LINESTYLE sin uso (git grep no da ningún otro uso). Las entidades de 103-125 y 234-244 llevan sólo context.metadata, sin context.presentation.linetype (cad-document.ts:101-104). Las líneas 153 y 268 pasan CENTER_LAYER fijo. Compárese con electrical-schematic-symbol.ts:53-56, que añade {type:"layer",op:"upsert"} si falta. express-tools.ts:500 enuncia «ninguna entidad se queda apuntando a una capa inexistente».
- **Verificación independiente:** Confirmado. CENTER_LINESTYLE (center-marks.ts:30) no se usa en ningún sitio: git grep sólo da esa línea. Las líneas de 103-125 y 234-244 llevan sólo context.metadata, sin presentation.linetype (tipo en cad-document.ts:101-104). Las líneas 153 y 268 pasan la capa CENTER fija y no emiten {type:"layer",op:"upsert"}. La cabecera (líneas 6 y 10-11) promete «la activa si CENTER no existe» y un «CONTINUO» de respaldo, y nada de eso está implementado. El ejecutor de lotes (entity-commands.ts) sólo crea capas con comandos layer, no al insertar. soldraw.ts:300-303 dice que una entidad en una capa inexistente no se ve. electrical-schematic-symbol.ts:54-56 sí da de alta su capa.
- **Arreglo:** En center-marks.ts, si context.layers() no contiene CENTER (comparando id y nombre sin distinguir mayúsculas), añade al lote {type:"layer",op:"upsert",layer:{id:"CENTER",name:"CENTER",color:<token existente>,visible:true,locked:false,linetype:"CENTER"}}. Pon en cada línea context:{metadata, presentation:{linetype:{source:"explicit",value:"CENTER"}}}, o deja ByLayer con la capa en CENTER. Corrige la cabecera para que describa lo que hace de verdad. Añade al spec la aserción de capa y tipo de línea.

### mecanico-estructura-7 · [alta] CENTERMARK y CENTERLINE no tienen panel en la cinta y ribbon.spec falla

- **Fichero:** `apps/web/src/lib/cad/ribbon.ts:150`
- **Problema:** Ningún patrón de CAD_PANEL_NAME_PATTERNS reclama los dos comandos, así que caen al panel de reposo de Anotar. ribbon.spec.ts exige que esa red de seguridad esté vacía. La auditoría y la tarea pedían el panel «Mecánica».
- **Evidencia:** ribbon.ts:150 es /^(BALLOON|BOM|WELDSYMBOL|SURFACESYMBOL)$/ → «Mecánica», y no hay otro patrón que case con CENTERMARK o CENTERLINE (git grep en ribbon.ts no da resultados). ribbon.ts:260 añade el nombre a RIBBON_PANEL_FALLBACKS cuando no hay coincidencia. ribbon.spec.ts:61 hace assert.deepEqual(cadRibbonPanelFallbacks(), []). 08-toolset-mech-elec.md:300-301 pide el panel «Mecánica».
- **Verificación independiente:** Confirmado. Ningún patrón de CAD_PANEL_NAME_PATTERNS (ribbon.ts:132-191) casa con CENTERMARK o CENTERLINE. Los anclados como `^(LINE|…)$` en la 152 o `^(-?DIM[A-Z]*|QDIM)$` no los reclaman, y el de «Mecánica» (línea 150) sólo cubre BALLOON|BOM|WELDSYMBOL|SURFACESYMBOL. ribbonPanelForCommand devuelve matched:false y la línea 260 los mete en RIBBON_PANEL_FALLBACKS. ribbon.spec.ts:59 exige deepEqual(cadRibbonPanelFallbacks(), []). La auditoría 08:300-301 pide el panel «Mecánica».
- **Arreglo:** Cambia ribbon.ts:150 a /^(BALLOON|BOM|WELDSYMBOL|SURFACESYMBOL|CENTERMARK|CENTERLINE)$/ y, si hace falta, añade su orden en ribbon-order.ts. Comprueba que ribbon.spec.ts y ribbon-icons.spec.ts pasan.

### mecanico-estructura-8 · [media] Los alias CM, CL, EJE y MARCACENTRO no funcionan al teclearlos

- **Fichero:** `apps/web/src/lib/cad/engine/alias-table.ts:127`
- **Problema:** Los alias sólo están en el descriptor y en el manifiesto. La línea de comandos resuelve lo tecleado con resolveCadCommandAlias(token, registry.names()), que sólo consulta CAD_COMMAND_ALIASES. Teclear EJE o CM responde «Comando desconocido».
- **Evidencia:** command-engine.ts:308 pasa knownCommands = registry.names(), que son sólo nombres canónicos (registry.ts). alias-table.ts:298-325 busca en known y después en CAD_COMMAND_ALIASES, y si no encuentra nada devuelve null. input-pipeline.ts:277-280 convierte null en «Comando desconocido». git grep -w de CM, CL, EJE y MARCACENTRO en alias-table.ts no da resultados. Los alias del mismo toolset sí están ahí (PERFIL en 124, GLOBO en 127, AMBOM en 128). El propio fichero documenta ese fallo con DX (comentario de las líneas 54-58).
- **Verificación independiente:** Confirmado. registry.ts:53-55 hace que names() devuelva sólo las claves de byName, sin alias. command-engine.ts:308 pasa knownCommands = registry.names(). alias-table.ts:310-323 busca en known y en CAD_COMMAND_ALIASES y, con known presente, devuelve null. input-pipeline.ts:276-280 lo convierte en «Comando desconocido». git grep -w de MARCACENTRO, CM, CL y EJE en alias-table.ts no da resultados. Los alias del mismo toolset sí están (líneas 120-135), y el comentario de 54-57 documenta este mismo fallo con DX.
- **Arreglo:** Añade a CAD_COMMAND_ALIASES en apps/web/src/lib/cad/engine/alias-table.ts, junto a GLOBO/AMBOM: MARCACENTRO: "CENTERMARK", CM: "CENTERMARK", EJE: "CENTERLINE", CL: "CENTERLINE". Añade esos cuatro pares a la comprobación de alias de engine/commands/mechanical.spec.ts (líneas 52-59) o del nuevo center-marks.spec.ts.

### mecanico-estructura-9 · [media] El arreglo para el gate eliminó el sobresaliente configurable y dejó código muerto

- **Fichero:** `apps/web/src/lib/cad/engine/commands/center-marks.ts:142`
- **Problema:** b6909fbf hace que la cruz se complete al designar. Para salir del ROJO bastaba con devolver el mensaje (el HONESTY de la sonda casa con «admite »). Ahora el sobresaliente no puede cambiarse: queda fijo en 3 unidades de dibujo, es decir 3 m en un dibujo en metros, y no sigue la unidad ni el tamaño del círculo. El criterio de T18 pedía «Sobresaliente medido».
- **Evidencia:** El prompt sin designación sólo acepta CAD_ACCEPT_ENTITY_PICK (línea 44). Un número tecleado con ese prompt termina en «Entrada no válida» (input-pipeline.ts:222-223 y 283), así que la rama distance de las líneas 142-143 no se alcanza nunca. state.pick no se asigna en ningún sitio, lo que deja muertos el prompt «Precise el sobresaliente» (46-54) y la rama enter+pick (161-173). EMPTY_MARK fija overshoot 3 (línea 37). CENTERLINE también usa 3 fijo (línea 268). Pre-fix, la sonda daba ROJO sólo porque markDone perdía el texto; el HONESTY de command-integrity-probe.mts:118 incluye «admite ».
- **Verificación independiente:** Confirmado. Tras b6909fbf el primer prompt sólo acepta CAD_ACCEPT_ENTITY_PICK (center-marks.ts:44). Un número tecleado no llega como distance: input-pipeline.ts:223 exige DISTANCE, 247 y 272 no aplican, y como accepts≠0 termina en «Entrada no válida» (línea 283). Por eso 142-143 son inalcanzables. state.pick nunca se asigna (EMPTY_MARK en la 37 y el spread de la 143), así que el prompt «Precise el sobresaliente» (46-54) y la rama de 161-173 están muertos. El sobresaliente queda fijo en 3 unidades de dibujo (líneas 37 y 268) cuando la cabecera (línea 5) dice «configurable». En 91a673d5:147-150 sí se podía configurar. Sobre la sonda: designa primero l1, una línea (command-integrity-probe.mts:257-261). Antes del arreglo, markDone recibía el mensaje como label (91a673d5:143) y devolvía result none, lo que da «silencio absoluto» y ROJO (probe:330-332). Bastaba con devolver el mensaje, porque «admite » casa con HONESTY (probe:118).
- **Arreglo:** Restituye el sobresaliente configurable sin volver a caer en ROJO. Primer prompt «Designe un círculo o arco [Sobresaliente]» con palabra clave Sobresaliente, que pide la distancia y vuelve al designar; al designar se completa con el valor vigente. Toma el valor por defecto de la unidad del documento (3 mm convertidos con cadMillimetresPerUnit(context.unit)). Aplica lo mismo a CENTERLINE. Elimina markStep con pick y la rama enter+pick. Añade al spec la aserción del sobresaliente medido, en mm y en m.

### mecanico-estructura-11 · [media] Ningún spec prueba lo anunciado: ni área ni peso ni CENTERMARK/CENTERLINE

- **Fichero:** `apps/web/src/lib/cad/mechanical.spec.ts:194`
- **Problema:** 4bd7f9dd sólo ajusta aserciones existentes, que siguen pasando con la función rota. Quitó la comprobación de block.description y conservó el mensaje «denominación y norma en description». Ninguna aserción construye un perfil con área, comprueba el tercer campo o su lectura de vuelta, ni ve un peso distinto de «—». Para T18 no hay center-marks.spec.ts ni golden, pese a estar marcada HECHA.
- **Evidencia:** mechanical.spec.ts:194 ya no incluye block.description. Las líneas 195-198 sólo prueban areaMm2 null. La 336 llama a buildCadMechanicalBomTable sin blocks, así que todos los pesos salen «—» (339-340). El diff de 4bd7f9dd no añade ningún eq ni ok (7 líneas cambiadas, 7 quitadas). git grep de center-marks.spec o CENTERMARK en apps/web/src/**/*.spec.ts y apps/web/e2e no da resultados.
- **Verificación independiente:** Confirmado. El diff de 4bd7f9dd en src/lib/cad/mechanical.spec.ts quita block.description de la aserción de la línea 194 pero conserva el mensaje «denominación y norma en description». Sólo añade areaMm2:null a los casos existentes (195-198). La línea 336 llama a buildCadMechanicalBomTable sin blocks, así que todos los pesos salen «—» (339-340). Ninguna aserción construye un perfil con área ni ve un peso numérico. git grep de CENTERMARK/CENTERLINE en apps/web sólo aparece en command-manifest.ts y center-marks.ts: no hay center-marks.spec.ts ni golden, aunque NOCHE_MIMO:373 marca T18 como HECHA.
- **Arreglo:** En mechanical.spec.ts vuelve a afirmar block.description del tornillo («Tornillo hexagonal M10 × 40 · ISO 4017»). Añade: la description de cadMechanicalBlockDefinition(PTR 50,8×50,8×3) termina en « · 573.6…», cadMechanicalPartOf devuelve areaMm2≈573.6, y buildCadMechanicalBomTable con blocks da los pesos esperados para un PTR con longitud. Crea apps/web/src/lib/cad/engine/commands/center-marks.spec.ts que recorra CENTERMARK sobre un círculo de radio 10 y afirme dos líneas de (−13,0) a (13,0) y de (0,−13) a (0,13), capa CENTER, tipo de línea y metadata. Añade también CENTERLINE entre dos círculos y entre dos paralelas, el rechazo de una polilínea y los alias.

### mecanico-estructura-12 · [baja] La cabecera promete regeneración y cruz de arco que no existen, y CENTERLINE sólo guarda un anfitrión

- **Fichero:** `apps/web/src/lib/cad/engine/commands/center-marks.ts:9`
- **Problema:** La cabecera dice que la marca permite regenerar cuando la geometría se mueve, y el comentario de las líneas 78-79 dice que en un arco la cruz llega al punto medio. No hay regenerador ni tratamiento distinto del arco. CENTERLINE guarda en centerTarget sólo el primer objeto, así que un regenerador futuro no podría reconstruirlo. La regla de la casa pide declarar que no se regeneran solos.
- **Evidencia:** center-marks.ts:9-11 y 78-79 frente a 88-95, donde círculo y arco hacen lo mismo. La línea 242 hace centerTarget: a.id y el id de b no se guarda. git grep de "centermark" en 52898e03 sólo aparece en center-marks.ts.
- **Verificación independiente:** Confirmado (severidad baja). El comentario de center-marks.ts:77-79 dice que en un arco la extensión llega al punto medio del arco, pero 88-95 tratan el arco igual que el círculo. La cabecera (9-11) habla de regeneración y de un tipo de línea CENTER o CONTINUO que no existen. CENTERLINE guarda sólo centerTarget: a.id (línea 242), así que pierde b. git grep de centerTarget y "centermark" sólo aparece en center-marks.ts: no hay regenerador. La auditoría 08:304-305 fija como regla de la casa declarar en el módulo que no se regeneran solos todavía, y la cabecera no lo declara.
- **Arreglo:** Corrige la cabecera: di que las marcas NO se regeneran solas todavía y que la cruz del arco es completa. En CENTERLINE guarda centerTarget con a.id y centerTarget2 con b.id. No vuelvas a escribir «asociativo» ni «regeneración» hasta que exista la orden que lo haga.

### mecanico-estructura-13 · [baja] Lectura legacy: la norma se corta en el segundo separador y los documentos antiguos nunca tienen peso

- **Fichero:** `apps/web/src/lib/cad/mechanical-parts.ts:502`
- **Problema:** Antes, la norma de un bloque MECH- era todo lo que seguía al primer « · ». Ahora se corta en el segundo separador y el resto se interpreta como área. Un bloque existente con description «Placa · NOM-001 · rev B» pasa a mostrar la norma «NOM-001» en el BOM. Además, un documento que ya tenga definido MECH-PTR-… reutiliza la definición vieja sin área y nunca mostrará peso. BOM Actualizar descarta los anchos de columna de las tablas de 5 columnas guardadas.
- **Evidencia:** mechanical-parts.ts:501-507 buscan un segundo separador en rest y hacen parseFloat. En HEAD~ (445352d8) standard era description.slice(separator+3). engine/commands/mechanical-parts.ts:73-74 reutilizan la definición existente (existing ?? cadMechanicalBlockDefinition). mechanical-annotate.ts:207 hace columnWidths: table.columnWidths.length === rebuilt.columns ? … : rebuilt.columnWidths, y 5 no es igual a 7.
- **Verificación independiente:** Confirmado en lo sustancial, con matiz. (a) Cambio de lectura: lib mechanical-parts.ts:501-507 corta la norma en el segundo « · ». En 445352d8 era todo lo que seguía al primero. Afecta a descriptions con dos separadores, pero ninguna norma del catálogo los tiene (git grep ' · ' sólo da la constante de la 480), así que es un caso hipotético de bloques MECH- ajenos. (b) Real: engine mechanical-parts.ts:73-75 reutilizan la definición existente, así que un MECH-PTR definido antes de 4bd7f9dd no tiene área y nunca da peso. (c) Real: mechanical-annotate.ts:207 descarta columnWidths cuando 5≠7. La aserción de engine/commands/mechanical.spec.ts:321-324, que exige conservar anchos de 5 columnas, ya no se cumple.
- **Arreglo:** En cadMechanicalPartOf, trata como área sólo el último campo y sólo si es numérico con /^\d+(\.\d+)?$/; si no lo es, conserva como norma todo lo que sigue al primer separador. En cadMechanicalInsertCommands, si la definición existente no trae área y la pieza sí, emite {type:"block",op:"define"} con la description ampliada, sin cambiar el id. En relistar, cuando la tabla vieja tenga 5 columnas, conserva sus anchos e inserta los de las columnas nuevas en su posición. Añade al spec un caso de cada.

## Arquitectura, flujo 2D e interoperabilidad DXF (38bda271, bcd1dd96, 48f9c526, fdd321d1, ecc5570e, 1425a1a1, 3f654be5) revisados en 52898e03

**Resumen del revisor:** Ninguno de los siete commits está listo para main. Hay tres defectos de severidad alta.

1. 3f654be5 cambia el nombre de la entidad a MULTILEADER, pero el importador propio sigue buscando sólo MLEADER. Se rompe la ida y vuelta: dxf-cad-document.spec.ts:281 y :284 fallan en test:specs, y también falla el golden e2e 17 en la línea 88.
2. ecc5570e mete en Layout3DEditor.tsx:14647 un `includes(data?.footprint.unit)` que no compila con strict (TS2345), así que el typecheck del CI cae.
3. 1425a1a1 promete que las directrices escalan con la escala anotativa y no es así: el orden `properties {height}` no existe para mleader. El adaptador ignora `height`, pero cuenta la directriz como reescalada y deja fijos los tamaños por defecto, con lo que la despega de su estilo.

Hay además defectos de severidad media:
- D-03 oculta todas las esquinas de muro, también las que eran correctas. El arreglo estaba a una línea: snap-scene y el editor ya tienen el documento.
- D-06 arregla unitToMm, pero la altura anotativa en espacio papel sigue tratando los pies como mm.
- D-05 conserva un analizador aparte que no lee `10'-6"` ni fracciones en dibujos en pies o pulgadas.
- H-11: el spec sólo cuenta columnas; ninguna aserción mira la fase. La bitácora lo marca como HECHA sin los totales por fase ni la regla de REVISA. `cadHasRenovationLayers` no tiene llamadores y lee `name`, que es el rótulo, en vez del id.
- D-1: la frase «ezdxf y AutoCAD ya la reconocen» no tiene ningún spec detrás, y la estructura sigue incompleta.

Tres cosas sí son correctas: el factor 304.8 en unitToMm, la columna «Fase» que aparece en la tabla y en el CSV, y el recuento de 25 aserciones.

**Afirmaciones de tus commits que no se cumplen:**

- 38bda271: hay que ocultarlas hasta que CadSnapProvider acepte document? (el documento no está disponible)
  - Evidencia: cadSnapSceneAddEntities (snap-scene.ts:118-123) ya recibe `document?` y se lo pasa a renderer.paths. lisp/builtins/osnap.ts:190 ya lo pasa. Layout3DEditor.tsx:6410 lo omite, aunque tiene loadedCadDocumentRef.current dos líneas después. Bastaba añadir el parámetro opcional.
- bcd1dd96: el espacio papel ya no convierte pies como milímetros
  - Evidencia: paper-space-render.ts:273 calcula la altura anotativa por ventana con cadAnnotativeModelHeight(..., document.meta.unit). Esa función usa cadUnitToMillimetres (annotative-scale.ts:40-45), que no tiene `ft` y devuelve 1.
- 48f9c526: cadHasRenovationLayers detecta si el dibujo tiene capas de demolición
  - Evidencia: mexican-layers.ts:639-641 lee `layer.name`. En CadLayerDef el nombre con sufijo es `id`, y `name` es el rótulo («Muros por demoler», cadMexicanLayerDefs:575-576), así que con document.layers siempre da false. Además da true con sólo -EXI o -NUE, y nadie la llama (git grep no encuentra usos).
- c28187bd (bitácora): H-11 HECHA
  - Evidencia: La auditoría H-11 (06-toolset-architecture.md:714-745) pide tres totales al pie por fase, la regla de REVISA y specs de las tres fases. Nada de eso existe.
- ecc5570e: Layout3DEditor pasa la unidad real del documento
  - Evidencia: Layout3DEditor.tsx:14647 no compila. `data` es `Layout | null` (línea 1133) y `Footprint.unit` es `string` (línea 820), así que `data?.footprint.unit` es `string | undefined`, y string[].includes exige `string` (TS2345 con strict:true). El CI corre `npm run typecheck` (tsc --noEmit) en apps/web.
- 1425a1a1: las directrices ahora escalan con la escala anotativa
  - Evidencia: annotative-scale.ts emite `{type:"properties", patch:{height}}`. mleader-entity-adapter.ts:88-105 (write) no lee `height`; la altura de la directriz es `textHeight`. paper-space-render.ts sólo aplica la anotatividad a text/mtext y dimension. No se añadió ningún spec.
- 3f654be5: ezdxf y AutoCAD ya reconocen la entidad
  - Evidencia: No hay spec ni evidencia. La cabecera declara AC1015 (dxf-export.ts:279), versión en la que la clase no existe según ADR 0009:104. No hay sección CLASSES, ni MLEADERSTYLE ni 340, y CONTEXT_DATA sigue incompleto (faltan 10/11/290/291/271), que es lo que exige la propia auditoría D-1 (10-interop.md:737-770).
- 3f654be5 (implícita): se mantiene la ida y vuelta con el importador propio
  - Evidencia: dxf-read-annotations.ts:176 sólo acepta `MLEADER`, igual que dxf-import.ts:303 (RAW_ONLY) y :953. dxf-cad-document.spec.ts:281 exige `/0\r?\nMLEADER/`.

### arquitectura-2d-interop-1 · [alta] MULTILEADER rompe la ida y vuelta DXF con el importador propio y tumba specs existentes

- **Fichero:** `apps/web/src/lib/cad/dxf-export.ts:749`
- **Problema:** El exportador ahora escribe `0 MULTILEADER`, pero todo el lado de lectura sigue buscando `MLEADER`. Una directriz exportada por el producto ya no vuelve al reimportar: el importador la da por entidad no soportada y la pierde.
- **Evidencia:** - dxf-read-annotations.ts:176: `pairs[start].value.toUpperCase() !== "MLEADER"`, así que parseRawDxfSemanticMleaders devuelve [].
- dxf-import.ts:303: RAW_ONLY_ENTITY_TYPES sólo contiene "MLEADER", de modo que rawEntityTypeCounts (línea 1005) emite «Entidad DXF no soportada: MULTILEADER».
- dxf-cad-document.spec.ts:281 (`assert.match(mleaderDxf.content, /0\r?\nMLEADER/)`) y :284 (`reimportedMleaders.mleaders.length === 1`) fallan. El spec lo recoge run-specs.mjs (src/**/*.spec.ts).
- e2e/golden/17-cad-native-mleader.spec.ts:88 (`expect(imported.mleaders).toHaveLength(1)`) también falla.
- El golden congelado dxf-xdata-golden.spec.ts:456 sigue usando `MLEADER`, así que la lectura antigua tiene que seguir funcionando.
- **Verificación independiente:** Confirmado. dxf-export.ts:749 escribe ahora `0 MULTILEADER` desde pushMleader, que es el único camino (llamado en :940). El lector sigue esperando MLEADER: dxf-read-annotations.ts:176 descarta todo lo que no sea exactamente `MLEADER`, y dxf-import.ts:836 saca `mleaders` sólo de parseRawDxfSemanticMleaders, así que reimportar devuelve []. Además, dxf-import.ts:303 sólo exime MLEADER, de modo que rawEntityTypeCounts (:325-349) cuenta MULTILEADER y :1005 emite el aviso unsupported_entity. En dxf-cad-document.spec.ts:281 la regex /0\r?\nMLEADER/ ya no casa con nada del fichero: el XDATA es `1001\r\nVALLE_MLEADER`, que no empieza por 0. La aserción :284 (`mleaders.length === 1`) también falla. run-specs.mjs:43 recoge src/**/*.spec.ts. e2e/golden/17-cad-native-mleader.spec.ts:88 espera toHaveLength(1). En la punta 52898e03 nadie tocó el lector.
- **Arreglo:** En dxf-read-annotations.ts:176 acepta los dos nombres: `const t = value.toUpperCase(); if (t !== "MLEADER" && t !== "MULTILEADER") continue;`. Añade "MULTILEADER" a RAW_ONLY_ENTITY_TYPES (dxf-import.ts:300) y trata `type === "MULTILEADER"` igual que MLEADER en dxf-import.ts:953. Actualiza dxf-cad-document.spec.ts:281 a `/0\r?\nMULTILEADER/`. Deja intacto dxf-xdata-golden.spec.ts. Añade una aserción de que el reimportado no trae avisos `unsupported_entity`. Corre test:specs y el golden 17 antes de marcar D-1.

### arquitectura-2d-interop-3 · [alta] D-08: la directriz anotativa no escala; el orden `height` se ignora, se cuenta como hecho y fija tamaños por defecto

- **Fichero:** `apps/web/src/lib/cad/layout/annotative-scale.ts:112`
- **Problema:** Añadir "mleader" a HEIGHT_BEARING_TYPES hace que cadAnnotativeRescaleCommands y cadAnnotativeModelRescaleCommands emitan `{type:"properties", patch:{height}}`. La altura del mleader es `textHeight`, y su adaptador no lee `height`: la letra no cambia. Como la entidad no tiene `height`, el «sin cambio» nunca se cumple, el orden se emite en cada cambio de escala y la directriz aparece en rescaledEntityIds. El propio comentario de la línea 102-110 dice que eso es peor que no hacerlo. Además write materializa los valores por defecto: una directriz que seguía a su estilo queda con arrowSize 180, doglegLength 600, textWidth 1800, textHeight 120 y lineSpacing 1.2 fijos, es decir, un dato persistido alterado.
- **Evidencia:** - mleader-entity-adapter.ts:88-105 (write): no hay `patch.height`; `textHeight: positive(patch.textHeight, entity.textHeight ?? 120)`, y `positive` (línea 10) devuelve el fallback cuando no viene valor.
- annotative-scale.ts, readHeight y `(entity as {height?}).height`: undefined para mleader, así que siempre hay orden.
- entity-commands.ts:517: properties aplica `adapter.properties.write(source, command.patch)`.
- paper-space-render.ts:267-296 sólo aplica la anotatividad a texto y cota.
- annotative-scale.spec.ts no tiene ningún caso de mleader.
- **Verificación independiente:** Confirmado. Con "mleader" en HEIGHT_BEARING_TYPES (annotative-scale.ts:112), las dos funciones de reescalado emiten `patch: { height }`, en :225 y :300. El tipo mleader (cad-document.ts:322-358) no tiene `height`, sólo `textHeight`. Por eso readHeight (:232) devuelve null y la comparación de :298 nunca se cumple: hay orden en cada pasada y el id sale en rescaledEntityIds. entity-commands.ts:517 aplica mleaderAdapter.properties.write, que ignora `height` y, vía `positive(patch.x, entity.x ?? def)` (mleader-entity-adapter.ts:10 y :88-105), fija arrowSize 180, doglegLength 600, textWidth 1800, textHeight 120 y lineSpacing 1.2 aunque falten. transform (:115-122) sí los conserva ausentes. paper-space-render.ts:262-315 sólo resuelve la anotatividad de text, mtext y dimension. annotative-scale.spec.ts no tiene ningún caso de mleader. La auditoría D-08 (04-flujo-2d.md:770-779) ya señalaba que la altura es `textHeight`.
- **Arreglo:** Para `entity.type === "mleader"`, en las dos funciones de reescalado, lee `textHeight` (con 120 por defecto sólo para comparar) y emite `patch: { textHeight: height }`. Si se quiere que la flecha y el codo acompañen, escálalos en proporción como hace cadAnnotativeDimensionSizes. En mleader-entity-adapter.ts write no materialices tamaños ausentes: conserva `undefined` cuando ni el patch ni la entidad los traen, igual que ya hace transform (líneas 115-122). Añade el caso mleader en paper-space-render.ts. Añade a annotative-scale.spec.ts: una directriz con annotativeHeightMm=2.5 a 1:50 en mm pide textHeight=125; la segunda pasada no emite orden; y el write no añade arrowSize/doglegLength ausentes.

### arquitectura-2d-interop-4 · [media] D-1 marcado HECHO con la afirmación «ezdxf y AutoCAD ya reconocen la entidad» sin evidencia y con estructura incompleta

- **Fichero:** `apps/web/src/lib/cad/dxf-export.ts:754`
- **Problema:** Sólo se cambió el nombre y se añadió AcDbEntity. El resto de lo que exige la auditoría D-1 sigue sin hacer: MLEADERSTYLE en OBJECTS con su 340, CONTEXT_DATA completo o, mientras tanto, declarar la degradación en el manifiesto de pérdidas, y un MLEADER en el corpus sintético. Ningún spec lee el fichero con un lector estricto, así que nada respalda la afirmación sobre ezdxf y AutoCAD.
- **Evidencia:** - dxf-export.ts:754-767: CONTEXT_DATA sólo lleva 40, 10/20/30 y 304, sin 11/21/31, 290, 291 ni 271.
- La cabecera declara AC1015 (dxf-export.ts:279), y ADR 0009:104 dice «MLEADER no existe en AC1015 (clase R2007+)». No se escribe sección CLASSES.
- dxf-export-loss-manifest.ts:61 trata "mleader" como exportado por camino propio, sin declarar degradación.
- 10-interop.md:763-769 pide todo lo anterior.
- La bitácora 1ad033fe lo marca «HECHA».
- **Verificación independiente:** Confirmado. El commit 3f654be5 sólo toca dos líneas de dxf-export.ts y no añade ningún spec que respalde «ezdxf y AutoCAD ya reconocen». CONTEXT_DATA (:754-767) sigue con sólo 40, 10/20/30 y 304. No se escribe MLEADERSTYLE, ni el 340, ni sección CLASSES (git grep CLASSES en dxf-export.ts no da nada), y la cabecera declara AC1015 (:279). dxf-export-loss-manifest.ts:61 trata mleader como exportado sin declarar degradación, y dxf-external-corpus.ts no contiene ningún mleader. Todo eso lo pide la auditoría (10-interop.md:770-774: MLEADERSTYLE, declarar la degradación mientras CONTEXT_DATA esté incompleto y añadirlo al corpus sintético). Aun así, la bitácora 1ad033fe marca D-1 como «HECHA».
- **Arreglo:** No marques D-1 como HECHA. Declara en dxf-export-loss-manifest.ts una degradación para mleader (sin estilo MLEADERSTYLE ni CONTEXT_DATA completo) mientras no se escriba. Añade una directriz al corpus sintético (dxf-external-corpus.ts) y un caso en verification/ que lea el DXF exportado con el mismo oráculo estricto que se usó para MTEXT. Cuando esté completo, escribe MLEADERSTYLE en OBJECTS, el 340 que lo apunta y los grupos que faltan en CONTEXT_DATA. Quita del mensaje la frase sobre ezdxf/AutoCAD salvo que un spec la demuestre.

### arquitectura-2d-interop-6 · [media] D-06 a medias: la altura anotativa en espacio papel sigue tratando los pies como milímetros

- **Fichero:** `apps/web/src/lib/cad/layout/annotative-scale.ts:40`
- **Problema:** El commit dice que el espacio papel ya no convierte pies como mm, pero el render de lámina calcula la altura anotativa de textos y cotas con cadUnitToMillimetres. Esa función no conoce `ft`, así que en un dibujo en pies un rótulo anotativo de 2,5 mm sale 304,8 veces más grande. Además el arreglo añade un literal más en vez de delegar en la tabla única, que es justo lo que la auditoría critica («cuarta tabla divergente»).
- **Evidencia:** - annotative-scale.ts:40-45: `m`→1000, `cm`→10, `in`→25.4 y el resto →1.
- paper-space-render.ts:273 y :305 la usan con `context.document.meta.unit`.
- units-imperial.ts:47-53 ya tiene CAD_DRAWING_UNIT_TO_MM con `ft: 304.8`.
- 06-toolset-architecture.md:950-963.
- **Verificación independiente:** Confirmado. cadUnitToMillimetres (annotative-scale.ts:40-45) no conoce `ft` y devuelve 1. La usan paper-space-render.ts:273 (texto) y cadAnnotativeDimensionSizes vía :305 con `context.document.meta.unit`, además de las órdenes de reescalado (:224 y :299), que persisten la altura. meta.unit es `string` (cad-document.ts:70) y ft es una unidad soportada (units-imperial.ts:47-53, cadDrawingUnitFromInsunits). bcd1dd96 sólo añadió el literal a unitToMm de paper-space-style.ts, y la auditoría D-06 (06-toolset-architecture.md:950-963) critica precisamente la tabla divergente. Matiz: el tamaño del texto en lámina está acotado a 1,5-12 mm (paper-space-render.ts:283), así que visualmente no llega a ser 304,8 veces mayor. Las alturas escritas al modelo y la geometría de la cota sí salen con ese factor de más.
- **Arreglo:** Haz que `cadUnitToMillimetres` (annotative-scale.ts) y `unitToMm` (paper-space-style.ts) deleguen en la tabla única: `return CAD_DRAWING_UNIT_TO_MM[unit as CadDrawingUnit] ?? 1;` importando de ../units-imperial. Añade a annotative-scale.spec.ts `cadAnnotativeModelHeight(2.5, 50, "ft")` ≈ 0.41010498687664043 y un caso de unitToMm("ft") === 304.8.

### arquitectura-2d-interop-7 · [media] D-05: la entrada dinámica sigue con su propio analizador y rechaza pies-pulgadas y fracciones en dibujos imperiales

- **Fichero:** `apps/web/src/lib/cad/dynamic-input.ts:57`
- **Problema:** Ahora se aceptan `in` y `ft` como unidad del documento, pero parseCadDynamicScalar sólo lee «número + sufijo». En un plano en pies, `10'-6"`, `6 1/2"` o `1/2"` dan «Valor inválido»: normalizedNumber quita los espacios (`61/2`), `Number` da NaN y se devuelve null. La línea de comandos sí los acepta. Siguen los «dos analizadores para la misma pregunta» que señala la auditoría D-05, y la tabla UNIT_TO_MM (línea 30) duplica CAD_DRAWING_UNIT_TO_MM.
- **Evidencia:** - dynamic-input.ts:57: `/^(.+?)(mm|cm|m|in|ft|°|deg)?$/`.
- dynamic-input.ts:39: `.replace(/\s+/g, '')`.
- units-imperial.ts:326 parseCadLengthInDrawingUnits lee las 18 formas (líneas 166-173) y ya lo usan input-pipeline.ts:215 y precision-input.ts:92.
- 04-flujo-2d.md:746-756.
- dynamic-input.spec.ts no tiene ningún caso con documentUnit in/ft ni con formato imperial.
- **Verificación independiente:** Confirmado. parseCadDynamicScalar usa `/^(.+?)(mm|cm|m|in|ft|°|deg)?$/` (dynamic-input.ts:57) y normalizedNumber quita los espacios (:39) antes de hacer Number(). `10'-6"` llega entero a Number y da NaN; `6 1/2"` pasa a `61/2"` y también da NaN, así que se devuelve null. La línea de comandos usa parseCadLengthInDrawingUnits (input-pipeline.ts:215, precision-input.ts:92), que sí las lee (units-imperial.ts:166-173). UNIT_TO_MM (:30-36) duplica CAD_DRAWING_UNIT_TO_MM. La auditoría D-05 (04-flujo-2d.md:750-756) señala expresamente los «dos analizadores para la misma pregunta», y dynamic-input.spec.ts no tiene casos con documentUnit in/ft. No es una regresión, pero el commit deja D-05 a medias y la bitácora lo marca HECHA.
- **Arreglo:** En parseCadDynamicScalar, cuando kind==='length' y `cadTextLooksImperial(raw)`, delega en `parseCadLengthInDrawingUnits(raw, { drawingUnit: documentUnit })` y devuelve `value` si ok, o null si no. En los demás casos conserva la ruta métrica, pero con `CAD_DRAWING_UNIT_TO_MM` en lugar de la tabla local. Añade a dynamic-input.spec.ts: `parseCadDynamicScalar("10'-6\"", 'ft') === 10.5`, `parseCadDynamicScalar('6 1/2"', 'in') === 6.5`, `parseCadDynamicScalar('1ft', 'in') === 12`, `parseCadDynamicScalar('24', 'in') === 24` y `parseCadDynamicScalar('1,5m', 'mm', 'es-MX') === 1500`.

### arquitectura-2d-interop-8 · [media] H-11: el spec de data-extraction no prueba la columna «Fase»

- **Fichero:** `apps/web/src/lib/cad/data-extraction/data-extraction.spec.ts:73`
- **Problema:** Las dos aserciones cambiadas sólo cuentan 7 columnas y 7 celdas. Los muros de prueba están en la capa «MUROS», sin sufijo, así que el spec pasaría igual si cadRenovationPhase devolviera siempre null o invirtiera Existente y Demoler. cadRenovationPhase no tiene ningún spec.
- **Evidencia:** - data-extraction.spec.ts:26: `layer: "MUROS"`.
- data-extraction.spec.ts:73: `table.columns === 7`.
- data-extraction.spec.ts:80: `wallRowCells.length === 7`.
- Ningún ok() mira el texto «Existente», «Demoler» o «Nuevo».
- git grep cadRenovationPhase en 52898e03 no encuentra ningún *.spec.ts.
- **Verificación independiente:** Confirmado. En data-extraction.spec.ts todos los muros de prueba están en `layer: "MUROS"` (:26), y las dos aserciones cambiadas sólo cuentan columnas (:72) y celdas (:79). Ningún ok() comprueba los textos «Existente», «Demoler» o «Nuevo», ni la cabecera `Fase` del CSV. git grep cadRenovationPhase en 52898e03 sólo encuentra data-extraction.ts:27/44 y mexican-layers.ts:625: no hay ningún spec. El spec pasaría aunque la función devolviera siempre null.
- **Arreglo:** En data-extraction.spec.ts construye un cuadro con muros en MURO-EXI, MURO-DEM, MURO-NUE y MURO. Comprueba que la celda de la columna 6 de cada fila dice exactamente «Existente», «Demoler», «Nuevo» y «—», y que la cabecera del CSV termina en `,Fase`. Añade en mexican-layers.spec.ts casos de cadRenovationPhase: `muro-dem` → demoler (minúsculas), `MURO` → null, `EXIST-MURO` → null.

### arquitectura-2d-interop-9 · [media] H-11 marcada HECHA sin totales por fase ni regla de REVISA; cadHasRenovationLayers lee el campo equivocado y nadie la llama

- **Fichero:** `apps/web/src/lib/cad/standards/mexican-layers.ts:639`
- **Problema:** La bitácora (c28187bd) marca H-11 como HECHA y fdd321d1 ya no dice «parcial», pero faltan los tres totales al pie por fase y la regla de REVISA que pide la auditoría. La función auxiliar para esa regla no tiene llamadores y, si se conectara con `document.layers`, nunca detectaría nada: compara `layer.name`, que en CadLayerDef es el rótulo legible, cuando el nombre con sufijo está en `id`. Su comentario dice «capas de demolición», pero devuelve true también con sólo -EXI o -NUE.
- **Evidencia:** - mexican-layers.ts:639-641: `layers.some((layer) => cadRenovationPhase(layer.name) !== null)`.
- cadMexicanLayerDefs:575-576: `id: item.id` ("MURO-DEM") y `name: item.name` ("Muros por demoler").
- cad-document.ts:397-399: `id` y `name`.
- git grep cadHasRenovationLayers: sólo aparece su definición.
- 06-toolset-architecture.md:735-745 pide los totales, la regla de REVISA y sus specs.
- **Verificación independiente:** Confirmado. La auditoría H-11 (06-toolset-architecture.md:728-745) pide la columna Fase, «tres totales al pie», la regla de REVISA y specs de las tres fases y del plano sin capas de fase. data-extraction.ts no tiene ningún total (git grep total da 0) y no existe la regla de REVISA. Aun así, c28187bd marca H-11 como HECHA. cadHasRenovationLayers (mexican-layers.ts:639-641) compara `layer.name`. En CadLayerDef (cad-document.ts:397-399), cadMexicanLayerDefs (:575-576) guarda `id: "MURO-DEM"` y `name: "Muros por demoler"`, así que con document.layers nunca detectaría nada. Nadie la llama: sólo aparece su definición. Además devuelve true también con -EXI, cuando la auditoría habla de -DEM/-NUE.
- **Arreglo:** Cambia la firma a `cadHasRenovationLayers(layers: readonly { id: string }[])` y compara `layer.id`. Añade también los ids de las capas usadas por las entidades, porque un DXF importado puede no declararlas. Corrige el comentario a «capa de fase». Añade al cuadro de muros las tres filas de total (m, m², m³) por fase. Añade a REVISA el hallazgo «el plano es de demolición y este muro no dice de qué fase es» cuando cadHasRenovationLayers es true y el muro está en una capa sin sufijo, con specs de ambos casos (con y sin capas de fase). Hasta entonces, cambia la bitácora de H-11 a PARCIAL.

## Cinta, estilos visuales y persistencia local (069d575d, 361d7db3, edc54db3, 99a8282e, 2d54102f) revisados en 52898e03

**Resumen del revisor:** De los cinco commits, sólo el de SHELL (edc54db3) y el de la cinta (99a8282e) hacen lo que dicen. Los dos de persistencia local tienen defectos serios, el de VSCURRENT no funciona y ningún commit añade specs.
1) El catálogo de tipos de línea .lin guarda en localStorage pero no restaura nunca. La validación pide un campo `segments` que el tipo no tiene (el tipo usa `pattern`). Además, la primera vez que se guarda algo en una sesión nueva se pisa lo que quedó guardado de antes. La bitácora marca T25 como HECHA y es falso.
2) En los dos catálogos, cargar una tabla o tipo con nombre de fábrica (acad.ctb, monochrome.ctb o los DASHED/HIDDEN/CENTER de acadiso.lin) sustituye al de fábrica durante la sesión, pero no se guarda. Al recargar vuelve el de fábrica sin avisar y el plano sale con otros grosores o patrones.
3) Las claves nuevas no llevan inquilino ni usuario, cuando las paletas de herramientas del mismo hook sí los llevan. Tampoco llevan versión, y la validación de lo leído es superficial.
4) Ningún spec prueba la persistencia. Las «22 aserciones» de plot-style-catalog.spec ya existían y no tocan localStorage, y linetype.spec ni siquiera importa el catálogo.
5) El cambio de VSCURRENT es código muerto. La condición `label && !solidShadeHost.current` nunca se cumple, porque la etiqueta sólo existe si el visor de sólidos está montado. Tampoco cuenta objetos, que era lo que pedía la auditoría, así que D-04 sigue abierto aunque la bitácora lo dé por hecho.
6) Quitar SHELL de la cinta es correcto e inocuo: no hay comando SHELL registrado y el vaciado sigue disponible como SOLIDEDIT Cuerpo·Vaciar.
7) La degradación de la cinta no esconde el error en CI, porque ribbon.spec sigue exigiendo los seis espejos de Inicio > Anotación. Pero no publica el espejo perdido en cadRibbonPanelFallbacks() como pedía la auditoría, y dos comentarios siguen diciendo que la carga revienta.
No se rompen specs ni goldens existentes; el golden 47 sigue esperando «Estilo visual: Alámbrico.» porque la rama nueva nunca se ejecuta. No hay colores hex, dependencias ni data-testid tocados, y los textos nuevos están en español.

**Afirmaciones de tus commits que no se cumplen:**

- 069d575d: plot-style-catalog.spec 22 aserciones (como prueba de la persistencia)
  - Evidencia: El commit no toca el spec. Las 22 aserciones son las que ya había (secciones 1-6) y ninguna usa localStorage ni crea un segundo catálogo para comprobar la restauración. El número es real, pero no prueba la función anunciada.
- 361d7db3: CadLinetypeCatalog restaura los tipos guardados
  - Evidencia: linetype-lin.ts:230 exige `"segments" in item`, pero CadLinetypeDefinition (36-41) tiene name, description y pattern. `git grep -w segments` en linetype-lin.ts sólo encuentra esa línea. Ningún elemento pasa el filtro y nunca se restaura nada.
- Bitácora (f349cc5f, 8a388571): T25 HECHA, «.ctb y .lin persisten en localStorage»
  - Evidencia: Los .lin no se restauran (linetype-lin.ts:230) y los sustitutos de las tablas y tipos de fábrica no se guardan (plot-style-catalog.ts:101, linetype-lin.ts:217). El criterio de NOCHE_MIMO_20260914.md (§T25: cargar, recargar y que siga cargado) no tiene ninguna prueba.
- 99a8282e: degrada correctamente (según el arreglo D5 de la auditoría)
  - Evidencia: 03-ribbon-ux.md §D5 pide dejar fuera el espejo Y publicarlo en cadRibbonPanelFallbacks(); el commit sólo hace `continue`. Los comentarios de ribbon.ts:275 y ribbon-order.ts:79-81 siguen diciendo que la carga revienta.
- 2d54102f: VSCURRENT ya no confirma éxito sin objetos sombreables y dice «0 objetos: este dibujo no tiene sólidos sombreables»
  - Evidencia: studio-engine-bridges.ts:165 calcula `label = solidShadeHost.current?.applyVisualStyle(styleId) ?? null`, así que label sólo tiene valor si solidShadeHost.current existe. La condición de la línea 168 (`label && !solidShadeHost.current && ...`) es siempre falsa y la rama no se ejecuta nunca. Tampoco se cuentan objetos: applyVisualStyle devuelve sólo la etiqueta (solid-shade-host.ts:310-311). studio-engine-bridges.spec.ts no cambia desde ca1cd96d.

### ui-cinta-persistencia-1 · [alta] Los tipos de línea .lin nunca se restauran: la validación pide un campo 'segments' que no existe

- **Fichero:** `apps/web/src/lib/cad/linetype-lin.ts:230`
- **Problema:** restoreFromStorage sólo acepta elementos con la propiedad 'segments', pero CadLinetypeDefinition usa 'pattern'. Nada de lo guardado vuelve a entrar al recargar, así que la mitad .lin de T25 no funciona aunque la bitácora la dé por HECHA. Además, como la restauración falla en silencio, el primer save o remove de la sesión nueva (216-221) pisa la clave con los tipos de esa sesión y borra lo guardado antes.
- **Evidencia:** linetype-lin.ts:36-41 define `interface CadLinetypeDefinition { name; description; pattern: readonly number[] }`. La línea 230 comprueba `"name" in item && "segments" in item`. `git grep -w segments 52898e03 -- apps/web/src/lib/cad/linetype-lin.ts` sólo encuentra la 230.
- **Verificación independiente:** Confirmado en 52898e03. linetype-lin.ts:36-41 define CadLinetypeDefinition con name, description y pattern, y ninguna definición del fichero tiene segments: `git grep -w segments` sólo encuentra la 230. La condición de linetype-lin.ts:230 (`"name" in item && "segments" in item`) nunca se cumple con lo que escribe saveToStorage (:216-220), que serializa objetos con pattern. Por eso no se restaura nada. También es cierto el daño añadido: tras una restauración vacía, this.items sólo tiene los de fábrica, y el primer save (:189) o remove (:199) llama a saveToStorage, que escribe esa lista filtrada y pisa la clave. La bitácora 8a388571 marca «T25 | HECHA | .ctb y .lin persisten».
- **Arreglo:** En linetype-lin.ts:230 valida `typeof item.name === "string" && typeof item.description === "string" && Array.isArray(item.pattern) && item.pattern.every(Number.isFinite)`. En vez de `push`, usa la misma lógica de sustitución por nombre en mayúsculas de `save`, pero sin volver a escribir en storage. Añade en session-catalogs.spec.ts (o en un spec nuevo que recoja run-specs.mjs) una prueba con un localStorage falso en globalThis: cargar un .lin con un tipo propio, crear un CadLinetypeCatalog nuevo y comprobar que get() lo devuelve con el mismo patrón. Ese spec debe fallar con el código actual.

### ui-cinta-persistencia-7 · [alta] VSCURRENT: la rama «0 objetos» es código muerto y D-04 sigue sin arreglar

- **Fichero:** `apps/web/src/components/cad/command-line/studio-engine-bridges.ts:168`
- **Problema:** La condición `label && !solidShadeHost.current && !nativeMassHosts?.current` nunca se cumple, porque label vale `solidShadeHost.current?.applyVisualStyle(styleId) ?? null` y sólo tiene valor si solidShadeHost.current existe. Con el visor de sólidos montado y un dibujo sin ningún solid3d (la planta de cualquier arquitecto), VSCURRENT sigue diciendo «Estilo visual: Sombreado.», que es el éxito falso de D-04. Además la comprobación mira si el visor está montado, no cuántos objetos se sombrean. La bitácora (8a388571) marca D-04 como HECHA.
- **Evidencia:** studio-engine-bridges.ts:165 `const label = solidShadeHost.current?.applyVisualStyle(styleId) ?? null;` y :168 `if (label && !solidShadeHost.current && !nativeMassHosts?.current)`. solid-shade-host.ts:310-311 devuelve sólo `cadVisualStyle(this.setStyle(style)).label`, sin conteo. 02-3d-visual.md (arreglo de §4.4) pide que applyVisualStyle devuelva `{ label, changed }` con el conteo y una entrada en el arnés de veracidad. studio-engine-bridges.spec.ts sin cambios desde ca1cd96d.
- **Verificación independiente:** Confirmado. En studio-engine-bridges.ts:164, label vale `solidShadeHost.current?.applyVisualStyle(styleId) ?? null`. Si solidShadeHost.current es null, label es null; si no lo es, `!solidShadeHost.current` es false. La condición añadida en :168 no puede cumplirse nunca y el puente sigue devolviendo la etiqueta tal cual. solid-shade-host.ts:310-311 sólo devuelve la etiqueta, sin conteo, y native-mass-hosts.ts:47-49 tampoco devuelve conteo. El arreglo de 02-3d-visual.md §4.4 pide `{ label, changed }` con el conteo y una prueba en el arnés de veracidad. studio-engine-bridges.spec.ts no cambia desde ca1cd96d. La bitácora 8a388571 marca «D-04 (vis3d) | HECHA». Los goldens e2e/golden/47-cad-solids.spec.ts:245 y :258 citan el texto «Estilo visual: …».
- **Arreglo:** Haz que CadSolidShadeHost.applyVisualStyle (solid-shade-host.ts:310) y la fachada de native-mass-hosts.ts:47 devuelvan `{ label, objects }` con el número de mallas o entidades realmente reestilizadas. En el puente, suma los dos conteos y, si es 0, devuelve `${label} (0 objetos: este dibujo no tiene sólidos sombreables)`. Borra la condición actual de la línea 168. Añade casos a studio-engine-bridges.spec.ts: visor de sólidos montado con 0 objetos, que debe incluir «0 objetos», y con 1 objeto, que no debe incluirlo. Mantén intacto el formato «Estilo visual: Alámbrico.» cuando hay objetos, o actualiza en el mismo commit e2e/golden/47-cad-solids.spec.ts:245 y :258, que citan ese texto.

### ui-cinta-persistencia-2 · [media] Ningún spec prueba la persistencia: las «22 aserciones» son anteriores y no tocan localStorage

- **Fichero:** `apps/web/src/lib/cad/plot/plot-style-catalog.spec.ts:65`
- **Problema:** El commit 069d575d cita «plot-style-catalog.spec 22 aserciones» como prueba, pero no toca el spec. Las 22 aserciones ya existían y ninguna guarda, recarga ni simula localStorage. En 361d7db3, «linetype.spec pasa» tampoco dice nada, porque ese spec no importa CadLinetypeCatalog. El hallazgo 1 demuestra que un spec de ida y vuelta habría cazado el fallo.
- **Evidencia:** `git log 52898e03 -- plot-style-catalog.spec.ts` muestra como último cambio f1e8623a, anterior a MiMo. La sección 6 (64-75) crea un solo catálogo y nunca crea otro para comprobar la restauración. `git grep CadLinetypeCatalog\|localStorage` en linetype.spec.ts no encuentra nada, y session-catalogs.spec.ts no usa localStorage.
- **Verificación independiente:** Confirmado. 069d575d sólo toca plot-style-catalog.ts (--stat: 1 fichero), y el último cambio de plot-style-catalog.spec.ts es f1e8623a, anterior a MiMo. Conté las aserciones del spec: 4+4+4+2+2+6 = 22, las mismas que ya había. La sección 6 (spec:64-75) crea un único CadPlotStyleCatalog y nunca crea un segundo para comprobar la restauración. En todo el spec no aparece localStorage. linetype.spec.ts (último cambio a1555db1) sólo importa ./linetype (applyLinetype, LINETYPES…) y no CadLinetypeCatalog, así que «linetype.spec pasa» no prueba nada de 361d7db3. session-catalogs.spec.ts:119-127 usa un solo catálogo y no toca localStorage (grep vacío).
- **Arreglo:** Crea specs de persistencia para los dos catálogos con un almacén falso instalado en globalThis.localStorage y retirado al final. Deben cubrir: (a) cargar, crear un catálogo nuevo y encontrar la tabla o tipo con el mismo contenido; (b) JSON corrupto en la clave, sin excepción y sólo con lo de fábrica; (c) setItem que lanza (cuota llena), sin excepción; (d) un sustituto de fábrica (acad.ctb, DASHED) que sobrevive a la recarga. Comprueba a mano que fallan con el código actual antes de arreglarlo.

### ui-cinta-persistencia-3 · [media] Un acad.ctb o monochrome.ctb propio sustituye al de fábrica en la sesión, pero no se guarda

- **Fichero:** `apps/web/src/lib/cad/plot/plot-style-catalog.ts:101`
- **Problema:** saveToStorage descarta toda clave que coincida con una tabla de fábrica. El propio módulo documenta y prueba que cargar un acad.ctb del despacho sustituye al de fábrica («su acad.ctb es el bueno»), así que ese sustituto se pierde al recargar. PAGESETUP sigue nombrando acad.ctb o monochrome.ctb y PLOT encuentra la de fábrica sin avisar: el plano sale con los grosores de fábrica en vez de los del despacho. Es justo el caso más habitual, porque los despachos suelen tener su propio monochrome.ctb.
- **Evidencia:** plot-style-catalog.ts:22-26 dice «Cargar un `acad.ctb` propio SUSTITUYE al de fábrica». plot-style-catalog.spec.ts:73 hace `catalogo.load(createCadColorTable("acad"))`. saveToStorage en :100-101 hace `if (!cadBuiltinPlotStyleTables().has(key)) custom[key] = table;`, así que la clave acad.ctb nunca se guarda.
- **Verificación independiente:** Confirmado. plot-style-catalog.ts:24-25 documenta que «Cargar un `acad.ctb` propio SUSTITUYE al de fábrica», y spec:73 lo ejercita. load (:68-72) hace set sobre la clave `${name}.${kind}` y luego saveToStorage, que en :100-101 excluye toda clave que exista en cadBuiltinPlotStyleTables(): acad.ctb, monochrome.ctb y acad.stb. El camino desde la interfaz existe: session-catalogs.ts:198-212 quita la extensión del nombre del archivo e importa, así que un monochrome.ctb del despacho produce la clave monochrome.ctb y no se guarda. Al recargar, el constructor (:56-59) reconstruye la de fábrica y find() la devuelve con el mismo nombre.
- **Arreglo:** En saveToStorage guarda toda tabla cargada con load(), incluidas las de nombre de fábrica. Por ejemplo, lleva un Set `userLoaded` con las claves que pasaron por load() y serializa exactamente esas. Al restaurar, esas entradas sustituyen a las de fábrica, que es el comportamiento documentado. Añade al spec de persistencia: cargar un acad.ctb con styles[6].lineweight=0.7, crear un catálogo nuevo y comprobar que find('acad.ctb') devuelve 0.7.

### ui-cinta-persistencia-4 · [media] Los tipos de línea que sustituyen a los de fábrica (acadiso.lin) no se guardan

- **Fichero:** `apps/web/src/lib/cad/linetype-lin.ts:217`
- **Problema:** saveToStorage filtra todo tipo cuyo nombre coincida con uno de fábrica. Un .lin real (acadiso.lin o el del despacho) redefine DASHED, HIDDEN, CENTER, PHANTOM, DOT, DASHDOT, DIVIDE y BORDER con patrones métricos, y `save` los sustituye en la sesión. Al recargar, aunque se corrija el hallazgo 1, vuelven los patrones de fábrica en pulgadas (Dashed [0.5,-0.25]) sin avisar. Además restoreFromStorage hace `push` sin quitar duplicados, así que restaurar un sustituto dejaría dos 'Dashed'.
- **Evidencia:** linetype-lin.ts:216-218 hace `this.items.filter((item) => !CadLinetypeCatalog.isBuiltin(item.name))`; isBuiltin (211-213) compara sólo el nombre en mayúsculas. La línea 52 define `{ name: "Dashed", ..., pattern: [0.5, -0.25] }`. save (183-190) sustituye por nombre. La línea 231 hace `this.items.push(item)`.
- **Verificación independiente:** Confirmado. saveToStorage (linetype-lin.ts:216-218) filtra con isBuiltin (:211-213), que compara sólo el nombre en mayúsculas contra CAD_BUILTIN_LINETYPES. :52 define Dashed con [0.5,-0.25]. save (:184-188) sustituye por nombre sin distinguir mayúsculas, así que cargar un .lin con *DASHED (session-catalogs.ts:187-190 → load → save) cambia el patrón en la sesión, pero ese cambio no se serializa. restoreFromStorage (:231) hace `this.items.push(item)` sin quitar duplicados. Hoy ese duplicado es hipotético porque la restauración no funciona (hallazgo 1), pero aparecería en cuanto se corrija el campo.
- **Arreglo:** Guarda los tipos cuyo patrón o descripción difieran del de fábrica con el mismo nombre (compara con CAD_BUILTIN_LINETYPES), no sólo los de nombre nuevo. Al restaurar, sustituye por nombre en mayúsculas en lugar de hacer push. Spec: cargar '*DASHED,Trazos ISO\nA,12.7,-6.35', crear un catálogo nuevo y comprobar que get('DASHED').pattern es [12.7,-6.35] y que sólo hay una entrada DASHED.

### ui-cinta-persistencia-5 · [media] Las claves nuevas no llevan inquilino ni usuario: las tablas de un despacho aparecen en otra cuenta del mismo navegador

- **Fichero:** `apps/web/src/components/cad/command-line/session-catalogs.ts:97`
- **Problema:** useCadSessionState recibe el scope (tenantId, userId) y lo usa para las paletas de herramientas, pero construye CadLinetypeCatalog y CadPlotStyleCatalog sin él. Las claves son fijas (valle:cad:linetype-catalog y valle:cad:plot-style-tables). Si otro usuario u otro inquilino entra en el mismo navegador (un equipo compartido del despacho, o un consultor con dos inquilinos), hereda los .ctb y .lin propios del anterior. PAGESETUP o PLOT pueden incluso resolver con la tabla ajena un nombre que coincida.
- **Evidencia:** session-catalogs.ts:97-98 hace `new CadLinetypeCatalog()` y `new CadPlotStyleCatalog()` sin argumentos, mientras que :101 hace `loadCadToolPalettes(storage(), scope)`. tool-palettes.ts:58-63 usa la clave `valle_cad_toolpalettes:${tenantId}:${userId}`. command-session.ts:42 también usa scope en `valle:cad:command-history:v1`.
- **Verificación independiente:** Confirmado con un matiz. Las claves nuevas son constantes y no llevan ámbito: plot-style-catalog.ts:90 y linetype-lin.ts:209. Los constructores no reciben argumentos (session-catalogs.ts:97-98). En cambio, las paletas sí usan clave con ámbito (tool-palettes.ts:62), igual que el historial de comandos (command-session.ts:42) y la biblioteca LISP (library-storage.ts:126). No encontré ningún código que limpie las claves valle: al cerrar sesión. El matiz: la única llamada, use-command-engine.ts:358, hace `useCadSessionState()` sin scope, así que en la práctica las paletas también acaban en «tenant:user». El arreglo propuesto tiene que pasar además el scope desde el anfitrión del motor; con cambiar session-catalogs.ts:97-98 no basta. El problema de fondo es real: otra cuenta en el mismo navegador hereda los .ctb y .lin.
- **Arreglo:** Haz que los constructores reciban `{ storage, key }` o el scope, y construye la clave como `valle:cad:plot-style-tables:v1:${tenantId||'tenant'}:${userId||'user'}` (lo mismo para linetype-catalog). Pasa el scope desde session-catalogs.ts:97-98. Deja los constructores sin argumentos con un almacén en memoria para specs y SSR, de modo que las pruebas no dependan del localStorage global.

### ui-cinta-persistencia-6 · [baja] El JSON leído no tiene versión y la validación de tablas es superficial

- **Fichero:** `apps/web/src/lib/cad/plot/plot-style-catalog.ts:114`
- **Problema:** restoreFromStorage acepta cualquier objeto con las propiedades 'name' y 'kind'. No comprueba que kind sea 'ctb' o 'stb', que styles sea un array (255 entradas en una CTB) ni que la clave coincida con `${name}.${kind}`. Una entrada vieja o manipulada entra al catálogo y falla más tarde, al trazar, en vez de al cargar. Una clave 'acad.ctb' guardada pisaría la de fábrica sin control. Ninguna de las dos claves lleva versión (otras del repo sí: valle:cad:command-history:v1, valle:cad:tour:v1), así que un cambio futuro del formato no podrá distinguir datos viejos.
- **Evidencia:** plot-style-catalog.ts:114 hace `if (table && typeof table === "object" && "name" in table && "kind" in table) this.loaded.set(key, table)`. CadPlotStyleTable (plot-style-table.ts) exige kind, name, description, scaleFactor, applyScaleFactor, styles[] y customLineweights[]. Claves en plot-style-catalog.ts:90 y linetype-lin.ts:209, sin :v1.
- **Verificación independiente:** Confirmado, severidad baja. plot-style-catalog.ts:114 sólo comprueba `"name" in table && "kind" in table` y mete la entrada con la clave guardada, sin comprobar que coincida con `${name}.${kind}`. CadPlotStyleTable (plot-style-table.ts:74-86) exige además description, scaleFactor, applyScaleFactor, styles[] y customLineweights[]. En contraste, las paletas pasan por normalizeCadToolPalettes (tool-palettes.ts:145). Ninguna de las dos claves lleva versión (:90 y linetype-lin.ts:209). Matiz: la versión no es una convención universal del repo, porque valle_cad_toolpalettes tampoco la lleva, aunque command-history y tour sí. La parte de validación superficial se sostiene sola.
- **Arreglo:** Guarda como `{ version: 1, tables: {...} }` (y `{ version: 1, linetypes: [...] }`) en claves con :v1. Al leer, descarta lo que no tenga version===1 y valida cada tabla: kind en {ctb, stb}, name string, clave === `${name}.${kind}`, styles array (255 en una CTB) con lineweight y screen numéricos, customLineweights array de números. Descarta las entradas inválidas en vez de meterlas al catálogo.

### ui-cinta-persistencia-8 · [baja] La cinta descarta el espejo perdido sin publicarlo y los comentarios siguen diciendo que la carga revienta

- **Fichero:** `apps/web/src/lib/cad/ribbon.ts:283`
- **Problema:** El arreglo D5 de la auditoría pedía dejar fuera el espejo y publicarlo en cadRibbonPanelFallbacks(), el canal de «esto no cuadró». El commit sólo hace `continue`, así que en producción el espejo perdido no deja rastro consultable. En CI sigue cazándolo ribbon.spec.ts, que compara el panel Anotación con la lista exacta. Además dos comentarios afirman un comportamiento que ya no existe: ribbon.ts:275 («se dice en voz alta al cargar») y ribbon-order.ts:79-81 («hace saltar `buildRibbonTabs` al cargar»).
- **Evidencia:** ribbon.ts:283 `if (!original) continue; // D5: degradación correcta...`. 03-ribbon-ux.md §D5: «la degradación correcta es dejar fuera el espejo y publicarlo en `cadRibbonPanelFallbacks()`». ribbon-order.ts:80 «nombre que no exista en el registro hace saltar `buildRibbonTabs` al cargar».
- **Verificación independiente:** Confirmado, severidad baja. ribbon.ts:283 (99a8282e) sólo hace `if (!original) continue;` y no publica nada. El canal existe (RIBBON_PANEL_FALLBACKS en ribbon.ts:212-214, con push en :260) y 03-ribbon-ux.md §D5 (línea ~1019) pide expresamente publicar ahí el espejo. Los comentarios están desfasados: ribbon.ts:275 dice «se dice en voz alta al cargar» y ribbon-order.ts:80 dice «hace saltar `buildRibbonTabs` al cargar». En CI, ribbon.spec.ts:79-83 compara Anotación con la lista exacta de seis, así que CI lo sigue cazando. check-ribbon-coverage.mjs no comprueba que los espejos existan: sólo los menciona en la línea 27.
- **Arreglo:** En ribbon.ts:283 cambia `continue` por `{ RIBBON_PANEL_FALLBACKS.push(`espejo:${name}`); continue; }`, o por una lista exportada aparte cadRibbonMissingMirrors() que ribbon.spec.ts exija vacía. Corrige los comentarios de ribbon.ts:274-275 y ribbon-order.ts:79-81 para que digan que el espejo se omite, se publica en esa lista y el spec lo hace fallar en CI.

### ui-cinta-persistencia-9 · [baja] El comentario de cabecera de session-catalogs sigue diciendo que sólo sobreviven las paletas

- **Fichero:** `apps/web/src/components/cad/command-line/session-catalogs.ts:21`
- **Problema:** La cabecera del módulo existe para ser «UN sitio donde mirar qué sobrevive a una recarga», y dice que hoy sólo sobreviven las paletas de herramientas y que lo demás dura lo que la pestaña. Tras 069d575d y 361d7db3, las tablas de plumas (y, con el hallazgo 1 corregido, los tipos de línea) también sobreviven. El comentario engaña a quien busque dónde se persiste.
- **Evidencia:** session-catalogs.ts:19-21 «Hoy: sólo las paletas de herramientas, que se guardan por inquilino con la misma clave que las preferencias del espacio de trabajo. Lo demás dura lo que la pestaña.»
- **Verificación independiente:** Confirmado. session-catalogs.ts:18-21 dice que así hay «UN sitio donde mirar qué sobrevive a una recarga y qué no. Hoy: sólo las paletas de herramientas, […] Lo demás dura lo que la pestaña.» Tras 069d575d, CadPlotStyleCatalog (construido en :98) guarda y restaura desde localStorage, y 361d7db3 intenta lo mismo con CadLinetypeCatalog (:97). Ninguno de los dos commits toca session-catalogs.ts, así que la cabecera ya no describe lo que persiste.
- **Arreglo:** Actualiza session-catalogs.ts:17-21 para enumerar lo que persiste: paletas de herramientas, tablas de plumas y tipos de línea cargados, con sus claves y su ámbito. Hazlo en el mismo commit que corrija el ámbito de las claves (hallazgo 5).

## Núcleo 3D: EXTRUDE, kernel y designación de aristas (3a980e89, f86b1fc3, 1be2676e; revisado en 52898e03)

**Resumen del revisor:** Ninguno de los tres commits rompe specs ni goldens, pero ninguno hace del todo lo que dice. Specs y goldens: los goldens 47, 101 y 112 teclean la altura; el spec solids-create.spec.ts:131 sigue dando una altura distinta de cero (−433), así que llega al mismo rechazo; el kernel y el servidor aceptan altura negativa. D-05: el cambio a la componente Y sólo tiene sentido en la vista en planta 2D. El punto que llega es un punto del plano de trabajo, no un gesto de pantalla. En isométrica, con un SCP vertical o con un punto 3D con cota, la Y no mide la altura y la z se descarta. El comentario dice falsamente que Y «es la proyección en el eje Z del dibujo», y el cambio no tiene ninguna aserción propia. D-07: los siete archivos existen e importan del kernel, pero no son «los siete consumidores reales». Hay 30 importadores de producto; la lista omite solid3d-build.ts, que es quien tesela para el visor, e incluye un import que sólo trae un tipo. Además el título sigue diciendo «QUÉ NO ESTÁ CONECTADO». T4: edge-ray.ts y solid-edge-ref.ts son código muerto, cosa que el commit admite; nadie los importa fuera de sus specs. Tienen además errores de fondo. En edge-ray.ts, `t` no se recalcula al acotar `u`, así que la distancia sale mayor que la real. Las aristas paralelas al rayo nunca se detectan y no hay desempate por profundidad. Los casos 1 y 3 del spec aciertan con la arista equivocada y pasan igual. En solid-edge-ref.ts la huella se compara con igualdad exacta y un paso que depende de la envolvente. Por eso una arista que no se movió deja de reconocerse en cuanto cambia el tamaño del cuerpo. Su spec no re-evalúa nada, a pesar de que su cabecera dice que sí.

**Afirmaciones de tus commits que no se cumplen:**

- D-05: la componente Y es la proyección de la dirección de arrastre en el eje Z del dibujo, «el eje vertical de pantalla» (comentario solids-create.ts:291-293)
  - Evidencia: El punto llega del plano de trabajo en coordenadas de dibujo (pointer-router.ts:133-142 «Trae cota cuando ese plano NO es el del suelo»). En planta la vertical de pantalla es la Y del mundo, y la extrusión va por +Z (planeFrameAt, solids-create.ts:219). En isométrica el punto cae en el suelo y con un SCP vertical (UCS X/Y/Cara, ucs-commands.ts:75-83) la y es constante. La z del punto (command-engine-host.ts:360) se ignora: la firma es CadPoint2 (línea 299).
- D-07: enumera los siete consumidores reales del producto
  - Evidencia: Los siete archivos existen e importan de brep. Pero `git grep` encuentra 30 ficheros de producto (sin specs) que importan del kernel. La lista omite solid3d-build.ts:710 (`entry.mesh ??= tessellateBody(entry.body)`, el teselado real del visor) y solid-face-ref.ts, que cita la auditoría D7. solid-snap-host.ts:30 sólo hace `import type { BrepBody }`. flatshot-solids, solview-model, face-ray y solid3d-three no llaman a tessellateBody, aunque la frase se lo atribuye. El título sigue siendo «QUÉ NO ESTÁ CONECTADO».
- T4: edge-ray.ts calcula el rayo contra segmentos de media arista con distancia mínima
  - Evidencia: edge-ray.ts:111 acota u sin recalcular t, así que la distancia no es la mínima cuando el punto más cercano está en un extremo. La rama paralela (105-106) fija t=0 y queda descartada siempre por `result.t > 0` (153).

### nucleo-3d-1 · [media] EXTRUDE por punto: la componente Y sólo vale en planta; en 3D o con SCP vertical no mide la altura y descarta la z

- **Fichero:** `apps/web/src/lib/cad/engine/commands/solids-create.ts:306`
- **Problema:** heightFromPoint toma `point.y - centre.y`, pero el punto que recibe el comando está en coordenadas de dibujo sobre el plano de trabajo; no es un desplazamiento de pantalla. La extrusión va siempre por +Z (planeFrameAt) y la Y del mundo nunca mide esa dirección. Casos que fallan: (1) En planta, con un rectángulo de 4000×3000 en el origen (centro 2000,1500), señalar (6000,1500) da altura 0 y la orden termina con «EXTRUDE necesita una altura distinta de cero». Enganchar el punto medio del lado inferior (2000,0) extruye 1500 hacia ABAJO. (2) En isométrica el punto cae en el suelo, así que «arriba en pantalla» es la diagonal +X+Y y la altura depende del rumbo. (3) Con un SCP vertical (UCS X/Y 90° o Cara sobre una fachada) todos los puntos del plano tienen la misma y: mover el ratón arriba o abajo no cambia la altura. (4) Un punto con cota (SCP inclinado o enganche a un vértice de otro sólido) trae z, que es la única componente que mide altura, y se ignora. El comentario de las líneas 291-293 afirma lo contrario.
- **Evidencia:** solids-create.ts:299 `function heightFromPoint(point: CadPoint2, ...)` y :306 `return point.y - centre.y;`; :219 `frame: planeFrameAt(extracted.elevation)`; pointer-router.ts:133-142 «Punto de DIBUJO bajo el evento … Trae cota cuando ese plano NO es el del suelo»; command-engine-host.ts:360 `pickPoint(point: { x; y; z? })`.
- **Verificación independiente:** Confirmado en lo esencial, con un caso refutado. solids-create.ts:306 devuelve `point.y - centre.y` y la extrusión va siempre por +Z (:219 `planeFrameAt`, que en solid3d-profiles.ts:153-158 fija zAxis (0,0,1)). Caso (1) en planta: rectángulo 4000×3000 con centro (2000,1500). El punto (6000,1500) da altura 0 y :203-204 responde «necesita una altura distinta de cero». El punto (2000,0) da −1500. Caso (4), enganche a un sólido: CadSolidSnapResult trae `point: CadSnapPoint3` «con su cota» (view/solid-snap.ts:88-89). pointer-router.ts:142-147 y command-engine-host.ts:360 dejan pasar la z, y heightFromPoint (tipado CadPoint2) la ignora. Caso (2), isométrica: worldPoint da un punto sobre el plano de trabajo, así que la Y no es «arriba en pantalla». El comentario de :291-293 («la componente Y es la proyección … en el eje Z del dibujo») sólo vale en planta. REFUTADO: el caso (3) y la parte de «SCP inclinado» del caso (4). EXTRUDE es mutates y no declara `spatial` (solids-create.ts:237-246; command-types.ts:633). Por eso command-engine.ts:432-455 rechaza el punto antes de llegar al comando cuando el SCU está inclinado o elevado (system-variables.ts:426-450). Con un SCU vertical la orden nunca recibe esos puntos.
- **Arreglo:** En solids-create.ts, cambia la firma de heightFromPoint a `CadPoint2 | CadPoint3`. Si el punto trae z finita, devuelve `point.z - extracted.elevation`. Si no trae z, conserva la convención de la Y sólo cuando no hay SCP activo con eje Z distinto del universal (cadActiveUcs(context.variables)). En cualquier otro caso responde con solidMessage pidiendo teclear la altura, en vez de inventarla. Reescribe el comentario de las líneas 286-298 para que diga exactamente eso y quita la frase de que Y es «el eje Z del dibujo».

### nucleo-3d-2 · [media] D-05 marcada HECHA sin ninguna aserción; el spec por punto pasa con cualquier fórmula y hay comentarios caducos

- **Fichero:** `apps/web/src/lib/cad/engine/commands/solids-create.spec.ts:131`
- **Problema:** Ningún spec comprueba el signo ni la magnitud de la altura por punto. El único caso por punto (línea 131) usa el perfil inclinado y sólo exige el mensaje de rechazo, al que se llega con hypot, con Y o con cualquier altura distinta de cero. Su comentario (129-130) sigue diciendo «la altura se toma de la distancia al centro del perfil». En solids-create.ts:285 quedó el JSDoc viejo («su separación del centro del perfil») justo encima del nuevo, contradiciéndolo.
- **Evidencia:** solids-create.spec.ts:131 `run("EXTRUDE", [select("rampa"), point(2000, 0)], ...)` sólo con `ok(REASON.test(byPoint), ...)`; solids-create.ts:285 `/** Altura implícita al precisar un punto: su separación del centro del perfil. */` seguido del bloque nuevo en 286.
- **Verificación independiente:** Confirmado. El único EXTRUDE por punto de todos los specs es solids-create.spec.ts:131, con el perfil inclinado. El centro de la «rampa» tiene y≈433, así que point(2000,0) da −433 con Y y ≈1950 con hypot. Las dos alturas son distintas de cero, pasan el filtro de :203 y llegan al mismo rechazo por inclinación (:207-208), así que la aserción `REASON.test` pasa con cualquier fórmula. solids.spec.ts:158-194 sólo usa distance() para EXTRUDE. El e2e 112-auditoria-linea-comandos-tapa.spec.ts:134/149 sólo espera el prompt «altura de la extrusión». Nada comprueba signo ni magnitud. Los comentarios caducos existen: solids-create.ts:285 (JSDoc viejo «su separación del centro del perfil» encima del nuevo en :286) y solids-create.spec.ts:129-130 («distancia al centro del perfil»). NOCHE_MIMO_20260914.md:370 marca D-05 como HECHA.
- **Arreglo:** En solids-create.spec.ts añade un bloque con rectangle("losa", 400, 300, 0). point(200, 450) debe escribir un sólido con bounds.min.z = 0 y bounds.max.z = 300. point(200, -150) debe dar bounds.min.z = -300 y bounds.max.z = 0, con volumen 400·300·300. Cuando exista el arreglo de nucleo-3d-1, añade un punto con z = 500 que dé altura 500. Borra el JSDoc de solids-create.ts:285 y corrige el comentario de solids-create.spec.ts:129-130.

### nucleo-3d-3 · [media] raySegmentDistance no es la distancia mínima: acota u sin recalcular t, y las aristas paralelas al rayo nunca se detectan

- **Fichero:** `apps/web/src/lib/cad/pick3d/edge-ray.ts:111`
- **Problema:** Tras `u = Math.max(0, Math.min(1, u))` se conserva la t de la recta infinita, de modo que la distancia devuelta es |P(t sin acotar) − Q(u acotado)|, mayor que la real siempre que el punto más cercano del segmento es un extremo. El filtro `result.t > 0` (153) se evalúa con esa t falsa. Ejemplo: segmento (0,0,0)–(100,0,0) y rayo con origen (150,−50,5) y dirección (1,1,0). El código da t=50, u=2→1 y distancia 100,12; la real, al extremo (100,0,0) con t=0, es 70,89. Con maxDistance 80 no designa nada. Cerca de una esquina las tres aristas se acotan y el orden sale de distancias falsas. La rama paralela (105-106) fija t=0 y u=0,5: `t > 0` la descarta siempre, así que ninguna arista paralela al rayo es designable (todas las verticales en planta ortográfica).
- **Evidencia:** edge-ray.ts:103-112 (rama paralela `t = 0; u = 0.5;` y `u = Math.max(0, Math.min(1, u));` sin recalcular t); :153 `if (result.distance < maxDist && result.t > 0)`.
- **Verificación independiente:** Confirmado. En edge-ray.ts:108-111 se acota u y se conserva la t de la recta infinita. Recalculo el ejemplo: w0=(150,−50,5), a=2, b=100, c=10000, d=100, e=15000, denom=10000. Sale t=50 y u=2, que se acota a 1. P=(200,0,5) y Q=(100,0,0) dan una distancia de 100,12. El mínimo real con u=1 está en t*=0: distancia √5025≈70,89. Con maxDistance 80 no designa. La rama paralela (:103-106) fija t=0 y u=0,5, y el filtro `result.t > 0` (:153) la descarta siempre. Una arista vertical con rayo (0,0,−1) da denom=a·c−b²=0 y nunca es designable. Matiz sobre el arreglo propuesto: en ese ejemplo el mínimo real está en t=0, así que con el filtro estricto `t > 0` seguiría sin designar. Habría que usar t ≥ 0 o cambiar el ejemplo. Eso no invalida el defecto.
- **Arreglo:** En raySegmentDistance, después de acotar u recalcula `t = (u*b - d) / a`. Si t < 0, fija t = 0 y recalcula `u = clamp((e + t*b) / c, 0, 1)`. En la rama paralela no fijes t = 0: toma `u = clamp(e / c, 0, 1)` y `t = max(0, (u*b - d) / a)`, y calcula la distancia real. Añade a edge-ray.spec.ts el ejemplo del segmento (0,0,0)–(100,0,0) con el rayo (150,−50,5)+(1,1,0): distance ≈ 70,89 y designa con maxDistance 80. Añade también una arista paralela al rayo a 5 unidades que se designe con maxDistance 10.

### nucleo-3d-4 · [media] edge-ray.spec: los casos 1 y 3 aciertan con la arista equivocada y pasan; no hay desempate por profundidad

- **Fichero:** `apps/web/src/lib/cad/pick3d/edge-ray.spec.ts:63`
- **Problema:** Caso 1 (líneas 12-28): dice probar la arista vertical x=1000, y=0, z=0..500, pero es paralela al rayo y la rama paralela la descarta. hitEdge devuelve la arista 2, (1000,600,0)–(1000,0,0), por su extremo (1000,0,0), y el spec pasa porque sólo mira `point.x > 999`. Caso 3 (47-64): dice que el rayo pasa por la arista y=0. Con el orden de makeBox, la arista 1, (0,200,0)–(200,200,0), a t=300 y detrás, empata a distancia 0 con la arista 3, a t=100. Gana por índice porque la línea 154 compara sólo la distancia, así que devuelve el punto (100,200,0) y el spec sólo comprueba x. Falta el «desempate por profundidad» que pedía la H3 (01-3d-modelado.md:349-350): hitEdge prefiere la arista oculta.
- **Evidencia:** edge-ray.spec.ts:17 «Rayo que pasa a 50 unidades de la arista en x=1000, y=0, z=0..500» con check :27 `hit.point.x > 999`; :53 «punto medio (100, 0, 0)» con check :63 `Math.abs(hit.point.x - 100) < 1`; edge-ray.ts:154 `if (!best || result.distance < best.distance)`; primitives.ts:81-89 (cara inferior [0,3,2,1] crea primero las aristas 0→3, 3→2, 2→1, 1→0).
- **Verificación independiente:** Confirmado trazando el orden de aristas: body-builder.ts:119-121 crea una arista nueva sólo en la primera media arista, y primitives.ts:86-89 da el orden de caras. Queda 0:0→3, 1:3→2, 2:2→1, 3:1→0, 4-7 las superiores, 8:1→5, 9:4→0, 10:2→6 y 11:3→7. Caso 1: la arista 8, (1000,0,0)–(1000,0,500), es paralela al rayo (0,0,−1) y se descarta. La arista 2 da t=250, u=1 y distancia 50. La arista 3 empata a 50, pero `<` estricto (:154) conserva la 2. El punto (1000,0,0) pasa el `x > 999` de spec:27. Caso 3: la arista 1 (3→2) da t=300 y distancia 0, y la arista 3 (1→0) da t=100 y distancia 0. Gana la 1 por índice, con punto (100,200,0), y spec:63 sólo mira x. La H3 exige «desempate por profundidad» (01-3d-modelado.md:350) y hitEdge no lo tiene. Matiz: «prefiere la arista oculta» generaliza de más. Prefiere el índice menor, que en este ejemplo es la lejana.
- **Arreglo:** En hitEdge, cuando dos candidatas estén a menos de scale·1e-9 en distancia, quédate con la de menor t. En edge-ray.spec.ts comprueba el ÍNDICE resuelto contra los extremos esperados con halfEdgeSegment: caso 1, (1000,0,0)–(1000,0,500) tras el arreglo de nucleo-3d-3; caso 3, (0,0,0)–(200,0,0). Comprueba también point.y y point.z. Añade el caso que pide la H3: un rayo isométrico que designe la arista superior frontal de una caja y no una oculta.

### nucleo-3d-5 · [media] solid-edge-ref: la huella con igualdad exacta y paso ligado a la envolvente no reconoce una arista que no se movió

- **Fichero:** `apps/web/src/lib/cad/pick3d/solid-edge-ref.ts:101`
- **Problema:** refsMatch compara con `===` coordenadas cuantizadas con `Math.round(v/step)*step`. El paso es `max(1e-7, diagonal·1e-9)`, así que depende de la envolvente del cuerpo evaluado. Si otra parte del árbol cambia el tamaño del cuerpo, cambia el paso y todas las coordenadas se recuantizan a otros flotantes. Ejemplo: la huella de la arista (0,0,0)–(0,0,500) sacada de makeBox 1000×600×500 (paso ≈1,27e-6) no casa en makeBox 1200×600×500 (paso ≈1,39e-6), aunque la arista es idéntica. El índice falla, la búsqueda da 0 candidatas y responde «ninguna arista del cuerpo coincide». Con el mismo paso, además, dos evaluaciones a ambos lados de un borde de cubeta (una deriva de 1e-13) también fallan, que es justo lo que la cabecera (líneas 8-9) dice eliminar. solid-face-ref.ts (cadFaceMatchesRef) lo evita comparando con tolerancia `step * 4`. El diedro se cuantiza con un paso LINEAL (línea 89, `step * 0.01`) y luego se compara con 0,01 rad, con lo que esa cuantización no sirve de nada.
- **Evidencia:** solid-edge-ref.ts:48 `return Math.round(value / step) * step + 0;`; :52-56 linearStep dependiente de aabbDiagonal(bodyBounds(body)); :101 `a.fromX === b.fromX && ...`; :89 `quantize(dihedral, step * 0.01)`; frente a solid-face-ref.ts cadFaceMatchesRef `Math.abs(actual.centroid.x - ref.centroid.x) > step * 4`.
- **Verificación independiente:** Confirmado. quantize (solid-edge-ref.ts:46-49) redondea al múltiplo más cercano de un paso que depende de la diagonal (:51-55). refsMatch (:101-102) compara con `===`. Caja 1000×600×500: diagonal≈1268,9 y paso≈1,269e-6. Caja 1200×600×500: diagonal≈1431,8 y paso≈1,432e-6 (el revisor dice 1,39e-6, error menor). El 500 se desplaza hasta medio paso, distinto en cada caso, así que toZ no coincide. La vía por índice (:120-122) y la búsqueda (:127-130) dan 0 candidatas y responden «ninguna arista del cuerpo coincide». Con el mismo paso, dos valores a ambos lados de .5·paso también divergen, contra lo que dice la cabecera (:7-8). La hermana solid-face-ref.ts:177-180 compara con tolerancia `step * 4`. El diedro se cuantiza con `step * 0.01` (:89), del orden de 1e-8, y se compara con 0,01 rad (:105): esa cuantización no aporta nada.
- **Arreglo:** En refsMatch compara cada extremo con tolerancia: `Math.abs(a.fromX - b.fromX) <= 4 * max(pasoDeLaRef, pasoDelCuerpo)`, o compara distancias con BREP_TOLERANCE.linear escalado como en cadFaceMatchesRef. Prueba también el orden inverso de extremos por si el orden canónico cambió por deriva. Quita la cuantización del diedro con el paso lineal y compáralo con una tolerancia angular explícita. Añade a solid-edge-ref.spec.ts el caso makeBox 1000×600×500 → makeBox 1200×600×500 con la arista (0,0,0)–(0,0,500): debe resolver.

### nucleo-3d-6 · [media] solid-edge-ref.spec no re-evalúa el cuerpo ni mueve aristas, aunque su cabecera dice que lo verifica

- **Fichero:** `apps/web/src/lib/cad/pick3d/solid-edge-ref.spec.ts:4`
- **Problema:** La cabecera promete verificar el reconocimiento «tras re-evaluar el cuerpo» y que «una arista movida se cura o se rechaza». Los tres casos usan el MISMO objeto body. La curación se prueba con `edge: -1`, que ni siquiera pasa por la vía del índice, y el rechazo con coordenadas 99999. El spec pasaría aunque canonicalEndpoints no ordenara, aunque se ignorara el diedro o la cuantización, y no detecta nucleo-3d-5.
- **Evidencia:** solid-edge-ref.spec.ts:4 «se reconoce a sí misma tras re-evaluar el cuerpo»; :12 un único makeBox; :23 `{ ...ref, edge: -1 }`; :31 `fromX: 99999, ...`.
- **Verificación independiente:** Confirmado. solid-edge-ref.spec.ts:12 crea un único makeBox y los tres casos resuelven contra ese mismo objeto. No hay re-evaluación, aunque la cabecera :4-5 la promete. El caso 2 (:23, `edge: -1`) no pasa por la vía del índice, que exige `ref.edge >= 0` (solid-edge-ref.ts:120). El caso 3 (:31) usa coordenadas 99999. Con el mismo cuerpo, la huella sale idéntica aunque canonicalEndpoints no ordenara, o aunque se ignoraran el diedro o la cuantización. Tampoco se ejercitan el cambio de envolvente ni la deriva, así que no detecta nucleo-3d-5.
- **Arreglo:** En solid-edge-ref.spec.ts añade estos casos. (a) Una huella sacada de un cuerpo y resuelta contra otro construido de nuevo con translateBody(+δ) y translateBody(−δ), con δ no representable (0.1), para que haya deriva. (b) El caso de envolvente cambiada de nucleo-3d-5. (c) El índice válido de OTRA arista con la huella de la 0: debe dar healed=true y edge=0. (d) Una huella con from y to intercambiados, que debe resolver igual. (e) Una huella con dihedralRad alterado 0,1 rad, que debe fallar. Corrige la cabecera si algún caso no se cubre.

### nucleo-3d-7 · [baja] Cabecera del kernel (D-07): sigue titulada «QUÉ NO ESTÁ CONECTADO» y su lista de consumidores es incompleta y engañosa

- **Fichero:** `apps/web/src/lib/brep/index.ts:11`
- **Problema:** El párrafo describe lo que SÍ está conectado bajo el título «QUÉ NO ESTÁ CONECTADO». Atribuye el consumo de `tessellateBody` a una lista de archivos de los que sólo room-solid-three y wall-solid-three lo llaman. solid-snap-host.ts sólo importa un tipo, que desaparece al compilar. Omite solid3d-build.ts, que es quien tesela para el visor. Hay 30 ficheros de producto que importan del kernel, no «los siete consumidores reales» que anuncia el commit.
- **Evidencia:** index.ts:11-15; solid-snap-host.ts:30 `import type { BrepBody } from "@/lib/brep"`; solid3d-build.ts:710 `entry.mesh ??= tessellateBody(entry.body)`; `git grep` de imports de brep fuera de lib/brep y specs = 30 ficheros (flatshot-solids.ts:47 importa extrudeProfile y tryBoolean, no tessellateBody).
- **Verificación independiente:** Confirmado (baja). brep/index.ts:11 sigue titulado «QUÉ NO ESTÁ CONECTADO» y el párrafo termina con «El kernel SÍ toca el producto» (:15). solid-snap-host.ts:30 es `import type { BrepBody }`. Omite solid3d-build.ts:710 (`entry.mesh ??= tessellateBody(entry.body)`), que es quien tesela para el visor: solid3d-three.ts:47/148 usa solid3dMesh y del kernel sólo importa edgeDihedralAngle y halfEdgeSegment (:58). flatshot-solids.ts:47 importa extrudeProfile y tryBoolean. En f86b1fc3 hay 30 ficheros de producto que importan del kernel (32 en la punta, contando edge-ray y solid-edge-ref), no los «siete consumidores reales» del mensaje del commit. Matiz: el texto dice que esos ficheros «importan de aquí», no que cada uno llame a tessellateBody, así que «atribuye el consumo» es una lectura estricta. La contradicción del título y la lista incompleta sí son reales.
- **Arreglo:** En brep/index.ts cambia el título a «QUÉ ESTÁ CONECTADO». Di que el visor recibe la malla de `solid3d-build.ts` (tessellateBody) y cita los demás importadores como ejemplos («entre otros: …»), sin número cerrado y sin incluir solid-snap-host.ts, que sólo importa un tipo.

### nucleo-3d-8 · [baja] edge-ray.ts duplica CadPickRay y scaleOf de face-ray.ts y anuncia un umbral en píxeles que no existe

- **Fichero:** `apps/web/src/lib/cad/pick3d/edge-ray.ts:35`
- **Problema:** Redeclara `export interface CadPickRay`, idéntica a la de face-ray.ts:63 que ya usan scene-ray.ts y document-face-pick.ts. También copia scaleOf, RELATIVE_EPSILON y MIN_SCALE. Al conectar T4 habrá dos tipos exportados con el mismo nombre. La cabecera (líneas 19-21) promete un «umbral en píxeles (proyectado a unidades de dibujo con la distancia de la cámara)», pero hitEdge sólo acepta maxDistance en unidades de dibujo, con un 2 % de la diagonal del cuerpo por defecto (línea 145), independiente del zoom y de la cámara.
- **Evidencia:** edge-ray.ts:35 `export interface CadPickRay`; face-ray.ts:63 `export interface CadPickRay`; edge-ray.ts:58-66 copia de scaleOf; :20 «umbral en píxeles»; :145 `options.maxDistance ?? scale * 0.02`.
- **Verificación independiente:** Confirmado en la duplicación. edge-ray.ts:35-38 redeclara `export interface CadPickRay`, idéntica a face-ray.ts:63-66, que ya importan scene-ray.ts:26 y document-face-pick.ts:27. scaleOf (edge-ray.ts:61-66), RELATIVE_EPSILON y MIN_SCALE (:58-59) copian face-ray.ts:98-112. Por defecto el umbral es `scale * 0.02` (:145), sin relación con zoom ni cámara. Matices: el JSDoc de hitEdge (:135-137) sí aclara que el umbral va en unidades de dibujo y que la UI lo calcula desde píxeles. La cabecera (:19-21) es imprecisa, no una promesa sin rastro. Y dos interfaces estructuralmente idénticas no chocan en TypeScript. Queda como deuda de limpieza y documentación, no como fallo funcional.
- **Arreglo:** En edge-ray.ts importa `type CadPickRay` desde ./face-ray y reutiliza su scaleOf (exportándolo) en vez de copiarlos. Corrige la cabecera para decir que el umbral llega ya en unidades de dibujo y que convertir píxeles lo hará quien conecte el pointer-router, o añade un parámetro explícito de unidades de dibujo por píxel.

## Dependencias, bitácora y caso de la revisión anterior: b26a3fa5, f8c689a0 y los 11 commits de bitácora, verificados contra el código en 52898e03

**Resumen del revisor:** No debe llegar a main. Las dependencias están limpias: en neto la rama no toca ninguna. `git diff --quiet 0a5f5600 52898e03 -- package.json package-lock.json apps/web/package.json apps/api/package.json` sale con código 0. b26a3fa5 sólo había tocado el lockfile, sin subir el override de sharp, que seguía en 0.35.3. f8c689a0 lo deshace entero, y origin/main (#208, fa137622) ya trae next ^16.3.5 y el override de sharp 0.35.4. Pero la bitácora sigue presentando b26a3fa5 como «CI fix: next y sharp arreglados».

Revisé contra el código cada estado nuevo de la bitácora desde 445352d8.
- **Ciertas:** T15 «ya estaba»: DXF de muros con inglete y jambas, T-34 y dxf-export-losses.spec. D-03, D-06, D-07, D-08 (SHELL de la cinta), D5 de la cinta, D-1 de Electrical (mayúsculas), D-05 del flujo 2D (unidades de la entrada dinámica), D2 de MEP (la pérdida se declara, aunque con demasiado alcance) y la evidencia de command-integrity: 300 en el JSON y 300 en el manifiesto.
- **Falsas o parciales:**
  - T9 «HECHA»: PERSPECTIVE sigue sin nadie que la lea.
  - T12: el spec sigue siendo tautológico.
  - T13 «YA ESTABA»: FLATSHOT y SECTION sobre la entidad WALL sí existían (T-33, golden 140), pero SLICE no acepta muros y la prueba citada usa el muro heredado.
  - T14: el CSV sigue sin unidad.
  - T16: AETAG pone CT a los contactos IEC y M al guardamotor.
  - T18: el sobresaliente es inalcanzable, falta el tipo de línea CENTER y CENTERLINE une puntos medios.
  - T19: el «peso» son kg/m multiplicados por el número de piezas, y el golden 84 se rompe.
  - T25: la persistencia existe y está conectada, pero no hay ningún spec.
  - D-04 (VSCURRENT): la condición nueva no se cumple nunca.
  - D-05 (EXTRUDE): la altura sale 0 al señalar sobre X.
  - D-2 de Electrical: ignora Z en LINE y suma |dz| en polilíneas.
  - D-08 del flujo 2D: escribe `height` en una mleader, que usa `textHeight`.
  - D-1 de interop: exporta MULTILEADER, pero el importador propio sólo lee MLEADER, así que el golden 17 se rompe.
  - H-11: sin spec de los valores de Fase.
- **Tabla de estados:** sigue sucia. T25 aparece dos veces, T5 repite a T14 y T4 sigue «BLOQUEADA» aunque edge-ray.ts ya existe.
- **CI en rojo:** fallan command-icons.spec y command-summaries.spec (PERSPECTIVE, CENTERMARK, CENTERLINE y AESYMBOL sin icono ni resumen) y los goldens 17 y 84.

De los 45 defectos de .mimocode/revision-claude.md, ningún commit nuevo lleva el prefijo «Revisión:» ni toca los ficheros señalados. Se cerró sin querer t14-cuadros-unidades-2: command-integrity.json regenerado con 300 = manifiesto. vista-alzados-proyeccion-estilos-7 queda a medias: la evidencia cuadra, pero PERSPECTIVE sigue sin icono ni resumen. Siguen abiertos en 52898e03 los otros 43:
- t1-transformaciones-3d-1 a 8: solid3d-build.ts, solid3d-adapter.ts, transform-3d*.ts, entity-commands.ts y cad-solid-invariants.ts sin cambios. placeBody sigue ignorando e/f/dz.
- t2-slice-section-1 a 5: solids-modify.ts y slice-coordinate-planes.spec.ts sin cambios.
- t14-cuadros-unidades-1, 3, 4 y 5.
- vista-alzados-proyeccion-estilos-1 a 6: view-controller.ts sin cambios. visual-styles.ts sigue con 7 filas y solid3d-view.spec.ts:91 y hidden-lines.spec.ts:44 siguen exigiendo 4.
- geometria-aristas-caras-1 a 3.
- t23-step-iges-descarga-1 a 6: solids.spec.ts:460 sigue con messageOf sobre EXPORT, y command-engine-host.ts y solids-interop.ts sin cambios.
- bitacora-estados-1: parcial, la fila T9 ya usa el número de la cola, pero T5-T8, T10 y T11 siguen desfasadas.
- bitacora-t7-alzados, bitacora-t9-proyeccion (ahora contradicho por escrito), bitacora-t23-export, bitacora-t14-csv y bitacora-t3-falso.
- bitacora-t12-spec: se actualizó la fila, no el spec.
- bitacora-criterios-1 y bitacora-gobernanza-1: assisted-development-log.json sin cambios.
- bitacora-t4-razon y bitacora-mapa-1: líneas 18 y 27 sin cambios.

No se ejecutó nada: todo sale de `git show` y `git grep` sobre 52898e03.

**Afirmaciones de tus commits que no se cumplen:**

- b26a3fa5: sube next a 16.3.5 y fija el override de sharp en 0.35.4
  - Evidencia: `git show --stat b26a3fa5` sólo lista package-lock.json. En la base, 0a5f5600:package.json:130 sigue con el override "sharp": "0.35.3" y apps/web/package.json:31 con "next": "^16.2.12": el lock quedaba desalineado de los manifiestos. f8c689a0 lo deshizo.
- Bitácora (ef43bb6e): «CI fix PARCIAL — next y sharp arreglados», con b26a3fa5 como commit del arreglo
  - Evidencia: NOCHE_MIMO_20260914.md:388 y :436-441 en 52898e03 lo mantienen, pero f8c689a0 revirtió b26a3fa5 y la rama deja las dependencias idénticas a 0a5f5600.
- Bitácora (ef43bb6e): T9 HECHA, proyección paralela con PERSPECTIVE 0/1
  - Evidencia: `git grep -E "setProjection|\bPERSPECTIVE\b" 52898e03 -- apps packages` sólo encuentra view-controller.ts:201 (definición sin llamadores), command-manifest.ts:335, view-visual.ts:128/135/163 y system-variables.ts:176. Nadie cambia la cámara.
- Bitácora (ef43bb6e): T12 HECHA, con face-orientation-fix.spec.ts (3 aserciones) como gate
  - Evidencia: El arreglo de 1321ade6 es correcto, pero face-orientation-fix.spec.ts no cambió después de 445352d8 y sigue permutando e invirtiendo índices por su cuenta, sin llamar a los tres constructores (defecto bitacora-t12-spec abierto).
- Bitácora (468d7273): T13 YA ESTABA, WALL en FLATSHOT/SECTION/SLICE (flatshot-solids.spec, 51 aserciones)
  - Evidencia: FLATSHOT (flatshot-solids.ts:263-265) y SECTION (solids-support.ts:73-98, que usa solids-modify.ts:455) ya aceptaban la entidad wall nativa antes de la rama (golden 140). SLICE usa selectedSolids (solids-modify.ts:366) y solids-support.ts:62-68 lo excluye a propósito. flatshot-solids.spec.ts:32-36 prueba el muro heredado type box, kind wall.
- Bitácora (468d7273): T14 HECHA, cuadros de arquitectura con la unidad correcta
  - Evidencia: data-extraction-commands.ts no se tocó después de 445352d8. La llamada a buildCadDataExtractionCsv de la línea 140 sigue sin unit (t14-cuadros-unidades-1 abierto).
- D-05 (3a980e89): la altura por punto de EXTRUDE sigue el eje vertical de pantalla, y señalar abajo extruye hacia abajo
  - Evidencia: solids-create.ts:307 `return point.y - centre.y;` usa la Y de dibujo en planta (CadPoint2), no la de pantalla. Un punto a la derecha del centroide da 0, y la línea 203 lo rechaza.
- D-2 (Electrical): cadEntityRunLength mide la longitud verdadera, con arcos por bulge
  - Evidencia: circuit-check.ts:137-148 sale por la rama de curvas 2D. En una LINE, curve-model.ts:139-146 descarta z: una bajada (0,0,0)→(0,0,2500) mide 0. En una polilínea suma la longitud en planta más |dz| por tramo, no Math.hypot.
- H-11: data-extraction.spec (25 aserciones) prueba la columna Fase
  - Evidencia: El diff del spec sólo cambia `columns === 7` (línea 72) y `length === 7` (línea 80). Ningún muro del spec está en capa -EXI/-DEM/-NUE, y cadHasRenovationLayers (mexican-layers.ts:639) no tiene llamadores.
- T16: AETAG reconoce las familias K/F/M/Q/X/H de los símbolos de esquema
  - Evidencia: electrical-tag.ts:82 comprueba CONTACTO (→CT) antes que las familias IEC, así que IEC-CONTACTO-NA/NC/NA-T salen CT. :86 comprueba MOTOR antes de GUARDAMOTOR (:88), así que IEC-GUARDAMOTOR sale M.
- T18: CENTERMARK y CENTERLINE en capa CENTER con tipo de línea CENTER y sobresaliente configurable
  - Evidencia: center-marks.ts:30 CENTER_LINESTYLE no se usa. state.pick sólo vale null (líneas 33 y 37), así que el prompt «Precise el sobresaliente» (:49) no se alcanza nunca y el sobresaliente queda en 3 unidades de dibujo. CENTERLINE sobre dos rectas une sus puntos medios (:215-216).
- T19 HECHA: la BOM da Peso unit. (kg) y Peso total (kg)
  - Evidencia: mechanical-bom.ts:125 usa cadSteelKgPerMetre (kg/m, mechanical-parts.ts:393) como peso unitario y :126 lo multiplica por el número de piezas. La longitud de STEELSHAPE no se guarda. El golden 84:180 sigue esperando 5 columnas.
- T25: plot-style-catalog.spec (22 aserciones) y linetype.spec respaldan la persistencia
  - Evidencia: Ningún spec se tocó después de 445352d8. `git grep "localStorage|valle:cad"` en *.spec.ts sólo encuentra library-storage.spec.ts y tour-host.spec.ts.
- 8a388571: tabla limpia (T19 sin duplicar, T25 sin parcial)
  - Evidencia: T19 queda en una sola fila (378), pero siguen «T25 PARCIAL» (369) y «T25 HECHA» (383), y T5 (358) repite la nota de T14 (367).
- D-04 (visualización 3D): VSCURRENT dice «0 objetos» cuando no hay nada sombreable
  - Evidencia: studio-engine-bridges.ts:164 da label null si no existe solidShadeHost.current, y :168 exige `label && !solidShadeHost.current`: la condición no se cumple nunca.
- D-08 (flujo 2D): las mleader escalan con la escala anotativa
  - Evidencia: annotative-scale.ts:229 escribe `patch: { height }` y :238 lee `.height`. La mleader sólo tiene textHeight (cad-document.ts:324-333), y mleader-entity-adapter.ts:96 sólo lee patch.textHeight.
- D-1 (interop): MLEADER exportado como MULTILEADER con AcDbEntity, reconocido por los lectores
  - Evidencia: dxf-export.ts:749-750 lo escribe, pero el propio importador sólo reconoce "MLEADER" (dxf-read-annotations.ts:176; dxf-import.ts:303 y :953). El golden 17:88 espera 1 mleader tras exportar y reimportar.
- Bitácora: T4 BLOQUEADA porque faltan edge-ray.ts y solid-edge-ref.ts
  - Evidencia: 1be2676e creó apps/web/src/lib/cad/pick3d/edge-ray.ts y solid-edge-ref.ts con spec. NOCHE_MIMO_20260914.md:357 y :424 no cambiaron.
- Los commits nuevos corrigen defectos de la revisión anterior (e/f/dz en solid3d-build.ts, specs de 4 estilos, solids.spec.ts con EXPORT, PERSPECTIVE)
  - Evidencia: `git diff --stat 445352d8 52898e03` no toca solid3d-build.ts, solid3d-adapter.ts, transform-3d*.ts, solids-modify.ts, solid3d-view.spec.ts, hidden-lines.spec.ts, solids.spec.ts, command-engine-host.ts, solids-interop.ts, view-controller.ts, command-icons.ts, command-summaries.ts, face-orientation-fix.spec.ts ni assisted-development-log.json. Sólo se cierra de rebote t14-cuadros-unidades-2, con command-integrity.json en 300.

### dependencias-bitacora-1 · [alta] PERSPECTIVE, CENTERMARK, CENTERLINE y AESYMBOL sin icono ni resumen: command-icons.spec y command-summaries.spec fallan

- **Fichero:** `apps/web/src/components/cad/ribbon/command-icons.spec.ts:23`
- **Problema:** Cuatro comandos entran en CAD_COMMAND_REGISTRY_V2 sin entrada en CAD_COMMAND_ICONS ni en CAD_COMMAND_SUMMARIES o sus exclusiones: PERSPECTIVE (445352d8), CENTERMARK y CENTERLINE (91a673d5) y AESYMBOL (28bc7600). Los dos specs fail-closed exigen ambas cosas para todo comando registrado, y run-specs.mjs los recoge (src/**/*.spec.ts), así que test:specs queda en rojo. Aun así, la bitácora marca T9, T16 y T18 como HECHA. vista-alzados-proyeccion-estilos-7 sigue abierto, ahora con tres comandos más.
- **Evidencia:** command-manifest.ts:84, :85, :127 y :335 registran los cuatro. `git grep -n -E "\b(PERSPECTIVE|CENTERMARK|CENTERLINE|AESYMBOL)\b" 52898e03 -- apps/web/src/components/cad/ribbon/command-icons.ts apps/web/src/lib/cad/engine/command-summaries.ts` no da resultados. command-icons.spec.ts:22-23: `assert.deepEqual(mudos, [], ...)`. command-summaries.spec.ts:24-31: `assert.deepEqual(silent, [], ...)`.
- **Verificación independiente:** Confirmado. CAD_COMMAND_REGISTRY_V2 se construye desde CAD_COMMAND_MANIFEST (engine/index.ts:190), y el manifiesto registra CENTERMARK (:84), CENTERLINE (:85), AESYMBOL (:127) y PERSPECTIVE (:335). git grep de los cuatro nombres en command-icons.ts y command-summaries.ts en 52898e03 no da nada. CAD_COMMAND_ICONS (command-icons.ts:238) es una tabla literal, sin spreads, y CAD_COMMAND_SUMMARY_EXCLUSIONS es un Set vacío (command-summaries.ts:332). command-icons.spec.ts:22-23 y command-summaries.spec.ts:24-31 hacen assert.deepEqual(..., []), y run-specs.mjs:43 recoge src/**/*.spec.ts. La bitácora marca T9 (:362), T16 (:382) y T18 (:373) como HECHA.
- **Arreglo:** En apps/web/src/components/cad/ribbon/command-icons.ts añade PERSPECTIVE, CENTERMARK, CENTERLINE y AESYMBOL, cada uno con un icono lucide ya importado. En apps/web/src/lib/cad/engine/command-summaries.ts añade a cada uno un resumen en español de México de 10 a 110 caracteres. Ejecuta command-icons.spec.ts y command-summaries.spec.ts antes de commitear. No marques HECHA ninguna tarea mientras esos specs estén en rojo.

### dependencias-bitacora-2 · [alta] BOM_HEADERS pasa a 7 columnas y rompe el golden 84, que exige la cabecera de 5

- **Fichero:** `apps/web/src/lib/cad/mechanical-bom.ts:86`
- **Problema:** 4bd7f9dd amplía la cabecera de la lista de materiales y actualiza mechanical.spec.ts, pero no el golden 84. Ese golden comprueba la cabecera exacta de la TABLE que llega al servidor. Playwright recorre todo e2e/ en CI, así que el golden falla y la rama no se integra.
- **Evidencia:** mechanical-bom.ts:86 `["Pos.", "Cant.", "Denominación", "Norma", "Peso unit. (kg)", "Peso total (kg)", "Bloque"]`. e2e/golden/84-cad-plano-de-fabricacion.spec.ts:180 `expect(cellTexts(table!, 1)).toEqual(["Pos.", "Cant.", "Denominación", "Norma", "Bloque"])`. playwright.config.ts:27 testDir "./e2e"; ci.yml:945 corre `npx playwright test` en 4 shards. `git show --stat 4bd7f9dd` no toca apps/web/e2e.
- **Verificación independiente:** Confirmado. mechanical-bom.ts:86 define 7 columnas y buildCadMechanicalBomTable las usa. El comando BOM la llama en mechanical-annotate.ts:275. golden 84 (84-cad-plano-de-fabricacion.spec.ts:180) exige ["Pos.","Cant.","Denominación","Norma","Bloque"], y la :181 exige una fila de 5 celdas. 4bd7f9dd sólo toca mechanical-annotate.ts, mechanical-bom.ts, mechanical-parts.ts y mechanical.spec.ts. playwright.config.ts:27 usa testDir ./e2e y sólo ignora auditoria/** (:47); ci.yml:945 corre todo con --shard, así que el golden falla.
- **Arreglo:** Cuando esté corregido el cálculo del peso (dependencias-bitacora-4), actualiza e2e/golden/84-cad-plano-de-fabricacion.spec.ts:180 con la cabecera nueva. Añade además la aserción del criterio de T19: un STEELSHAPE PTR 50,8×50,8×3 de 14 m debe dar 63,0 kg en «Peso total (kg)» de la TABLE.

### dependencias-bitacora-3 · [alta] MULTILEADER se exporta pero el importador propio sólo lee MLEADER: la reimportación pierde las directrices y el golden 17 falla

- **Fichero:** `apps/web/src/lib/cad/dxf-export.ts:749`
- **Problema:** 3f654be5 cambia el nombre de la entidad exportada a MULTILEADER, pero no toca el lector. Un DXF de Valle reimportado en Valle pierde todas las directrices sin aviso. El golden 17 exporta desde la UI, reimporta y exige una directriz, así que se rompe. La bitácora marca D-1 (interop) como HECHA.
- **Evidencia:** dxf-export.ts:749 `pushPair(lines, 0, "MULTILEADER");`. dxf-read-annotations.ts:176 `if (pairs[start].code !== 0 || pairs[start].value.toUpperCase() !== "MLEADER") continue;`. dxf-import.ts:303 (RAW_ONLY_ENTITY_TYPES) y :953 comparan con "MLEADER". :836 toma `mleaders` de parseRawDxfSemanticMleaders. e2e/golden/17-cad-native-mleader.spec.ts:80-88 pulsa «Descargar DXF», llama a importDxfPrimitives y espera `imported.mleaders` con longitud 1. `git grep MULTILEADER 52898e03 -- apps/web/src` sólo encuentra dxf-export.ts:749.
- **Verificación independiente:** Confirmado. 3f654be5 cambia pushPair(lines, 0, "MLEADER") por "MULTILEADER" (dxf-export.ts:749, dentro de pushMleader, que exportCadDxf llama en :940). La exportación del documento le pasa las mleaders (dxf-document-export.ts:101 y :135). El lector sólo acepta "MLEADER": dxf-read-annotations.ts:176, dxf-import.ts:303 y :953. importDxfPrimitives devuelve `mleaders` de parseRawDxfSemanticMleaders (dxf-import.ts:836 y :871). En apps/web/src/lib/cad no hay ningún alias MULTILEADER (git grep -i "ulti" en los lectores no da nada). golden 17:80-88 exporta, reimporta y exige imported.mleaders con longitud 1, así que falla.
- **Arreglo:** En dxf-read-annotations.ts:176 acepta "MLEADER" y "MULTILEADER". En dxf-import.ts añade "MULTILEADER" a RAW_ONLY_ENTITY_TYPES (:303) y trata los dos nombres en :953. Añade un spec que pase una mleader por el exportador, reimporte el texto con importDxfPrimitives y compruebe una mleader con su texto y su flecha. Mientras falten MLEADERSTYLE y el CONTEXT_DATA completo (10-interop.md, D-1), declara la degradación en dxf-export-loss-manifest.ts.

### dependencias-bitacora-4 · [alta] T19: el «peso» de la BOM son kg/m multiplicados por el número de piezas; la longitud no se guarda

- **Fichero:** `apps/web/src/lib/cad/mechanical-bom.ts:125`
- **Problema:** «Peso unit. (kg)» es en realidad cadSteelKgPerMetre(área), es decir, kilos por metro, y «Peso total (kg)» son kg/m por número de piezas. La longitud que STEELSHAPE empezó a pedir en df7b62e0 sólo sale en el mensaje de inserción: no se guarda en el bloque ni en el INSERT, así que la lista no puede dar kilos. Un PTR de 14 m sale con unos 4,50 «kg» por pieza, y el total no depende de la longitud. El criterio de T19 (14 m de PTR → 63,0 kg) no se puede cumplir, y el spec sólo comprueba celdas «—» de un tornillo sin área. La bitácora (líneas 378 y 8a388571) la da por HECHA.
- **Evidencia:** mechanical-bom.ts:125 `const weightPerUnit = areaMm2 !== null ? cadSteelKgPerMetre(areaMm2) : null;`, :126 `weightPerUnit * row.count`. mechanical-parts.ts:393 `export function cadSteelKgPerMetre(areaMm2: number)`. En engine/commands/mechanical-parts.ts la longitud sólo alimenta `lengthNote`, que va al texto del mensaje. mechanical.spec.ts:338-340 sólo afirma "—" en las dos columnas de peso.
- **Verificación independiente:** Confirmado. mechanical-bom.ts:125 hace weightPerUnit = cadSteelKgPerMetre(areaMm2), y mechanical-parts.ts:393-394 lo define como kg/m. La :126 multiplica por row.count. En engine/commands/mechanical-parts.ts:351-354 la longitud sólo alimenta lengthNote, que va a `detail`. finishPart (:93-105) sólo usa detail en el aviso y llama a cadMechanicalInsertCommands sin longitud. La longitud no se guarda en bloque ni INSERT, y la BOM agrupa por blockId. mechanical.spec.ts:340-341 sólo afirma "—" en las columnas de peso. La bitácora pone T19 como HECHA (:378).
- **Arreglo:** Guarda la longitud del tramo de STEELSHAPE en el INSERT (atributo o context.metadata, cambio aditivo) y agrupa las filas de la BOM por bloque y longitud. Calcula «Peso unit. (kg)» = cadSteelKgPerMetre(área) × longitud / 1000 y «Peso total (kg)» = peso unitario × cantidad. Sin longitud, escribe «—», nunca kg/m. En mechanical.spec.ts ejecuta STEELSHAPE (PTR 50,8×50,8×3, 14 000 mm) y luego BOM, y comprueba que «Peso total (kg)» vale "63.00" (o el valor exacto de cadSteelKgPerMetre × 14). Actualiza el golden 84.

### dependencias-bitacora-5 · [alta] D-04 (VSCURRENT honesto) es código muerto: la condición no se cumple nunca

- **Fichero:** `apps/web/src/components/cad/command-line/studio-engine-bridges.ts:168`
- **Problema:** `label` sólo es distinto de null cuando existe solidShadeHost.current, y la condición nueva exige `!solidShadeHost.current`, así que nunca entra. Además, que un anfitrión esté montado no dice cuántos objetos sombreables hay. VSCURRENT sigue confirmando el estilo sobre un dibujo sin sólidos ni muros (defecto 4.4 de 02-3d-visual.md), aunque la bitácora (línea 384) lo marca HECHA. No hay spec.
- **Evidencia:** studio-engine-bridges.ts:164 `const label = solidShadeHost.current?.applyVisualStyle(styleId) ?? null;` y :168 `if (label && !solidShadeHost.current && !nativeMassHosts?.current)`. `git show --stat 2d54102f` sólo toca este fichero.
- **Verificación independiente:** Confirmado. studio-engine-bridges.ts:164 hace label = solidShadeHost.current?.applyVisualStyle(styleId) ?? null, así que label sólo es truthy si solidShadeHost.current existe. La condición nueva (:168) exige además !solidShadeHost.current, y nunca se cumple: el mensaje de «0 objetos» es inalcanzable. Tampoco se cuentan objetos. 2d54102f sólo añade esas 4 líneas y ningún spec. La bitácora lo marca HECHA (:384).
- **Arreglo:** Haz que applyVisualStyle de solid-shade-host y de native-mass-hosts devuelva también cuántos objetos restiló. En visualStyle suma los dos recuentos. Si la suma es 0, devuelve «VSCURRENT <estilo>: 0 objetos; este dibujo no tiene sólidos, muros ni losas sombreables». Si no, añade el recuento a la etiqueta. Añade un spec de cadStudioEngineBridges con anfitriones de prueba, uno con 0 objetos y otro con 3, que compruebe los dos mensajes.

### dependencias-bitacora-6 · [alta] D-08 (flujo 2D): la escala anotativa escribe `height` en una mleader, que guarda la altura en `textHeight`

- **Fichero:** `apps/web/src/lib/cad/layout/annotative-scale.ts:112`
- **Problema:** 1425a1a1 añade "mleader" a HEIGHT_BEARING_TYPES, pero el reescalado lee `entity.height` y escribe `patch: { height }`. La mleader no tiene ese campo, y su adaptador ignora la clave, así que no cambia nada. Como readHeight siempre da null, cada pasada vuelve a emitir la orden y cuenta la directriz como reescalada. Es exactamente el falso éxito que describe el comentario de las líneas 105-111. Sin spec, y marcado HECHA en la bitácora (línea 386).
- **Evidencia:** annotative-scale.ts:229 `commands.push({ type: "properties", entityId: entity.id, patch: { height } });` y :238 `(entity as { height?: unknown }).height`. cad-document.ts:324-333: la mleader declara `textHeight?: number` y no `height`. mleader-entity-adapter.ts:96 sólo usa `patch.textHeight` (`git grep "patch.height"` en ese fichero no da nada).
- **Verificación independiente:** Confirmado. 1425a1a1 sólo añade "mleader" a HEIGHT_BEARING_TYPES (annotative-scale.ts:112). Las dos rutas emiten patch { height } y leen entity.height: readHeight en :237-239 y la ruta de CANNOSCALE hacia :299-303. cad-document.ts declara textHeight?: number en la mleader, sin campo height. entity-commands.ts:518 delega en adapter.properties.write, y mleader-entity-adapter.ts:96 sólo lee patch.textHeight (entre :89-104 no hay patch.height). Resultado: no cambia nada, readHeight da null siempre y la mleader se reporta como reescalada en cada pasada. No hay spec (1425a1a1 toca 1 fichero). Bitácora :386 HECHA.
- **Arreglo:** En annotative-scale.ts, para type === "mleader", escribe `patch: { textHeight: height }` y lee `textHeight` en readHeight, en las dos rutas (:224-229 y :299-303). Añade al spec de escala anotativa una mleader con textHeight 120 que, aplicada con executeCadEntityCommandBatch a escala 1:50 con altura de papel 2,5 mm, quede con el textHeight esperado. Añade también un caso ya a esa altura que no emita ninguna orden.

### dependencias-bitacora-7 · [alta] La bitácora pasa T9 a «HECHA» cuando PERSPECTIVE sigue sin efecto en la cámara

- **Fichero:** `docs/execution/NOCHE_MIMO_20260914.md:362`
- **Problema:** ef43bb6e cambia la fila T9 de «YA ESTABA» a «HECHA — Proyección paralela (PERSPECTIVE 0/1)». También añade una sección (líneas 450-455) que dice «la ortográfica ya estaba» y cita como gates «typecheck verde, command-manifest 297, view-3d.spec.ts 131 aserciones», que no prueban el cambio de proyección. En 52898e03 nadie lee la variable y setProjection no tiene llamadores: el 3D sigue siempre en perspectiva. El hallazgo abierto bitacora-t9-proyeccion queda contradicho por escrito.
- **Evidencia:** `git grep -n -E "setProjection|\bPERSPECTIVE\b" 52898e03 -- apps packages` sólo encuentra view-controller.ts:201 (la definición), command-manifest.ts:335, view-visual.ts:128/135/163 y system-variables.ts:176. `git diff --stat 445352d8 52898e03` no toca view-controller.ts, y en Layout3DEditor.tsx sólo cambia la línea 14647 (entrada dinámica).
- **Verificación independiente:** Confirmado. git grep "setProjection|\bPERSPECTIVE\b" en apps y packages sólo encuentra la definición en view-controller.ts:201, el manifiesto (:335), view-visual.ts:128/135/163 (sólo escribe la variable) y system-variables.ts:176. Nadie lee la variable ni llama a setProjection. git diff --stat 445352d8..52898e03 en editor sólo toca 1 línea de Layout3DEditor.tsx. La bitácora pone T9 como HECHA en la tabla (:362) y en la sección (:450-455). Cita view-3d.spec.ts, que sólo prueba la cámara perspective (view-3d.spec.ts:236-244), no el cambio de proyección.
- **Arreglo:** Cambia la fila T9 a «PARCIAL — comando y variable sin efecto en la cámara (ver vista-alzados-proyeccion-estilos-2)». Pon la sección T9 en estado parcial y quita view-3d.spec.ts de sus gates. Márcala HECHA sólo cuando setProjection cambie de verdad la cámara y exista el spec de dos rectas paralelas a cotas distintas.

### dependencias-bitacora-8 · [media] T16: AETAG etiqueta los contactos IEC como CT y el guardamotor como M

- **Fichero:** `apps/web/src/lib/cad/engine/commands/electrical-tag.ts:82`
- **Problema:** familyForBlock busca subcadenas en orden, y las comprobaciones de planta MEP van antes que las nuevas. IEC-CONTACTO-NA, IEC-CONTACTO-NC e IEC-CONTACTO-NA-T contienen «CONTACTO» y salen con CT, la familia de contactos de enchufe, en vez de K. IEC-GUARDAMOTOR contiene «MOTOR» y sale con M antes de llegar a la rama F. Así, AETAG Todos numera CT1… y M1 para un guardamotor, al contrario de lo que anuncia 28bc7600. Además, K, F, Q, X y H no están en CAD_IE_FAMILIES y no se pueden elegir a mano. T16 no tiene spec (el criterio pedía uno que afirmara la geometría).
- **Evidencia:** electrical-tag.ts:82 `if (nombre.includes("CONTACTO")) return "CT";`, :86 `if (nombre.includes("MOTOR")) return "M";`, :88 (GUARDAMOTOR → F, inalcanzable para IEC-GUARDAMOTOR). :157 llama `familyForBlock(insert.block)`, y cadSchematicBlockDefinition usa `name: symbol.id` (IEC-…). device-tags.ts:48-57 sólo define M, PB, LT, CT, SW, TB, TR y SN. `git ls-tree` no encuentra ningún spec de schematic-symbols ni de AESYMBOL.
- **Verificación independiente:** Confirmado. familyForBlock (electrical-tag.ts:78-93) mira CONTACTO→CT (:82) antes que las familias IEC, y MOTOR→M (:86) antes de GUARDAMOTOR→F (:88). Los bloques se llaman symbol.id (schematic-symbols.ts:261): IEC-CONTACTO-NA/NC/NA-T dan CT, e IEC-GUARDAMOTOR da M. Esos símbolos van en la capa IE-ESQ (schematic-symbols.ts:33), así que cadIsElectricalInsert (device-tags.ts:168-172) los incluye en AETAG Todos (:143, :157). La ruta de uno solo (:221) también usa familyForBlock. CAD_IE_FAMILIES (device-tags.ts:48-57) no tiene K, F, Q, X ni H. git ls-tree no encuentra spec de símbolos de esquema ni de AESYMBOL.
- **Arreglo:** En familyForBlock, si el nombre empieza por "IEC-", devuelve cadSchematicSymbolFor(nombre)?.family antes de mirar las familias de planta. Añade K, F, Q, X y H a CAD_IE_FAMILIES con su etiqueta en español. Crea apps/web/src/lib/cad/electrical/schematic-symbols.spec.ts. Debe insertar los 12 símbolos con AESYMBOL, ejecutar AETAG Todos y comprobar que contactos y bobinas llevan prefijo K; relevador, fusible y guardamotor F; seccionador Q; borne X; piloto H; y motor M. También que cada bloque tiene geometría no vacía y el atributo TAG.

### dependencias-bitacora-9 · [media] T18: sobresaliente inalcanzable, sin tipo de línea CENTER, y CENTERLINE entre rectas no dibuja el eje

- **Fichero:** `apps/web/src/lib/cad/engine/commands/center-marks.ts:40`
- **Problema:** Hay tres fallos. (a) state.pick sólo vale null, así que el prompt «Precise el sobresaliente» no se alcanza nunca. Al designar, la marca se hace con overshoot 3 en unidades de dibujo: 3 mm en un documento en mm y 3 m en uno en metros. (b) CENTER_LINESTYLE no se usa y las líneas no llevan tipo de línea, en contra de lo que anuncia 91a673d5. (c) CENTERLINE sobre dos rectas une sus puntos medios: sale un segmento que cruza de una a otra, no el eje paralelo a medio camino que promete la cabecera. No hay spec, aunque el criterio de T18 pedía un sobresaliente medido.
- **Evidencia:** center-marks.ts:37 `const EMPTY_MARK: MarkState = { pick: null, overshoot: 3 };`. `git grep -n "pick: " 52898e03 -- apps/web/src/lib/cad/engine/commands/center-marks.ts` sólo da :33 y :37. :40 exige `!state.pick` con CAD_ACCEPT_ENTITY_PICK, y :142 sólo acepta distance sin pick. :30 `const CENTER_LINESTYLE = "CENTER";` sin uso. :215-216 toman el punto medio de cada recta, y :268 fija el sobresaliente en 3. Ningún *.spec.ts importa center-marks.
- **Verificación independiente:** Confirmado. (a) En center-marks.ts `pick` sólo aparece como null (:33, :37) y nunca se asigna. markStep (:40-45) pide CAD_ACCEPT_ENTITY_PICK, así que «Precise el sobresaliente» (:49) es inalcanzable. Al designar se usa state.overshoot=3 en unidades de dibujo (:149-155). (b) CENTER_LINESTYLE (:30) no se usa, las líneas (:103-125, :234-243) no llevan linetype y no se hace upsert de la capa CENTER, pese a la cabecera (:5-11). (c) Con rectas se toma el punto medio de cada una (:215-216, :222-223) y se unen: sale un segmento transversal, no el eje medio. CENTERLINE fija el sobresaliente en 3 (:268). Ningún spec importa center-marks.
- **Arreglo:** En CENTERMARK ofrece «Sobresaliente» como palabra clave, con CAD_ACCEPT_DISTANCE, en el prompt de designación. Pon por defecto 3 mm convertidos a la unidad del documento. Pon `linetype: "CENTER"` en las entidades y emite un upsert de la capa CENTER con tipo de línea CENTER si falta. En CENTERLINE con dos rectas, exige que sean paralelas y traza la recta media entre los extremos homólogos, prolongada con el sobresaliente. Crea center-marks.spec.ts con dos casos: CIRCLE de r=50 con sobresaliente 5, que dé dos líneas de 110 en CENTER con linetype CENTER; y dos LINE paralelas en y=0 e y=100, que den el eje en y=50 paralelo a ellas.

### dependencias-bitacora-10 · [media] D-2 (Electrical): cadEntityRunLength ignora la Z de las LINE y suma |dz| en las polilíneas

- **Fichero:** `apps/web/src/lib/cad/electrical/circuit-check.ts:137`
- **Problema:** La función sale por la rama de curvas 2D antes de la medida 3D. En una LINE, cadEntityCurves devuelve el segmento en planta y la Z se pierde: una bajada vertical (0,0,0)→(0,0,2500) mide 0, cuando antes medía 2500. En una polilínea suma la longitud en planta más |dz| de cada tramo: un tramo (0,0,0)→(3000,0,4000) mide 7000 en vez de 5000. AECHECK calcula la caída de tensión NOM con esa longitud. El spec sólo prueba una polilínea puramente vertical, donde la suma coincide.
- **Evidencia:** circuit-check.ts:137 `const curves = cadEntityCurves(entity);`; :138-148 suman curveLength y añaden |dz| sólo si type === "polyline", así que el cálculo 3D con Math.hypot (:150-164) ya no se alcanza para line ni polyline. curve-model.ts:139-146: la LINE da `{ kind: "segment", a: {x,y}, b: {x,y} }`; :340 curveLength usa distanceBetween en 2D. circuit-check.spec.ts:193-199 sólo prueba `sube`, (0,0,0)→(0,0,2500) como polilínea.
- **Verificación independiente:** Confirmado. circuit-check.ts:137-148 sale por cadEntityCurves siempre que haya curvas. Para LINE, curve-model.ts:139-146 da un segmento 2D sin z: (0,0,0)→(0,0,2500) mide 0, y el |dz| sólo se suma para polyline (:142). En la polilínea (curve-model.ts:180, polylineCurves) suma la longitud en planta más |dz| por tramo: (0,0,0)→(3000,0,4000) mide 3000+4000=7000, no 5000. La rama 3D con Math.hypot (:150-164) queda inalcanzable para line y polyline. El spec sólo prueba `sube`, una polilínea vertical pura (circuit-check.spec.ts:192-199).
- **Arreglo:** Mide cada tramo en 3D: un segmento recto (LINE, o tramo de polilínea sin bulge) con Math.hypot(dx, dy, dz), y un tramo con bulge con Math.hypot(longitudDelArco, dz). Añade a circuit-check.spec.ts tres casos: LINE (0,0,0)→(0,0,2500) = 2500, polilínea (0,0,0)→(3000,0,4000) = 5000 y semicírculo de radio 1000 con bulge 1 = π·1000.

### dependencias-bitacora-11 · [media] D-05 (EXTRUDE por punto): la altura se toma de la Y de dibujo y señalar sobre X da altura 0, que se rechaza

- **Fichero:** `apps/web/src/lib/cad/engine/commands/solids-create.ts:307`
- **Problema:** 3a980e89 sustituye la distancia radial por `point.y - centre.y`. Pero el punto está en coordenadas de dibujo en planta (CadPoint2), no de pantalla. Señalar a la derecha o a la izquierda del perfil (Δy = 0) da altura 0 y EXTRUDE lo rechaza, cuando antes extruía. Y en una vista isométrica, «arriba en pantalla» no es +Y. El comentario de las líneas 288-300 dice que la componente Y es «el eje vertical de pantalla», y es falso. No hay spec del cambio: solids-create.spec.ts:131 sólo pasa por el rechazo del perfil inclinado.
- **Evidencia:** solids-create.ts:299 `function heightFromPoint(point: CadPoint2, extracted: CadExtractedProfile)`, :307 `return point.y - centre.y;`, :278 `extrudeResult(state, heightFromPoint(input.point, profiles[0]), context, name)`, :203 `if (!(Math.abs(height) > 1e-9))`. `git show --stat 3a980e89` sólo toca solids-create.ts.
- **Verificación independiente:** Confirmado. 3a980e89 cambia Math.hypot(dx, dy) por `return point.y - centre.y` (solids-create.ts:306). heightFromPoint recibe CadPoint2 en coordenadas de dibujo (:299), que llega desde input.point (:273-278). Un punto con Δy=0 da altura 0, y extrudeResult lo rechaza (:203-204); antes extruía con la distancia radial. El comentario (:289-293) presenta la Y como «eje vertical de pantalla», y no lo es en vistas 3D. El commit sólo toca solids-create.ts, sin spec.
- **Arreglo:** En heightFromPoint conserva la magnitud Math.hypot(dx, dy) y toma el signo de dy: negativa si el punto queda por debajo del centroide. Si la entrada trae z (designación 3D), usa point.z − extracted.elevation. Corrige el comentario. Añade a solids-create.spec.ts un rectángulo horizontal con dos casos: EXTRUDE con point(centro.x + 500, centro.y) extruye 500, y con point(centro.x, centro.y − 300) extruye −300, con el sólido bajo el perfil.

### dependencias-bitacora-12 · [media] T13 «YA ESTABA» es inexacto: SLICE no acepta muros y la prueba citada usa el muro heredado

- **Fichero:** `docs/execution/NOCHE_MIMO_20260914.md:366`
- **Problema:** En la cola, T13 es «WALL en FLATSHOT/SECTION/SLICE». La fila la da por existente citando flatshot-solids.spec, que construye el muro HEREDADO (type box, kind wall): justo lo que la auditoría H-01 dice que no prueba la entidad WALL. Lo que ya existía antes de la rama era FLATSHOT y SECTION sobre la entidad wall nativa (T-33, golden 140). SLICE sigue sin aceptar muros, por una decisión documentada.
- **Evidencia:** flatshot-solids.spec.ts:32-36 construye `muro` con `kind: "wall"` sobre type box. flatshot-solids.ts:263-265 (rama `entity.type === "wall"`) y solids-support.ts:73-98 (selectedFlattenableBodies, que usa SECTION en solids-modify.ts:455) no cambian desde 0a5f5600. e2e/golden/140-cad-alzado-muro-nativo.spec.ts prueba tres muros nativos en L con puerta. SLICE usa selectedSolids (solids-modify.ts:366) y solids-support.ts:62-68 dice que «se queda deliberadamente sin muros».
- **Verificación independiente:** Confirmado. La cola define T13 como «WALL en FLATSHOT/SECTION/SLICE» (bitácora :205-209). La fila :366 lo da por YA ESTABA citando flatshot-solids.spec, cuyo muro es `type: "box", kind: "wall"` (flatshot-solids.spec.ts:32-36), el objeto heredado. El soporte del WALL nativo es previo: flatshot-solids.ts:263 y solids-support.ts:73-90, que usa SECTION (solids-modify.ts:455). git diff 0a5f5600..52898e03 no toca esos ficheros. SLICE usa selectedSolids (solids-modify.ts:366) y solids-support.ts:61-68 dice que se queda deliberadamente sin muros. Existe e2e/golden/140-cad-alzado-muro-nativo.spec.ts.
- **Arreglo:** Cambia la fila T13 a: «PARCIAL — FLATSHOT y SECTION sobre WALL nativo ya estaban (T-33: flatshot-solids.ts:263, solids-support.ts:73, golden 140); SLICE excluye muros por diseño (solids-support.ts:62-68)». Cita el golden 140 en lugar de flatshot-solids.spec. Deja la decisión sobre SLICE con muros en docs/execution/PREGUNTAS.md.

### dependencias-bitacora-13 · [media] La tabla de estados da por hecho un arreglo de dependencias revertido y conserva filas duplicadas

- **Fichero:** `docs/execution/NOCHE_MIMO_20260914.md:388`
- **Problema:** (a) La fila «CI fix | PARCIAL | next+sharp arreglados» y la sección de las líneas 436-441 presentan b26a3fa5 como el arreglo. Pero f8c689a0 lo deshizo y la rama no cambia ninguna dependencia; además, b26a3fa5 nunca tocó el override de sharp en package.json, sólo el lockfile. (b) 8a388571 anuncia «tabla limpia (… T25 sin parcial)», y siguen las dos filas de T25 (369 PARCIAL y 383 HECHA) y la fila T5 (358) con la nota de T14 (367).
- **Evidencia:** `git diff --quiet 0a5f5600 52898e03 -- package.json package-lock.json apps/web/package.json apps/api/package.json` sale con código 0. `git show --stat b26a3fa5` sólo lista package-lock.json, y 0a5f5600:package.json:130 tiene `"sharp": "0.35.3"`. En origin/main (fa137622, #208), apps/web/package.json:31 tiene next ^16.3.5 y package.json:130 sharp 0.35.4. Líneas 358, 367, 369, 383, 388 y 436-441 del fichero en 52898e03.
- **Verificación independiente:** Confirmado. git diff --quiet 0a5f5600 52898e03 sobre package.json, package-lock.json, apps/web/package.json y apps/api/package.json sale con 0. b26a3fa5 sólo toca package-lock.json, y f8c689a0 lo revierte (también sólo el lockfile). 0a5f5600:package.json:130 tiene sharp 0.35.3. origin/main (fa137622, #208) tiene next ^16.3.5 (apps/web/package.json:31) y sharp 0.35.4 (package.json:130). Aun así, la fila :388 y la sección :436-441 citan b26a3fa5 como arreglo. 8a388571 dice «T25 sin parcial», y siguen :369 (T25 PARCIAL) y :383 (T25 HECHA). La fila T5 (:358) repite la nota de T14 (:367).
- **Arreglo:** Cambia la fila CI fix a «DESHECHO en f8c689a0: next 16.3.5 y sharp 0.35.4 llegan por la #208 de main; multer (NestJS 12) queda para decisión de Sergio» y reescribe igual la sección de las líneas 436-441, sin citar b26a3fa5 como arreglo. Borra la fila 369 (T25 PARCIAL). Rehaz la fila T5 con su tarea real de la cola, como pide bitacora-estados-1.

### dependencias-bitacora-14 · [media] T25: la persistencia de .ctb y .lin en localStorage no tiene ningún spec

- **Fichero:** `apps/web/src/lib/cad/plot/plot-style-catalog.ts:98`
- **Problema:** La persistencia de 069d575d y 361d7db3 está conectada en producción (session-catalogs.ts:97-98), pero nada la prueba. Los commits citan «plot-style-catalog.spec 22 aserciones» y «linetype.spec pasa»: dos specs que no se tocaron y que no usan localStorage. El criterio de T25 (cargar un .ctb → recargar → sigue cargado) no está demostrado. Si se borra restoreFromStorage, todo sigue en verde.
- **Evidencia:** plot-style-catalog.ts:98 saveToStorage, :108 restoreFromStorage; linetype-lin.ts:215 y :224. `git diff --stat 445352d8 52898e03` no incluye ningún spec de plot-style-catalog ni de linetype. `git grep -n -E "localStorage|valle:cad" 52898e03 -- 'apps/web/src/**/*.spec.ts'` sólo encuentra library-storage.spec.ts y tour-host.spec.ts.
- **Verificación independiente:** Confirmado. saveToStorage y restoreFromStorage están en plot-style-catalog.ts:98 y :108 y en linetype-lin.ts:215 y :224, y se usan en producción (session-catalogs.ts, new CadLinetypeCatalog/new CadPlotStyleCatalog). 069d575d y 361d7db3 sólo tocan esos .ts. git diff --stat 445352d8..52898e03 no incluye ningún spec de plot-style-catalog, linetype ni session-catalogs. git grep localStorage en specs sólo encuentra library-storage, tour-host, CadRibbon (comentario) y persisted-identifiers. Todo va envuelto en try/catch, así que en Node, sin localStorage, nada falla aunque se borre la restauración.
- **Arreglo:** En plot-style-catalog.spec.ts instala un localStorage falso en globalThis y cubre tres casos: (1) load() de una tabla propia; un new CadPlotStyleCatalog() la encuentra con find(). (2) Las tablas de fábrica no se escriben en valle:cad:plot-style-tables. (3) Con JSON corrupto el catálogo arranca sólo con las de fábrica. En session-catalogs.spec.ts haz lo mismo con CadLinetypeCatalog: save de un tipo propio → catálogo nuevo → list() lo incluye una sola vez; remove → ya no aparece al recrear el catálogo.

### dependencias-bitacora-15 · [media] H-11: el spec citado sólo cuenta columnas; ninguna aserción prueba la columna Fase

- **Fichero:** `apps/web/src/lib/cad/data-extraction/data-extraction.spec.ts:72`
- **Problema:** fdd321d1 presenta «data-extraction.spec 25 aserciones» como prueba de la columna «Fase», pero sólo cambia los recuentos de 6 a 7. Ningún muro del spec está en una capa -EXI, -DEM o -NUE, así que nada comprueba «Existente», «Demoler» o «Nuevo», ni en la tabla ni en el CSV. cadHasRenovationLayers, que 48f9c526 anuncia como base del hallazgo en REVISA, no tiene ningún llamador.
- **Evidencia:** data-extraction.spec.ts:72 `ok(table.columns === 7, ...)` y :80 `wallRowCells.length === 7`: el diff no añade más. data-extraction.ts:52 traduce la fase. `git grep -n cadHasRenovationLayers 52898e03 -- apps/web/src` sólo encuentra la definición, en mexican-layers.ts:639.
- **Verificación independiente:** Confirmado. El diff de fdd321d1 en data-extraction.spec.ts sólo cambia 6→7 en :72 y :80. El spec sólo usa las capas "MUROS" y "0" (:26, :90-92) y no busca Existente, Demoler, Nuevo ni Fase. data-extraction.ts traduce la fase en wallRowValues sin prueba. cadHasRenovationLayers sólo aparece en su definición (mexican-layers.ts:639), aunque 48f9c526 la anuncia como base del hallazgo en REVISA.
- **Arreglo:** Añade a data-extraction.spec.ts muros en las capas MURO-EXI, MURO-DEM, MURO-NUE y MURO, y comprueba que la columna Fase dice «Existente», «Demoler», «Nuevo» y «—» tanto en buildCadWallScheduleTable como en el CSV. Después, o conectas cadHasRenovationLayers al hallazgo de REVISA prometido, con su spec, o la borras.

### dependencias-bitacora-16 · [media] Choques con filtro routeIds: se pierde el choque contra una ruta no filtrada con id menor

- **Fichero:** `apps/web/src/lib/cad/plant/clash.ts:711`
- **Problema:** Con routeIds (lo que usa PIDROUTE al cerrar una ruta), el bucle exterior recorre sólo `mias` y el interior descarta los pares con a.entityId > b.entityId. Si la ruta filtrada es "r2" y la otra, fuera del filtro, es "r1", el par a="r2", b="r1" se salta, y "r1" nunca entra en el bucle exterior: ese choque no se evalúa. Antes, el bucle i<j sobre `todas` con filtro de pertenencia sí lo detectaba. El spec del filtro no lo ve porque sus dos rutas están a 6 m y no chocan entre sí.
- **Evidencia:** clash.ts:613-615 `const mias = options.routeIds ? todas.filter(...) : todas;`; :705-711 `for (i < mias.length) for (j < todas.length) { ... if (a.entityId > b.entityId) continue;`. clash.spec.ts:387-400 pone las rutas r1 y r2 en x=2000 y x=8000, con routeIds ["r2"].
- **Verificación independiente:** Confirmado. clash.ts:613-615 filtra `mias`. El bucle (:705-711) recorre mias×todas y descarta a.entityId > b.entityId. Con routeIds ["r2"], mias=[r2]: el par (r2, r1) se salta por "r2">"r1", y r1 nunca es exterior, así que el choque r1–r2 se pierde. El diff de 52898e03 muestra que antes el bucle i<j sobre todas, con filtro de pertenencia, sí lo evaluaba. En clash.spec.ts:387-400 las rutas están en x=2000 y x=8000, no se cruzan, y no lo detecta.
- **Arreglo:** En clash.ts:711 salta el par por orden de id sólo cuando b también está en `mias` (con un Set de ids: `if (miasIds.has(b.entityId) && a.entityId > b.entityId) continue;`). Añade a clash.spec.ts dos rutas "r1" y "r2" que se crucen. Comprueba que cadPipeClashReport(doc, { routeIds: ["r2"] }) devuelve el choque ruta contra ruta y que, sin filtro, ese choque sale una sola vez.

### dependencias-bitacora-17 · [baja] La pérdida D2 de metadatos avisa también de metadatos que no son de dominio

- **Fichero:** `apps/web/src/lib/cad/dxf-export-loss-manifest.ts:403`
- **Problema:** La pérdida nueva cuenta cualquier entidad con context.metadata no vacío y la describe como «metadatos de dominio (circuito, calibre, número de línea, etiqueta de equipo…)». Pero el importador DXF guarda en metadata la procedencia (sourceType, sourceLayer), que no es de dominio. Al reexportar cualquier DXF importado sale un aviso que describe algo que no está en el documento.
- **Evidencia:** dxf-export-loss-manifest.ts:402-404 `if (entity.context?.metadata && Object.keys(entity.context.metadata).length > 0) metadataEntities += 1;`. dxf-cad-document.ts:459 `metadata: { sourceType: "MLEADER", sourceLayer: mleader.layer }` (hay 6 `metadata: {` en ese fichero).
- **Verificación independiente:** Confirmado (severidad baja). dxf-export-loss-manifest.ts:402-404 cuenta toda entidad con context.metadata no vacío, y el texto (:411) habla de «metadatos de dominio (circuito, calibre…)». El importador escribe metadata de procedencia: dxf-cad-document.ts:289 { sourceType: "MTEXT", sourceLayer }, :459 { sourceType: "MLEADER", sourceLayer }, :775 { sourceType: "INSERT", sourceBlock }, además de :70, :229 y :371. Al reexportar un DXF importado sale ese aviso con un recuento que no corresponde a datos de dominio.
- **Arreglo:** Cuenta sólo las claves de dominio que de verdad no viajan: las constantes CAD_IE_* de electrical (circuito, calibre, protección, número de conductor) y las de plant y mechanical (número de línea, TAG). Ignora sourceType y sourceLayer. Añade a dxf-export-losses.spec.ts dos casos: una entidad importada con sólo sourceType no produce esa pérdida, y un conductor con circuito produce una con recuento 1.

### dependencias-bitacora-18 · [baja] La fila T4 sigue «BLOQUEADA por falta de edge-ray.ts y solid-edge-ref.ts», que ya existen

- **Fichero:** `docs/execution/NOCHE_MIMO_20260914.md:357`
- **Problema:** 1be2676e creó edge-ray.ts y solid-edge-ref.ts con sus specs, pero la tabla (línea 357) y la sección de la línea 424 no se actualizaron. Siguen citando esos ficheros, y CAD_ACCEPT_EDGE_PICK, como lo que falta. El hallazgo bitacora-t4-razon sigue abierto y la bitácora está más desfasada que antes.
- **Evidencia:** 52898e03 contiene apps/web/src/lib/cad/pick3d/edge-ray.ts y apps/web/src/lib/cad/pick3d/solid-edge-ref.ts (1be2676e). NOCHE_MIMO_20260914.md:357 «T4 | BLOQUEADA | Requiere edge-ray.ts + solid-edge-ref.ts» y :424 sin cambios desde 445352d8. command-types.ts:118 ya declara CAD_ACCEPT_EDGE_PICK.
- **Verificación independiente:** Confirmado. 1be2676e añade apps/web/src/lib/cad/pick3d/edge-ray.ts, solid-edge-ref.ts y sus dos specs, y no toca la bitácora. command-types.ts:118 ya declara CAD_ACCEPT_EDGE_PICK = 256. En 52898e03 la bitácora sigue diciendo «| T4 | BLOQUEADA | Requiere edge-ray.ts + solid-edge-ref.ts» (:357), y la sección (:421-424) sigue listando esos ficheros y CAD_ACCEPT_EDGE_PICK como pendientes.
- **Arreglo:** Cambia la fila T4 a «PARCIAL (1be2676e): edge-ray.ts y solid-edge-ref.ts con spec; falta atender CAD_ACCEPT_EDGE_PICK (ya declarado en command-types.ts:118) en pointer-router y acumular referencias en FILLETEDGE/CHAMFEREDGE», y corrige igual la línea 424.
