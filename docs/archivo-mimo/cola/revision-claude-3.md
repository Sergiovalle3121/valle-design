# Tercera revisión de Claude: tus 18 commits de 05:37 a 08:54 (15-sep)

Varios revisores leyeron tus commits en `d9c1cac5` y un verificador escéptico por bloque intentó refutar cada hallazgo. **Sólo aparecen aquí los que sobrevivieron a la verificación.**

Cómo trabajarlos:
- Van antes que cualquier tarea nueva: primero los que rompen el CI (specs o goldens existentes que fallan), luego los de severidad alta.
- Un commit por hallazgo con el prefijo `Revisión:` y el id del hallazgo en el mensaje.
- Cada arreglo lleva un spec que habría fallado con el defecto: que ejecute el comando o la función real, no una copia hecha a mano.
- Anota en la bitácora cada hallazgo cerrado, con su id.
- Si un hallazgo te parece falso después de leer el código, escribe la razón en `docs/execution/PREGUNTAS.md` y sigue con el siguiente.

## T4 · Designación de aristas en 3D y FILLETEDGE/CHAMFEREDGE con aristas designadas (1d9373a6, a82a2756, 2473693a, 924042f9, 42700c05, 2633f9a2; punta d9c1cac5)

**Resumen del revisor:** Qué funciona de verdad: la cadena está conectada de punta a punta. En 3D, el clic llega a CadEnginePointerRouter.click (Layout3DEditor.tsx:7303). Como el paso de FILLETEDGE/CHAMFEREDGE declara CAD_ACCEPT_EDGE_PICK, el router llama a bridge.hitEdge, que usa cadEdgePickerFor → cadDocumentEdgeUnderRay → hitEdge. Luego host.pickEdge envía un input edgePick; el comando lo acumula como {entityId, edge} y escribe el índice en `edges` del nodo fillet/chamfer, y el kernel (filletEdges/chamferEdges) redondea ese índice. En el caso sencillo funciona: sólido sin reflexión, cámara oblicua, arista visible no paralela al rayo, clic justo encima. Qué no funciona: (1) No hay referencia persistente. solid-edge-ref.ts no tiene ni un llamador fuera de su spec, el nodo guarda el índice crudo que el propio kernel dice que cambia, y from/to se descartan. (2) En un sólido reflejado con MIRROR, el índice sale del cuerpo colocado (reconstruido con reverseBody), no del operando, así que se redondea otra arista. (3) Siguen en d9c1cac5, sin cambios desde 52898e03, los defectos ya confirmados de raySegmentDistance: acota u sin recalcular t y descarta las aristas paralelas al rayo. edge-ray.spec sigue aprobando con la arista equivocada, y el cableado nuevo hereda todo esto. (4) No hay oclusión ni desempate por profundidad, así que se designan aristas ocultas o de sólidos que quedan detrás. (5) La tolerancia es el 2 % de la diagonal del cuerpo, no píxeles. (6) Las aristas de un sólido no designado se cuentan en el prompt y luego se descartan en silencio, y se aplica la auto-selección. Pinchar dos veces la misma arista hace fallar la orden. (7) No hay resaltado al pasar el cursor ni forma de quitar una arista. (8) Ningún spec ejecuta el camino nuevo, y solid-edge-ref.spec sigue sin re-evaluar el cuerpo. (9) La bitácora marca T4 como HECHA, lo cual es falso. No se rompe ningún spec existente: solids.spec sólo cubre la auto-selección, que sigue igual.

**Afirmaciones de tus commits que no se cumplen:**

- 2473693a: el input edgePick acumula REFERENCIAS de arista
  - Evidencia: solids-modify.ts:209 guarda sólo {entityId, edge} (índice). Los campos from/to, que command-types.ts:144 describe como 'la huella que se comprueba', se descartan, y no se calcula ninguna CadSolidEdgeRef.
- 2473693a: el usuario ahora puede pinchar la arista que quiere redondear
  - Evidencia: Una arista paralela al rayo nunca se detecta (edge-ray.ts:103-106 + t>0 en 153). Se eligen aristas ocultas (edge-ray.ts:154 sin t). En un sólido reflejado se redondea otra arista (solid3d-build.ts:639 reverseBody frente a 406-409 operando sin colocar). No hay resaltado que permita ver qué se designó.
- 2633f9a2: T4 HECHA, 'edge picking completo: edge-ray + solid-edge-ref + ... + FILLETEDGE/CHAMFEREDGE acumulan aristas'
  - Evidencia: NOCHE_MIMO_20260914.md:357 y 387. solid-edge-ref no participa en el camino, no hay persistencia por huella, siguen los defectos de raySegmentDistance, falla con sólidos reflejados y ningún spec ejercita edgePick, hitEdge, cadDocumentEdgeUnderRay ni cadEdgePickerFor (git grep sobre *.spec.* sin resultados).
- Los defectos confirmados en 52898e03 (raySegmentDistance, edge-ray.spec, solid-edge-ref con igualdad exacta, solid-edge-ref.spec sin re-evaluar) están corregidos en d9c1cac5
  - Evidencia: git diff --stat 52898e03 d9c1cac5 -- apps/web/src/lib/cad/pick3d sólo toca document-face-pick.ts y scene-ray.ts; edge-ray.ts, edge-ray.spec.ts, solid-edge-ref.ts y solid-edge-ref.spec.ts no cambian.

### t4-designar-aristas-1 · [alta] Se persiste el índice crudo de la arista; la huella (solid-edge-ref) nunca se usa

- **Fichero:** `apps/web/src/lib/cad/engine/commands/solids-modify.ts:209`
- **Problema:** FILLETEDGE/CHAMFEREDGE guardan en el nodo `edges: pickedForSource` (líneas 226-232), que son índices de body.edges. Descartan from/to del edgePick y no calculan ni guardan ninguna CadSolidEdgeRef. En el kernel, resolveEdges (solid3d-build.ts:494-497) usa esos índices a ciegas. El propio kernel lo desaconseja: 'los índices de arista cambian si se edita un nodo anterior del árbol' (solid3d-build.ts:455-457). Al re-evaluar tras cambiar algo aguas arriba (o el orden de salida de una booleana), se redondea otra arista sin avisar, o el sólido deja de evaluar con 'Índice de arista fuera de rango' (fillet.ts:209-210). resolveCadEdgeRef (solid-edge-ref.ts:115) no tiene llamadores; cadEdgeRefFromBody se importa sin uso en document-face-pick.ts:31. Además refsMatch sigue comparando con igualdad exacta (solid-edge-ref.ts:99-107), y su spec no re-evalúa ningún árbol (solid-edge-ref.spec.ts:11-34).
- **Evidencia:** git grep 'resolveCadEdgeRef\|cadEdgeRefFromBody' d9c1cac5 sólo encuentra solid-edge-ref.ts, su spec y el import muerto de document-face-pick.ts:31. solids-modify.ts:209: `pickedEdges: [...state.pickedEdges, { entityId: input.entityId, edge: input.edge }]`. cad-entities-v5.ts:204: `edges: number[]`.
- **Verificación independiente:** Confirmado. solids-modify.ts:226-232 guarda en el nodo `edges: pickedForSource`: índices crudos. from/to del edgePick se tiran en :209. solid3d-build.ts:494-497 (resolveEdges) usa esos índices tal cual contra evaluate(node.operand), y el comentario de :455-457 reconoce que 'los índices de arista cambian si se edita un nodo anterior del árbol'. Si el índice sale fuera de rango, fillet.ts:209-210 lanza. git grep en d9c1cac5 encuentra resolveCadEdgeRef y cadEdgeRefFromBody sólo en solid-edge-ref.ts, su spec y el import muerto de document-face-pick.ts:31. refsMatch (solid-edge-ref.ts:99-107) compara los extremos cuantizados con ===, y el spec (líneas 11-34) resuelve contra el mismo objeto `body`. Matiz: índices en el nodo es el esquema previsto (cad-entities-v5.ts:203; cabecera de solids-modify.ts:42-43) y no encontré ningún camino que remapee edges. El riesgo llega al re-evaluar tras editar el operando, o si cambia el orden de salida del kernel. Aun así la huella no protege nada y la bitácora la vende como parte de T4.
- **Arreglo:** 1) Añade al nodo fillet/chamfer de cad-entities-v5.ts un campo opcional y aditivo `edgeRefs?: CadSolidEdgeRef[]`, sin tocar `edges`. 2) En solids-modify.ts, al ejecutar, calcula cada huella con cadEdgeRefFromBody sobre el cuerpo del OPERANDO sin colocar (el mismo que evalúa el nodo) y guárdala en edgeRefs. 3) En solid3d-build.ts, cuando haya edgeRefs, resuelve cada una con resolveCadEdgeRef y lanza un error con `reason` si ok=false, en vez de fiarte del índice. 4) Cambia refsMatch a comparación con tolerancia (|Δ| ≤ 2·linearStep) en lugar de igualdad de valores cuantizados. 5) En solid-edge-ref.spec añade un caso que re-evalúe el árbol con una operación anterior que renumere aristas sin mover la designada (p. ej. unión con una caja lejana) y exija ok=true, healed=true y la arista correcta.

### t4-designar-aristas-2 · [alta] En un sólido reflejado (MIRROR) se redondea una arista distinta de la pinchada

- **Fichero:** `apps/web/src/lib/cad/pick3d/document-face-pick.ts:116`
- **Problema:** El índice se obtiene de solid3dBody(entity), que es el cuerpo YA COLOCADO (solid3d-build.ts:334). Cuando placement refleja (determinante < 0, que es lo que compone MIRROR según solid3d-adapter.ts:20-21), placeBody devuelve reverseBody(moved) (solid3d-build.ts:607 y 639). reverseBody invierte cada lazo y lo reconstruye con buildBody, que numera las aristas por orden de aparición (body-builder.ts:119-121 y 268-278). El nodo fillet, en cambio, resuelve el índice contra evaluate(node.operand) sin colocar (solid3d-build.ts:405-419). Los dos cuerpos numeran distinto.
- **Evidencia:** Cara con lazo [a,b,c,d]: sin colocar se numera (a,b)=0, (b,c)=1, (c,d)=2, (d,a)=3. Invertida [d,c,b,a], buildBody crea d→c primero: (c,d)=0, (b,c)=1, (a,b)=2. El usuario pincha (c,d), se persiste edges=[0] y el kernel redondea (a,b).
- **Verificación independiente:** Confirmado. cadDocumentEdgeUnderRay usa solid3dBody(entity) (document-face-pick.ts:116), que devuelve placeBody(evaluate(root), placement) (solid3d-build.ts:334). El nodo fillet resuelve el índice sobre evaluate(node.operand), sin colocar (solid3d-build.ts:405-419). MIRROR compone la reflexión en placement (solid3d-adapter.ts:20-21 y 307-317), y con determinante negativo placeBody devuelve reverseBody(moved) (solid3d-build.ts:607 y 639). reverseBody invierte cada lazo (body-builder.ts:268-278). buildBody numera las aristas al crear la primera semiarista de cada par (body-builder.ts:119-121), y loopHalfEdges empieza en loop.first (topology.ts:134-147). Sin reflexión, la reconstrucción conserva el orden. Con reflexión, el lazo [a,b,c,d] pasa a [d,c,b,a]: (c,d)=0, (b,c)=1, (a,b)=2, cuando sin colocar (a,b)=0 y (c,d)=2. El índice pinchado apunta a otra arista del operando.
- **Arreglo:** En cadDocumentEdgeUnderRay (o en el comando), obtén índice y huella sobre el cuerpo del operando SIN placement. Evalúa el árbol sin placeBody y transforma el rayo con la inversa de entity.placement antes de llamar a hitEdge, o casa por huella en coordenadas locales. Añade un spec: EXTRUDE de un rectángulo, MIRROR, rayo que apunte a una arista concreta, FILLETEDGE con ese edgePick. Debe comprobar que la arista redondeada es exactamente la pinchada (sus extremos locales ya no existen en el cuerpo resultante y los de las demás sí).

### t4-designar-aristas-3 · [alta] raySegmentDistance sigue mal: acota u sin recalcular t y descarta las aristas paralelas al rayo; ahora se llega desde la UI

- **Fichero:** `apps/web/src/lib/cad/pick3d/edge-ray.ts:103`
- **Problema:** Sin cambios desde 52898e03, y ahora es el primer eslabón del clic real. Si el segmento es paralelo al rayo se fuerza t=0 y u=0.5 (103-106), y la condición `result.t > 0` (153) lo descarta siempre: la arista vertical de una esquina vista desde arriba nunca se designa y se devuelve otra. Si u sale de [0,1], se acota (111) pero se conserva el t de la recta infinita, así que la distancia se sobrestima: aristas válidas quedan fuera del umbral o pierden frente a otras. edge-ray.spec.ts:17-28 aprueba con la arista equivocada.
- **Evidencia:** Caso 1 del spec: rayo O=(1050,0,250), d=(0,0,-1). La arista vertical x=1000,y=0 da denom=0 → t=0 → rechazada. Gana una horizontal en z=0 (t=250, u=1.05 acotado a 1, distancia 50), y `point.x > 999` también se cumple con ella. Sobrestimación: segmento (0,0,0)-(1000,0,0), rayo O=(1300,100,0), d=(-1,-1,0) → u=1.2 acotado a 1, P=(1200,0,0), distancia 200; la mínima real a (1000,0,0) es 141,4. Con maxDistance 150 se rechaza una arista que sí está dentro.
- **Verificación independiente:** Confirmado en edge-ray.ts. Con rayo y segmento paralelos se fija t=0, u=0.5 (:103-106), y `result.t > 0` (:153) los rechaza siempre. Si u sale de [0,1] se acota (:111) pero se conserva el t de la recta infinita (:108), así que P y la distancia salen de un t que no corresponde. Recalculé el ejemplo O=(1300,100,0), d=(-1,-1,0) contra (0,0,0)-(1000,0,0): t=100, u=1.2→1, P=(1200,0,0), distancia 200, cuando la real es 141,4. En el caso 1 del spec (edge-ray.spec.ts:15-28), con la numeración de makeBox (primitives.ts:87, cara [0,3,2,1]), la arista vertical x=1000,y=0 se rechaza por paralela. Gana e2, la horizontal (1000,600,0)-(1000,0,0) con u=1 y distancia 50. El revisor citó la de u=1,05, que empata en 50 pero pierde por índice; da igual, gana una horizontal en z=0 y `point.x > 999` también se cumple. El spec no comprueba el índice.
- **Arreglo:** Reescribe raySegmentDistance: si u sale de [0,1], acota u y RECALCULA t = max(0, dir·(Q(u)−O)/|dir|²) y la distancia con ese t. En el caso paralelo, calcula la distancia perpendicular entre el rayo y la recta y toma el punto del segmento más cercano con t ≥ 0, en vez de t=0 y u=0.5. Cambia el caso 1 de edge-ray.spec para que exija el ÍNDICE de la arista esperada (búscalo por extremos en body.edges) y añade un rayo oblicuo junto a un extremo con el valor numérico de la distancia mínima.

### t4-designar-aristas-10 · [alta] Bitácora: T4 marcada HECHA con el camino roto

- **Fichero:** `docs/execution/NOCHE_MIMO_20260914.md:357`
- **Problema:** Las filas 357 y 387 dicen 'T4 | HECHA | Edge picking completo: edge-ray + solid-edge-ref + …'. solid-edge-ref no participa, el índice no sobrevive a re-evaluar, falla con sólidos reflejados, siguen los defectos de raySegmentDistance, no hay oclusión ni resaltado y ningún spec cubre el camino.
- **Evidencia:** git grep -n 'T4 | HECHA' d9c1cac5 -- docs/execution/NOCHE_MIMO_20260914.md → líneas 357 y 387; hallazgos 1 a 9.
- **Verificación independiente:** Confirmado. docs/execution/NOCHE_MIMO_20260914.md:357 y :387 dicen 'T4 | HECHA | Edge picking completo: edge-ray + solid-edge-ref + …'. solid-edge-ref no tiene llamadores (hallazgo 1), el índice falla con sólidos reflejados (2), hay errores numéricos y de oclusión en la designación (3, 4) y ningún spec cubre el camino real (9). Los propios mensajes de 1d9373a6 y a82a2756 decían '(parcial)'. Además la fila está duplicada.
- **Arreglo:** Devuelve T4 a PARCIAL en las líneas 357 y 387 y enumera lo pendiente: huella persistente en el nodo, índice sobre el operando sin colocar (reflexión), raySegmentDistance, oclusión, tolerancia en píxeles, deduplicación y filtro por selección, resaltado y specs del camino real. No la marques HECHA hasta que exista el spec de extremo a extremo.

### t4-designar-aristas-4 · [media] Designa aristas ocultas: sin oclusión y sin desempate por profundidad

- **Fichero:** `apps/web/src/lib/cad/pick3d/edge-ray.ts:154`
- **Problema:** hitEdge sólo compara la distancia perpendicular (`result.distance < best.distance`) e ignora t. Una arista trasera, oculta por la cara que el usuario ve, gana si queda más cerca del rayo o empata y tiene índice menor. cadDocumentEdgeUnderRay repite el criterio entre sólidos (document-face-pick.ts:122), mientras que la designación de caras ordena por t (document-face-pick.ts:74): la arista de un sólido de detrás gana a la del de delante.
- **Evidencia:** Caso 3 de edge-ray.spec.ts:54-63: el rayo O=(100,-100,0), d=(0,1,0) está a distancia 0 de (0,0,0)-(200,0,0) con t=100 y de (0,200,0)-(200,200,0) con t=300. La comparación estricta se queda con la de menor índice, no con la más cercana, y el spec sólo mira point.x≈100, así que no las distingue.
- **Verificación independiente:** Confirmado, y peor de lo descrito. hitEdge sólo compara `result.distance < best.distance` (edge-ray.ts:154) y no mira t ni lanza ningún rayo contra caras. cadDocumentEdgeUnderRay elige entre sólidos por distance (document-face-pick.ts:122), mientras que la designación de caras ordena por t (:74). Calculé el caso 3 del spec (edge-ray.spec.ts:50-63) con el orden de makeBox. La arista trasera (0,200,0)-(200,200,0) es e1, con distancia 0 y t=300. La delantera (200,0,0)-(0,0,0) es e3, con distancia 0 y t=100. e1 va antes y el < estricto la conserva, así que hitEdge devuelve la arista OCULTA. El spec aprueba igual porque sólo comprueba point.x≈100.
- **Arreglo:** En hitEdge, lanza primero cadFaceRayHit contra el cuerpo y acepta sólo aristas con t ≤ t_cara + tolerancia (o aristas de la cara impactada). Ante distancias iguales desempata por menor t. En cadDocumentEdgeUnderRay elige entre sólidos por t y no por distancia perpendicular. Haz que el caso 3 del spec exija el índice de la arista delantera (y=0) y añade uno con dos sólidos alineados en el que deba ganar el de delante.

### t4-designar-aristas-5 · [media] La tolerancia de designación no está en píxeles: es el 2 % de la diagonal de cada cuerpo

- **Fichero:** `apps/web/src/lib/cad/pick3d/document-face-pick.ts:117`
- **Problema:** cadDocumentEdgeUnderRay llama a hitEdge(body, ray) sin maxDistance, así que el umbral es `scale * 0.02` en unidades de dibujo (edge-ray.ts:145). Eso contradice lo que documenta edge-ray.ts:19-21 y 135-137 ('umbral en píxeles proyectado con la distancia de la cámara'). Un sólido pequeño o lejano tiene una tolerancia de menos de un píxel y apenas se puede designar; uno grande y ampliado acepta clics muy lejos de la arista. Como el umbral es distinto en cada cuerpo, la comparación entre sólidos mezcla escalas.
- **Evidencia:** document-face-pick.ts:117 `hit = hitEdge(body, ray);`; edge-ray.ts:145 `const maxDist = options.maxDistance ?? scale * 0.02; // 2% de la diagonal por defecto`.
- **Verificación independiente:** Confirmado. document-face-pick.ts:117 llama a `hitEdge(body, ray)` sin opciones, y edge-ray.ts:145 usa entonces `scale * 0.02`, con scale = diagonal del AABB de cada cuerpo (:61-66). cadEdgePickerFor (scene-ray.ts) no pasa ninguna tolerancia. La cabecera de edge-ray.ts:19-21 y el JSDoc :135-137 prometen un umbral en píxeles proyectado con la cámara que no existe. Cada sólido tiene su propio umbral, así que la comparación entre sólidos de :122 mezcla escalas.
- **Arreglo:** Añade a cadEdgePickerFor un parámetro de tolerancia en píxeles. Conviértela a unidades de dibujo a la profundidad t del candidato con el tamaño de píxel de la cámara (como cadPointerWorldTolerance) y pásala como maxDistance. Compara los candidatos por distancia en píxeles. Si no se implementa, borra de edge-ray.ts la documentación que promete un umbral en píxeles.

### t4-designar-aristas-6 · [media] Aristas de un sólido no designado: cuentan en el prompt, se descartan y se aplica la auto-selección

- **Fichero:** `apps/web/src/lib/cad/engine/commands/solids-modify.ts:226`
- **Problema:** El paso acepta el edgePick de cualquier sólido del documento (207-209) y el prompt dice '(N arista(s) designada(s))' (183-185). Al ejecutar, sólo se usan las del sólido que está en la selección. Si ninguna le corresponde, pickedForSource queda vacío y el nodo recibe `edges: []`, es decir, preferredFeatureEdges: se redondea el juego automático, justo lo contrario de lo que pidió el usuario, y sin aviso. Con varios sólidos designados, los que no tienen aristas pinchadas también reciben la auto-selección.
- **Evidencia:** Con A designado previamente, clic en una arista de B → prompt 'Precise el radio del redondeo (1 arista(s) designada(s))' → se teclea 10 → A recibe fillet con edges=[] (auto) y B no cambia (solids-modify.ts:218-232 sólo recorre selectedSolids).
- **Verificación independiente:** Confirmado. El paso con selección no vacía acepta DISTANCE|POINT|EDGE_PICK sin ENTITY_PICK (solids-modify.ts:186-190). cadDocumentEdgeUnderRay recorre TODOS los solid3d del documento (document-face-pick.ts:111). La rama edgePick acumula sin mirar si input.entityId está en la selección (:207-209), y el prompt cuenta todas las acumuladas (:183-185). Al ejecutar sólo se recorren selectedSolids(context, state.selection) (:218-221; solids-support.ts:46-53), filtrando pickedEdges por source.id (:226-228). Si ninguna coincide, el nodo recibe edges=[], y resolveEdges lo convierte en preferredFeatureEdges (solid3d-build.ts:494-497). Se aplica la auto-selección sin aviso, y lo mismo pasa a los sólidos designados sin aristas pinchadas cuando otros sí las tienen.
- **Arreglo:** En la rama edgePick, si input.entityId no está en state.selection, devuelve solidMessage explicando que la arista no pertenece a los sólidos designados, o añade el sólido a la selección. Al ejecutar, si state.pickedEdges no está vacío, no apliques la auto-selección a los sólidos sin aristas designadas: déjalos intactos y dilo en el aviso. Que el prompt cuente sólo las aristas que se van a aplicar.

### t4-designar-aristas-7 · [media] Pinchar dos veces la misma arista hace fallar la orden entera

- **Fichero:** `apps/web/src/lib/cad/engine/commands/solids-modify.ts:209`
- **Problema:** La acumulación no deduplica. Un segundo clic sobre la misma arista produce edges=[k,k]. chamferEdges compara cada par con edgesShareVertex, y una arista comparte vértices consigo misma, así que lanza 'Las aristas k y k comparten un vértice…'. finishedSolid lo convierte en 'FILLETEDGE no produjo un sólido válido' por una única arista que el usuario ve.
- **Evidencia:** fillet.ts:228-233 recorre los pares i<j y lanza si edgesShareVertex; fillet.ts:286-292 devuelve true con distancia 0 entre extremos idénticos. solids-modify.ts:209 añade sin comprobar si ya estaba.
- **Verificación independiente:** Confirmado. solids-modify.ts:209 añade {entityId, edge} sin comprobar si ya estaba, y :228 lo mapea a [k,k]. validateSolidTree sólo detecta nodos duplicados, no aristas repetidas (solid3d-build.ts:172-174). chamferEdges, al que delega filletEdges (fillet.ts:249-251), recorre los pares i<j (:228-234). edgesShareVertex (:286-292) devuelve true con distancia 0 entre extremos idénticos y lanza 'Las aristas k y k comparten un vértice…'. finishedSolid lo atrapa y responde '<orden> no produjo un sólido válido: …' (solids-support.ts:161-167).
- **Arreglo:** Al acumular, deduplica por (entityId, edge), o mejor por huella. Si la arista ya estaba designada, quítala (alternar) e indícalo en el prompt.

### t4-designar-aristas-8 · [media] Sin resaltado al pasar el cursor, sin mostrar las designadas y sin forma de quitar una

- **Fichero:** `apps/web/src/components/cad/viewport/pointer-router.ts:296`
- **Problema:** move() (296-325) nunca consulta hitEdge ni pinta nada cuando el paso acepta EDGE_PICK. El paso de FILLETEDGE/CHAMFEREDGE no devuelve preview (solids-modify.ts:186-190), así que las aristas designadas tampoco se ven, y no hay opción para quitar la última: sólo cancelar toda la orden. Como hitEdge puede elegir una arista oculta o equivocada (hallazgos 3 y 4), el usuario no tiene cómo enterarse antes de teclear el radio.
- **Evidencia:** pointer-router.ts:296-325 sólo resuelve la captura y refreshPreview; solids-modify.ts:188 `prompt: { message: ..., options: [] }` sin preview; no hay ninguna rama de keyword en el step de edgeFeatureDescriptor.
- **Verificación independiente:** Confirmado (es una carencia de UX más que un fallo de lógica). pointer-router.ts:296-325 (move) sólo resuelve captura, cursor y refreshPreview, y nunca llama a hitEdge; git grep 'hitEdge' en apps/web/src sólo lo encuentra en el click (pointer-router.ts:359), el bridge del editor y document-face-pick.ts:117. El paso de FILLETEDGE/CHAMFEREDGE devuelve prompt con `options: []` y sin preview (solids-modify.ts:186-190), y step() no tiene rama keyword, así que no hay forma de quitar una arista salvo cancelar. Con los hallazgos 3, 4 y 6, el usuario no puede ver qué arista se designó antes de teclear el radio.
- **Arreglo:** En move(), si accepts & CAD_ACCEPT_EDGE_PICK, llama a bridge.hitEdge y dibuja el segmento from→to en el overlay de previsualización con un token de color del sistema de diseño, nunca un hex. Guarda from/to en el estado del comando y devuelve como preview del paso las aristas ya designadas. Añade la opción «Deshacer» (atajo U) para quitar la última arista designada.

### t4-designar-aristas-9 · [media] Ningún spec ejercita el camino nuevo; los que existen no prueban lo que dicen

- **Fichero:** `apps/web/src/lib/cad/pick3d/edge-ray.spec.ts:17`
- **Problema:** No hay spec de cadDocumentEdgeUnderRay, cadEdgePickerFor, la rama EDGE_PICK del router, ni FILLETEDGE/CHAMFEREDGE con edgePick. solids.spec.ts:316 y 330 sólo cubren la auto-selección. edge-ray.spec aprueba con la arista equivocada (caso 1) y no distingue la delantera de la trasera (caso 3). solid-edge-ref.spec.ts:11-34 resuelve la huella contra el mismo objeto body, sin re-evaluar ningún árbol, así que no prueba 'se reconoce a sí misma tras re-evaluar el cuerpo' como anuncia su cabecera (línea 4).
- **Evidencia:** git grep 'edgePick\|hitEdge\|cadDocumentEdgeUnderRay\|cadEdgePickerFor\|pickEdge\|resolveCadEdgeRef' d9c1cac5 -- '*.spec.ts' '*.spec.tsx' '*.spec.mjs' '*.test.ts' apps/web/e2e, excluyendo edge-ray.spec.ts y solid-edge-ref.spec.ts, no devuelve nada.
- **Verificación independiente:** Confirmado. git grep 'edgePick|hitEdge|cadDocumentEdgeUnderRay|cadEdgePickerFor|pickEdge|resolveCadEdgeRef' en *.spec.ts/*.spec.tsx/*.spec.mjs/*.test.ts y apps/web/e2e sólo devuelve edge-ray.spec.ts y solid-edge-ref.spec.ts. solids.spec.ts:316 y :330 ejecutan CHAMFEREDGE/FILLETEDGE con [select, distance], es decir, sólo la auto-selección. edge-ray.spec aprueba el caso 1 con una arista horizontal y el caso 3 con la trasera (ver hallazgos 3 y 4). solid-edge-ref.spec.ts:4 anuncia 'se reconoce a sí misma tras re-evaluar el cuerpo', pero :12-16 resuelve contra el mismo `body`, sin re-evaluar nada.
- **Arreglo:** Añade: (a) un spec de cadDocumentEdgeUnderRay con dos sólidos y rayo oblicuo que exija entityId e índice concretos; (b) un spec de CadEnginePointerRouter con host real ejecutando FILLETEDGE y un bridge falso cuyo hitEdge devuelva una arista, que compruebe que el nodo final tiene exactamente esa arista; (c) en solids.spec, FILLETEDGE con [select, edgePick(arista k), distance] comparando el volumen retirado con el de redondear sólo k y exigiendo que difiera del juego automático; (d) en solid-edge-ref.spec, re-evaluar con evaluateSolidTree un árbol modificado antes de resolver. Corrige también los casos 1 y 3 de edge-ray.spec para que comprueben índices.

### t4-designar-aristas-11 · [baja] Import muerto de cadEdgeRefFromBody fuera del presupuesto de lint

- **Fichero:** `apps/web/src/lib/cad/pick3d/document-face-pick.ts:31`
- **Problema:** Se importa cadEdgeRefFromBody y no se usa. @typescript-eslint/no-unused-vars está en 'warn' (apps/web/eslint.config.mjs:42) y scripts/lint-budget.json no tiene entrada para este fichero (presupuesto 0), así que check:lint-budget, que forma parte de check:cad y corre en CI (ci.yml:238), cuenta un aviso por encima del presupuesto.
- **Evidencia:** document-face-pick.ts:31 `import { cadEdgeRefFromBody } from "./solid-edge-ref";` sin ningún uso en el fichero; check-lint-budget.mjs:100-103 falla si have > max, con max = 0 por defecto.
- **Verificación independiente:** Confirmado. document-face-pick.ts:31 importa cadEdgeRefFromBody, y git grep no encuentra ningún otro uso en el fichero. apps/web/eslint.config.mjs:42-45 pone @typescript-eslint/no-unused-vars en 'warn', y el nombre no empieza por _. scripts/lint-budget.json:26-30 sólo presupuesta Layout3DEditor.tsx, i18n/coverage.ts y pick3d/face-push.spec.ts para esa regla. check-lint-budget.mjs:99-103 usa max=0 por defecto y falla si have>max. package.json:60 incluye `npm run check:lint-budget` en check:cad, que corre en CI (ci.yml:237-238). No hay noUnusedLocals en tsconfig, así que sólo lo detecta ese trinquete.
- **Arreglo:** Quita el import, o úsalo de verdad para construir la huella que se persiste (hallazgo 1).

### t4-designar-aristas-12 · [baja] Layout3DEditor crece 8 líneas duplicando el cierre sceneRay, ya por encima del trinquete

- **Fichero:** `apps/web/src/components/cad/editor/Layout3DEditor.tsx:6607`
- **Problema:** a82a2756 copia en hitEdge (6607-6614) el mismo bloque mode/document/frame/sceneRay de hitFace (6599-6606) y lleva el fichero de 16901 a 16909 líneas. La asignación de scripts/cad/monolith-budget.json:29 es 16896 y el gate sólo permite bajar. scene-ray.ts:76-79 dice que existe precisamente para que el editor gane líneas mínimas.
- **Evidencia:** wc -l: 1d9373a6 → 16901, a82a2756 → 16909; monolith-budget.json:29 → 16896.
- **Verificación independiente:** Confirmado. wc -l de Layout3DEditor.tsx: 16896 en 0a5f5600, 16901 en 1d9373a6 y en su padre (1d9373a6 no toca el fichero), y 16909 en a82a2756 y d9c1cac5. La asignación es 16896 (scripts/cad/monolith-budget.json:29), y check-monolith-budget.mjs:196 da error si lines > allowance; ratchetSlack sólo sirve para exigir bajar la asignación (:201). El fichero ya estaba 5 líneas por encima antes de T4, y a82a2756 añade 8 más. El bloque de hitEdge (Layout3DEditor.tsx:6607-6614) repite literalmente mode/document/frame/sceneRay de hitFace (:6599-6606).
- **Arreglo:** Define una sola vez `const pickDeps = { mode: …, document: …, frame: …, sceneRay: … }` y pásalo a cadFacePickerFor y cadEdgePickerFor, o exporta desde scene-ray.ts una fábrica que devuelva ambos resolutores. No aumentes el tamaño de Layout3DEditor.tsx.

### t4-designar-aristas-13 · [baja] Comentario obsoleto: dice que CAD_ACCEPT_EDGE_PICK no existe

- **Fichero:** `apps/web/src/lib/cad/engine/commands/solids-edit-branches.ts:58`
- **Problema:** El comentario afirma 'CAD_ACCEPT_EDGE_PICK no existe —cero apariciones en el árbol—'. En d9c1cac5 existe (command-types.ts:118) y lo usan pointer-router y solids-modify, así que el comentario engaña a quien mantenga «Arista · Copiar».
- **Evidencia:** solids-edit-branches.ts:58-62 frente a git grep CAD_ACCEPT_EDGE_PICK d9c1cac5 (command-types.ts:118, pointer-router.ts:40/358, solids-modify.ts:58/189).
- **Verificación independiente:** Confirmado. solids-edit-branches.ts:58-62 dice '`CAD_ACCEPT_EDGE_PICK` no existe —cero apariciones en el árbol—'. En d9c1cac5 existe (command-types.ts:118, `= 256`) y lo usan pointer-router.ts:40/358 y solids-modify.ts:58/189. El comentario quedó obsoleto con estos commits.
- **Arreglo:** Actualiza el comentario de solids-edit-branches.ts:58-62: di que el bit ya existe y que «Arista · Copiar» todavía copia todas las aristas porque no usa la designación de arista, o conecta esa rama a EDGE_PICK.

## MEP y Plant: choques y empalmes (c90cc749, 101a42d1, 91a0e155, ef4ac4f5, d9c1cac5)

**Resumen del revisor:** Casi nada de lo anunciado funciona.

1. **El defecto de 52898e03 sigue en d9c1cac5.** El filtro `if (a.entityId > b.entityId) continue;` (clash.ts:720) se salta todo par en que la ruta filtrada tenga un id mayor que la otra. En el editor los ids son `cad_<Date.now en base36>_<aleatorio>` (Layout3DEditor.tsx:978-979), así que la ruta recién cerrada siempre tiene el id mayor. Resultado: PIDROUTE (plant-route.ts:297-300) y PIPE (mep-tracing.ts:185-188) nunca avisan de choques contra conducciones ya dibujadas, y esos pares tampoco se declaran en skipped.

2. **D4 no hace nada en la práctica.** El código hace lo que dice el mensaje: busca los puntos de empalme y descarta el par si el peor punto queda a menos de 2·(ra+rb) de uno. Pero en un empalme la distancia entre ejes es ≤ 1, y ese es el mínimo global del par. El peor punto cae siempre en el empalme y el par entero se descarta, igual que antes. Un solape real a 3 m de una te, o dos ejes que se cruzan, siguen sin reportarse. Lo que sí se mantiene: no aparecen choques falsos en tes ni codos (el spec 8 sigue en 0). La bitácora marca D4 «HECHA» sin serlo.

3. **D7 existe, pero no aporta nada y añade ruido.**
   - El pase contra obstáculos ya declaraba cada ruta sin diámetro.
   - Ahora se declara otra vez por cada par, y PIDMTO repite la misma ruta N veces en «sin medir».
   - PIDROUTE y PIPE nunca leen skipped.

4. **Sin specs.** Ningún commit añade specs. No hay geometría que falle sin D4 ni sin D7, ni un caso ruta-contra-ruta con routeIds.

5. **Fuera de MEP.** 91a0e155 marca D5 (colaboración) «HECHA», pero bc557a01 quitó el selector `cad-review-markup` que el golden e2e 22 sigue usando (líneas 143 y 168), así que ese golden se rompe. Además, el comentario dice que el valor por defecto es «note» y en el código es «cloud».

6. **Menor.** Queda código muerto (`seEmpalman`).

No se tocaron dependencias, flags DWG ni esquemas, y los textos nuevos están en español.

**Afirmaciones de tus commits que no se cumplen:**

- Foco: el defecto confirmado en 52898e03 (el filtro routeIds pierde los choques entre la ruta filtrada y rutas con id menor) está corregido en d9c1cac5
  - Evidencia: clash.ts:714-720 en d9c1cac5 sigue igual: bucle `mias × todas` con `if (a.entityId > b.entityId) continue;`. Con routeIds=['r2'], a=r2 y b=r1 da 'r2' > 'r1' y el par se salta. Ni c90cc749 ni 101a42d1 tocan esas líneas.
- c90cc749: el nuevo comportamiento es «coherente con el pase contra obstáculos»
  - Evidencia: El pase contra obstáculos declara cada ruta una vez (clash.ts:631). D7 la declara una vez más por cada par (líneas 727-736). Con 4 rutas y r1 sin diámetro, skipped trae r1 cuatro veces, y PIDMTO las imprime todas (plant-route.ts:474-477). Los pares que salta la línea 720 siguen sin declararse.
- c90cc749 (premisa): sin D7, una ruta sin diámetro se descartaba en silencio y no llegaba a skipped
  - Evidencia: Antes de D7, clash.ts:628-638 ya empujaba en sinDiametro toda ruta de `mias` sin radio, con su motivo («no dice su tamaño» o «no es una medida en pulgadas»). Lo único que se callaba eran las rutas fuera del filtro.
- 101a42d1: las conducciones que se tocan ya no quedan exentas en toda su longitud
  - Evidencia: En un empalme la distancia entre ejes es ≤ CAD_PL_JOIN_TOLERANCE = 1 (pipe-route.ts:64), así que gap ≈ −(ra+rb), el mínimo posible. `peor` (línea 754, con `<` estricto) queda en el empalme, nearJoin da true y el par entero se descarta. Ejemplo: cabezal 'a' 6" (0,0,0)→(10000,0,0) y ramal 'b' 4" (5000,0,0)→(5000,1000,0)→(8000,1000,0)→(8000,50,0). A x=8000 el solape es de 77 unidades, a 3000 del empalme, y el informe da 0 choques, igual que antes de D4.
- 91a0e155: bitácora «D5 (colab) HECHA — Selector de markup Cloud/Arrow/Note oculto»
  - Evidencia: bc557a01 borró el <select data-testid="cad-review-markup">, pero el golden apps/web/e2e/golden/22-cad-compare-collaboration.spec.ts:143 hace `getByTestId('cad-review-markup').selectOption('arrow')` y en la línea 168 espera `markup: { kind: 'arrow' }`, así que el golden falla. El comentario de CadCollaborationPalette.tsx:575 dice que el valor por defecto es «note», pero en la línea 108 es "cloud".
- d9c1cac5: bitácora «D4 (MEP/Plant) HECHA — Conducciones que se tocan no exentas en toda su longitud»
  - Evidencia: docs/execution/NOCHE_MIMO_20260914.md:395. Como se explica arriba, el peor punto de un par empalmado siempre cae en el empalme y el par se sigue descartando entero.

### mep-plant-1 · [alta] El filtro routeIds sigue perdiendo choques ruta-contra-ruta (defecto de 52898e03 sin corregir)

- **Fichero:** `apps/web/src/lib/cad/plant/clash.ts:720`
- **Problema:** El bucle exterior recorre `mias` (rutas filtradas) y el interior `todas`. `if (a.entityId > b.entityId) continue;` descarta todo par en que la ruta filtrada tenga un id mayor que la otra. PIDROUTE y PIPE llaman con `routeIds: [nueva.id]` y los ids se generan con `cad_${Date.now().toString(36)}_…`, así que la ruta nueva siempre tiene el id mayor y nunca se mide contra las conducciones existentes. Esos pares ni se miden ni se declaran en skipped, lo que contradice «Nunca se calla» (línea 534).
- **Evidencia:** clash.ts:714-720 en d9c1cac5. Generador de ids: Layout3DEditor.tsx:978-979. Llamadas con filtro: plant-route.ts:297-300 y mep-tracing.ts:185-188. El spec 11 (clash.spec.ts:385-403) sólo prueba el filtro contra un muro, nunca ruta contra ruta, y por eso no lo detecta. Caso: r1 6" (0,0,5000)→(10000,0,5000) y r2 4" (5000,-5000,5050)→(5000,5000,5050). Sin filtro sale 1 choque-duro (spec 7); con routeIds ['r2'] salen 0.
- **Verificación independiente:** Confirmado leyendo d9c1cac5. En clash.ts:714-720 el bucle exterior recorre `mias` y el interior `todas`, y `if (a.entityId > b.entityId) continue;` salta el par cuando la ruta filtrada tiene el id mayor. c90cc749 y 101a42d1 no tocan esa línea, y ningún camino declara ese par en `skipped` (clash.ts:794). Antes de 52898e03 el bucle era `for j = i+1` sobre `todas` con un filtro «alguna de las dos en routeIds», así que la regresión viene de ahí y sigue sin arreglar. PIDROUTE (plant-route.ts:220-221, 297-300) y el trazado MEP (mep-tracing.ts:185-188, 221) llaman con `routeIds: [nueva.id]`, y `nueva.id` sale de `context.newEntityId()`. El editor lo implementa como `newId("cad")` = `cad_${Date.now().toString(36)}_…` (Layout3DEditor.tsx:978-979, 4775), así que una ruta nueva queda por encima de las anteriores del mismo generador. Traza en seco del caso de la evidencia: mias=[r2] y todas=[r1,r2]. El par (r2,r1) cae en la línea 720 y (r2,r2) en la 718, así que salen 0 choques, cuando el spec 7 (clash.spec.ts:295-301) da 1 sin filtro. El spec 11 (clash.spec.ts:385-403) sólo mide contra un muro.
- **Arreglo:** En cadPipeClashReport, antes del bucle ruta-contra-ruta, crea `const enFiltro = new Set(mias.map((r) => r.entityId));`. Sustituye la línea 720 por `if (enFiltro.has(b.entityId) && b.entityId < a.entityId) continue;`: el orden por id sólo debe evitar duplicar pares con las dos rutas dentro del filtro. Añade a la sección 11 de clash.spec.ts el caso r1/r2 de la evidencia con `routeIds: ['r2']`: debe dar 1 choque con `kind` 'choque-duro', `againstKind` 'ruta', `routeId` 'r2' y `againstId` 'r1'. Verifica que falla con el código actual.

### mep-plant-2 · [alta] D4 no hace nada: el peor punto de un par empalmado siempre es el empalme y el par se sigue descartando entero

- **Fichero:** `apps/web/src/lib/cad/plant/clash.ts:758`
- **Problema:** La exención depende sólo del `peor` global del par. En un empalme la distancia entre ejes es ≤ CAD_PL_JOIN_TOLERANCE (1), así que gap ≈ −(ra+rb), el valor más negativo posible. Cualquier otro punto tiene distancia ≥ 0 y nunca lo supera, y ante un empate el primero encontrado no se reemplaza (`gap < peor.gap`, línea 754). `peor` queda en el empalme, nearJoin da true y el par entero se descarta, exactamente como con el antiguo `seEmpalman`. Un solape real lejos de la te, o dos ejes que se cruzan, siguen sin reportarse. La bitácora (docs/execution/NOCHE_MIMO_20260914.md:395) lo marca HECHA.
- **Evidencia:** clash.ts:743-763. Caso 1: cabezal 'a' 6" (0,0,0)→(10000,0,0) y ramal 'b' 4" (5000,0,0)→(5000,1000,0)→(8000,1000,0)→(8000,50,0). joinPoints = [(5000,0,0)]. El segmento n=1 da distancia 0, gap −127, at (5000,0,0). El segmento n=3 da distancia 50 y gap −77 (choque duro a 3000 del empalme, más que 2·127=254), pero −77 no es menor que −127, así que `peor` sigue en el empalme y el resultado es 0 choques. Caso 2: si el ramal termina en (8000,−1000,0), su último tramo cruza el eje del cabezal (distancia 0), empata con el empalme, se conserva el primero y tampoco se reporta.
- **Verificación independiente:** Confirmado con matiz. En clash.ts:744-755 `peor` es el mínimo global con `gap < peor.gap` estricto, y la exención (758-763) sólo mira `peor.at`. Caso 1 en seco: joinPoints=[(5000,0,0)], porque sólo la punta inicial del ramal toca el cabezal (empalmePoints, 581-586) y (8000,50,0) queda a 50 > CAD_PL_JOIN_TOLERANCE=1 (pipe-route.ts:64). Con m=1, n=1, distanciaSegmentoSegmento (clash.ts:246-289) da s=0.5, distancia 0 y at=(5000,0,0), así que gap=-127 (radios 76.2 y 50.8, spec:100-101). Con n=3 la distancia es 50 y el gap -77, que no es menor que -127, y `peor` sigue en el empalme. Luego hypot 0 < 254 y hay `continue`: 0 choques. En el caso 2 (distancia 0 en n=3) empata y tampoco reemplaza. Matiz: «no hace nada» exagera un poco. D4 sí reporta cuando el cruce lejano aparece antes que el tramo del empalme en el orden m/n, o cuando el empalme está a una distancia de (0,1] y el cruce a menos, pero eso depende del orden del bucle y no de la geometría. La bitácora marca D4 HECHA (NOCHE_MIMO_20260914.md:395).
- **Arreglo:** En clash.ts:743-763 quita la comprobación sobre el `peor` global. Con joinPoints no vacío y para cada par de segmentos (m,n), recorta en AMBOS segmentos los intervalos paramétricos a menos de R = 2*(ra+rb) de cada punto de empalme (resuelve |P0 + t·d − J|² = R² para t y quita [t1,t2]∩[0,1]). Calcula distanciaSegmentoSegmento sólo entre los subtramos que quedan y toma el peor de esos. Sin joinPoints, deja el cálculo como está. Añade a clash.spec.ts los dos casos de la evidencia: el caso 1 debe dar 1 choque-duro contra ruta con depth ≈ 77 (76.2+50.8−50, tolerancia 0.011) y at.x ≈ 8000; el caso 2 debe dar 1 choque-duro. El spec 8 (te) debe seguir en 0. Cambia la fila D4 de la bitácora (línea 395) a PENDIENTE hasta que esos specs pasen.

### mep-plant-3 · [alta] El golden e2e 22 sigue usando el selector cad-review-markup que D5 (colaboración) borró

- **Fichero:** `apps/web/e2e/golden/22-cad-compare-collaboration.spec.ts:143`
- **Problema:** 91a0e155 marca D5 (colab) como HECHA, pero bc557a01 borró el `<select data-testid="cad-review-markup">` sin tocar el golden que lo usa. `getByTestId('cad-review-markup').selectOption('arrow')` se quedará esperando hasta el timeout, y la línea 168 espera `markup: { kind: 'arrow' }` en el hilo guardado, cuando ahora siempre será 'cloud'. El golden corre en CI (ci.yml:893, «Playwright E2E (goldens + …)»).
- **Evidencia:** git grep de 'cad-review-markup' en d9c1cac5 sólo encuentra 22-cad-compare-collaboration.spec.ts:143. CadCollaborationPalette.tsx:575 tiene ahora el comentario en lugar del select, y en la línea 108 el estado inicial es `useState("cloud")`.
- **Verificación independiente:** Confirmado. bc557a01 borra el `<select data-testid="cad-review-markup">` de CadCollaborationPalette.tsx (hoy sólo queda el comentario en 575-577) y no toca el golden. `git grep cad-review-markup d9c1cac5` sólo encuentra apps/web/e2e/golden/22-cad-compare-collaboration.spec.ts:143 (`selectOption('arrow')`), y la línea 168 espera `markup: { kind: 'arrow' }`. El estado sigue fijo en "cloud" (Palette:108, 196). El golden es un `test(` sin skip (línea 81) y playwright.config.ts tiene testDir "./e2e" y sólo ignora auditoria/**. El paso de CI «Playwright E2E (goldens + …)» ejecuta `npx playwright test` sin rutas, con shard (ci.yml ~945), así que lo incluye. 91a0e155 marca D5 (colab) como HECHA.
- **Arreglo:** En el mismo commit que oculta el selector, actualiza apps/web/e2e/golden/22-cad-compare-collaboration.spec.ts: borra la línea 143 (selectOption) y cambia la expectativa de la línea 168 a `markup: { kind: 'cloud' }`, o a lo que decidas persistir. Haz que corra el golden 22 en CI antes de marcar D5 como HECHA en la bitácora.

### mep-plant-4 · [media] D7 declara la misma ruta sin diámetro una vez por par: PIDMTO repite N veces cada ruta en «sin medir»

- **Fichero:** `apps/web/src/lib/cad/plant/clash.ts:727`
- **Problema:** El pase contra obstáculos ya declara cada ruta de `mias` sin diámetro (líneas 628-638). D7 vuelve a declararla por cada par en que aparece (líneas 726-737), así que `skipped` trae N entradas para la misma ruta. PIDMTO lista `skipped` entero y el aviso se llena de duplicados. En PIDROUTE y PIPE `skipped` no se lee, así que ahí D7 no llega al usuario. Y los pares que la línea 720 salta ni siquiera se declaran.
- **Evidencia:** Documento con r1 sin diámetro (size «6» sin comillas) y r2, r3, r4 de 6", sin filtro. La línea 631 añade r1, y los pares (r1,r2), (r1,r3) y (r1,r4) añaden r1 otra vez en la línea 732: 4 entradas. plant-route.ts:474-477 imprime `sin medir: r1 (…); r1 (…); r1 (…); r1 (…)`. plant-route.ts:297-314 y mep-tracing.ts:185-197 no usan `informe.skipped`.
- **Verificación independiente:** Confirmado. El pase contra obstáculos añade cada ruta de `mias` sin radio a `sinDiametro` (clash.ts:628-638), y lo hace antes y aparte del bucle de obstáculos. D7 la vuelve a añadir en cada par (726-737) sin deduplicar. Con r1 de tamaño "6" (cadPipeNominalMillimetres devuelve null, pipe-route.ts:139-158, y lo afirma el spec 1:108) y r2, r3, r4 de 6" sin filtro, r1 entra una vez en 631 y otra en cada par (r1,r2), (r1,r3) y (r1,r4) por la línea 732. Los pares inversos caen en la 720, así que quedan 4 entradas. PIDMTO las imprime todas en plant-route.ts:474-476 (`sin medir: …join("; ")`). PIDROUTE (plant-route.ts:297-314) y choquesDe de mep-tracing.ts (185-197) no leen `skipped`, y con filtro los pares que salta la línea 720 tampoco se declaran.
- **Arreglo:** Después del pase contra obstáculos, crea `const declarados = new Set(sinDiametro.map((s) => s.entityId));`. En el pase ruta-contra-ruta, cuando ra o rb sea null, haz push sólo si el entityId no está en `declarados`, añádelo al set y haz `continue`. Añade a clash.spec.ts: con r1 sin diámetro y r2, r3, r4 con 6" y sin filtro, `skipped.filter(s => s.entityId === 'r1').length` debe ser 1. Con `routeIds: ['r2']` y r1 fuera del filtro, r1 debe aparecer exactamente una vez.

### mep-plant-5 · [media] D4 y D7 se marcaron HECHA sin ningún spec que ejecute el análisis con geometría que falle sin el arreglo

- **Fichero:** `apps/web/src/lib/cad/plant/clash.spec.ts:311`
- **Problema:** c90cc749 y 101a42d1 sólo modifican clash.ts. clash.spec.ts en d9c1cac5 no tiene ningún caso de skipped en el pase ruta-contra-ruta, ni de un par empalmado con solape lejos del empalme, ni de ruta contra ruta con routeIds. El spec 8 (te, líneas 311-326) da el mismo resultado con y sin D4, así que no prueba el cambio. Con estos casos habrían saltado mep-plant-1, mep-plant-2 y mep-plant-4.
- **Evidencia:** `git show --stat c90cc749 101a42d1` sólo lista apps/web/src/lib/cad/plant/clash.ts. En clash.spec.ts no aparece `skipped` y el único caso de empalme es la sección 8.
- **Verificación independiente:** Confirmado. `git show --stat` de c90cc749 y 101a42d1 sólo lista apps/web/src/lib/cad/plant/clash.ts. En clash.spec.ts de d9c1cac5 no aparece `skipped` en ningún sitio. El único empalme es la sección 8 (311-326), una te simple donde el peor punto es el empalme y el resultado sale 0 con y sin D4. El único caso con routeIds es el 11 (385-403), contra un muro. Otros specs no cubren esto: solids-create.spec.ts no usa cadPipeClashReport, skipped ni routeIds, y los axe de e2e/a11y no prueban geometría. Ningún spec habría detectado mep-plant-1, 2 ni 4.
- **Arreglo:** Añade a clash.spec.ts los casos descritos en los arreglos de mep-plant-1, mep-plant-2 y mep-plant-4. Llaman a cadPipeClashReport con entidades polyline reales (helper `ruta`) y comprueban valores exactos (número de choques, kind, againstId, depth, número de entradas en skipped). Confirma que cada caso falla sobre d9c1cac5 antes de aplicar el arreglo.

### mep-plant-6 · [baja] seEmpalman quedó como código muerto

- **Fichero:** `apps/web/src/lib/cad/plant/clash.ts:558`
- **Problema:** 101a42d1 dice que reemplaza `seEmpalman` por `empalmePoints`, pero deja la función definida (líneas 550-560, con su comentario) y ya no se llama desde ningún sitio.
- **Evidencia:** git grep -n seEmpalman d9c1cac5 sólo encuentra clash.ts:558 (definición) y el comentario de la línea 740.
- **Verificación independiente:** Confirmado. `git grep -n seEmpalman d9c1cac5` sólo encuentra la definición en clash.ts:558 y el comentario de la línea 740. 101a42d1 cambió su cuerpo a `return empalmePoints(a, b).length > 0;` y quitó la única llamada (antes en 739). No es exportada, así que es código muerto. Severidad baja bien puesta: tsconfig no activa noUnusedLocals y ESLint sólo avisa (`@typescript-eslint/no-unused-vars: "warn"`, eslint.config.mjs:42-45), así que no rompe CI.
- **Arreglo:** Borra la función `seEmpalman` (clash.ts:558-560) y lleva su comentario explicativo (líneas 550-557) encima de `empalmePoints`.

### mep-plant-7 · [baja] El comentario de D5 (colab) da un valor por defecto falso y setMarkup quedó sin uso

- **Fichero:** `apps/web/src/components/cad/palettes/CadCollaborationPalette.tsx:575`
- **Problema:** El comentario dice «El valor por defecto "note" se mantiene como metadata», pero el estado inicial es "cloud" y cada hilo nuevo se guarda con `markup: { kind: "cloud" }`. Además, `setMarkup` ya no se usa en ningún sitio.
- **Evidencia:** CadCollaborationPalette.tsx:108 `useState<"note" | "arrow" | "cloud">("cloud")`; línea 196 `markup: { kind: markup, … }`; git grep de setMarkup sólo encuentra la línea 108.
- **Verificación independiente:** Confirmado. CadCollaborationPalette.tsx:575-577 dice «El valor por defecto "note" se mantiene como metadata», pero la línea 108 es `useState<"note" | "arrow" | "cloud">("cloud")` y addThread guarda `markup: { kind: markup, … }` (línea 196), así que cada hilo se guarda como "cloud". `git grep setMarkup d9c1cac5` sólo encuentra la desestructuración de la línea 108: su único uso era el onChange del select que borró bc557a01.
- **Arreglo:** Corrige el comentario de la línea 575 para que diga el valor real ("cloud") o cambia el estado inicial a "note" si eso es lo que quieres persistir. Sustituye el useState de la línea 108 por una constante (`const markup = "cloud" as const;`) para que no quede un setter sin uso, y ajusta el golden 22 a ese mismo valor (ver mep-plant-3).

## Bloque robustez del motor, colaboración, raster e importación (df21d268, 7bbdb190, bc557a01, 914a5144 y bitácora 5307b50b, 2d5af3b7, eddf703c), revisado en d9c1cac5

**Resumen del revisor:** Qué funciona de verdad:
- D-6 cumple lo que dice. `MAX_DWG_IMPORT_BYTES` es el mismo valor y ahora llega desde document-import-validation. El ciclo de importación A ya no existe en tiempo de ejecución: sólo queda un `import type`, que TypeScript borra al compilar. El commit no toca el laboratorio DWG, y DWG_IMPORT_FLAG y DWG_EXPORT_FLAG siguen en false.
- D-4 hace lo que anuncia en el motor. Si begin() lanza, el error llega al usuario como mensaje de nivel error y el estado del motor queda limpio. Ningún spec existente esperaba esa excepción.
- D5 quita el selector, y los hilos ya guardados siguen mostrando su tipo de markup en la lista.
- D-1 pone en sólo lectura los seis campos que el adaptador de IMAGE no escribe, y no hay otra vía para editarlos.

Qué no funciona:
- **D-1 se pasó de largo.** `rotation` quedó en sólo lectura para TODOS los tipos, no sólo para IMAGE. Ya no se puede girar un INSERT, TEXT, MTEXT, ATTDEF ni TABLE desde la paleta, aunque sus adaptadores sí escriben la rotación. Eso rompe el golden 18.
- **D5 rompe CI por dos lados.**
  - El golden 22 sigue usando el `data-testid` `cad-review-markup` que se eliminó y espera un markup `arrow` guardado.
  - `setMarkup` quedó sin uso y deja un aviso de lint nuevo en un archivo cuyo presupuesto de avisos es 0. Eso tumba `check:lint-budget` y, con él, `check:cad`.
  - El comentario del código dice que el valor por defecto es "note", pero es "cloud". Cada comentario nuevo se sigue guardando y mostrando como «cloud».
- **D-4 no trae spec.** La auditoría la pedía. Además, un guión SCRIPT en el estudio antes se detenía si un comando lanzaba en begin; ahora sigue empujando renglones.
- **D-1 tampoco trae spec.** La auditoría pedía una con un sujeto IMAGE.
- **Bitácora:** D-6 figura como HECHA con el ciclo B todavía presente. D-1 raster y D5 colaboración figuran como HECHA con goldens rotos.

Sobre el español: estos commits no añaden textos visibles. La paleta ya mostraba antes los nombres de clave en inglés (`pixelWidth`, `drawnWidth`), igual que en todas las propiedades.

**Afirmaciones de tus commits que no se cumplen:**

- Bitácora: «D-6 (calidad) HECHA»
  - Evidencia: La ficha D-6 (docs/execution/auditoria-fable/dimensiones/19-calidad-codigo.md, «Dos ciclos de importación en el puente DWG») incluye el ciclo B, que sigue vivo: dwg-document-bridge.ts:59 importa mapLayers de dwg-document-bridge-layers.ts, y ese archivo, en su línea 20, importa DWG_BRIDGE_LOSS_CODES de vuelta desde ./dwg-document-bridge.
- Bitácora: «D-4 (comp3) HECHA»
  - Evidencia: La auditoría (21-completitud-3.md §4, «Cuatro cosas, cuatro pruebas») pedía una prueba, y el commit no añade ninguna: command-engine.spec.ts no tiene ningún descriptor cuyo begin lance. Además, el guión SCRIPT del estudio pierde el fallo cerrado (ver hallazgo robustez-d4-script).
- D5 (comentario en el código): «El valor por defecto "note" se mantiene como metadata»
  - Evidencia: CadCollaborationPalette.tsx:108 inicia el estado con useState("cloud"), y la línea 196 guarda markup: { kind: "cloud", color: "#f43f5e" } en cada comentario nuevo.
- Bitácora: «D5 (colab) HECHA» sin romper goldens ni gates
  - Evidencia: El golden 22 usa el testid eliminado (22-cad-compare-collaboration.spec.ts:143) y espera markup kind 'arrow' (:168). Además, setMarkup sin uso dispara el trinquete de lint (scripts/check-lint-budget.mjs, que corre dentro de check:cad en ci.yml:238).
- D-1: el cambio sólo afecta a IMAGE
  - Evidencia: READONLY_KEYS es global por nombre (property-model.ts:257). Por eso `rotation` queda en sólo lectura para insert (block-text-adapters.ts:472 sí la escribe), mtext (:210), text (text-entity-adapter.ts:192), attdef y table (annotation-v4-adapters.ts:186 y :361).
- Bitácora: «D-1 (raster) HECHA»
  - Evidencia: La ficha (09-toolset-map-raster.md, D-1) pedía una spec con un sujeto image y advertía que «rotation SÍ es escribible en insert y no lo es en image, y una lista por nombre no sabe distinguirlas». No hay spec, y el cambio rompe la edición de rotación en otros tipos, además del golden 18.

### robustez-d1-rotation-global · [alta] «rotation» en READONLY_KEYS global rompe la rotación editable de INSERT/TEXT/MTEXT/ATTDEF/TABLE y el golden 18

- **Fichero:** `apps/web/src/components/cad/palettes/property-model.ts:226`
- **Problema:** READONLY_KEYS se aplica por nombre de clave a cualquier tipo de entidad (editorKindOf, línea 257). Al añadir "rotation" para IMAGE, la rotación pasa a sólo lectura en todas las entidades que la exponen y SÍ la escriben. Girar un bloque, un texto o una tabla desde la paleta de propiedades deja de ser posible.
- **Evidencia:** block-text-adapters.ts:472 (insert) escribe `rotation: finite(patch.rotation, entity.rotation)`, y lo mismo hacen block-text-adapters.ts:210 (mtext), text-entity-adapter.ts:192 y annotation-v4-adapters.ts:186/361. El golden apps/web/e2e/golden/18-cad-professional-blocks.spec.ts:136 llama a applyNativeProperty(page, 'rotation', '45'), que hace field.fill(value) (e2e/fixtures/dynamic-input.ts:320) sobre lo que ahora es un <input readOnly>: Playwright rechaza fill en elementos no editables, el reintento agota el tiempo y tampoco se cumple la expectativa posterior `rotation: 45`. La propia ficha 09-toolset-map-raster.md D-1 advertía que una lista por nombre no distingue insert de image.
- **Verificación independiente:** Confirmado. En d9c1cac5, property-model.ts:214-227 añade "rotation" a READONLY_KEYS, y editorKindOf (property-model.ts:257) decide sólo por el nombre de la clave, sin mirar el tipo. Por eso la rotación también sale readonly en INSERT, que la escribe (block-text-adapters.ts:463 la lee y :472 la escribe). Lo mismo pasa con MTEXT (:210), TEXT (text-entity-adapter.ts:192) y annotation-v4-adapters.ts:186/361. CadEntityPropertiesPanel.tsx:83 construye el modelo, y CadPropertiesPalette.tsx:110-121 pinta la fila readonly como <input readOnly> con el mismo data-testid. El golden 18 lee primero toHaveValue('30'), que pasa, pero en la línea 133 (no la 136) llama a applyNativeProperty(page,'rotation','45'). Ese helper (dynamic-input.ts:312-339) hace field.fill() dentro de toPass. Playwright no rellena un input readonly, así que se agota SETTLE_MS; y aunque siguiera, la aserción rotation:45 no se cumpliría. La propia ficha 09-toolset-map-raster.md (D-1) avisa de que rotation sí es escribible en insert. El golden no está en skip.
- **Arreglo:** Quita "rotation" (y, por coherencia, las otras cinco claves de IMAGE) de READONLY_KEYS global. Crea en property-model.ts un `READONLY_KEYS_BY_TYPE: Record<string, ReadonlySet<string>> = { image: new Set(["definition","pixelWidth","pixelHeight","drawnWidth","drawnHeight","rotation"]) }`. Haz que editorKindOf reciba los sujetos y devuelva "readonly" si la clave está en READONLY_KEYS o si ALGÚN sujeto es de un tipo cuya lista la contiene. En property-model.spec.ts añade un bloque con un sujeto `type: "image"` que afirme kind "readonly" para las seis claves y kind "editable" en brightness, y otro con un sujeto `insert` (y otro `text`) que afirme `row(model, "rotation").kind === "number"`. No toques el golden 18: debe volver a pasar tal cual.

### robustez-d5-golden22 · [alta] Quitar el selector rompe el golden 22, que usa cad-review-markup y espera markup 'arrow'

- **Fichero:** `apps/web/e2e/golden/22-cad-compare-collaboration.spec.ts:143`
- **Problema:** El commit elimina el elemento con data-testid="cad-review-markup" sin tocar el golden que lo usa. El paso selectOption no encuentra el elemento y el test agota el tiempo. Aunque se quitara esa línea, la aserción sobre el documento guardado espera kind 'arrow', y ahora siempre se guarda 'cloud'.
- **Evidencia:** En la línea 143: `await page.getByTestId('cad-review-markup').selectOption('arrow');`. En la línea 168: `expect(stored.collaboration?.threads[0]).toMatchObject({ ..., markup: { kind: 'arrow' } });`. playwright.config.ts usa testDir ./e2e y ci.yml:945 corre todos los goldens por fragmentos; el golden 22 no está marcado como skip.
- **Verificación independiente:** Confirmado. El diff de bc557a01 borra el <select data-testid="cad-review-markup"> y no toca el golden. En d9c1cac5, 22-cad-compare-collaboration.spec.ts:143 sigue haciendo selectOption('arrow') sobre ese testid, y la línea 168 espera markup:{kind:'arrow'}. Sin selector, addThread guarda kind:"cloud", el valor inicial de CadCollaborationPalette.tsx:108 y :196. El spec no tiene skip ni fixme. playwright.config.ts:27 usa testDir ./e2e y sólo ignora auditoria/**, y el trabajo e2e-shard de ci.yml:945 corre todo por fragmentos. Ningún commit posterior hasta d9c1cac5 toca el golden.
- **Arreglo:** En el mismo commit que retira el selector, borra la línea 143 del golden 22 y cambia la aserción de la línea 168 al nuevo contrato: si se aplica el arreglo de robustez-d5-default-cloud, `expect(stored.collaboration?.threads[0]).toMatchObject({ status: 'resolved', assignedTo: 'qa@example.com' })` más `expect(stored.collaboration?.threads[0].markup).toBeUndefined()`. Añade también `await expect(page.getByTestId('cad-review-markup')).toHaveCount(0)` para que el golden pruebe que el selector está oculto.

### robustez-d5-lint-budget · [alta] setMarkup sin uso añade un aviso no-unused-vars en un archivo con presupuesto 0 y tumba check:lint-budget / check:cad

- **Fichero:** `apps/web/src/components/cad/palettes/CadCollaborationPalette.tsx:108`
- **Problema:** Tras quitar el <select>, `const [markup, setMarkup] = useState(...)` deja setMarkup sin uso. Eso genera un aviso @typescript-eslint/no-unused-vars. El trinquete de lint es por archivo y este archivo no tiene presupuesto para esa regla (máximo 0), así que el gate falla.
- **Evidencia:** apps/web/eslint.config.mjs:42-45 pone la regla en warn con sólo varsIgnorePattern "^_" y sin destructuredArrayIgnorePattern. scripts/lint-budget.json no menciona CadCollaborationPalette (git grep sale con código 1). scripts/check-lint-budget.mjs toma `budgetByFile[file] ?? 0` y falla si hay más avisos que el máximo. check:lint-budget forma parte de check:cad (package.json:60), que corre en ci.yml:238.
- **Verificación independiente:** Confirmado. En d9c1cac5, CadCollaborationPalette.tsx:108 conserva `const [markup, setMarkup] = useState(...)`. `markup` se lee en :196, pero el grep no encuentra ningún otro uso de setMarkup. apps/web/eslint.config.mjs:42-45 deja @typescript-eslint/no-unused-vars en warn con varsIgnorePattern "^_" y sin destructuredArrayIgnorePattern, así que un elemento de array desestructurado sin uso se avisa. `git grep CadCollaborationPalette d9c1cac5 -- scripts/lint-budget.json` sale con código 1. check-lint-budget.mjs toma `budgetByFile[file] ?? 0` y falla si hay más avisos que ese máximo. check:lint-budget va dentro de check:cad (package.json:60), que corre en ci.yml:238.
- **Arreglo:** Elimina el useState de markup en CadCollaborationPalette.tsx:108 y deja de leer `markup` en addThread (ver robustez-d5-default-cloud). No subas el presupuesto de lint-budget.json ni renombres la variable a _setMarkup: el estado no debe existir si no hay selector.

### robustez-d5-default-cloud · [media] Sin selector, cada comentario nuevo se guarda y se muestra como markup «cloud»; el comentario dice «note»

- **Fichero:** `apps/web/src/components/cad/palettes/CadCollaborationPalette.tsx:196`
- **Problema:** La regla fix-or-hide pedía dejar de prometer un markup que no se dibuja. Pero addThread sigue persistiendo `markup: { kind: "cloud", color: "#f43f5e" }` en todos los hilos nuevos, y la lista los muestra como «Drawing · cloud»: la promesa sigue en la interfaz y en el documento, ahora sin que el usuario la elija. El comentario de las líneas 575-577 afirma que el valor por defecto es "note", lo cual es falso.
- **Evidencia:** La línea 108 hace `useState<"note" | "arrow" | "cloud">("cloud")`, la 196 guarda `markup: { kind: markup, color: "#f43f5e" }`, la 577 dice «El valor por defecto "note" se mantiene como metadata» y la 599 pinta `thread.markup?.kind ?? "comment"`. CadReviewThread.markup es opcional (cad-document-collaboration.ts:31), así que omitirlo no cambia el esquema.
- **Verificación independiente:** Confirmado. CadCollaborationPalette.tsx:108 arranca el estado en "cloud", y :196 persiste `markup: { kind: markup, color: "#f43f5e" }` en cada hilo nuevo. Sin selector, eso siempre es cloud. La lista (:599) pinta `thread.markup?.kind ?? "comment"`, así que cada comentario nuevo aparece como «Drawing · cloud». El comentario de :575-577 dice que el valor por defecto es "note", y es falso. La ficha 11-colaboracion.md D5 describe justo eso como el defecto: la única consecuencia visible es la palabra «cloud» en la lista. Ocultar el select no la quita; ahora se impone sin que el usuario la elija.
- **Arreglo:** En addThread (CadCollaborationPalette.tsx:186-197), elimina la propiedad `markup` del hilo nuevo, de modo que se guarde sin markup y la lista lo muestre como «comment». Conserva el render de la línea 599 para que los hilos antiguos con markup sigan viéndose. Corrige el comentario de las líneas 575-577 para que diga que los hilos nuevos no llevan markup hasta que REVCLOUD genere geometría. Añade en cad-collaboration.spec.ts (o en un spec de la paleta) una aserción de que un hilo creado sin markup se guarda sin `markup` y que un hilo antiguo con `markup.kind: "cloud"` se conserva al añadir otro.

### robustez-d4-script · [media] SCRIPT del estudio deja de pararse cuando un comando lanza en begin: el error ya no llega a runCadScript

- **Fichero:** `apps/web/src/lib/cad/engine/command-engine.ts:211`
- **Problema:** runCadScript (el ejecutor contra el anfitrión vivo) sólo detecta un renglón fallido si sink.submit lanza. Antes de D-4, un comando que lanzaba en begin propagaba la excepción por engine.submit, y el guión se paraba con un fallo "threw" y su renglón. Ahora el catch la convierte en un mensaje que va al diálogo: runCadScript cuenta el renglón como ejecutado y sigue empujando renglones sobre un motor sin comando activo. Es justo lo que el propio ejecutor declara inaceptable: «un dibujo que nadie escribió».
- **Evidencia:** En apps/web/src/lib/cad/script-runner.ts:258-280, sólo el `catch (cause)` alrededor de sink.submit añade un fallo "threw" y para el guión; la cabecera (líneas 220-228) reconoce que no ve los mensajes de error internos del motor. El estudio llama a `runCadScript(text, engine)` con el anfitrión real (components/cad/command-line/use-command-engine.ts:513 y :628). En command-engine.ts:211-216 el catch devuelve sólo un efecto message.
- **Verificación independiente:** Confirmado. CadCommandEngineHost.submit (command-engine-host.ts:315-317) llama a dispatch, y dispatch (:642-700) invoca cadCommandEngineReduce sin try/catch. Antes de 7bbdb190, un begin que lanzaba subía por submit hasta el catch de runCadScript (script-runner.ts, en el try/catch alrededor de sink.submit), que registra "threw" y para el guión. Ahora command-engine.ts:208-216 lo convierte en un efecto message de nivel error y submit vuelve con normalidad. runCadScript suma executed y sigue con el renglón siguiente sobre un motor sin comando activo. Ambos llamadores (use-command-engine.ts:513 y :628) llaman antes a warmCommands, así que dispatch corre síncrono y la excepción de antes sí llegaba. Matiz: la cabecera de runCadScript ya reconocía este hueco para los rechazos internos (comando desconocido); D-4 lo amplía a los begin que lanzan. Además, el tipo CadScriptFailureCode ya incluye "rejected", así que no hace falta añadirlo.
- **Arreglo:** En CadCommandEngineHost, registra si durante el submit/dispatch en curso se aplicó un efecto message de nivel "error" y exponlo (por ejemplo, un getter `lastSubmitFailed: string | null` que se reinicia al empezar cada submit). En runCadScript, tras cada sink.submit, si `sink.lastSubmitFailed` existe, añade un CadScriptError con código nuevo "rejected" (declara el tipo como unión aditiva en la línea 128) y detén el guión salvo con continueOnError. Añade un caso en script-runner.spec.ts con un anfitrión real y un descriptor cuyo begin lance, que afirme stoppedAtLine igual a ese renglón y que los renglones siguientes no se ejecutan.

### robustez-d4-sin-spec · [media] D-4 marcado HECHA sin ninguna prueba; quitar el try/catch no haría fallar nada

- **Fichero:** `apps/web/src/lib/cad/engine/command-engine.spec.ts:45`
- **Problema:** El commit sólo toca command-engine.ts. Ningún spec registra un descriptor cuyo begin lance, así que no está demostrado que el motor no lance, que el error llegue como mensaje de nivel error ni que el estado quede limpio, en particular en el caso transparente con un comando suspendido. La ficha de auditoría pedía la prueba.
- **Evidencia:** Los descriptores de prueba de command-engine.spec.ts (línea 45 y siguientes) tienen begin que devuelve un paso; git grep "throw new Error" en ese spec no encuentra nada. docs/execution/auditoria-fable/dimensiones/21-completitud-3.md §4 dice: «`try/catch` en `begin()` (D4) ... Cuatro cosas, cuatro pruebas». La bitácora NOCHE_MIMO_20260914.md:391 marca «D-4 (comp3) HECHA».
- **Verificación independiente:** Confirmado. `git show --stat 7bbdb190` sólo toca command-engine.ts. Entre 7bbdb190 y d9c1cac5, ningún commit toca apps/web/src/lib/cad/engine/*.spec (el único cambio en engine es 2473693a, en solids-modify.ts). En command-engine.spec.ts, `throw` sólo aparece en el assert.throws de registro duplicado (:589). En los specs de command-line, los throw son de oyentes y puertos (plot-host, sheet-set-host, xref-host), ninguno de un descriptor cuyo begin lance. La ficha 21-completitud-3.md §4 pide «Cuatro cosas, cuatro pruebas», y la bitácora la marca HECHA. Revertir el try/catch no haría fallar ningún spec.
- **Arreglo:** En command-engine.spec.ts, añade un descriptor BOOM con `begin: () => { throw new Error("sin vista"); }` (y transparent: true) a un registro de prueba. Caso 1, sin comando activo: `cadCommandEngineReduce(EMPTY_CAD_COMMAND_ENGINE, {kind:"invoke", command:"BOOM"}, ctx, reg)` no lanza, devuelve state.active === null y un efecto message de nivel "error" cuyo texto contiene "BOOM: sin vista". Caso 2, con LINE activo y token "'BOOM": state.active.name sigue siendo "LINE" y state.suspended.length sigue en 0. Comprueba que el spec falla si revierto command-engine.ts:208-217.

### robustez-d1-sin-spec · [media] D-1 sin spec con sujeto IMAGE; el spec existente no mira el tipo de editor de rotation

- **Fichero:** `apps/web/src/components/cad/palettes/property-model.spec.ts:208`
- **Problema:** La ficha pedía una spec con un sujeto image, y el commit no la añade. El único caso con rotation (un insert) sólo comprueba la categoría, no el tipo de editor. Por eso ni el objetivo (IMAGE en sólo lectura) ni la regresión (rotation en sólo lectura para insert) quedan cubiertos por specs unitarios.
- **Evidencia:** property-model.spec.ts:208 hace `assert.equal(row(model, "rotation").category, "Geometría")` y no mira `.kind`. En la ficha 09-toolset-map-raster.md, D-1: «Y una spec en `property-model.spec.ts` con un sujeto `image`». La bitácora NOCHE_MIMO_20260914.md:394 marca «D-1 (raster) HECHA».
- **Verificación independiente:** Confirmado. 914a5144 sólo toca property-model.ts, y ningún commit posterior hasta d9c1cac5 toca property-model.spec.ts. En ese spec no hay ningún sujeto image (el grep de «image» no devuelve nada). El único caso con rotation está en la línea 210, no la 208, y es `assert.equal(row(model, "rotation").category, "Geometría")`, que no mira .kind. Por eso ni la regresión del INSERT ni el objetivo de IMAGE están cubiertos en specs unitarios. La ficha 09-toolset-map-raster.md D-1 pide expresamente «una spec en property-model.spec.ts con un sujeto image».
- **Arreglo:** En property-model.spec.ts, en el bloque del INSERT (línea 208), añade `assert.equal(row(model, "rotation").kind, "number", "la rotación de un INSERT se escribe")`. Añade un bloque nuevo con un sujeto `{ id: "img", type: "image", properties: { definition: "def", pixelWidth: 1024, pixelHeight: 768, drawnWidth: 5000, drawnHeight: 3750, rotation: 0, brightness: 50, layer: "0" } }` que afirme kind "readonly" en las seis claves derivadas y kind "number" en brightness. Para dos sujetos mixtos (image + insert), afirma que rotation sale "readonly".

### robustez-d6-hecha-parcial · [media] Bitácora marca D-6 HECHA pero el ciclo B de la misma ficha sigue en el puente DWG

- **Fichero:** `docs/execution/NOCHE_MIMO_20260914.md:390`
- **Problema:** La ficha D-6 de la auditoría son «Dos ciclos de importación en el puente DWG». El commit sólo resuelve el A, pero la tabla de estado declara D-6 completa. El ciclo B es una zona muerta latente (acceso a DWG_BRIDGE_LOSS_CODES antes de inicializarse) si el bundler aplana a CJS, y queda oculto como hecho.
- **Evidencia:** En apps/web/src/lib/cad/dwg-document-bridge.ts:59 está `import { mapLayers } from "./dwg-document-bridge-layers";` y en apps/web/src/lib/cad/dwg-document-bridge-layers.ts:20, `import { DWG_BRIDGE_LOSS_CODES } from "./dwg-document-bridge";`. La ficha está en docs/execution/auditoria-fable/dimensiones/19-calidad-codigo.md, sección «D-6 · Dos ciclos...», con el arreglo del ciclo B.
- **Verificación independiente:** Confirmado con un matiz. La ficha 19-calidad-codigo.md D-6 abarca dos ciclos, y df21d268 sólo resuelve el A (dwg-document-bridge.ts:54-55 ya importa de document-import-validation). El ciclo B sigue en d9c1cac5: dwg-document-bridge.ts:59 importa mapLayers de dwg-document-bridge-layers, y ese fichero, en su línea 20, importa DWG_BRIDGE_LOSS_CODES, que se define en dwg-document-bridge.ts:105. NOCHE_MIMO_20260914.md:390 marca «D-6 (calidad) | HECHA». El matiz: la nota de esa fila dice «Ciclo A de importación eliminado», así que no queda del todo oculto, pero el estado HECHA es incorrecto. Tampoco es un fallo hoy: DWG_BRIDGE_LOSS_CODES sólo se usa dentro de funciones (layers.ts:55-103) y la ficha lo describe como TDZ latente.
- **Arreglo:** Crea apps/web/src/lib/cad/dwg-bridge-loss-codes.ts y mueve allí la definición de DWG_BRIDGE_LOSS_CODES, sin cambiar su valor. Impórtala desde ese archivo en dwg-document-bridge.ts y en dwg-document-bridge-layers.ts, y deja en dwg-document-bridge.ts un `export { DWG_BRIDGE_LOSS_CODES } from "./dwg-bridge-loss-codes";` para no romper los specs que la importan del puente. No toques el laboratorio DWG ni DWG_IMPORT_FLAG/DWG_EXPORT_FLAG. Hasta que eso entre, cambia la fila de la línea 390 a «D-6 (calidad) | PARCIAL | Ciclo A eliminado; ciclo B pendiente».
