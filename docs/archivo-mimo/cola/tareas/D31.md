# D31 · El panel de láminas —donde se fija la escala del plano— está en inglés

**Gravedad:** se-ve-precario   **Esfuerzo:** minutos
**Fichero:** `apps/web/src/components/cad/palettes/CadLayoutManager.tsx:477`

## El problema
«Standard scale» (:477), «Custom» (:501), «Custom scale» (:510), «Annotation scale» (:532), «Named view» (:554) y «Viewport» (:309), mientras las etiquetas de deshacer que están a dos líneas de distancia sí están en español («Escala estándar», «Escala personalizada», «Escala anotativa»). Es exactamente el panel donde el arquitecto fija 1:50 antes de imprimir, o sea el eslabón 5 del flujo, y es la única superficie del estudio con inglés visible según el barrido de rótulos.

## La tarea
Traducir a español de México TODOS los rótulos visibles de apps/web/src/components/cad/palettes/CadLayoutManager.tsx, no sólo seis, dejando intacta la lógica.

Traducir (líneas en main): :301 «Viewports · {n}» → «Ventanas · {n}»; :309 botón «Viewport» → «Ventana»; :433 «Paper {field}» y :457 «Model {field}» → «Papel …» / «Modelo …» mediante un mapa literal de rótulo (x→X, y→Y, width→Ancho, height→Alto) — NO interpolar `field` traducido, porque esa misma variable arma `data-testid={`cad-viewport-paper-${field}`}` y debe seguir en inglés; :477 «Standard scale» → «Escala estándar»; :501 TEXTO del option «Custom» → «Personalizada»; :510 «Custom scale» → «Escala personalizada»; :532 «Annotation scale» → «Escala anotativa»; :554 «Named view» → «Vista nombrada»; :630 «Reset» → «Restablecer»; :639 «Add a viewport to begin.» → «Agrega una ventana para empezar.»; y :290 la etiqueta de deshacer `Viewport ${section}.${field}` → `Ventana ${section}.${field}`.

NO tocar bajo ninguna circunstancia: el atributo `value="custom"` de :501 ni sus comparaciones de :485 (`: "custom"`) y :489 (`!== "custom"`); los `value={scale}` numéricos de los options (e2e los conduce con selectOption("50") en apps/web/e2e/golden/102-auditoria-imprimir-reimprimir.spec.ts:352 y 108-auditoria-escala-bloqueada.spec.ts:74); ningún `data-testid`; ningún nombre de campo, prop o clase.

TERMINADO CUANDO: apps/web/src/components/cad/palettes/CadLayoutManager.spec.ts se amplía con un caso que renderiza el componente con un `space` que SÍ trae un viewport (el fixture actual usa `viewports: []` y con eso el bloque de escala no se monta) y afirma sobre el marcado, vía renderToStaticMarkup, que: (a) el texto visible no contiene ninguna de las cadenas Standard scale, Custom scale, Annotation scale, Named view, Paper width, Paper height, Model width, Model height, Reset, «Add a viewport to begin», ni «Viewport»/«Viewports» como rótulo; (b) siguen presentes los data-testid cad-viewport-scale, cad-viewport-custom-scale, cad-viewport-annotation-scale, cad-viewport-named-view y cad-viewport-paper-width / cad-viewport-model-height; (c) el select de escala sigue emitiendo `value="custom"` y una opción `value="50"`. Además, un segundo render con `viewports: []` afirma que el estado vacío dice el texto en español. El spec corre con `npx tsx src/components/cad/palettes/CadLayoutManager.spec.ts` y los e2e golden 20, 102 y 108 siguen pasando sin editarlos.

## Terminado cuando
Un spec de DOM sobre CadLayoutManager que afirma que ninguno de los rótulos renderizados coincide con /Standard|Custom|Annotation|Named view|^Viewport$/ y que los `data-testid` cad-viewport-scale, -custom-scale, -annotation-scale y -named-view siguen presentes.

---
No toques dependencias, presupuestos, umbrales ni aserciones. Un commit por tarea.
Si al hacerla descubres que el hallazgo es falso, ANOTALO en la bitacora y pasa a la siguiente:
no inventes un arreglo para un problema que no existe.
