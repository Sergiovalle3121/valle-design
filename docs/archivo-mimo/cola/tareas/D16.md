# D16 · /sla promete que sus cifras salen del catálogo real y están escritas a mano

**Gravedad:** se-ve-precario   **Esfuerzo:** minutos
**Fichero:** `apps/web/src/app/sla/SlaPage.tsx:119`

## El problema
La entrada de la página afirma: «Compromisos de disponibilidad, respaldo y respuesta, con la misma fuente que usa el resto del producto: el catálogo real, no una propuesta aparte». No es cierto. Del catálogo sale ÚNICAMENTE el nombre de cada columna: `nombresDeColumna()` sólo lee `plan?.name` (SlaPage.tsx:36-43). Todos los compromisos —99,5 % y 99,9 % de disponibilidad, backup «cada 6 h» y «cada hora», «restauración de prueba verificada semanal», «24x7 para Sev-1», retención de 90 días— son constantes escritas en el array de filas (SlaPage.tsx:126-142) y nada del producto las mide ni las produce. Es la única cifra inventada que encontré en toda la superficie pública: la portada tiene prohibido por spec publicar importes y /precios los lee del catálogo de verdad.

## La tarea
Corregir la atribución de la intro de /sla para que el catálogo se lleve sólo lo que aporta (los nombres de plan) y los compromisos apunten a su fuente real, `docs/ops/SLA.md`.

1) En `D:\dev\valle-design\apps\web\src\app\sla\SlaPage.tsx:119`, reescribir el `intro` siguiendo la redacción que ya es correcta en `apps/web/src/app/sla/page.tsx:9` («con los nombres del catálogo real»): los NOMBRES de columna se leen del catálogo público en vivo, y los compromisos vienen de `docs/ops/SLA.md`. No tocar ningún valor de los arrays de filas (126-142, 163-182, 190-193): decidir esas cifras es del dueño, no de ingeniería.

2) NO añadir ningún rótulo de «objetivos no medidos» sobre las tablas. Sería falso —`docs/ops/SLA.md` §1 define el instrumento de medida de la disponibilidad, §5 registra un ejercicio de restauración medido el 2026-08-15 y §7 lista comprobaciones ejecutables— y además la página ya declara la procedencia y la falta de revisión legal en su sección final «Borrador pendiente de revisión legal».

3) Cubrirlo AMPLIANDO el spec existente `apps/web/src/app/sla/sla-surface.spec.ts` (no crear un fichero nuevo, y no prohibir la subcadena «catálogo real»: es verdadera respecto de las columnas y es la tesis del spec actual). Añadir dos aserciones positivas sobre el texto del atributo `intro=` de SlaPage.tsx:
   - si la intro menciona el catálogo, debe hacerlo ligado a «nombres» (que no vuelva a atribuirle los compromisos);
   - la intro debe nombrar `docs/ops/SLA.md` como fuente de los compromisos.
   Mantener intacta la aserción vigente de que la página usa `fetchPublicCatalog` y los tres códigos `standalone-trial` / `individual` / `despacho`.

TERMINADO CUANDO: `node --test` (o el runner que use apps/web para los `*.spec.ts`) pasa `sla-surface.spec.ts` con las dos aserciones nuevas, la intro de /sla ya no atribuye los compromisos al catálogo, y ni un solo valor de las tablas ha cambiado.

## Terminado cuando
Un spec que afirma que la intro de /sla no contiene «catálogo real» mientras las filas de la tabla sigan siendo constantes del módulo.

---
No toques dependencias, presupuestos, umbrales ni aserciones. Un commit por tarea.
Si al hacerla descubres que el hallazgo es falso, ANOTALO en la bitacora y pasa a la siguiente:
no inventes un arreglo para un problema que no existe.
