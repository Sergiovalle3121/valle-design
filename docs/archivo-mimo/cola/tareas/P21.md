# P21 · Bajar el titular de 4 líneas a 2

**Impacto:** quita-saturacion   **Frente:** portada   **Esfuerzo:** minutos
**Fichero:** `D:\dev\valle-design\apps\web\src\app\page.tsx`

## Que cambia para quien mira la app
Medido: `#hero-title` a 84 px en una columna de 552 px sale a 4 líneas y 329 px de alto — el 37 % del pliegue es solo titular, y es la mitad de la razón por la que el botón cae fuera de pantalla. En móvil son 149 px a 38 px de cuerpo.

## Que hacer
TÍTULO: Bajar el titular del hero de 4 líneas a 2 dentro de un presupuesto de 22 caracteres

POR QUÉ (medido en la rama, no estimado): a 1440x900 `#hero-title` sale a 552 px de ancho y 329 px de alto — 4 líneas de 82.32 px (`font-size` 84 px, `line-height` 0.98) — el 36.6 % del pliegue. Consecuencia medida: `[data-testid="hero-demo-cta"]` termina en y=936 con pliegue de 900, o sea 36 px fuera de pantalla; bajar el titular a 2 líneas libera 164 px y por sí solo mete el botón dentro. A 390x844 el mismo titular mide 149 px (4 líneas de 37.24 px a 38 px de cuerpo).

DÓNDE:
- D:\dev\valle-design\apps\web\src\app\page.tsx (línea 323, el `<h1 id="hero-title" className="type-display mt-5 max-w-2xl">`)
- D:\dev\valle-design\apps\web\src\app\opengraph-image.tsx (línea 18, `title:`)
- D:\dev\valle-design\apps\web\src\app\twitter-image.tsx (línea 19, `title:`)
Los tres repiten hoy la misma cadena «Dibuja en 2D. Modela en 3D directo. En tu navegador.». Si sólo se cambia el h1, las tarjetas sociales quedan anunciando el titular viejo.

QUÉ HACER:
1. Sustituir esa cadena en los TRES ficheros por un titular de 22 caracteres o menos. El presupuesto no es estilístico, es geométrico: a 84 px en 552 px la palabra «navegador.» mide 417 px sola (75 % de la línea), así que cualquier titular de 30+ caracteres se va a 3 líneas / 247 px y FALLA el criterio. Candidatas ya medidas a 2 líneas / 165 px en escritorio y 74 px en móvil: «CAD en el navegador.» · «Dibuja y modela ya.» · «Dibuja y modela aquí.» · «CAD sin instalar nada.» · «CAD real, sin instalar.». La elección final de voz es de Sergio; lo que no es negociable es que pase el spec. Soltar «2D/3D» del titular es correcto y no abre hueco: la insignia `Brep3DBadge` inmediatamente encima (page.tsx:316) ya dice «Nuevo: modelado 3D directo, sobre el mismo documento que tu plano».
2. NO tocar `type-display` ni añadir `font-size` arbitrario, `text-[Npx]` ni pesos nuevos. El tamaño es token de `globals.css:1435`.
3. NO tocar `apps/web/src/lib/marketing/faq.ts`, `SiteFooter.tsx` ni `brand.ts`. La contradicción de voz 2D/3D entre hero, FAQ, pie y tagline ya está levantada aparte en `.mimocode/vallecad-lanzamiento.md:582` y es decisión de Sergio; meterla aquí rompe la atomicidad.

TERMINADO CUANDO:
- Spec Playwright nuevo en `apps/web/e2e/public/` (p. ej. `portada-titular-pliegue.spec.ts`), midiendo el elemento y no el contenedor, porque a 1440 el límite lo pone la columna del grid (552 px) y no `max-w-2xl` (672 px):
  · a 1440x900, `boundingBox().height` de `#hero-title` <= 175 px (hoy 329; dos líneas exactas = 165);
  · a 390x844, <= 80 px (hoy 149; dos líneas exactas = 74.5 — techo apretado a lo medido, no los 120 px holgados que dejarían pasar 3 líneas);
  · y la consecuencia que motiva todo, que es la aserción que de verdad importa: a 1440x900 `boundingBox().y + height` de `[data-testid="hero-demo-cta"]` <= 900 (hoy 936).
- Spec de nodo: el `<h1 id="hero-title">` sigue llevando `type-display` y su propio `className` no contiene `font-` ni `text-[`. Acotado al h1, no a un recuento global de page.tsx. NO añadir una regla anti-`text-[Npx]`: la regla 1 de `apps/web/src/components/ui/design-system.spec.ts` ya la aplica sobre todo `src/**/*.tsx` y está en verde — duplicarla es trabajo ya hecho.
- Aserción de coherencia: la cadena del `<h1>` y los `title:` de `opengraph-image.tsx` y `twitter-image.tsx` son idénticos entre sí, para que la próxima vez no se desincronicen.

UN COMMIT. No toca dependencias, presupuestos, umbrales ni aserciones existentes: sólo añade specs. No toca tokens de relleno ni de tinta.

## Terminado cuando (medible, no «se ve mejor»)
Spec Playwright: a 1440x900 el alto de `#hero-title` es <= 175 px (dos líneas de 84 px con line-height 0,98; hoy 329) y a 390x844 <= 120 px. Spec de nodo: el `<h1>` sigue llevando `type-display` y `page.tsx` no introduce ninguna clase `text-[` ni `font-` de peso nuevo (hoy solo hay 2 `font-semibold`).

---
Un commit. Respeta el sistema de diseno y su gate (relleno y tinta son dos tokens; `--primary` no es relleno de boton). Comprobacion EJECUTADA antes de marcar hecho. No toques dependencias, presupuestos, umbrales, aserciones ajenas ni artefactos de evidencia.
