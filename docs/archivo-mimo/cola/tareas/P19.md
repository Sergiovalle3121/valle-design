# P19 · Meter el botón principal dentro del primer pliegue

**Impacto:** quita-saturacion   **Frente:** portada   **Esfuerzo:** una-sesion
**Fichero:** `D:\dev\valle-design\apps\web\src\app\page.tsx`

## Que cambia para quien mira la app
Medido: `a[href="/register"]` está en y=887 de un viewport de 900, o sea 13 px de 48 visibles. En los primeros diez segundos el visitante no tiene ningún botón que pulsar, y ha leído 112 palabras para no encontrarlo.

## Que hacer
TÍTULO: Meter el botón principal dentro del primer pliegue recortando el párrafo del hero

POR QUÉ: Medido en producción con caja delimitadora, no estimado. A 1440x900, `section[aria-labelledby="hero-title"] a[href="/register"]` está en `top=887, bottom=935, height=48`: 13 px de 48 visibles. El párrafo de entrada ocupa `height=243px` = 8 líneas de 30.4px, y sus 70 palabras / 438 caracteres repiten lo que `featureList` (page.tsx:219), `CapabilityExplorer` (banda `capacidades`, page.tsx:469-476) y la banda de capacidades ya dicen completo más abajo. El visitante lee 97 palabras antes de encontrar un botón. A 390x844 el botón YA está dentro (`top=726, bottom=774`): ahí no hay nada que arreglar, sólo que defender.

DÓNDE: D:\dev\valle-design\apps\web\src\app\page.tsx (rama `claude/noche-mimo-razones-para-pagar`)

QUÉ HACER:
1. Recortar el `<p className="type-lead mt-6 max-w-xl text-muted-foreground">` de las líneas 326-335 a UNA frase de <=25 palabras que diga qué es y para quién. A 552px de ancho y 16.34px de tipo caben ~75 caracteres por línea: 25 palabras son 2-3 líneas, y el recorte devuelve 150-182px. Eso baja `bottom` de 935 a ~753-785, con 115px de margen sobre el pliegue.
2. Borrar el párrafo «Antes de que lo preguntes: no abrimos archivos DWG...» (líneas 362-366). Es duplicado literal de `limits[0]` en page.tsx:162-166 («No abrimos ni escribimos DWG» + el párrafo del DXF con manifiesto de pérdidas), que se pinta en la banda de límites (page.tsx:511). `seo-surface.spec.ts:402-420` sólo PROHÍBE claims de DWG; no exige este texto en ninguna parte.
3. NO borrar `<FreeLaunchNote/>` (línea 361). Es la única aparición de la promesa de lanzamiento en la portada — el otro uso vive en `AuthPage.tsx:227`, que es el alta, no la portada — y está por debajo del botón, así que no cuesta ni un píxel dentro del pliegue. Si se quiere fuera del hero, es OTRA tarea y hay que decir a qué banda va.
4. No tocar `buttonClass({ variant: "primary", size: "lg" })` ni ningún token: el gate del sistema de diseño trata relleno y tinta como dos tokens y `--primary` no sirve de relleno.

TERMINADO CUANDO:
A) Spec Playwright nuevo en `apps/web/e2e/public/portada-pliegue.spec.ts` (carpeta ya existente para specs de páginas públicas), sobre `/`:
   - a `viewport 1440x900`: `const b = await page.locator('section[aria-labelledby="hero-title"] a[href="/register"]').boundingBox(); expect(b.y + b.height).toBeLessThanOrEqual(900)`.
   - a `viewport 390x844`: la misma medida `<= 844`. Esta segunda ya pasa hoy (`bottom=774`) y entra como guarda de regresión declarada en el comentario del spec, no como arreglo.
   - Añadir en el mismo spec: `expect(columnaIzquierda.height).toBeGreaterThan(figuraDelHero.height)`. El contenedor es `grid items-center` (hoy izquierda 871px contra figura 473px): mientras la izquierda sea la más alta, cada píxel recortado sube el botón 1:1; si baja de la altura de la figura, `items-center` recentra y la mitad de cada recorte futuro se pierde. Esta aserción hace visible ese suelo en vez de dejar que alguien lo descubra midiendo.
B) Spec de nodo nuevo en `apps/web/src/app/hero-carga-de-lectura.spec.ts` (lo descubre solo `apps/web/scripts/run-specs.mjs`, patrón `src/**/*.spec.ts`):
   - Lee `src/app/page.tsx`, recorta el bloque entre `<section aria-labelledby="hero-title"` y la primera aparición de `href="/register"`, borra comentarios `{/* */}` y `/* */` con la misma técnica que `seo-surface.spec.ts:390-393` (`visibleCopy`), sustituye `{PRODUCT_LABEL.design}` por `BRAND.productNames.design` (`packages/contracts/src/brand.ts:95` = "VALLECAD", una palabra), quita etiquetas y expresiones JSX, y parte por espacios.
   - `assert.ok(palabras.length <= 45)`. LÍNEA BASE REAL: 97 palabras en esta rama, no 112 (contadas en el fuente y confirmadas con un TreeWalker sobre el DOM de producción, que da 98 porque el build desplegado rotula «VALLE Design», dos palabras). El comentario del spec debe escribir 97 y decir de dónde sale, o documenta una cifra falsa.
   - `assert.doesNotMatch(bloqueHero, /no abrimos archivos DWG/i, "el aviso de DWG vive en la sección limits, no en el hero")`.
   - `assert.match(page, /<FreeLaunchNote/, "la portada no puede quedarse sin la promesa de lanzamiento: es su única aparición fuera del alta")`.
   - Terminar con un `console.log` de resumen: `run-specs.mjs:70-73` falla cualquier spec que salga con éxito sin imprimir nada.
C) `cd apps/web && npm run test:specs` y `npx playwright test e2e/public/portada-pliegue.spec.ts` en verde, sin tocar dependencias, presupuestos, umbrales ni aserciones existentes.

## Terminado cuando (medible, no «se ve mejor»)
Spec Playwright: a 1440x900 el `a[href="/register"]` del hero tiene `bottom <= 900`; a 390x844, `bottom <= 844`. Spec de nodo en `apps/web/src/app/`: el bloque del hero de `page.tsx` cuenta <= 45 palabras visibles antes del `href="/register"` (hoy 112).

---
Un commit. Respeta el sistema de diseno y su gate (relleno y tinta son dos tokens; `--primary` no es relleno de boton). Comprobacion EJECUTADA antes de marcar hecho. No toques dependencias, presupuestos, umbrales, aserciones ajenas ni artefactos de evidencia.
