# P14 · Las seis caras del ViewCube se pueden pulsar (hoy «Der» es inalcanzable)

**Impacto:** corrige-mentira   **Frente:** taller-jerarquia   **Esfuerzo:** una-sesion
**Fichero:** `D:\dev\valle-design\apps\web\src\components\cad\viewport\CadViewCube.tsx`

## Que cambia para quien mira la app
Medido a 1440x900 con elementFromPoint en 9 puntos por cara: la cara «Der» devuelve `cad-viewcube-face-front` en los 9 — la vista lateral derecha no se puede pedir con el ratón. La cara «Sup» mide 65x12 px. Y no hay una sola referencia a `cad-viewcube` en e2e/ ni en src/*.spec.ts: nada lo prueba.

## Que hacer
TÍTULO: Las seis vistas del ViewCube se pueden pulsar de verdad (hoy «Der» está de espaldas y «Sup» es una lámina de 12 px)

POR QUÉ: En `CadViewCube.tsx` la cara `right` lleva `rotateY(90deg) translateZ(22px)`; sumado al `rotateY(35deg)` del padre su normal queda a 125° del observador (z = -0,546): está DE ESPALDAS. Su cuadrilátero no sale a la derecha del cubo, se pliega sobre el de `front` (proyectados: Fte span 40 px, Der span 27 px, mismo borde derecho), y como el centroide z'' de `front` (19,8) es mayor que el de `right` (-9,35), `front` se pinta encima: `document.elementFromPoint` en el centro de `right` devuelve `cad-viewcube-face-front` y la vista lateral derecha NO se puede pedir con el ratón. La cara `top`, con sólo -18° de elevación, proyecta 65,0 x 11,6 px: una lámina. Y `grep -rn "cad-viewcube"` fuera del componente devuelve VACÍO — ninguna prueba lo toca, pese a que `apps/web/e2e/fixtures/camera-preset.ts` afirme en su comentario que «el ViewCube tiene su propio golden» y que «funciona».

DÓNDE:
- D:\dev\valle-design\apps\web\src\components\cad\viewport\CadViewCube.tsx (líneas 94-118: las tres caras).
- Golden nuevo: D:\dev\valle-design\apps\web\e2e\golden\215-cad-viewcube-seis-caras-pulsables.spec.ts (el máximo actual es 214).

QUÉ HACER (alcance cerrado, un commit):
1. Arreglar las TRES caras del cubo. Ojo: no comparten caja como se dijo — `top` es `left-1 top-0 h-11 w-11` (línea 99) y sólo `front` y `right` comparten `left-1 top-3 h-11 w-11` (107 y 115). Los dos defectos son distintos: `right` está de espaldas (hay que girarla al lado visible, p. ej. `rotateY(-90deg)` o mover el `translateZ` al eje correcto, y que su proyección caiga A LA DERECHA del borde compartido, no encima de `front`); `top` es una lámina por la elevación de 18°.
2. Vale cualquiera de las dos salidas que ya proponía: reconstruir la geometría 3D para que las seis regiones se repartan el área, o separar la geometría visual (decorativa, `pointer-events-none`) de unas zonas de clic explícitas superpuestas. La segunda es la más barata y la que no depende de cómo Chromium ordene en profundidad.
3. NO tocar los tres satélites (`left`/`back`/`iso`, líneas 122-141): ya miden ~32x25 px y ya pasan el umbral. NO tocar `camera-view-presets.ts` ni `applyCadCameraViewPreset` (Layout3DEditor.tsx:11576-11582): el cableado está bien. Los seis `data-testid="cad-viewcube-face-${preset}"` se conservan con ese nombre exacto.
4. No ampliar el widget: el contenedor `cad-viewcube` no debe crecer.
5. Tokens: reusar los que ya están (`bg-surface/95`, `border-border/70`, y `bg-brand-strong` + `text-primary-foreground` para activo/hover). No introducir `--primary` como relleno — relleno y tinta son dos tokens.
6. Borrar del comentario de `apps/web/e2e/fixtures/camera-preset.ts` la afirmación «El ViewCube tiene su propio golden» / «el ViewCube funciona» y dejar apuntado el golden 215 (al escribirlo pasa a ser verdad).

TERMINADO CUANDO el golden 215 pasa, con estas cuatro aserciones:
(a) REACHABILIDAD POR BARRIDO (sustituye a «4 puntos interiores», que no es medible sobre un paralelogramo proyectado). En modo 3D, dentro de un solo `page.evaluate`: tomar la caja de `cad-viewcube`, barrerla en rejilla de 2 px llamando `document.elementFromPoint` en cada punto y anotar, para cada punto, a cuál de las seis caras pertenece (la cara misma o un descendiente suyo). Para CADA uno de los seis `cad-viewcube-face-*` debe existir al menos un cuadrado alineado al eje de 20x20 px en el que TODOS los puntos barridos pertenezcan a esa cara. Falla nombrando la cara y quién le gana el punto. Esto mide a la vez que no se solapan y que ninguna queda por debajo de 20 px de zona útil, sin suponer que las caras sean rectángulos y sin números de posición que envejezcan.
(b) CLIC REAL: `await page.getByTestId('cad-viewcube-face-X').click()` para los seis presets (`top`, `front`, `right`, `back`, `left`, `iso`). La comprobación de accionabilidad de Playwright falla con «intercepts pointer events» si otra capa se queda el punto, así que es una segunda prueba independiente de la (a) y desde el lado del usuario.
(c) NO CRECE: la caja de `cad-viewcube` mide como máximo 120 x 110 px (hoy ~105 x 95). Dejar en la cabecera del spec la medida real observada, como hacen los goldens del repo.
(d) El barrido de (a) y los clics de (b) sólo valen en 3D: entrar con `enter3DView` de `apps/web/e2e/fixtures/view-mode.ts`, montar con `installCadStudioBackend` + `loginAsStandaloneOwner` sobre `/legacy/studio` y saltar el tour (`cad-guided-tour-skip`), igual que `67-cad-nada-tapa-un-control.spec.ts`. No usar `getByTitle` para el ViewCube: el gate `check:e2e-localizadores` lo prohíbe fuera de `camera-preset.ts`; ir siempre por testid.

Prohibido, como marca la regla: tocar dependencias, presupuestos (`scripts/cad/monolith-budget.json`), umbrales o aserciones existentes. El golden 215 es nuevo y no modifica ningún otro.

## Terminado cuando (medible, no «se ve mejor»)
Golden nuevo que en modo 3D prueba `document.elementFromPoint` en el centro y en 4 puntos interiores de cada una de las seis caras `cad-viewcube-face-*`: los cinco puntos resuelven a esa cara o a un descendiente suyo, para las seis; y ninguna cara mide menos de 20 px de lado.

---
Un commit. Respeta el sistema de diseno y su gate (relleno y tinta son dos tokens; `--primary` no es relleno de boton). Comprobacion EJECUTADA antes de marcar hecho. No toques dependencias, presupuestos, umbrales, aserciones ajenas ni artefactos de evidencia.
