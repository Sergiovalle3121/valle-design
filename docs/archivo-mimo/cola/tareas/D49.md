# D49 · NO TOCAR: la base de datos `valle_design_*` y los 5 nombres npm

**Gravedad:** deuda-interna   **Esfuerzo:** minutos
**Fichero:** `.env.example:6`

## El problema
`valle_design` aparece 51 veces en 24 ficheros y es casi todo el NOMBRE DE LA BASE DE DATOS Postgres: .env.example:6,11,182 (valle_design_dev, valle_design_test), .github/workflows/ci.yml:159,334 (valle_design_ci), docker-compose.yml (6 veces), más las guías de backup/restore. Renombrar la base obliga a recrearla en Railway y a reescribir cada cadena de conexión; no lo ve ningún cliente. Lo mismo con los 5 nombres npm (package.json:2 «valle-design», apps/api/package.json:2 «valle-design-api», @valle-design/contracts, @valle-design/dwg-codec, @valle/design-sdk): 24 ocurrencias en 7 ficheros de paquete más el lockfile, y 109 líneas de import en 71 ficheros. Son identificadores internos, no marca — y el propio brand.ts:11-16 ya defiende esa separación.

## La tarea
No renombrar nada. Añadir un tercer punto al bloque «Separación deliberada» del docblock de CABECERA de packages/contracts/src/brand.ts (justo después de la línea 14, dentro del mismo comentario, NO al pie del fichero), con esta redacción exacta en cuanto al fondo: que existe una tercera categoría, los IDENTIFICADORES CONGELADOS, que SÍ contienen la cadena «valle» pero NO se renombran en un rebranding porque son estado o formato ya escrito, no superficie visible; y nombrarlos los tres grupos completos: (a) los nombres de paquete npm `valle-design`, `valle-design-api`, `@valle-design/contracts`, `@valle-design/dwg-codec` y `@valle/design-sdk` — ojo, dos scopes distintos —, fijados por el lockfile y por ~153 líneas de import; (b) las bases y volúmenes Postgres `valle_design_*` (dev, test, ci, e2e, deploy, staging, y los volúmenes valle_design_pgdata / valle_design_minio), que viven en Railway y en el VPS y cuyo renombrado es una operación de producción; (c) las APPID de XDATA `VALLE_DIM`, `VALLE_MLEADER` y `VALLE_BLOCK`, que viajan dentro de DXF ya exportados a discos de clientes y están fijadas por dxf-xdata-app-names.spec.ts y dxf-xdata-golden.spec.ts. NO escribir que son «neutrales»: contradice brand.ts:10-12, donde neutral significa que no contiene la marca (ese es PRODUCT_CODES = ["design"] en product-catalog.ts). La longitud la marca el contenido, no un tope de 3 líneas; ajustarse al ancho de 80 columnas del fichero. TERMINADO CUANDO: el punto nuevo está dentro del docblock de cabecera de brand.ts, cita los cinco nombres npm (incluidos @valle/design-sdk, valle-design y valle-design-api), `valle_design_*` y VALLE_DIM/VALLE_MLEADER/VALLE_BLOCK, dice «congelados por ser estado/formato» y no dice «neutrales»; y `npm run check:surface` sigue en verde. Cambio sólo de comentario: ni una línea de código, configuración o fichero de paquete.

## Terminado cuando
El comentario existe en brand.ts y cita los tres: `@valle-design/*`, `valle_design_*` y `VALLE_DIM/VALLE_MLEADER/VALLE_BLOCK`.

---
No toques dependencias, presupuestos, umbrales ni aserciones. Un commit por tarea.
Si al hacerla descubres que el hallazgo es falso, ANOTALO en la bitacora y pasa a la siguiente:
no inventes un arreglo para un problema que no existe.
