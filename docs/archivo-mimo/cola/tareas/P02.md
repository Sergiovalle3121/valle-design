# P02 · En 3D, el plano deja de pintarse ENCIMA de los volúmenes

**Impacto:** cambia-la-primera-impresion   **Frente:** demo-con-solidos   **Esfuerzo:** una-sesion
**Fichero:** `D:\dev\valle-design\apps\web\src\lib\cad\entity-three.ts`

## Que cambia para quien mira la app
Hoy los rótulos son sprites y las líneas de plano llevan `depthTest: false`, así que «SALA, COMEDOR, COCINA, BAÑO» y los trazos seguirían flotando sobre los muros nuevos — la misma foto que el dueño rechazó, ahora con volumen detrás.

## Que hacer
TÍTULO: En 3D, la tinta de plano se ocluye detrás de los volúmenes

POR QUÉ: `entity-three.ts` no conoce el modo del visor (grep de `mode` sólo acierta en `gl_Position`, :59), así que fuerza `depthTest:false` siempre. Los muros se dibujan con `MeshLambertMaterial` sin override (`wall-solid-three.ts:267`) → `depthTest:true`, `renderOrder 0`, y por tanto la tinta de plano a `renderOrder 28-31` se pinta encima de las caras. Es la foto que el dueño rechazó, ahora con volumen detrás.

DÓNDE: `D:\dev\valle-design\apps\web\src\lib\cad\entity-three.ts` y dos líneas en `apps\web\src\components\cad\editor\Layout3DEditor.tsx`.

QUÉ HACER

1) `CadThreeViewport` (`entity-three.ts:20-28`) gana un campo OPCIONAL `mode?: "2d" | "3d"`. Opcional y con `?? "2d"` en el consumo, porque hay llamadores que no se tocan y tienen que seguir viéndose igual: `browser-scene-setup.ts:174`, `entity-runtime-benchmark.spec.ts:104` y los literales de `entity-three.spec.ts`.

2) Deriva una sola vez `const planoPlano = (viewport.mode ?? "2d") !== "3d";` y aplícalo en CINCO construcciones de material (son seis los `depthTest:false` del fichero; la sexta queda fuera a propósito, ver punto 3):
   - :189 `SpriteMaterial` de `buildCadMTextSprite` — en 3d `depthTest:true` y el sprite pierde `renderOrder = 30` (:195), queda en 0.
   - :294 `LineBasicMaterial` del overview — en 3d `depthTest:true`, fuera `renderOrder = 28` (:298).
   - :388 `ShaderMaterial` del lote de INSERT — en 3d `depthTest:true`, fuera `renderOrder = 29` (:392). Su vértice ya sitúa la geometría en `cadElevation` real, así que la prueba de profundidad tiene sentido.
   - :661 `MeshBasicMaterial` del relleno de hatch — en 3d `depthTest:true`, fuera `renderOrder = 29` (:667).
   - :686 `LineBasicMaterial` del trazo de `adapter.renderer.paths` — es el trazo de TODA entidad, no sólo del hatch. En 3d `depthTest:true`, fuera `renderOrder = 30` (:689).
   En modo 2d los cinco quedan EXACTAMENTE como hoy (`depthTest:false` + su `renderOrder`): es lo que evita el z-fighting entre entidades a distinta elevación bajo cámara ortográfica cenital.

3) NO toques :702-706. Esa esfera es el agarradero de selección (`adapter.grips.grips(entity)`, `marker.visible = selected`), no tinta de plano: con `depthTest:true` un muro esconde los grips de la entidad designada y se pierde la edición por arrastre en 3D. Se queda en `depthTest:false` + `renderOrder 31` en los dos modos, y dilo en un comentario para que la próxima sesión no lo «arregle».

4) Monolito, EXACTAMENTE 2 líneas (16891 hoy, asignación 16896 en `scripts\cad\monolith-budget.json:29`; queda en 16893 y el techo NO se sube):
   - En el literal `viewport` de `syncNativeScene` (`Layout3DEditor.tsx:3037-3042`) añade `mode: viewControllerRef.current?.mode ?? "2d",`. Ése es el objeto que ya viaja a `buildCadInsertBatchObject` (:3047), `buildCadNativeObject` (:3055) y `buildCadNativeOverviewObject` (:3187), y `viewControllerRef.current` es la MISMA fuente que lee `CadSolidShadeHost` (`:5958-5960`; contrato en `solid-shade-host.ts:71`, `readonly mode: "2d" | "3d"`).
   - En `configureOrbitControlsForMode` (`:8324-8326`), que hoy sólo hace `setMode` + `applyCadCameraPolicy`, añade `syncNativeScene();` — sin eso el primer 3D se dibuja con los materiales del 2D.
   NO pases por `CadNativeMassHosts(sceneViewport)` (`:5964`): ese anfitrión alimenta los sólidos de muro y masa (`native-mass-hosts.ts:27`) y no construye ninguno de estos materiales.

TERMINADO CUANDO

A) Spec de Node en `apps\web\src\lib\cad\entity-three.spec.ts` (lo corre `apps\web\scripts\run-specs.mjs` vía `npm run test:specs`), SÓLO aserciones nuevas — el fichero hoy no menciona `depthTest`, `renderOrder` ni `material` en ninguna línea, así que no se modifica ninguna existente. Para una entidad `text`, una `line` y un `hatch`, construidas dos veces con el mismo viewport (una sin `mode`, otra con `mode:"3d"`):
   - hijo con `userData.nativePath` de la `line` y hijo con `userData.nativeFill` del `hatch`: `material.depthTest === false` sin modo y `=== true` con `mode:"3d"`.
   - hijo con `userData.nativeText` del `text`: `material.depthTest === false` y `renderOrder === 30` sin modo; `material.depthTest === true` y `renderOrder === 0` con `mode:"3d"`.
   - hijo con `userData.nativeGrip`: `material.depthTest === false` en LOS DOS modos (fija el punto 3 y evita que se revierta).

B) Golden nuevo en `apps\web\e2e\golden\`, con el patrón de `140-cad-alzado-muro-nativo.spec.ts` para sembrar `type: 'wall'` por `installMockBackend` + `CadV1Backend` (sin API viva) y el de `101-auditoria-tresd-dibujar-en-fachada.spec.ts` para la cámara: `enter3DView` (`e2e\fixtures\view-mode.ts`) + `isoView`/`fitFootprint` (`e2e\fixtures\camera-preset.ts`). Siembra dos muros de `height: 2_700` y un `mtext` cuya inserción caiga DETRÁS de una cara desde el ojo isométrico. Sobre `canvas.screenshot()`, en un recorte pequeño centrado en esa cara:
   - en 3D ningún píxel del recorte coincide con la tinta del rótulo (capa del rótulo, tolerancia por canal declarada en el spec);
   - en el MISMO recorte en 2D el rótulo SÍ aparece.
   Las dos mitades son obligatorias: sin la segunda, el spec pasa igual si el rótulo desap

[...recortado; el fichero citado arriba es la fuente]

## Terminado cuando (medible, no «se ve mejor»)
Spec de Node sobre el constructor de objetos nativos: para una entidad `text`, una `line` y un `hatch`, el material construido tiene `depthTest === false` en modo 2d y `depthTest === true` en modo 3d, y el sprite de texto pierde su `renderOrder = 30` en 3d. Ninguna aserción existente se modifica (los goldens afirman DOM y datos, no materiales). En `demo-volumen.spec.ts`, una captura recortada al centro de la huella en 3D donde el píxel sobre una cara de muro NO es el color del rótulo.

---
Un commit. Respeta el sistema de diseno y su gate (relleno y tinta son dos tokens; `--primary` no es relleno de boton). Comprobacion EJECUTADA antes de marcar hecho. No toques dependencias, presupuestos, umbrales, aserciones ajenas ni artefactos de evidencia.
