# D47 · F0 — Verificar que ninguna variable de entorno gane al código

**Gravedad:** deuda-interna   **Esfuerzo:** minutos
**Fichero:** `.github/workflows/release.yml:169`

## El problema
`NEXT_PUBLIC_BRAND_NAME` (release.yml:169) y `NEXT_PUBLIC_BRAND_PRODUCT_NAME_DESIGN` (release.yml:186) se inyectan como build-args desde `vars.RELEASE_BRAND_*` y como ARG del Dockerfile (apps/web/Dockerfile:90,105). Si alguna está puesta con «Valle Design», cambiar el default del código NO cambia nada en producción: el resolvedor (packages/contracts/src/brand.ts:171,181) da prioridad al entorno. Hay que saberlo ANTES de tocar código, o la fase 1 se dará por hecha sin efecto.

## La tarea
F0 — Inventario SÓLO LECTURA de qué `BRAND_*` gana al default del código (sin tocar ninguna variable).

Ámbito: únicamente el servicio web. La API queda fuera: no consume marca (`git grep "BRAND_" main -- apps/api` no devuelve nada).

Paso 1 — GitHub: YA VERIFICADO, sólo hay que dejarlo escrito. Evidencia recogida el 2026-09-16 sobre main (6ae2ad62):
  - `gh api repos/Sergiovalle3121/valle-design/actions/variables` → `{"variables":[],"total_count":0}`.
  - Owner `"type":"User"` → no puede haber variables de organización.
  - El job `release` de `.github/workflows/release.yml:58` no declara `environment:`, así que el único environment existente (`honest-possibility / production`) no aporta `vars` a los build-args de las líneas 169 y 185.
  ⇒ Todas las `vars.RELEASE_BRAND_*` resuelven a vacío y, por `str()` en `packages/contracts/src/brand.ts`, vacío deja pasar el default del código.

Paso 2 — Railway (lo único pendiente de verdad): en el servicio `valle-web`, environments `staging` y `production`, listar las variables de BUILD y anotar cuáles de las 17 `NEXT_PUBLIC_BRAND_*` están puestas y con qué valor. Las 17 son las del bloque ARG de `apps/web/Dockerfile` (NAME, LEGAL_ENTITY, FOUNDER, DESCRIPTOR, TAGLINE_EN, TAGLINE_ES, SUPPORT_EMAIL, SALES_EMAIL, PRIVACY_EMAIL, WEBSITE_URL, COPYRIGHT, COPYRIGHT_YEAR, TRADEMARK_STATUS, TRADEMARK_SYMBOL, LOGO_MARK, LOGO_ICON, PRODUCT_NAME_DESIGN), no sólo las dos del título: `resolveBrandManifest` deja que el entorno pise todas. Requiere acceso al panel de Railway o `RAILWAY_TOKEN` — ninguno disponible en la máquina local, es ACCIÓN DEL TITULAR.

Paso 3 — Escribir en `docs/ops/railway.md` una sección nueva «Inventario de variables de marca (2026-09-16)» con: la evidencia de GitHub del paso 1, la tabla del paso 2 (clave / puesta sí-no / valor), y una frase que diga explícitamente cuáles de esas variables pisarían un cambio del default y cuáles no.

NO cambiar ni crear ninguna variable en este paso. Si el inventario revela alguna `NEXT_PUBLIC_BRAND_*` en Railway que fije el nombre viejo, eso se arregla en una tarea posterior, junto con el cambio del default del código, no antes — cambiarla ahora rebrandearía producción por delante de la decisión de código.

TERMINADO CUANDO: `docs/ops/railway.md` contiene esa sección fechada, enumerando las 17 claves con su estado en GitHub (todas ausentes, con la evidencia) y en Railway web por environment, y una conclusión explícita de si el default del código manda o no.

## Terminado cuando
Una sección en docs/ops/railway.md que enumera, con fecha, las variables BRAND_* existentes en GitHub y en Railway, y confirma que ninguna fija «Valle Design».

---
No toques dependencias, presupuestos, umbrales ni aserciones. Un commit por tarea.
Si al hacerla descubres que el hallazgo es falso, ANOTALO en la bitacora y pasa a la siguiente:
no inventes un arreglo para un problema que no existe.
