# D06 · F6 — Los legales: cambiar el nombre obliga a PUBLICAR versión nueva, no a editar

**Gravedad:** bloquea-al-usuario   **Esfuerzo:** una-sesion
**Fichero:** `scripts/legal/check-legal-content.mjs:97`

## El problema
check:legal recalcula el sha256 del FICHERO ENTERO apps/web/src/app/terms/page.tsx y lo compara con `contentHash` en apps/api/src/modules/legal/legal-documents.ts:110 (terms) y :120 (privacy). Ambas páginas nombran la marca en su description (terms/page.tsx:16, privacy/page.tsx:16), así que el rename cambia el hash y el gate se pone ROJO. La regla del candado es explícita: «una versión publicada NUNCA se edita»: hay que publicar entrada nueva y actualizar el espejo apps/web/src/lib/legal/legal-versions.ts:14,15. SOBRE LA REACEPTACIÓN: terms tiene `requiereAceptacion: true`, así que una versión nueva SÍ exigiría reaceptar — pero el propio registro declara en su cabecera (legal-documents.ts:24-38) que el enganche al web no está construido todavía, y hoy no hay usuarios. Es literalmente el mejor momento posible.

## La tarea
En UN SOLO commit, y sólo cuando el texto definitivo de ambas páginas esté fijado (si el rename toca esos ficheros otra vez habría que publicar OTRA versión):

1. Editar el literal de marca: apps/web/src/app/terms/page.tsx:16 y apps/web/src/app/privacy/page.tsx:16, «Valle Design» → «VALLECAD». MANTENERLO LITERAL, no sustituirlo por BRAND.productNames.design: config/brand.ts:6-9 manda resolver la marca por manifiesto en las páginas normales, pero en un texto legal eso sería una regresión — el texto publicado cambiaría al cambiar NEXT_PUBLIC_BRAND_* sin mover hash ni versión (el hueco que ya documenta .mimocode/vallecad-lanzamiento.md:222 para el intro de privacy, que usa BRAND en privacy/page.tsx:25). El candado tiene que poder ver el byte.

2. Guardar ambos ficheros en LF (core.autocrlf=input; hoy disco y blob dan el mismo sha256 — si se escriben en CRLF el hash local diverge del de CI). Recalcular después de escribir: `sha256sum apps/web/src/app/terms/page.tsx apps/web/src/app/privacy/page.tsx`.

3. En apps/api/src/modules/legal/legal-documents.ts REEMPLAZAR (no añadir) la fila vigente de cada documento: terms version/publicadoEn = '2026-09-16' y privacy version/publicadoEn = '2026-09-16', con los hashes nuevos. Respetar EXACTAMENTE la forma que exige el regex currentEntry (check-legal-content.mjs:48-51): comillas simples, orden de campos documento → version → publicadoEn → url → requiereAceptacion → contentHash, y NINGÚN comentario dentro del literal de objeto. Conservar url y requiereAceptacion (terms true, privacy false).

4. Poner el comentario justificativo ENCIMA del objeto (nunca dentro), al estilo de las entradas previas: que se publica versión nueva porque la marca pasa a VALLECAD y una versión publicada nunca se edita. Sustituir también los comentarios de las versiones que se reemplazan, para no dejar una cabecera que describa filas inexistentes.

5. Actualizar apps/web/src/lib/legal/legal-versions.ts:14,15 con las mismas versión y fecha, con COMILLAS DOBLES (el regex mirrorEntry, check-legal-content.mjs:69-71, exige `"`).

6. Corregir de paso la cabecera rancia legal-documents.ts:24-38, que declara como PENDIENTE un enganche al web que ya existe (CheckoutStarter.tsx:98-127 pide documents+acceptances y postea la versión exacta; AuthPage.tsx:51 y el golden e2e/golden/197-auditoria-terminos-al-alta.spec.ts cubren el alta). Dejar escrito el efecto real: quien ya aceptara terms 2026-09-06 quedaría bloqueado en el checkout hasta reaceptar; se publica ahora precisamente porque todavía no hay usuarios.

NO tocar apps/web/e2e/golden/197-auditoria-terminos-al-alta.spec.ts:19-20: su fixture es un mock propio de GET /v1/legal/documents, ya desfasado respecto al registro, y seguirá pasando.

TERMINADO CUANDO: `npm run check:legal` imprime «Candado legal OK», legal-versions.ts y legal-documents.ts declaran la misma versión y fecha para ambos documentos, y la suite de apps/api/src/modules/legal/legal-documents.spec.ts sigue verde (validateLegalDocumentsRegistry devuelve []).

## Terminado cuando
`npm run check:legal` imprime «Candado legal OK» y las versiones/fechas de legal-versions.ts coinciden con las de legal-documents.ts.

---
No toques dependencias, presupuestos, umbrales ni aserciones. Un commit por tarea.
Si al hacerla descubres que el hallazgo es falso, ANOTALO en la bitacora y pasa a la siguiente:
no inventes un arreglo para un problema que no existe.
