# D22 · F3 — El taller CAD escribe la marca a mano en 8 ficheros

**Gravedad:** se-ve-precario   **Esfuerzo:** una-sesion
**Fichero:** `apps/web/src/components/cad/CadStudioHost.tsx:108`

## El problema
El taller hardcodea la marca en 13 sitios en vez de leer `PRODUCT_LABEL.design`: CadStudioHost.tsx:108 (`brandName: "Valle Design"`), :110 (`productLabel`), :186 (el `<h1 className="sr-only">`); Layout3DEditor.tsx:1056,1058,5884,14346; CadStudioDialogs.tsx:108 (`<strong>VALLE DESIGN</strong>`, visible); ReviewLinkClient.tsx:274 (el pie del enlace de revisión que ve un tercero); demo-port.ts:70 (`PROJECT: "Demostración · Valle Design"`, va en el cajetín del /demo); lisp-runtime.ts:198; CadDxfExportDialog.tsx:311; export-host.ts:730. Es la pantalla que el dueño miró esta noche.

## La tarea
F3 (corregida) — Sincronizar la marca del taller CAD con el manifiesto, SIN re-letrearla por accidente y SIN meter config de marca en el monolito.

PASO 0 (bloqueante, decidir antes de tocar código). Hoy `DEFAULT_PRODUCT_NAMES.design = "VALLE Design"` (packages/contracts/src/brand.ts:81) y el override está comentado en .env.example:224 y apps/web/.env.example:21. El dueño debe decidir cuál es el nombre visible. Si la respuesta es "Valle Design", cambiar ese default a `design: "Valle Design"` en packages/contracts/src/brand.ts EN ESTE MISMO commit; si la respuesta es "VALLE Design", dejarlo y asumir que las 13 superficies cambian de grafía. Sin este paso, el resto del cambio altera texto que ve un tercero (el pie de ReviewLinkClient.tsx:274 y el cajetín del /demo) sin que nadie lo haya pedido.

PASO 1 — La frontera que YA importa la marca (apps/web/src/components/cad/CadStudioHost.tsx, ya importa `BRAND` en :36; añadir `PRODUCT_LABEL` al mismo import):
  :108  `brandName: BRAND.brandName,`            ← NO `PRODUCT_LABEL.design`
  :110  `productLabel: PRODUCT_LABEL.design,`
  :186  `<h1 className="sr-only">{`Editor de planos — ${PRODUCT_LABEL.design}`}</h1>`

PASO 2 — Componentes de la app web (no kernel), importar `PRODUCT_LABEL` de `@/config/brand`:
  collab/ReviewLinkClient.tsx:274 → `{PRODUCT_LABEL.design} · Este enlace da acceso únicamente a este plano.`
  dialogs/CadDxfExportDialog.tsx:311 → interpolar `PRODUCT_LABEL.design`.

PASO 3 — El monolito y el kernel: NO importar `@/config/brand`. Se consumen por la prop `branding` que el host ya inyecta:
  editor/Layout3DEditor.tsx:5884 y :14346 → usar `branding.productLabel` (o el `DEFAULT_BRANDING` vigente si no hay plataforma), no un literal.
  editor/Layout3DEditor.tsx:1056/1058 (`DEFAULT_BRANDING`) → SE QUEDAN como literales: son el respaldo documentado para cuando el editor se monta sin plataforma, y es el único punto donde un literal está justificado (dejar comentario que lo diga).
  editor/export-host.ts:730 y document-lifecycle/demo-port.ts:70 → recibir la etiqueta por parámetro/opciones desde el host, no importarla.

PASO 4 — Casos que NO son copy de UI:
  dialogs/CadStudioDialogs.tsx:108 es un logotipo en versales dentro de `.cad-print-sheet` (`display:none` fuera de impresión, globals.css:765). Conservar la grafía: añadir `text-transform: uppercase` a `.cad-print-sheet header strong` en globals.css y poner `<strong>{PRODUCT_LABEL.design}</strong>`.
  lisp/lisp-runtime.ts:198 `updatedBy` es un campo de datos de las rutinas de fábrica, persistido y mostrado como autoría. Dejarlo FUERA de esta tarea (tarea aparte, con nota de qué pasa con las bibliotecas ya guardadas).

TERMINADO CUANDO:
  `git grep -n 'Valle Design\|VALLE DESIGN' -- 'apps/web/src/components/cad' ':!*.spec.ts' ':!*.spec.tsx'` devuelve sólo líneas de comentario más `DEFAULT_BRANDING` (Layout3DEditor.tsx:1056/1058) y `lisp-runtime.ts:198`, ambas con comentario que explique por qué siguen ahí.
  `npm run typecheck && npm run test:specs && npm run check:cad` en verde, y además `npx playwright test e2e/golden/47-cad-lisp-appload.spec.ts` (afirma 'Valle Design' en cad-lisp-log) y los goldens de cajetín/PDF, que son los que ca

[...recortado. El fichero y la linea de arriba son la fuente: velo tu mismo.]

## Terminado cuando
`git grep -n 'Valle Design\|VALLE DESIGN' apps/web/src/components/cad -- ':!*.spec.ts'` devuelve sólo líneas de comentario, y `npm run typecheck && npm run test:specs && npm run check:cad` en verde.

---
No toques dependencias, presupuestos, umbrales ni aserciones. Un commit por tarea.
Si al hacerla descubres que el hallazgo es falso, ANOTALO en la bitacora y pasa a la siguiente:
no inventes un arreglo para un problema que no existe.
