# D09 · «Seleccio nar»: el botón de la paleta mide 48 px de contenido y parte las palabras a media sílaba

**Gravedad:** se-ve-precario   **Esfuerzo:** minutos
**Fichero:** `apps/web/src/components/cad/editor/CadToolPalette.tsx:148`

## El problema
El botón es `w-14` (56 px) con `px-1` (4+4) → 48 px útiles, y la etiqueta lleva `w-full break-words` (CadToolPalette.tsx:169). A `type-micro` (0,6875 rem = 11 px, globals.css:1570) caben ~8 caracteres; todas las etiquetas de 9 o más se parten dentro de la palabra: «Seleccionar» (11), «Rectángulo» (10), «Distancia» (9), «Polilínea» (9), y «Encuadre»/«Símbolos»/«Deshacer» (8) quedan en el filo — exactamente los cuatro que viste. El golden 212 (e2e/golden/212-cad-etiqueta-seleccionar-no-se-encima.spec.ts:100-113) sólo comprueba que la etiqueta no SE SALGA del botón, así que da por bueno el corte a media palabra. Además dos líneas por botón suben su alto de ~45 px a ~60 px, lo que engorda la columna entera.

## La tarea
Un solo cambio, en D:\dev\valle-design\apps\web\src\components\cad\editor\CadToolPalette.tsx:

1. Línea 148: `w-14` → `w-20` (80 px; 72 px útiles con `px-1`). Medido con la Inter real del repo a 0,6875 rem / peso 500 / letter-spacing 0,005em: la más ancha es «Ajustar todo» (64,73 px), luego «Seleccionar» (62,65) y «Rectángulo» (59,85). Las diecisiete caben en una línea en 72 px.

2. Línea 169: quitar `break-words` y poner `truncate` (= `whitespace-nowrap overflow-hidden text-ellipsis`). NO `whitespace-nowrap` a secas: el margen queda en 7 px y a 13 px de cuerpo (tamaño mínimo de fuente del navegador) «Ajustar todo» mide 76,5 px; sin recorte el texto vuelve a salirse por los dos lados y a montarse sobre el vecino de la rejilla, que es exactamente el defecto que el comentario de las líneas 158-168 dice haber arreglado. NO añadir `hyphens-none`: es un no-op, el corte lo produce `overflow-wrap: break-word`, no la partición silábica.

3. Reescribir el comentario de las líneas 158-168: hoy afirma que «Seleccionar» es «el más largo de los dieciséis» y que «no cabe en los 56 px del botón a ningún tamaño de fuente del sistema». Lo más largo es «Ajustar todo», y el problema no es que no quepa sino que el remedio elegido parte palabras.

4. No tocar apps/web/src/lib/cad/toolbar.ts: con 72 px ninguna etiqueta necesita acortarse.

TERMINADO CUANDO, en apps/web/e2e/golden/212-cad-etiqueta-seleccionar-no-se-encima.spec.ts: generalizar `medirBotonSeleccionar` para que recorra los diecisiete botones leyendo los rótulos de `CAD_TOOLBAR_ACTIONS` (el mismo patrón que apps/web/e2e/fixtures/tool-palette.ts: el rótulo es prosa de producto, el id es la identidad), y aplicar A TODOS, en los tres viewports que ya recorre (1280x900, 1280x760, 1024x668), las dos aserciones de contención en x que hoy sólo se le hacen a «Seleccionar» MÁS una tercera de una sola línea: `etiqueta.height <= 18` (una línea mide 15,13 px, dos miden 30,25 px; el 18 va comentado con esos dos números). Hoy fallan siete botones —Seleccionar, Ajustar todo, Rectángulo, Deshacer, Encuadre, Distancia y Símbolos—, no uno; después pasan los diecisiete.

VERIFICAR ADEMÁS: los goldens 67 (apps/web/e2e/golden/67-cad-nada-tapa-un-control.spec.ts) y 68 (68-cad-nada-tapa-el-lienzo.spec.ts), porque la paleta pasa de 68 a 92 px de ancho, y de 126 a 174 px en la rejilla de dos columnas que globals.css:1676 activa bajo 820 px de alto.

## Terminado cuando
Ampliar el golden 212 con una tercera aserción: `etiqueta.height <= 18` (una sola línea) en los tres viewports que ya recorre (1280x900, 1280x760, 1024x668). Falla hoy, pasa después.

---
No toques dependencias, presupuestos, umbrales ni aserciones. Un commit por tarea.
Si al hacerla descubres que el hallazgo es falso, ANOTALO en la bitacora y pasa a la siguiente:
no inventes un arreglo para un problema que no existe.
