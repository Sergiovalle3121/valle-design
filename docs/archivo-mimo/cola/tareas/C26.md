# C26 · Reanimar la prueba de guardar y reabrir un sólido con historial

**Frente:** confianza   **Esfuerzo:** una-sesion
**Fichero:** `apps/web/src/lib/cad/engine/commands/solids.spec.ts:468`

## Por que importa al arquitecto
Es la única prueba en todo el repositorio de que una pieza con árbol de construcción sobrevive al disco, y hoy no se ejecuta: `npm test` del web está en rojo y la garantía que anuncia el console.log final no se comprueba.

## Que hacer
TÍTULO: Reparar el recorte off-by-one que aborta la suite de sólidos en la línea 470

POR QUÉ IMPORTA (corregido — NO es la única prueba de que el árbol sobrevive al disco: `apps/web/src/lib/cad/solid3d.spec.ts:455-468` ya lo prueba y está verde con 59 aserciones): un error de un carácter en el propio spec aborta `solids.spec.ts` a mitad, así que quedan sin ejecutar la aserción de volumen tras IMPORT (475), el rechazo de un texto que no es ningún formato (478) y el bloque completo «La definición de terminado» (481-512). Como `apps/web/package.json` → `test` → `test:specs` → `scripts/run-specs.mjs` globa `src/**/*.spec.ts`, ese fichero mantiene ROJO el gate de specs del web. Se arregla el arnés de prueba, no el motor: el motor ya pasa.

DÓNDE: `D:\dev\valle-design\apps\web\src\lib\cad\engine\commands\solids.spec.ts:468`

QUÉ HACER (dos cambios, ninguna aserción existente se toca; no se toca `src/lib/`, ni dependencias, ni umbrales):
1. Sustituir en la línea 468
   `const payload = downloadRequest.content.slice(downloadRequest.content.indexOf("ISO-10303-21") - 1);`
   por
   `const payload = downloadRequest.content.slice(downloadRequest.content.indexOf("ISO-10303-21"));`
   Motivo, para el comentario del commit: `step-export.ts:210` emite `"ISO-10303-21;"` como primer elemento del `join("\n")` y `solids-interop.ts:153` no prefija nada, así que `indexOf` vale 0 y `slice(-1)` entregaba un solo `"\n"`.
2. Añadir inmediatamente después DOS aserciones nuevas, antes del `run("IMPORT", ...)` de la línea 469, para que un recorte mal hecho vuelva a fallar como recorte y no como «IMPORT no escribe»:
   `assert.ok(payload.startsWith("ISO-10303-21;"), "el recorte conserva la CABECERA del STEP: sin ella detectInteropFormat devuelve null y el fallo se disfraza de 'IMPORT no inserta'");`
   `assert.ok(payload.includes("END-ISO-10303-21;"), "y conserva el cierre del archivo: el payload es el STEP completo, no un fragmento");`
   La de `startsWith` es la que de verdad atrapa esta clase de error; la de `END-` sola no lo haría, porque el recorte roto perdía el principio, no el final.

TERMINADO CUANDO (todo comprobado desde `D:\dev\valle-design\apps\web`):
- `npx tsx src/lib/cad/engine/commands/solids.spec.ts` sale con código 0 e imprime su línea de resumen que empieza por `sólidos: EXTRUDE, PRESSPULL, REVOLVE, ...` (confirmado: con el arreglo la suite llega al final).
- El bloque de la 481 se ejecuta de verdad contra geometría real: volumen 1.120.000 en la pieza construida, `parseCadDocument(serializeCadDocument(document))`, `restored.nodes.length === 3`, el documento serializado NO contiene `"positions"`, y tras `clearSolidCache()` el volumen se reconstruye a 1.120.000 con género 1.
- Prueba de mutación obligatoria, y hay que ver los dos colores: (a) cambiar el volumen esperado de la línea 508 a `-1_120_000` pone la suite roja con `y el volumen se reconstruye al reabrir: 1119999.9999999998, se esperaba -1120000`; (b) restaurar el `- 1` de la línea 468 pone la suite roja en la guarda NUEVA de `startsWith`, no en la línea 470. Revertir ambas mutaciones antes de commitear y dejar `git status --porcelain` limpio salvo el fichero del punto 1-2.
- `node scripts/run-specs.mjs` ya no reporta `solids.spec.ts` como fallo. (Si el runner completo destapa OTROS specs rojos, no entran en este commit: anotarlos y proponerlos aparte, uno por tarea.)

## Terminado cuando
`npx tsx src/lib/cad/engine/commands/solids.spec.ts` desde `apps/web` termina imprimiendo su línea de resumen, y el bloque «La definición de terminado» de la línea 481 se ejecuta de verdad: volumen 1.120.000 tras `parseCadDocument(serializeCadDocument(...))` con `clearSolidCache()` de por medio y género 1 en la pieza reabierta. Comprobable invirtiendo el signo del volumen esperado y viendo que la suite se pone roja ahí.

---
Un commit. Comprobacion EJECUTADA antes de marcar hecho: el spec conduce el comando REAL contra el motor real; un spec que se prueba a si mismo, que hace grep del fuente, o cuyo arnes puede pasar sin el arreglo, se considera falso. No toques dependencias, presupuestos, umbrales, aserciones ajenas ni artefactos de evidencia de docs/cad/evidence/ o docs/dwg/.
