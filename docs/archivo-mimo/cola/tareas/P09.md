# P09 · Capturar un tiro 3D sombreado con el propio motor

**Impacto:** demuestra-capacidad   **Frente:** portada   **Esfuerzo:** una-sesion
**Fichero:** `D:\dev\valle-design\apps\web\scripts\capture-product-shots.mts`

## Que cambia para quien mira la app
El activo más fuerte del producto —26 comandos de sólidos con kernel B-rep— no aparece en ninguna de las 6 capturas, porque el script conmuta a 2D a propósito; sin este PNG no hay nada honesto que poner en el hero.

## Que hacer
TÍTULO: Capturar un tiro 3D sombreado del plano de ejemplo con el propio motor

POR QUÉ: Ninguna de las 6 capturas de `apps/web/public/product/` enseña volumen — `frameThePlan()` conmuta a 2D a propósito, con su motivo escrito en el guion. El dueño abre el 3D en producción, ve una planta inclinada y concluye que el 3D es flojo. Los 6 muros del plano de ejemplo ya tienen `height 2400` y `thickness 200` y se teselan con el kernel B-rep propio (`src/lib/cad/wall-solid-three.ts:18` importa `tessellateBody` de `../brep`), así que la foto honesta se puede tomar HOY sin dibujar geometría nueva.

DÓNDE: `D:\dev\valle-design\apps\web\scripts\capture-product-shots.mts` (rama `claude/noche-mimo-razones-para-pagar`).

QUÉ HACER:

1. Importar las fixtures, NO copiar sus localizadores (el guion ya importa de `../e2e/fixtures/`):
   `import { enter3DView } from "../e2e/fixtures/view-mode";`
   `import { isoView } from "../e2e/fixtures/camera-preset";`
   Copiar `getByTitle(/Vista isométrica/)` a mano reintroduciría el título ambiguo en un segundo sitio; el gate `check:e2e-localizadores` sólo audita `apps/web/e2e/`, así que aquí no lo atraparía nadie.

2. Insertar la sección nueva DENTRO de `if (theme === "dark")`, entre el `shoot(page, "paleta-propiedades", ...)` con su `Escape` y el comentario «Espacio papel con el cajetín». Ahí todavía se está en espacio MODELO; al final del bloque ya se pulsó la pestaña de presentación (`cad-space-tabs` nth(1)) y el conmutador 3D no aplica.

3. La sección, sin llamar a `frameThePlan()`:
   - `await enter3DView(page);`
   - `await isoView(page);`
   - Encuadrar tecleando: `await type(page, "ZOOM"); await type(page, "EX");` — nunca el botón «Ajustar a la planta», por el motivo que el propio guion documenta (la barra se desplaza y recorta la captura). `ZOOM EXtensión` sólo ajusta encuadre (`lib/cad/view/view-navigation.ts:338` llama a `fit`), no reorienta la cámara.
   - `await type(page, "VSCURRENT"); await type(page, "C");` (atajo de `shaded-edges` en `STYLE_SHORTCUTS`, `engine/commands/view-visual.ts:32-38`).
   - Esperar el asentamiento con el MISMO gancho que el golden 60: `await page.getByTestId("cad-render-pipeline").waitFor(...)` hasta `data-settled === "true"`, plazo 30 s (`viewport/RenderPipelineBadge.tsx:69`).

4. ANTES del disparo, una función nueva `assertVolumeIsReal(page)` con el mismo patrón que `assertNoDeadProductVocabulary` —acumula problemas y lanza UNA excepción con los tres números medidos y el motivo— que lea `[data-testid="cad-3d-solid-diagnostics"]` y falle si:
   - `data-mesh-count < 6` (menos de un volumen por muro: golden 60 prueba que un muro da 1),
   - `Number(data-vertex-count) === 0`,
   - `data-visual-style !== "shaded-edges"` (el valor de fábrica es `"shaded"`, así que la comprobación no es tautológica).

5. Disparar con un rótulo que diga la verdad estrecha:
   `await shoot(page, "modelo-3d-sombreado", "los muros y las masas en volumen, teselados por el kernel B-rep propio");`
   NO escribir «los 23 comandos de sólidos»: `CadNativeMassHosts.getSnapshot()` (`viewport/native-mass-hosts.ts:60-84`) recorre sólo su propio grupo —muros y masas arquitectónicas— y nunca `CadSolidShadeHost`, el anfitrión de SOLID3D. Ningún comando BOX/EXTRUDE/UNION entra en este tiro; eso es otra tarea.

6. Volver a 2D al terminar (`await frameThePlan(page);`) para que la lámina siguiente se fotografíe como hasta ahora.

7. NO tocar la tabla del MANIFIESTO: se genera con `...taken.map((s) => ...)` al final de `main()`, así que la fila aparece sola al llamar a `shoot()`. Verificarlo, no duplicarlo.

Prohibido tocar dependencias, presupuestos, umbrales o aserciones existentes, y prohibido modificar `frameThePlan` o el orden de las 6 capturas actuales.

TERMINADO CUANDO:

- `npm run capture:product -- --start` escribe `apps/web/public/product/modelo-3d-sombreado.png` a 2880x1800 (comprobable leyendo la cabecera PNG: octetos 16-23), y `apps/web/public/product/MANIFIESTO.md` pasa de 6 a 7 filas, con `modelo-3d-sombreado.png` entre ellas.
- Con cualquiera de los tres umbrales roto, el guion sale con código 1, la excepción imprime los tres valores leídos, `modelo-3d-sombreado.png` NO existe, y `MANIFIESTO.md` conserva sus 6 filas anteriores (su `writeFile` está después del bucle, así que no se ejecuta). Verificable forzando el fallo: congelar la capa `layout` antes de la sección nueva lleva `data-mesh-count` a 0 — es exactamente lo que mide el golden 60 en su segundo paso.
- No se afirma que no se escriba «ningún archivo»: para cuando corre esta comprobación ya están en disco `estudio-dark.png`, `linea-de-comandos.png`, `paleta-capas.png`, `paleta-propiedades.png` y `src/lib/cad/sample-plan.json`, escritos antes por el mismo recorrido.
- Las 6 capturas anteriores siguen a 2880x1800 y `git status` no enseña ningún otro cambio de producto.

## Terminado cuando (medible, no «se ve mejor»)
`npm run capture:product -- --start` escribe `apps/web/public/product/modelo-3d-sombreado.png` a 2880x1800, y aborta SIN escribir ningún archivo si `data-mesh-count < 6`, `data-vertex-count === 0` o `data-visual-style !== "shaded-edges"`. `apps/web/public/product/MANIFIESTO.md` lista la fila nueva.

---
Un commit. Respeta el sistema de diseno y su gate (relleno y tinta son dos tokens; `--primary` no es relleno de boton). Comprobacion EJECUTADA antes de marcar hecho. No toques dependencias, presupuestos, umbrales, aserciones ajenas ni artefactos de evidencia.
