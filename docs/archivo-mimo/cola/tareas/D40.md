# D40 · Las jambas de puertas y ventanas siguen sin snaps aunque llegue el documento

**Gravedad:** falta-capacidad   **Esfuerzo:** una-sesion
**Fichero:** `apps/web/src/lib/cad/opening-entity-adapter.ts:265`

## El problema
Segunda mitad del mismo defecto, pero es una entrega separada: aunque `CadSnapProvider` reciba el documento, el adaptador del hueco sigue devolviendo `[]`. La geometría ya está resuelta —`openingPaths(entity, 96, document)` se usa en :256 para el hit-test— así que los puntos existen y no se ofrecen. Acotar un vano (la cota más repetida de un plano de arquitectura: esquina-jamba-jamba-esquina) es imposible hoy, y la propia hoja de ruta lo marca como «falta» con valor 4.

## La tarea
Devolver los puntos de referencia del hueco desde `opening-entity-adapter.ts`, con la misma cadena que ya usa `openingPaths`, y hacerlos llegar al editor.

PRECONDICIÓN (no es parte de esta entrega): `CadSnapProvider.snaps` de `apps/web/src/lib/cad/entity-runtime.ts:176-178` debe aceptar un tercer argumento `document?: CadDocument`, igual que render, caja y hit-test, y `snap-scene.ts:206` pasarlo. Sin eso esta tarea no se puede ni escribir; si esa mitad no ha aterrizado, esta queda bloqueada, no se adelanta con un atajo.

1) En `opening-entity-adapter.ts:265`, sustituir `snaps: { snaps: () => [] }` por una implementación que reutilice EXACTAMENTE la cadena de `openingPaths` (:200-217), sin duplicar aritmética: `openingHost(entity, document)`; si no hay anfitrión o no hay documento → `[]`; `wallAxisFrame(host)` y `hostFootprint(host, document)`; si falta cualquiera → `[]`; `if (!wallOpeningFit(host, entity).ok) return []` — misma condición que los trazos, para que no haya un imán donde no hay dibujo (fix-or-hide). Con eso, de `wallOpeningJambs(wallFaces(frame, footprint), wallOpeningSpan(entity))` emitir los CUATRO puntos como `kind: "endpoint"` (los dos de la cara izquierda y los dos de la derecha, en `span.from` y `span.to`), con etiquetas que digan cuál es cuál, y UN único `kind: "midpoint"` en el centro del vano definido como `wallAxisPoint(frame, (span.from + span.to) / 2, 0)` — un solo punto sobre el eje, no uno por cara.
   Extraer la parte común (host + frame + footprint + fit + jambas) a una función privada del módulo que consuman tanto `openingPaths` como `snaps`: dos teselados distintos del mismo hueco harían que el punto que imanta deje de ser el que se ve, que es justo el argumento que el fichero ya usa para `blockChildPaths` (:182-191).
   Actualizar la cabecera (:30-39) y el comentario de :262-263, que hoy afirman lo contrario, y la promesa de `cad-entities-v7.ts:34` sólo en lo que toca a snaps (los grips siguen sin existir: eso es D-10/H-05 aparte).

2) En `apps/web/src/components/cad/editor/Layout3DEditor.tsx:6410`, pasar el documento vivo a `cadSnapSceneAddEntities` (5.º argumento, `loadedCadDocumentRef.current`, que ya se lee dos líneas más abajo como `documentoVivo`; el 4.º queda `undefined` para conservar `CAD_SNAP_SCENE_CURVE_SEGMENTS`). Sin esta línea el hueco sigue devolviendo `[]` en el producto y la entrega es inerte. OJO al efecto colateral, que hay que medir y declarar en el PR: con documento, los trazos del MURO pasan a ser el contorno UNIDO y con las caras partidas por el vano, así que `scene.segments` cambia y con él `Cercano`, `Intersección` y `Perpendicular` sobre muros — es más correcto, pero hay que correr las specs de enganche (`professional-snapping.spec.ts`, `snap-scene.spec.ts`, `professional-snap-query-benchmark.spec.ts`) y el golden `e2e/golden/28-cad-osnap-pointer.spec.ts` antes de darlo por bueno.

TERMINADO CUANDO: un spec de Node en `apps/web/src/lib/cad/` monta un documento con un WALL de 4.000 de largo y grosor conocido y un OPENING alojado con `width` 900 en una `position` conocida, pide `CAD_ENTITY_REGISTRY.adapter(opening).snaps.snaps(opening, cursor, document)` y afirma: (a) cuatro puntos `end

[...recortado. El fichero y la linea de arriba son la fuente: velo tu mismo.]

## Terminado cuando
Un spec que coloca un WALL y un DOOR con DOOR a distancia conocida, pide los snaps del hueco con el documento y afirma cuatro puntos `endpoint` en las coordenadas exactas de las jambas que `wall-openings` recorta, más el `midpoint` del vano.

---
No toques dependencias, presupuestos, umbrales ni aserciones. Un commit por tarea.
Si al hacerla descubres que el hallazgo es falso, ANOTALO en la bitacora y pasa a la siguiente:
no inventes un arreglo para un problema que no existe.
