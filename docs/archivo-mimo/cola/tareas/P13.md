# P13 · El recorrido guiado vuelve a arrancar plegado y su spec deja de mentir

**Impacto:** corrige-mentira   **Frente:** taller-jerarquia   **Esfuerzo:** una-sesion
**Fichero:** `D:\dev\valle-design\apps\web\src\components\cad\editor\CadLienzoAncho.spec.ts`

## Que cambia para quien mira la app
El arreglo T6 de anoche se revirtió: el registro del recorrido nace con `minimized: false` (guided-tour.ts:261), así que el panel arranca desplegado y cubre 110.590 px2 = 29 % del lienzo a 1280x720. Y su guardián, que promete «lienzo >= 75 % a 1800x962», sólo busca la cadena `useState(true)` — que ya no existe: el spec está ROJO y el suite entero con él.

## Que hacer
TÍTULO: El recorrido guiado arranca plegado en /demo y su guardián mide área en vez de leer cadenas

DÓNDE (rutas absolutas):
- D:\dev\valle-design\apps\web\src\components\cad\onboarding\CadGuidedTourDock.tsx
- D:\dev\valle-design\apps\web\src\components\cad\onboarding\tour-host.ts
- D:\dev\valle-design\apps\web\src\components\cad\editor\CadLienzoAncho.spec.ts
- NUEVO: D:\dev\valle-design\apps\web\e2e\golden\215-cad-recorrido-arranca-plegado.spec.ts

QUÉ HACER — un solo commit, tres piezas:

(1) PLEGADO DE FÁBRICA, SIN TOCAR EL DEFECTO.
En `tour-host.ts` añadir SÓLO dos lectores de sólo lectura al `CadTourHost` (no cambiar `attach`, `reset`, `dispatch` ni el reductor):
  `isLoaded = (): boolean => this.loaded;`
  `hasStoredRecord = (): boolean => readStorage(this.key) !== null;`
Añadir métodos no rompe `tour-host.spec.ts:66`, que compara el SNAPSHOT con `EMPTY_CAD_TOUR_RECORD`, no la forma de la clase.
En `CadGuidedTourDock.tsx`, un efecto que se ejecuta UNA vez y sólo cuando el almacén ya está leído (guardar el «ya lo hice» en un `useRef`, no en estado):
  si `cadTourHost.isLoaded() && !cadTourHost.hasStoredRecord()` → `cadTourHost.dispatch({ type: "minimize", minimized: true })`.
El efecto debe depender de `record` (o de `cadTourHost.isLoaded()` observado por el `useSyncExternalStore` que ya existe) para que corra DESPUÉS de que el padre llame `attach()`: `CadStudioHost.tsx:94-96` ata el almacén en un efecto del padre, que en React corre después de los del hijo, y un dispatch prematuro se pierde. Prohibido tocar `EMPTY_CAD_TOUR_RECORD` (`guided-tour.ts:261`), `parseCadTourRecord`, `reset` ni ninguna aserción de `guided-tour.spec.ts` / `tour-host.spec.ts`. Cero cambios de estilo: el pliegue ya está resuelto en el render (`data-collapsed`, línea 164, y el `cx` de `p-2` vs `max-h-[32vh] p-3.5`).

(2) NUEVO GOLDEN 215 — LA MEDICIÓN REAL (Playwright, ahí donde hay navegador).
`apps\web\e2e\golden\215-cad-recorrido-arranca-plegado.spec.ts`, al estilo de 163 y 211: `page.setViewportSize({width:1280,height:720})`, `page.goto("/demo")` con localStorage VACÍO (contexto nuevo; nada de fixture que siembre `valle:cad:tour:v1*`), esperar `cad-native-entity-list` visible (timeout 60_000, igual que el golden 163) y `cad-canvas` visible. Asertar, en este orden:
  a. `expect(page.getByTestId("cad-guided-tour")).toHaveAttribute("data-collapsed", "true")`.
  b. Solape medido con `getBoundingClientRect` de `cad-guided-tour` contra `cad-canvas`: `areaSolape / areaLienzo <= 0.02`.
  c. Área del lienzo sobre el área de la ventana: `areaLienzo / (innerWidth*innerHeight) >= 0.75` — el umbral que la cabecera del spec T6 ya prometía, ahora medido de verdad, y a 1280×720, que es el tamaño del que habla el hallazgo.
  d. La paleta sigue cerrada de fábrica: `expect(page.locator(".cad-tool-grid")).toHaveCount(0)` (hoy es cierto: `CadToolPalette.tsx:103` arranca con `localStorage.getItem("cad-tool-palette-open") === "true"`, o sea cerrada, y `.cad-tool-grid` sólo se monta dentro de `open &&`, línea 132).
  e. Que el pliegue sea reversible: pulsar `cad-guided-tour-toggle` y comprobar `data-collapsed="false"` — si no, «plegado» sería «roto».
El golden imprime los tres números (área lienzo, área solape, porcentajes) en el mensaje de fallo, para que quien lo rompa sepa cuánto se pasó. NO duplicar el golden 68 (robo de ratón con `elementFromPoint`) ni el 211 (hueco vertical con la línea de comandos): aquí se mide ÁREA, que hoy no mide nadie.

(3) `CadLienzoAncho.spec.ts` DEJA DE MENTIR (sigue siendo spec de tsx, sin navegador).
Reescribir la cabecera para que diga lo que este fichero comprueba de verdad —que el cableado del plegado por defecto EXISTE— y remitir la medición al golden 215 por número. Sustituir la aserción muerta `tour.includes("useState(true)")` por comprobaciones sobre el cableado nuevo, no sobre una cadena que el render ya no usa: que `CadGuidedTourDock.tsx` contiene `hasStoredRecord` y `{ type: "minimize", minimized: true }`, y que sigue emitiendo `data-collapsed`. Mantener las dos aserciones de la paleta (líneas 19-20), que hoy pasan. Conservar el `console.log` final: `scripts\run-specs.mjs` exige que cada spec imprima algo en stdout o lo cuenta como colgado.

TERMINADO CUANDO (todo objetivo):
- `cd D:\dev\valle-design\apps\web && npx tsx src/components/cad/editor/CadLienzoAncho.spec.ts` imprime su línea de comprobaciones y sale 0 (hoy: `AssertionError: el recorrido arranca minimizado`).
- `npm run test:specs` vuelve a verde, incluidos `guided-tour.spec.ts` y `tour-host.spec.ts` SIN tocar ninguna de sus aserciones.
- `npx playwright test e2e/golden/215-cad-recorrido-arranca-plegado.spec.ts` pasa, y los goldens 68, 163, 211 y 213 siguen pasando.
- Ningún cambio en package.json, umbrales, presupuestos ni tokens de diseño.

## Terminado cuando (medible, no «se ve mejor»)
Golden en /demo con localStorage vacío a 1280x720: `cad-guided-tour` con `data-collapsed="true"` y solape con `cad-canvas` <= 2 % del área del lienzo; la paleta sigue sin montar `.cad-tool-grid`. `npx tsx src/components/cad/editor/CadLienzoAncho.spec.ts` pasa (hoy lanza AssertionError «el recorrido arranca minimizado») y `npm run test:specs` vuelve a verde.

---
Un commit. Respeta el sistema de diseno y su gate (relleno y tinta son dos tokens; `--primary` no es relleno de boton). Comprobacion EJECUTADA antes de marcar hecho. No toques dependencias, presupuestos, umbrales, aserciones ajenas ni artefactos de evidencia.
