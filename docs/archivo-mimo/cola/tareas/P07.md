# P07 · El primer minuto ofrece .dwg por clic, no solo arrastrando

**Impacto:** cambia-la-primera-impresion   **Frente:** dwg-recien-encendido   **Esfuerzo:** minutos
**Fichero:** `D:\dev\valle-design\apps\web\src\app\dashboard\FirstMinute.tsx:157`

## Que cambia para quien mira la app
En la tarjeta del tablero vacio el `.dwg` entra si lo ARRASTRAS (el drop no mira `accept`) y no aparece si pulsas «Elegir archivo»: el mismo archivo, dos respuestas, en la pantalla que decide la venta.

## Que hacer
TÍTULO: Una sola lista de `accept` para importar: el primer minuto y el tablero dejan de divergir

POR QUÉ (corregido, sin la afirmación falsa): hay DOS listas de extensiones escritas a mano. `apps/web/src/app/dashboard/page.tsx:529-533` la calcula con `isDwgNativeImportBetaEnabled()` y ofrece `.dwg` (con beta) más `.obj,.stl,.gltf,.glb,.dae`; `apps/web/src/app/dashboard/FirstMinute.tsx:157` la tiene fija sin `.dwg` y sin las cinco de malla. Consecuencia REAL: quien llega con un DWG o una malla en su primer minuto no puede ni seleccionar su archivo en el diálogo del sistema, mientras el tablero grande sí lo admite. NO afirmar que arrastrarlo lo importa: en `state === "empty"` no hay `selectedProject` y `useImportDocument` se niega en silencio por las dos vías (`apps/web/src/app/dashboard/import-status.tsx:212`), cosa que ya afirma `apps/web/e2e/golden/150-cad-arrastrar-dxf-tablero.spec.ts:172-186`. Se arregla porque una lista duplicada volverá a divergir, no porque se vea mejor.

QUÉ HACER (un commit):
1. En `apps/web/src/lib/cad/document-import-client.ts`, exportar una función PURA con la bandera como parámetro (mismo patrón que `document-import-door.ts:54`):
   `export function documentImportAcceptAttribute(dwgBetaEnabled: boolean = isDwgNativeImportBetaEnabled()): string`
   que devuelva exactamente las dos cadenas que hoy viven en page.tsx:531-532. Sin añadir ni quitar ninguna extensión: mover, no cambiar el conjunto.
2. `page.tsx:529-533` pasa a `accept={documentImportAcceptAttribute()}`, y en la llamada a `<FirstMinute …>` (page.tsx:574) añadir `accept={documentImportAcceptAttribute()}`.
3. `FirstMinute.tsx`: nueva prop `accept: string` en la firma (líneas 32-45) y `accept={accept}` en el input (línea 157). Borrar la cadena literal. Añadir `data-testid="first-minute-import-input"` a ese input (el testid `first-minute-import` es del botón, línea 148).
4. Añadir `data-testid="dashboard-import-input"` al input de page.tsx:527 (hoy sin testid) para que el spec no dependa de la etiqueta.
Nada más: ni tokens, ni estilos, ni dependencias, ni umbrales, ni los gates de DWG/malla. Cero cambio visual.

TERMINADO CUANDO (medible, ejecutable en el CI que existe):
A. Spec de Node junto al helper (`apps/web/src/lib/cad/document-import-client.spec.ts` o el fichero de specs vecino) que, sin navegador y sin recompilar, afirme: `documentImportAcceptAttribute(true)` === `".dxf,.json,.shp,.shx,.dbf,.prj,.cpg,.dwg,.obj,.stl,.gltf,.glb,.dae"` y `documentImportAcceptAttribute(false)` === `".dxf,.json,.shp,.shx,.dbf,.prj,.cpg,.obj,.stl,.gltf,.glb,.dae"`. Ésa es la cobertura «bandera encendida y apagada» que la ficha pedía, sin los dos builds de Next.
B. UN golden nuevo de Playwright en `apps/web/e2e/golden/` que, en `/dashboard` con el estado vacío (los dos inputs coexisten: page.tsx:424-431 sólo reordena la sección con `order-3`), lea `accept` de `getByTestId("first-minute-import-input")` y de `getByTestId("dashboard-import-input")` y afirme IGUALDAD ESTRICTA entre ambos SIN fijar el valor esperado, más que ambos son no vacíos. Así pasa en cualquier build, con la bandera como esté: CI tiene un único build de web para Playwright y va con la bandera encendida (.github/workflows/ci.yml:931-934, goldens en el job de la línea 999), y exigir una segunda corrida apagada obligaría a otro job y otro build — fuera de un commit corto.
C. `npx playwright test` del golden nuevo y del 150 en verde (el 150 sigue afirmando el silencio del estado vacío: no se toca).

## Terminado cuando (medible, no «se ve mejor»)
Spec de Playwright que lee el atributo `accept` del input `first-minute-import` y del input del tablero grande y afirma que son IDENTICOS, con la bandera encendida y con la bandera apagada (dos corridas).

---
Un commit. Respeta el sistema de diseno y su gate (relleno y tinta son dos tokens; `--primary` no es relleno de boton). Comprobacion EJECUTADA antes de marcar hecho. No toques dependencias, presupuestos, umbrales, aserciones ajenas ni artefactos de evidencia.
