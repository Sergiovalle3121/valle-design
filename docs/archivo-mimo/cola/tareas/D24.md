# D24 · F5 — Las guías de /docs: 18 ocurrencias en 8 ficheros de prosa

**Gravedad:** se-ve-precario   **Esfuerzo:** una-sesion
**Fichero:** `apps/web/src/app/docs/dxf-vs-dwg/page.tsx:57`

## El problema
Las guías son prosa donde la marca aparece en frases («Valle Design importa y exporta DXF de texto» :59, «Valle Design no abre ni escribe archivos DWG» :86). No las vigila ningún gate de nombre: check:surface las exime a propósito (scripts/design/check-public-surface.mjs:26-32), así que se pueden quedar desincronizadas para siempre. Reparto: dxf-vs-dwg 5, acotacion-asociativa 4, api 4, docs/page.tsx 2, imprimir-planos-pdf-escala 1, automatizar-con-autolisp 1, docs/opengraph-image.tsx 1.

## La tarea
Objetivo: que las guías de /docs lean el nombre de producto de la configuración en vez de codificarlo, quedando alineadas con `PRODUCT_LABEL.design` (hoy «VALLE Design»). NO se introduce «VALLECAD» en ningún sitio: esa cadena no existe en el repo.

Alcance: 17 literales visibles en 7 ficheros de D:\dev\valle-design\apps\web\src\app\docs\. En cada uno, importar `import { PRODUCT_LABEL } from "@/config/brand";`

A) Texto JSX -> interpolar `{PRODUCT_LABEL.design}`:
- acotacion-asociativa/page.tsx:45, :69, :148, :149
- api/page.tsx:90
- automatizar-con-autolisp/page.tsx:47
- dxf-vs-dwg/page.tsx:59, :86 (dentro del <strong>), :163, :169
- imprimir-planos-pdf-escala/page.tsx:144
- page.tsx:107

B) Cadenas y atributos -> plantilla `${PRODUCT_LABEL.design}` (NO interpolación JSX, ahí no compila):
- dxf-vs-dwg/page.tsx:57  title={`Qué hace exactamente ${PRODUCT_LABEL.design}`}
- api/page.tsx:27 (description de `publicPageMetadata`), :34 (title=)
- page.tsx:29 (title=)
- opengraph-image.tsx:10  export const alt = `${PRODUCT_LABEL.design} · Guías de dibujo técnico`;

NO TOCAR (y decirlo en el commit):
- api/page.tsx:11 y api/console-contract.spec.ts:68 — son COMENTARIOS; check:surface ya los descarta con stripComments y no los ve ningún usuario.
- api/operations.generated.json:5 — es GENERADO por scripts/cad/build-api-console.mjs desde packages/contracts/specs/design-api.v1.yaml:3. Editarlo a mano hace fallar console-contract.spec.ts. Si el título de la API debe cambiar, va en el YAML + regenerar, en una tarea aparte.

TERMINADO CUANDO:
1. `git grep -n 'Valle Design' -- apps/web/src/app/docs` devuelve EXACTAMENTE 3 líneas: api/page.tsx:11, api/console-contract.spec.ts:68 y api/operations.generated.json:5 (las tres excluidas arriba). No 0.
2. `npm run lint` (raíz) en verde.
3. `npm run test:specs` ejecutado DESDE apps/web (o `npm run test -w apps/web`) en verde — en particular seo-surface.spec.ts, que acota longitudes de title/description; «VALLE Design» mide lo mismo que «Valle Design» (12 caracteres), así que los límites no se mueven.
4. `npm run check:surface` sigue en verde (docs no entra en sus globs; sirve de control de que no se rompió nada).

Fuera de alcance, pero anotar como seguimiento: el mismo patrón existe en superficie que el gate SÍ barre —lib/marketing/faq.ts:227,247 y lib/seo/social-card.tsx:92— y allí tampoco lo detecta nadie, porque FORBIDDEN sólo mira /autocad|autodesk/. Si de verdad se quiere impedir la deriva, la tarea que la cierra es un gate de marca nuevo (prohibir el literal del nombre de producto fuera de config/brand.ts, TrademarkNotice y comentarios), no arreglar 17 sitios a mano.

## Terminado cuando
`git grep -c 'Valle Design' apps/web/src/app/docs` devuelve 0 y `npm run lint && npm run test:specs` en verde.

---
No toques dependencias, presupuestos, umbrales ni aserciones. Un commit por tarea.
Si al hacerla descubres que el hallazgo es falso, ANOTALO en la bitacora y pasa a la siguiente:
no inventes un arreglo para un problema que no existe.
