# P01 · Casa habitación en volumen: 9 muros extruidos, 5 vanos restados, losa y cubierta

**Impacto:** cambia-la-primera-impresion   **Frente:** demo-con-solidos   **Esfuerzo:** una-sesion
**Fichero:** `D:\dev\valle-design\apps\web\src\lib\cad\demo\demo-volume.ts (NUEVO) + D:\dev\valle-design\apps\web\src\components\cad\document-lifecycle\demo-port.ts:58`

## Que cambia para quien mira la app
Al pulsar 3D se ve un edificio con espesor de muro y huecos de puerta y ventana en vez de una planta tumbada, y el panel derecho pasa de confesar «Polilínea 1, Texto 1» a listar «Muro 1 · Muros».

## Que hacer
TÍTULO: Casa habitación en volumen: 9 muros extruidos, 5 vanos restados, losa y cubierta derivadas

POR QUÉ: Al pulsar 3D se ve un edificio con espesor de muro y huecos de puerta y ventana en vez de una planta tumbada, y el panel derecho pasa de «Polilínea 1, Texto 1» a listar «Muro 1 · MURO».

DÓNDE: D:\dev\valle-design\apps\web\src\lib\cad\demo\demo-volume.ts (NUEVO) + D:\dev\valle-design\apps\web\src\components\cad\document-lifecycle\demo-port.ts:58 (`buildDemoDocument`).

NO TOCAR: `templates.ts` (4982 líneas = su asignación exacta en scripts/cad/monolith-budget.json:38), `template-document.ts` (dispara los 149 hashes de `check:template-gallery`), `bim-schedule.ts`, `room-solid-host.ts`, ni ninguna aserción, umbral o presupuesto existente.

QUÉ HACER
Módulo nuevo `demo-volume.ts` junto a `demo-constants.ts`, con UNA función que recibe el `CadDocument` de `buildCadTemplateDocument('casa-habitacion')` y devuelve otro. `buildDemoDocument` la llama y nada más (sigue poniendo el cajetín de «Demostración»).

QUITA (11 entidades, ids verificados en el constructor): `tpl-shell`, `tpl-sala`, `tpl-comedor`, `tpl-cocina`, `tpl-recamara-1`, `tpl-recamara-2`, `tpl-bano`, `tpl-puerta-principal-jamba-a`, `tpl-puerta-principal-jamba-b`, `tpl-puerta-principal-hoja`, `tpl-puerta-principal-abatimiento`.

CONSERVA (11, con sus ids): `tpl-cama`, `tpl-sofa`, `tpl-mesa`, `tpl-estufa`, `tpl-lb-sala`, `tpl-lb-comedor`, `tpl-lb-cocina`, `tpl-lb-recamara-1`, `tpl-lb-recamara-2`, `tpl-lb-bano`, `tpl-nt-titulo`.

AÑADE 9 entidades `wall`, layer `MURO`, `start`/`end` como `CadPoint3` con `z: 0`, `thickness: defaultWallThickness(document.meta.unit)` y `height: defaultWallHeight(document.meta.unit)` (`engine/commands/draw-wall.ts:49-56`; con `meta.unit === "mm"` dan 200 y 2400 — comprobado). Perímetro en lazo cerrado (`material: 'brick'`): sur (700,700)→(11300,700); este (11300,700)→(11300,7300); norte (11300,7300)→(700,7300); oeste (700,7300)→(700,700). Particiones (`material: 'drywall'`): horizontal (700,4400)→(11300,4400); verticales (5000,700)→(5000,4400), (8200,700)→(8200,4400), (4800,4400)→(4800,7300), (7800,4400)→(7800,7300). Materiales de `wall-materials.ts:46-52`.

AÑADE 5 entidades `opening`, layer `VANO`, cada una con TODOS los campos que exige `cad-entities-v7.ts` —`kind`, `hostId` (el id del muro anfitrión), `position` (medida sobre el eje desde `start`, al CENTRO), `width`, `height`, `sill`, `swing` y `hinge` (los dos son obligatorios en el tipo aunque una ventana no los dibuje)—:
· puerta principal → hostId = muro sur, kind 'door', position 2250, 900x2100, sill 0, swing 'left', hinge 'start' (cae en x=2950, el centro de la puerta que tenía la plantilla).
· ventana este → muro este, 'window', position 2100, 1200x1200, sill 900.
· ventana norte → muro norte, 'window', position 8300, 1500x1200, sill 900.
· ventana oeste → muro oeste, 'window', position 4500, 1200x1200, sill 900.
· puerta de recámara → partición horizontal, 'door', position 2000, 900x2100, sill 0.

ORDEN (obligatorio, no cosmético): `entities` = [9 muros, 5 vanos, 11 conservadas] y `modelSpace.entityIds` en EL MISMO orden. `CadNativeEntityList` pinta solo las primeras 20 (`CadNativeEntityList.tsx:40-56`) y toma el orden de `document.entities` (`Layout3DEditor.tsx:3828-3833`): con los muros al final, «Muro 1» no se vería. `history` queda en una sola entrada: «Demostración: casa habitación en volumen».

Piso, cielorraso y cubierta NO se escriben: los deriva `CadArchitecturalMassHost` del anillo de muros (`room-solid-host.ts:118-130`).

TERMINADO CUANDO
1) Spec de Node `apps/web/src/lib/cad/demo/demo-volume.spec.ts` (estilo script: `node:assert/strict` + un `console.log` final, contrato de `apps/web/scripts/run-specs.mjs`), con los valores MEDIDOS en su cabecera:
   · exactamente 9 entidades `wall`, 5 `opening` y las 11 conservadas → 25 en total, y `modelSpace.entityIds` idéntico en contenido y ORDEN a `entities.map(id)`; `history.length === 1`.
   · `detectCadRooms(muros).exteriorRing !== null` y con 10 puntos (medido).
   · dos llamadas al constructor producen el MISMO JSON.
   · sobre `new CadNativeMassHosts(() => ({scale:0.01,width:12000,height:8000}))` sincronizado con el documento: `getSnapshot()` da `meshCount === 12` y `vertexCount === 690`, e `invalidGeometry()` devuelve `wallIds`, `massKinds` y `openingCuts` vacíos (los tres, medidos).
   · PRUEBA DE QUE LOS HUECOS SON HUECOS: el mismo documento sin los 5 `opening` da `vertexCount === 360`; con ellos 690. Un vano decorativo no cambiaría el número.
   · NO asertar `detectCadRooms(...).problems` vacío: esta planta produce 4 avisos «lados paralelos consecutivos» y `builtArea: null` por el partido en T de las particiones; es comportamiento vigente del algoritmo y queda fuera del alcance.
   (Esta spec vive en `lib/` y puede importar `components/cad/viewport/native-mass-hosts`: `scripts/check-import-direction.mjs` exime los `.spec.ts` por regla escrita.)

2) Spec nueva `apps/web/e2e/public/demo-volumen.spec.ts`: con `localStorage` limpio, `page.goto('/demo?cadDiag=1')` —el `?cadDiag=1` es imprescindible: `cad-3d-solid-diagnostics` solo se monta con esa bandera (`CadDiagnosticsReadout.tsx:66-77`, `CadS

[...recortado; el fichero citado arriba es la fuente]

## Terminado cuando (medible, no «se ve mejor»)
Spec nueva `apps/web/e2e/public/demo-volumen.spec.ts`: con localStorage limpio, `/demo` deja `cad-3d-solid-diagnostics` con `data-mesh-count` ≥ 12 (hoy vale 0) y `data-vertex-count` ≤ 6000 —el valor medido se escribe en la cabecera de la spec—, `cad-native-entity-list` contiene «Muro 1» y el botón `getByTitle('Vista 3D (órbita libre)')` no cambia el conteo. Más una spec de Node sobre el constructor: exactamente 9 entidades `wall` y 5 `opening`, `detectCadRooms(walls).exteriorRing !== null`, `nativeMassHosts.invalidGeometry()` vacío para ese documento y dos llamadas al constructor producen el MISMO JSON. `apps/web/e2e/public/demo-studio.spec.ts` sigue en verde sin tocarlo (su aserción es «≥ 5 entidades», que se cumple).

---
Un commit. Respeta el sistema de diseno y su gate (relleno y tinta son dos tokens; `--primary` no es relleno de boton). Comprobacion EJECUTADA antes de marcar hecho. No toques dependencias, presupuestos, umbrales, aserciones ajenas ni artefactos de evidencia.
