# D12 · El panel «PRIMEROS CINCO MINUTOS» se come 480x346 px del dibujo y crece hacia arriba desde la línea de comandos

**Gravedad:** se-ve-precario   **Esfuerzo:** minutos
**Fichero:** `apps/web/src/components/cad/onboarding/CadGuidedTourDock.tsx:171`

## El problema
El acompañante se monta dentro del envoltorio de la línea de comandos (CadCommandLineDock.tsx:48), que es `absolute bottom-3 left-3 z-30 w-[min(30rem,42vw)]` sobre el lienzo. Con `max-h-[32vh]` son 346 px a 1080 y 288 px a 900, por 480 px de ancho: 166.000 px² de dibujo tapados en la esquina donde un dibujante empieza a encuadrar. Sí es descartable —«Saltar» (línea 241) persiste en `localStorage` vía `cadTourHost` (tour-host.ts:34-37, clave `valle:cad:tour:v1:<userId>`)— pero el PLIEGUE no: `const [minimized, setMinimized] = useState(false)` (línea 84) y el propio comentario declara que no se guarda a propósito. Y como es bottom-anchored y crece hacia arriba, la banda muerta cae cada vez a una altura distinta del plano. Debería ser lateral: el muelle izquierdo ya tiene pestañas («Puntos heredados» / «Biblioteca», CadLeftDockPanel.tsx:152-166) y ahí no tapa nada.

## La tarea
Persistir el pliegue del acompañante en el registro del recorrido, tocando TRES ficheros:

1. apps/web/src/lib/cad/onboarding/guided-tour.ts
   - Añadir `minimized: boolean` a `CadTourRecord` (junto a `acknowledged`/`plotted`), con `minimized: false` en `EMPTY_CAD_TOUR_RECORD`.
   - Añadir la acción `{ type: "minimize"; minimized: boolean }` a `CadTourAction` y su rama en `cadGuidedTourReduce`, devolviendo el MISMO objeto cuando el valor no cambia (`record.minimized === action.minimized ? record : { ...record, minimized: action.minimized }`), igual que hacen `acknowledge` y `plot`, para que el almacén que compara por identidad no publique un cambio inexistente. No tocar el cortocircuito de completed/skipped (líneas 317-321): con el recorrido cerrado el panel no se pinta y el no-op es correcto.
   - Añadir `minimized: parsed.minimized === true` a `parseCadTourRecord`, que tolera basura por contrato.

2. apps/web/src/components/cad/onboarding/tour-host.ts
   - OBLIGATORIO: añadir `a.minimized === b.minimized` a `sameRecord()` (líneas 97-105). Sin esto, `attach()` descarta el registro cargado cuando la única diferencia es el pliegue y la persistencia no se nota al recargar.

3. apps/web/src/components/cad/onboarding/CadGuidedTourDock.tsx
   - Sustituir el `useState(false)` de la línea 86 por `const minimized = record.minimized;` (el registro ya llega por `useSyncExternalStore` en las líneas 61-65) y el `onClick` de la línea 222 por `cadTourHost.dispatch({ type: "minimize", minimized: !minimized })`.
   - Reescribir el comentario de 75-85, que hoy afirma lo contrario («No se guarda en localStorage a propósito»), explicando por qué ahora sí se guarda.

Pruebas:
   - Unidad: en guided-tour.spec.ts, que `cadGuidedTourReduce` conmute `minimized` y devuelva la misma referencia cuando no cambia, y que `parseCadTourRecord` lo lea; en tour-host.spec.ts, que tras `dispatch({type:"minimize",minimized:true})` + `reset()` + `attach(mismo userId)` el snapshot vuelva con `minimized === true` (esta es la que caza el fallo de `sameRecord`).
   - Golden: AMPLIAR apps/web/e2e/golden/211-cad-recorrido-no-tapa-comandos.spec.ts, que ya abre el estudio sin saltar el recorrido, en vez de crear uno nuevo. Tras el `toggle.click()` que ya existe (línea 102), hacer `page.reload()`, esperar `cad-guided-tour` visible y afirmar: `[data-testid="cad-guided-tour"]` con `data-collapsed="true"`, el toggle con `aria-expanded="false"`, `cad-guided-tour-progress` oculto, y la altura con el criterio RELATIVO del propio fichero (`plegadoTrasRecarga.height < alturaAbierta * 0.5`), no un umbral absoluto de 60 px.

No mover el panel al muelle izquierdo en esta tarea: la reubicación (que es lo que de verdad libera los ~166.000 px² del lienzo) es un cambio aparte, y esta sólo hace que el pliegue sobreviva.

## Terminado cuando
Un golden abre el estudio, pulsa `cad-guided-tour-toggle`, recarga la página y afirma `[data-testid="cad-guided-tour"][data-collapsed="true"]` y que su caja mide menos de 60 px de alto.

---
No toques dependencias, presupuestos, umbrales ni aserciones. Un commit por tarea.
Si al hacerla descubres que el hallazgo es falso, ANOTALO en la bitacora y pasa a la siguiente:
no inventes un arreglo para un problema que no existe.
