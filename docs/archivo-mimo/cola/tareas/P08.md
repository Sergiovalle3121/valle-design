# P08 · El mobiliario del demo como SOLID3D: extrude y un SUBTRACT de verdad

**Impacto:** demuestra-capacidad   **Frente:** demo-con-solidos   **Esfuerzo:** una-sesion
**Fichero:** `D:\dev\valle-design\apps\web\src\lib\cad\demo\demo-volume.ts`

## Que cambia para quien mira la app
Ninguna de las 149 plantillas emite un solo `solid3d`, así que el documento que el visitante abre no ejecuta ni una vez el kernel B-rep de los 23 comandos de sólidos; con cuatro muebles extruidos y un hueco restado, el demo corre EXTRUDE y SUBTRACT sobre su propio dibujo y MASSPROP puede decir su volumen.

## Que hacer
TÍTULO: Los 4 muebles de la demo como SOLID3D, con un SUBTRACT de verdad en la estufa

DÓNDE (corregido — el fichero de la propuesta no existe):
- NUEVO: `D:\dev\valle-design\apps\web\src\lib\cad\demo\demo-solids.ts`
- NUEVO: `D:\dev\valle-design\apps\web\src\lib\cad\demo\demo-solids.spec.ts`
- EDITAR (3 líneas): `D:\dev\valle-design\apps\web\src\components\cad\document-lifecycle\demo-port.ts` — `buildDemoDocument()` (línea 58).
- PROHIBIDO tocar `apps/web/src/lib/cad/template-document.ts`: su `assetEntities` sirve a las 149 plantillas y la rama `furniture` cae en el `default` compartido (`template-document.ts:227`). Cambiarla ahí convierte el catálogo entero y deja de ser atómico.

QUÉ HACER

1) `demo-solids.ts` exporta UNA función pura, `withDemoFurnitureSolids(document: CadDocument): CadDocument`, que no importa nada de `components/` (`check:conventions` vigila la dirección) y sólo depende de `cad-entities-v5`, `cad-document` y `solid3d-build`. No la importa `demo-constants.ts`: ese módulo debe seguir siendo hoja sin dependencias para no viajar al primer pintado.

2) Tabla de alturas, en mm, indexada por `ref` de la plantilla:
   `cama: 550`, `sofa: 800`, `mesa: 750`, `estufa: 900`.

3) Para cada `ref` de esa tabla, localizar en `document.entities` la polilínea cuyo `id` es `tpl-${ref}` (el esquema de ids de `template-document.ts:319`; hoy es una polilínea cerrada de 4 vértices, única entidad del mueble). Derivar el perfil de SUS PROPIOS VÉRTICES — `profile.outer = vertices.map(({x, y}) => ({x, y}))` — en vez de copiar números de `templates.ts`. Así el sólido sigue cuadrado con el dibujo si alguna vez cambia el tamaño base o la rejilla de instanciación, y el id sale del `ref` sin inventarlo. Si el id no aparece, la función devuelve el documento intacto (nunca lanza en producción; la spec sí exige que los cuatro estén).

4) Sustituir esa polilínea, en su misma posición del array `entities` y en `modelSpace.entityIds`, por:
   ```
   { id: `tpl-${ref}`, type: "solid3d", layer: "MOBILIARIO",
     name: <el label del asset: "Cama matrimonial" | "Sofá 3 plazas" | "Mesa comedor 4" | "Estufa">,
     nodes: [{ id: `tpl-${ref}-base`, op: "extrude", profile: { outer }, height }],
     root: `tpl-${ref}-base` }
   ```
   Sin `placement`: el perfil ya va en coordenadas de dibujo. Reutilizar el id del mueble mantiene el determinismo y evita ids huérfanos en `modelSpace`.

5) LA BOOLEANA — sólo la estufa (base 760×600 en 8450,1500; alto 900). Tres nodos y `root` en el subtract:
   ```
   nodes: [
     { id: "tpl-estufa-base",  op: "extrude", profile: { outer }, height: 900 },
     { id: "tpl-estufa-hueco", op: "box",
       min: { x: 8530, y: 1600, z: 850 },
       max: { x: 9130, y: 2000, z: 1000 } },
     { id: "tpl-estufa-solido", op: "subtract",
       operands: ["tpl-estufa-base", "tpl-estufa-hueco"] },
   ],
   root: "tpl-estufa-solido"
   ```
   La hornilla es 600×400 CENTRADA en planta (8450+80 = 8530; 1500+100 = 1600) y el `max.z` sube a 1000, por encima de la tapa en z=900, a propósito: así el corte no comparte cara coplanar con la tapa y sale una cavidad abierta, no un vacío interno invisible. El volumen restado es sólo la parte intersecada, 600×400×50.
   Igual que el orden que documenta el esquema: `operands[0]` menos los demás (`cad-entities-v5.ts:197-198`).

6) En `demo-port.ts`, envolver lo que ya devuelve: `buildDemoDocument()` pasa el documento por `withDemoFurnitureSolids(...)` antes de reescribir el cajetín. Nada más.

TERMINADO CUANDO — `apps/web/src/lib/cad/demo/demo-solids.spec.ts` pasa con `npm run test:specs` (desde `apps/web`), sobre el documento que devuelve `buildDemoDocument()`:

A. Hay exactamente 4 entidades `type === "solid3d"`, con ids `tpl-cama`, `tpl-sofa`, `tpl-mesa`, `tpl-estufa`, las 4 en `layer === "MOBILIARIO"`, las 4 con `name` no vacío, y ninguna con `placement`.
B. Ya NO existe ninguna polilínea con esos ids, y cada id sigue presente en `modelSpace.entityIds` (una vez, sin huérfanos).
C. `"MOBILIARIO"` figura en `document.layers` (lo declara `planta-arquitectonica`, `starter-templates.ts:159`) — la aserción impide que un cambio de starter deje los sólidos en una capa que el gate de visibilidad no conoce.
D. Para cada una, `solid3dBody(entity)` no lanza, y `solid3dMassProperties(entity).volume` queda a menos del 1 % del volumen analítico:
   cama 1 400·2 000·550 = 1 540 000 000 mm³; sofá 2 100·900·800 = 1 512 000 000; mesa 1 200·800·750 = 720 000 000; estufa 760·600·900 − 600·400·50 = 410 400 000 − 12 000 000 = 398 400 000.
E. Al menos un árbol tiene un nodo con `op === "subtract"` y su `root` ES ese nodo; y el hueco muerde de verdad: el volumen de la estufa es estrictamente MENOR que 760·600·900 (si el subtract se cayera en silencio, D podría pasar por poco y esta aserción no).
F. COSTE DEL KERNEL: teselar el documento completo (muros + masas + los 4 sólidos) 3 veces, llamando a `clearSolidCache()` antes de CADA repetición — sin eso la memoización de `solid3d-build.ts:709-719` mediría caché, no kernel. La MEDIANA de las 3 queda bajo un techo declarado como constante con nombre en la cabecera de la spec, con su comentario de por qué ese número; la spec imprime las 3 medida

[...recortado; el fichero citado arriba es la fuente]

## Terminado cuando (medible, no «se ve mejor»)
Spec de Node sobre el constructor: ≥4 entidades `solid3d`; para cada una `solid3dBody()` no lanza y `solid3dMassProperties().volume` cae a menos del 1 % del volumen analítico esperado; al menos un árbol contiene un nodo con `op === 'subtract'` y su `root` es ese nodo; y el tiempo de teselar el documento completo (muros + masas + sólidos) medido 3 veces queda bajo un techo declarado en la cabecera de la spec, que es la primera cifra que existirá sobre el coste del kernel en el producto.

---
Un commit. Respeta el sistema de diseno y su gate (relleno y tinta son dos tokens; `--primary` no es relleno de boton). Comprobacion EJECUTADA antes de marcar hecho. No toques dependencias, presupuestos, umbrales, aserciones ajenas ni artefactos de evidencia.
