# P06 · Decir que versiones de DWG entran ANTES de abrir el selector

**Impacto:** cambia-la-primera-impresion   **Frente:** dwg-recien-encendido   **Esfuerzo:** una-sesion
**Fichero:** `D:\dev\valle-design\apps\web\src\app\dashboard\page.tsx:519-536`

## Que cambia para quien mira la app
El usuario ve `.dwg` en el dialogo sin una sola palabra de beta ni de version, elige su archivo de AutoCAD 2024 (AC1032, el guardado por defecto) y se come un rechazo: la primera prueba del activo mas fuerte del producto termina en error.

## Que hacer
TÍTULO: Decir qué versiones de DWG entran ANTES de abrir el selector, derivado de las banderas

DÓNDE: D:\dev\valle-design\apps\web\src\app\dashboard\page.tsx:524-535 (el `<label>` «Importar como documento» y su input `sr-only`), D:\dev\valle-design\apps\web\src\lib\cad\dwg-interop-flag.ts, D:\dev\valle-design\apps\web\src\lib\cad\dwg-native-reader.ts

QUÉ HACER:
1. En `dwg-interop-flag.ts` (fichero SIN imports, seguro para el bundle del cliente) exportar la fuente única:
   - `export const DWG_BASE_VERSION_CODES = ["AC1015"] as const;`
   - `export const DWG_AC1018_VERSION_CODES = ["AC1018"] as const;`
   - `export const DWG_MODERN_VERSION_CODES = ["AC1024", "AC1027", "AC1032"] as const;`
   - `export function dwgAcceptedVersionCodes(opts: { allowAc1018: boolean; allowModern: boolean }): readonly string[]`
   - `export function describeDwgAcceptedVersions(codes: readonly string[]): string` — moviendo aquí el cuerpo de `describeAcceptedVersions` con sus rótulos actuales («AutoCAD 2000 (AC1015)», «2004 (AC1018)», «2010 (AC1024)», «2013 (AC1027)», «2018 (AC1032)»).
2. En `dwg-native-reader.ts`: BORRAR `MODERN_VERSION_CODES` y `describeAcceptedVersions` locales; `readDwgNeutralDatabase` construye `acceptedVersions` con `dwgAcceptedVersionCodes(...)` y compone el mensaje de rechazo con `describeDwgAcceptedVersions(...)`. Cero literales de versión sueltos en el lector (verificable por grep de `AC10` en el fichero: sólo quedan los del import).
3. En `page.tsx`, inmediatamente DESPUÉS del `</label>` del input de importación, cuando `isDwgNativeImportBetaEnabled()` sea true, pintar un `<p data-testid="dashboard-dwg-beta-nota">` cuyo texto se construya en runtime:
   `` `.dwg en beta: ${describeDwgAcceptedVersions(dwgAcceptedVersionCodes({ allowAc1018: isDwgAc1018ImportBetaEnabled(), allowModern: false }))}. Si tu CAD es más nuevo, Guardar como con esa versión, o exporta a DXF.` ``
   `allowModern: false` literal y comentado: `DWG_MODERN_BETA_AUTHORIZATION.ownerSigned` es `false` y `dwgModernBetaImportIsEnabled` cierra la conjunción ahí, así que la familia moderna NO se nombra como admitida aunque su variable esté encendida. PROHIBIDO importar de `dwg-native-reader.ts` en `page.tsx` (arrastra el códec y hay presupuesto en `scripts/perf/bundle-budget.json:31`); todo llega de `dwg-interop-flag.ts` y `document-import-client.ts`.
4. Sistema de diseño: dos tokens, relleno y tinta — p. ej. `bg-warning/10` + `text-warning-ink` (el patrón que ya usa `dashboard-read-only` en la misma página). Nunca `--primary` como fondo.

TERMINADO CUANDO:
A. Spec de Playwright, tres builds, midiendo contra el `<label>` VISIBLE que contiene «Importar como documento» (no contra el input, que es `sr-only` con caja de 1×1):
   - Con `NEXT_PUBLIC_DWG_NATIVE_IMPORT_BETA=true` y `NEXT_PUBLIC_DWG_AC1018_IMPORT_BETA` sin definir: `dashboard-dwg-beta-nota` es visible, su `getBoundingClientRect().top` está a menos de 80 px del `bottom` del label, su texto contiene «2000» y NO contiene «2004».
   - Con ambas en `true`: el texto contiene «2000» y «2004».
   - Con `NEXT_PUBLIC_DWG_NATIVE_IMPORT_BETA` apagada: `locator('[data-testid="dashboard-dwg-beta-nota"]')` tiene `count() === 0`.
B. Aserción de Node: para cada combinación de `{allowAc1018, allowModern}`, el conjunto que `readDwgNeutralDatabase` usa para aceptar/rechazar (probado con bytes de firma AC1015/AC1018/AC1032, como ya hace `dwg-native-reader-modern.spec.ts:96-99`) coincide exactamente con `dwgAcceptedVersionCodes(...)`, y el rótulo del mensaje de rechazo es idéntico a `describeDwgAcceptedVersions(...)`. Más un grep-test de que `page.tsx` no importa nada de `dwg-native-reader`.
C. Pasan los gates de presupuesto de `/dashboard` (`scripts/perf/bundle-budget.json`, `scripts/lint-budget.json`) sin tocar sus umbrales.

## Terminado cuando (medible, no «se ve mejor»)
Spec de Playwright con NEXT_PUBLIC_DWG_NATIVE_IMPORT_BETA=true que afirma: el nodo es visible, su `getBoundingClientRect().top` esta a menos de 80 px del input de importacion, su texto contiene «2000» y «2004», y con la bandera apagada el nodo no existe. Ademas una asercion de Node de que la cadena de versiones proviene de la misma constante que usa `readDwgNeutralDatabase`.

---
Un commit. Respeta el sistema de diseno y su gate (relleno y tinta son dos tokens; `--primary` no es relleno de boton). Comprobacion EJECUTADA antes de marcar hecho. No toques dependencias, presupuestos, umbrales, aserciones ajenas ni artefactos de evidencia.
