# D19 · /terms enseña una ruta cruda «/sla» como texto de enlace

**Gravedad:** se-ve-precario   **Esfuerzo:** minutos
**Fichero:** `apps/web/src/app/terms/page.tsx:69`

## El problema
«Los niveles de servicio informativos están en /sla» — el texto visible del enlace es la ruta, no un nombre. Es el detalle de una web escrita por ingeniería para ingeniería, en la página que un comprador lee antes de decidir si el negocio existe.

## La tarea
En apps/web/src/app/terms/page.tsx:68-69, reescribir la frase para que el enlace tenga nombre y la oración no lo duplique, CONSERVANDO `href="/sla"`:

  de:  Los niveles de servicio informativos están en <Link href="/sla">/sla</Link>, con la misma advertencia...
  a:   Los compromisos de disponibilidad informativos están en <Link href="/sla">Niveles de servicio</Link>, con la misma advertencia...

(«Niveles de servicio» es el título canónico de la página destino: sla/page.tsx metadata y SlaPage.tsx:118.)

TERMINADO CUANDO: una aserción POSITIVA, en el estilo de los specs existentes (lectura del fuente con readFileSync), comprueba que apps/web/src/app/terms/page.tsx sigue conteniendo `href="/sla"` Y que el contenido de ese Link no es una ruta — es decir, coincide con `>Niveles de servicio</Link>` y NO coincide con `>/sla</Link>`. Añadirla junto a la aserción ya existente de apps/web/src/app/sla/sla-surface.spec.ts, que debe seguir en verde. NO escribir un spec que prohíba la subcadena «/sla» en la página.

## Terminado cuando
Un spec que afirma que el texto renderizado de /terms no contiene la subcadena «/sla».

---
No toques dependencias, presupuestos, umbrales ni aserciones. Un commit por tarea.
Si al hacerla descubres que el hallazgo es falso, ANOTALO en la bitacora y pasa a la siguiente:
no inventes un arreglo para un problema que no existe.
