# D25 · F7 — Las 149 plantillas: una línea cambia 298 hashes (y ningún docHash)

**Gravedad:** se-ve-precario   **Esfuerzo:** minutos
**Fichero:** `apps/web/src/lib/cad/template-render.ts:153`

## El problema
Las 149 plantillas NO son ficheros SVG en disco: se dibujan bajo demanda por renderCadTemplateSvg. La marca está en UNA línea: el cajetín escribe «VALLE DESIGN · plantilla del catálogo». docs/cad/evidence/template-gallery.json guarda por plantilla docHash, svgOscuro y svgClaro, y check:template-gallery (apps/web/scripts/template-gallery-evidence.mts:60-83) falla si el fichero committeado no coincide byte a byte con lo que produce el motor. VERIFICADO: docHash hashea `built.document` y el texto sólo entra en el render, así que cambian los 298 hashes de SVG (149 oscuros + 149 claros) y NINGÚN docHash. El mensaje de error listará las 149 como «cambiaron de dibujo»: es esperado, no una alarma.

## La tarea
BLOQUEO PREVIO (resolver con el dueño antes de tocar código): «VALLECAD» no es un nombre que exista en este repositorio y contradice la marca configurada (`brandName: "VALLE"`, `productNames.design: "VALLE Design"`, packages/contracts/src/brand.ts:80-88). No inventar el literal. Confirmar el nombre destino antes de continuar.

COMMIT 1 — atómico, SÓLO dos ficheros (es el único que toca hashes):
1. apps/web/src/lib/cad/template-render.ts:153 — dejar de quemar el nombre a mano. Importar `DEFAULT_BRAND_MANIFEST` de `@valle-design/contracts` (ya re-exportado en packages/contracts/src/index.ts:9) y componer el cajetín con `DEFAULT_BRAND_MANIFEST.productNames.design`. Es legal: lib/cad ya importa contracts (p. ej. apps/web/src/lib/cad/dxf-export.ts:9) y scripts/check-import-direction.mjs sólo prohíbe lib/ → components/|app/.
   CRÍTICO: usar `DEFAULT_BRAND_MANIFEST`, NUNCA el `BRAND` resuelto por entorno de apps/web/src/config/brand.ts. `.github/workflows/release.yml:169` inyecta `NEXT_PUBLIC_BRAND_NAME=${{ vars.RELEASE_BRAND_NAME }}`; un cajetín dependiente del entorno volvería el hash no determinista y pondría `check:template-gallery` en rojo según dónde corra. Si el rebranding se quiere de verdad, se aplica cambiando el valor por defecto del manifiesto en packages/contracts/src/brand.ts, no el render.
2. Regenerar EN EL MISMO COMMIT: `npm run evidence:template-gallery` y commitear docs/cad/evidence/template-gallery.json.
TERMINADO CUANDO: `npm run check:template-gallery` imprime «Manifiesto de galería OK: 149 plantillas, 3480 entidades, hashes al día» Y `git diff --numstat docs/cad/evidence/template-gallery.json` del commit da exactamente `298 298`, con `git diff --unified=0 | grep '^+' | grep -c docHash` = 0.

COMMIT 2 — aparte, porque no mueve ningún hash, y TODO junto o nada (si no, /plantillas/[id] queda con dos nombres a la vez):
- apps/web/src/lib/marketing/template-gallery.ts:145 («Ábrela en Valle Design…»)
- apps/web/src/components/gallery/PlanRender.tsx:35 (el alt; cuidar la prosa: es «plantilla CAD de Valle Design», no un token suelto)
- apps/web/src/app/plantillas/[id]/page.tsx:123 («Dibujado por el motor de Valle Design a escala 1:»)
- apps/web/src/app/plantillas/[id]/opengraph-image.tsx:9 (`alt = "Plantilla de plano CAD de Valle Design"`)
- apps/web/src/components/cad/dialogs/CadStudioDialogs.tsx:108 (`<strong>VALLE DESIGN</strong>`)
Verificar con `npm run check:template-gallery` (debe seguir verde y con 0 líneas de diff en el JSON, probando que este commit no toca el motor) y con la spec apps/web/src/lib/marketing/template-gallery.spec.ts:29, que exige que la descripción SEO siga conteniendo «editables».

## Terminado cuando
`npm run check:template-gallery` imprime «Manifiesto de galería OK: 149 plantillas, 3480 entidades, hashes al día», y `git diff` del JSON muestra 298 hashes cambiados y 0 docHash cambiados.

---
No toques dependencias, presupuestos, umbrales ni aserciones. Un commit por tarea.
Si al hacerla descubres que el hallazgo es falso, ANOTALO en la bitacora y pasa a la siguiente:
no inventes un arreglo para un problema que no existe.
