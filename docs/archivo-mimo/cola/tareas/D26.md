# D26 · F8 — La API: correos, CFDI y el emisor TOTP

**Gravedad:** se-ve-precario   **Esfuerzo:** una-sesion
**Fichero:** `apps/api/src/modules/outbox-receiver/email-templates.ts:38`

## El problema
La API no lee el manifiesto de marca (sólo importa @valle-design/contracts para permisos y límites): tiene el nombre a mano en email-templates.ts:38 (`const PRODUCT_NAME = 'Valle Design'`, que forma los asuntos de TODOS los correos), cfdi-issuance.service.ts:173,174 (el concepto impreso en la factura fiscal), identity.controller.ts:57 (`IDENTITY_MFA_ISSUER ?? 'Valle Design'`, el nombre que aparece en Google Authenticator), organizations.controller.ts:200 y main.ts:226 (log de arranque). Seis specs afirman los literales viejos (email-templates.spec.ts:21,43,81; resend-email.sender.spec.ts:11,17; outbox-receiver.circuit.pg.spec.ts:204; facturama-cfdi.provider.spec.ts:59,145; identity-mfa.spec.ts:144; email-sender.config.spec.ts:10,27), así que cambio y specs van en el mismo commit o CI se pone rojo. El issuer TOTP es el único con coste real —renombra la entrada de quien ya se enroló— y hoy no hay usuarios.

## La tarea
Hacer que la API lea la marca del manifiesto que ya existe (packages/contracts/src/brand.ts), en vez de crear una segunda fuente de verdad en el módulo de correo.

1) En apps/api/src, resolver el manifiesto UNA vez y exponer el nombre visible del producto. Crear apps/api/src/common/brand/product-brand.ts:

   import { resolveBrandManifest, productDisplayName } from '@valle-design/contracts';
   export const BRAND = resolveBrandManifest(process.env);
   export const PRODUCT_DISPLAY_NAME = productDisplayName(BRAND, 'design'); // "VALLE Design"

   (@valle-design/contracts ya es dependencia de valle-design-api y resolveBrandManifest es puro: mismo env ⇒ mismo resultado.)

2) Sustituir los 6 literales 'Valle Design' por PRODUCT_DISPLAY_NAME en 5 ficheros:
   - modules/outbox-receiver/email-templates.ts:38 → `const PRODUCT_NAME = PRODUCT_DISPLAY_NAME;` (se conserva la constante local, ya usada en ~18 sitios del fichero; sólo cambia su origen; NO exportarla).
   - modules/commercial/cfdi-issuance.service.ts:173,174 → `Suscripción ${PRODUCT_DISPLAY_NAME} (${invoice.number})` y `` `Suscripción ${PRODUCT_DISPLAY_NAME}` ``.
   - modules/identity/identity.controller.ts:57 → `process.env.IDENTITY_MFA_ISSUER?.trim() || PRODUCT_DISPLAY_NAME` (se mantiene el .slice(0, 48) y el override por entorno).
   - modules/organizations/organizations.controller.ts:200 → `` `El trial de ${PRODUCT_DISPLAY_NAME} no está disponible.` ``.
   - main.ts:226 → `` `${PRODUCT_DISPLAY_NAME} API escuchando en :${port} ...` ``.

   OJO: el nombre resultante es "VALLE Design" (VALLE en mayúsculas), no "Valle Design". Ese cambio de capitalización es intencional: es el valor canónico del manifiesto.

3) Actualizar SÓLO los 2 specs que realmente se rompen, importando la misma constante en vez de reescribir el literal a mano:
   - modules/outbox-receiver/email-templates.spec.ts:21,43,81
   - modules/outbox-receiver/outbox-receiver.circuit.pg.spec.ts:204
   NO tocar resend-email.sender.spec.ts, email-sender.config.spec.ts, facturama-cfdi.provider.spec.ts ni identity-mfa.spec.ts: su literal es fixture de entrada propio, siguen verdes y cambiarlo sólo añade ruido al diff.

4) NO tocar los 7 comentarios de prosa que nombran el producto (app.module.ts:36, common/tenant/tenant-context.service.ts:32, load-probe/load-driver.ts:5, load-probe/main.ts:2, modules/auth/auth.module.ts:4, modules/cad-documents/review-link-token.ts:8, seed.ts:10): son documentación, no superficie de cliente.

FUERA DE ALCANCE (issue aparte): el resto de campos del manifiesto (supportEmail, websiteUrl, copyright, trademarkSymbol) en plantillas y PDF.

TERMINADO CUANDO:
  - `npm test --workspace=valle-design-api` en verde.
  - `git grep -n "'Valle Design'\|Valle Design (\${" apps/api/src -- ':!*.spec.ts'` no devuelve nada (exit 1): cero literales de marca en código ejecutable, con los comentarios intactos.
  - `git grep -n 'PRODUCT_DISPLAY_NAME' apps/api/src` muestra las 5 superficies leyendo del manifiesto.

## Terminado cuando
`npm test --workspace=valle-design-api` en verde y `git grep -c 'Valle Design' apps/api/src -- ':!*.spec.ts'` devuelve 0.

---
No toques dependencias, presupuestos, umbrales ni aserciones. Un commit por tarea.
Si al hacerla descubres que el hallazgo es falso, ANOTALO en la bitacora y pasa a la siguiente:
no inventes un arreglo para un problema que no existe.
