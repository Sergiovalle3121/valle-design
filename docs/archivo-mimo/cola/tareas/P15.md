# P15 · Gate de recorte: ningún control habilitado queda fuera de la ventana sin barra visible

**Impacto:** corrige-mentira   **Frente:** taller-jerarquia   **Esfuerzo:** una-sesion
**Fichero:** `D:\dev\valle-design\apps\web\e2e\golden\`

## Que cambia para quien mira la app
Los 38 controles perdidos de la barra superior y los 4.654 px ocultos de la cinta pasan todos los gates del repo porque el golden 67 salta a propósito lo que cae fuera del viewport. Sin este trinquete, el próximo control que se caiga de la barra tampoco se verá en rojo — y es el defecto que produjo el veredicto «un sitio barato».

## Que hacer
TÍTULO: Golden 215: ningún control habilitado del estudio queda fuera de la ventana sin barra de scroll medible

DÓNDE: crear SÓLO `D:\dev\valle-design\apps\web\e2e\golden\215-cad-ningun-control-fuera-de-la-ventana.spec.ts` (215 está libre; el último es 214). No se toca `67-cad-nada-tapa-un-control.spec.ts` ni ningún otro archivo: se duplica a propósito su receta `openStudio` y su función `identidad()` dentro del nuevo archivo, con un comentario que diga que la duplicación es deliberada para no modificar el 67.

QUÉ MIDE, en una sola `page.evaluate` por viewport (como el 67, para no cruzar la frontera una vez por botón):
1. Abre el estudio igual que el 67: `installMockBackend` + `loginAsStandaloneOwner` + `installCadStudioBackend`, `page.goto('/legacy/studio')`, esperar `cad-canvas`, descartar `cad-guided-tour-skip`, `waitForTimeout(2_500)`. Estado en reposo, sin nada desplegado.
2. Dos viewports, con `page.setViewportSize` y un reflow antes de medir (`waitForTimeout(500)`): 1280x720 y 1024x768.
3. Recorre `button, select, input, [role="tab"]` aplicando EXACTAMENTE los mismos filtros del 67: salta `disabled`, `aria-hidden="true"`, cajas de menos de 4x4, `visibility:hidden`, `display:none`, `opacity:0` y `pointer-events:none`.
4. Un control es HALLAZGO si el centro de su caja cae fuera de `[0, innerWidth] x [0, innerHeight]`, o fuera del rectángulo de cliente de su ancestro más cercano cuyo `overflow-x` computado sea `auto`, `scroll` o `hidden` (recorte por contenedor, no sólo por ventana).
5. Sólo se perdona si ese ancestro ofrece una salida MEDIBLE, y esto es la definición operativa, no una impresión:
   (a) barra real: `ancestro.offsetHeight - ancestro.clientHeight >= 8` Y `getComputedStyle(ancestro).scrollbarWidth !== 'none'`; o
   (b) afordancia declarada: existe dentro del ancestro un control visible con `data-scroll-affordance` cuyo propio centro está dentro de la ventana (un atributo suelto no basta: tiene que haber un control pulsable).
   Hoy ninguno de los dos contenedores acusados califica, y está verificado: `cad-top-toolbar` (Layout3DEditor.tsx:13439) y `cad-ribbon-panels-*` (CadRibbon.tsx:164-165) llevan `[scrollbar-width:none]` y `[&::-webkit-scrollbar]:hidden`, y `data-scroll-affordance` no existe en `apps\web\src`.
6. El mensaje nombra el control por SÍ MISMO (testid propio, luego `aria-label`, luego `title`, luego texto — nunca por `closest('[data-testid]')`), su `x`, su ancho y el viewport; y nombra el contenedor que lo recorta por separado, que es lo que hay que arreglar.
7. Alcance declarado en la cabecera: se mide la pestaña de la cinta abierta en reposo, porque `TabPanel` (`apps\web\src\components\ui\Tabs.tsx:145`) devuelve `null` en las inactivas y no están en el DOM. Recorrer todas las pestañas es otra tarea, no ésta.

TRINQUETE (idéntico en espíritu al `RESIDUO_CONOCIDO` del 67, en las DOS direcciones): const `RESIDUO_FUERA_DE_VENTANA: { control: string; viewport: string; motivo: string }[]`, poblada con lo que devuelva la corrida real en este árbol, con un motivo escrito por entrada (prohibidas las vacías). Un hallazgo que no esté en la lista → falla. Una entrada de la lista sin hallazgo → falla pidiendo borrar la línea. Nada de números mágicos: no se escribe «38» ni «4.654» en ninguna aserción; la cuenta real va en el cuerpo del commit.

TERMINADO CUANDO (todo verificable en este commit, sin depender de ninguna otra tarea):
1. `cd apps/web && npx playwright test e2e/golden/215-cad-ningun-control-fuera-de-la-ventana.spec.ts` corre VERDE con la lista declarada tal como la midió el árbol actual, y el cuerpo del commit pega la salida con el número real de residuos por viewport.
2. Regresión provocada, pegada en el commit: bajar el viewport del recorrido a 900 px de ancho (o mover un control hoy visible al final de `cad-top-toolbar`) hace FALLAR el golden nombrando el control y su `x`.
3. Falla también al revés, pegado en el commit: borrar una línea de `RESIDUO_FUERA_DE_VENTANA` hace fallar con el mensaje que pide restaurarla o arreglar el control.
4. `npx playwright test e2e/golden/67-cad-nada-tapa-un-control.spec.ts` sigue verde y `git diff --stat` muestra UN solo archivo nuevo: cero cambios en dependencias, presupuestos, umbrales, aserciones ajenas o tokens del sistema de diseño.

## Terminado cuando (medible, no «se ve mejor»)
El golden corre verde con la lista de residuos declarada tras las tareas 1 y 2; si se devuelve a mano `cad-save` dentro del contenedor con scroll, el golden falla nombrando el control y su x. Y falla también si un residuo declarado deja de tener hallazgo, pidiendo borrar su línea.

---
Un commit. Respeta el sistema de diseno y su gate (relleno y tinta son dos tokens; `--primary` no es relleno de boton). Comprobacion EJECUTADA antes de marcar hecho. No toques dependencias, presupuestos, umbrales, aserciones ajenas ni artefactos de evidencia.
