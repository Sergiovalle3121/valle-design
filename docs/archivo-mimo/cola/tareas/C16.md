# C16 · Iconos para los cinco comandos mudos

**Frente:** transformar-visualizar   **Esfuerzo:** una-sesion
**Fichero:** `apps/web/src/components/cad/ribbon/command-icons.ts:425`

## Por que importa al arquitecto
command-icons.spec.ts está rojo en esta rama y 3DMOVE/3DROTATE sólo se pueden teclear: no llegan ni a la cinta ni a la paleta Ctrl+K, así que el arquitecto no sabe que existen.

## Que hacer
TÍTULO: Cerrar el gate de iconos: cinco filas con dibujo propio (no es discoverability, es CI)

POR QUÉ IMPORTA (corregido): `command-icons.spec.ts` falla en :23 y bloquea la rama. Los cinco comandos YA tienen botón en la cinta (ribbon.ts:259-262, `CAD_RIBBON_UNEXPOSED` vacía) y YA están en la paleta Ctrl+K (command-palette.ts:46); lo que les falta es dibujo propio — hoy heredan el genérico del panel por el fallback de CadRibbonButton.tsx:33. Es el propósito declarado del fichero, no un descubrimiento nuevo.

DÓNDE: `D:\dev\valle-design\apps\web\src\components\cad\ribbon\command-icons.ts` — bloque 3D en :423-428 y bloque de anotación; el import va al bloque de imports (`Rotate3d` está en :175).

QUÉ HACER (un commit):
1. Importar `Move3d` (y el glifo que elijas para 3DROTATE) en el bloque de imports. NO usar `Rotate3D` ni `Move3D` como si fueran otro dibujo: en lucide-react 1.41.0 son el MISMO objeto que `Rotate3d`/`Move3d` (verificable con `node -e "const l=require('lucide-react');console.log(l.Rotate3D===l.Rotate3d)"` → true).
2. Añadir cinco filas, cada una con un glifo que NO aparezca ya en la tabla ni se repita entre ellas: `"3DMOVE": Move3d` junto al bloque 3D; `"3DROTATE"` con un glifo LIBRE (`Rotate3d` ya lo gasta 3DFORBIT en :426 — si prefieres dárselo a 3DROTATE por semántica, mueve 3DFORBIT a un glifo libre en la misma fila y dilo en el mensaje del commit); y `AESYMBOL`, `CENTERLINE`, `CENTERMARK` en su zona de anotación/dibujo. Libres hoy: Move3d, IterationCw, Crosshair, Target, CircuitBoard, Radius, AlignVerticalJustifyCenter, Gauge, Dot.
3. No tocar la aserción `distintos >= 150` ni ninguna otra del spec.

TERMINADO CUANDO (objetivo, y NO la línea impresa — «0 mudos» es texto literal en la plantilla del console.log de las líneas 39-41 y se imprime siempre):
a) `cd D:\dev\valle-design\apps\web && node ../../node_modules/tsx/dist/cli.mjs src/components/cad/ribbon/command-icons.spec.ts` sale con código 0; hoy muere en :23 con `actual: [ '3DMOVE', '3DROTATE', 'AESYMBOL', 'CENTERLINE', 'CENTERMARK' ]`.
b) La cuenta de dibujos distintos sube de 198 a 203 EXACTAS (295 filas → 300). Pegar en el commit la salida de: `node -e "const l=require('lucide-react');const s=require('fs').readFileSync('apps/web/src/components/cad/ribbon/command-icons.ts','utf8');const b=s.slice(s.indexOf('CAD_COMMAND_ICONS'));const u=[...b.matchAll(/:\s*([A-Z][A-Za-z0-9]+),/g)].map(m=>m[1]);console.log('filas',u.length,'distintos',new Set(u.map(n=>l[n])).size)"` — resolver por OBJETO, no por nombre, es lo que delata el truco del alias. Si sale 202 o menos, hay un glifo compartido: arréglalo, no ajustes el número.
c) `cd apps/web && npm run test:specs` pasa entero (la suite completa tarda; lánzala en segundo plano).

FUERA DE ALCANCE, y como dos tareas siguientes por separado (esta NO las hace): (1) 3DMOVE, 3DROTATE y AESYMBOL caen en el panel de relleno `inicio/Dibujo` por `ribbonPanelForCommand` (ribbon.ts:205-209) en vez de un panel 3D/Anotar — eso sí es discoverability real; (2) ninguno de los cinco tiene un spec que ejecute el comando contra el motor y compruebe geometría (`grep -rln "3DMOVE\|CENTERMARK\|AESYMBOL" --include=*.spec.ts` da cero).

## Terminado cuando
`cd apps/web && npm run test:specs` pasa command-icons.spec.ts y su línea impresa dice «0 mudos». Hoy la lista es exactamente ['3DMOVE','3DROTATE','AESYMBOL','CENTERLINE','CENTERMARK'] — lo medí contra CAD_COMMAND_REGISTRY_V2 y CAD_COMMAND_ICONS reales.

---
Un commit. Comprobacion EJECUTADA antes de marcar hecho: el spec conduce el comando REAL contra el motor real; un spec que se prueba a si mismo, que hace grep del fuente, o cuyo arnes puede pasar sin el arreglo, se considera falso. No toques dependencias, presupuestos, umbrales, aserciones ajenas ni artefactos de evidencia de docs/cad/evidence/ o docs/dwg/.
