# P20 · Una sola «Crear cuenta» arriba, no dos

**Impacto:** quita-saturacion   **Frente:** portada   **Esfuerzo:** minutos
**Fichero:** `D:\dev\valle-design\apps\web\src\components\PublicNav.tsx`

## Que cambia para quien mira la app
Medido: 12 elementos pulsables en el primer pliegue, 8 con texto, y `href="/register"` aparece DOS veces —en la barra y en el hero— compitiendo entre sí. Cuando dos botones piden lo mismo, ninguno manda.

## Que hacer
TÍTULO: En la portada, la barra revela «Crear cuenta» sólo cuando el hero se va

POR QUÉ: A 1440 px, `href="/register"` aparece dos veces en el primer pliegue de `/` y ambas piden lo mismo: la barra pública (apps/web/src/components/PublicNav.tsx:105, «Crear cuenta») y el hero (apps/web/src/app/page.tsx:338, «Crear cuenta gratis»). Cuando dos botones piden lo mismo, ninguno manda. La barra ya sabe cuándo el hero salió de pantalla; sólo no lo está usando para esto.

DÓNDE:
- D:\dev\valle-design\apps\web\src\components\PublicNav.tsx — estado `scrolled` en las líneas 41 y 44-53; CTA de escritorio en las líneas 103-108.
- D:\dev\valle-design\apps\web\src\app\page.tsx:287 — el único montaje de `<PublicNav />` cuyo primer pliegue ya trae un `/register`.

QUÉ HACER:
1. Añadir a `PublicNav` una prop opcional `heroConAltaPropia?: boolean`, por defecto `false`.
2. Renderizar el CTA de escritorio `href="/register"` (línea 105) sólo cuando `!heroConAltaPropia || scrolled`. Reutilizar `buttonClass({ variant: "primary" })` sin tocar tokens: relleno y tinta ya están resueltos en apps/web/src/components/ui/styles.ts:126, y `--primary` no sirve de relleno.
3. Pasar `heroConAltaPropia` SÓLO en apps/web/src/app/page.tsx:287. `PublicPageShell` (apps/web/src/app/docs/PublicPageShell.tsx:26) no lo pasa, así que `/precios`, `/docs`, `/casos-de-uso`, `/plantillas` y `/seguridad` conservan su «Crear cuenta» desde el primer píxel sin editar ni una línea suya.
4. No tocar `LINKS` (siguen cuatro), ni la rama móvil, ni el «Crear cuenta gratis» del menú plegado (línea 193). No hay oyente nuevo: el `passive` sobre `window.scrollY > 8` es el que ya está.
5. El comentario de cabecera del componente dice hoy que al bajar media pantalla «la única salida hacia Crear cuenta desaparecía». Actualizarlo para que describa la regla nueva: en la portada la barra la revela al salir el hero; en el resto de páginas públicas está desde el principio.

TERMINADO CUANDO: nuevo spec D:\dev\valle-design\apps\web\e2e\public\nav-cta-unico.spec.ts, viewport 1440x900, con `const navCta = page.getByTestId("public-nav").locator('a[href="/register"]')` (el `data-testid="public-nav"` ya existe en PublicNav.tsx:70):
1. En `/` al cargar, sin scrollear: `await expect(navCta).toHaveCount(0)`; y midiendo cajas, los `a[href="/register"]` cuyo `boundingBox()` da `y < 900` son exactamente 1 — el del hero, cuyo texto es «Crear cuenta gratis». (Hoy esa cuenta es 2: es la aserción que cae con el arreglo.)
2. En `/`, tras `await page.evaluate(() => window.scrollTo(0, 1200))` y `await page.waitForFunction(() => window.scrollY > 8)`: `await expect(navCta).toHaveCount(1)` y `await expect(navCta).toBeVisible()`.
3. En `/precios` y en `/docs`, sin scrollear: `await expect(navCta).toHaveCount(1)` y `await expect(navCta).toBeVisible()` en ambas.
4. Sin editar ningún spec existente, siguen verdes apps/web/e2e/public/mobile-accessibility.spec.ts (corre a 390x844, donde la barra nunca llevó ese CTA) y apps/web/e2e/real/free-launch-funnel.spec.ts (pulsa el del hero, `/Crear cuenta gratis/iu`). El gate del sistema de diseño y apps/web/src/app/public-pages.spec.ts pasan sin cambios.

Un commit: el cambio en PublicNav.tsx, la prop en page.tsx y el spec nuevo.

## Terminado cuando (medible, no «se ve mejor»)
Spec Playwright: a 1440x900, al cargar, hay exactamente 1 elemento visible con `href="/register"` en los primeros 900 px (hoy 2); tras `window.scrollTo(0, 1200)` vuelven a haber 2. La barra pública de `/precios` y `/docs` —que comparten el componente— conserva su «Crear cuenta» desde el primer píxel.

---
Un commit. Respeta el sistema de diseno y su gate (relleno y tinta son dos tokens; `--primary` no es relleno de boton). Comprobacion EJECUTADA antes de marcar hecho. No toques dependencias, presupuestos, umbrales, aserciones ajenas ni artefactos de evidencia.
