# C24 · El ViewCube dice desde dónde miras

**Frente:** transformar-visualizar   **Esfuerzo:** una-sesion
**Fichero:** `apps/web/src/components/cad/editor/Layout3DEditor.tsx:14297`

## Por que importa al arquitecto
El cubo existe, está montado y sus seis caras llaman a presets reales, pero `active` no se le pasa NUNCA: ninguna cara se resalta jamás, así que es una fila de botones con forma de cubo y el arquitecto se pierde en 3D. No hacen falta NAVVCUBE ni NAVBAR como comandos: la órbita y el encuadre con ratón ya funcionan.

## Que hacer
TITULO: El ViewCube dice desde donde miras, y se calla al orbitar

DONDE (verificado): Layout3DEditor.tsx:11576 (`viewPreset`) y :14297 (`<CadViewCube onSelect={viewPreset} />`); CadViewCube.tsx:68 (`active?`); view-controller.ts:165 (`onChange`) y :177-183 (`revision`).

QUE HACER

1) CERO `useState` nuevos en el monolito, y maximo +3 lineas netas alli. Razon: `monolith-budget.json` fija `stateCeilings` de Layout3DEditor.tsx en 118, el fichero tiene hoy exactamente 118 `useState[<(]`, y el gate falla tanto si sube como si baja (check-monolith-budget.mjs:208-225); la asignacion de lineas es 16896 con 16891 actuales. PROHIBIDO editar monolith-budget.json.
   Crear `apps/web/src/components/cad/viewport/viewcube-active-preset.ts` ("use client") con DOS cosas separadas:
   - un rastreador PURO, sin React, probable en Node: `markCadViewCubePreset(preset, revision)` / `cadViewCubeActiveAt(state, revision)` — devuelve el preset solo si la revision registrada coincide con la actual.
   - el hook fino `useCadViewCubeActivePreset(controllerRef)` que devuelve `[active, mark]`, se suscribe con `controller.onChange` (view-controller.ts:165) y limpia cuando `controller.revision` difiere de la registrada. La suscripcion debe montarse de forma tolerante a que `viewControllerRef.current` aun sea null (se fija en Layout3DEditor.tsx:6057, dentro del efecto de escena): suscribirse dentro de `mark`, o reintentar en el efecto, pero NUNCA asumir que existe al primer render.
   En el monolito, solo: 1 linea de import, 1 linea `const [viewCubeActive, markViewCube] = useCadViewCubeActivePreset(viewControllerRef);`, 1 linea `markViewCube(preset);` DENTRO de `viewPreset` (11576-11582) JUSTO DESPUES de `applyCadCameraViewPreset(...)`, y editar la linea 14297 a `<CadViewCube active={viewCubeActive} onSelect={viewPreset} />`.

2) La revision se captura DESPUES de aplicar el preset. `applyCadCameraViewPreset` termina en `controls.update()`, que emite el "change" de OrbitControls de forma sincronica -> `adoptPerspectiveFraming` -> `setView` -> `emit()` -> revision+1. Si se compara contra la revision previa, la cara se apaga en el mismo clic y el golden pasa igualmente. Nada de temporizadores ni rAF: el orden `aplicar -> leer revision -> registrar` basta porque el planeo esta apagado (camera-policy.ts:146).

3) Golden nuevo `apps/web/e2e/golden/215-cad-viewcube-dice-la-vista.spec.ts` (215 libre; el maximo actual es 214; el sharding de CI reparte archivos solo, no hay manifiesto que tocar). SIEMPRE por `getByTestId` — `check-e2e-localizadores.mjs` prohibe `getByTitle` de titulos de preset fuera de fixtures/camera-preset.ts. Pasos:
   a. abrir el estudio con el backend habitual + `enter3DView(page)` (e2e/fixtures/view-mode.ts).
   b. clic en `cad-viewcube-face-front` -> `aria-pressed="true"` en esa cara y `"false"` en las otras cinco (top, iso, back, left, right). HOY FALLA: `active` no llega nunca.
   c. esperar ~1 s y RE-ASERTAR que `front` sigue en `"true"`. Este asserto es el que mata el error del punto 2 (captura de revision antes de aplicar).
   d. arrastre con boton IZQUIERDO sobre el centro de `cad-canvas` (~150 px, `steps: 12`) -> las seis caras en `"false"`. En 3D y sin comando activo el izquierdo orbita (camera-policy.ts). No es un asserto vacio precisamente por (c): la cara venia de "true" comprobado un instante antes y entre medias solo ocurrio el arrastre.
   e. clic en `cad-viewcube-face-top` -> `"true"` (el limpiado no mato la funcion).

TERMINADO CUANDO: `node scripts/cad/check-monolith-budget.mjs` pasa sin haber editado el manifiesto; `npm run -w apps/web test:specs` pasa incluyendo un spec nuevo del rastreador PURO (`viewcube-active-preset.spec.ts`, estilo `check`/`report` de `@/lib/brep/spec-support`, que no redefine la logica); y el golden 215 pasa en Chromium contra el editor real, fallando en (b) si se quita `active=` y en (d) si se quita el limpiado por revision.

NO SE TOCA: camera-view-presets.ts, command-manifest.ts, alias-table.ts, dependencias, presupuestos ni umbrales. No se anade ningun comando.

## Terminado cuando
Golden de navegador que pulsa cad-viewcube-face-front y comprueba aria-pressed='true' en esa cara y 'false' en las demás, y que tras arrastrar para orbitar ninguna queda en 'true'. Sin el limpiado al orbitar el segundo asserto falla, que es justo la mentira que se está quitando.

---
Un commit. Comprobacion EJECUTADA antes de marcar hecho: el spec conduce el comando REAL contra el motor real; un spec que se prueba a si mismo, que hace grep del fuente, o cuyo arnes puede pasar sin el arreglo, se considera falso. No toques dependencias, presupuestos, umbrales, aserciones ajenas ni artefactos de evidencia de docs/cad/evidence/ o docs/dwg/.
