# P11 · Fuzzear readDwg, no solo probeDwg

**Impacto:** demuestra-capacidad   **Frente:** dwg-recien-encendido   **Esfuerzo:** varias-sesiones
**Fichero:** `D:\dev\valle-design\packages\dwg-codec\fuzz\smoke.ts (hoy importa solo `probeDwg`, linea 5)`

## Que cambia para quien mira la app
`securityReviewCleared` sigue en false y con razon: el fuzzer actual solo golpea el reconocimiento de firma; el decodificador completo, que es el que procesa los bytes hostiles del usuario una vez pasada la firma, no tiene ni un caso de mutacion.

## Que hacer
TÍTULO: Pasada de fuzz de mutación determinista sobre `readDwg` en fuzz/smoke.ts

POR QUÉ: `securityReviewCleared` es false en `apps/web/src/lib/cad/dwg-interop-flag.ts:73` y el blocker que imprime el propio repo es «falta la revisión de seguridad del límite binario». Hoy `fuzz/smoke.ts` importa sólo `probeDwg` (línea 5): el fuzzer cubre el reconocimiento de firma, nunca el decodificador. Ninguna spec de `tests/adversarial/` llama a `readDwg`, y los ~40 usos de `readDwg` en `tests/unit/` son round-trips de bytes bien formados. El decodificador completo —el que come los bytes hostiles una vez pasada la firma— no tiene ni un caso de mutación.

DÓNDE: SÓLO `D:\dev\valle-design\packages\dwg-codec\fuzz\smoke.ts`. Cero cambios en `src/`, en `package.json`, en los límites del codec o en cualquier aserción existente.

QUÉ HACER (concreto, siguiendo el patrón que el fichero ya tiene):

1. Añadir imports: `readDwg` y `writeDwg` de `../src/index.js`, y la CLASE `DwgParseError` de `../src/security/parse-error.js` (no está exportada en `src/index.ts`, que sólo expone el tipo `DwgError`). `fuzz/` no lo escanean `check-no-io.ts` ni `check-boundary.ts` (ambos fijan `sourceRoot = src`), así que el import no rompe ningún gate.

2. Constantes nuevas y propias, sin tocar las de la pasada de probe:
   - `READ_SEED = "valle-dwg0-read-fuzz-2026-09-16-v1"` (semilla literal fija, como `SEED`).
   - `READ_CASES_PER_PASS = 1_024`. Medido: ~7,6 ms por caso, dos pasadas ≈ 16 s. NO usar 10_000: serían ~2,5 min añadidos a `npm run check`.
   - NO reutilizar `MAX_BYTES_PER_CASE = 1_024`: la base `writeDwg()` son 4 141 bytes y `DEFAULT_DWG_LIMITS.maxFileBytes` es 16 MiB, así que el corpus cabe sin tocar ningún tope.

3. Corpus: base inmutable `const READ_BASE = writeDwg();` (AC1015 sintético completo que el reader ya round-trippea, `tests/unit/api.spec.ts:124`). Cada caso `i`: copiar la base, derivar el bloque con `createHash("sha256").update(READ_SEED).update(String(i))...` igual que `deterministicBlock`, voltear entre 1 y 4 bytes en offsets tomados del digest (`digest.readUInt32LE(k) % bytes.length`) por XOR. Determinista y sin dependencias nuevas.

4. `runReadPass()` paralela a `runPass()`, con clasificación en TRES clases y no en dos:
   - éxito → clave `ok` (LEGÍTIMA: medido, 137 de 2 000 mutaciones siguen decodificando; el propio fichero ya documenta esa lección para probe en las líneas 116-119);
   - `error instanceof DwgParseError` → clave `error:${error.detail.code}`;
   - cualquier otra cosa → clave `raw:${nombre}` Y `throw` inmediato con el índice del caso, igual que el `catch` de las líneas 107-115.
   Digests de entrada y de resultado acumulados con `stableJson` + `createHash` como ya hace el fichero. Para los casos `ok`, digerir un resumen compacto y estable (conteos de `layers`/`blocks`/`modelSpaceEntities`/`unsupported`/`diagnostics` + `insunits`), no la base entera.

5. Invariantes duros de la pasada nueva (los que de verdad muerden, verificados contra el código):
   - CERO entradas `raw:*` en el histograma. Nota: `src/api/read.ts:59-64` ya normaliza todo error crudo, así que esto es red de seguridad, no el invariante principal.
   - CERO entradas `error:DWG_INTERNAL_ERROR`. ÉSTE es el invariante nuevo y el análogo real de «desconocido»: es la forma en que un `RangeError` del decodificador sale a producción («El worker de importación falló», sin código). Medido hoy: 0 casos, así que la pasada pasa y queda como regresión.
   - Toda clave de error distinta de `ok` pertenece a `DWG_ERROR_CODES` (`src/security/parse-error.ts:1-16`).
   - NO asertar presupuesto de trabajo: `DwgDatabase` (= `Ac1015NeutralDatabase`, `src/reader/database-model.ts:125-144`) no tiene campo `workUnits`. El agotamiento de presupuesto ya sale como `DWG_WORK_LIMIT_EXCEEDED`/`DWG_DEADLINE_EXCEEDED`, que son códigos conocidos y caen en la aserción anterior.
   - Dos pasadas comparadas: `inputDigest`, `resultDigest`, `histogram` serializado y `totalBytes` idénticos, con el mismo mensaje de divergencia que las líneas 165-174.

6. Evidencia: añadir la pasada como clave NUEVA del objeto `evidence` (p. ej. `readPass: { casesPerPass, executions, histogram, digests, seed, baseBytes, passDurationsMs }`) sin renombrar ni quitar ninguna clave existente (`caseGenerator`, `casesPerPass`, `digests`, `executions`, `histogram`, `maxBytesPerCase`, `passDurationsMs`, `passes`, `seed`, `status`, `totalBytesPerPass`).

TERMINADO CUANDO (todo objetivo y reproducible):
- `npm run fuzz:smoke --workspace=@valle-design/dwg-codec` sale con código 0 e imprime el JSON con `readPass.executions === 2048` (> 0), `readPass.histogram` SIN ninguna clave `raw:*` y SIN `error:DWG_INTERNAL_ERROR`, y `readPass.digests.inputSha256`/`resultSha256` iguales entre las dos pasadas.
- El histograma incluye `ok` con valor > 0 y `error:DWG_STRUCTURE_CORRUPT` con valor > 0 (prueba de que el corpus atraviesa la firma y llega al decodificador, que es todo el punto de la tarea).
- Prueba negativa hecha a mano y descrita en el commit: con `READ_CASES_PER_PASS` temporalmente a un caso que se fuerce a `DWG_INTERNAL_ERROR` (o inyectando un `throw` crudo en el bucle), el script falla con salida distinta d

[...recortado; el fichero citado arriba es la fuente]

## Terminado cuando (medible, no «se ve mejor»)
`npm run fuzz:smoke --workspace=@valle-design/dwg-codec` publica la pasada nueva con `executions > 0`, histograma con `desconocido: 0`, digests de las dos pasadas iguales, y falla si algun caso escapa con un error no tipado. El script ya corre dentro de `npm run check` del paquete, asi que entra en CI sin tocar el workflow.

---
Un commit. Respeta el sistema de diseno y su gate (relleno y tinta son dos tokens; `--primary` no es relleno de boton). Comprobacion EJECUTADA antes de marcar hecho. No toques dependencias, presupuestos, umbrales, aserciones ajenas ni artefactos de evidencia.
