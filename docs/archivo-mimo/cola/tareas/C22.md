# C22 · Plano de corte vivo (SECTIONPLANE)

**Frente:** transformar-visualizar   **Esfuerzo:** varias-sesiones
**Fichero:** `apps/web/src/lib/cad/solid3d-three.ts:220`

## Por que importa al arquitecto
Es lo único de Visualización que sirve a la tesis del producto: ver el corte del modelo sin destruirlo. Hoy `grep -rn 'clippingPlanes|localClippingEnabled'` sobre apps y packages da CERO; SLICE y SECTION existen pero son destructivos o producen una región muerta.

## Que hacer
TÍTULO: SECTIONPLANE — plano de corte vivo que corta muros y sólidos por igual

DÓNDE (tres sitios, no uno):
- Módulo nuevo `apps/web/src/lib/cad/view/section-plane.ts` (junto a `visual-styles.ts`/`visual-style-mesh.ts`, que es quien lo va a usar; NO en `Layout3DEditor.tsx`, 16891/16896 líneas en `scripts/cad/monolith-budget.json`). Precedente de módulo suelto: `apps/web/src/components/cad/viewport/camera-view-presets.ts`.
- `apps/web/src/lib/cad/view/visual-style-mesh.ts:59` Y `apps/web/src/lib/cad/solid3d-three.ts:220` — LOS DOS. El primero es el que sirve muros y losas (`room-solid-three.ts:18`, `wall-solid-three.ts:26`); el segundo sólo SOLID3D. Cortar uno y no el otro es el «éxito falso» que el encabezado de `visual-style-mesh.ts` ya nombra para VSCURRENT.
- Comando nuevo `apps/web/src/lib/cad/engine/commands/view-visual.ts` (donde ya vive VSCURRENT), más su fila en `command-manifest.ts` con `kind: "view", mutates: false, transparent: true, module: "commands/view-visual"`.

QUÉ HACER:
1. En `section-plane.ts`: `cadSectionPlaneToThree(plane: {origin: CadPoint3, normal: CadVector3}, viewport: CadThreeViewport): THREE.Plane`. Recibe el `CadThreeViewport` COMPLETO (`entity-three.ts:20-27`), no un `{s,W,H}` suelto: tiene que restar `viewport.origin` antes de escalar, igual que el resto del pipeline. Usa el mismo mapeo que `wall-solid-three.ts:56-58` — `((x-W/2)*s, z*s, (y-H/2)*s)` — reutilizándolo o, si no está exportado, exportándolo desde donde ya vive; no lo vuelvas a derivar a mano en el módulo nuevo.
2. Encender el corte: `renderer.localClippingEnabled = true` en `Layout3DEditor.tsx:5861` (una línea; cabe en las 5 de aire, verifica con `node scripts/cad/check-monolith-budget.mjs`) y pasar el plano vigente a `applyCadVisualStyleToGroup` y a `buildCadSolidObject` para que lo pongan en `material.clippingPlanes`. Sin plano activo, `clippingPlanes` queda `[]` (no `null`).
3. Comando SECTIONPLANE: pide punto y normal, o palabra clave XY/YZ/ZX + cota, copiando el `coordinatePlane()` de `solids-modify.ts:303` para no inventar una segunda convención de signos. Devuelve `{ kind: 'host', request: { kind: 'section-plane', plane | null } }`, y `plot-host.ts:176` lo atiende al lado de `visual-style`. `SECTIONPLANE` sin argumentos (o palabra clave Ninguno) lo apaga.
4. Publicar el estado REAL: añade `clippedMeshes` a `getSnapshot()` de `native-mass-hosts.ts:64` contando, recorriendo la escena, las mallas cuyo material tiene `clippingPlanes.length > 0`, y sácalo como `data-clipped-meshes` en `Cad3DSolidDiagnostics.tsx:71`. Es el mismo mecanismo que T-10a montó para no leer de vuelta la frase del comando.

TERMINADO CUANDO (nada de esto se satisface redefiniendo lógica en el propio fichero):
- Spec de `section-plane.ts` que, con un `CadThreeViewport` de ORIGEN NO NULO (p.ej. `{scale: 0.01, width: 4000, height: 3000, origin: {x: 500000, y: 200000}}`) y el plano XY a z=1500, comprueba con `distanceToPoint` que un punto de dibujo a z=1400 y otro a z=1600 caen en lados opuestos, con los signos esperados. Con `origin` ausente debe dar el mismo resultado relativo: así el spec falla si alguien se olvida del origen flotante.
- Spec que, llamando a `applyCadVisualStyleToGroup` (el camino de muros, no el de SOLID3D) SIN plano activo, comprueba que el material resultante tiene `clippingPlanes` de longitud 0; y CON plano activo, longitud 1 y el mismo `THREE.Plane` que devuelve el módulo.
- Golden de navegador en `apps/web/e2e/golden/`: sobre un documento con UN MURO real de 3000 de alto, teclear `SECTIONPLANE XY 1500` y afirmar que `data-clipped-meshes` del `[data-testid="cad-3d-solid-diagnostics"]` pasa de 0 a la cuenta de mallas del muro, y que `SECTIONPLANE` sin argumentos lo devuelve a 0. Se afirma la escena, no un diff de píxeles contra una línea base que este mismo commit generaría.
- No tocar dependencias, presupuestos, umbrales ni aserciones existentes.

## Terminado cuando
Spec pura del módulo nuevo que, dado un plano XY a z=1500 y el contexto de escena {s,W,H}, devuelve un THREE.Plane con normal y constante correctas (comprobadas con distanceToPoint sobre dos puntos, uno a cada lado); más un caso que comprueba que sin plano activo clippingPlanes queda vacío. Añade un golden de navegador que teclee SECTIONPLANE XY con z=1500 sobre una caja de 3000 y compare el lienzo.

---
Un commit. Comprobacion EJECUTADA antes de marcar hecho: el spec conduce el comando REAL contra el motor real; un spec que se prueba a si mismo, que hace grep del fuente, o cuyo arnes puede pasar sin el arreglo, se considera falso. No toques dependencias, presupuestos, umbrales, aserciones ajenas ni artefactos de evidencia de docs/cad/evidence/ o docs/dwg/.
