# P04 · Subir la imagen del hero del 22 % al 38 % del viewport

**Impacto:** cambia-la-primera-impresion   **Frente:** portada   **Esfuerzo:** minutos
**Fichero:** `D:\dev\valle-design\apps\web\src\app\page.tsx`

## Que cambia para quien mira la app
Medido: 608x473 = 22,2 % del viewport, y el dibujo útil 15,6 %. AutoCAD pone su render a media pantalla. A ese tamaño la imagen no es la pieza central, es una viñeta al lado del texto.

## Que hacer
TÍTULO: Ensanchar el contenedor del hero para llevar la figura del 22 % al >= 36 % del viewport sin robar ancho al titular

POR QUÉ: Medido y confirmado en el código: la figura del hero es 608x473 = 22,2 % del viewport a 1440x900. A ese tamaño es una viñeta al lado del texto, no la pieza central; AutoCAD pone su render a media pantalla. La propuesta original (mover el ratio a 1.5fr dentro de `max-w-7xl`) NO puede llegar al 38 %: con el contenedor fijo en 1280 px el presupuesto de columnas es 1216 − gap, así que a 1.5fr la imagen tope es 729,6 px incluso con gap-0 → 31,2 %. Y forzar 810 px (lo que el 38 % exige) pide ≈2.17fr y deja la columna de texto en 374 px, más estrecha que los 552 px que ya son el problema, con lo que el h1 de `type-display` (84 px, line-height 0.98 a 1440) se va a 5 líneas. La palanca correcta es ensanchar el contenedor SÓLO en el hero: la imagen crece y el texto se queda casi igual (~541 px frente a 552 px hoy), así que esta tarea deja de depender de la del titular.

DÓNDE: D:\dev\valle-design\apps\web\src\app\page.tsx (línea 297, el grid del hero) y un spec nuevo en D:\dev\valle-design\apps\web\e2e\public\portada-hero-escala.spec.ts

QUÉ HACER (un solo commit):
1. En el grid del hero (page.tsx:297) hacer tres cambios y nada más:
   - `max-w-7xl` → `max-w-7xl lg:max-w-[90rem]` (el hero, y sólo el hero, llega a 1440; las bandas de abajo siguen en max-w-7xl)
   - `lg:grid-cols-[minmax(0,1fr)_minmax(0,1.1fr)]` → `lg:grid-cols-[minmax(0,1fr)_minmax(0,1.5fr)]`
   - `gap-14` → `gap-14 lg:gap-6` (se aprieta sólo en escritorio; el ritmo vertical del móvil no se toca)
   Aritmética esperada a 1440x900: contenido 1440 − 64 = 1376; − 24 de gap = 1352; imagen 1352 × 1.5/2.5 = 811 px; texto 541 px; altura (811−56)/1.5 + 105 = 608 px; área 811 × 608 = 493.688 ≈ 38,1 %. El umbral se fija en 36 % para absorber la variación de métricas de fuente frente al modelo.
2. Añadir `data-testid="hero-figure"` al `<figure>` de D:\dev\valle-design\apps\web\src\components\marketing\PlanViewport.tsx (el repo ya usa esta convención: `data-testid="hero-demo-cta"` en page.tsx). Sin esto el spec tendría que colgarse de un selector estructural frágil.
3. No tocar colores ni tokens: este cambio es puramente geométrico, así que el gate de relleno/tinta del sistema de diseño no entra en juego.

TERMINADO CUANDO (spec Playwright nuevo, todo medido, nada de «se ve mejor»):
- A 1440x900: el área de `[data-testid="hero-figure"]` (`getBoundingClientRect().width * height`) es >= 36 % de 1440*900. Registrar el porcentaje en el mensaje de la aserción para dejar constancia del antes (22,2 %) y el después.
- A 1440x900: el h1 `#hero-title` no pasa de 4 líneas, que es lo que ya renderiza hoy — es decir, CERO regresión y sin dependencia de la tarea del titular. Medido como `Math.round(h1.getBoundingClientRect().height / parseFloat(getComputedStyle(h1).lineHeight)) <= 4`.
- A 1440x900: `document.documentElement.scrollWidth === 1440`.
- A 390x844: `document.documentElement.scrollWidth === 390` (el halo `-inset-10` ya desbordó la portada una vez; el comentario de page.tsx:389 explica por qué es `overflow-x-clip` y no `overflow-hidden`, y esta guarda impide que el cambio lo reabra).
- No modificar ni relajar ninguna aserción existente. La guarda móvil de desbordamiento que ya vive en e2e\public\mobile-accessibility.spec.ts:46 y e2e\real\movil.spec.ts:65 se queda intacta; el spec nuevo sólo añade la medición de escala del hero, que hoy no existe en ningún sitio.

## Terminado cuando (medible, no «se ve mejor»)
Spec Playwright: a 1440x900 el área de la figura del hero es >= 38 % del viewport (hoy 22,2 %) y `document.documentElement.scrollWidth === 1440`; a 390x844 `scrollWidth === 390` (el halo de `-inset-10` ya desbordó la portada una vez y está anotado en el código).

---
Un commit. Respeta el sistema de diseno y su gate (relleno y tinta son dos tokens; `--primary` no es relleno de boton). Comprobacion EJECUTADA antes de marcar hecho. No toques dependencias, presupuestos, umbrales, aserciones ajenas ni artefactos de evidencia.
