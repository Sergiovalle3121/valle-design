# P10 · El realce de los 20 comandos destacados llega a la etiqueta (hoy se pierde)

**Impacto:** demuestra-capacidad   **Frente:** taller-jerarquia   **Esfuerzo:** minutos
**Fichero:** `D:\dev\valle-design\apps\web\src\components\cad\ribbon\CadRibbonButton.tsx`

## Que cambia para quien mira la app
D33 pintó `data-primary`, `w-20` y `bg-muted/50`, pero el `font-semibold` va en el BOTÓN y `.type-micro` del SPAN (peso 500, capa components) lo gana: medido, la etiqueta de LINE y la de DIVIDE pesan las dos 500. La mitad del realce de anoche no se ve.

## Que hacer
TÍTULO: El realce de los 20 comandos destacados llega a la etiqueta (hoy la escala lo gana)

POR QUÉ: En `CadRibbonButton.tsx:53` el `font-semibold` está en el `<button>`, pero la etiqueta es el `<span className="type-micro">` de la línea 61, y `.type-micro` declara `font-weight: 500` (globals.css:1570-1575). Una declaración propia del hijo gana a la herencia del padre, así que la etiqueta de LINE (primary) y la de DIVIDE (no primary) pesan las dos 500: la mitad del realce de D33 no se ve. El comentario de la escala en globals.css:1399-1407 documenta el mecanismo correcto — la escala vive en `@layer components` precisamente para que «una utilidad suelta (`font-semibold`, `text-lg`) la sobrescribe donde de verdad hace falta».

DÓNDE:
- `D:\dev\valle-design\apps\web\src\components\cad\ribbon\CadRibbonButton.tsx`
- spec nueva: `D:\dev\valle-design\apps\web\e2e\golden\215-cad-realce-llega-a-la-etiqueta.spec.ts` (214 es el último ocupado)

QUÉ HACER:
1. Quitar `font-semibold` de la rama primary de la línea 53 (dejar `w-20 bg-muted/50 text-foreground`) y aplicar el peso al `<span>` de la línea 61, condicionado a `command.primary`. Ejemplo: `className={cx("type-micro text-center leading-tight", command.primary && "font-semibold")}`.
2. NO tocar `w-20`, `w-16`, `py-0.5`, `gap-0.5` ni `h-4 w-4` del icono: el golden 19 exige `canvas.height > 520` a 720 de alto (`19-cad-professional-workbench.spec.ts:32`).
3. NO tocar `bg-muted/50` ni `text-foreground` (relleno y tinta son dos tokens y el gate de diseño manda), ni el 500 de `.type-micro`, ni ningún umbral o aserción existente.
4. ANTES de editar, medir en HEAD y anotar en el comentario de la spec: `page.getByTestId("cad-ribbon").boundingBox()` a 1280x720 en /demo. Esa cifra medida —no un 122 supuesto; el comentario del propio componente registra 116 px el 2026-09-02— es la línea base de la aserción de altura.
5. Si la etiqueta de DIMLINEAR (9 caracteres, el nombre primary más largo, 68 px de contenido útil en `w-20`) se parte en dos renglones y la cinta crece, arreglarlo en la etiqueta (p. ej. `whitespace-nowrap` en el span). Prohibido ensanchar `w-20` o rebajar el 520 del golden 19.

TERMINADO CUANDO la spec 215 pasa en /demo a 1280x720 y afirma las cuatro cosas:
a) `getComputedStyle` del `<span>` interno del botón `cad-ribbon-command-LINE` (que tiene `data-primary="true"`) devuelve `fontWeight === "600"`.
b) `getComputedStyle` del `<span>` interno del botón `cad-ribbon-command-DIVIDE` (sin `data-primary`) devuelve `fontWeight === "500"`.
c) Barrido de los 20 nombres de `CAD_RIBBON_PRIMARY`: los visibles en la pestaña activa dan 600 en el span; ningún botón sin `data-primary` de esa misma pestaña pasa de 500.
d) `boundingBox().height` de `cad-ribbon` es EXACTAMENTE igual a la línea base medida en el paso 4, y cada span de primary sigue ocupando un solo renglón (`box.height` del span < 2 × `line-height` computado).
Y además: `19-cad-professional-workbench.spec.ts` sigue en verde sin modificarlo.

## Terminado cuando (medible, no «se ve mejor»)
Spec que lee estilos computados en /demo: `getComputedStyle(span)` del botón con `data-primary="true"` devuelve fontWeight 600 y el del botón sin `data-primary` devuelve 500; alto de `cad-ribbon` sin cambio (122 px a 1280x720).

---
Un commit. Respeta el sistema de diseno y su gate (relleno y tinta son dos tokens; `--primary` no es relleno de boton). Comprobacion EJECUTADA antes de marcar hecho. No toques dependencias, presupuestos, umbrales, aserciones ajenas ni artefactos de evidencia.
