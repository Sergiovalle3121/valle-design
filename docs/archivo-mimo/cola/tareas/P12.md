# P12 · «Guardar» y el estado del documento dejan de caerse de la barra superior

**Impacto:** corrige-mentira   **Frente:** taller-jerarquia   **Esfuerzo:** una-sesion
**Fichero:** `D:\dev\valle-design\apps\web\src\components\cad\editor\Layout3DEditor.tsx`

## Que cambia para quien mira la app
A 1280x720 el botón Guardar está en x=2569 y el selector Borrador/En revisión/Aprobado en x=2440, los dos fuera de la ventana y sin barra de desplazamiento: el control más importante del editor no está en pantalla y ningún gate lo delata.

## Que hacer
TÍTULO: «Guardar», el estado del documento y el cierre dejan de caerse de la barra superior

DÓNDE: D:\dev\valle-design\apps\web\src\components\cad\editor\Layout3DEditor.tsx (barra en 13438, cola en 14150-14208)

QUÉ HACER

A) Partir la barra en dos, respetando lo que ya depende de ella.
Hoy el div 13438 es a la vez el contenedor con scroll Y el elemento con `data-testid="cad-top-toolbar"`. Se convierte en envoltorio, con DOS hijos:

1. El envoltorio CONSERVA `data-testid="cad-top-toolbar"` — no negociable: `apps/web/e2e/fixtures/camera-preset.ts:24` hace `page.getByTestId("cad-top-toolbar").getByTitle(/Vista superior/)` y `apps/web/e2e/real/cad-3d-m1-real.spec.ts:348` hace lo mismo con otros títulos; si el testid baja a la banda con scroll, los controles de la cola dejan de ser descendientes y esas búsquedas se rompen. Conserva también `relative z-30 flex items-center border-b border-border bg-surface/90 px-4 backdrop-blur` y el ternario de altura `h-12 py-1.5` / `h-14 py-2.5` (19-cad-professional-workbench.spec.ts:34 afirma `height <= 52`). Le QUITAS `[&>*]:shrink-0`: con esa regla el hijo con scroll no podría encogerse y volvería a desbordar el mismo píxel.

2. Banda de iconos (hijo 1): `flex min-w-0 flex-1 flex-nowrap items-center gap-2 overflow-x-auto whitespace-nowrap [-ms-overflow-style:none] [scrollbar-width:none] [&::-webkit-scrollbar]:hidden [&>*]:shrink-0`. Dentro va todo desde el botón de cierre persistente (13453) hasta el último control de iconos. Ese botón lleva `sticky left-0` y su anclaje depende de estar DENTRO del contenedor que desplaza: se queda aquí.

3. Cola fija (hijo 2): `flex shrink-0 items-center gap-2` con el bloque `approval` (14151-14180), `cad-save` (14182) y el cierre del editor (14201). Fuera del scroll. Elimina el `<div className="flex-1" />` de 14150: el reparto lo hace ya `flex-1` de la banda.

B) El relleno del botón: reusar el par que ya existe, NO declarar uno nuevo.
Borra `style={{ background: "#e11d48" }}` (14194) y pon `bg-brand-strong text-primary-foreground` en el className. `--primary-foreground` sobre `--brand-primary-strong` mide 6,93:1 en claro y en oscuro, y el par ya está en la lista de `scripts/design/check-contrast.mjs` como «el botón principal en reposo»; la clase ya se usa en este fichero (13530, 13541). **No se toca check-contrast.mjs**: ni umbrales ni filas nuevas. Deja `text-foreground` fuera — relleno y tinta son dos tokens y la tinta del par es `--primary-foreground`.

C) Dar asidero al cierre del editor.
El botón de 14201 no tiene testid ni aria-label, sólo `title="Cerrar editor"` (única en todo `apps/web/src`, comprobado). Añádele `data-testid="cad-close-editor"` y `aria-label="Cerrar editor"` para que el spec no dependa de copy. El selector de estado no necesita testid: `getByLabel("Estado de aprobación del plano")` ya lo identifica, y renderiza siempre porque `setApproval` cae al valor por defecto `{status:"draft"}` al cargar el layout (Layout3DEditor.tsx:2562), sin necesidad de sembrar nada en el fixture.

TERMINADO CUANDO

1. Nuevo `apps/web/e2e/golden/215-cad-cola-barra-superior-dentro-del-viewport.spec.ts`, calcado del patrón de `214-cad-aviso-inferior-no-choca-con-comandos.spec.ts` (mismo `installMockBackend` + `loginAsStandaloneOwner` + `installCadStudioBackend`, mismo salto de `cad-guided-tour-skip`, `setViewportSize` antes de abrir). Para cada uno de los tres tamaños {1280×720, 1024×768, 1440×900}, y para cada uno de los tres controles —`page.getByTestId("cad-save")`, `page.getByLabel("Estado de aprobación del plano")`, `page.getByTestId("cad-close-editor")`—: `toBeVisible()` y su `boundingBox()` no nulo con `x >= 0`, `y >= 0`, `x + width <= viewport.width` y `y + height <= viewport.height`. Sin tolerancias negativas.
2. En el mismo spec, prueba de que la banda de iconos SIGUE desplazándose y el arreglo no fue «encoger todo»: `scrollWidth > clientWidth` en la banda a 1280×720.
3. `node scripts/design/check-contrast.mjs` verde con la MISMA lista de pares que hoy (92 pares medidos, par más ajustado 1,09:1) — diff vacío en ese fichero.
4. `grep -n 'style={{' ` sobre el bloque del botón `cad-save`: cero reglas de color. Y `grep -rn 'e11d48' apps/web/src` sin resultados.
5. `19-cad-professional-workbench.spec.ts`, los specs que usan `fixtures/camera-preset.ts` y `real/cad-3d-m1-real.spec.ts` siguen pasando sin editarlos: es la prueba de que el testid se quedó en el sitio correcto.

## Terminado cuando (medible, no «se ve mejor»)
Spec de Playwright a 1280x720, 1024x768 y 1440x900: los rect de `cad-save`, del selector de estado y del cierre están enteros dentro del viewport en los tres tamaños. `node scripts/design/check-contrast.mjs` verde con el par nuevo declarado y ninguna regla de color en `style=` en ese botón.

---
Un commit. Respeta el sistema de diseno y su gate (relleno y tinta son dos tokens; `--primary` no es relleno de boton). Comprobacion EJECUTADA antes de marcar hecho. No toques dependencias, presupuestos, umbrales, aserciones ajenas ni artefactos de evidencia.
