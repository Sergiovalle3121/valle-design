# D13 · «Algo salió mal» no es un error: es el botón de soporte montado encima del panel de Biblioteca

**Gravedad:** se-ve-precario   **Esfuerzo:** una-sesion
**Fichero:** `apps/web/src/components/cad/studio/CadIncidentReporter.tsx:137`

## El problema
No hay ningún ErrorBoundary activado. El ErrorBoundary real de la app nunca dice eso: dice «Esta parte se rompió» y «{zona} no se pudo mostrar» (apps/web/src/components/ui/ErrorBoundary.tsx:102 y :122). El literal «Algo salió mal» sólo existe en este componente (CadIncidentReporter.tsx:148), que es el botón para reportar incidencias. Se monta en `pointer-events-none fixed left-3 top-[11.5rem] z-[75]`, es decir a 12 px del borde IZQUIERDO DE LA VENTANA y a 184 px del superior. Pero ahí no hay lienzo: ahí está la columna del dock izquierdo, `w-60` (240 px) anclada en x=0 (apps/web/src/components/cad/editor/CadLeftDockPanel.tsx:122), cuya fila de pestañas —la que contiene «Biblioteca» (CadLeftDockPanel.tsx:165)— empieza justo al acabar la cinta. El par «Algo salió mal» + «Comentarios» cae sobre esa fila y se lee como una tercera pestaña del panel, rota. El comentario del propio archivo (líneas 111-136) documenta que esa posición se eligió midiendo contra el LIENZO, sin contar con que el dock izquierdo ocupa esa banda.

## La tarea
Mover el par «Algo salió mal» / «Comentarios» a la bandeja de la barra de estado, siguiendo el patrón EXACTO de `CallBar.tsx` (portal + respaldo fijo), en `apps/web/src/components/cad/studio/CadIncidentReporter.tsx`:

1. Añadir `const tray = useStudioTraySlot();` (de `@/components/cad/studio/use-studio-tray`) junto al resto de hooks, ANTES del `if (estado === "cerrado") return …` de la línea 95 — no dentro de la rama, o se rompen las reglas de hooks.

2. Extraer el contenido actual de la línea 137-164 (los dos botones + `<FeedbackDialog>`) a una constante `botones`. Si `tray` existe, devolver `createPortal(<span className="inline-flex items-center gap-1">{botones}</span>, tray)`.

3. CONSERVAR el envoltorio `fixed left-3 top-[11.5rem] z-[75] pointer-events-none … [&>*]:pointer-events-auto` como respaldo para cuando `tray` es `null`. Es obligatorio: `CadIncidentReporter` se monta en `Layout3DEditor.tsx:16877` como hijo directo de `cad-shell`, fuera del ternario `error/!data` de la línea 14251 donde vive `CadStatusBar` (14795), así que con el documento sin cargar la bandeja no existe y el canal de reporte debe seguir visible. En ese estado tampoco existe `cad-left-dock`, de modo que la posición fija ya no tapa nada. Reescribir el comentario 111-136 para decir esto: la banda `top-[11.5rem]` NO es lienzo, es la columna del muelle izquierdo (`CadLeftDockPanel.tsx:122`, `w-60` anclado en x=0 bajo la cinta), y la posición fija queda sólo como respaldo sin editor.

4. Ajustar el peso visual de la copia de bandeja al resto del renglón, como hizo `CallBar.tsx:163-182`: `rounded-control border border-border bg-surface px-1.5 py-0.5 type-micro` en vez de `rounded-lg … px-2.5 py-1 shadow`, y envolver los rótulos en `<span className="@max-[40rem]:hidden">` dejando `title` y `aria-label` (o `sr-only`) con el texto completo. `cad-status-bar` es `flex-wrap` (`CadStatusBar.tsx:173`) y dos rótulos de ~180 px abrirían un cuarto renglón que se come alto de lienzo (golden 19: 474 px contra 520 de mínimo).

5. No tocar `data-testid="cad-incident-open"`, `cad-feedback-open`, `cad-incident-dialog` ni el resto del cuadro: `e2e/real/primera-hora.spec.ts:192` los usa por testid y debe seguir pasando sin cambios.

TERMINADO CUANDO: un spec nuevo en `apps/web/e2e/golden/` que, con `page.setViewportSize({ width: 1280, height: 720 })` (imprescindible: por debajo de 1100 px el muelle lleva `max-[1100px]:hidden` y la aserción se volvería verde sin arreglar nada) y perfil por defecto `drafting`, abre el estudio con un documento cargado y afirma que el `boundingBox()` de `[data-testid="cad-incident-open"]` no intersecta el de `[data-testid="cad-left-dock"]`, comprobándolo en los DOS estados del muelle: expandido y tras pulsar `[data-testid="cad-left-dock-toggle"]` (colapsado `w-9`, que es el caso peor — hoy el botón cubre la mitad baja del riel y el clic del golden 213 pasa por 4 px). Falla hoy en los dos, pasa tras el cambio.

## Terminado cuando
Un spec que abre el estudio y afirma que `getBoundingClientRect()` de `[data-testid="cad-incident-open"]` no intersecta la de `[data-testid="cad-left-dock"]`. Falla hoy, pasa tras el cambio.

---
No toques dependencias, presupuestos, umbrales ni aserciones. Un commit por tarea.
Si al hacerla descubres que el hallazgo es falso, ANOTALO en la bitacora y pasa a la siguiente:
no inventes un arreglo para un problema que no existe.
