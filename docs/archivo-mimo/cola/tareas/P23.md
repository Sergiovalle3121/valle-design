# P23 · El informe de perdidas del DWG se ve entero, no seis lineas

**Impacto:** quita-saturacion   **Frente:** dwg-recien-encendido   **Esfuerzo:** una-sesion
**Fichero:** `D:\dev\valle-design\apps\web\src\app\dashboard\import-status.tsx:127-142`

## Que cambia para quien mira la app
Un DWG real llega con doce codigos de perdida distintos (unidad asumida, codepage sin decodificar, estado de capa sin mapear, contorno de HATCH curvo, objetos sin decodificar...) y la tarjeta de importacion pinta solo las seis primeras sin agrupar: el usuario cree que tiene el plano entero.

## Que hacer
TÍTULO: Toda importación sin informe DXF agrupa sus pérdidas por código (una fila por código, nada recortado)

DÓNDE (sólo la rama `else`, la del listado plano):
- `D:\dev\valle-design\apps\web\src\app\dashboard\import-status.tsx:132-143` — el `state.report.warnings.slice(0, 6)`.
- NO tocar `import-status.tsx:127-130` (rama `state.report.dxfReport` → `CadDxfImportReportPanel`), que tiene su propio panel y sus propios specs.

QUÉ HACER (un commit):
1. Módulo PURO nuevo `D:\dev\valle-design\apps\web\src\components\cad\interop\import-losses-by-code.ts`, siguiendo el precedente explícito de `import-report-view.ts` («está en un módulo aparte del componente porque es la parte que se puede probar en Node»). Exporta:
   - `groupImportWarningsByCode(warnings: ReadonlyArray<{ code: string; message: string }>): Array<{ code: string; label: string; count: number; sample: string }>` — agrupa ESTRICTAMENTE por `code` (no por mensaje: los detalles DWG llevan el handle dentro), orden determinista `count` desc y luego `code` asc.
   - `IMPORT_LOSS_LABEL_ES`: mapa código→rótulo en español que cubre los TRECE valores de `DWG_BRIDGE_LOSS_CODES` (`apps/web/src/lib/cad/dwg-document-bridge.ts:105-128`). Para un código no catalogado, el fallback usa el `message` de la advertencia, NUNCA el código desnudo: `import-report-view.spec.ts:181-193` ya prohíbe jerga visible y el spec nuevo debe heredar esa regla.
   - Un componente presentacional mínimo y exportado desde este módulo (o un `.tsx` hermano) que pinte la lista de filas. Debe renderizarse sin `ImportStatus`: `import-status.tsx` importa `@/lib/cad/repositories/client` y `document-import-client`, módulos de navegador, y montar el componente entero en Node es un riesgo innecesario.
2. En `import-status.tsx`, sustituir el `.slice(0, 6)` por una fila por código con su cuenta, conservando el texto del total de la línea 135. Sin severidades inventadas: `report.warnings` es `{ code, message }` y NO trae severidad (`document-import.ts:61`; el puente la descarta en `dwg-document-bridge.ts:640-644`). Si se quiere severidad, leerla de `state.report.document.lossManifest[].severity` y de ningún otro sitio.
3. Sin tokens nuevos ni rellenos de marca: no introducir `--primary` como relleno, respetar relleno/tinta como dos tokens (gates en `scripts/design/check-brand-literal.mjs` y `check-contrast.mjs`).

TERMINADO CUANDO `apps\web\src\components\cad\interop\import-losses-by-code.spec.ts` (el runner sólo recoge `src/**/*.spec.ts`, debe imprimir algo en stdout y terminar — `apps/web/scripts/run-specs.mjs`) pasa con un informe sintético de 40 advertencias repartidas sobre los 13 códigos reales de `DWG_BRIDGE_LOSS_CODES`, y afirma:
- se pintan exactamente 13 filas (una por código distinto);
- la suma de las cuentas mostradas es 40;
- cada uno de los 13 códigos aparece en la salida (ninguno oculto), comprobado por rótulo, no por código;
- ningún `dwg_...` crudo queda visible en el texto tras quitar etiquetas HTML (misma aserción anti-jerga que `import-report-view.spec.ts`);
- el orden es determinista entre dos ejecuciones con la misma entrada;
- caso de control: un informe CON `dxfReport` sigue pintando `CadDxfImportReportPanel` y no la lista agrupada.

## Terminado cuando (medible, no «se ve mejor»)
Spec de componente/Playwright con un informe sintetico de 40 advertencias sobre 12 codigos: afirma que se pinta una fila por codigo distinto (12), que la suma de las cuentas mostradas es 40, y que ningun codigo queda oculto. Sin tocar el `slice` de la rama DXF, que tiene su propio panel.

---
Un commit. Respeta el sistema de diseno y su gate (relleno y tinta son dos tokens; `--primary` no es relleno de boton). Comprobacion EJECUTADA antes de marcar hecho. No toques dependencias, presupuestos, umbrales, aserciones ajenas ni artefactos de evidencia.
