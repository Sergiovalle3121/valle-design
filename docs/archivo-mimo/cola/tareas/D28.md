# D28 · La paleta flotante parte las palabras a media sílaba y repite lo que ya está en la cinta

**Gravedad:** se-ve-precario   **Esfuerzo:** una-sesion
**Fichero:** `apps/web/src/components/cad/editor/CadToolPalette.tsx:148`

## El problema
El botón mide `w-14` (56 px) y la etiqueta lleva `break-words` (:169). El propio comentario lo admite: «Seleccionar —el más largo de los dieciséis, 11 caracteres— no cabe en los 56 px del botón a ningún tamaño de fuente del sistema». `break-words` (overflow-wrap: break-word) parte DENTRO de la palabra cuando no hay otro punto de quiebre, y de ahí salen «Seleccio/nar», «Encuadr/e», «Distanci/a», «Rectáng/ulo» exactamente como se ven en vivo. Además la paleta es `absolute top-3 left-3 z-20` (:100), es decir flota SOBRE el lienzo, y sus dieciséis acciones ya están en la cinta: en una pantalla de portátil el lienzo queda reducido a una ventana central.

## La tarea
Ensanchar el botón de `CadToolPalette` para que ninguna etiqueta de UNA SOLA PALABRA se parta a media palabra, conservando icono + etiqueta.

1. `apps/web/src/components/cad/editor/CadToolPalette.tsx:148`: cambiar `w-14` por `w-20` (80 px → 72 px de contenido con `px-1`). Medido con Inter 11 px/500 y `type-micro` reales: la palabra suelta más ancha es «Seleccionar» (62,6 px), seguida de «Rectángulo» (59,9), «Deshacer» (50,7), «Encuadre» (50,2), «Distancia» (49,2) y «Símbolos» (48,9); las seis caben en 72 px. «Ajustar todo» (64,7 px) también cabe, pero si algún día no cupiera puede envolver por su espacio sin problema: el objetivo es el corte INTRA-palabra, no el envolver.

2. `:169`: dejar `break-words` puesto como red de seguridad (una etiqueta futura más larga debe seguir sin salirse del botón — eso es lo que vigila el golden 212), pero reescribir el comentario de :158-168, que hoy afirma un hecho ya falso («no cabe en los 56 px … a ningún tamaño de fuente») y documenta el corte a media palabra como si fuera el resultado deseado. Que diga la medida nueva y por qué `break-words` se queda.

3. NO tocar el tooltip ni pasar a icono solo: el nombre accesible del botón es el texto visible y de él dependen `apps/web/e2e/fixtures/tool-palette.ts` (`getByRole("button", { name: action.label, exact: true })`) y el helper `medirBotonSeleccionar` del golden 212.

4. Verificar el modo de dos columnas: `.cad-tool-grid` pasa a dos columnas bajo `@media (max-height: 820px)` (`apps/web/src/app/globals.css:1675-1679`), así que a 1280×720 la paleta crece de ~128 px a ~180 px de ancho sobre el lienzo. Volver a correr `apps/web/e2e/golden/68-cad-nada-tapa-el-lienzo.spec.ts` y `67` y, si aparecen puntos nuevos de la paleta, actualizar su lista de capas admitidas con el motivo, no relajar la aserción.

5. Prueba: AMPLIAR `apps/web/e2e/golden/212-cad-etiqueta-seleccionar-no-se-encima.spec.ts` (no crear un golden paralelo), recorriendo `CAD_TOOLBAR_ACTIONS` entero en sus tres viewports ya declarados (1280×900, 1280×760, 1024×668) y afirmando, para cada etiqueta SIN espacios, que su nodo de texto renderiza en una sola línea:

   const lineas = await etiqueta.evaluate((el) => { const r = document.createRange(); r.selectNodeContents(el); return r.getClientRects().length; });
   expect(lineas, `«${action.label}» se parte en ${lineas} líneas`).toBe(1);

   Para las etiquetas con espacio («Ajustar todo») afirmar sólo que ningún quiebre cae dentro de una palabra (comparar el texto de cada rectángulo de línea contra los tokens separados por espacio). Prohibido afirmar sobre `textContent`: el quiebre de `overflow-wrap` es visual y nunca aparece ahí, así que esa aserción pasaría con el defecto puesto.

No tocar `lib/cad/toolbar.ts` (ids, rótulos ni atajos) ni la cinta en este cambio.

## Terminado cuando
Un golden/spec que mide con `elementsFromPoint` o `getBoundingClientRect` que ninguna etiqueta de `CAD_TOOLBAR_ACTIONS` renderiza en más de una línea a 1280×720, y un spec de DOM que afirma que el texto de cada botón no contiene salto interno.

---
No toques dependencias, presupuestos, umbrales ni aserciones. Un commit por tarea.
Si al hacerla descubres que el hallazgo es falso, ANOTALO en la bitacora y pasa a la siguiente:
no inventes un arreglo para un problema que no existe.
