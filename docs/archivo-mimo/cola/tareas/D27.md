# D27 · F9 — La marca dentro de los archivos que el cliente entrega (PDF, STEP, IGES)

**Gravedad:** se-ve-precario   **Esfuerzo:** una-sesion
**Fichero:** `apps/web/src/lib/cad/plot/plot-pdf.ts:272`

## El problema
El nombre viaja DENTRO de los archivos exportados: metadatos del PDF trazado (plot-pdf.ts:272 subject, :273 author, :275 creator), la ficha de fidelidad (plot-fidelity.ts:189 preparedBy), la cabecera IGES 5.3 (iges.ts:68 línea de arranque, :259 author, :260 organization) y la cabecera STEP (step-export.ts:211 FILE_DESCRIPTION, :212 FILE_NAME, que además incrusta 'valle-design-brep' y 'valle-design'). Un plano impreso hoy sale firmado «Valle Design» en las propiedades del PDF. Tres specs los afirman: sheet-set-pdf.spec.ts:48, dxf-export.spec.ts:32, paper-space.spec.ts:188,345.

## La tarea
F9 (corregida) — Sacar la marca quemada de los ficheros que el cliente recibe, tomándola del manifiesto de marca.

REGLA RECTORA: no sustituir un literal quemado por otro. El nombre sale de packages/contracts/src/brand.ts mediante `resolveBrandManifest(env)` + `productDisplayName(manifest, "design")` (producto, hoy «VALLE Design») o `manifest.brandName` (marca, hoy «VALLE»). «VALLECAD» no existe en el repo: no usarlo. Si el dueño quiere que el PDF diga otra cosa, se consigue con `BRAND_PRODUCT_NAME_DESIGN`, sin tocar código.

ALCANCE A (metadatos dentro del fichero exportado) — 4 ficheros, 11 literales (los 10 del reporte + iges.ts:75):
1. plot-pdf.ts:272/273/275 — `subject`, `author`, `creator` desde el manifiesto. Añadir el manifiesto como opción inyectable (p. ej. `options.brand`) con `resolveBrandManifest()` como valor por defecto: la función es pura y no debe leer `process.env` por su cuenta.
2. plot-fidelity.ts:189 — `preparedBy` igual.
3. iges.ts:68 (línea de arranque), :75 (`hollerith(...)` del id de sistema), :259 `author`, :260 `organization`. Cuidado: `pad72` trunca a 72 columnas y `hollerith` prefija longitud; con nombre ya dinámico, comprobar que un nombre largo no desalinea la sección Global.
4. step-export.ts:211 `FILE_DESCRIPTION`, :212 `FILE_NAME` (incluidos `'valle-design-brep'` y `'valle-design'`). OBLIGATORIO: escapar la comilla simple como `''` antes de interpolar; el nombre pasa a ser configurable por entorno y un apóstrofo corrompería el STEP.
NO TOCAR: VALLE_DIM / VALLE_MLEADER / VALLE_BLOCK (dxf-xdata-apps.ts:24,27,30) ni VALLE_DESIGN_SOLID (iges.ts:257, step-export.ts:111).

ALCANCE B (la marca VISIBLE en la lámina y en el DXF) — no es opcional, sin esto el objetivo del ticket no se cumple: reemplazar los `productLabel: "Valle Design"` quemados de CadStudioHost.tsx:110 y Layout3DEditor.tsx:1058 por `productDisplayName(manifest, "design")`. Eso arregla de una vez el pie impreso de cada hoja (sheet-set-pdf.ts:245), el comentario del DXF (export-host.ts:674) y el atributo PROJECT del cajetín (Layout3DEditor.tsx:1370). Si se prefiere entregar en dos pasos, hacer A y B como dos commits del MISMO PR, nunca cerrar F9 con sólo A.

SPECS — corrección del plan original:
- NO tocar paper-space.spec.ts:188/345: «Valle Design Demo» es el proyecto de un cliente ficticio en un round-trip de persistencia, no un literal de marca. Fuera de alcance.
- sheet-set-pdf.spec.ts:48 y dxf-export.spec.ts:32 son datos de ENTRADA; sólo cambiarlos si el cambio de tipos lo exige. No prueban nada de esto.
- AÑADIR un spec nuevo (el que falta) que fije los defaults y evite la regresión: trazar con `renderCadPlotPdf(sheets, {})` y aseverar que Autor/Creador/Asunto salen del manifiesto y que la cadena «Valle Design» NO aparece; ídem un STEP y un IGES generados con opciones vacías. Incluir un caso con manifiesto simulado (`BRAND_PRODUCT_NAME_DESIGN` distinto, y uno con apóstrofo para el escape STEP) que demuestre que el nombre viaja de verdad hasta el fichero.

TERMINADO CUANDO:
- `npm --prefix apps/web run test:specs` en verde (desde la raíz; `npm run test:specs` NO existe ahí), más `npm run lint` y `npm run typecheck`.
- `git grep -n "Valle Design" -

[...recortado. El fichero y la linea de arriba son la fuente: velo tu mismo.]

## Terminado cuando
`npm run test:specs` en verde y un PDF trazado de prueba muestra «VALLECAD» en Propiedades → Autor.

---
No toques dependencias, presupuestos, umbrales ni aserciones. Un commit por tarea.
Si al hacerla descubres que el hallazgo es falso, ANOTALO en la bitacora y pasa a la siguiente:
no inventes un arreglo para un problema que no existe.
