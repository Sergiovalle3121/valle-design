# D20 · F1 — El manifiesto: dos líneas que mueven 18 ficheros solas

**Gravedad:** se-ve-precario   **Esfuerzo:** minutos
**Fichero:** `packages/contracts/src/brand.ts:81`

## El problema
`DEFAULT_PRODUCT_NAMES = { design: "VALLE Design" }` (línea 81) y `brandName: "VALLE"` (línea 88) son el único sitio del que cuelgan el título de pestaña (apps/web/src/app/layout.tsx:62 usa `%s · ${BRAND.productNames.design}`), el manifiesto PWA (apps/web/src/app/manifest.ts:24,25), el logotipo de la barra (apps/web/src/components/brand/Logo.tsx:98,101), el pie, el JSON-LD, la pantalla de error global y el aviso de marcas. Son 18 ficheros que cambian sin tocarlos.

## La tarea
Renombrar la marca visible del producto de "VALLE Design"/"VALLE" a "VALLECAD" en TODA la superficie que un cliente ve, en un solo commit coherente.

A) Fuente de verdad (packages/contracts/src/brand.ts)
   - Línea 81: `design: "VALLE Design",` → `design: "VALLECAD",`
   - Línea 88: `brandName: "VALLE",` → `brandName: "VALLECAD",`
   - NO tocar `legalEntityName` ("Sergio Valle Enterprise Software": razón social, otra cosa) ni `founderName` ni `trademarkStatus` (sigue "unregistered", sin símbolo).

B) Los dos sitios de apps/web que SE SALTAN el manifiesto y que hoy dejarían el renombre a medias en los entregables (PDF y DXF):
   - apps/web/src/components/cad/CadStudioHost.tsx:108,110 → sustituir los literales `"Valle Design"` por `BRAND.brandName` y `PRODUCT_LABEL.design` (el fichero ya importa BRAND).
   - apps/web/src/components/cad/editor/Layout3DEditor.tsx:1056,1058 (DEFAULT_BRANDING) → es el fallback del kernel CAD desacoplado, que no puede importar @/config/brand; dejarlo neutro (`brandName: ""`, `productLabel: ""`) o marcarlo con un comentario de que sólo aplica cuando se monta sin plataforma. No dejar "Valle Design" ahí.

C) Coherencia (no son tests, son documentación que dice espejar el default):
   - apps/web/src/config/production-readiness.spec.ts:15 (`brandName`) y :19 (`productNames`) → VALLECAD. Aviso: es inerte, assessProductionBrandConfig no lee esos campos.
   - .env.example:222 y :224, y apps/web/.env.example:19 y :21 → VALLECAD.

D) Activo de marca con texto incrustado:
   - apps/web/public/brand/wordmark-claro.svg y wordmark-oscuro.svg / lockup-*.svg llevan `<text>Valle Design</text>` literal. Regenerarlos con `node scripts/brand/build-brand-assets.mjs` (o editar el texto) y confirmar que el generador toma el nombre del manifiesto; si no lo toma, ese es un bug aparte y hay que anotarlo.

E) Fuera de alcance de ESTE commit, pero hay que dejarlo escrito en el mensaje del commit como deuda inmediata (si no, el producto habla con dos nombres): apps/api/src/modules/outbox-receiver/email-templates.ts:38 (`PRODUCT_NAME`, con sus specs de asunto), apps/api/src/modules/identity/identity.controller.ts:57 (emisor MFA), apps/api/src/modules/commercial/cfdi-issuance.service.ts:173-174 (concepto del CFDI), más LICENSE y NOTICE.

F) Despliegue: comprobar las variables de repo de GitHub `RELEASE_BRAND_NAME` y `RELEASE_BRAND_PRODUCT_NAME_DESIGN` (.github/workflows/release.yml:169,185 → ARG de apps/web/Dockerfile:90,106). Si llevan el nombre viejo, sobrescriben el default y la imagen publicada NO se renombra: vaciarlas o ponerlas a VALLECAD.

TERMINADO CUANDO:
   1. `npm run typecheck` en verde.
   2. `npm --prefix apps/web run test:specs` en verde  (OJO: `npm run test:specs` NO existe en la raíz).
   3. `npm run check:surface` en verde (el gate prohíbe "AutoCAD"/"Autodesk" en la superficie pública; "VALLECAD" no lo dispara, pero hay que correrlo igual).
   4. `npm run check:legal` en verde.
   5. Criterio que SÍ detecta regresión, en vez del grep tautológico:
      `git grep -n -i -F "Valle Design" -- apps/web packages/contracts` no devuelve NADA salvo comentarios históricos deliberados.
      Y `git grep -n -F '"VALLE"' -- packages/contrac

[...recortado. El fichero y la linea de arriba son la fuente: velo tu mismo.]

## Terminado cuando
`npm run typecheck && npm run test:specs && npm run check:surface` en verde, y `git grep -l 'BRAND\.\|PRODUCT_LABEL' apps/web/src` sigue dando los mismos 18 ficheros consumidores.

---
No toques dependencias, presupuestos, umbrales ni aserciones. Un commit por tarea.
Si al hacerla descubres que el hallazgo es falso, ANOTALO en la bitacora y pasa a la siguiente:
no inventes un arreglo para un problema que no existe.
