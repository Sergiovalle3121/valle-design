# P18 · Medir si la cubierta derivada tapa el plano en 2D, y taparla sólo si lo hace

**Impacto:** quita-saturacion   **Frente:** demo-con-solidos   **Esfuerzo:** una-sesion
**Fichero:** `D:\dev\valle-design\apps\web\src\components\cad\viewport\native-mass-hosts.ts`

## Que cambia para quien mira la app
La primera pantalla del demo es la vista 2D, y la cubierta que se deriva sola (color 0x78716c, entre 2500 y 2700 mm) queda justo debajo de la cámara ortográfica cenital: el plano se leería sobre una mancha gris en vez de sobre el papel cuadriculado.

## Que hacer
TÍTULO: Gatear por modo de vista la visibilidad de las masas derivadas (piso, cielorraso, cubierta), sin tocar los muros

POR QUÉ: `CadArchitecturalMassHost` construye piso, cielorraso y cubierta en cuanto los muros cierran un anillo exterior (`room-solid-host.ts:116-130`), la cubierta es opaca y gris (`room-solid-three.ts:25`, `roof: 0x78716c`) y queda entre 2500 y 2700 mm — justo bajo la cámara cuando el 2D la aparca cenital (`Layout3DEditor.tsx:11679`). Nadie la oculta por modo: el único interruptor sobre el grupo nativo es de CAPA (`Layout3DEditor.tsx:2088`, `L.equipment`), y `applyViewMode` sólo mueve la cámara. Hoy no se ve en `/demo` porque el documento de arranque no tiene ni una entidad `wall` (`template-document.ts:117-228`: el asset «Muro perimetral» es `kind: "room"` y sale como polilínea) — pero cualquier dibujo CON muros ya tapa su propia planta, y el demo tendrá muros en cuanto aterrice la tarea del volumen.

DÓNDE:
- `D:\dev\valle-design\apps\web\src\components\cad\viewport\native-mass-hosts.ts` (el cambio)
- `D:\dev\valle-design\apps\web\src\components\cad\viewport\native-mass-hosts.spec.ts` (la medición; aserciones NUEVAS, sin tocar ninguna de las existentes)
- `D:\dev\valle-design\apps\web\src\components\cad\editor\Layout3DEditor.tsx:5964` (UNA línea)

QUÉ HACER:
1. `CadNativeMassHosts` acepta un segundo parámetro OPCIONAL `viewController?: () => CadSolidViewControllerLike | null` — el MISMO tipo y la misma forma de thunk que `CadSolidShadeHost` ya recibe (`solid-shade-host.ts:150-156`), leído de la MISMA fuente.
2. Suscripción PEREZOSA, copiando `ensureSubscribed` (`solid-shade-host.ts:168-175`): en la línea 5964 del monolito `viewControllerRef.current` todavía es `null` (el controlador nace en la 6051), así que suscribirse en el constructor no engancha nada. Al primer controlador disponible, `controller.onChange(...)` reaplica la visibilidad; guardar el `unsubscribe` y llamarlo en `dispose()`.
3. El gate vive EN LA FACHADA, no dentro de `CadArchitecturalMassHost.sync`: ese `sync` retorna temprano cuando la lista de muros no cambió por referencia (`room-solid-host.ts:111`, `sameWalls`), y un cambio de modo sin cambio de documento se perdería ahí. Un método privado `applyModeVisibility()` que hace `this.masses.group.visible = controller?.mode !== "2d"`, invocado desde `sync`, desde `ensureSubscribed` y desde el `onChange`.
4. Sin controlador (Node, previsualización de trazado): `visible = true`, el comportamiento de hoy.
5. Los muros NO se tocan: `this.walls.group.visible` queda intacto — un muro sólido visto desde arriba es su propia planta.
6. Monolito: sólo la línea 5964 pasa a `new CadNativeMassHosts(sceneViewport, () => viewControllerRef.current)`. Una línea, dentro del presupuesto.

TERMINADO CUANDO, en `native-mass-hosts.spec.ts`, con los 4 `shellWalls` de altura 2400 que la spec ya construye y un controlador falso de cinco funciones (basta con `mode`, `onChange` y el resto en stub, como permite `CadSolidViewControllerLike`):
a) con el falso en `mode: "2d"` y tras `sync`: `massesGroup.visible === false`, `wallsGroup.visible === true`, y `massesGroup.children.length === 3` (piso, cielorraso y cubierta siguen construidos — se ocultan, no se destruyen);
b) cambiando el falso a `mode: "3d"` y disparando su `onChange`: `massesGroup.visible === true`, y `getSnapshot().meshCount` idéntico al que da el mismo documento sin segundo parámetro (la escena 3D no pierde ni una malla);
c) construyendo SIN segundo parámetro: `massesGroup.visible === true` (nada cambia para quien no pasa controlador).
Nota para quien lo implemente: `getSnapshot()` recorre `this.group` con `traverse`, que NO salta objetos invisibles, así que `data-mesh-count` sigue contando las masas también en 2D. Es correcto y no hay que «arreglarlo»: lo que se mide en (b) es que el recuento no cae.

QUÉ NO HACER: nada en `/demo` ni en Playwright, y no crear `demo-volumen.spec.ts` (no existe). El muestreo de píxel propuesto no mide nada hoy: sin entidades `wall` en el documento de arranque no hay cubierta que muestrear y el centro de la huella saldría papel por ausencia de geometría. Cuando la tarea del volumen meta muros o sólidos en el demo, este gate ya estará puesto y probado en Node. Tampoco tocar dependencias, presupuestos, umbrales ni aserciones existentes.

## Terminado cuando (medible, no «se ve mejor»)
En `demo-volumen.spec.ts`, en vista 2D el píxel del centro de la huella coincide con el fondo del papel de la retícula y no con 0x78716c (±2 por canal), y en 3D `data-mesh-count` sigue ≥ 12 — o, si la medición demuestra que no había problema, el valor muestreado queda registrado en la cabecera de la spec y la tarea se cierra sin cambio de producto.

---
Un commit. Respeta el sistema de diseno y su gate (relleno y tinta son dos tokens; `--primary` no es relleno de boton). Comprobacion EJECUTADA antes de marcar hecho. No toques dependencias, presupuestos, umbrales, aserciones ajenas ni artefactos de evidencia.
