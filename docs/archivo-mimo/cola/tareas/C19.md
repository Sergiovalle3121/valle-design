# C19 · WALL nace a la cota del plano de trabajo

**Frente:** transformar-visualizar   **Esfuerzo:** una-sesion
**Fichero:** `apps/web/src/lib/cad/engine/commands/draw-wall.ts:76`

## Por que importa al arquitecto
Todo muro del producto nace con z:0 escrito a mano en tres sitios; dibujar la planta alta obliga a dibujarla en la baja y subirla después, y STAIR ya resolvió esto mismo bien.

## Que hacer
TÍTULO: WALL nace a la cota del plano de trabajo (y el motor deja de negárselo)

QUÉ HACER (dos ficheros de producto, un commit):
1. `apps/web/src/lib/cad/engine/commands/draw-wall.ts`: importar `cadLiftPoint` de `../spatial-point` y sustituir en `wallEntity` (:76-77)
   `start: { x: a.x, y: a.y, z: 0 }` / `end: { x: b.x, y: b.y, z: 0 }`
   por `start: cadLiftPoint(a, state.points[0])` / `end: cadLiftPoint(b, state.points[0])`.
   El primer vértice designado como `fallback` es deliberado y es el patrón de `draw-basics.ts:243`: un tramo encadenado cuyo punto llegue sin z se queda en el plano de la corrida, no cae al suelo a mitad de muro.
2. Declarar `spatial: "elevation"` en el DESCRIPTOR `wallCommand` de `draw-wall.ts` (junto a `mutates: true`, ~línea 167), NO en `command-manifest.ts`: el manifiesto lo genera `scripts/cad/build-command-manifest.mjs` (lee `d.spatial`, líneas 90 y 145). Regenerar con `npm run build:command-manifest` y dejar el diff generado en el commit.
   Es "elevation" y no `true` a propósito: la receta del muro es un eje en planta más altura, así que honra un SCU LLANO elevado, y sobre un SCU INCLINADO el motor debe seguir negándose (`command-engine.ts:443-447`).
3. NO tocar los cuatro `z: 0` de `wallPreview` (:89-90, :98-99). `wallFootprint` (`wall-geometry.ts:34-47`) ignora la z y `CadPreviewPath.points` es `CadPoint2[]` (`command-types.ts:154-157`): cambiarlos es un no-op que ningún spec puede comprobar. Si se cambian por coherencia de lectura, no cuentan como parte del terminado.

TERMINADO CUANDO las tres cosas pasan:
a) En `apps/web/src/lib/cad/engine/commands/draw-wall.spec.ts`, dos casos nuevos con el `descriptor` del registro real que ya usa el fichero: uno conduce `point(0,0,3000)` y `point(3000,0,3000)` + `enter` y afirma `wall.start.z === 3000 && wall.end.z === 3000`; otro encadena un tercer punto SIN z y afirma que ese tramo sigue a 3000 (la corrida no se desploma a mitad). Hay que extender el helper `point(x, y)` (:55) con un tercer argumento opcional, como hace `architecture-stair.spec.ts` (ver :243 `point(1000, 2000, 300)` y :254).
b) EL CASO QUE PRUEBA QUE EL MOTOR YA NO SE NIEGA — es el que no se puede falsear: en `apps/web/src/lib/cad/ucs-3d.spec.ts`, junto al bloque de :453-473 que ya eleva el SCU a +3000 con UCS y comprueba CIRCLE y ARC, añadir `teclea(s, ["WALL", "0,0", "3000,0", { kind: "enter" }])` y afirmar que el documento creció en una entidad `wall` con `start.z` y `end.z` a 3000 ± TOLERANCIA_MM. Con `spatial` olvidado este caso falla (no se escribe nada y el motor emite el mensaje de negativa), que es justo lo que el criterio original no detectaba.
c) `npm run check:command-manifest` pasa sin diferencias (queda `spatial: "elevation"` en la entrada WALL, regenerado, no escrito a mano), y `npm run check:command-integrity` sigue en verde.

PROHIBIDO en este commit: tocar umbrales, presupuestos, dependencias ni aserciones existentes. Los `assert.deepEqual(wall.start, { x: 0, y: 0, z: 0 })` que ya están (:81-82, :112, :141-142) deben seguir pasando TAL CUAL — son la compatibilidad, y si alguno estorba es señal de que el `fallback` está mal puesto, no de que la aserción sobre.

FUERA DE ALCANCE (anotar, no hacer): DOOR y WINDOW (`command-manifest.ts:108-109`) siguen sin `spatial`; sus vanos cuelgan del muro por `sill` (`wall-openings.ts:144-160`), así que heredan la cota del anfitrión, pero designar el vano sobre el SCU elevado seguirá rechazándose. Es otra tarea.

## Terminado cuando
Spec en apps/web/src/lib/cad/engine/commands/draw-wall.spec.ts que conduce WALL con puntos que traen z=3000 y comprueba que la entidad emitida tiene start.z=end.z=3000; y un segundo caso con puntos sin z que sigue dando 0 (compatibilidad). `npm run check:command-manifest` pasa sin diferencias.

---
Un commit. Comprobacion EJECUTADA antes de marcar hecho: el spec conduce el comando REAL contra el motor real; un spec que se prueba a si mismo, que hace grep del fuente, o cuyo arnes puede pasar sin el arreglo, se considera falso. No toques dependencias, presupuestos, umbrales, aserciones ajenas ni artefactos de evidencia de docs/cad/evidence/ o docs/dwg/.
