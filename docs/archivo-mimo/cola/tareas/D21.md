# D21 · F2 — El wordmark y los 7 SVG de marca, con un gate que no está enchufado

**Gravedad:** se-ve-precario   **Esfuerzo:** minutos
**Fichero:** `apps/web/src/components/brand/logo-geometry.ts:75`

## El problema
`export const WORDMARK = "Valle Design"` alimenta los 7 SVG de apps/web/public/brand/ (18 ocurrencias del nombre dentro de `<title>`, `aria-label` y `<text>`), generados por scripts/brand/build-brand-assets.mjs. HALLAZGO: ese generador tiene modo `--check` y la documentación (docs/design/BRAND.md:25, AGENTS.md:189, docs/design/DESIGN_SYSTEM.md:341) afirma que es un gate, pero `grep -n brand package.json` no devuelve NADA y no aparece en .github/workflows/ci.yml. Los SVG pueden quedarse con el nombre viejo y nadie se entera.

## La tarea
Enchufar el gate de marca que ya existe. NO tocar `WORDMARK` ni regenerar ningún SVG.

Sobre `main`, editar únicamente `package.json`:

1. Añadir el par de scripts, siguiendo el patrón literal del resto de generadores del repo (`build:X` / `check:X`), junto a `check:api-console`:

   "build:brand-assets": "node scripts/brand/build-brand-assets.mjs",
   "check:brand-assets": "node scripts/brand/build-brand-assets.mjs --check",

2. Encadenar `&& npm run check:brand-assets` al final de la cadena de `check:cad` (la misma línea que ya termina en `&& npm run check:api-console`). No hace falta tocar `.github/workflows/ci.yml`: CI ya ejecuta `npm run check:cad` en `.github/workflows/ci.yml:238`, así que con eso el gate queda cubierto.

Nada más. No cambiar `WORDMARK`, no ejecutar el generador en modo escritura, no commitear SVG: `node scripts/brand/build-brand-assets.mjs --check` ya devuelve verde hoy ("Marca OK: 7 archivos coinciden con la geometría única.", exit 0), así que este commit es sólo cableado y no debe traer ni un byte de diff en `apps/web/public/brand/`.

TERMINADO CUANDO:
- `npm run check:brand-assets` sale en verde (exit 0).
- `grep -n brand package.json` ya devuelve las tres líneas (build:brand-assets, check:brand-assets y la aparición dentro de check:cad).
- `git status --porcelain apps/web/public/brand apps/web/src/components/brand` está VACÍO (prueba de que no se coló ninguna regeneración).
- `npm run check:cad` sigue en verde.

NOTA para quien lo haga (no es parte de la tarea, no arreglarlo aquí): el `--check` sólo compara los 7 SVG byte a byte; de `apps/web/src/app/favicon.ico` únicamente verifica que EXISTA, no sus bytes — está justificado y documentado en el propio generador (dos versiones de `sharp` comprimen el mismo PNG distinto). Y si de verdad se quiere renombrar el producto a "VALLECAD", eso es un trabajo aparte que abarca los 233 ficheros que hoy dicen "Valle Design" y que además exige recalcular los `viewBox`/`width` codificados a mano en `wordmark()` y `lockup()`; no cabe en este commit.

## Terminado cuando
`npm run check:brand-assets` en verde y `git grep -c 'Valle Design' apps/web/public/brand` devuelve 0.

---
No toques dependencias, presupuestos, umbrales ni aserciones. Un commit por tarea.
Si al hacerla descubres que el hallazgo es falso, ANOTALO en la bitacora y pasa a la siguiente:
no inventes un arreglo para un problema que no existe.
